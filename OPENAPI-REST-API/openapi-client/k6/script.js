/*
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * OpenAPI spec version: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://github.com/OpenAPITools/openapi-generator
 *
 * Generator version: 7.21.0
 */


import http from "k6/http";
import { group, check, sleep } from "k6";

const BASE_URL = "https://my.interserver.net/apiv2";
// Sleep duration between successive requests.
// You might want to edit the value of this variable or remove calls to the sleep function on the script.
const SLEEP_DURATION = 0.1;
// Global variables should be initialized.
let sessionid = "TODO_EDIT_THE_SESSIONID";
let X_API_KEY = "TODO_EDIT_THE_X-API-KEY";

export default function() {
    group("/qs/{id}/traffic_usage", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getQsTrafficUsage
        {
            let url = BASE_URL + `/qs/${id}/traffic_usage`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Get QuickServer Traffic usage": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postQsTrafficUsage
        {
            let url = BASE_URL + `/qs/${id}/traffic_usage`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Submit QuickServer Traffic Usage": (r) => r.status === 200
            });
        }
    });

    group("/mail/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getMailInfo
        {
            let url = BASE_URL + `/mail/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Mail Information.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: updateMailInfo
        {
            let url = BASE_URL + `/mail/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: mailCancel
        {
            let url = BASE_URL + `/mail/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "Mail Cancel": (r) => r.status === 200
            });
        }
    });

    group("/affiliate/traffic_graph", () => {
        let days = 'TODO_EDIT_THE_DAYS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getAffiliateTrafficGraph
        {
            let url = BASE_URL + `/affiliate/traffic_graph?days=${days}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Affiliate traffic graph data broken down by time period.": (r) => r.status === 200
            });
        }
    });

    group("/ssl/{id}/invoices", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getSslInvoices
        {
            let url = BASE_URL + `/ssl/${id}/invoices`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Get Invoices response": (r) => r.status === 200
            });
        }
    });

    group("/billing/invoices/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getBillingInvoice
        {
            let url = BASE_URL + `/billing/invoices/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Detailed invoice payload for the requested invoice.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: deleteBillingInvoice
        {
            let url = BASE_URL + `/billing/invoices/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/affiliate/web_traffic", () => {

        // Request No. 1: getAffiliateWebTraffic
        {
            let url = BASE_URL + `/affiliate/web_traffic`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "The recent affiliate web traffic": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/disable_quota", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: doVpsDisableQuota
        {
            let url = BASE_URL + `/vps/${id}/disable_quota`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/vps/order", () => {

        // Request No. 1: getNewVps
        {
            let url = BASE_URL + `/vps/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "VPS Order information": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: putVps
        {
            let url = BASE_URL + `/vps/order`;
            // TODO: edit the parameters of the request body.
            let body = {"osDistro": "string", "slices": "integer", "vpsPlatform": "string", "controlpanel": "string", "period": "integer", "location": "integer", "osVersion": "string", "hostname": "string", "coupon": "string", "rootpass": "string", "comment": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Validate VPS order response.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: addVps
        {
            let url = BASE_URL + `/vps/order`;
            // TODO: edit the parameters of the request body.
            let body = {"osDistro": "string", "slices": "integer", "vpsPlatform": "string", "controlpanel": "string", "period": "integer", "location": "integer", "osVersion": "string", "hostname": "string", "coupon": "string", "rootpass": "string", "comment": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/account/creditcards/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: updateAccountCreditCard
        {
            let url = BASE_URL + `/account/creditcards/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: deleteAccountCreditCard
        {
            let url = BASE_URL + `/account/creditcards/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/servers", () => {

        // Request No. 1: getServerList
        {
            let url = BASE_URL + `/servers`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "The listing of `Servers` services on your account.": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/invoices", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getVpsInvoices
        {
            let url = BASE_URL + `/vps/${id}/invoices`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Get VPS Invoices response": (r) => r.status === 200
            });
        }
    });

    group("/affiliate/sales_graph", () => {
        let days = 'TODO_EDIT_THE_DAYS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getAffiliateSalesGraph
        {
            let url = BASE_URL + `/affiliate/sales_graph?days=${days}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Affiliate sales graph data broken down by time period.": (r) => r.status === 200
            });
        }
    });

    group("/domains/order/{domain}/{regType}", () => {
        let domain = 'TODO_EDIT_THE_DOMAIN'; // specify value as there is no example value for this parameter in OpenAPI spec
        let regType = 'TODO_EDIT_THE_REGTYPE'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getDomainOrderFields
        {
            let url = BASE_URL + `/domains/order/${domain}/${regType}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Registration-type-specific form field definitions.": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getQsInfo
        {
            let url = BASE_URL + `/qs/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Quickserver details": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: updateQsInfo
        {
            let url = BASE_URL + `/qs/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: quickserversCancel
        {
            let url = BASE_URL + `/qs/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "Rapid Deploy Servers Cancel": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/disable_quota", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: doQsDisableQuota
        {
            let url = BASE_URL + `/qs/${id}/disable_quota`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/mail/{id}/stats", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let time = 'TODO_EDIT_THE_TIME'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getStats
        {
            let url = BASE_URL + `/mail/${id}/stats?time=${time}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Mail service usage statistics.": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/start", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: doQsStart
        {
            let url = BASE_URL + `/qs/${id}/start`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/captcha", () => {

        // Request No. 1: getCaptcha
        {
            let url = BASE_URL + `/captcha`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "An array containing a `captcha` field containing a string with a base64 encoded captcha image.": (r) => r.status === 200
            });
        }
    });

    group("/floating_ips/{id}/change_ip", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: postFloatingIpsChangeIp
        {
            let url = BASE_URL + `/floating_ips/${id}/change_ip`;
            // TODO: edit the parameters of the request body.
            let body = {"ip": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/ssl/order", () => {

        // Request No. 1: getNewSsl
        {
            let url = BASE_URL + `/ssl/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: putSsl
        {
            let url = BASE_URL + `/ssl/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.put(url, params);

            check(request, {
                "Validate SSL Order response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: addSsl
        {
            let url = BASE_URL + `/ssl/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/dns/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getDnsDomain
        {
            let url = BASE_URL + `/dns/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "The DNS records for the specified domain.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: addDnsRecord
        {
            let url = BASE_URL + `/dns/${id}`;
            // TODO: edit the parameters of the request body.
            let body = {"name": "string", "type": "dnsrecordtype", "content": "string", "ttl": "integer", "prio": "integer"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Add DNS Domain Response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: deleteDnsDomain
        {
            let url = BASE_URL + `/dns/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/backups", () => {
        let all = 'TODO_EDIT_THE_ALL'; // specify value as there is no example value for this parameter in OpenAPI spec
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getQsBackups
        {
            let url = BASE_URL + `/qs/${id}/backups?all=${all}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "The listing of available backups for the QuickServer.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: deleteQsBackup
        {
            let url = BASE_URL + `/qs/${id}/backups?all=${all}&file=${file}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: downloadQsBackup
        {
            let url = BASE_URL + `/qs/${id}/backups?all=${all}`;
            // TODO: edit the parameters of the request body.
            let body = {"file": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.patch(url, JSON.stringify(body), params);

            check(request, {
                "Download URL for the backup file.": (r) => r.status === 200
            });
        }
    });

    group("/buy_now_servers_list", () => {

        // Request No. 1: getMPServers
        {
            let url = BASE_URL + `/buy_now_servers_list`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Marketplace Buy it now servers list": (r) => r.status === 200
            });
        }
    });

    group("/websites/{id}/reverse_dns", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: gettWebsiteReverseDns
        {
            let url = BASE_URL + `/websites/${id}/reverse_dns`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "List of reverse dns entries": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postWebsitesReverseDns
        {
            let url = BASE_URL + `/websites/${id}/reverse_dns`;
            // TODO: edit the parameters of the request body.
            let body = {"ips": "map"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Response from the update reverse DNS call.": (r) => r.status === 200
            });
        }
    });

    group("/backups", () => {

        // Request No. 1: getBackupsList
        {
            let url = BASE_URL + `/backups`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "The listing of backup storage services on your account.": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/buy_ip", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getVpsBuyIp
        {
            let url = BASE_URL + `/vps/${id}/buy_ip`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "VPS Buy IP information": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postVpsBuyIp
        {
            let url = BASE_URL + `/vps/${id}/buy_ip`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Submit VPS Buy IP form response": (r) => r.status === 200
            });
        }
    });

    group("/floating_ips/{id}/invoices", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getFloatingIpInvoices
        {
            let url = BASE_URL + `/floating_ips/${id}/invoices`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Get Invoices response": (r) => r.status === 200
            });
        }
    });

    group("/billing/creditcards", () => {

        // Request No. 1: addBillingCreditCard
        {
            let url = BASE_URL + `/billing/creditcards`;
            // TODO: edit the parameters of the request body.
            let body = {"name": "string", "address": "string", "city": "string", "state": "string", "country": "string", "zip": "string", "cc": "string", "ccExp": "string", "ccCcv2": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/domains/{id}/welcome_email", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getDomainsWelcomeEmail
        {
            let url = BASE_URL + `/domains/${id}/welcome_email`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/mail/order", () => {

        // Request No. 1: getNewMail
        {
            let url = BASE_URL + `/mail/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Mail ordering information.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: putMail
        {
            let url = BASE_URL + `/mail/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.put(url, params);

            check(request, {
                "Validate Mail order response.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: addMail
        {
            let url = BASE_URL + `/mail/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/licenses/{id}/welcome_email", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getLicensesWelcomeEmail
        {
            let url = BASE_URL + `/licenses/${id}/welcome_email`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/servers/{id}/bandwidth_graph", () => {
    });

    group("/account/iplimits", () => {

        // Request No. 1: updateAccountIpLimits
        {
            let url = BASE_URL + `/account/iplimits`;
            let body = {"start": "1.2.3.0", "end": "1.2.3.255"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: deleteIpLimit
        {
            let url = BASE_URL + `/account/iplimits`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.patch(url, params);

            check(request, {
                "IP Range removed.": (r) => r.status === 200
            });
        }
    });

    group("/tickets/{id}/update", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: updateTicketInfo
        {
            let url = BASE_URL + `/tickets/${id}/update`;
            // TODO: edit the parameters of the request body.
            let body = {"ip": "string", "ipAddress": "string", "customerServerAccess": "string", "rootPassword": "string", "sudoUsername": "string", "sudoPassword": "string", "port": "integer"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Response from updating a support ticket.": (r) => r.status === 200
            });
            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/change_webuzo_password", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getQsChangeWebuzoPassword
        {
            let url = BASE_URL + `/qs/${id}/change_webuzo_password`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "QuickServer Change Webuzo Password info response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postQsChangeWebuzoPassword
        {
            let url = BASE_URL + `/qs/${id}/change_webuzo_password`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/billing/prepays", () => {

        // Request No. 1: getBillingPrePays
        {
            let url = BASE_URL + `/billing/prepays`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: addBillingPrepay
        {
            let url = BASE_URL + `/billing/prepays`;
            // TODO: edit the parameters of the request body.
            let body = {"module": "string", "amount": "bigdecimal", "automaticUse": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/scrub_ips/order", () => {

        // Request No. 1: getOrderDetail
        {
            let url = BASE_URL + `/scrub_ips/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Order details": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: placeScrubOrder
        {
            let url = BASE_URL + `/scrub_ips/order`;
            // TODO: edit the parameters of the request body.
            let body = {"serviceType": "integer", "ip": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Request OK": (r) => r.status === 201
            });
        }
    });

    group("/domains/lookup/{name}", () => {
        let name = 'TODO_EDIT_THE_NAME'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getDomainLookup
        {
            let url = BASE_URL + `/domains/lookup/${name}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Availability, pricing, and field metadata for the requested domain.": (r) => r.status === 200
            });
        }
    });

    group("/mail/{id}/deliverability", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getMailDeliverability
        {
            let url = BASE_URL + `/mail/${id}/deliverability`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Deliverability metrics for the mail service.": (r) => r.status === 200
            });
        }
    });

    group("/servers/{id}/ipmi_live", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: serverIpmiLiveGet
        {
            let url = BASE_URL + `/servers/${id}/ipmi_live`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response from the Servers IPMI Live information request.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: serverIpmiLivePost
        {
            let url = BASE_URL + `/servers/${id}/ipmi_live`;
            // TODO: edit the parameters of the request body.
            let body = {"asset": "integer", "ip": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Response from the Servers IPMI Live information request.": (r) => r.status === 200
            });
        }
    });

    group("/signup", () => {

        // Request No. 1: submitSignup
        {
            let url = BASE_URL + `/signup`;
            let body = {"login": "user@domain.com", "passwd": "mypassword", "remember": "true", "g-recaptcha-response": {"__v_isShallow":false,"dep":{"w":0,"n":0},"__v_isRef":true,"_rawValue":"zzzzz","_value":"zzzzz"}};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Account created successfully.": (r) => r.status === 200
            });
        }
    });

    group("/vps", () => {

        // Request No. 1: getVpsList
        {
            let url = BASE_URL + `/vps`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "The listing of `Vps` services on your account.": (r) => r.status === 200
            });
        }
    });

    group("/websites/{id}/login", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getWebsitesLogin
        {
            let url = BASE_URL + `/websites/${id}/login`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Get Website Login response": (r) => r.status === 200
            });
        }
    });

    group("/domains/{id}/dnssec", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getDomainDnssec
        {
            let url = BASE_URL + `/domains/${id}/dnssec`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "DNSSEC records currently applied to the domain.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: addDomainDnssec
        {
            let url = BASE_URL + `/domains/${id}/dnssec`;
            // TODO: edit the parameters of the request body.
            let body = {"algorithm": "list", "digestType": "list", "digest": "list", "keyTag": "list"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: deleteDomainDnssec
        {
            let url = BASE_URL + `/domains/${id}/dnssec?action=${action}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/licenses/order/{catTag}", () => {
        let catTag = 'TODO_EDIT_THE_CATTAG'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getLicenseOrderCatTagInfo
        {
            let url = BASE_URL + `/licenses/order/${catTag}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "License types and pricing for the specified category.": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/change_timezone", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getQsChangeTimezone
        {
            let url = BASE_URL + `/qs/${id}/change_timezone`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "QuickServer Change Timezone info response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postQsChangeTimezone
        {
            let url = BASE_URL + `/qs/${id}/change_timezone`;
            // TODO: edit the parameters of the request body.
            let body = {"timezone": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/licenses/{id}/change_ip", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: postLicenseChangeIp
        {
            let url = BASE_URL + `/licenses/${id}/change_ip`;
            // TODO: edit the parameters of the request body.
            let body = {"ip": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/account/countries", () => {
        let fetch_by = 'TODO_EDIT_THE_FETCH_BY'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getCountries
        {
            let url = BASE_URL + `/account/countries?fetch_by=${fetch_by}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "The list of countries with both 2 letter abbreviations and the country names.": (r) => r.status === 200
            });
        }
    });

    group("/ssl/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getSslInfo
        {
            let url = BASE_URL + `/ssl/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: updateSslInfo
        {
            let url = BASE_URL + `/ssl/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: sslCancel
        {
            let url = BASE_URL + `/ssl/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "SSL Cancel": (r) => r.status === 200
            });
        }
    });

    group("/account/sshkey", () => {

        // Request No. 1: updateAccountSshKey
        {
            let url = BASE_URL + `/account/sshkey`;
            // TODO: edit the parameters of the request body.
            let body = {"sshKey": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/change_root_password", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getQsChangeRootPassword
        {
            let url = BASE_URL + `/qs/${id}/change_root_password`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "QuickServer Change Root Password response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postQsChangeRootPassword
        {
            let url = BASE_URL + `/qs/${id}/change_root_password`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/dns", () => {

        // Request No. 1: getDnsList
        {
            let url = BASE_URL + `/dns`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Listing of DNS domains on the account with their primary A record.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: addDnsDomain
        {
            let url = BASE_URL + `/dns`;
            let body = {"domain": "mydomain.com", "ip": "1.2.3.4"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/tickets/{id}/close", () => {
        let id = ['1511222'].shift(); // first element from list extracted from 'examples' field defined at the parameter level of OpenAPI spec

        // Request No. 1: CloseTicket
        {
            let url = BASE_URL + `/tickets/${id}/close`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Close ticket.": (r) => r.status === 200
            });
        }
    });

    group("/qs", () => {

        // Request No. 1: getQsList
        {
            let url = BASE_URL + `/qs`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "The listing of `Quickservers` services on your account.": (r) => r.status === 200
            });
        }
    });

    group("/billing/creditcards/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: updateBillingCreditCard
        {
            let url = BASE_URL + `/billing/creditcards/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: deleteBillingCreditCard
        {
            let url = BASE_URL + `/billing/creditcards/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/invoices", () => {
        let searchString = 'TODO_EDIT_THE_SEARCHSTRING'; // specify value as there is no example value for this parameter in OpenAPI spec
        let limit = 'TODO_EDIT_THE_LIMIT'; // specify value as there is no example value for this parameter in OpenAPI spec
        let skip = 'TODO_EDIT_THE_SKIP'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getInvoices
        {
            let url = BASE_URL + `/invoices?searchString=${searchString}&skip=${skip}&limit=${limit}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "search results matching criteria": (r) => r.status === 200
            });
        }
    });

    group("/mail/{id}/blocks", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getMailBlocks
        {
            let url = BASE_URL + `/mail/${id}/blocks`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/scrub_ips/{id}/disable", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: disableScrub
        {
            let url = BASE_URL + `/scrub_ips/${id}/disable`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Request OK": (r) => r.status === 200
            });
        }
    });

    group("/scrub_ips/{id}/logs", () => {
        let id = ['413232  '].shift(); // first element from list extracted from 'examples' field defined at the parameter level of OpenAPI spec

        // Request No. 1: getScrubIpLogs
        {
            let url = BASE_URL + `/scrub_ips/${id}/logs`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Scrub Ips logs": (r) => r.status === 200
            });
        }
    });

    group("/tickets", () => {
        let period = 'TODO_EDIT_THE_PERIOD'; // specify value as there is no example value for this parameter in OpenAPI spec
        let view = 'TODO_EDIT_THE_VIEW'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getTicketsList
        {
            let url = BASE_URL + `/tickets?page=${page}&period=${period}&view=${view}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "The listing of support tickets.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postTicketsList
        {
            let url = BASE_URL + `/tickets`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Search through the ticket system for a given email, subject, or ticket mask id.": (r) => r.status === 200
            });
        }
    });

    group("/logout", () => {

        // Request No. 1: Logout
        {
            let url = BASE_URL + `/logout`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/mail/{id}/reset_password", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: resetMailPassword
        {
            let url = BASE_URL + `/mail/${id}/reset_password`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/search", () => {

        // Request No. 1: getSearch
        {
            let url = BASE_URL + `/search`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Search autocomplete results for the account.": (r) => r.status === 200
            });
        }
    });

    group("/domains/{id}/renew", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getDomainRenewal
        {
            let url = BASE_URL + `/domains/${id}/renew`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postDomainRenewal
        {
            let url = BASE_URL + `/domains/${id}/renew`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/domains/{id}/invoices", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getDomainInvoices
        {
            let url = BASE_URL + `/domains/${id}/invoices`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Get Invoices response": (r) => r.status === 200
            });
        }
    });

    group("/tickets/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getTicketInfo
        {
            let url = BASE_URL + `/tickets/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Ticket Information": (r) => r.status === 200
            });
            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: putTicketInfo
        {
            let url = BASE_URL + `/tickets/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.put(url, params);

            check(request, {
                "Ticket Information": (r) => r.status === 200
            });
            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: postTicketInfo
        {
            let url = BASE_URL + `/tickets/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Ticket Information": (r) => r.status === 200
            });
            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 4: deleteTicketInfo
        {
            let url = BASE_URL + `/tickets/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "Ticket Information": (r) => r.status === 200
            });
            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/backups/{id}/invoices", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getBackupInvoices
        {
            let url = BASE_URL + `/backups/${id}/invoices`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Get Invoices response": (r) => r.status === 200
            });
        }
    });

    group("/mail", () => {

        // Request No. 1: getMailList
        {
            let url = BASE_URL + `/mail`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "The listing of `Mail` services on your account.": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/backup", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: postQsBackup
        {
            let url = BASE_URL + `/qs/${id}/backup`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/scrub_ips/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getScrubIpDetails
        {
            let url = BASE_URL + `/scrub_ips/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Scrub IP service details including firewall rules and filters.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: cancelScrubIp
        {
            let url = BASE_URL + `/scrub_ips/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "Request OK": (r) => r.status === 200
            });
        }
    });

    group("/scrub_ips/{id}/create_filter", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: createFilter
        {
            let url = BASE_URL + `/scrub_ips/${id}/create_filter`;
            // TODO: edit the parameters of the request body.
            let body = {"filterType": "string", "port": "integer"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Request OK": (r) => r.status === 201
            });
        }
    });

    group("/websites", () => {

        // Request No. 1: getWebsiteList
        {
            let url = BASE_URL + `/websites`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "The listing of `Websites` services on your account.": (r) => r.status === 200
            });
        }
    });

    group("/qs/order", () => {

        // Request No. 1: getNewQs
        {
            let url = BASE_URL + `/qs/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Quickserver Ordering information.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: putQs
        {
            let url = BASE_URL + `/qs/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.put(url, params);

            check(request, {
                "Validate QuickServer Order response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: addQs
        {
            let url = BASE_URL + `/qs/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/change_webuzo_password", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: postVpsChangeWebuzoPassword
        {
            let url = BASE_URL + `/vps/${id}/change_webuzo_password`;
            // TODO: edit the parameters of the request body.
            let body = {"password": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/mail/{id}/delist", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getMailDelist
        {
            let url = BASE_URL + `/mail/${id}/delist`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Blocklist entries and delist details for the mail service.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postMailDelist
        {
            let url = BASE_URL + `/mail/${id}/delist`;
            // TODO: edit the parameters of the request body.
            let body = {"unblock": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/scrub_ips", () => {

        // Request No. 1: getScrubIpsList
        {
            let url = BASE_URL + `/scrub_ips`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Scrub Ips list": (r) => r.status === 200
            });
            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/login", () => {

        // Request No. 1: getLoginInfo
        {
            let url = BASE_URL + `/login`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Various pieces of information useful for rendering a login page.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: submitLogin
        {
            let url = BASE_URL + `/login`;
            // TODO: edit the parameters of the request body.
            let body = {"login": "string", "passwd": "string", "remember": "string", "gRecaptchaResponse": {"__v_isShallow": "boolean", "dep": "loginsubmissionexample_g_recaptcha_response_dep", "__v_isRef": "boolean", "_rawValue": "string", "_value": "string"}, "tfa": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Successful login response containing the session token.": (r) => r.status === 200
            });
            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/welcome_email", () => {
        let id = ['4813'].shift(); // first element from list extracted from 'examples' field defined at the parameter level of OpenAPI spec

        // Request No. 1: getVpsWelcomeEmail
        {
            let url = BASE_URL + `/vps/${id}/welcome_email`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/domains/{id}/contact", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getDomainContact
        {
            let url = BASE_URL + `/domains/${id}/contact`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "The registrant/admin contact details for the domain.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: updateDomainContact
        {
            let url = BASE_URL + `/domains/${id}/contact`;
            // TODO: edit the parameters of the request body.
            let body = {"status": "string", "state": "string", "orgName": "string", "country": "string", "postalCode": "string", "email": "string", "fax": "string", "address2": "string", "address3": "string", "address1": "string", "city": "string", "phone": "string", "firstName": "string", "lastName": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/reverse_dns", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getQsReverseDns
        {
            let url = BASE_URL + `/qs/${id}/reverse_dns`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "QuickServer Reverse DNS info response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postQsReverseDns
        {
            let url = BASE_URL + `/qs/${id}/reverse_dns`;
            // TODO: edit the parameters of the request body.
            let body = {"ips": "map"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Update QuickServer Reverse DNS response": (r) => r.status === 200
            });
        }
    });

    group("/tickets/new", () => {

        // Request No. 1: getNewTicket
        {
            let url = BASE_URL + `/tickets/new`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "New ticket form data including departments and service categories.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: addNewTicket
        {
            let url = BASE_URL + `/tickets/new`;
            // TODO: edit the parameters of the request body.
            let body = {"subject": "string", "body": "string", "serviceId": "integer", "serviceModule": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "A successful response after creating a ticket": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getVpsInfo
        {
            let url = BASE_URL + `/vps/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "The VPS Information": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: updateVpsInfo
        {
            let url = BASE_URL + `/vps/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: VPSCancel
        {
            let url = BASE_URL + `/vps/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "VPS Cancel": (r) => r.status === 200
            });
        }
    });

    group("/mail/{id}/rules", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getRules
        {
            let url = BASE_URL + `/mail/${id}/rules`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "List of configured deny rules.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: addRule
        {
            let url = BASE_URL + `/mail/${id}/rules`;
            let body = {"user": "mb20682", "type": "email", "data": "domeinwo@server.guesshost.net"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Deny rule created successfully.": (r) => r.status === 200
            });
        }
    });

    group("/affiliate/landing_pg", () => {

        // Request No. 1: updateAffiliateLandingPage
        {
            let url = BASE_URL + `/affiliate/landing_pg`;
            // TODO: edit the parameters of the request body.
            let body = {"affiliateDockTitle": "string", "affiliateDockDescription": "string", "referrerCoupon": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Response with a text message field.": (r) => r.status === 200
            });
        }
    });

    group("/account/2fa", () => {

        // Request No. 1: getAccountTfaSetup
        {
            let url = BASE_URL + `/account/2fa`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Two-factor authentication setup data.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: updateAccountTfa
        {
            let url = BASE_URL + `/account/2fa`;
            // TODO: edit the parameters of the request body.
            let body = {"2faGoogleCode": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: deleteAccountTfa
        {
            let url = BASE_URL + `/account/2fa`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/insert_cd", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: postVpsInsertCd
        {
            let url = BASE_URL + `/vps/${id}/insert_cd`;
            // TODO: edit the parameters of the request body.
            let body = {"url": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/domains/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getDomainInfo
        {
            let url = BASE_URL + `/domains/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Domain Information.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: updateDomainInfo
        {
            let url = BASE_URL + `/domains/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: CancelDomain
        {
            let url = BASE_URL + `/domains/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "Domains Cancel": (r) => r.status === 200
            });
        }
    });

    group("/servers/order", () => {

        // Request No. 1: getNewServer
        {
            let url = BASE_URL + `/servers/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Server Ordering details": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: putServers
        {
            let url = BASE_URL + `/servers/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.put(url, params);

            check(request, {
                "Validate Server order response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: addServer
        {
            let url = BASE_URL + `/servers/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/account/creditcards", () => {

        // Request No. 1: addAccountCreditCard
        {
            let url = BASE_URL + `/account/creditcards`;
            // TODO: edit the parameters of the request body.
            let body = {"name": "string", "address": "string", "city": "string", "state": "string", "country": "string", "zip": "string", "cc": "string", "ccExp": "string", "ccCcv2": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/domains/{id}/nameservers", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getDomainNameservers
        {
            let url = BASE_URL + `/domains/${id}/nameservers`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Domain registered nameservers list response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: updateDomainNameservers
        {
            let url = BASE_URL + `/domains/${id}/nameservers`;
            // TODO: edit the parameters of the request body.
            let body = {"nameserver": "list"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Response with a text message field.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: addDomainNameserver
        {
            let url = BASE_URL + `/domains/${id}/nameservers`;
            // TODO: edit the parameters of the request body.
            let body = {"name": "string", "ipAddress": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Response with a text message field.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 4: deleteDomainNameserver
        {
            let url = BASE_URL + `/domains/${id}/nameservers?index=${index}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "Response with a text message field.": (r) => r.status === 200
            });
        }
    });

    group("/affiliate/sales_report", () => {

        // Request No. 1: getAffiliateSalesReport
        {
            let url = BASE_URL + `/affiliate/sales_report`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response with a text message field.": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/change_hostname", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getQsChangeHostname
        {
            let url = BASE_URL + `/qs/${id}/change_hostname`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "QuickServer Change Hostname info response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postQsChangeHostname
        {
            let url = BASE_URL + `/qs/${id}/change_hostname`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/domains/search/{name}", () => {
        let name = 'TODO_EDIT_THE_NAME'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getDomainSearch
        {
            let url = BASE_URL + `/domains/search/${name}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Suggested and lookup results for the supplied search term.": (r) => r.status === 200
            });
        }
    });

    group("/websites/{id}/backups", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getWebsitesBackups
        {
            let url = BASE_URL + `/websites/${id}/backups`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Get Website Backups response": (r) => r.status === 200
            });
        }
    });

    group("/tickets/{id}/reply", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: ReplyTicket
        {
            let url = BASE_URL + `/tickets/${id}/reply`;
            // TODO: edit the parameters of the request body.
            let body = {"content": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Post Reply to the ticket.": (r) => r.status === 200
            });
            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/setup_vnc", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getVpsSetupVnc
        {
            let url = BASE_URL + `/vps/${id}/setup_vnc`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Get VPS Setup VNC Information": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postVpsSetupVnc
        {
            let url = BASE_URL + `/vps/${id}/setup_vnc`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/licenses/{id}/invoices", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getLicenseInvoices
        {
            let url = BASE_URL + `/licenses/${id}/invoices`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Get Invoices response": (r) => r.status === 200
            });
        }
    });

    group("/ping", () => {

        // Request No. 1: pingServer
        {
            let url = BASE_URL + `/ping`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "A simple response of "pong" for use with simple tests to see if a service is up. ": (r) => r.status === 200
            });
            check(request, {
                "Something is wrong": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/block_smtp", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: doVpsBlockSmtp
        {
            let url = BASE_URL + `/vps/${id}/block_smtp`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/invoices", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getQsInvoices
        {
            let url = BASE_URL + `/qs/${id}/invoices`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Get Invoices response": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/restart", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: doVpsRestart
        {
            let url = BASE_URL + `/vps/${id}/restart`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/floating_ips/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getFloatingIpInfo
        {
            let url = BASE_URL + `/floating_ips/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: updateFloatingIpInfo
        {
            let url = BASE_URL + `/floating_ips/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: floating_ipsCancel
        {
            let url = BASE_URL + `/floating_ips/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "Floating Ip Cancel": (r) => r.status === 200
            });
        }
    });

    group("/mail/{id}/blocks/delete", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: delistBlock
        {
            let url = BASE_URL + `/mail/${id}/blocks/delete`;
            let body = {"email": "client@domain.com"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Email address removed from block list successfully.": (r) => r.status === 200
            });
        }
    });

    group("/domains", () => {

        // Request No. 1: getDomainsList
        {
            let url = BASE_URL + `/domains`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "The listing of `Domains` services on your account.": (r) => r.status === 200
            });
        }
    });

    group("/scrub_ips/{id}/delete_rule", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: scrubIpsDeleteRule
        {
            let url = BASE_URL + `/scrub_ips/${id}/delete_rule`;
            // TODO: edit the parameters of the request body.
            let body = {"ruleId": "integer"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Delete firewall rule for scrub ip": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/view_desktop", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getQsViewDesktop
        {
            let url = BASE_URL + `/qs/${id}/view_desktop`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Get QuickServer View Desktop Information": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postQsViewDesktop
        {
            let url = BASE_URL + `/qs/${id}/view_desktop`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Submit QuickServer View Desktop Information": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/change_root_password", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: postVpsChangeRootPassword
        {
            let url = BASE_URL + `/vps/${id}/change_root_password`;
            // TODO: edit the parameters of the request body.
            let body = {"password": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/reinstall_os", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getVpsReinstallOs
        {
            let url = BASE_URL + `/vps/${id}/reinstall_os`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "VPS Reinstall OS info response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postVpsReinstallOs
        {
            let url = BASE_URL + `/vps/${id}/reinstall_os`;
            // TODO: edit the parameters of the request body.
            let body = {"template": "string", "password": "string", "localPassword": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/stop", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: doVpsStop
        {
            let url = BASE_URL + `/vps/${id}/stop`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/account/apikey", () => {

        // Request No. 1: updateAccountApiKey
        {
            let url = BASE_URL + `/account/apikey`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/backups/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getBackupInfo
        {
            let url = BASE_URL + `/backups/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Full backup service details including serviceInfo, billingDetails, and client_links.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: updateBackupInfo
        {
            let url = BASE_URL + `/backups/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: cancelBackup
        {
            let url = BASE_URL + `/backups/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "Backups Cancel": (r) => r.status === 200
            });
        }
    });

    group("/websites/order", () => {

        // Request No. 1: getNewWebsite
        {
            let url = BASE_URL + `/websites/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Website ordering details including available plans and pricing.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: putWebsites
        {
            let url = BASE_URL + `/websites/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.put(url, params);

            check(request, {
                "Validate Website order response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: addWebsite
        {
            let url = BASE_URL + `/websites/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/backups/{id}/login", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getBackupLogin
        {
            let url = BASE_URL + `/backups/${id}/login`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Login session details for the backup storage service.": (r) => r.status === 200
            });
        }
    });

    group("/billing/invoices", () => {

        // Request No. 1: getBillingInvoices
        {
            let url = BASE_URL + `/billing/invoices`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Invoice listing and summary for the account.": (r) => r.status === 200
            });
        }
    });

    group("/domains/order/{domain}", () => {
        let domain = 'TODO_EDIT_THE_DOMAIN'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getDomainOrderSearchResults
        {
            let url = BASE_URL + `/domains/order/${domain}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Domain availability and pricing information.": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/setup_vnc", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getQsSetupVnc
        {
            let url = BASE_URL + `/qs/${id}/setup_vnc`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Get QuickServer Setup VNC Information": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postQsSetupVnc
        {
            let url = BASE_URL + `/qs/${id}/setup_vnc`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/licenses", () => {

        // Request No. 1: getLicenseList
        {
            let url = BASE_URL + `/licenses`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "The listing of `Licenses` services on your account.": (r) => r.status === 200
            });
        }
    });

    group("/mail/{id}/advsend", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: sendAdvMail
        {
            let url = BASE_URL + `/mail/${id}/advsend`;
            let body = {"subject": "Welcome", "body": "Hello", "from": {"email":"user@domain.com"}, "to": [{"email":"someone@client.com","name":"Mr Client"}], "attachments": [{"filename":"message.txt","data":"base64_encoded_contents"}], "id": 66};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Email queued successfully.": (r) => r.status === 200
            });
        }
    });

    group("/websites/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getWebsiteInfo
        {
            let url = BASE_URL + `/websites/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Website details": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: updateWebsiteInfo
        {
            let url = BASE_URL + `/websites/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: webhostingCancel
        {
            let url = BASE_URL + `/websites/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "Website cancel": (r) => r.status === 200
            });
        }
    });

    group("/account", () => {

        // Request No. 1: getAccountInfo
        {
            let url = BASE_URL + `/account`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Your account information.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: updateAccountInfo
        {
            let url = BASE_URL + `/account`;
            // TODO: edit the parameters of the request body.
            let body = {"name": "string", "company": "string", "address": "string", "address2": "string", "city": "string", "state": "string", "zip": "string", "country": "string", "phone": "string", "locale": "string", "emailInvoices": "string", "emailAbuse": "string", "disableReset": "boolean", "disableReinstall": "boolean", "disableServerNotifications": "boolean", "disableEmailNotifications": "boolean", "gstin": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/dns/{domainId}/{recordId}", () => {
        let recordId = 'TODO_EDIT_THE_RECORDID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let domainId = 'TODO_EDIT_THE_DOMAINID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: updateDnsRecord
        {
            let url = BASE_URL + `/dns/${domainId}/${recordId}`;
            // TODO: edit the parameters of the request body.
            let body = {"name": "string", "type": "dnsrecordtype", "content": "string", "ttl": "string", "prio": "string", "disabled": "string", "ordername": "string", "auth": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: deleteDnsRecord
        {
            let url = BASE_URL + `/dns/${domainId}/${recordId}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/mail/{id}/welcome_email", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getMailWelcomeEmail
        {
            let url = BASE_URL + `/mail/${id}/welcome_email`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/floating_ips", () => {

        // Request No. 1: getFloatingIpsList
        {
            let url = BASE_URL + `/floating_ips`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "The listing of `Floating IPs` services on your account.": (r) => r.status === 200
            });
            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/domains/{id}/transfer", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getDomainTransfer
        {
            let url = BASE_URL + `/domains/${id}/transfer`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postDomainTransfer
        {
            let url = BASE_URL + `/domains/${id}/transfer`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/licenses/{id}/change_os", () => {
    });

    group("/vps/{id}/view_desktop", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getVpsViewDesktop
        {
            let url = BASE_URL + `/vps/${id}/view_desktop`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Get VPS View Desktop Information": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postVpsViewDesktop
        {
            let url = BASE_URL + `/vps/${id}/view_desktop`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Submit VPS View Desktop Information": (r) => r.status === 200
            });
        }
    });

    group("/websites/{id}/buy_ip", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getWebsiteBuyIp
        {
            let url = BASE_URL + `/websites/${id}/buy_ip`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Current IP addresses and their reverse DNS hostnames for the website.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postWebsiteBuyIp
        {
            let url = BASE_URL + `/websites/${id}/buy_ip`;
            // TODO: edit the parameters of the request body.
            let body = {"ips": "map"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "DNS update result.": (r) => r.status === 200
            });
        }
    });

    group("/mail/{id}/log", () => {
        let endDate = '1673317008'; // extracted from 'example' field defined at the parameter level of OpenAPI spec
        let subject = 'Your order has shipped'; // extracted from 'example' field defined at the parameter level of OpenAPI spec
        let origin = '1.2.3.4'; // extracted from 'example' field defined at the parameter level of OpenAPI spec
        let messageId = '<abc123@yourdomain.com>'; // extracted from 'example' field defined at the parameter level of OpenAPI spec
        let delivered = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec
        let skip = '0'; // extracted from 'example' field defined at the parameter level of OpenAPI spec
        let mx = 'mx.google.com'; // extracted from 'example' field defined at the parameter level of OpenAPI spec
        let sort = 'time'; // extracted from 'example' field defined at the parameter level of OpenAPI spec
        let groupby = 'recipient'; // extracted from 'example' field defined at the parameter level of OpenAPI spec
        let dir = 'desc'; // extracted from 'example' field defined at the parameter level of OpenAPI spec
        let headerfrom = 'newsletter@sender.com'; // extracted from 'example' field defined at the parameter level of OpenAPI spec
        let replyto = 'replies@sender.com'; // extracted from 'example' field defined at the parameter level of OpenAPI spec
        let limit = '100'; // extracted from 'example' field defined at the parameter level of OpenAPI spec
        let from = 'me@sender.com'; // extracted from 'example' field defined at the parameter level of OpenAPI spec
        let mailid = '185997065c60008840'; // extracted from 'example' field defined at the parameter level of OpenAPI spec
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let id = '2604'; // extracted from 'example' field defined at the parameter level of OpenAPI spec
        let to = 'you@receiver.com'; // extracted from 'example' field defined at the parameter level of OpenAPI spec
        let startDate = '1641781008'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: viewMailLog
        {
            let url = BASE_URL + `/mail/${id}/log?id=${id}&origin=${origin}&mx=${mx}&from=${from}&to=${to}&subject=${subject}&mailid=${mailid}&messageId=${messageId}&replyto=${replyto}&headerfrom=${headerfrom}&delivered=${delivered}&skip=${skip}&limit=${limit}&startDate=${startDate}&endDate=${endDate}&sort=${sort}&dir=${dir}&groupby=${groupby}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Paginated list of mail log entries matching the specified filters.": (r) => r.status === 200
            });
        }
    });

    group("/affiliate/dock_setup", () => {

        // Request No. 1: updateAffiliateDockSetup
        {
            let url = BASE_URL + `/affiliate/dock_setup`;
            // TODO: edit the parameters of the request body.
            let body = {"affiliateDockTitle": "string", "affiliateDockDescription": "string", "referrerCoupon": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Response with a text message field.": (r) => r.status === 200
            });
        }
    });

    group("/home", () => {

        // Request No. 1: getHome
        {
            let url = BASE_URL + `/home`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "General information for use on the home page.": (r) => r.status === 200
            });
        }
    });

    group("/affiliate/banners", () => {

        // Request No. 1: getAffiliateBanners
        {
            let url = BASE_URL + `/affiliate/banners`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Affiliate Banners Array": (r) => r.status === 200
            });
        }
    });

    group("/mail/{id}/rules/{rule}", () => {
        let rule = ['34'].shift(); // first element from list extracted from 'examples' field defined at the parameter level of OpenAPI spec
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: deleteRule
        {
            let url = BASE_URL + `/mail/${id}/rules/${rule}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "Deny rule deleted successfully.": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/block_smtp", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: doQsBlockSmtp
        {
            let url = BASE_URL + `/qs/${id}/block_smtp`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/scrub_ips/{id}/delete_filter", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: deleteFilter
        {
            let url = BASE_URL + `/scrub_ips/${id}/delete_filter`;
            // TODO: edit the parameters of the request body.
            let body = {"filterType": "string", "port": "integer"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Delete filter for scrub ip": (r) => r.status === 200
            });
        }
    });

    group("/backups/{id}/welcome_email", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getBackupsWelcomeEmail
        {
            let url = BASE_URL + `/backups/${id}/welcome_email`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/scrub_ips/{id}/create_geo_rule", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: createGeoRule
        {
            let url = BASE_URL + `/scrub_ips/${id}/create_geo_rule`;
            // TODO: edit the parameters of the request body.
            let body = {"destinationPort": "integer", "countryCode": "integer", "asn": "integer", "xdpAction": "integer"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Create firewall rule for scrub ip": (r) => r.status === 201
            });
        }
    });

    group("/vps/{id}/reset_password", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: postVpsResetPassword
        {
            let url = BASE_URL + `/vps/${id}/reset_password`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/stop", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: doQsStop
        {
            let url = BASE_URL + `/qs/${id}/stop`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/reverse_dns", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getVpsReverseDns
        {
            let url = BASE_URL + `/vps/${id}/reverse_dns`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "VPS Reverse DNS info response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postVpsReverseDns
        {
            let url = BASE_URL + `/vps/${id}/reverse_dns`;
            // TODO: edit the parameters of the request body.
            let body = {"ips": "map"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Update VPS Reverse DNS response": (r) => r.status === 200
            });
        }
    });

    group("/servers/order/buy_now_server", () => {

        // Request No. 1: buyItNowServerOrder
        {
            let url = BASE_URL + `/servers/order/buy_now_server`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Available server configurations with pricing and hardware options.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: placeBuyNowServer
        {
            let url = BASE_URL + `/servers/order/buy_now_server`;
            // TODO: edit the parameters of the request body.
            let body = {"serverId": "bigdecimal", "serverHostname": "string", "serverRootPassword": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Order placed successfully.": (r) => r.status === 200
            });
            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/reset_password", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getQsResetPassword
        {
            let url = BASE_URL + `/qs/${id}/reset_password`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "QuickServer Reset password info": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postQsResetPassword
        {
            let url = BASE_URL + `/qs/${id}/reset_password`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/backup", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: postVpsBackup
        {
            let url = BASE_URL + `/vps/${id}/backup`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/slices", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getVpsSlices
        {
            let url = BASE_URL + `/vps/${id}/slices`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "VPS Slices information": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postVpsSlices
        {
            let url = BASE_URL + `/vps/${id}/slices`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Update VPS slices response": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/start", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: doVpsStart
        {
            let url = BASE_URL + `/vps/${id}/start`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/affiliate/payment_setup", () => {

        // Request No. 1: updateAffiliatePaymentSetup
        {
            let url = BASE_URL + `/affiliate/payment_setup`;
            // TODO: edit the parameters of the request body.
            let body = {"affiliatePaypal": "string", "affiliatePaymentMethod": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Response with a text message field.": (r) => r.status === 200
            });
        }
    });

    group("/ssl", () => {

        // Request No. 1: getSslList
        {
            let url = BASE_URL + `/ssl`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "The listing of `SSL` services on your account.": (r) => r.status === 200
            });
            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/billing/cart", () => {

        // Request No. 1: getBillingCart
        {
            let url = BASE_URL + `/billing/cart`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/websites/{id}/download_backups", () => {
    });

    group("/qs/{id}/restart", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: doQsRestart
        {
            let url = BASE_URL + `/qs/${id}/restart`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/restore", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: postVpsRestore
        {
            let url = BASE_URL + `/vps/${id}/restore`;
            // TODO: edit the parameters of the request body.
            let body = {"backup": "string", "password": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/account/timezones", () => {

        // Request No. 1: getTimezones
        {
            let url = BASE_URL + `/account/timezones`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "An array of all available timezone identifiers.": (r) => r.status === 200
            });
        }
    });

    group("/domains/order", () => {

        // Request No. 1: getNewDomain
        {
            let url = BASE_URL + `/domains/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Domain registration order information.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: putDomains
        {
            let url = BASE_URL + `/domains/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.put(url, params);

            check(request, {
                "Validate Domain Order response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: addDomain
        {
            let url = BASE_URL + `/domains/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 4: patchDomains
        {
            let url = BASE_URL + `/domains/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.patch(url, params);

            check(request, {
                "Validate Domain order response": (r) => r.status === 200
            });
        }
    });

    group("/account/oauth/{name}", () => {
        let name = 'TODO_EDIT_THE_NAME'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: deleteAccountOauthName
        {
            let url = BASE_URL + `/account/oauth/${name}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/reinstall_os", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getQsReinstallOs
        {
            let url = BASE_URL + `/qs/${id}/reinstall_os`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "QuickServer Reinstall OS info response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postQsReinstallOs
        {
            let url = BASE_URL + `/qs/${id}/reinstall_os`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/scrub_ips/{id}/enable", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: enableScrub
        {
            let url = BASE_URL + `/scrub_ips/${id}/enable`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Request OK": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/enable_quota", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: doQsEnableQuota
        {
            let url = BASE_URL + `/qs/${id}/enable_quota`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/licenses/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getLicenseInfo
        {
            let url = BASE_URL + `/licenses/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "License information.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: updateLicenseInfo
        {
            let url = BASE_URL + `/licenses/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: licensesCancel
        {
            let url = BASE_URL + `/licenses/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "License Cancel": (r) => r.status === 200
            });
        }
    });

    group("/servers/{id}/invoices", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getServerInvoices
        {
            let url = BASE_URL + `/servers/${id}/invoices`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Get Invoices response": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/change_hostname", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: postVpsChangeHostname
        {
            let url = BASE_URL + `/vps/${id}/change_hostname`;
            // TODO: edit the parameters of the request body.
            let body = {"hostname": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/websites/{id}/welcome_email", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getWebsitesWelcomeEmail
        {
            let url = BASE_URL + `/websites/${id}/welcome_email`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/ssl/{id}/welcome_email", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getSslWelcomeEmail
        {
            let url = BASE_URL + `/ssl/${id}/welcome_email`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/floating_ips/{id}/welcome_email", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getFloatingIpsWelcomeEmail
        {
            let url = BASE_URL + `/floating_ips/${id}/welcome_email`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/servers/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getServerInfo
        {
            let url = BASE_URL + `/servers/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Server details": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: updateServerInfo
        {
            let url = BASE_URL + `/servers/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: serversCancel
        {
            let url = BASE_URL + `/servers/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "Servers Cancel": (r) => r.status === 200
            });
        }
    });

    group("/ssl/{id}/resend_approver_email", () => {
    });

    group("/vps/{id}/enable_quota", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: doVpsEnableQuota
        {
            let url = BASE_URL + `/vps/${id}/enable_quota`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/servers/{id}/welcome_email", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getServersWelcomeEmail
        {
            let url = BASE_URL + `/servers/${id}/welcome_email`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/servers/{id}/ipmi_power", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: serverIpmiPowerGet
        {
            let url = BASE_URL + `/servers/${id}/ipmi_power`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response with a text message field.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: serverIpmiPowerPost
        {
            let url = BASE_URL + `/servers/${id}/ipmi_power`;
            // TODO: edit the parameters of the request body.
            let body = {"asset": "integer", "action": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Response with a text message field.": (r) => r.status === 200
            });
        }
    });

    group("/billing/prepays/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: deleteBillingPrepay
        {
            let url = BASE_URL + `/billing/prepays/${id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/info", () => {

        // Request No. 1: getInfo
        {
            let url = BASE_URL + `/info`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "The modules and services information from the server.": (r) => r.status === 200
            });
        }
    });

    group("/billing/payment_method", () => {

        // Request No. 1: updateBillingPaymentMethod
        {
            let url = BASE_URL + `/billing/payment_method`;
            // TODO: edit the parameters of the request body.
            let body = {"paymentMethod": "string", "ccAuto": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/account/oauth/{name}/logout", () => {
        let name = 'TODO_EDIT_THE_NAME'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: logoutAccountOauth
        {
            let url = BASE_URL + `/account/oauth/${name}/logout`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/pay/{method}/{invoices}", () => {
        let method = 'TODO_EDIT_THE_METHOD'; // specify value as there is no example value for this parameter in OpenAPI spec
        let invoices = 'TODO_EDIT_THE_INVOICES'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: initiatePayment
        {
            let url = BASE_URL + `/pay/${method}/${invoices}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Payment initiation response with redirect or form data.": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/eject_cd", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: doVpsEjectCd
        {
            let url = BASE_URL + `/vps/${id}/eject_cd`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/websites/{id}/invoices", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getWebsiteInvoices
        {
            let url = BASE_URL + `/websites/${id}/invoices`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Get Invoices response": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/eject_cd", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: doQsEjectCd
        {
            let url = BASE_URL + `/qs/${id}/eject_cd`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/scrub_ips/filter_types", () => {

        // Request No. 1: getScrubIpFilterTypes
        {
            let url = BASE_URL + `/scrub_ips/filter_types`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Supported scrub filter types for building firewall rules.": (r) => r.status === 200
            });
        }
    });

    group("/affiliate/rich_report", () => {

        // Request No. 1: getAffiliateRichReport
        {
            let url = BASE_URL + `/affiliate/rich_report`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response with a text message field.": (r) => r.status === 200
            });
        }
    });

    group("/mail/{id}/send", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: sendMail
        {
            let url = BASE_URL + `/mail/${id}/send`;
            // TODO: edit the parameters of the request body.
            let body = {"to": "string", "from": "string", "subject": "string", "body": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Email queued successfully.": (r) => r.status === 200
            });
        }
    });

    group("/oauth", () => {
        let provider = 'TODO_EDIT_THE_PROVIDER'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getOauthRedirect
        {
            let url = BASE_URL + `/oauth?provider=${provider}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "OAuth redirect URL for the requested provider.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postOauthCallback
        {
            let url = BASE_URL + `/oauth?provider=${provider}`;
            // TODO: edit the parameters of the request body.
            let body = {"provider": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "OAuth callback result.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: patchOauthTwoFactor
        {
            let url = BASE_URL + `/oauth`;
            // TODO: edit the parameters of the request body.
            let body = {"accountId": "integer", "code": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.patch(url, JSON.stringify(body), params);

            check(request, {
                "OAuth 2FA verification result.": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/insert_cd", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getQsInsertCd
        {
            let url = BASE_URL + `/qs/${id}/insert_cd`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "QuickServer Insert CD info response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postQsInsertCd
        {
            let url = BASE_URL + `/qs/${id}/insert_cd`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/scrub_ips/{id}/create_rule", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: createRule
        {
            let url = BASE_URL + `/scrub_ips/${id}/create_rule`;
            // TODO: edit the parameters of the request body.
            let body = {"destinationPort": "integer", "sourceIp": "string", "sourcePort": "integer", "protocolId": "integer", "xdpAction": "integer"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Create firewall rule for scrub ip": (r) => r.status === 201
            });
        }
    });

    group("/scrub_ips/{id}/delete_geo_rule", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: scrubIpsDeleteGeoRule
        {
            let url = BASE_URL + `/scrub_ips/${id}/delete_geo_rule`;
            // TODO: edit the parameters of the request body.
            let body = {"ruleId": "integer"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Delete firewall rule for scrub ip": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/buy_hd_space", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getVpsBuyHdSpace
        {
            let url = BASE_URL + `/vps/${id}/buy_hd_space`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Get VPS Buy HD Space information": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postVpsBuyHdSpace
        {
            let url = BASE_URL + `/vps/${id}/buy_hd_space`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Post Buy HD Space for VPS response": (r) => r.status === 200
            });
        }
    });

    group("/mail/{id}/invoices", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getMailInvoices
        {
            let url = BASE_URL + `/mail/${id}/invoices`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Get Invoices response": (r) => r.status === 200
            });
        }
    });

    group("/scrub_ips/{id}/invoices", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getScrubIpInvoices
        {
            let url = BASE_URL + `/scrub_ips/${id}/invoices`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Get Invoices response": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/backups", () => {
        let all = 'TODO_EDIT_THE_ALL'; // specify value as there is no example value for this parameter in OpenAPI spec
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getVpsBackups
        {
            let url = BASE_URL + `/vps/${id}/backups?all=${all}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "The listing of the available backups": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: deleteVpsBackup
        {
            let url = BASE_URL + `/vps/${id}/backups?all=${all}&file=${file}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: downloadVpsBackup
        {
            let url = BASE_URL + `/vps/${id}/backups?all=${all}`;
            // TODO: edit the parameters of the request body.
            let body = {"file": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.patch(url, JSON.stringify(body), params);

            check(request, {
                "Download URL for the backup file.": (r) => r.status === 200
            });
        }
    });

    group("/servers/{id}/reverse_dns", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getServerReverseDns
        {
            let url = BASE_URL + `/servers/${id}/reverse_dns`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Servers Reverse DNS info response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postServerReverseDns
        {
            let url = BASE_URL + `/servers/${id}/reverse_dns`;
            // TODO: edit the parameters of the request body.
            let body = {"ips": "map"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Update Server Reverse DNS response": (r) => r.status === 200
            });
        }
    });

    group("/account/features", () => {

        // Request No. 1: updateAccountFeatures
        {
            let url = BASE_URL + `/account/features`;
            // TODO: edit the parameters of the request body.
            let body = {"disableReset": "integer", "disableReinstall": "integer"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/disable_cd", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: doQsDisableCd
        {
            let url = BASE_URL + `/qs/${id}/disable_cd`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/welcome_email", () => {
        let id = ['133123'].shift(); // first element from list extracted from 'examples' field defined at the parameter level of OpenAPI spec

        // Request No. 1: getQsWelcomeEmail
        {
            let url = BASE_URL + `/qs/${id}/welcome_email`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response with a text message field.": (r) => r.status === 200
            });
        }
    });

    group("/billing/creditcards/{id}/verify", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getBillingCreditCardVerify
        {
            let url = BASE_URL + `/billing/creditcards/${id}/verify`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postBillingCreditCardVerify
        {
            let url = BASE_URL + `/billing/creditcards/${id}/verify`;
            // TODO: edit the parameters of the request body.
            let body = {"idx": "integer", "ccCcv2": "string", "ccAmount1": "string", "ccAmount2": "string", "terms": "boolean"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/account/password", () => {

        // Request No. 1: updateAccountPassword
        {
            let url = BASE_URL + `/account/password`;
            // TODO: edit the parameters of the request body.
            let body = {"password": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Response with a text message field.": (r) => r.status === 200
            });
        }
    });

    group("/licenses/order", () => {

        // Request No. 1: getNewLicense
        {
            let url = BASE_URL + `/licenses/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "License ordering information.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: putLicenses
        {
            let url = BASE_URL + `/licenses/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.put(url, params);

            check(request, {
                "Validate Licenses order response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: addLicense
        {
            let url = BASE_URL + `/licenses/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/change_timezone", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getVpsChangeTimezone
        {
            let url = BASE_URL + `/vps/${id}/change_timezone`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "VPS Change Timezone info response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: postVpsChangeTimezone
        {
            let url = BASE_URL + `/vps/${id}/change_timezone`;
            // TODO: edit the parameters of the request body.
            let body = {"timezone": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/ssl/{id}/change_approver_email", () => {
    });

    group("/domains/{id}/whois", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getDomainWhoisPrivacy
        {
            let url = BASE_URL + `/domains/${id}/whois`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: updateDomainWhoisPrivacy
        {
            let url = BASE_URL + `/domains/${id}/whois`;
            // TODO: edit the parameters of the request body.
            let body = {"func": "string", "csrfToken": "string", "domainFirstname": "string", "domainLastname": "string", "domainEmail": "string", "domainAddress": "string", "domainAddress2": "string", "domainAddress3": "string", "domainCity": "string", "domainState": "string", "domainZip": "string", "domainCountry": "string", "domainPhone": "string", "domainFax": "string", "domainCompany": "string", "domainExtra": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/disable_cd", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: doVpsDisableCd
        {
            let url = BASE_URL + `/vps/${id}/disable_cd`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/backups/order", () => {

        // Request No. 1: getNewBackup
        {
            let url = BASE_URL + `/backups/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Information needed to generate an order form.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: validateBackupOrder
        {
            let url = BASE_URL + `/backups/order`;
            // TODO: edit the parameters of the request body.
            let body = {"validateOnly": "boolean", "serviceType": "integer", "coupon": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Validate Backup Order Response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: addBackup
        {
            let url = BASE_URL + `/backups/order`;
            // TODO: edit the parameters of the request body.
            let body = {"validateOnly": "boolean", "serviceType": "integer", "coupon": "string"};
            let params = {
                headers: {
                    "Content-Type": "multipart/form-data", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Response from the backup order call including invoice IDs for payment.": (r) => r.status === 200
            });
            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/mail/{id}/alerts", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getMailAlerts
        {
            let url = BASE_URL + `/mail/${id}/alerts`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Alert configuration for the mail service.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: updateMailAlert
        {
            let url = BASE_URL + `/mail/${id}/alerts`;
            // TODO: edit the parameters of the request body.
            let body = {"alertId": "integer", "type": "string", "value": "string", "to": "string", "enabled": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: createMailAlert
        {
            let url = BASE_URL + `/mail/${id}/alerts`;
            // TODO: edit the parameters of the request body.
            let body = {"type": "string", "value": "string", "to": "string", "enabled": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 4: deleteMailAlert
        {
            let url = BASE_URL + `/mail/${id}/alerts?alert_id=${alert_id}`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "A response indicating the operation completed successfully with a text message.": (r) => r.status === 200
            });
        }
    });

    group("/qs/{id}/restore", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: postQuickServerRestore
        {
            let url = BASE_URL + `/qs/${id}/restore`;
            // TODO: edit the parameters of the request body.
            let body = {"backup": "string", "password": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Response message from sending a service queue.": (r) => r.status === 200
            });
        }
    });

    group("/floating_ips/order", () => {

        // Request No. 1: getNewFloatingIp
        {
            let url = BASE_URL + `/floating_ips/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: putFloating_ips
        {
            let url = BASE_URL + `/floating_ips/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.put(url, params);

            check(request, {
                "Validate Floating IPs order response": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: addFloatingIp
        {
            let url = BASE_URL + `/floating_ips/order`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Default response": (r) => r.status === 200
            });
        }
    });

    group("/vps/{id}/traffic_usage", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: getVpsTrafficUsage
        {
            let url = BASE_URL + `/vps/${id}/traffic_usage`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.get(url, params);

            check(request, {
                "Get VPS Traffic usage": (r) => r.status === 200
            });
        }
    });

    group("/websites/{id}/migration", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: postWebsiteMigration
        {
            let url = BASE_URL + `/websites/${id}/migration`;
            // TODO: edit the parameters of the request body.
            let body = {"custPortal": "string", "regEmail": "string", "password": "string", "ctrlPanel": "string", "ftpUsername": "string", "ftpPassword": "string", "siteBusyMig": "string", "splReqMig": "string", "domainReg": "string", "dataMig": "string", "domainRegPortal": "string", "domainRegEmail": "string", "domainRegPassword": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Migration request submitted.": (r) => r.status === 200
            });
        }
    });

    group("/account/username", () => {

        // Request No. 1: changeAccountUsername
        {
            let url = BASE_URL + `/account/username`;
            let params = {
                headers: {
                    "X-API-KEY": `${X_API_KEY}`, "sessionid": `${sessionid}`, "Accept": "application/json"
                }, cookies: {
                    "sessionid": `${sessionid}`
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Response with a text message field.": (r) => r.status === 200
            });
        }
    });

    group("/mail/{id}/deny_rules", () => {
    });

}
