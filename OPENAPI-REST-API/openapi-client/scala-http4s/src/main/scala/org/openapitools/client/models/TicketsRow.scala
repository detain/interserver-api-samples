/** InterServer Management API
  * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
  *
  * The version of the OpenAPI document: 0.9.0
  * Contact: support@interserver.net
  *
  * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
  * https://openapi-generator.tech
  * Do not edit the class manually.
  */
package org.openapitools.client.models

import io.circe.*
import io.circe.syntax.*
import io.circe.{Decoder, DecodingFailure, Encoder}
import cats.syntax.functor.*


/** Information about a single ticket.
  * @param title 
  * @param ticketmaskid 
  * @param lastreplier 
  * @param status 
  * @param priority 
  * @param total_replies 
  * @param lastactivity 
  * @param departmenttitle 
  * @param ticketid 
  * @param can_close 
  * @param attachments 
  * @param status_text 
  * @param checked 
  */
case class TicketsRow(
    title: String,
    ticketmaskid: String,
    lastreplier: String,
    status: String,
    priority: String,
    total_replies: Int,
    lastactivity: String,
    departmenttitle: String,
    ticketid: Int,
    can_close: String,
    attachments: AnyType,
    status_text: String,
    checked: Boolean
)
  
object TicketsRow {
  given encoderTicketsRow: Encoder[TicketsRow] = Encoder.instance { t =>
    Json.fromFields{
      Seq(
        Some("title" -> t.title.asJson),
        Some("ticketmaskid" -> t.ticketmaskid.asJson),
        Some("lastreplier" -> t.lastreplier.asJson),
        Some("status" -> t.status.asJson),
        Some("priority" -> t.priority.asJson),
        Some("total_replies" -> t.total_replies.asJson),
        Some("lastactivity" -> t.lastactivity.asJson),
        Some("departmenttitle" -> t.departmenttitle.asJson),
        Some("ticketid" -> t.ticketid.asJson),
        Some("can_close" -> t.can_close.asJson),
        Some("attachments" -> t.attachments.asJson),
        Some("status_text" -> t.status_text.asJson),
        Some("checked" -> t.checked.asJson)
      ).flatten
    }
  }
  given decoderTicketsRow: Decoder[TicketsRow] = Decoder.instance { c =>
    for {
      title <- c.downField("title").as[String]
      ticketmaskid <- c.downField("ticketmaskid").as[String]
      lastreplier <- c.downField("lastreplier").as[String]
      status <- c.downField("status").as[String]
      priority <- c.downField("priority").as[String]
      total_replies <- c.downField("total_replies").as[Int]
      lastactivity <- c.downField("lastactivity").as[String]
      departmenttitle <- c.downField("departmenttitle").as[String]
      ticketid <- c.downField("ticketid").as[Int]
      can_close <- c.downField("can_close").as[String]
      attachments <- c.downField("attachments").as[AnyType]
      status_text <- c.downField("status_text").as[String]
      checked <- c.downField("checked").as[Boolean]
    } yield TicketsRow(
      title = title,
      ticketmaskid = ticketmaskid,
      lastreplier = lastreplier,
      status = status,
      priority = priority,
      total_replies = total_replies,
      lastactivity = lastactivity,
      departmenttitle = departmenttitle,
      ticketid = ticketid,
      can_close = can_close,
      attachments = attachments,
      status_text = status_text,
      checked = checked
    )
  }
}

