/** InterServer Management API
  * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
  *
  * The version of the OpenAPI document: 0.9.0
  * Contact: support@interserver.net
  *
  * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
  * https://openapi-generator.tech
  * Do not edit the class manually.
  */
package org.openapitools.client.models

import io.circe.*
import io.circe.syntax.*
import io.circe.{Decoder, Encoder}

import scala.collection.immutable.Seq

/** 
  * @param serviceInfo 
  * @param client_links Links related to the mail service for clients.
  * @param billingDetails 
  * @param custCurrency The customer's currency.
  * @param custCurrencySymbol The currency symbol for the customer.
  * @param `package` The package of the mail service.
  * @param extraInfoTables 
  * @param serviceType 
  * @param usage_count The usage count of the mail service.
  * @param serviceExtra Extra information for the mail service.
  */
case class MailSchema(
    serviceInfo: MailServiceInfo,
    client_links: Seq[MailClientLink],
    billingDetails: MailBillingDetails,
    custCurrency: String,
    custCurrencySymbol: String,
    `package`: String,
    extraInfoTables: MailSchemaExtraInfoTables,
    serviceType: MailServiceType,
    usage_count: String,
    serviceExtra: Option[Seq[String]] = None
)
  
object MailSchema {
  given encoderMailSchema: Encoder[MailSchema] = Encoder.instance { t =>
    Json.fromFields{
      Seq(
        Some("serviceInfo" -> t.serviceInfo.asJson),
        Some("client_links" -> t.client_links.asJson),
        Some("billingDetails" -> t.billingDetails.asJson),
        Some("custCurrency" -> t.custCurrency.asJson),
        Some("custCurrencySymbol" -> t.custCurrencySymbol.asJson),
        Some("package" -> t.`package`.asJson),
        Some("extraInfoTables" -> t.extraInfoTables.asJson),
        Some("serviceType" -> t.serviceType.asJson),
        Some("usage_count" -> t.usage_count.asJson),
        t.serviceExtra.map(v => "serviceExtra" -> v.asJson)
      ).flatten
    }
  }
  given decoderMailSchema: Decoder[MailSchema] = Decoder.instance { c =>
    for {
      serviceInfo <- c.downField("serviceInfo").as[MailServiceInfo]
      client_links <- c.downField("client_links").as[Seq[MailClientLink]]
      billingDetails <- c.downField("billingDetails").as[MailBillingDetails]
      custCurrency <- c.downField("custCurrency").as[String]
      custCurrencySymbol <- c.downField("custCurrencySymbol").as[String]
      `package` <- c.downField("package").as[String]
      extraInfoTables <- c.downField("extraInfoTables").as[MailSchemaExtraInfoTables]
      serviceType <- c.downField("serviceType").as[MailServiceType]
      usage_count <- c.downField("usage_count").as[String]
      serviceExtra <- c.downField("serviceExtra").as[Option[Seq[String]]]
    } yield MailSchema(
      serviceInfo = serviceInfo,
      client_links = client_links,
      billingDetails = billingDetails,
      custCurrency = custCurrency,
      custCurrencySymbol = custCurrencySymbol,
      `package` = `package`,
      extraInfoTables = extraInfoTables,
      serviceType = serviceType,
      usage_count = usage_count,
      serviceExtra = serviceExtra
    )
  }
}

