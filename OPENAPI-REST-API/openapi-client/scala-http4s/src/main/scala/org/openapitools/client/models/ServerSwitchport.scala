/** InterServer Management API
  * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
  *
  * The version of the OpenAPI document: 0.9.0
  * Contact: support@interserver.net
  *
  * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
  * https://openapi-generator.tech
  * Do not edit the class manually.
  */
package org.openapitools.client.models

import io.circe.*
import io.circe.syntax.*
import io.circe.{Decoder, Encoder}

import scala.collection.immutable.Seq

/** 
  * @param switchport_id Unique identifier for the switchport.
  * @param switch_id Unique identifier for the switch associated with the switchport.
  * @param `switch` Name of the switch associated with the switchport.
  * @param port Port name on the switch.
  * @param blade Blade name associated with the port.
  * @param justport Port identifier.
  * @param graph_id Identifier for the graph associated with the switchport.
  * @param asset_id Unique identifier of the asset associated with the switchport.
  * @param vlans List of VLANs associated with the switchport.
  * @param vlans6 List of IPv6 VLANs associated with the switchport.
  */
case class ServerSwitchport(
    switchport_id: Int,
    switch_id: String,
    `switch`: String,
    port: String,
    blade: String,
    justport: String,
    graph_id: String,
    asset_id: Int,
    vlans: Option[Seq[String]] = None,
    vlans6: Option[Seq[String]] = None
)
  
object ServerSwitchport {
  given encoderServerSwitchport: Encoder[ServerSwitchport] = Encoder.instance { t =>
    Json.fromFields{
      Seq(
        Some("switchport_id" -> t.switchport_id.asJson),
        Some("switch_id" -> t.switch_id.asJson),
        Some("switch" -> t.`switch`.asJson),
        Some("port" -> t.port.asJson),
        Some("blade" -> t.blade.asJson),
        Some("justport" -> t.justport.asJson),
        Some("graph_id" -> t.graph_id.asJson),
        Some("asset_id" -> t.asset_id.asJson),
        t.vlans.map(v => "vlans" -> v.asJson),
        t.vlans6.map(v => "vlans6" -> v.asJson)
      ).flatten
    }
  }
  given decoderServerSwitchport: Decoder[ServerSwitchport] = Decoder.instance { c =>
    for {
      switchport_id <- c.downField("switchport_id").as[Int]
      switch_id <- c.downField("switch_id").as[String]
      `switch` <- c.downField("switch").as[String]
      port <- c.downField("port").as[String]
      blade <- c.downField("blade").as[String]
      justport <- c.downField("justport").as[String]
      graph_id <- c.downField("graph_id").as[String]
      asset_id <- c.downField("asset_id").as[Int]
      vlans <- c.downField("vlans").as[Option[Seq[String]]]
      vlans6 <- c.downField("vlans6").as[Option[Seq[String]]]
    } yield ServerSwitchport(
      switchport_id = switchport_id,
      switch_id = switch_id,
      `switch` = `switch`,
      port = port,
      blade = blade,
      justport = justport,
      graph_id = graph_id,
      asset_id = asset_id,
      vlans = vlans,
      vlans6 = vlans6
    )
  }
}

