/** InterServer Management API
  * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
  *
  * The version of the OpenAPI document: 0.9.0
  * Contact: support@interserver.net
  *
  * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
  * https://openapi-generator.tech
  * Do not edit the class manually.
  */
package org.openapitools.client.models

import io.circe.*
import io.circe.syntax.*
import io.circe.{Decoder, Encoder}

import scala.collection.immutable.Seq

/** 
  * @param last_login_ip Last login IP.
  * @param last_login Last login time.
  * @param currency Currency symbol.
  * @param amount Amount with currency.
  * @param invoice_list Number of invoices.
  * @param balance Balance with currency.
  * @param full_name Users full name.
  * @param email User email address.
  * @param tickets List of tickets.
  * @param ticketStatus 
  * @param ticketStatusView 
  * @param details 
  * @param services 
  * @param AFFILIATE_AMOUNT Affiliate amount with currency.
  */
case class Home(
    last_login_ip: String,
    last_login: String,
    currency: String,
    amount: String,
    invoice_list: Int,
    balance: String,
    full_name: String,
    email: String,
    tickets: Seq[String],
    ticketStatus: HomeTicketStatus,
    ticketStatusView: HomeTicketStatusView,
    details: HomeDetails,
    services: HomeServices,
    AFFILIATE_AMOUNT: String
)
  
object Home {
  given encoderHome: Encoder[Home] = Encoder.instance { t =>
    Json.fromFields{
      Seq(
        Some("last_login_ip" -> t.last_login_ip.asJson),
        Some("last_login" -> t.last_login.asJson),
        Some("currency" -> t.currency.asJson),
        Some("amount" -> t.amount.asJson),
        Some("invoice_list" -> t.invoice_list.asJson),
        Some("balance" -> t.balance.asJson),
        Some("full_name" -> t.full_name.asJson),
        Some("email" -> t.email.asJson),
        Some("tickets" -> t.tickets.asJson),
        Some("ticketStatus" -> t.ticketStatus.asJson),
        Some("ticketStatusView" -> t.ticketStatusView.asJson),
        Some("details" -> t.details.asJson),
        Some("services" -> t.services.asJson),
        Some("AFFILIATE_AMOUNT" -> t.AFFILIATE_AMOUNT.asJson)
      ).flatten
    }
  }
  given decoderHome: Decoder[Home] = Decoder.instance { c =>
    for {
      last_login_ip <- c.downField("last_login_ip").as[String]
      last_login <- c.downField("last_login").as[String]
      currency <- c.downField("currency").as[String]
      amount <- c.downField("amount").as[String]
      invoice_list <- c.downField("invoice_list").as[Int]
      balance <- c.downField("balance").as[String]
      full_name <- c.downField("full_name").as[String]
      email <- c.downField("email").as[String]
      tickets <- c.downField("tickets").as[Seq[String]]
      ticketStatus <- c.downField("ticketStatus").as[HomeTicketStatus]
      ticketStatusView <- c.downField("ticketStatusView").as[HomeTicketStatusView]
      details <- c.downField("details").as[HomeDetails]
      services <- c.downField("services").as[HomeServices]
      AFFILIATE_AMOUNT <- c.downField("AFFILIATE_AMOUNT").as[String]
    } yield Home(
      last_login_ip = last_login_ip,
      last_login = last_login,
      currency = currency,
      amount = amount,
      invoice_list = invoice_list,
      balance = balance,
      full_name = full_name,
      email = email,
      tickets = tickets,
      ticketStatus = ticketStatus,
      ticketStatusView = ticketStatusView,
      details = details,
      services = services,
      AFFILIATE_AMOUNT = AFFILIATE_AMOUNT
    )
  }
}

