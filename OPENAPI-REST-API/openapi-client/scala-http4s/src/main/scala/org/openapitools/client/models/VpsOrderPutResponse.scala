/** InterServer Management API
  * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
  *
  * The version of the OpenAPI document: 0.9.0
  * Contact: support@interserver.net
  *
  * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
  * https://openapi-generator.tech
  * Do not edit the class manually.
  */
package org.openapitools.client.models

import io.circe.*
import io.circe.syntax.*
import io.circe.{Decoder, Encoder}

import scala.collection.immutable.Seq

/** Response from VPS order validation request
  * @param `continue` 
  * @param errors 
  * @param coupon_code 
  * @param service_cost 
  * @param slice_cost 
  * @param service_type 
  * @param repeat_slice_cost 
  * @param original_slice_cost 
  * @param original_cost 
  * @param repeat_service_cost 
  * @param monthly_service_cost 
  * @param custid 
  * @param os 
  * @param slices 
  * @param platform 
  * @param controlpanel 
  * @param period 
  * @param location 
  * @param version 
  * @param hostname 
  * @param coupon 
  * @param rootpass 
  */
case class VpsOrderPutResponse(
    `continue`: Option[Boolean] = None,
    errors: Option[Seq[AnyType]] = None,
    coupon_code: Option[Int] = None,
    service_cost: Option[Int] = None,
    slice_cost: Option[Int] = None,
    service_type: Option[Int] = None,
    repeat_slice_cost: Option[Int] = None,
    original_slice_cost: Option[Int] = None,
    original_cost: Option[Int] = None,
    repeat_service_cost: Option[Int] = None,
    monthly_service_cost: Option[Int] = None,
    custid: Option[String] = None,
    os: Option[String] = None,
    slices: Option[String] = None,
    platform: Option[String] = None,
    controlpanel: Option[String] = None,
    period: Option[Int] = None,
    location: Option[Int] = None,
    version: Option[String] = None,
    hostname: Option[String] = None,
    coupon: Option[String] = None,
    rootpass: Option[String] = None
)
  
object VpsOrderPutResponse {
  given encoderVpsOrderPutResponse: Encoder[VpsOrderPutResponse] = Encoder.instance { t =>
    Json.fromFields{
      Seq(
        t.`continue`.map(v => "continue" -> v.asJson),
        t.errors.map(v => "errors" -> v.asJson),
        t.coupon_code.map(v => "coupon_code" -> v.asJson),
        t.service_cost.map(v => "service_cost" -> v.asJson),
        t.slice_cost.map(v => "slice_cost" -> v.asJson),
        t.service_type.map(v => "service_type" -> v.asJson),
        t.repeat_slice_cost.map(v => "repeat_slice_cost" -> v.asJson),
        t.original_slice_cost.map(v => "original_slice_cost" -> v.asJson),
        t.original_cost.map(v => "original_cost" -> v.asJson),
        t.repeat_service_cost.map(v => "repeat_service_cost" -> v.asJson),
        t.monthly_service_cost.map(v => "monthly_service_cost" -> v.asJson),
        t.custid.map(v => "custid" -> v.asJson),
        t.os.map(v => "os" -> v.asJson),
        t.slices.map(v => "slices" -> v.asJson),
        t.platform.map(v => "platform" -> v.asJson),
        t.controlpanel.map(v => "controlpanel" -> v.asJson),
        t.period.map(v => "period" -> v.asJson),
        t.location.map(v => "location" -> v.asJson),
        t.version.map(v => "version" -> v.asJson),
        t.hostname.map(v => "hostname" -> v.asJson),
        t.coupon.map(v => "coupon" -> v.asJson),
        t.rootpass.map(v => "rootpass" -> v.asJson)
      ).flatten
    }
  }
  given decoderVpsOrderPutResponse: Decoder[VpsOrderPutResponse] = Decoder.instance { c =>
    for {
      `continue` <- c.downField("continue").as[Option[Boolean]]
      errors <- c.downField("errors").as[Option[Seq[AnyType]]]
      coupon_code <- c.downField("coupon_code").as[Option[Int]]
      service_cost <- c.downField("service_cost").as[Option[Int]]
      slice_cost <- c.downField("slice_cost").as[Option[Int]]
      service_type <- c.downField("service_type").as[Option[Int]]
      repeat_slice_cost <- c.downField("repeat_slice_cost").as[Option[Int]]
      original_slice_cost <- c.downField("original_slice_cost").as[Option[Int]]
      original_cost <- c.downField("original_cost").as[Option[Int]]
      repeat_service_cost <- c.downField("repeat_service_cost").as[Option[Int]]
      monthly_service_cost <- c.downField("monthly_service_cost").as[Option[Int]]
      custid <- c.downField("custid").as[Option[String]]
      os <- c.downField("os").as[Option[String]]
      slices <- c.downField("slices").as[Option[String]]
      platform <- c.downField("platform").as[Option[String]]
      controlpanel <- c.downField("controlpanel").as[Option[String]]
      period <- c.downField("period").as[Option[Int]]
      location <- c.downField("location").as[Option[Int]]
      version <- c.downField("version").as[Option[String]]
      hostname <- c.downField("hostname").as[Option[String]]
      coupon <- c.downField("coupon").as[Option[String]]
      rootpass <- c.downField("rootpass").as[Option[String]]
    } yield VpsOrderPutResponse(
      `continue` = `continue`,
      errors = errors,
      coupon_code = coupon_code,
      service_cost = service_cost,
      slice_cost = slice_cost,
      service_type = service_type,
      repeat_slice_cost = repeat_slice_cost,
      original_slice_cost = original_slice_cost,
      original_cost = original_cost,
      repeat_service_cost = repeat_service_cost,
      monthly_service_cost = monthly_service_cost,
      custid = custid,
      os = os,
      slices = slices,
      platform = platform,
      controlpanel = controlpanel,
      period = period,
      location = location,
      version = version,
      hostname = hostname,
      coupon = coupon,
      rootpass = rootpass
    )
  }
}

