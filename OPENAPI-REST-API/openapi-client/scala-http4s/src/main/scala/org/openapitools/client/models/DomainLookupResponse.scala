/** InterServer Management API
  * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
  *
  * The version of the OpenAPI document: 0.9.0
  * Contact: support@interserver.net
  *
  * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
  * https://openapi-generator.tech
  * Do not edit the class manually.
  */
package org.openapitools.client.models

import io.circe.*
import io.circe.syntax.*
import io.circe.{Decoder, DecodingFailure, Encoder}
import cats.syntax.functor.*

import io.circe.Json

/** Availability, pricing, and order-field metadata for a domain lookup.
  * @param available Whether the domain is available to register.
  * @param premium Whether the domain is marked as premium by the registrar.
  * @param website Indicates if the domain is already used by a website service.
  * @param domain_service Indicates if the domain already exists as a domain service on the account.
  * @param service Service catalog details for the domain's TLD.
  * @param whois_privacy Whether Whois privacy is available for the TLD.
  * @param `new` Calculated registration price, when available.
  * @param renewal Calculated renewal price, when available.
  * @param transfer Calculated transfer price, when available.
  * @param fields Registrar field requirements for this domain/TLD.
  * @param currencies Pricing information normalized to supported currencies.
  */
case class DomainLookupResponse(
    available: Option[Boolean] = None,
    premium: Option[Boolean] = None,
    website: Option[Boolean] = None,
    domain_service: Option[Boolean] = None,
    service: Option[Json] = None,
    whois_privacy: Option[Boolean] = None,
    `new`: Option[String] = None,
    renewal: Option[String] = None,
    transfer: Option[String] = None,
    fields: Option[Json] = None,
    currencies: Option[Json] = None
)
  
object DomainLookupResponse {
  given encoderDomainLookupResponse: Encoder[DomainLookupResponse] = Encoder.instance { t =>
    Json.fromFields{
      Seq(
        t.available.map(v => "available" -> v.asJson),
        t.premium.map(v => "premium" -> v.asJson),
        t.website.map(v => "website" -> v.asJson),
        t.domain_service.map(v => "domain_service" -> v.asJson),
        t.service.map(v => "service" -> v.asJson),
        t.whois_privacy.map(v => "whois_privacy" -> v.asJson),
        t.`new`.map(v => "new" -> v.asJson),
        t.renewal.map(v => "renewal" -> v.asJson),
        t.transfer.map(v => "transfer" -> v.asJson),
        t.fields.map(v => "fields" -> v.asJson),
        t.currencies.map(v => "currencies" -> v.asJson)
      ).flatten
    }
  }
  given decoderDomainLookupResponse: Decoder[DomainLookupResponse] = Decoder.instance { c =>
    for {
      available <- c.downField("available").as[Option[Boolean]]
      premium <- c.downField("premium").as[Option[Boolean]]
      website <- c.downField("website").as[Option[Boolean]]
      domain_service <- c.downField("domain_service").as[Option[Boolean]]
      service <- c.downField("service").as[Option[Json]]
      whois_privacy <- c.downField("whois_privacy").as[Option[Boolean]]
      `new` <- c.downField("new").as[Option[String]]
      renewal <- c.downField("renewal").as[Option[String]]
      transfer <- c.downField("transfer").as[Option[String]]
      fields <- c.downField("fields").as[Option[Json]]
      currencies <- c.downField("currencies").as[Option[Json]]
    } yield DomainLookupResponse(
      available = available,
      premium = premium,
      website = website,
      domain_service = domain_service,
      service = service,
      whois_privacy = whois_privacy,
      `new` = `new`,
      renewal = renewal,
      transfer = transfer,
      fields = fields,
      currencies = currencies
    )
  }
}

