//
// InitiatePayment200Response.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct InitiatePayment200Response: Codable {
    public enum ModelType: String, Codable, CaseIterable {
        case redirect = "redirect"
        case submit = "submit"
        case single = "single"
    }
    /// The response type indicating how to handle the payment. Possible values: `redirect` (redirect user to a URL), `submit` (submit a form to a URL), `single` (immediate result).
    public var type: ModelType?
    /// URL to redirect the user to for payment (when type is `redirect`).
    public var redirect: String?
    /// Form action URL (when type is `submit`).
    public var action: String?
    /// HTTP method for the form submission (when type is `submit`).
    public var method: String?
    /// Form field name-value pairs to submit (when type is `submit`).
    public var items: [String: Any]?
    /// Status or result text.
    public var text: String?

    public init(type: ModelType? = nil, redirect: String? = nil, action: String? = nil, method: String? = nil, items: [String: Any]? = nil, text: String? = nil) {
        self.type = type
        self.redirect = redirect
        self.action = action
        self.method = method
        self.items = items
        self.text = text
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case type
        case redirect
        case action
        case method
        case items
        case text
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        type = try container.decodeIfPresent(ModelType.self, forKey: .type)
        redirect = try container.decodeIfPresent(String.self, forKey: .redirect)
        action = try container.decodeIfPresent(String.self, forKey: .action)
        method = try container.decodeIfPresent(String.self, forKey: .method)
        items = try container.decodeIfPresent([String: Any].self, forKey: .items)
        text = try container.decodeIfPresent(String.self, forKey: .text)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(type, forKey: .type)
        try container.encodeIfPresent(redirect, forKey: .redirect)
        try container.encodeIfPresent(action, forKey: .action)
        try container.encodeIfPresent(method, forKey: .method)
        if let items = items {
            try container.encodeIfPresent(try JSONSerialization.data(withJSONObject: items), forKey: .items)
        }
        try container.encodeIfPresent(text, forKey: .text)
    }
}
