/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
/* tslint:disable:no-unused-variable member-ordering */

import { Observable } from 'rxjs/Observable';

import { map } from 'rxjs/operators';
import IHttpClient from '../IHttpClient';
import { inject, injectable } from 'inversify';
import { IAPIConfiguration } from '../IAPIConfiguration';
import { Headers } from '../Headers';
import HttpResponse from '../HttpResponse';

import { DnsListItem } from '../model/dnsListItem';
import { DnsRecord } from '../model/dnsRecord';
import { DnsRecordType } from '../model/dnsRecordType';
import { GetAccountInfo401Response } from '../model/getAccountInfo401Response';

import { COLLECTION_FORMATS }  from '../variables';



@injectable()
export class DNSService {
    private basePath: string = 'https://my.interserver.net/apiv2';

    constructor(@inject('IApiHttpClient') private httpClient: IHttpClient,
        @inject('IAPIConfiguration') private APIConfiguration: IAPIConfiguration ) {
        if(this.APIConfiguration.basePath)
            this.basePath = this.APIConfiguration.basePath;
    }

    /**
     * Create DNS Domain
     * Creates a new DNS domain and assigns an initial A record pointing to the supplied IP address. The domain is immediately available on InterServer\&#39;s DNS servers. Use &#x60;/dns/{id}&#x60; to manage records after creation.
     * @param domain The domain name.
     * @param ip IP Address to point the domain to.
     
     */
    public addDnsDomain(domain: string, ip: string, observe?: 'body', headers?: Headers): Observable<any>;
    public addDnsDomain(domain: string, ip: string, observe?: 'response', headers?: Headers): Observable<HttpResponse<any>>;
    public addDnsDomain(domain: string, ip: string, observe: any = 'body', headers: Headers = {}): Observable<any> {
        if (domain === null || domain === undefined){
            throw new Error('Required parameter domain was null or undefined when calling addDnsDomain.');
        }

        if (ip === null || ip === undefined){
            throw new Error('Required parameter ip was null or undefined when calling addDnsDomain.');
        }

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.APIConfiguration.apiKeys && this.APIConfiguration.apiKeys['X-API-KEY']) {
            headers['X-API-KEY'] = this.APIConfiguration.apiKeys['X-API-KEY'];
        }
        // authentication (sessionIdHeaderAuth) required
        if (this.APIConfiguration.apiKeys && this.APIConfiguration.apiKeys['sessionid']) {
            headers['sessionid'] = this.APIConfiguration.apiKeys['sessionid'];
        }
        headers['Accept'] = 'application/json';

        let formData: FormData = new FormData();
        headers['Content-Type'] = 'multipart/form-data';
        if (domain !== undefined) {
            formData.append('domain', <any>domain);
        }
        if (ip !== undefined) {
            formData.append('ip', <any>ip);
        }

        const response: Observable<HttpResponse<any>> = this.httpClient.post(`${this.basePath}/dns`, formData, headers);
        if (observe === 'body') {
               return response.pipe(
                   map((httpResponse: HttpResponse) => <any>(httpResponse.response))
               );
        }
        return response;
    }


    /**
     * Add DNS Record to Domain
     * Adds a new DNS record to the specified domain. Provide the record type (A, AAAA, CNAME, MX, TXT, etc.), name, content, TTL, and priority. The record takes effect on the DNS servers immediately. Use &#x60;GET /dns/{id}&#x60; afterward to confirm the record was created.
     * @param id The DNS Domain ID.
     * @param name Name part of record
     * @param type 
     * @param content Content of record
     * @param ttl Time-to-live
     * @param prio Priority
     
     */
    public addDnsRecord(id: string, name: string, type: DnsRecordType, content: string, ttl?: number, prio?: number, observe?: 'body', headers?: Headers): Observable<any>;
    public addDnsRecord(id: string, name: string, type: DnsRecordType, content: string, ttl?: number, prio?: number, observe?: 'response', headers?: Headers): Observable<HttpResponse<any>>;
    public addDnsRecord(id: string, name: string, type: DnsRecordType, content: string, ttl?: number, prio?: number, observe: any = 'body', headers: Headers = {}): Observable<any> {
        if (id === null || id === undefined){
            throw new Error('Required parameter id was null or undefined when calling addDnsRecord.');
        }

        if (name === null || name === undefined){
            throw new Error('Required parameter name was null or undefined when calling addDnsRecord.');
        }

        if (type === null || type === undefined){
            throw new Error('Required parameter type was null or undefined when calling addDnsRecord.');
        }

        if (content === null || content === undefined){
            throw new Error('Required parameter content was null or undefined when calling addDnsRecord.');
        }

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.APIConfiguration.apiKeys && this.APIConfiguration.apiKeys['X-API-KEY']) {
            headers['X-API-KEY'] = this.APIConfiguration.apiKeys['X-API-KEY'];
        }
        // authentication (sessionIdHeaderAuth) required
        if (this.APIConfiguration.apiKeys && this.APIConfiguration.apiKeys['sessionid']) {
            headers['sessionid'] = this.APIConfiguration.apiKeys['sessionid'];
        }
        headers['Accept'] = 'application/json';

        let formData: FormData = new FormData();
        headers['Content-Type'] = 'multipart/form-data';
        if (name !== undefined) {
            formData.append('name', <any>name);
        }
        if (type !== undefined) {
            formData.append('type', <any>type);
        }
        if (content !== undefined) {
            formData.append('content', <any>content);
        }
        if (ttl !== undefined) {
            formData.append('ttl', <any>ttl);
        }
        if (prio !== undefined) {
            formData.append('prio', <any>prio);
        }

        const response: Observable<HttpResponse<any>> = this.httpClient.post(`${this.basePath}/dns/${encodeURIComponent(String(id))}`, formData, headers);
        if (observe === 'body') {
               return response.pipe(
                   map((httpResponse: HttpResponse) => <any>(httpResponse.response))
               );
        }
        return response;
    }


    /**
     * Delete DNS Domain
     * Deletes a DNS domain and all of its associated records from the DNS servers. This action is permanent and cannot be undone. Any services relying on these DNS records will be affected immediately.
     * @param id The DNS domain ID to delete. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain.
     
     */
    public deleteDnsDomain(id: string, observe?: 'body', headers?: Headers): Observable<any>;
    public deleteDnsDomain(id: string, observe?: 'response', headers?: Headers): Observable<HttpResponse<any>>;
    public deleteDnsDomain(id: string, observe: any = 'body', headers: Headers = {}): Observable<any> {
        if (id === null || id === undefined){
            throw new Error('Required parameter id was null or undefined when calling deleteDnsDomain.');
        }

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.APIConfiguration.apiKeys && this.APIConfiguration.apiKeys['X-API-KEY']) {
            headers['X-API-KEY'] = this.APIConfiguration.apiKeys['X-API-KEY'];
        }
        // authentication (sessionIdHeaderAuth) required
        if (this.APIConfiguration.apiKeys && this.APIConfiguration.apiKeys['sessionid']) {
            headers['sessionid'] = this.APIConfiguration.apiKeys['sessionid'];
        }
        headers['Accept'] = 'application/json';

        const response: Observable<HttpResponse<any>> = this.httpClient.delete(`${this.basePath}/dns/${encodeURIComponent(String(id))}`, headers);
        if (observe === 'body') {
               return response.pipe(
                   map((httpResponse: HttpResponse) => <any>(httpResponse.response))
               );
        }
        return response;
    }


    /**
     * Delete DNS Record
     * Removes a DNS record from the specified domain. The deletion takes effect on the DNS servers immediately. Use &#x60;GET /dns/{id}&#x60; to verify the record has been removed.
     * @param domainId The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain.
     * @param recordId The DNS record ID within the domain. Use the record &#x60;id&#x60; from &#x60;GET /dns/{id}&#x60; to identify the record.
     
     */
    public deleteDnsRecord(domainId: number, recordId: number, observe?: 'body', headers?: Headers): Observable<any>;
    public deleteDnsRecord(domainId: number, recordId: number, observe?: 'response', headers?: Headers): Observable<HttpResponse<any>>;
    public deleteDnsRecord(domainId: number, recordId: number, observe: any = 'body', headers: Headers = {}): Observable<any> {
        if (domainId === null || domainId === undefined){
            throw new Error('Required parameter domainId was null or undefined when calling deleteDnsRecord.');
        }

        if (recordId === null || recordId === undefined){
            throw new Error('Required parameter recordId was null or undefined when calling deleteDnsRecord.');
        }

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.APIConfiguration.apiKeys && this.APIConfiguration.apiKeys['X-API-KEY']) {
            headers['X-API-KEY'] = this.APIConfiguration.apiKeys['X-API-KEY'];
        }
        // authentication (sessionIdHeaderAuth) required
        if (this.APIConfiguration.apiKeys && this.APIConfiguration.apiKeys['sessionid']) {
            headers['sessionid'] = this.APIConfiguration.apiKeys['sessionid'];
        }
        headers['Accept'] = 'application/json';

        const response: Observable<HttpResponse<any>> = this.httpClient.delete(`${this.basePath}/dns/${encodeURIComponent(String(domainId))}/${encodeURIComponent(String(recordId))}`, headers);
        if (observe === 'body') {
               return response.pipe(
                   map((httpResponse: HttpResponse) => <any>(httpResponse.response))
               );
        }
        return response;
    }


    /**
     * List Domain DNS Records
     * Returns the full set of DNS records for the specified domain, including NS, A, AAAA, CNAME, MX, TXT, and other record types. Use the record &#x60;id&#x60; values with &#x60;/dns/{domainId}/{recordId}&#x60; to update or delete individual records.
     * @param id The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain.
     
     */
    public getDnsDomain(id: number, observe?: 'body', headers?: Headers): Observable<Array<DnsRecord>>;
    public getDnsDomain(id: number, observe?: 'response', headers?: Headers): Observable<HttpResponse<Array<DnsRecord>>>;
    public getDnsDomain(id: number, observe: any = 'body', headers: Headers = {}): Observable<any> {
        if (id === null || id === undefined){
            throw new Error('Required parameter id was null or undefined when calling getDnsDomain.');
        }

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.APIConfiguration.apiKeys && this.APIConfiguration.apiKeys['X-API-KEY']) {
            headers['X-API-KEY'] = this.APIConfiguration.apiKeys['X-API-KEY'];
        }
        // authentication (sessionIdHeaderAuth) required
        if (this.APIConfiguration.apiKeys && this.APIConfiguration.apiKeys['sessionid']) {
            headers['sessionid'] = this.APIConfiguration.apiKeys['sessionid'];
        }
        headers['Accept'] = 'application/json';

        const response: Observable<HttpResponse<Array<DnsRecord>>> = this.httpClient.get(`${this.basePath}/dns/${encodeURIComponent(String(id))}`, headers);
        if (observe === 'body') {
               return response.pipe(
                   map((httpResponse: HttpResponse) => <Array<DnsRecord>>(httpResponse.response))
               );
        }
        return response;
    }


    /**
     * List DNS Domains
     * Returns the DNS domains on your account along with their primary A record content. Use the &#x60;id&#x60; from each entry with &#x60;/dns/{id}&#x60; to retrieve the full record set, or to add, update, and delete individual records.
     
     */
    public getDnsList(observe?: 'body', headers?: Headers): Observable<Array<DnsListItem>>;
    public getDnsList(observe?: 'response', headers?: Headers): Observable<HttpResponse<Array<DnsListItem>>>;
    public getDnsList(observe: any = 'body', headers: Headers = {}): Observable<any> {
        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.APIConfiguration.apiKeys && this.APIConfiguration.apiKeys['X-API-KEY']) {
            headers['X-API-KEY'] = this.APIConfiguration.apiKeys['X-API-KEY'];
        }
        // authentication (sessionIdHeaderAuth) required
        if (this.APIConfiguration.apiKeys && this.APIConfiguration.apiKeys['sessionid']) {
            headers['sessionid'] = this.APIConfiguration.apiKeys['sessionid'];
        }
        headers['Accept'] = 'application/json';

        const response: Observable<HttpResponse<Array<DnsListItem>>> = this.httpClient.get(`${this.basePath}/dns`, headers);
        if (observe === 'body') {
               return response.pipe(
                   map((httpResponse: HttpResponse) => <Array<DnsListItem>>(httpResponse.response))
               );
        }
        return response;
    }


    /**
     * Update DNS Record
     * Updates an existing DNS record with new values. Use &#x60;GET /dns/{id}&#x60; to list records and retrieve the record IDs before updating. Changes propagate to the DNS servers immediately.
     * @param domainId The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain.
     * @param recordId The DNS record ID within the domain. Use the record &#x60;id&#x60; from &#x60;GET /dns/{id}&#x60; to identify the record.
     * @param name 
     * @param type 
     * @param content 
     * @param ttl 
     * @param prio 
     * @param disabled 
     * @param ordername 
     * @param auth 
     
     */
    public updateDnsRecord(domainId: number, recordId: number, name?: string, type?: DnsRecordType, content?: string, ttl?: string, prio?: string, disabled?: string, ordername?: string, auth?: string, observe?: 'body', headers?: Headers): Observable<any>;
    public updateDnsRecord(domainId: number, recordId: number, name?: string, type?: DnsRecordType, content?: string, ttl?: string, prio?: string, disabled?: string, ordername?: string, auth?: string, observe?: 'response', headers?: Headers): Observable<HttpResponse<any>>;
    public updateDnsRecord(domainId: number, recordId: number, name?: string, type?: DnsRecordType, content?: string, ttl?: string, prio?: string, disabled?: string, ordername?: string, auth?: string, observe: any = 'body', headers: Headers = {}): Observable<any> {
        if (domainId === null || domainId === undefined){
            throw new Error('Required parameter domainId was null or undefined when calling updateDnsRecord.');
        }

        if (recordId === null || recordId === undefined){
            throw new Error('Required parameter recordId was null or undefined when calling updateDnsRecord.');
        }

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.APIConfiguration.apiKeys && this.APIConfiguration.apiKeys['X-API-KEY']) {
            headers['X-API-KEY'] = this.APIConfiguration.apiKeys['X-API-KEY'];
        }
        // authentication (sessionIdHeaderAuth) required
        if (this.APIConfiguration.apiKeys && this.APIConfiguration.apiKeys['sessionid']) {
            headers['sessionid'] = this.APIConfiguration.apiKeys['sessionid'];
        }
        headers['Accept'] = 'application/json';

        let formData: FormData = new FormData();
        headers['Content-Type'] = 'multipart/form-data';
        if (name !== undefined) {
            formData.append('name', <any>name);
        }
        if (type !== undefined) {
            formData.append('type', <any>type);
        }
        if (content !== undefined) {
            formData.append('content', <any>content);
        }
        if (ttl !== undefined) {
            formData.append('ttl', <any>ttl);
        }
        if (prio !== undefined) {
            formData.append('prio', <any>prio);
        }
        if (disabled !== undefined) {
            formData.append('disabled', <any>disabled);
        }
        if (ordername !== undefined) {
            formData.append('ordername', <any>ordername);
        }
        if (auth !== undefined) {
            formData.append('auth', <any>auth);
        }

        const response: Observable<HttpResponse<any>> = this.httpClient.post(`${this.basePath}/dns/${encodeURIComponent(String(domainId))}/${encodeURIComponent(String(recordId))}`, formData, headers);
        if (observe === 'body') {
               return response.pipe(
                   map((httpResponse: HttpResponse) => <any>(httpResponse.response))
               );
        }
        return response;
    }

}
