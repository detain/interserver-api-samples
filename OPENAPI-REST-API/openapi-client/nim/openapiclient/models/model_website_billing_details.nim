#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_object

type WebsiteBillingDetails* = object
  ## Billing information for a webhosting service including payment status, billing cycle, and cost.
  serviceLastInvoiceDate*: Option[string] ## Last invoice date for the service
  servicePaymentStatus*: Option[string] ## Payment status for the service
  serviceFrequency*: Option[string] ## Frequency of the service
  nextDate*: Option[string] ## Next date for the service
  serviceNextInvoiceDate*: Option[string] ## Next invoice date for the service
  serviceCurrency*: Option[string] ## Currency for the service
  serviceCurrencySymbol*: Option[string] ## Currency symbol for the service
  serviceCoupon*: Option[string] ## Coupon for the service
  serviceCostInfo*: Option[string] ## Cost information for the service
  serviceExtra*: Option[JsonNode] ## Extra information for the service
  serviceExtraJson*: Option[string] ## Extra information in JSON format for the service


# Custom JSON deserialization for WebsiteBillingDetails with custom field names
proc to*(node: JsonNode, T: typedesc[WebsiteBillingDetails]): WebsiteBillingDetails =
  result = WebsiteBillingDetails()
  if node.kind == JObject:
    if node.hasKey("service_last_invoice_date") and node["service_last_invoice_date"].kind != JNull:
      result.serviceLastInvoiceDate = some(to(node["service_last_invoice_date"], typeof(result.serviceLastInvoiceDate.get())))
    if node.hasKey("service_payment_status") and node["service_payment_status"].kind != JNull:
      result.servicePaymentStatus = some(to(node["service_payment_status"], typeof(result.servicePaymentStatus.get())))
    if node.hasKey("service_frequency") and node["service_frequency"].kind != JNull:
      result.serviceFrequency = some(to(node["service_frequency"], typeof(result.serviceFrequency.get())))
    if node.hasKey("next_date") and node["next_date"].kind != JNull:
      result.nextDate = some(to(node["next_date"], typeof(result.nextDate.get())))
    if node.hasKey("service_next_invoice_date") and node["service_next_invoice_date"].kind != JNull:
      result.serviceNextInvoiceDate = some(to(node["service_next_invoice_date"], typeof(result.serviceNextInvoiceDate.get())))
    if node.hasKey("service_currency") and node["service_currency"].kind != JNull:
      result.serviceCurrency = some(to(node["service_currency"], typeof(result.serviceCurrency.get())))
    if node.hasKey("service_currency_symbol") and node["service_currency_symbol"].kind != JNull:
      result.serviceCurrencySymbol = some(to(node["service_currency_symbol"], typeof(result.serviceCurrencySymbol.get())))
    if node.hasKey("service_coupon") and node["service_coupon"].kind != JNull:
      result.serviceCoupon = some(to(node["service_coupon"], typeof(result.serviceCoupon.get())))
    if node.hasKey("service_cost_info") and node["service_cost_info"].kind != JNull:
      result.serviceCostInfo = some(to(node["service_cost_info"], typeof(result.serviceCostInfo.get())))
    if node.hasKey("service_extra") and node["service_extra"].kind != JNull:
      result.serviceExtra = some(to(node["service_extra"], typeof(result.serviceExtra.get())))
    if node.hasKey("service_extra_json") and node["service_extra_json"].kind != JNull:
      result.serviceExtraJson = some(to(node["service_extra_json"], typeof(result.serviceExtraJson.get())))

# Custom JSON serialization for WebsiteBillingDetails with custom field names
proc `%`*(obj: WebsiteBillingDetails): JsonNode =
  result = newJObject()
  if obj.serviceLastInvoiceDate.isSome():
    result["service_last_invoice_date"] = %obj.serviceLastInvoiceDate.get()
  if obj.servicePaymentStatus.isSome():
    result["service_payment_status"] = %obj.servicePaymentStatus.get()
  if obj.serviceFrequency.isSome():
    result["service_frequency"] = %obj.serviceFrequency.get()
  if obj.nextDate.isSome():
    result["next_date"] = %obj.nextDate.get()
  if obj.serviceNextInvoiceDate.isSome():
    result["service_next_invoice_date"] = %obj.serviceNextInvoiceDate.get()
  if obj.serviceCurrency.isSome():
    result["service_currency"] = %obj.serviceCurrency.get()
  if obj.serviceCurrencySymbol.isSome():
    result["service_currency_symbol"] = %obj.serviceCurrencySymbol.get()
  if obj.serviceCoupon.isSome():
    result["service_coupon"] = %obj.serviceCoupon.get()
  if obj.serviceCostInfo.isSome():
    result["service_cost_info"] = %obj.serviceCostInfo.get()
  if obj.serviceExtra.isSome():
    result["service_extra"] = %obj.serviceExtra.get()
  if obj.serviceExtraJson.isSome():
    result["service_extra_json"] = %obj.serviceExtraJson.get()

