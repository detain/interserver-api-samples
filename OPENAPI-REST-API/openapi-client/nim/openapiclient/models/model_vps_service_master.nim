#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type VpsServiceMaster* = object
  ## Information about the host node (hypervisor) running this VPS, including hardware specs and resource utilization.
  vpsId*: Option[string] ## VPS ID
  vpsName*: Option[string] ## VPS name
  vpsIp*: Option[string] ## IP address of the VPS
  vpsType*: Option[string] ## VPS type
  vpsHdsize*: Option[string] ## Hard drive size
  vpsHdfree*: Option[string] ## Free hard drive space
  vpsBits*: Option[string] ## Bits
  vpsLoad*: Option[string] ## CPU load
  vpsRam*: Option[string] ## RAM
  vpsCpuModel*: Option[string] ## CPU model
  vpsCpuMhz*: Option[string] ## CPU frequency in MHz
  vpsLocation*: Option[string] ## Location of the VPS
  vpsLastUpdate*: Option[string] ## Last update date
  vpsRaidBuilding*: Option[string] ## RAID building status
  vpsKernel*: Option[string] ## Kernel version
  vpsAvailable*: Option[string] ## Available
  vpsCores*: Option[string] ## Number of CPU cores
  vpsIowait*: Option[string] ## I/O wait
  vpsRaidStatus*: Option[string] ## RAID status
  vpsMounts*: Option[string] ## Mounts
  vpsServerMax*: Option[string] ## Maximum number of servers
  vpsServerMaxSlices*: Option[string] ## Maximum number of server slices
  vpsDriveType*: Option[string] ## Drive type
  vpsOrder*: Option[string] ## Order number


# Custom JSON deserialization for VpsServiceMaster with custom field names
proc to*(node: JsonNode, T: typedesc[VpsServiceMaster]): VpsServiceMaster =
  result = VpsServiceMaster()
  if node.kind == JObject:
    if node.hasKey("vps_id") and node["vps_id"].kind != JNull:
      result.vpsId = some(to(node["vps_id"], typeof(result.vpsId.get())))
    if node.hasKey("vps_name") and node["vps_name"].kind != JNull:
      result.vpsName = some(to(node["vps_name"], typeof(result.vpsName.get())))
    if node.hasKey("vps_ip") and node["vps_ip"].kind != JNull:
      result.vpsIp = some(to(node["vps_ip"], typeof(result.vpsIp.get())))
    if node.hasKey("vps_type") and node["vps_type"].kind != JNull:
      result.vpsType = some(to(node["vps_type"], typeof(result.vpsType.get())))
    if node.hasKey("vps_hdsize") and node["vps_hdsize"].kind != JNull:
      result.vpsHdsize = some(to(node["vps_hdsize"], typeof(result.vpsHdsize.get())))
    if node.hasKey("vps_hdfree") and node["vps_hdfree"].kind != JNull:
      result.vpsHdfree = some(to(node["vps_hdfree"], typeof(result.vpsHdfree.get())))
    if node.hasKey("vps_bits") and node["vps_bits"].kind != JNull:
      result.vpsBits = some(to(node["vps_bits"], typeof(result.vpsBits.get())))
    if node.hasKey("vps_load") and node["vps_load"].kind != JNull:
      result.vpsLoad = some(to(node["vps_load"], typeof(result.vpsLoad.get())))
    if node.hasKey("vps_ram") and node["vps_ram"].kind != JNull:
      result.vpsRam = some(to(node["vps_ram"], typeof(result.vpsRam.get())))
    if node.hasKey("vps_cpu_model") and node["vps_cpu_model"].kind != JNull:
      result.vpsCpuModel = some(to(node["vps_cpu_model"], typeof(result.vpsCpuModel.get())))
    if node.hasKey("vps_cpu_mhz") and node["vps_cpu_mhz"].kind != JNull:
      result.vpsCpuMhz = some(to(node["vps_cpu_mhz"], typeof(result.vpsCpuMhz.get())))
    if node.hasKey("vps_location") and node["vps_location"].kind != JNull:
      result.vpsLocation = some(to(node["vps_location"], typeof(result.vpsLocation.get())))
    if node.hasKey("vps_last_update") and node["vps_last_update"].kind != JNull:
      result.vpsLastUpdate = some(to(node["vps_last_update"], typeof(result.vpsLastUpdate.get())))
    if node.hasKey("vps_raid_building") and node["vps_raid_building"].kind != JNull:
      result.vpsRaidBuilding = some(to(node["vps_raid_building"], typeof(result.vpsRaidBuilding.get())))
    if node.hasKey("vps_kernel") and node["vps_kernel"].kind != JNull:
      result.vpsKernel = some(to(node["vps_kernel"], typeof(result.vpsKernel.get())))
    if node.hasKey("vps_available") and node["vps_available"].kind != JNull:
      result.vpsAvailable = some(to(node["vps_available"], typeof(result.vpsAvailable.get())))
    if node.hasKey("vps_cores") and node["vps_cores"].kind != JNull:
      result.vpsCores = some(to(node["vps_cores"], typeof(result.vpsCores.get())))
    if node.hasKey("vps_iowait") and node["vps_iowait"].kind != JNull:
      result.vpsIowait = some(to(node["vps_iowait"], typeof(result.vpsIowait.get())))
    if node.hasKey("vps_raid_status") and node["vps_raid_status"].kind != JNull:
      result.vpsRaidStatus = some(to(node["vps_raid_status"], typeof(result.vpsRaidStatus.get())))
    if node.hasKey("vps_mounts") and node["vps_mounts"].kind != JNull:
      result.vpsMounts = some(to(node["vps_mounts"], typeof(result.vpsMounts.get())))
    if node.hasKey("vps_server_max") and node["vps_server_max"].kind != JNull:
      result.vpsServerMax = some(to(node["vps_server_max"], typeof(result.vpsServerMax.get())))
    if node.hasKey("vps_server_max_slices") and node["vps_server_max_slices"].kind != JNull:
      result.vpsServerMaxSlices = some(to(node["vps_server_max_slices"], typeof(result.vpsServerMaxSlices.get())))
    if node.hasKey("vps_drive_type") and node["vps_drive_type"].kind != JNull:
      result.vpsDriveType = some(to(node["vps_drive_type"], typeof(result.vpsDriveType.get())))
    if node.hasKey("vps_order") and node["vps_order"].kind != JNull:
      result.vpsOrder = some(to(node["vps_order"], typeof(result.vpsOrder.get())))

# Custom JSON serialization for VpsServiceMaster with custom field names
proc `%`*(obj: VpsServiceMaster): JsonNode =
  result = newJObject()
  if obj.vpsId.isSome():
    result["vps_id"] = %obj.vpsId.get()
  if obj.vpsName.isSome():
    result["vps_name"] = %obj.vpsName.get()
  if obj.vpsIp.isSome():
    result["vps_ip"] = %obj.vpsIp.get()
  if obj.vpsType.isSome():
    result["vps_type"] = %obj.vpsType.get()
  if obj.vpsHdsize.isSome():
    result["vps_hdsize"] = %obj.vpsHdsize.get()
  if obj.vpsHdfree.isSome():
    result["vps_hdfree"] = %obj.vpsHdfree.get()
  if obj.vpsBits.isSome():
    result["vps_bits"] = %obj.vpsBits.get()
  if obj.vpsLoad.isSome():
    result["vps_load"] = %obj.vpsLoad.get()
  if obj.vpsRam.isSome():
    result["vps_ram"] = %obj.vpsRam.get()
  if obj.vpsCpuModel.isSome():
    result["vps_cpu_model"] = %obj.vpsCpuModel.get()
  if obj.vpsCpuMhz.isSome():
    result["vps_cpu_mhz"] = %obj.vpsCpuMhz.get()
  if obj.vpsLocation.isSome():
    result["vps_location"] = %obj.vpsLocation.get()
  if obj.vpsLastUpdate.isSome():
    result["vps_last_update"] = %obj.vpsLastUpdate.get()
  if obj.vpsRaidBuilding.isSome():
    result["vps_raid_building"] = %obj.vpsRaidBuilding.get()
  if obj.vpsKernel.isSome():
    result["vps_kernel"] = %obj.vpsKernel.get()
  if obj.vpsAvailable.isSome():
    result["vps_available"] = %obj.vpsAvailable.get()
  if obj.vpsCores.isSome():
    result["vps_cores"] = %obj.vpsCores.get()
  if obj.vpsIowait.isSome():
    result["vps_iowait"] = %obj.vpsIowait.get()
  if obj.vpsRaidStatus.isSome():
    result["vps_raid_status"] = %obj.vpsRaidStatus.get()
  if obj.vpsMounts.isSome():
    result["vps_mounts"] = %obj.vpsMounts.get()
  if obj.vpsServerMax.isSome():
    result["vps_server_max"] = %obj.vpsServerMax.get()
  if obj.vpsServerMaxSlices.isSome():
    result["vps_server_max_slices"] = %obj.vpsServerMaxSlices.get()
  if obj.vpsDriveType.isSome():
    result["vps_drive_type"] = %obj.vpsDriveType.get()
  if obj.vpsOrder.isSome():
    result["vps_order"] = %obj.vpsOrder.get()

