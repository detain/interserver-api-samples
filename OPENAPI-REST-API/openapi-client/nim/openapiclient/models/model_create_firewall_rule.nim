#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type ProtocolId* {.pure.} = enum
  `1`
  `2`

type XdpAction* {.pure.} = enum
  `0`
  `1`

type CreateFirewallRule* = object
  ## Create firewall rule for your ip
  protocolId*: ProtocolId ## 1 = TCP, 2 = UDP
  xdpAction*: XdpAction ## 1 = Block,  0 = Whitelist
  destinationPort*: Option[int]
  sourceIp*: Option[string]
  sourcePort*: Option[int]

func `%`*(v: ProtocolId): JsonNode =
  result = case v:
    of ProtocolId.`1`: %"1"
    of ProtocolId.`2`: %"2"
func `$`*(v: ProtocolId): string =
  result = case v:
    of ProtocolId.`1`: $("1")
    of ProtocolId.`2`: $("2")

proc to*(node: JsonNode, T: typedesc[ProtocolId]): ProtocolId =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum ProtocolId, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("1"):
    return ProtocolId.`1`
  of $("2"):
    return ProtocolId.`2`
  else:
    raise newException(ValueError, "Invalid enum value for ProtocolId: " & strVal)

func `%`*(v: XdpAction): JsonNode =
  result = case v:
    of XdpAction.`0`: %"0"
    of XdpAction.`1`: %"1"
func `$`*(v: XdpAction): string =
  result = case v:
    of XdpAction.`0`: $("0")
    of XdpAction.`1`: $("1")

proc to*(node: JsonNode, T: typedesc[XdpAction]): XdpAction =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum XdpAction, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("0"):
    return XdpAction.`0`
  of $("1"):
    return XdpAction.`1`
  else:
    raise newException(ValueError, "Invalid enum value for XdpAction: " & strVal)


# Custom JSON deserialization for CreateFirewallRule with custom field names
proc to*(node: JsonNode, T: typedesc[CreateFirewallRule]): CreateFirewallRule =
  result = CreateFirewallRule()
  if node.kind == JObject:
    if node.hasKey("protocol_id"):
      result.protocolId = to(node["protocol_id"], ProtocolId)
    if node.hasKey("xdp_action"):
      result.xdpAction = to(node["xdp_action"], XdpAction)
    if node.hasKey("destination_port") and node["destination_port"].kind != JNull:
      result.destinationPort = some(to(node["destination_port"], typeof(result.destinationPort.get())))
    if node.hasKey("source_ip") and node["source_ip"].kind != JNull:
      result.sourceIp = some(to(node["source_ip"], typeof(result.sourceIp.get())))
    if node.hasKey("source_port") and node["source_port"].kind != JNull:
      result.sourcePort = some(to(node["source_port"], typeof(result.sourcePort.get())))

# Custom JSON serialization for CreateFirewallRule with custom field names
proc `%`*(obj: CreateFirewallRule): JsonNode =
  result = newJObject()
  result["protocol_id"] = %obj.protocolId
  result["xdp_action"] = %obj.xdpAction
  if obj.destinationPort.isSome():
    result["destination_port"] = %obj.destinationPort.get()
  if obj.sourceIp.isSome():
    result["source_ip"] = %obj.sourceIp.get()
  if obj.sourcePort.isSome():
    result["source_port"] = %obj.sourcePort.get()

