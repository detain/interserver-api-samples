#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type BackupServiceInfo* = object
  ## Core service record for a backup storage service including ID, status, quota, and billing details.
  backupId*: Option[string] ## Backup ID.
  backupServer*: Option[string] ## Backup server ID.
  backupUsername*: Option[string] ## Backup username.
  backupType*: Option[string] ## Backup type.
  backupCurrency*: Option[string] ## Backup currency.
  backupOrderDate*: Option[string] ## Backup order date.
  backupCustid*: Option[string] ## Backup customer ID.
  backupQuota*: Option[string] ## Backup quota.
  backupIp*: Option[string] ## Backup IP address.
  backupStatus*: Option[string] ## Backup status.
  backupInvoice*: Option[string] ## Backup invoice.
  backupCoupon*: Option[string] ## Backup coupon.
  backupExtra*: Option[string] ## Backup extra information.
  backupServerStatus*: Option[string] ## Backup server status.
  backupComment*: Option[string] ## Backup comment.


# Custom JSON deserialization for BackupServiceInfo with custom field names
proc to*(node: JsonNode, T: typedesc[BackupServiceInfo]): BackupServiceInfo =
  result = BackupServiceInfo()
  if node.kind == JObject:
    if node.hasKey("backup_id") and node["backup_id"].kind != JNull:
      result.backupId = some(to(node["backup_id"], typeof(result.backupId.get())))
    if node.hasKey("backup_server") and node["backup_server"].kind != JNull:
      result.backupServer = some(to(node["backup_server"], typeof(result.backupServer.get())))
    if node.hasKey("backup_username") and node["backup_username"].kind != JNull:
      result.backupUsername = some(to(node["backup_username"], typeof(result.backupUsername.get())))
    if node.hasKey("backup_type") and node["backup_type"].kind != JNull:
      result.backupType = some(to(node["backup_type"], typeof(result.backupType.get())))
    if node.hasKey("backup_currency") and node["backup_currency"].kind != JNull:
      result.backupCurrency = some(to(node["backup_currency"], typeof(result.backupCurrency.get())))
    if node.hasKey("backup_order_date") and node["backup_order_date"].kind != JNull:
      result.backupOrderDate = some(to(node["backup_order_date"], typeof(result.backupOrderDate.get())))
    if node.hasKey("backup_custid") and node["backup_custid"].kind != JNull:
      result.backupCustid = some(to(node["backup_custid"], typeof(result.backupCustid.get())))
    if node.hasKey("backup_quota") and node["backup_quota"].kind != JNull:
      result.backupQuota = some(to(node["backup_quota"], typeof(result.backupQuota.get())))
    if node.hasKey("backup_ip") and node["backup_ip"].kind != JNull:
      result.backupIp = some(to(node["backup_ip"], typeof(result.backupIp.get())))
    if node.hasKey("backup_status") and node["backup_status"].kind != JNull:
      result.backupStatus = some(to(node["backup_status"], typeof(result.backupStatus.get())))
    if node.hasKey("backup_invoice") and node["backup_invoice"].kind != JNull:
      result.backupInvoice = some(to(node["backup_invoice"], typeof(result.backupInvoice.get())))
    if node.hasKey("backup_coupon") and node["backup_coupon"].kind != JNull:
      result.backupCoupon = some(to(node["backup_coupon"], typeof(result.backupCoupon.get())))
    if node.hasKey("backup_extra") and node["backup_extra"].kind != JNull:
      result.backupExtra = some(to(node["backup_extra"], typeof(result.backupExtra.get())))
    if node.hasKey("backup_server_status") and node["backup_server_status"].kind != JNull:
      result.backupServerStatus = some(to(node["backup_server_status"], typeof(result.backupServerStatus.get())))
    if node.hasKey("backup_comment") and node["backup_comment"].kind != JNull:
      result.backupComment = some(to(node["backup_comment"], typeof(result.backupComment.get())))

# Custom JSON serialization for BackupServiceInfo with custom field names
proc `%`*(obj: BackupServiceInfo): JsonNode =
  result = newJObject()
  if obj.backupId.isSome():
    result["backup_id"] = %obj.backupId.get()
  if obj.backupServer.isSome():
    result["backup_server"] = %obj.backupServer.get()
  if obj.backupUsername.isSome():
    result["backup_username"] = %obj.backupUsername.get()
  if obj.backupType.isSome():
    result["backup_type"] = %obj.backupType.get()
  if obj.backupCurrency.isSome():
    result["backup_currency"] = %obj.backupCurrency.get()
  if obj.backupOrderDate.isSome():
    result["backup_order_date"] = %obj.backupOrderDate.get()
  if obj.backupCustid.isSome():
    result["backup_custid"] = %obj.backupCustid.get()
  if obj.backupQuota.isSome():
    result["backup_quota"] = %obj.backupQuota.get()
  if obj.backupIp.isSome():
    result["backup_ip"] = %obj.backupIp.get()
  if obj.backupStatus.isSome():
    result["backup_status"] = %obj.backupStatus.get()
  if obj.backupInvoice.isSome():
    result["backup_invoice"] = %obj.backupInvoice.get()
  if obj.backupCoupon.isSome():
    result["backup_coupon"] = %obj.backupCoupon.get()
  if obj.backupExtra.isSome():
    result["backup_extra"] = %obj.backupExtra.get()
  if obj.backupServerStatus.isSome():
    result["backup_server_status"] = %obj.backupServerStatus.get()
  if obj.backupComment.isSome():
    result["backup_comment"] = %obj.backupComment.get()

