#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type BackupServiceMaster* = object
  ## 
  backupId*: Option[int] ## Backup ID of the service master.
  backupName*: Option[string] ## Name of the backup service.
  backupIp*: Option[string] ## IP address of the backup service.
  backupType*: Option[int] ## Type of the backup service.
  backupHdsize*: Option[int] ## Size of the backup service's hard drive.
  backupHdfree*: Option[int] ## Amount of free space on the backup service's hard drive.
  backupLastUpdate*: Option[string] ## Last update timestamp of the backup service.
  backupAvailable*: Option[int] ## Availability status of the backup service.
  backupIowait*: Option[int] ## I/O wait status of the backup service.
  backupOrder*: Option[int] ## Order associated with the backup service.


# Custom JSON deserialization for BackupServiceMaster with custom field names
proc to*(node: JsonNode, T: typedesc[BackupServiceMaster]): BackupServiceMaster =
  result = BackupServiceMaster()
  if node.kind == JObject:
    if node.hasKey("backup_id") and node["backup_id"].kind != JNull:
      result.backupId = some(to(node["backup_id"], typeof(result.backupId.get())))
    if node.hasKey("backup_name") and node["backup_name"].kind != JNull:
      result.backupName = some(to(node["backup_name"], typeof(result.backupName.get())))
    if node.hasKey("backup_ip") and node["backup_ip"].kind != JNull:
      result.backupIp = some(to(node["backup_ip"], typeof(result.backupIp.get())))
    if node.hasKey("backup_type") and node["backup_type"].kind != JNull:
      result.backupType = some(to(node["backup_type"], typeof(result.backupType.get())))
    if node.hasKey("backup_hdsize") and node["backup_hdsize"].kind != JNull:
      result.backupHdsize = some(to(node["backup_hdsize"], typeof(result.backupHdsize.get())))
    if node.hasKey("backup_hdfree") and node["backup_hdfree"].kind != JNull:
      result.backupHdfree = some(to(node["backup_hdfree"], typeof(result.backupHdfree.get())))
    if node.hasKey("backup_last_update") and node["backup_last_update"].kind != JNull:
      result.backupLastUpdate = some(to(node["backup_last_update"], typeof(result.backupLastUpdate.get())))
    if node.hasKey("backup_available") and node["backup_available"].kind != JNull:
      result.backupAvailable = some(to(node["backup_available"], typeof(result.backupAvailable.get())))
    if node.hasKey("backup_iowait") and node["backup_iowait"].kind != JNull:
      result.backupIowait = some(to(node["backup_iowait"], typeof(result.backupIowait.get())))
    if node.hasKey("backup_order") and node["backup_order"].kind != JNull:
      result.backupOrder = some(to(node["backup_order"], typeof(result.backupOrder.get())))

# Custom JSON serialization for BackupServiceMaster with custom field names
proc `%`*(obj: BackupServiceMaster): JsonNode =
  result = newJObject()
  if obj.backupId.isSome():
    result["backup_id"] = %obj.backupId.get()
  if obj.backupName.isSome():
    result["backup_name"] = %obj.backupName.get()
  if obj.backupIp.isSome():
    result["backup_ip"] = %obj.backupIp.get()
  if obj.backupType.isSome():
    result["backup_type"] = %obj.backupType.get()
  if obj.backupHdsize.isSome():
    result["backup_hdsize"] = %obj.backupHdsize.get()
  if obj.backupHdfree.isSome():
    result["backup_hdfree"] = %obj.backupHdfree.get()
  if obj.backupLastUpdate.isSome():
    result["backup_last_update"] = %obj.backupLastUpdate.get()
  if obj.backupAvailable.isSome():
    result["backup_available"] = %obj.backupAvailable.get()
  if obj.backupIowait.isSome():
    result["backup_iowait"] = %obj.backupIowait.get()
  if obj.backupOrder.isSome():
    result["backup_order"] = %obj.backupOrder.get()

