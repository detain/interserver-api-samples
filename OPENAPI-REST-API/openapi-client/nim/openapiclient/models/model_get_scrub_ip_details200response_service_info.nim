#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type ScrubIpStatus* {.pure.} = enum
  Active
  Pending
  Canceled
  Expired

type GetScrubIpDetails200responseServiceInfo* = object
  ## 
  scrubIpId*: Option[string]
  scrubIpType*: Option[string]
  scrubIpCustid*: Option[string]
  scrubIpOrderDate*: Option[string]
  scrubIpIp*: Option[string]
  scrubIpServiceId*: Option[string]
  scrubIpServiceModule*: Option[string]
  scrubIpStatus*: Option[ScrubIpStatus]
  scrubIpInvoice*: Option[string]
  scrubIpCurrency*: Option[string]
  scrubIpCoupon*: Option[string]
  scrubIpComment*: Option[string]

func `%`*(v: ScrubIpStatus): JsonNode =
  result = case v:
    of ScrubIpStatus.Active: %"active"
    of ScrubIpStatus.Pending: %"pending"
    of ScrubIpStatus.Canceled: %"canceled"
    of ScrubIpStatus.Expired: %"expired"
func `$`*(v: ScrubIpStatus): string =
  result = case v:
    of ScrubIpStatus.Active: $("active")
    of ScrubIpStatus.Pending: $("pending")
    of ScrubIpStatus.Canceled: $("canceled")
    of ScrubIpStatus.Expired: $("expired")

proc to*(node: JsonNode, T: typedesc[ScrubIpStatus]): ScrubIpStatus =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum ScrubIpStatus, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("active"):
    return ScrubIpStatus.Active
  of $("pending"):
    return ScrubIpStatus.Pending
  of $("canceled"):
    return ScrubIpStatus.Canceled
  of $("expired"):
    return ScrubIpStatus.Expired
  else:
    raise newException(ValueError, "Invalid enum value for ScrubIpStatus: " & strVal)


# Custom JSON deserialization for GetScrubIpDetails200responseServiceInfo with custom field names
proc to*(node: JsonNode, T: typedesc[GetScrubIpDetails200responseServiceInfo]): GetScrubIpDetails200responseServiceInfo =
  result = GetScrubIpDetails200responseServiceInfo()
  if node.kind == JObject:
    if node.hasKey("scrub_ip_id") and node["scrub_ip_id"].kind != JNull:
      result.scrubIpId = some(to(node["scrub_ip_id"], typeof(result.scrubIpId.get())))
    if node.hasKey("scrub_ip_type") and node["scrub_ip_type"].kind != JNull:
      result.scrubIpType = some(to(node["scrub_ip_type"], typeof(result.scrubIpType.get())))
    if node.hasKey("scrub_ip_custid") and node["scrub_ip_custid"].kind != JNull:
      result.scrubIpCustid = some(to(node["scrub_ip_custid"], typeof(result.scrubIpCustid.get())))
    if node.hasKey("scrub_ip_order_date") and node["scrub_ip_order_date"].kind != JNull:
      result.scrubIpOrderDate = some(to(node["scrub_ip_order_date"], typeof(result.scrubIpOrderDate.get())))
    if node.hasKey("scrub_ip_ip") and node["scrub_ip_ip"].kind != JNull:
      result.scrubIpIp = some(to(node["scrub_ip_ip"], typeof(result.scrubIpIp.get())))
    if node.hasKey("scrub_ip_service_id") and node["scrub_ip_service_id"].kind != JNull:
      result.scrubIpServiceId = some(to(node["scrub_ip_service_id"], typeof(result.scrubIpServiceId.get())))
    if node.hasKey("scrub_ip_service_module") and node["scrub_ip_service_module"].kind != JNull:
      result.scrubIpServiceModule = some(to(node["scrub_ip_service_module"], typeof(result.scrubIpServiceModule.get())))
    if node.hasKey("scrub_ip_status") and node["scrub_ip_status"].kind != JNull:
      result.scrubIpStatus = some(to(node["scrub_ip_status"], ScrubIpStatus))
    if node.hasKey("scrub_ip_invoice") and node["scrub_ip_invoice"].kind != JNull:
      result.scrubIpInvoice = some(to(node["scrub_ip_invoice"], typeof(result.scrubIpInvoice.get())))
    if node.hasKey("scrub_ip_currency") and node["scrub_ip_currency"].kind != JNull:
      result.scrubIpCurrency = some(to(node["scrub_ip_currency"], typeof(result.scrubIpCurrency.get())))
    if node.hasKey("scrub_ip_coupon") and node["scrub_ip_coupon"].kind != JNull:
      result.scrubIpCoupon = some(to(node["scrub_ip_coupon"], typeof(result.scrubIpCoupon.get())))
    if node.hasKey("scrub_ip_comment") and node["scrub_ip_comment"].kind != JNull:
      result.scrubIpComment = some(to(node["scrub_ip_comment"], typeof(result.scrubIpComment.get())))

# Custom JSON serialization for GetScrubIpDetails200responseServiceInfo with custom field names
proc `%`*(obj: GetScrubIpDetails200responseServiceInfo): JsonNode =
  result = newJObject()
  if obj.scrubIpId.isSome():
    result["scrub_ip_id"] = %obj.scrubIpId.get()
  if obj.scrubIpType.isSome():
    result["scrub_ip_type"] = %obj.scrubIpType.get()
  if obj.scrubIpCustid.isSome():
    result["scrub_ip_custid"] = %obj.scrubIpCustid.get()
  if obj.scrubIpOrderDate.isSome():
    result["scrub_ip_order_date"] = %obj.scrubIpOrderDate.get()
  if obj.scrubIpIp.isSome():
    result["scrub_ip_ip"] = %obj.scrubIpIp.get()
  if obj.scrubIpServiceId.isSome():
    result["scrub_ip_service_id"] = %obj.scrubIpServiceId.get()
  if obj.scrubIpServiceModule.isSome():
    result["scrub_ip_service_module"] = %obj.scrubIpServiceModule.get()
  if obj.scrubIpStatus.isSome():
    result["scrub_ip_status"] = %obj.scrubIpStatus.get()
  if obj.scrubIpInvoice.isSome():
    result["scrub_ip_invoice"] = %obj.scrubIpInvoice.get()
  if obj.scrubIpCurrency.isSome():
    result["scrub_ip_currency"] = %obj.scrubIpCurrency.get()
  if obj.scrubIpCoupon.isSome():
    result["scrub_ip_coupon"] = %obj.scrubIpCoupon.get()
  if obj.scrubIpComment.isSome():
    result["scrub_ip_comment"] = %obj.scrubIpComment.get()

