#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_home_details
import model_home_services
import model_home_ticket_status
import model_home_ticket_status_view

type Home* = object
  ## 
  lastLoginIp*: string ## Last login IP.
  lastLogin*: string ## Last login time.
  currency*: string ## Currency symbol.
  amount*: string ## Amount with currency.
  invoiceList*: int ## Number of invoices.
  balance*: string ## Balance with currency.
  fullName*: string ## Users full name.
  email*: string ## User email address.
  tickets*: seq[string] ## List of tickets.
  ticketStatus*: Home_ticketStatus
  ticketStatusView*: Home_ticketStatusView
  details*: Home_details
  services*: Home_services
  AFFILIATE_AMOUNT*: string ## Affiliate amount with currency.


# Custom JSON deserialization for Home with custom field names
proc to*(node: JsonNode, T: typedesc[Home]): Home =
  result = Home()
  if node.kind == JObject:
    if node.hasKey("last_login_ip"):
      result.lastLoginIp = to(node["last_login_ip"], string)
    if node.hasKey("last_login"):
      result.lastLogin = to(node["last_login"], string)
    if node.hasKey("currency"):
      result.currency = to(node["currency"], string)
    if node.hasKey("amount"):
      result.amount = to(node["amount"], string)
    if node.hasKey("invoice_list"):
      result.invoiceList = to(node["invoice_list"], int)
    if node.hasKey("balance"):
      result.balance = to(node["balance"], string)
    if node.hasKey("full_name"):
      result.fullName = to(node["full_name"], string)
    if node.hasKey("email"):
      result.email = to(node["email"], string)
    if node.hasKey("tickets"):
      result.tickets = to(node["tickets"], seq[string])
    if node.hasKey("ticketStatus"):
      result.ticketStatus = to(node["ticketStatus"], Home_ticketStatus)
    if node.hasKey("ticketStatusView"):
      result.ticketStatusView = to(node["ticketStatusView"], Home_ticketStatusView)
    if node.hasKey("details"):
      result.details = to(node["details"], Home_details)
    if node.hasKey("services"):
      result.services = to(node["services"], Home_services)
    if node.hasKey("AFFILIATE_AMOUNT"):
      result.AFFILIATE_AMOUNT = to(node["AFFILIATE_AMOUNT"], string)

# Custom JSON serialization for Home with custom field names
proc `%`*(obj: Home): JsonNode =
  result = newJObject()
  result["last_login_ip"] = %obj.lastLoginIp
  result["last_login"] = %obj.lastLogin
  result["currency"] = %obj.currency
  result["amount"] = %obj.amount
  result["invoice_list"] = %obj.invoiceList
  result["balance"] = %obj.balance
  result["full_name"] = %obj.fullName
  result["email"] = %obj.email
  result["tickets"] = %obj.tickets
  result["ticketStatus"] = %obj.ticketStatus
  result["ticketStatusView"] = %obj.ticketStatusView
  result["details"] = %obj.details
  result["services"] = %obj.services
  result["AFFILIATE_AMOUNT"] = %obj.AFFILIATE_AMOUNT

