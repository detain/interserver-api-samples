#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type Creator* {.pure.} = enum
  User
  Staff

type TicketPostDetailsInner* = object
  ## 
  postId*: Option[int]
  date*: Option[string]
  contents*: Option[string]
  creator*: Option[Creator]
  creatorEmail*: Option[string]
  creatorName*: Option[string]
  hasattachments*: Option[int]
  attachmentDownload*: Option[string]

func `%`*(v: Creator): JsonNode =
  result = case v:
    of Creator.User: %"User"
    of Creator.Staff: %"Staff"
func `$`*(v: Creator): string =
  result = case v:
    of Creator.User: $("User")
    of Creator.Staff: $("Staff")

proc to*(node: JsonNode, T: typedesc[Creator]): Creator =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Creator, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("User"):
    return Creator.User
  of $("Staff"):
    return Creator.Staff
  else:
    raise newException(ValueError, "Invalid enum value for Creator: " & strVal)


# Custom JSON deserialization for TicketPostDetailsInner with custom field names
proc to*(node: JsonNode, T: typedesc[TicketPostDetailsInner]): TicketPostDetailsInner =
  result = TicketPostDetailsInner()
  if node.kind == JObject:
    if node.hasKey("post_id") and node["post_id"].kind != JNull:
      result.postId = some(to(node["post_id"], typeof(result.postId.get())))
    if node.hasKey("date") and node["date"].kind != JNull:
      result.date = some(to(node["date"], typeof(result.date.get())))
    if node.hasKey("contents") and node["contents"].kind != JNull:
      result.contents = some(to(node["contents"], typeof(result.contents.get())))
    if node.hasKey("creator") and node["creator"].kind != JNull:
      result.creator = some(to(node["creator"], Creator))
    if node.hasKey("creator_email") and node["creator_email"].kind != JNull:
      result.creatorEmail = some(to(node["creator_email"], typeof(result.creatorEmail.get())))
    if node.hasKey("creator_name") and node["creator_name"].kind != JNull:
      result.creatorName = some(to(node["creator_name"], typeof(result.creatorName.get())))
    if node.hasKey("hasattachments") and node["hasattachments"].kind != JNull:
      result.hasattachments = some(to(node["hasattachments"], typeof(result.hasattachments.get())))
    if node.hasKey("attachment_download") and node["attachment_download"].kind != JNull:
      result.attachmentDownload = some(to(node["attachment_download"], typeof(result.attachmentDownload.get())))

# Custom JSON serialization for TicketPostDetailsInner with custom field names
proc `%`*(obj: TicketPostDetailsInner): JsonNode =
  result = newJObject()
  if obj.postId.isSome():
    result["post_id"] = %obj.postId.get()
  if obj.date.isSome():
    result["date"] = %obj.date.get()
  if obj.contents.isSome():
    result["contents"] = %obj.contents.get()
  if obj.creator.isSome():
    result["creator"] = %obj.creator.get()
  if obj.creatorEmail.isSome():
    result["creator_email"] = %obj.creatorEmail.get()
  if obj.creatorName.isSome():
    result["creator_name"] = %obj.creatorName.get()
  if obj.hasattachments.isSome():
    result["hasattachments"] = %obj.hasattachments.get()
  if obj.attachmentDownload.isSome():
    result["attachment_download"] = %obj.attachmentDownload.get()

