#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type ScrubIpsLogRowSchema* = object
  ## 
  date*: Option[string]
  filter*: Option[string]
  blockedIp*: Option[string]
  targetIp*: Option[string]
  targetPort*: Option[float]
  protocol*: Option[string]
  byteCount*: Option[float]
  xdpAction*: Option[string]


# Custom JSON deserialization for ScrubIpsLogRowSchema with custom field names
proc to*(node: JsonNode, T: typedesc[ScrubIpsLogRowSchema]): ScrubIpsLogRowSchema =
  result = ScrubIpsLogRowSchema()
  if node.kind == JObject:
    if node.hasKey("date") and node["date"].kind != JNull:
      result.date = some(to(node["date"], typeof(result.date.get())))
    if node.hasKey("filter") and node["filter"].kind != JNull:
      result.filter = some(to(node["filter"], typeof(result.filter.get())))
    if node.hasKey("blocked_ip") and node["blocked_ip"].kind != JNull:
      result.blockedIp = some(to(node["blocked_ip"], typeof(result.blockedIp.get())))
    if node.hasKey("target_ip") and node["target_ip"].kind != JNull:
      result.targetIp = some(to(node["target_ip"], typeof(result.targetIp.get())))
    if node.hasKey("target_port") and node["target_port"].kind != JNull:
      result.targetPort = some(to(node["target_port"], typeof(result.targetPort.get())))
    if node.hasKey("protocol") and node["protocol"].kind != JNull:
      result.protocol = some(to(node["protocol"], typeof(result.protocol.get())))
    if node.hasKey("byte_count") and node["byte_count"].kind != JNull:
      result.byteCount = some(to(node["byte_count"], typeof(result.byteCount.get())))
    if node.hasKey("xdp_action") and node["xdp_action"].kind != JNull:
      result.xdpAction = some(to(node["xdp_action"], typeof(result.xdpAction.get())))

# Custom JSON serialization for ScrubIpsLogRowSchema with custom field names
proc `%`*(obj: ScrubIpsLogRowSchema): JsonNode =
  result = newJObject()
  if obj.date.isSome():
    result["date"] = %obj.date.get()
  if obj.filter.isSome():
    result["filter"] = %obj.filter.get()
  if obj.blockedIp.isSome():
    result["blocked_ip"] = %obj.blockedIp.get()
  if obj.targetIp.isSome():
    result["target_ip"] = %obj.targetIp.get()
  if obj.targetPort.isSome():
    result["target_port"] = %obj.targetPort.get()
  if obj.protocol.isSome():
    result["protocol"] = %obj.protocol.get()
  if obj.byteCount.isSome():
    result["byte_count"] = %obj.byteCount.get()
  if obj.xdpAction.isSome():
    result["xdp_action"] = %obj.xdpAction.get()

