#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_domain_order_response
import model_domain_prov_process_pending

type DomainBillingExtra* = object
  ## Extended billing context for a domain including registrar order details and contact info.
  order*: Option[DomainOrderResponse]
  orderId*: Option[string]
  domainId*: Option[string]
  provProcessPending*: Option[DomainProvProcessPending]
  email*: Option[string]
  firstname*: Option[string]
  lastname*: Option[string]
  company*: Option[string]
  address*: Option[string]
  address2*: Option[string]
  address3*: Option[string]
  city*: Option[string]
  state*: Option[string]
  zip*: Option[string]
  country*: Option[string]
  phone*: Option[string]
  fax*: Option[string]


# Custom JSON deserialization for DomainBillingExtra with custom field names
proc to*(node: JsonNode, T: typedesc[DomainBillingExtra]): DomainBillingExtra =
  result = DomainBillingExtra()
  if node.kind == JObject:
    if node.hasKey("order") and node["order"].kind != JNull:
      result.order = some(to(node["order"], typeof(result.order.get())))
    if node.hasKey("order_id") and node["order_id"].kind != JNull:
      result.orderId = some(to(node["order_id"], typeof(result.orderId.get())))
    if node.hasKey("domain_id") and node["domain_id"].kind != JNull:
      result.domainId = some(to(node["domain_id"], typeof(result.domainId.get())))
    if node.hasKey("provProcessPending") and node["provProcessPending"].kind != JNull:
      result.provProcessPending = some(to(node["provProcessPending"], typeof(result.provProcessPending.get())))
    if node.hasKey("email") and node["email"].kind != JNull:
      result.email = some(to(node["email"], typeof(result.email.get())))
    if node.hasKey("firstname") and node["firstname"].kind != JNull:
      result.firstname = some(to(node["firstname"], typeof(result.firstname.get())))
    if node.hasKey("lastname") and node["lastname"].kind != JNull:
      result.lastname = some(to(node["lastname"], typeof(result.lastname.get())))
    if node.hasKey("company") and node["company"].kind != JNull:
      result.company = some(to(node["company"], typeof(result.company.get())))
    if node.hasKey("address") and node["address"].kind != JNull:
      result.address = some(to(node["address"], typeof(result.address.get())))
    if node.hasKey("address2") and node["address2"].kind != JNull:
      result.address2 = some(to(node["address2"], typeof(result.address2.get())))
    if node.hasKey("address3") and node["address3"].kind != JNull:
      result.address3 = some(to(node["address3"], typeof(result.address3.get())))
    if node.hasKey("city") and node["city"].kind != JNull:
      result.city = some(to(node["city"], typeof(result.city.get())))
    if node.hasKey("state") and node["state"].kind != JNull:
      result.state = some(to(node["state"], typeof(result.state.get())))
    if node.hasKey("zip") and node["zip"].kind != JNull:
      result.zip = some(to(node["zip"], typeof(result.zip.get())))
    if node.hasKey("country") and node["country"].kind != JNull:
      result.country = some(to(node["country"], typeof(result.country.get())))
    if node.hasKey("phone") and node["phone"].kind != JNull:
      result.phone = some(to(node["phone"], typeof(result.phone.get())))
    if node.hasKey("fax") and node["fax"].kind != JNull:
      result.fax = some(to(node["fax"], typeof(result.fax.get())))

# Custom JSON serialization for DomainBillingExtra with custom field names
proc `%`*(obj: DomainBillingExtra): JsonNode =
  result = newJObject()
  if obj.order.isSome():
    result["order"] = %obj.order.get()
  if obj.orderId.isSome():
    result["order_id"] = %obj.orderId.get()
  if obj.domainId.isSome():
    result["domain_id"] = %obj.domainId.get()
  if obj.provProcessPending.isSome():
    result["provProcessPending"] = %obj.provProcessPending.get()
  if obj.email.isSome():
    result["email"] = %obj.email.get()
  if obj.firstname.isSome():
    result["firstname"] = %obj.firstname.get()
  if obj.lastname.isSome():
    result["lastname"] = %obj.lastname.get()
  if obj.company.isSome():
    result["company"] = %obj.company.get()
  if obj.address.isSome():
    result["address"] = %obj.address.get()
  if obj.address2.isSome():
    result["address2"] = %obj.address2.get()
  if obj.address3.isSome():
    result["address3"] = %obj.address3.get()
  if obj.city.isSome():
    result["city"] = %obj.city.get()
  if obj.state.isSome():
    result["state"] = %obj.state.get()
  if obj.zip.isSome():
    result["zip"] = %obj.zip.get()
  if obj.country.isSome():
    result["country"] = %obj.country.get()
  if obj.phone.isSome():
    result["phone"] = %obj.phone.get()
  if obj.fax.isSome():
    result["fax"] = %obj.fax.get()

