#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_domain_all_info_attributes_contact_set
import model_domain_name_server

type DomainAllInfoAttributes* = object
  ## 
  contactSet*: Option[DomainAllInfo_attributes_contact_set]
  registryCreatedate*: Option[string]
  registryExpiredate*: Option[string]
  tldData*: Option[string]
  letExpire*: Option[string]
  autoRenew*: Option[string]
  sponsoringRsp*: Option[string]
  gdprConsentStatus*: Option[string]
  nameserverList*: Option[seq[DomainNameServer]]
  registryUpdatedate*: Option[string]
  affiliateId*: Option[string]
  expiredate*: Option[string]


# Custom JSON deserialization for DomainAllInfoAttributes with custom field names
proc to*(node: JsonNode, T: typedesc[DomainAllInfoAttributes]): DomainAllInfoAttributes =
  result = DomainAllInfoAttributes()
  if node.kind == JObject:
    if node.hasKey("contact_set") and node["contact_set"].kind != JNull:
      result.contactSet = some(to(node["contact_set"], typeof(result.contactSet.get())))
    if node.hasKey("registry_createdate") and node["registry_createdate"].kind != JNull:
      result.registryCreatedate = some(to(node["registry_createdate"], typeof(result.registryCreatedate.get())))
    if node.hasKey("registry_expiredate") and node["registry_expiredate"].kind != JNull:
      result.registryExpiredate = some(to(node["registry_expiredate"], typeof(result.registryExpiredate.get())))
    if node.hasKey("tld_data") and node["tld_data"].kind != JNull:
      result.tldData = some(to(node["tld_data"], typeof(result.tldData.get())))
    if node.hasKey("let_expire") and node["let_expire"].kind != JNull:
      result.letExpire = some(to(node["let_expire"], typeof(result.letExpire.get())))
    if node.hasKey("auto_renew") and node["auto_renew"].kind != JNull:
      result.autoRenew = some(to(node["auto_renew"], typeof(result.autoRenew.get())))
    if node.hasKey("sponsoring_rsp") and node["sponsoring_rsp"].kind != JNull:
      result.sponsoringRsp = some(to(node["sponsoring_rsp"], typeof(result.sponsoringRsp.get())))
    if node.hasKey("gdpr_consent_status") and node["gdpr_consent_status"].kind != JNull:
      result.gdprConsentStatus = some(to(node["gdpr_consent_status"], typeof(result.gdprConsentStatus.get())))
    if node.hasKey("nameserver_list") and node["nameserver_list"].kind != JNull:
      result.nameserverList = some(to(node["nameserver_list"], typeof(result.nameserverList.get())))
    if node.hasKey("registry_updatedate") and node["registry_updatedate"].kind != JNull:
      result.registryUpdatedate = some(to(node["registry_updatedate"], typeof(result.registryUpdatedate.get())))
    if node.hasKey("affiliate_id") and node["affiliate_id"].kind != JNull:
      result.affiliateId = some(to(node["affiliate_id"], typeof(result.affiliateId.get())))
    if node.hasKey("expiredate") and node["expiredate"].kind != JNull:
      result.expiredate = some(to(node["expiredate"], typeof(result.expiredate.get())))

# Custom JSON serialization for DomainAllInfoAttributes with custom field names
proc `%`*(obj: DomainAllInfoAttributes): JsonNode =
  result = newJObject()
  if obj.contactSet.isSome():
    result["contact_set"] = %obj.contactSet.get()
  if obj.registryCreatedate.isSome():
    result["registry_createdate"] = %obj.registryCreatedate.get()
  if obj.registryExpiredate.isSome():
    result["registry_expiredate"] = %obj.registryExpiredate.get()
  if obj.tldData.isSome():
    result["tld_data"] = %obj.tldData.get()
  if obj.letExpire.isSome():
    result["let_expire"] = %obj.letExpire.get()
  if obj.autoRenew.isSome():
    result["auto_renew"] = %obj.autoRenew.get()
  if obj.sponsoringRsp.isSome():
    result["sponsoring_rsp"] = %obj.sponsoringRsp.get()
  if obj.gdprConsentStatus.isSome():
    result["gdpr_consent_status"] = %obj.gdprConsentStatus.get()
  if obj.nameserverList.isSome():
    result["nameserver_list"] = %obj.nameserverList.get()
  if obj.registryUpdatedate.isSome():
    result["registry_updatedate"] = %obj.registryUpdatedate.get()
  if obj.affiliateId.isSome():
    result["affiliate_id"] = %obj.affiliateId.get()
  if obj.expiredate.isSome():
    result["expiredate"] = %obj.expiredate.get()

