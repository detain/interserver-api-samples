#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type DomainWhoisPrivacyRequest* = object
  ## Request payload for enabling or disabling Whois privacy.
  `func`*: Option[string] ## Action to perform (enable or disableCancel).
  csrfToken*: Option[string] ## CSRF token if the API requires it for the account.
  domainFirstname*: Option[string]
  domainLastname*: Option[string]
  domainEmail*: Option[string]
  domainAddress*: Option[string]
  domainAddress2*: Option[string]
  domainAddress3*: Option[string]
  domainCity*: Option[string]
  domainState*: Option[string]
  domainZip*: Option[string]
  domainCountry*: Option[string]
  domainPhone*: Option[string]
  domainFax*: Option[string]
  domainCompany*: Option[string]
  domainExtra*: Option[string]


# Custom JSON deserialization for DomainWhoisPrivacyRequest with custom field names
proc to*(node: JsonNode, T: typedesc[DomainWhoisPrivacyRequest]): DomainWhoisPrivacyRequest =
  result = DomainWhoisPrivacyRequest()
  if node.kind == JObject:
    if node.hasKey("func") and node["func"].kind != JNull:
      result.`func` = some(to(node["func"], typeof(result.`func`.get())))
    if node.hasKey("csrf_token") and node["csrf_token"].kind != JNull:
      result.csrfToken = some(to(node["csrf_token"], typeof(result.csrfToken.get())))
    if node.hasKey("domain_firstname") and node["domain_firstname"].kind != JNull:
      result.domainFirstname = some(to(node["domain_firstname"], typeof(result.domainFirstname.get())))
    if node.hasKey("domain_lastname") and node["domain_lastname"].kind != JNull:
      result.domainLastname = some(to(node["domain_lastname"], typeof(result.domainLastname.get())))
    if node.hasKey("domain_email") and node["domain_email"].kind != JNull:
      result.domainEmail = some(to(node["domain_email"], typeof(result.domainEmail.get())))
    if node.hasKey("domain_address") and node["domain_address"].kind != JNull:
      result.domainAddress = some(to(node["domain_address"], typeof(result.domainAddress.get())))
    if node.hasKey("domain_address2") and node["domain_address2"].kind != JNull:
      result.domainAddress2 = some(to(node["domain_address2"], typeof(result.domainAddress2.get())))
    if node.hasKey("domain_address3") and node["domain_address3"].kind != JNull:
      result.domainAddress3 = some(to(node["domain_address3"], typeof(result.domainAddress3.get())))
    if node.hasKey("domain_city") and node["domain_city"].kind != JNull:
      result.domainCity = some(to(node["domain_city"], typeof(result.domainCity.get())))
    if node.hasKey("domain_state") and node["domain_state"].kind != JNull:
      result.domainState = some(to(node["domain_state"], typeof(result.domainState.get())))
    if node.hasKey("domain_zip") and node["domain_zip"].kind != JNull:
      result.domainZip = some(to(node["domain_zip"], typeof(result.domainZip.get())))
    if node.hasKey("domain_country") and node["domain_country"].kind != JNull:
      result.domainCountry = some(to(node["domain_country"], typeof(result.domainCountry.get())))
    if node.hasKey("domain_phone") and node["domain_phone"].kind != JNull:
      result.domainPhone = some(to(node["domain_phone"], typeof(result.domainPhone.get())))
    if node.hasKey("domain_fax") and node["domain_fax"].kind != JNull:
      result.domainFax = some(to(node["domain_fax"], typeof(result.domainFax.get())))
    if node.hasKey("domain_company") and node["domain_company"].kind != JNull:
      result.domainCompany = some(to(node["domain_company"], typeof(result.domainCompany.get())))
    if node.hasKey("domain_extra") and node["domain_extra"].kind != JNull:
      result.domainExtra = some(to(node["domain_extra"], typeof(result.domainExtra.get())))

# Custom JSON serialization for DomainWhoisPrivacyRequest with custom field names
proc `%`*(obj: DomainWhoisPrivacyRequest): JsonNode =
  result = newJObject()
  if obj.`func`.isSome():
    result["func"] = %obj.`func`.get()
  if obj.csrfToken.isSome():
    result["csrf_token"] = %obj.csrfToken.get()
  if obj.domainFirstname.isSome():
    result["domain_firstname"] = %obj.domainFirstname.get()
  if obj.domainLastname.isSome():
    result["domain_lastname"] = %obj.domainLastname.get()
  if obj.domainEmail.isSome():
    result["domain_email"] = %obj.domainEmail.get()
  if obj.domainAddress.isSome():
    result["domain_address"] = %obj.domainAddress.get()
  if obj.domainAddress2.isSome():
    result["domain_address2"] = %obj.domainAddress2.get()
  if obj.domainAddress3.isSome():
    result["domain_address3"] = %obj.domainAddress3.get()
  if obj.domainCity.isSome():
    result["domain_city"] = %obj.domainCity.get()
  if obj.domainState.isSome():
    result["domain_state"] = %obj.domainState.get()
  if obj.domainZip.isSome():
    result["domain_zip"] = %obj.domainZip.get()
  if obj.domainCountry.isSome():
    result["domain_country"] = %obj.domainCountry.get()
  if obj.domainPhone.isSome():
    result["domain_phone"] = %obj.domainPhone.get()
  if obj.domainFax.isSome():
    result["domain_fax"] = %obj.domainFax.get()
  if obj.domainCompany.isSome():
    result["domain_company"] = %obj.domainCompany.get()
  if obj.domainExtra.isSome():
    result["domain_extra"] = %obj.domainExtra.get()

