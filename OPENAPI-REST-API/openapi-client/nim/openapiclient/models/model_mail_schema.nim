#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_mail_billing_details
import model_mail_client_link
import model_mail_schema_extra_info_tables
import model_mail_service_info
import model_mail_service_type

type MailSchema* = object
  ## 
  serviceInfo*: MailServiceInfo
  clientLinks*: seq[MailClientLink] ## Links related to the mail service for clients.
  billingDetails*: MailBillingDetails
  custCurrency*: string ## The customer's currency.
  custCurrencySymbol*: string ## The currency symbol for the customer.
  package*: string ## The package of the mail service.
  extraInfoTables*: MailSchema_extraInfoTables
  serviceType*: MailServiceType
  usageCount*: string ## The usage count of the mail service.
  serviceExtra*: Option[seq[string]] ## Extra information for the mail service.


# Custom JSON deserialization for MailSchema with custom field names
proc to*(node: JsonNode, T: typedesc[MailSchema]): MailSchema =
  result = MailSchema()
  if node.kind == JObject:
    if node.hasKey("serviceInfo"):
      result.serviceInfo = to(node["serviceInfo"], MailServiceInfo)
    if node.hasKey("client_links"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["client_links"]
      if arrayNode.kind == JArray:
        result.clientLinks = @[]
        for item in arrayNode.items:
          result.clientLinks.add(to(item, MailClientLink))
    if node.hasKey("billingDetails"):
      result.billingDetails = to(node["billingDetails"], MailBillingDetails)
    if node.hasKey("custCurrency"):
      result.custCurrency = to(node["custCurrency"], string)
    if node.hasKey("custCurrencySymbol"):
      result.custCurrencySymbol = to(node["custCurrencySymbol"], string)
    if node.hasKey("package"):
      result.package = to(node["package"], string)
    if node.hasKey("extraInfoTables"):
      result.extraInfoTables = to(node["extraInfoTables"], MailSchema_extraInfoTables)
    if node.hasKey("serviceType"):
      result.serviceType = to(node["serviceType"], MailServiceType)
    if node.hasKey("usage_count"):
      result.usageCount = to(node["usage_count"], string)
    if node.hasKey("serviceExtra") and node["serviceExtra"].kind != JNull:
      result.serviceExtra = some(to(node["serviceExtra"], typeof(result.serviceExtra.get())))

# Custom JSON serialization for MailSchema with custom field names
proc `%`*(obj: MailSchema): JsonNode =
  result = newJObject()
  result["serviceInfo"] = %obj.serviceInfo
  result["client_links"] = %obj.clientLinks
  result["billingDetails"] = %obj.billingDetails
  result["custCurrency"] = %obj.custCurrency
  result["custCurrencySymbol"] = %obj.custCurrencySymbol
  result["package"] = %obj.package
  result["extraInfoTables"] = %obj.extraInfoTables
  result["serviceType"] = %obj.serviceType
  result["usage_count"] = %obj.usageCount
  if obj.serviceExtra.isSome():
    result["serviceExtra"] = %obj.serviceExtra.get()

