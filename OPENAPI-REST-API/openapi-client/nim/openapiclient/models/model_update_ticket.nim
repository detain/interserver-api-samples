#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type CustomerServerAccess* {.pure.} = enum
  Y
  N

type UpdateTicket* = object
  ## Update custom fields of the ticket
  ip*: Option[string]
  ipAddress*: Option[string]
  customerServerAccess*: Option[CustomerServerAccess]
  rootPassword*: Option[string]
  sudoUsername*: Option[string]
  sudoPassword*: Option[string]
  port*: Option[int]

func `%`*(v: CustomerServerAccess): JsonNode =
  result = case v:
    of CustomerServerAccess.Y: %"y"
    of CustomerServerAccess.N: %"n"
func `$`*(v: CustomerServerAccess): string =
  result = case v:
    of CustomerServerAccess.Y: $("y")
    of CustomerServerAccess.N: $("n")

proc to*(node: JsonNode, T: typedesc[CustomerServerAccess]): CustomerServerAccess =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum CustomerServerAccess, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("y"):
    return CustomerServerAccess.Y
  of $("n"):
    return CustomerServerAccess.N
  else:
    raise newException(ValueError, "Invalid enum value for CustomerServerAccess: " & strVal)


# Custom JSON deserialization for UpdateTicket with custom field names
proc to*(node: JsonNode, T: typedesc[UpdateTicket]): UpdateTicket =
  result = UpdateTicket()
  if node.kind == JObject:
    if node.hasKey("ip") and node["ip"].kind != JNull:
      result.ip = some(to(node["ip"], typeof(result.ip.get())))
    if node.hasKey("ip_address") and node["ip_address"].kind != JNull:
      result.ipAddress = some(to(node["ip_address"], typeof(result.ipAddress.get())))
    if node.hasKey("customer_server_access") and node["customer_server_access"].kind != JNull:
      result.customerServerAccess = some(to(node["customer_server_access"], CustomerServerAccess))
    if node.hasKey("root_password") and node["root_password"].kind != JNull:
      result.rootPassword = some(to(node["root_password"], typeof(result.rootPassword.get())))
    if node.hasKey("sudo_username") and node["sudo_username"].kind != JNull:
      result.sudoUsername = some(to(node["sudo_username"], typeof(result.sudoUsername.get())))
    if node.hasKey("sudo_password") and node["sudo_password"].kind != JNull:
      result.sudoPassword = some(to(node["sudo_password"], typeof(result.sudoPassword.get())))
    if node.hasKey("port") and node["port"].kind != JNull:
      result.port = some(to(node["port"], typeof(result.port.get())))

# Custom JSON serialization for UpdateTicket with custom field names
proc `%`*(obj: UpdateTicket): JsonNode =
  result = newJObject()
  if obj.ip.isSome():
    result["ip"] = %obj.ip.get()
  if obj.ipAddress.isSome():
    result["ip_address"] = %obj.ipAddress.get()
  if obj.customerServerAccess.isSome():
    result["customer_server_access"] = %obj.customerServerAccess.get()
  if obj.rootPassword.isSome():
    result["root_password"] = %obj.rootPassword.get()
  if obj.sudoUsername.isSome():
    result["sudo_username"] = %obj.sudoUsername.get()
  if obj.sudoPassword.isSome():
    result["sudo_password"] = %obj.sudoPassword.get()
  if obj.port.isSome():
    result["port"] = %obj.port.get()

