#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type DomainServiceInfo* = object
  ## Detailed domain service record for a domain order.
  domainId*: Option[string]
  domainHostname*: Option[string]
  domainUsername*: Option[string]
  domainPassword*: Option[string]
  domainType*: Option[string]
  domainCurrency*: Option[string]
  domainExpireDate*: Option[string]
  domainOrderDate*: Option[string]
  domainCustid*: Option[string]
  domainStatus*: Option[string]
  domainInvoice*: Option[string]
  domainCoupon*: Option[string]


# Custom JSON deserialization for DomainServiceInfo with custom field names
proc to*(node: JsonNode, T: typedesc[DomainServiceInfo]): DomainServiceInfo =
  result = DomainServiceInfo()
  if node.kind == JObject:
    if node.hasKey("domain_id") and node["domain_id"].kind != JNull:
      result.domainId = some(to(node["domain_id"], typeof(result.domainId.get())))
    if node.hasKey("domain_hostname") and node["domain_hostname"].kind != JNull:
      result.domainHostname = some(to(node["domain_hostname"], typeof(result.domainHostname.get())))
    if node.hasKey("domain_username") and node["domain_username"].kind != JNull:
      result.domainUsername = some(to(node["domain_username"], typeof(result.domainUsername.get())))
    if node.hasKey("domain_password") and node["domain_password"].kind != JNull:
      result.domainPassword = some(to(node["domain_password"], typeof(result.domainPassword.get())))
    if node.hasKey("domain_type") and node["domain_type"].kind != JNull:
      result.domainType = some(to(node["domain_type"], typeof(result.domainType.get())))
    if node.hasKey("domain_currency") and node["domain_currency"].kind != JNull:
      result.domainCurrency = some(to(node["domain_currency"], typeof(result.domainCurrency.get())))
    if node.hasKey("domain_expire_date") and node["domain_expire_date"].kind != JNull:
      result.domainExpireDate = some(to(node["domain_expire_date"], typeof(result.domainExpireDate.get())))
    if node.hasKey("domain_order_date") and node["domain_order_date"].kind != JNull:
      result.domainOrderDate = some(to(node["domain_order_date"], typeof(result.domainOrderDate.get())))
    if node.hasKey("domain_custid") and node["domain_custid"].kind != JNull:
      result.domainCustid = some(to(node["domain_custid"], typeof(result.domainCustid.get())))
    if node.hasKey("domain_status") and node["domain_status"].kind != JNull:
      result.domainStatus = some(to(node["domain_status"], typeof(result.domainStatus.get())))
    if node.hasKey("domain_invoice") and node["domain_invoice"].kind != JNull:
      result.domainInvoice = some(to(node["domain_invoice"], typeof(result.domainInvoice.get())))
    if node.hasKey("domain_coupon") and node["domain_coupon"].kind != JNull:
      result.domainCoupon = some(to(node["domain_coupon"], typeof(result.domainCoupon.get())))

# Custom JSON serialization for DomainServiceInfo with custom field names
proc `%`*(obj: DomainServiceInfo): JsonNode =
  result = newJObject()
  if obj.domainId.isSome():
    result["domain_id"] = %obj.domainId.get()
  if obj.domainHostname.isSome():
    result["domain_hostname"] = %obj.domainHostname.get()
  if obj.domainUsername.isSome():
    result["domain_username"] = %obj.domainUsername.get()
  if obj.domainPassword.isSome():
    result["domain_password"] = %obj.domainPassword.get()
  if obj.domainType.isSome():
    result["domain_type"] = %obj.domainType.get()
  if obj.domainCurrency.isSome():
    result["domain_currency"] = %obj.domainCurrency.get()
  if obj.domainExpireDate.isSome():
    result["domain_expire_date"] = %obj.domainExpireDate.get()
  if obj.domainOrderDate.isSome():
    result["domain_order_date"] = %obj.domainOrderDate.get()
  if obj.domainCustid.isSome():
    result["domain_custid"] = %obj.domainCustid.get()
  if obj.domainStatus.isSome():
    result["domain_status"] = %obj.domainStatus.get()
  if obj.domainInvoice.isSome():
    result["domain_invoice"] = %obj.domainInvoice.get()
  if obj.domainCoupon.isSome():
    result["domain_coupon"] = %obj.domainCoupon.get()

