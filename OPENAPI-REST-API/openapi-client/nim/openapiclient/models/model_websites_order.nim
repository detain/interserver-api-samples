#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_websites_order_json_service_offers
import model_websites_order_json_services
import model_websites_order_packages
import model_websites_order_packges
import model_websites_order_service_offers
import model_websites_order_service_types

type WebsitesOrder* = object
  ## Schema for the WebsitesOrder object
  step*: string ## Step description
  website*: string ## Website description
  period*: int ## Period description
  serviceOfferId*: int ## Service offer ID description
  packages*: WebsitesOrderPackages
  enableDomainRegistering*: bool ## Enable domain registering description
  jsonServices*: WebsitesOrderJsonServices
  jsonServiceOffers*: WebsitesOrderJsonServiceOffers
  serviceTypes*: WebsitesOrder_serviceTypes
  serviceOffers*: WebsitesOrder_serviceOffers
  packges*: Option[WebsitesOrder_packges]


# Custom JSON deserialization for WebsitesOrder with custom field names
proc to*(node: JsonNode, T: typedesc[WebsitesOrder]): WebsitesOrder =
  result = WebsitesOrder()
  if node.kind == JObject:
    if node.hasKey("step"):
      result.step = to(node["step"], string)
    if node.hasKey("website"):
      result.website = to(node["website"], string)
    if node.hasKey("period"):
      result.period = to(node["period"], int)
    if node.hasKey("serviceOfferId"):
      result.serviceOfferId = to(node["serviceOfferId"], int)
    if node.hasKey("packages"):
      result.packages = to(node["packages"], WebsitesOrderPackages)
    if node.hasKey("enableDomainRegistering"):
      result.enableDomainRegistering = to(node["enableDomainRegistering"], bool)
    if node.hasKey("jsonServices"):
      result.jsonServices = to(node["jsonServices"], WebsitesOrderJsonServices)
    if node.hasKey("jsonServiceOffers"):
      result.jsonServiceOffers = to(node["jsonServiceOffers"], WebsitesOrderJsonServiceOffers)
    if node.hasKey("serviceTypes"):
      result.serviceTypes = to(node["serviceTypes"], WebsitesOrder_serviceTypes)
    if node.hasKey("serviceOffers"):
      result.serviceOffers = to(node["serviceOffers"], WebsitesOrder_serviceOffers)
    if node.hasKey("packges") and node["packges"].kind != JNull:
      result.packges = some(to(node["packges"], typeof(result.packges.get())))

# Custom JSON serialization for WebsitesOrder with custom field names
proc `%`*(obj: WebsitesOrder): JsonNode =
  result = newJObject()
  result["step"] = %obj.step
  result["website"] = %obj.website
  result["period"] = %obj.period
  result["serviceOfferId"] = %obj.serviceOfferId
  result["packages"] = %obj.packages
  result["enableDomainRegistering"] = %obj.enableDomainRegistering
  result["jsonServices"] = %obj.jsonServices
  result["jsonServiceOffers"] = %obj.jsonServiceOffers
  result["serviceTypes"] = %obj.serviceTypes
  result["serviceOffers"] = %obj.serviceOffers
  if obj.packges.isSome():
    result["packges"] = %obj.packges.get()

