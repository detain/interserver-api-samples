#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type LicenseServiceInfo* = object
  ## 
  licenseId*: string ## License ID
  licenseType*: string ## License type
  licenseCurrency*: string ## License currency
  licenseOrderDate*: string ## License order date
  licenseCustid*: string ## Customer ID
  licenseIp*: string ## License IP
  licenseStatus*: string ## License status
  licenseInvoice*: string ## License invoice
  licenseCoupon*: string ## License coupon
  licenseHostname*: Option[string] ## License hostname
  licenseKey*: Option[string] ## License key
  licenseExtra*: Option[string] ## Additional license information


# Custom JSON deserialization for LicenseServiceInfo with custom field names
proc to*(node: JsonNode, T: typedesc[LicenseServiceInfo]): LicenseServiceInfo =
  result = LicenseServiceInfo()
  if node.kind == JObject:
    if node.hasKey("license_id"):
      result.licenseId = to(node["license_id"], string)
    if node.hasKey("license_type"):
      result.licenseType = to(node["license_type"], string)
    if node.hasKey("license_currency"):
      result.licenseCurrency = to(node["license_currency"], string)
    if node.hasKey("license_order_date"):
      result.licenseOrderDate = to(node["license_order_date"], string)
    if node.hasKey("license_custid"):
      result.licenseCustid = to(node["license_custid"], string)
    if node.hasKey("license_ip"):
      result.licenseIp = to(node["license_ip"], string)
    if node.hasKey("license_status"):
      result.licenseStatus = to(node["license_status"], string)
    if node.hasKey("license_invoice"):
      result.licenseInvoice = to(node["license_invoice"], string)
    if node.hasKey("license_coupon"):
      result.licenseCoupon = to(node["license_coupon"], string)
    if node.hasKey("license_hostname") and node["license_hostname"].kind != JNull:
      result.licenseHostname = some(to(node["license_hostname"], typeof(result.licenseHostname.get())))
    if node.hasKey("license_key") and node["license_key"].kind != JNull:
      result.licenseKey = some(to(node["license_key"], typeof(result.licenseKey.get())))
    if node.hasKey("license_extra") and node["license_extra"].kind != JNull:
      result.licenseExtra = some(to(node["license_extra"], typeof(result.licenseExtra.get())))

# Custom JSON serialization for LicenseServiceInfo with custom field names
proc `%`*(obj: LicenseServiceInfo): JsonNode =
  result = newJObject()
  result["license_id"] = %obj.licenseId
  result["license_type"] = %obj.licenseType
  result["license_currency"] = %obj.licenseCurrency
  result["license_order_date"] = %obj.licenseOrderDate
  result["license_custid"] = %obj.licenseCustid
  result["license_ip"] = %obj.licenseIp
  result["license_status"] = %obj.licenseStatus
  result["license_invoice"] = %obj.licenseInvoice
  result["license_coupon"] = %obj.licenseCoupon
  if obj.licenseHostname.isSome():
    result["license_hostname"] = %obj.licenseHostname.get()
  if obj.licenseKey.isSome():
    result["license_key"] = %obj.licenseKey.get()
  if obj.licenseExtra.isSome():
    result["license_extra"] = %obj.licenseExtra.get()

