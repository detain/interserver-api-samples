#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_home_details_modules_backups
import model_home_details_modules_domains
import model_home_details_modules_licenses
import model_home_details_modules_quickservers
import model_home_details_modules_servers
import model_home_details_modules_vps
import model_home_details_modules_webhosting

type HomeDetailsModules* = object
  ## 
  domains*: Option[HomeDetailsModulesDomains]
  webhosting*: Option[HomeDetailsModulesWebhosting]
  vps*: Option[HomeDetailsModulesVps]
  licenses*: Option[HomeDetailsModulesLicenses]
  backups*: Option[HomeDetailsModulesBackups]
  servers*: Option[HomeDetailsModulesServers]
  quickservers*: Option[HomeDetailsModulesQuickservers]


# Custom JSON deserialization for HomeDetailsModules with custom field names
proc to*(node: JsonNode, T: typedesc[HomeDetailsModules]): HomeDetailsModules =
  result = HomeDetailsModules()
  if node.kind == JObject:
    if node.hasKey("domains") and node["domains"].kind != JNull:
      result.domains = some(to(node["domains"], typeof(result.domains.get())))
    if node.hasKey("webhosting") and node["webhosting"].kind != JNull:
      result.webhosting = some(to(node["webhosting"], typeof(result.webhosting.get())))
    if node.hasKey("vps") and node["vps"].kind != JNull:
      result.vps = some(to(node["vps"], typeof(result.vps.get())))
    if node.hasKey("licenses") and node["licenses"].kind != JNull:
      result.licenses = some(to(node["licenses"], typeof(result.licenses.get())))
    if node.hasKey("backups") and node["backups"].kind != JNull:
      result.backups = some(to(node["backups"], typeof(result.backups.get())))
    if node.hasKey("servers") and node["servers"].kind != JNull:
      result.servers = some(to(node["servers"], typeof(result.servers.get())))
    if node.hasKey("quickservers") and node["quickservers"].kind != JNull:
      result.quickservers = some(to(node["quickservers"], typeof(result.quickservers.get())))

# Custom JSON serialization for HomeDetailsModules with custom field names
proc `%`*(obj: HomeDetailsModules): JsonNode =
  result = newJObject()
  if obj.domains.isSome():
    result["domains"] = %obj.domains.get()
  if obj.webhosting.isSome():
    result["webhosting"] = %obj.webhosting.get()
  if obj.vps.isSome():
    result["vps"] = %obj.vps.get()
  if obj.licenses.isSome():
    result["licenses"] = %obj.licenses.get()
  if obj.backups.isSome():
    result["backups"] = %obj.backups.get()
  if obj.servers.isSome():
    result["servers"] = %obj.servers.get()
  if obj.quickservers.isSome():
    result["quickservers"] = %obj.quickservers.get()

