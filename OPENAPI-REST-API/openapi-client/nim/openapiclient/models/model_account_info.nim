#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_account_info_country_currencies
import model_account_info_data
import model_account_info_limits
import model_account_info_oauth_config
import model_account_info_oauthproviders

type AccountInfo* = object
  ## Contains the full account profile including personal info, billing details, OAuth connections, and security settings.
  custid*: Option[string]
  ima*: Option[string]
  data*: Option[AccountInfoData]
  ip*: Option[string]
  oauthproviders*: Option[AccountInfo_oauthproviders]
  oauthconfig*: Option[AccountInfoOauthConfig]
  oauthadapters*: Option[seq[string]]
  limits*: Option[seq[AccountInfoLimits_inner]]
  language*: Option[string]
  countryCurrencies*: Option[Table[string, seq[string]]]
  enableLocales*: Option[bool]
  enableCurrencies*: Option[bool]
  gravatar*: Option[string]


# Custom JSON deserialization for AccountInfo with custom field names
proc to*(node: JsonNode, T: typedesc[AccountInfo]): AccountInfo =
  result = AccountInfo()
  if node.kind == JObject:
    if node.hasKey("custid") and node["custid"].kind != JNull:
      result.custid = some(to(node["custid"], typeof(result.custid.get())))
    if node.hasKey("ima") and node["ima"].kind != JNull:
      result.ima = some(to(node["ima"], typeof(result.ima.get())))
    if node.hasKey("data") and node["data"].kind != JNull:
      result.data = some(to(node["data"], typeof(result.data.get())))
    if node.hasKey("ip") and node["ip"].kind != JNull:
      result.ip = some(to(node["ip"], typeof(result.ip.get())))
    if node.hasKey("oauthproviders") and node["oauthproviders"].kind != JNull:
      result.oauthproviders = some(to(node["oauthproviders"], typeof(result.oauthproviders.get())))
    if node.hasKey("oauthconfig") and node["oauthconfig"].kind != JNull:
      result.oauthconfig = some(to(node["oauthconfig"], typeof(result.oauthconfig.get())))
    if node.hasKey("oauthadapters") and node["oauthadapters"].kind != JNull:
      result.oauthadapters = some(to(node["oauthadapters"], typeof(result.oauthadapters.get())))
    if node.hasKey("limits") and node["limits"].kind != JNull:
      result.limits = some(to(node["limits"], typeof(result.limits.get())))
    if node.hasKey("language") and node["language"].kind != JNull:
      result.language = some(to(node["language"], typeof(result.language.get())))
    if node.hasKey("countryCurrencies") and node["countryCurrencies"].kind != JNull:
      result.countryCurrencies = some(to(node["countryCurrencies"], typeof(result.countryCurrencies.get())))
    if node.hasKey("enableLocales") and node["enableLocales"].kind != JNull:
      result.enableLocales = some(to(node["enableLocales"], typeof(result.enableLocales.get())))
    if node.hasKey("enableCurrencies") and node["enableCurrencies"].kind != JNull:
      result.enableCurrencies = some(to(node["enableCurrencies"], typeof(result.enableCurrencies.get())))
    if node.hasKey("gravatar") and node["gravatar"].kind != JNull:
      result.gravatar = some(to(node["gravatar"], typeof(result.gravatar.get())))

# Custom JSON serialization for AccountInfo with custom field names
proc `%`*(obj: AccountInfo): JsonNode =
  result = newJObject()
  if obj.custid.isSome():
    result["custid"] = %obj.custid.get()
  if obj.ima.isSome():
    result["ima"] = %obj.ima.get()
  if obj.data.isSome():
    result["data"] = %obj.data.get()
  if obj.ip.isSome():
    result["ip"] = %obj.ip.get()
  if obj.oauthproviders.isSome():
    result["oauthproviders"] = %obj.oauthproviders.get()
  if obj.oauthconfig.isSome():
    result["oauthconfig"] = %obj.oauthconfig.get()
  if obj.oauthadapters.isSome():
    result["oauthadapters"] = %obj.oauthadapters.get()
  if obj.limits.isSome():
    result["limits"] = %obj.limits.get()
  if obj.language.isSome():
    result["language"] = %obj.language.get()
  if obj.countryCurrencies.isSome():
    result["countryCurrencies"] = %obj.countryCurrencies.get()
  if obj.enableLocales.isSome():
    result["enableLocales"] = %obj.enableLocales.get()
  if obj.enableCurrencies.isSome():
    result["enableCurrencies"] = %obj.enableCurrencies.get()
  if obj.gravatar.isSome():
    result["gravatar"] = %obj.gravatar.get()

