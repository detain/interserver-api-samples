#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_object

type DomainLookupResponse* = object
  ## Availability, pricing, and order-field metadata for a domain lookup.
  available*: Option[bool] ## Whether the domain is available to register.
  premium*: Option[bool] ## Whether the domain is marked as premium by the registrar.
  website*: Option[bool] ## Indicates if the domain is already used by a website service.
  domainService*: Option[bool] ## Indicates if the domain already exists as a domain service on the account.
  service*: Option[JsonNode] ## Service catalog details for the domain's TLD.
  whoisPrivacy*: Option[bool] ## Whether Whois privacy is available for the TLD.
  new*: Option[string] ## Calculated registration price, when available.
  renewal*: Option[string] ## Calculated renewal price, when available.
  transfer*: Option[string] ## Calculated transfer price, when available.
  fields*: Option[JsonNode] ## Registrar field requirements for this domain/TLD.
  currencies*: Option[JsonNode] ## Pricing information normalized to supported currencies.


# Custom JSON deserialization for DomainLookupResponse with custom field names
proc to*(node: JsonNode, T: typedesc[DomainLookupResponse]): DomainLookupResponse =
  result = DomainLookupResponse()
  if node.kind == JObject:
    if node.hasKey("available") and node["available"].kind != JNull:
      result.available = some(to(node["available"], typeof(result.available.get())))
    if node.hasKey("premium") and node["premium"].kind != JNull:
      result.premium = some(to(node["premium"], typeof(result.premium.get())))
    if node.hasKey("website") and node["website"].kind != JNull:
      result.website = some(to(node["website"], typeof(result.website.get())))
    if node.hasKey("domain_service") and node["domain_service"].kind != JNull:
      result.domainService = some(to(node["domain_service"], typeof(result.domainService.get())))
    if node.hasKey("service") and node["service"].kind != JNull:
      result.service = some(to(node["service"], typeof(result.service.get())))
    if node.hasKey("whois_privacy") and node["whois_privacy"].kind != JNull:
      result.whoisPrivacy = some(to(node["whois_privacy"], typeof(result.whoisPrivacy.get())))
    if node.hasKey("new") and node["new"].kind != JNull:
      result.new = some(to(node["new"], typeof(result.new.get())))
    if node.hasKey("renewal") and node["renewal"].kind != JNull:
      result.renewal = some(to(node["renewal"], typeof(result.renewal.get())))
    if node.hasKey("transfer") and node["transfer"].kind != JNull:
      result.transfer = some(to(node["transfer"], typeof(result.transfer.get())))
    if node.hasKey("fields") and node["fields"].kind != JNull:
      result.fields = some(to(node["fields"], typeof(result.fields.get())))
    if node.hasKey("currencies") and node["currencies"].kind != JNull:
      result.currencies = some(to(node["currencies"], typeof(result.currencies.get())))

# Custom JSON serialization for DomainLookupResponse with custom field names
proc `%`*(obj: DomainLookupResponse): JsonNode =
  result = newJObject()
  if obj.available.isSome():
    result["available"] = %obj.available.get()
  if obj.premium.isSome():
    result["premium"] = %obj.premium.get()
  if obj.website.isSome():
    result["website"] = %obj.website.get()
  if obj.domainService.isSome():
    result["domain_service"] = %obj.domainService.get()
  if obj.service.isSome():
    result["service"] = %obj.service.get()
  if obj.whoisPrivacy.isSome():
    result["whois_privacy"] = %obj.whoisPrivacy.get()
  if obj.new.isSome():
    result["new"] = %obj.new.get()
  if obj.renewal.isSome():
    result["renewal"] = %obj.renewal.get()
  if obj.transfer.isSome():
    result["transfer"] = %obj.transfer.get()
  if obj.fields.isSome():
    result["fields"] = %obj.fields.get()
  if obj.currencies.isSome():
    result["currencies"] = %obj.currencies.get()

