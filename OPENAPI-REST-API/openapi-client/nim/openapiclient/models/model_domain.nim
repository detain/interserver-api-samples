#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_backup_extra_info_tables
import model_domain_all_info
import model_domain_billing_details
import model_domain_billing_extra
import model_domain_client_link
import model_domain_contact_details
import model_domain_service_info
import model_domain_service_type

type Domain* = object
  ## Full detail view of a domain service including billing, contacts, DNS, and configuration.
  serviceInfo*: Option[DomainServiceInfo]
  serviceTypes*: Option[Table[string, DomainServiceType]]
  clientLinks*: Option[seq[DomainClientLink]]
  billingDetails*: Option[DomainBillingDetails]
  custCurrency*: Option[string]
  custCurrencySymbol*: Option[string]
  serviceExtra*: Option[DomainBillingExtra]
  extraInfoTables*: Option[BackupExtraInfoTables]
  serviceType*: Option[DomainServiceType]
  contactDetails*: Option[DomainContactDetails]
  pwarning*: Option[string]
  transferInfo*: Option[string]
  errors*: Option[bool]
  domainLogs*: Option[seq[string]]
  allInfo*: Option[DomainAllInfo]
  registrarStatus*: Option[string]
  locked*: Option[string]
  whoisPrivacy*: Option[string]
  autoRenew*: Option[string]


# Custom JSON deserialization for Domain with custom field names
proc to*(node: JsonNode, T: typedesc[Domain]): Domain =
  result = Domain()
  if node.kind == JObject:
    if node.hasKey("serviceInfo") and node["serviceInfo"].kind != JNull:
      result.serviceInfo = some(to(node["serviceInfo"], typeof(result.serviceInfo.get())))
    if node.hasKey("serviceTypes") and node["serviceTypes"].kind != JNull:
      result.serviceTypes = some(to(node["serviceTypes"], typeof(result.serviceTypes.get())))
    if node.hasKey("client_links") and node["client_links"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["client_links"]
      if arrayNode.kind == JArray:
        var arr: seq[DomainClientLink] = @[]
        for item in arrayNode.items:
          arr.add(to(item, DomainClientLink))
        result.clientLinks = some(arr)
    if node.hasKey("billingDetails") and node["billingDetails"].kind != JNull:
      result.billingDetails = some(to(node["billingDetails"], typeof(result.billingDetails.get())))
    if node.hasKey("custCurrency") and node["custCurrency"].kind != JNull:
      result.custCurrency = some(to(node["custCurrency"], typeof(result.custCurrency.get())))
    if node.hasKey("custCurrencySymbol") and node["custCurrencySymbol"].kind != JNull:
      result.custCurrencySymbol = some(to(node["custCurrencySymbol"], typeof(result.custCurrencySymbol.get())))
    if node.hasKey("serviceExtra") and node["serviceExtra"].kind != JNull:
      result.serviceExtra = some(to(node["serviceExtra"], typeof(result.serviceExtra.get())))
    if node.hasKey("extraInfoTables") and node["extraInfoTables"].kind != JNull:
      result.extraInfoTables = some(to(node["extraInfoTables"], typeof(result.extraInfoTables.get())))
    if node.hasKey("serviceType") and node["serviceType"].kind != JNull:
      result.serviceType = some(to(node["serviceType"], typeof(result.serviceType.get())))
    if node.hasKey("contact_details") and node["contact_details"].kind != JNull:
      result.contactDetails = some(to(node["contact_details"], typeof(result.contactDetails.get())))
    if node.hasKey("pwarning") and node["pwarning"].kind != JNull:
      result.pwarning = some(to(node["pwarning"], typeof(result.pwarning.get())))
    if node.hasKey("transfer_info") and node["transfer_info"].kind != JNull:
      result.transferInfo = some(to(node["transfer_info"], typeof(result.transferInfo.get())))
    if node.hasKey("errors") and node["errors"].kind != JNull:
      result.errors = some(to(node["errors"], typeof(result.errors.get())))
    if node.hasKey("domain_logs") and node["domain_logs"].kind != JNull:
      result.domainLogs = some(to(node["domain_logs"], typeof(result.domainLogs.get())))
    if node.hasKey("allInfo") and node["allInfo"].kind != JNull:
      result.allInfo = some(to(node["allInfo"], typeof(result.allInfo.get())))
    if node.hasKey("registrarStatus") and node["registrarStatus"].kind != JNull:
      result.registrarStatus = some(to(node["registrarStatus"], typeof(result.registrarStatus.get())))
    if node.hasKey("locked") and node["locked"].kind != JNull:
      result.locked = some(to(node["locked"], typeof(result.locked.get())))
    if node.hasKey("whoisPrivacy") and node["whoisPrivacy"].kind != JNull:
      result.whoisPrivacy = some(to(node["whoisPrivacy"], typeof(result.whoisPrivacy.get())))
    if node.hasKey("autoRenew") and node["autoRenew"].kind != JNull:
      result.autoRenew = some(to(node["autoRenew"], typeof(result.autoRenew.get())))

# Custom JSON serialization for Domain with custom field names
proc `%`*(obj: Domain): JsonNode =
  result = newJObject()
  if obj.serviceInfo.isSome():
    result["serviceInfo"] = %obj.serviceInfo.get()
  if obj.serviceTypes.isSome():
    result["serviceTypes"] = %obj.serviceTypes.get()
  if obj.clientLinks.isSome():
    result["client_links"] = %obj.clientLinks.get()
  if obj.billingDetails.isSome():
    result["billingDetails"] = %obj.billingDetails.get()
  if obj.custCurrency.isSome():
    result["custCurrency"] = %obj.custCurrency.get()
  if obj.custCurrencySymbol.isSome():
    result["custCurrencySymbol"] = %obj.custCurrencySymbol.get()
  if obj.serviceExtra.isSome():
    result["serviceExtra"] = %obj.serviceExtra.get()
  if obj.extraInfoTables.isSome():
    result["extraInfoTables"] = %obj.extraInfoTables.get()
  if obj.serviceType.isSome():
    result["serviceType"] = %obj.serviceType.get()
  if obj.contactDetails.isSome():
    result["contact_details"] = %obj.contactDetails.get()
  if obj.pwarning.isSome():
    result["pwarning"] = %obj.pwarning.get()
  if obj.transferInfo.isSome():
    result["transfer_info"] = %obj.transferInfo.get()
  if obj.errors.isSome():
    result["errors"] = %obj.errors.get()
  if obj.domainLogs.isSome():
    result["domain_logs"] = %obj.domainLogs.get()
  if obj.allInfo.isSome():
    result["allInfo"] = %obj.allInfo.get()
  if obj.registrarStatus.isSome():
    result["registrarStatus"] = %obj.registrarStatus.get()
  if obj.locked.isSome():
    result["locked"] = %obj.locked.get()
  if obj.whoisPrivacy.isSome():
    result["whoisPrivacy"] = %obj.whoisPrivacy.get()
  if obj.autoRenew.isSome():
    result["autoRenew"] = %obj.autoRenew.get()

