#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type ServerSwitchport* = object
  ## 
  switchportId*: int ## Unique identifier for the switchport.
  switchId*: string ## Unique identifier for the switch associated with the switchport.
  switch*: string ## Name of the switch associated with the switchport.
  port*: string ## Port name on the switch.
  blade*: string ## Blade name associated with the port.
  justport*: string ## Port identifier.
  graphId*: string ## Identifier for the graph associated with the switchport.
  assetId*: int ## Unique identifier of the asset associated with the switchport.
  vlans*: Option[seq[string]] ## List of VLANs associated with the switchport.
  vlans6*: Option[seq[string]] ## List of IPv6 VLANs associated with the switchport.


# Custom JSON deserialization for ServerSwitchport with custom field names
proc to*(node: JsonNode, T: typedesc[ServerSwitchport]): ServerSwitchport =
  result = ServerSwitchport()
  if node.kind == JObject:
    if node.hasKey("switchport_id"):
      result.switchportId = to(node["switchport_id"], int)
    if node.hasKey("switch_id"):
      result.switchId = to(node["switch_id"], string)
    if node.hasKey("switch"):
      result.switch = to(node["switch"], string)
    if node.hasKey("port"):
      result.port = to(node["port"], string)
    if node.hasKey("blade"):
      result.blade = to(node["blade"], string)
    if node.hasKey("justport"):
      result.justport = to(node["justport"], string)
    if node.hasKey("graph_id"):
      result.graphId = to(node["graph_id"], string)
    if node.hasKey("asset_id"):
      result.assetId = to(node["asset_id"], int)
    if node.hasKey("vlans") and node["vlans"].kind != JNull:
      result.vlans = some(to(node["vlans"], typeof(result.vlans.get())))
    if node.hasKey("vlans6") and node["vlans6"].kind != JNull:
      result.vlans6 = some(to(node["vlans6"], typeof(result.vlans6.get())))

# Custom JSON serialization for ServerSwitchport with custom field names
proc `%`*(obj: ServerSwitchport): JsonNode =
  result = newJObject()
  result["switchport_id"] = %obj.switchportId
  result["switch_id"] = %obj.switchId
  result["switch"] = %obj.switch
  result["port"] = %obj.port
  result["blade"] = %obj.blade
  result["justport"] = %obj.justport
  result["graph_id"] = %obj.graphId
  result["asset_id"] = %obj.assetId
  if obj.vlans.isSome():
    result["vlans"] = %obj.vlans.get()
  if obj.vlans6.isSome():
    result["vlans6"] = %obj.vlans6.get()

