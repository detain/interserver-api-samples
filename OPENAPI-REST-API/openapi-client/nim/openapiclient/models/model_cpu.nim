#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type Cpu* = object
  ## A CPU option available for dedicated server ordering.
  id*: Option[int]
  shortDesc*: Option[string]
  longDesc*: Option[string]
  `type`*: Option[string]
  speed*: Option[string]
  numCores*: Option[string]
  numCpus*: Option[string]
  benchmark*: Option[string]
  monthlyPrice*: Option[float]
  monthlyPriceDisplay*: Option[string]
  maxRam*: Option[string]
  minRam*: Option[string]
  maxLff*: Option[string]
  maxSff*: Option[string]
  maxNve*: Option[string]
  visible*: Option[string]
  active*: Option[string]


# Custom JSON deserialization for Cpu with custom field names
proc to*(node: JsonNode, T: typedesc[Cpu]): Cpu =
  result = Cpu()
  if node.kind == JObject:
    if node.hasKey("id") and node["id"].kind != JNull:
      result.id = some(to(node["id"], typeof(result.id.get())))
    if node.hasKey("short_desc") and node["short_desc"].kind != JNull:
      result.shortDesc = some(to(node["short_desc"], typeof(result.shortDesc.get())))
    if node.hasKey("long_desc") and node["long_desc"].kind != JNull:
      result.longDesc = some(to(node["long_desc"], typeof(result.longDesc.get())))
    if node.hasKey("type") and node["type"].kind != JNull:
      result.`type` = some(to(node["type"], typeof(result.`type`.get())))
    if node.hasKey("speed") and node["speed"].kind != JNull:
      result.speed = some(to(node["speed"], typeof(result.speed.get())))
    if node.hasKey("num_cores") and node["num_cores"].kind != JNull:
      result.numCores = some(to(node["num_cores"], typeof(result.numCores.get())))
    if node.hasKey("num_cpus") and node["num_cpus"].kind != JNull:
      result.numCpus = some(to(node["num_cpus"], typeof(result.numCpus.get())))
    if node.hasKey("benchmark") and node["benchmark"].kind != JNull:
      result.benchmark = some(to(node["benchmark"], typeof(result.benchmark.get())))
    if node.hasKey("monthly_price") and node["monthly_price"].kind != JNull:
      result.monthlyPrice = some(to(node["monthly_price"], typeof(result.monthlyPrice.get())))
    if node.hasKey("monthly_price_display") and node["monthly_price_display"].kind != JNull:
      result.monthlyPriceDisplay = some(to(node["monthly_price_display"], typeof(result.monthlyPriceDisplay.get())))
    if node.hasKey("max_ram") and node["max_ram"].kind != JNull:
      result.maxRam = some(to(node["max_ram"], typeof(result.maxRam.get())))
    if node.hasKey("min_ram") and node["min_ram"].kind != JNull:
      result.minRam = some(to(node["min_ram"], typeof(result.minRam.get())))
    if node.hasKey("max_lff") and node["max_lff"].kind != JNull:
      result.maxLff = some(to(node["max_lff"], typeof(result.maxLff.get())))
    if node.hasKey("max_sff") and node["max_sff"].kind != JNull:
      result.maxSff = some(to(node["max_sff"], typeof(result.maxSff.get())))
    if node.hasKey("max_nve") and node["max_nve"].kind != JNull:
      result.maxNve = some(to(node["max_nve"], typeof(result.maxNve.get())))
    if node.hasKey("visible") and node["visible"].kind != JNull:
      result.visible = some(to(node["visible"], typeof(result.visible.get())))
    if node.hasKey("active") and node["active"].kind != JNull:
      result.active = some(to(node["active"], typeof(result.active.get())))

# Custom JSON serialization for Cpu with custom field names
proc `%`*(obj: Cpu): JsonNode =
  result = newJObject()
  if obj.id.isSome():
    result["id"] = %obj.id.get()
  if obj.shortDesc.isSome():
    result["short_desc"] = %obj.shortDesc.get()
  if obj.longDesc.isSome():
    result["long_desc"] = %obj.longDesc.get()
  if obj.`type`.isSome():
    result["type"] = %obj.`type`.get()
  if obj.speed.isSome():
    result["speed"] = %obj.speed.get()
  if obj.numCores.isSome():
    result["num_cores"] = %obj.numCores.get()
  if obj.numCpus.isSome():
    result["num_cpus"] = %obj.numCpus.get()
  if obj.benchmark.isSome():
    result["benchmark"] = %obj.benchmark.get()
  if obj.monthlyPrice.isSome():
    result["monthly_price"] = %obj.monthlyPrice.get()
  if obj.monthlyPriceDisplay.isSome():
    result["monthly_price_display"] = %obj.monthlyPriceDisplay.get()
  if obj.maxRam.isSome():
    result["max_ram"] = %obj.maxRam.get()
  if obj.minRam.isSome():
    result["min_ram"] = %obj.minRam.get()
  if obj.maxLff.isSome():
    result["max_lff"] = %obj.maxLff.get()
  if obj.maxSff.isSome():
    result["max_sff"] = %obj.maxSff.get()
  if obj.maxNve.isSome():
    result["max_nve"] = %obj.maxNve.get()
  if obj.visible.isSome():
    result["visible"] = %obj.visible.get()
  if obj.active.isSome():
    result["active"] = %obj.active.get()

