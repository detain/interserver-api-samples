#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type ServerOrderBandwidth* = object
  ## A bandwidth option available when ordering a dedicated server.
  id*: Option[string] ## Bandwidth ID.
  price*: Option[int] ## Bandwidth price.
  img*: Option[string] ## Bandwidth image.
  shortDesc*: Option[string] ## Short description of the bandwidth.
  longDesc*: Option[string] ## Long description of the bandwidth.
  `type`*: Option[string] ## Bandwidth type.
  qty*: Option[string] ## Quantity of bandwidth.
  active*: Option[string] ## Active status.
  monthlyPrice*: Option[int] ## Monthly price.
  priceDisplay*: Option[string] ## Display of bandwidth price.
  monthlyPriceDisplay*: Option[string] ## Display of monthly bandwidth price.


# Custom JSON deserialization for ServerOrderBandwidth with custom field names
proc to*(node: JsonNode, T: typedesc[ServerOrderBandwidth]): ServerOrderBandwidth =
  result = ServerOrderBandwidth()
  if node.kind == JObject:
    if node.hasKey("id") and node["id"].kind != JNull:
      result.id = some(to(node["id"], typeof(result.id.get())))
    if node.hasKey("price") and node["price"].kind != JNull:
      result.price = some(to(node["price"], typeof(result.price.get())))
    if node.hasKey("img") and node["img"].kind != JNull:
      result.img = some(to(node["img"], typeof(result.img.get())))
    if node.hasKey("short_desc") and node["short_desc"].kind != JNull:
      result.shortDesc = some(to(node["short_desc"], typeof(result.shortDesc.get())))
    if node.hasKey("long_desc") and node["long_desc"].kind != JNull:
      result.longDesc = some(to(node["long_desc"], typeof(result.longDesc.get())))
    if node.hasKey("type") and node["type"].kind != JNull:
      result.`type` = some(to(node["type"], typeof(result.`type`.get())))
    if node.hasKey("qty") and node["qty"].kind != JNull:
      result.qty = some(to(node["qty"], typeof(result.qty.get())))
    if node.hasKey("active") and node["active"].kind != JNull:
      result.active = some(to(node["active"], typeof(result.active.get())))
    if node.hasKey("monthly_price") and node["monthly_price"].kind != JNull:
      result.monthlyPrice = some(to(node["monthly_price"], typeof(result.monthlyPrice.get())))
    if node.hasKey("price_display") and node["price_display"].kind != JNull:
      result.priceDisplay = some(to(node["price_display"], typeof(result.priceDisplay.get())))
    if node.hasKey("monthly_price_display") and node["monthly_price_display"].kind != JNull:
      result.monthlyPriceDisplay = some(to(node["monthly_price_display"], typeof(result.monthlyPriceDisplay.get())))

# Custom JSON serialization for ServerOrderBandwidth with custom field names
proc `%`*(obj: ServerOrderBandwidth): JsonNode =
  result = newJObject()
  if obj.id.isSome():
    result["id"] = %obj.id.get()
  if obj.price.isSome():
    result["price"] = %obj.price.get()
  if obj.img.isSome():
    result["img"] = %obj.img.get()
  if obj.shortDesc.isSome():
    result["short_desc"] = %obj.shortDesc.get()
  if obj.longDesc.isSome():
    result["long_desc"] = %obj.longDesc.get()
  if obj.`type`.isSome():
    result["type"] = %obj.`type`.get()
  if obj.qty.isSome():
    result["qty"] = %obj.qty.get()
  if obj.active.isSome():
    result["active"] = %obj.active.get()
  if obj.monthlyPrice.isSome():
    result["monthly_price"] = %obj.monthlyPrice.get()
  if obj.priceDisplay.isSome():
    result["price_display"] = %obj.priceDisplay.get()
  if obj.monthlyPriceDisplay.isSome():
    result["monthly_price_display"] = %obj.monthlyPriceDisplay.get()

