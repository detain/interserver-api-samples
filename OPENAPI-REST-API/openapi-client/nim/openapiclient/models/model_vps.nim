#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_any_type
import model_vps_billing_details
import model_vps_client_link
import model_vps_cp_data
import model_vps_da_data
import model_vps_extra_info_tables
import model_vps_plesk12_data
import model_vps_service_addons
import model_vps_service_extra
import model_vps_service_info
import model_vps_service_master

type Vps* = object
  ## 
  serviceInfo*: VpsServiceInfo
  clientLinks*: seq[VpsClientLink]
  billingDetails*: VpsBillingDetails
  custCurrency*: string
  custCurrencySymbol*: string
  serviceMaster*: VpsServiceMaster
  package*: string
  serviceExtra*: VpsServiceExtra
  extraInfoTables*: VpsExtraInfoTables
  module*: string
  token*: string
  daLink*: int
  srLink*: int
  cpData*: VpsCPData
  daData*: VpsDAData
  plesk12Data*: VpsPlesk12Data
  serviceAddons*: VpsServiceAddons
  osTemplate*: Option[string]
  cpuGraphData*: Option[JsonNode]


# Custom JSON deserialization for Vps with custom field names
proc to*(node: JsonNode, T: typedesc[Vps]): Vps =
  result = Vps()
  if node.kind == JObject:
    if node.hasKey("serviceInfo"):
      result.serviceInfo = to(node["serviceInfo"], VpsServiceInfo)
    if node.hasKey("client_links"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["client_links"]
      if arrayNode.kind == JArray:
        result.clientLinks = @[]
        for item in arrayNode.items:
          result.clientLinks.add(to(item, VpsClientLink))
    if node.hasKey("billingDetails"):
      result.billingDetails = to(node["billingDetails"], VpsBillingDetails)
    if node.hasKey("custCurrency"):
      result.custCurrency = to(node["custCurrency"], string)
    if node.hasKey("custCurrencySymbol"):
      result.custCurrencySymbol = to(node["custCurrencySymbol"], string)
    if node.hasKey("serviceMaster"):
      result.serviceMaster = to(node["serviceMaster"], VpsServiceMaster)
    if node.hasKey("package"):
      result.package = to(node["package"], string)
    if node.hasKey("serviceExtra"):
      result.serviceExtra = to(node["serviceExtra"], VpsServiceExtra)
    if node.hasKey("extraInfoTables"):
      result.extraInfoTables = to(node["extraInfoTables"], VpsExtraInfoTables)
    if node.hasKey("module"):
      result.module = to(node["module"], string)
    if node.hasKey("token"):
      result.token = to(node["token"], string)
    if node.hasKey("da_link"):
      result.daLink = to(node["da_link"], int)
    if node.hasKey("sr_link"):
      result.srLink = to(node["sr_link"], int)
    if node.hasKey("cp_data"):
      result.cpData = to(node["cp_data"], VpsCPData)
    if node.hasKey("da_data"):
      result.daData = to(node["da_data"], VpsDAData)
    if node.hasKey("plesk12_data"):
      result.plesk12Data = to(node["plesk12_data"], VpsPlesk12Data)
    if node.hasKey("serviceAddons"):
      result.serviceAddons = to(node["serviceAddons"], VpsServiceAddons)
    if node.hasKey("os_template") and node["os_template"].kind != JNull:
      result.osTemplate = some(to(node["os_template"], typeof(result.osTemplate.get())))
    if node.hasKey("cpu_graph_data") and node["cpu_graph_data"].kind != JNull:
      result.cpuGraphData = some(to(node["cpu_graph_data"], typeof(result.cpuGraphData.get())))

# Custom JSON serialization for Vps with custom field names
proc `%`*(obj: Vps): JsonNode =
  result = newJObject()
  result["serviceInfo"] = %obj.serviceInfo
  result["client_links"] = %obj.clientLinks
  result["billingDetails"] = %obj.billingDetails
  result["custCurrency"] = %obj.custCurrency
  result["custCurrencySymbol"] = %obj.custCurrencySymbol
  result["serviceMaster"] = %obj.serviceMaster
  result["package"] = %obj.package
  result["serviceExtra"] = %obj.serviceExtra
  result["extraInfoTables"] = %obj.extraInfoTables
  result["module"] = %obj.module
  result["token"] = %obj.token
  result["da_link"] = %obj.daLink
  result["sr_link"] = %obj.srLink
  result["cp_data"] = %obj.cpData
  result["da_data"] = %obj.daData
  result["plesk12_data"] = %obj.plesk12Data
  result["serviceAddons"] = %obj.serviceAddons
  if obj.osTemplate.isSome():
    result["os_template"] = %obj.osTemplate.get()
  if obj.cpuGraphData.isSome():
    result["cpu_graph_data"] = %obj.cpuGraphData.get()

