#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_asset_server
import model_config_ids
import model_config_lists
import model_cpu
import model_cpu_with_defaults
import model_field_label
import model_form_values
import model_object
import model_region

type ServerOrderGetResponse* = object
  ## Configuration options and pricing data returned when starting a dedicated server order.
  formValues*: FormValues
  configIds*: ConfigIds
  cpu*: int
  cpuLi*: Table[string, Cpu]
  configLi*: ConfigLists
  frequency*: int
  currency*: string
  country*: string
  step*: string
  fieldLabel*: Option[Table[string, FieldLabel]]
  cpuCores*: Option[Table[string, Table[string, CpuWithDefaults]]]
  currencySymbol*: Option[string]
  custid*: Option[int]
  ima*: Option[string]
  regions*: Option[seq[Region]]
  assetServers*: Option[seq[AssetServer]]
  buyItServers*: Option[seq[JsonNode]]
  displayShowmore*: Option[string]
  custDiscount*: Option[float]


# Custom JSON deserialization for ServerOrderGetResponse with custom field names
proc to*(node: JsonNode, T: typedesc[ServerOrderGetResponse]): ServerOrderGetResponse =
  result = ServerOrderGetResponse()
  if node.kind == JObject:
    if node.hasKey("form_values"):
      result.formValues = to(node["form_values"], FormValues)
    if node.hasKey("config_ids"):
      result.configIds = to(node["config_ids"], ConfigIds)
    if node.hasKey("cpu"):
      result.cpu = to(node["cpu"], int)
    if node.hasKey("cpu_li"):
      result.cpuLi = to(node["cpu_li"], Table[string, Cpu])
    if node.hasKey("config_li"):
      result.configLi = to(node["config_li"], ConfigLists)
    if node.hasKey("frequency"):
      result.frequency = to(node["frequency"], int)
    if node.hasKey("currency"):
      result.currency = to(node["currency"], string)
    if node.hasKey("country"):
      result.country = to(node["country"], string)
    if node.hasKey("step"):
      result.step = to(node["step"], string)
    if node.hasKey("field_label") and node["field_label"].kind != JNull:
      result.fieldLabel = some(to(node["field_label"], typeof(result.fieldLabel.get())))
    if node.hasKey("cpu_cores") and node["cpu_cores"].kind != JNull:
      result.cpuCores = some(to(node["cpu_cores"], typeof(result.cpuCores.get())))
    if node.hasKey("currencySymbol") and node["currencySymbol"].kind != JNull:
      result.currencySymbol = some(to(node["currencySymbol"], typeof(result.currencySymbol.get())))
    if node.hasKey("custid") and node["custid"].kind != JNull:
      result.custid = some(to(node["custid"], typeof(result.custid.get())))
    if node.hasKey("ima") and node["ima"].kind != JNull:
      result.ima = some(to(node["ima"], typeof(result.ima.get())))
    if node.hasKey("regions") and node["regions"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["regions"]
      if arrayNode.kind == JArray:
        var arr: seq[Region] = @[]
        for item in arrayNode.items:
          arr.add(to(item, Region))
        result.regions = some(arr)
    if node.hasKey("asset_servers") and node["asset_servers"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["asset_servers"]
      if arrayNode.kind == JArray:
        var arr: seq[AssetServer] = @[]
        for item in arrayNode.items:
          arr.add(to(item, AssetServer))
        result.assetServers = some(arr)
    if node.hasKey("buy_it_servers") and node["buy_it_servers"].kind != JNull:
      result.buyItServers = some(to(node["buy_it_servers"], typeof(result.buyItServers.get())))
    if node.hasKey("display_showmore") and node["display_showmore"].kind != JNull:
      result.displayShowmore = some(to(node["display_showmore"], typeof(result.displayShowmore.get())))
    if node.hasKey("cust_discount") and node["cust_discount"].kind != JNull:
      result.custDiscount = some(to(node["cust_discount"], typeof(result.custDiscount.get())))

# Custom JSON serialization for ServerOrderGetResponse with custom field names
proc `%`*(obj: ServerOrderGetResponse): JsonNode =
  result = newJObject()
  result["form_values"] = %obj.formValues
  result["config_ids"] = %obj.configIds
  result["cpu"] = %obj.cpu
  result["cpu_li"] = %obj.cpuLi
  result["config_li"] = %obj.configLi
  result["frequency"] = %obj.frequency
  result["currency"] = %obj.currency
  result["country"] = %obj.country
  result["step"] = %obj.step
  if obj.fieldLabel.isSome():
    result["field_label"] = %obj.fieldLabel.get()
  if obj.cpuCores.isSome():
    result["cpu_cores"] = %obj.cpuCores.get()
  if obj.currencySymbol.isSome():
    result["currencySymbol"] = %obj.currencySymbol.get()
  if obj.custid.isSome():
    result["custid"] = %obj.custid.get()
  if obj.ima.isSome():
    result["ima"] = %obj.ima.get()
  if obj.regions.isSome():
    result["regions"] = %obj.regions.get()
  if obj.assetServers.isSome():
    result["asset_servers"] = %obj.assetServers.get()
  if obj.buyItServers.isSome():
    result["buy_it_servers"] = %obj.buyItServers.get()
  if obj.displayShowmore.isSome():
    result["display_showmore"] = %obj.displayShowmore.get()
  if obj.custDiscount.isSome():
    result["cust_discount"] = %obj.custDiscount.get()

