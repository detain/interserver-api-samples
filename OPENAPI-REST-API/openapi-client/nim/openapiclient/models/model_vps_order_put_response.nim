#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_any_type

type VpsOrderPutResponse* = object
  ## Response from VPS order validation request
  `continue`*: Option[bool]
  errors*: Option[seq[JsonNode]]
  couponCode*: Option[int]
  serviceCost*: Option[int]
  sliceCost*: Option[int]
  serviceType*: Option[int]
  repeatSliceCost*: Option[int]
  originalSliceCost*: Option[int]
  originalCost*: Option[int]
  repeatServiceCost*: Option[int]
  monthlyServiceCost*: Option[int]
  custid*: Option[string]
  os*: Option[string]
  slices*: Option[string]
  platform*: Option[string]
  controlpanel*: Option[string]
  period*: Option[int]
  location*: Option[int]
  version*: Option[string]
  hostname*: Option[string]
  coupon*: Option[string]
  rootpass*: Option[string]


# Custom JSON deserialization for VpsOrderPutResponse with custom field names
proc to*(node: JsonNode, T: typedesc[VpsOrderPutResponse]): VpsOrderPutResponse =
  result = VpsOrderPutResponse()
  if node.kind == JObject:
    if node.hasKey("continue") and node["continue"].kind != JNull:
      result.`continue` = some(to(node["continue"], typeof(result.`continue`.get())))
    if node.hasKey("errors") and node["errors"].kind != JNull:
      result.errors = some(to(node["errors"], typeof(result.errors.get())))
    if node.hasKey("coupon_code") and node["coupon_code"].kind != JNull:
      result.couponCode = some(to(node["coupon_code"], typeof(result.couponCode.get())))
    if node.hasKey("service_cost") and node["service_cost"].kind != JNull:
      result.serviceCost = some(to(node["service_cost"], typeof(result.serviceCost.get())))
    if node.hasKey("slice_cost") and node["slice_cost"].kind != JNull:
      result.sliceCost = some(to(node["slice_cost"], typeof(result.sliceCost.get())))
    if node.hasKey("service_type") and node["service_type"].kind != JNull:
      result.serviceType = some(to(node["service_type"], typeof(result.serviceType.get())))
    if node.hasKey("repeat_slice_cost") and node["repeat_slice_cost"].kind != JNull:
      result.repeatSliceCost = some(to(node["repeat_slice_cost"], typeof(result.repeatSliceCost.get())))
    if node.hasKey("original_slice_cost") and node["original_slice_cost"].kind != JNull:
      result.originalSliceCost = some(to(node["original_slice_cost"], typeof(result.originalSliceCost.get())))
    if node.hasKey("original_cost") and node["original_cost"].kind != JNull:
      result.originalCost = some(to(node["original_cost"], typeof(result.originalCost.get())))
    if node.hasKey("repeat_service_cost") and node["repeat_service_cost"].kind != JNull:
      result.repeatServiceCost = some(to(node["repeat_service_cost"], typeof(result.repeatServiceCost.get())))
    if node.hasKey("monthly_service_cost") and node["monthly_service_cost"].kind != JNull:
      result.monthlyServiceCost = some(to(node["monthly_service_cost"], typeof(result.monthlyServiceCost.get())))
    if node.hasKey("custid") and node["custid"].kind != JNull:
      result.custid = some(to(node["custid"], typeof(result.custid.get())))
    if node.hasKey("os") and node["os"].kind != JNull:
      result.os = some(to(node["os"], typeof(result.os.get())))
    if node.hasKey("slices") and node["slices"].kind != JNull:
      result.slices = some(to(node["slices"], typeof(result.slices.get())))
    if node.hasKey("platform") and node["platform"].kind != JNull:
      result.platform = some(to(node["platform"], typeof(result.platform.get())))
    if node.hasKey("controlpanel") and node["controlpanel"].kind != JNull:
      result.controlpanel = some(to(node["controlpanel"], typeof(result.controlpanel.get())))
    if node.hasKey("period") and node["period"].kind != JNull:
      result.period = some(to(node["period"], typeof(result.period.get())))
    if node.hasKey("location") and node["location"].kind != JNull:
      result.location = some(to(node["location"], typeof(result.location.get())))
    if node.hasKey("version") and node["version"].kind != JNull:
      result.version = some(to(node["version"], typeof(result.version.get())))
    if node.hasKey("hostname") and node["hostname"].kind != JNull:
      result.hostname = some(to(node["hostname"], typeof(result.hostname.get())))
    if node.hasKey("coupon") and node["coupon"].kind != JNull:
      result.coupon = some(to(node["coupon"], typeof(result.coupon.get())))
    if node.hasKey("rootpass") and node["rootpass"].kind != JNull:
      result.rootpass = some(to(node["rootpass"], typeof(result.rootpass.get())))

# Custom JSON serialization for VpsOrderPutResponse with custom field names
proc `%`*(obj: VpsOrderPutResponse): JsonNode =
  result = newJObject()
  if obj.`continue`.isSome():
    result["continue"] = %obj.`continue`.get()
  if obj.errors.isSome():
    result["errors"] = %obj.errors.get()
  if obj.couponCode.isSome():
    result["coupon_code"] = %obj.couponCode.get()
  if obj.serviceCost.isSome():
    result["service_cost"] = %obj.serviceCost.get()
  if obj.sliceCost.isSome():
    result["slice_cost"] = %obj.sliceCost.get()
  if obj.serviceType.isSome():
    result["service_type"] = %obj.serviceType.get()
  if obj.repeatSliceCost.isSome():
    result["repeat_slice_cost"] = %obj.repeatSliceCost.get()
  if obj.originalSliceCost.isSome():
    result["original_slice_cost"] = %obj.originalSliceCost.get()
  if obj.originalCost.isSome():
    result["original_cost"] = %obj.originalCost.get()
  if obj.repeatServiceCost.isSome():
    result["repeat_service_cost"] = %obj.repeatServiceCost.get()
  if obj.monthlyServiceCost.isSome():
    result["monthly_service_cost"] = %obj.monthlyServiceCost.get()
  if obj.custid.isSome():
    result["custid"] = %obj.custid.get()
  if obj.os.isSome():
    result["os"] = %obj.os.get()
  if obj.slices.isSome():
    result["slices"] = %obj.slices.get()
  if obj.platform.isSome():
    result["platform"] = %obj.platform.get()
  if obj.controlpanel.isSome():
    result["controlpanel"] = %obj.controlpanel.get()
  if obj.period.isSome():
    result["period"] = %obj.period.get()
  if obj.location.isSome():
    result["location"] = %obj.location.get()
  if obj.version.isSome():
    result["version"] = %obj.version.get()
  if obj.hostname.isSome():
    result["hostname"] = %obj.hostname.get()
  if obj.coupon.isSome():
    result["coupon"] = %obj.coupon.get()
  if obj.rootpass.isSome():
    result["rootpass"] = %obj.rootpass.get()

