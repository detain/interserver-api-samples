#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type BackupRow* = object
  ## A result row from the `Backups` `GET` request.
  backupId*: Option[string] ## The id of the backup.
  backupName*: Option[string] ## The name of the backup.
  backupCost*: Option[string] ## The cost of the backup.
  backupUsername*: Option[string] ## The username of the backup.
  backupStatus*: Option[string] ## The status of the backup.
  servicesName*: Option[string] ## The services name of the backup.


# Custom JSON deserialization for BackupRow with custom field names
proc to*(node: JsonNode, T: typedesc[BackupRow]): BackupRow =
  result = BackupRow()
  if node.kind == JObject:
    if node.hasKey("backup_id") and node["backup_id"].kind != JNull:
      result.backupId = some(to(node["backup_id"], typeof(result.backupId.get())))
    if node.hasKey("backup_name") and node["backup_name"].kind != JNull:
      result.backupName = some(to(node["backup_name"], typeof(result.backupName.get())))
    if node.hasKey("backup_cost") and node["backup_cost"].kind != JNull:
      result.backupCost = some(to(node["backup_cost"], typeof(result.backupCost.get())))
    if node.hasKey("backup_username") and node["backup_username"].kind != JNull:
      result.backupUsername = some(to(node["backup_username"], typeof(result.backupUsername.get())))
    if node.hasKey("backup_status") and node["backup_status"].kind != JNull:
      result.backupStatus = some(to(node["backup_status"], typeof(result.backupStatus.get())))
    if node.hasKey("services_name") and node["services_name"].kind != JNull:
      result.servicesName = some(to(node["services_name"], typeof(result.servicesName.get())))

# Custom JSON serialization for BackupRow with custom field names
proc `%`*(obj: BackupRow): JsonNode =
  result = newJObject()
  if obj.backupId.isSome():
    result["backup_id"] = %obj.backupId.get()
  if obj.backupName.isSome():
    result["backup_name"] = %obj.backupName.get()
  if obj.backupCost.isSome():
    result["backup_cost"] = %obj.backupCost.get()
  if obj.backupUsername.isSome():
    result["backup_username"] = %obj.backupUsername.get()
  if obj.backupStatus.isSome():
    result["backup_status"] = %obj.backupStatus.get()
  if obj.servicesName.isSome():
    result["services_name"] = %obj.servicesName.get()

