#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_license_billing_details
import model_license_client_link
import model_license_extra_info_tables
import model_license_service_info
import model_license_service_type

type License* = object
  ## 
  serviceInfo*: LicenseServiceInfo
  clientLinks*: seq[LicenseClientLink]
  billingDetails*: LicenseBillingDetails
  custCurrency*: string ## Customer's currency
  custCurrencySymbol*: string ## Currency symbol for customer
  package*: string ## Package name
  serviceExtra*: seq[string] ## Extra service information
  extraInfoTables*: License_extraInfoTables
  serviceOverviewExtra*: string ## Extra service overview information
  serviceType*: LicenseServiceType
  licenseKey*: string ## License key


# Custom JSON deserialization for License with custom field names
proc to*(node: JsonNode, T: typedesc[License]): License =
  result = License()
  if node.kind == JObject:
    if node.hasKey("serviceInfo"):
      result.serviceInfo = to(node["serviceInfo"], LicenseServiceInfo)
    if node.hasKey("client_links"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["client_links"]
      if arrayNode.kind == JArray:
        result.clientLinks = @[]
        for item in arrayNode.items:
          result.clientLinks.add(to(item, LicenseClientLink))
    if node.hasKey("billingDetails"):
      result.billingDetails = to(node["billingDetails"], LicenseBillingDetails)
    if node.hasKey("custCurrency"):
      result.custCurrency = to(node["custCurrency"], string)
    if node.hasKey("custCurrencySymbol"):
      result.custCurrencySymbol = to(node["custCurrencySymbol"], string)
    if node.hasKey("package"):
      result.package = to(node["package"], string)
    if node.hasKey("serviceExtra"):
      result.serviceExtra = to(node["serviceExtra"], seq[string])
    if node.hasKey("extraInfoTables"):
      result.extraInfoTables = to(node["extraInfoTables"], License_extraInfoTables)
    if node.hasKey("service_overview_extra"):
      result.serviceOverviewExtra = to(node["service_overview_extra"], string)
    if node.hasKey("serviceType"):
      result.serviceType = to(node["serviceType"], LicenseServiceType)
    if node.hasKey("license_key"):
      result.licenseKey = to(node["license_key"], string)

# Custom JSON serialization for License with custom field names
proc `%`*(obj: License): JsonNode =
  result = newJObject()
  result["serviceInfo"] = %obj.serviceInfo
  result["client_links"] = %obj.clientLinks
  result["billingDetails"] = %obj.billingDetails
  result["custCurrency"] = %obj.custCurrency
  result["custCurrencySymbol"] = %obj.custCurrencySymbol
  result["package"] = %obj.package
  result["serviceExtra"] = %obj.serviceExtra
  result["extraInfoTables"] = %obj.extraInfoTables
  result["service_overview_extra"] = %obj.serviceOverviewExtra
  result["serviceType"] = %obj.serviceType
  result["license_key"] = %obj.licenseKey

