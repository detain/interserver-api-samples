#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_server_lease

type ServerAsset* = object
  ## 
  id*: int ## Unique identifier for the asset.
  orderId*: string ## Order identifier for the asset.
  hostname*: string ## Hostname associated with the asset.
  status*: string ## Status of the asset.
  primaryIpv4*: string ## Primary IPv4 address of the asset.
  primaryIpv6*: string ## Primary IPv6 address of the asset.
  datacenter*: string ## Datacenter identifier for the asset.
  typeId*: string ## Type identifier for the asset.
  assetTag*: string ## Asset tag associated with the asset.
  rack*: string ## Rack identifier for the asset.
  row*: string ## Row identifier for the asset.
  col*: string ## Column identifier for the asset.
  unitStart*: string ## Starting unit identifier for the asset.
  unitEnd*: string ## Ending unit identifier for the asset.
  unitSub*: string ## Subunit identifier for the asset.
  ipmiMac*: string ## IPMI MAC address associated with the asset.
  ipmiIp*: string ## IPMI IP address associated with the asset.
  ipmiWorking*: string ## IPMI working status associated with the asset.
  company*: string ## Company associated with the asset.
  comments*: string ## Comments associated with the asset.
  make*: string ## Make of the asset.
  model*: string ## Model of the asset.
  description*: string ## Description of the asset.
  customerId*: string ## Customer identifier for the asset.
  externalId*: string ## External identifier for the asset.
  billingStatus*: string ## Billing status of the asset.
  overdue*: string ## Overdue status of the asset.
  assetId*: string ## Asset identifier for the asset.
  assetName*: string ## Name of the asset.
  rackId*: string ## Rack identifier for the asset.
  rackName*: string ## Rack name associated with the asset.
  rackLocation*: string ## Location of the rack associated with the asset.
  rackSize*: string ## Size of the rack associated with the asset.
  rackX*: string ## X-coordinate of the asset within the rack.
  rackY*: string ## Y-coordinate of the asset within the rack.
  switchports*: seq[int] ## List of switchports associated with the asset.
  vlans*: seq[string] ## List of VLANs associated with the asset.
  vlans6*: seq[string] ## List of IPv6 VLANs associated with the asset.
  lease*: ServerLease
  mac*: Option[string] ## MAC address associated with the asset.
  ipmiAdminUsername*: Option[string] ## IPMI admin username associated with the asset.
  ipmiAdminPassword*: Option[string] ## IPMI admin password associated with the asset.
  ipmiClientUsername*: Option[string] ## IPMI client username associated with the asset.
  ipmiClientPassword*: Option[string] ## IPMI client password associated with the asset.
  ipmiUpdated*: Option[string] ## IPMI update status associated with the asset.
  createTimestamp*: Option[string] ## Timestamp of asset creation.
  updateTimestamp*: Option[string] ## Timestamp of asset update.
  comment*: Option[string] ## Comment associated with the asset.


# Custom JSON deserialization for ServerAsset with custom field names
proc to*(node: JsonNode, T: typedesc[ServerAsset]): ServerAsset =
  result = ServerAsset()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], int)
    if node.hasKey("order_id"):
      result.orderId = to(node["order_id"], string)
    if node.hasKey("hostname"):
      result.hostname = to(node["hostname"], string)
    if node.hasKey("status"):
      result.status = to(node["status"], string)
    if node.hasKey("primary_ipv4"):
      result.primaryIpv4 = to(node["primary_ipv4"], string)
    if node.hasKey("primary_ipv6"):
      result.primaryIpv6 = to(node["primary_ipv6"], string)
    if node.hasKey("datacenter"):
      result.datacenter = to(node["datacenter"], string)
    if node.hasKey("type_id"):
      result.typeId = to(node["type_id"], string)
    if node.hasKey("asset_tag"):
      result.assetTag = to(node["asset_tag"], string)
    if node.hasKey("rack"):
      result.rack = to(node["rack"], string)
    if node.hasKey("row"):
      result.row = to(node["row"], string)
    if node.hasKey("col"):
      result.col = to(node["col"], string)
    if node.hasKey("unit_start"):
      result.unitStart = to(node["unit_start"], string)
    if node.hasKey("unit_end"):
      result.unitEnd = to(node["unit_end"], string)
    if node.hasKey("unit_sub"):
      result.unitSub = to(node["unit_sub"], string)
    if node.hasKey("ipmi_mac"):
      result.ipmiMac = to(node["ipmi_mac"], string)
    if node.hasKey("ipmi_ip"):
      result.ipmiIp = to(node["ipmi_ip"], string)
    if node.hasKey("ipmi_working"):
      result.ipmiWorking = to(node["ipmi_working"], string)
    if node.hasKey("company"):
      result.company = to(node["company"], string)
    if node.hasKey("comments"):
      result.comments = to(node["comments"], string)
    if node.hasKey("make"):
      result.make = to(node["make"], string)
    if node.hasKey("model"):
      result.model = to(node["model"], string)
    if node.hasKey("description"):
      result.description = to(node["description"], string)
    if node.hasKey("customer_id"):
      result.customerId = to(node["customer_id"], string)
    if node.hasKey("external_id"):
      result.externalId = to(node["external_id"], string)
    if node.hasKey("billing_status"):
      result.billingStatus = to(node["billing_status"], string)
    if node.hasKey("overdue"):
      result.overdue = to(node["overdue"], string)
    if node.hasKey("asset_id"):
      result.assetId = to(node["asset_id"], string)
    if node.hasKey("asset_name"):
      result.assetName = to(node["asset_name"], string)
    if node.hasKey("rack_id"):
      result.rackId = to(node["rack_id"], string)
    if node.hasKey("rack_name"):
      result.rackName = to(node["rack_name"], string)
    if node.hasKey("rack_location"):
      result.rackLocation = to(node["rack_location"], string)
    if node.hasKey("rack_size"):
      result.rackSize = to(node["rack_size"], string)
    if node.hasKey("rack_x"):
      result.rackX = to(node["rack_x"], string)
    if node.hasKey("rack_y"):
      result.rackY = to(node["rack_y"], string)
    if node.hasKey("switchports"):
      result.switchports = to(node["switchports"], seq[int])
    if node.hasKey("vlans"):
      result.vlans = to(node["vlans"], seq[string])
    if node.hasKey("vlans6"):
      result.vlans6 = to(node["vlans6"], seq[string])
    if node.hasKey("lease"):
      result.lease = to(node["lease"], ServerLease)
    if node.hasKey("mac") and node["mac"].kind != JNull:
      result.mac = some(to(node["mac"], typeof(result.mac.get())))
    if node.hasKey("ipmi_admin_username") and node["ipmi_admin_username"].kind != JNull:
      result.ipmiAdminUsername = some(to(node["ipmi_admin_username"], typeof(result.ipmiAdminUsername.get())))
    if node.hasKey("ipmi_admin_password") and node["ipmi_admin_password"].kind != JNull:
      result.ipmiAdminPassword = some(to(node["ipmi_admin_password"], typeof(result.ipmiAdminPassword.get())))
    if node.hasKey("ipmi_client_username") and node["ipmi_client_username"].kind != JNull:
      result.ipmiClientUsername = some(to(node["ipmi_client_username"], typeof(result.ipmiClientUsername.get())))
    if node.hasKey("ipmi_client_password") and node["ipmi_client_password"].kind != JNull:
      result.ipmiClientPassword = some(to(node["ipmi_client_password"], typeof(result.ipmiClientPassword.get())))
    if node.hasKey("ipmi_updated") and node["ipmi_updated"].kind != JNull:
      result.ipmiUpdated = some(to(node["ipmi_updated"], typeof(result.ipmiUpdated.get())))
    if node.hasKey("create_timestamp") and node["create_timestamp"].kind != JNull:
      result.createTimestamp = some(to(node["create_timestamp"], typeof(result.createTimestamp.get())))
    if node.hasKey("update_timestamp") and node["update_timestamp"].kind != JNull:
      result.updateTimestamp = some(to(node["update_timestamp"], typeof(result.updateTimestamp.get())))
    if node.hasKey("comment") and node["comment"].kind != JNull:
      result.comment = some(to(node["comment"], typeof(result.comment.get())))

# Custom JSON serialization for ServerAsset with custom field names
proc `%`*(obj: ServerAsset): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["order_id"] = %obj.orderId
  result["hostname"] = %obj.hostname
  result["status"] = %obj.status
  result["primary_ipv4"] = %obj.primaryIpv4
  result["primary_ipv6"] = %obj.primaryIpv6
  result["datacenter"] = %obj.datacenter
  result["type_id"] = %obj.typeId
  result["asset_tag"] = %obj.assetTag
  result["rack"] = %obj.rack
  result["row"] = %obj.row
  result["col"] = %obj.col
  result["unit_start"] = %obj.unitStart
  result["unit_end"] = %obj.unitEnd
  result["unit_sub"] = %obj.unitSub
  result["ipmi_mac"] = %obj.ipmiMac
  result["ipmi_ip"] = %obj.ipmiIp
  result["ipmi_working"] = %obj.ipmiWorking
  result["company"] = %obj.company
  result["comments"] = %obj.comments
  result["make"] = %obj.make
  result["model"] = %obj.model
  result["description"] = %obj.description
  result["customer_id"] = %obj.customerId
  result["external_id"] = %obj.externalId
  result["billing_status"] = %obj.billingStatus
  result["overdue"] = %obj.overdue
  result["asset_id"] = %obj.assetId
  result["asset_name"] = %obj.assetName
  result["rack_id"] = %obj.rackId
  result["rack_name"] = %obj.rackName
  result["rack_location"] = %obj.rackLocation
  result["rack_size"] = %obj.rackSize
  result["rack_x"] = %obj.rackX
  result["rack_y"] = %obj.rackY
  result["switchports"] = %obj.switchports
  result["vlans"] = %obj.vlans
  result["vlans6"] = %obj.vlans6
  result["lease"] = %obj.lease
  if obj.mac.isSome():
    result["mac"] = %obj.mac.get()
  if obj.ipmiAdminUsername.isSome():
    result["ipmi_admin_username"] = %obj.ipmiAdminUsername.get()
  if obj.ipmiAdminPassword.isSome():
    result["ipmi_admin_password"] = %obj.ipmiAdminPassword.get()
  if obj.ipmiClientUsername.isSome():
    result["ipmi_client_username"] = %obj.ipmiClientUsername.get()
  if obj.ipmiClientPassword.isSome():
    result["ipmi_client_password"] = %obj.ipmiClientPassword.get()
  if obj.ipmiUpdated.isSome():
    result["ipmi_updated"] = %obj.ipmiUpdated.get()
  if obj.createTimestamp.isSome():
    result["create_timestamp"] = %obj.createTimestamp.get()
  if obj.updateTimestamp.isSome():
    result["update_timestamp"] = %obj.updateTimestamp.get()
  if obj.comment.isSome():
    result["comment"] = %obj.comment.get()

