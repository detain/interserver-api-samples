#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type VpsServiceAddons* = object
  ## Add-on services and IP address information for a VPS, including control panel licenses and extra IP assignments.
  hasCpanel*: Option[bool] ## Whether a cPanel license is active on this VPS.
  hasDirectadmin*: Option[bool] ## Whether a DirectAdmin license is active on this VPS.
  hasFantastico*: Option[bool] ## Whether a Fantastico license is active on this VPS.
  hasSoftaculous*: Option[bool] ## Whether a Softaculous license is active on this VPS.
  hasHdspace*: Option[bool] ## Whether extra disk space has been added to this VPS.
  dedicatedIp*: Option[bool] ## Whether a dedicated IP address is assigned to this VPS.
  extraIps*: Option[seq[string]] ## List of additional IPv4 addresses assigned to this VPS.
  extraIps6*: Option[seq[string]] ## List of additional IPv6 addresses assigned to this VPS.
  unpaidIps*: Option[seq[string]] ## List of IP addresses that have unpaid charges.
  ips*: Option[seq[string]] ## All IPv4 addresses assigned to this VPS.
  ips6*: Option[seq[string]] ## All IPv6 addresses assigned to this VPS.
  cpanelId*: Option[int] ## The add-on service ID for the cPanel license.
  cost*: Option[int] ## Total monthly add-on cost in cents.
  ids*: Option[seq[string]] ## List of add-on service IDs active on this VPS.
  rdata*: Option[seq[string]] ## Raw add-on data entries.


# Custom JSON deserialization for VpsServiceAddons with custom field names
proc to*(node: JsonNode, T: typedesc[VpsServiceAddons]): VpsServiceAddons =
  result = VpsServiceAddons()
  if node.kind == JObject:
    if node.hasKey("has_cpanel") and node["has_cpanel"].kind != JNull:
      result.hasCpanel = some(to(node["has_cpanel"], typeof(result.hasCpanel.get())))
    if node.hasKey("has_directadmin") and node["has_directadmin"].kind != JNull:
      result.hasDirectadmin = some(to(node["has_directadmin"], typeof(result.hasDirectadmin.get())))
    if node.hasKey("has_fantastico") and node["has_fantastico"].kind != JNull:
      result.hasFantastico = some(to(node["has_fantastico"], typeof(result.hasFantastico.get())))
    if node.hasKey("has_softaculous") and node["has_softaculous"].kind != JNull:
      result.hasSoftaculous = some(to(node["has_softaculous"], typeof(result.hasSoftaculous.get())))
    if node.hasKey("has_hdspace") and node["has_hdspace"].kind != JNull:
      result.hasHdspace = some(to(node["has_hdspace"], typeof(result.hasHdspace.get())))
    if node.hasKey("dedicated_ip") and node["dedicated_ip"].kind != JNull:
      result.dedicatedIp = some(to(node["dedicated_ip"], typeof(result.dedicatedIp.get())))
    if node.hasKey("extra_ips") and node["extra_ips"].kind != JNull:
      result.extraIps = some(to(node["extra_ips"], typeof(result.extraIps.get())))
    if node.hasKey("extra_ips6") and node["extra_ips6"].kind != JNull:
      result.extraIps6 = some(to(node["extra_ips6"], typeof(result.extraIps6.get())))
    if node.hasKey("unpaid_ips") and node["unpaid_ips"].kind != JNull:
      result.unpaidIps = some(to(node["unpaid_ips"], typeof(result.unpaidIps.get())))
    if node.hasKey("ips") and node["ips"].kind != JNull:
      result.ips = some(to(node["ips"], typeof(result.ips.get())))
    if node.hasKey("ips6") and node["ips6"].kind != JNull:
      result.ips6 = some(to(node["ips6"], typeof(result.ips6.get())))
    if node.hasKey("cpanel_id") and node["cpanel_id"].kind != JNull:
      result.cpanelId = some(to(node["cpanel_id"], typeof(result.cpanelId.get())))
    if node.hasKey("cost") and node["cost"].kind != JNull:
      result.cost = some(to(node["cost"], typeof(result.cost.get())))
    if node.hasKey("ids") and node["ids"].kind != JNull:
      result.ids = some(to(node["ids"], typeof(result.ids.get())))
    if node.hasKey("rdata") and node["rdata"].kind != JNull:
      result.rdata = some(to(node["rdata"], typeof(result.rdata.get())))

# Custom JSON serialization for VpsServiceAddons with custom field names
proc `%`*(obj: VpsServiceAddons): JsonNode =
  result = newJObject()
  if obj.hasCpanel.isSome():
    result["has_cpanel"] = %obj.hasCpanel.get()
  if obj.hasDirectadmin.isSome():
    result["has_directadmin"] = %obj.hasDirectadmin.get()
  if obj.hasFantastico.isSome():
    result["has_fantastico"] = %obj.hasFantastico.get()
  if obj.hasSoftaculous.isSome():
    result["has_softaculous"] = %obj.hasSoftaculous.get()
  if obj.hasHdspace.isSome():
    result["has_hdspace"] = %obj.hasHdspace.get()
  if obj.dedicatedIp.isSome():
    result["dedicated_ip"] = %obj.dedicatedIp.get()
  if obj.extraIps.isSome():
    result["extra_ips"] = %obj.extraIps.get()
  if obj.extraIps6.isSome():
    result["extra_ips6"] = %obj.extraIps6.get()
  if obj.unpaidIps.isSome():
    result["unpaid_ips"] = %obj.unpaidIps.get()
  if obj.ips.isSome():
    result["ips"] = %obj.ips.get()
  if obj.ips6.isSome():
    result["ips6"] = %obj.ips6.get()
  if obj.cpanelId.isSome():
    result["cpanel_id"] = %obj.cpanelId.get()
  if obj.cost.isSome():
    result["cost"] = %obj.cost.get()
  if obj.ids.isSome():
    result["ids"] = %obj.ids.get()
  if obj.rdata.isSome():
    result["rdata"] = %obj.rdata.get()

