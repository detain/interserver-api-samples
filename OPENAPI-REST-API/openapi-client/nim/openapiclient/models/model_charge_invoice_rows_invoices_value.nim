#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_charge_invoice_rows_invoices_value_paid_invoices_value

type InvoicesPaid* {.pure.} = enum
  `0`
  `1`

type ChargeInvoiceRowsInvoicesValue* = object
  ## 
  invoicesId*: Option[float] ## Unique invoice ID.
  invoicesDescription*: Option[string] ## Description of the invoice charge.
  invoicesAmount*: Option[float] ## Invoice amount.
  invoicesDate*: Option[string] ## Invoice date.
  invoicesPaid*: Option[InvoicesPaid]
  invoicesDueDate*: Option[string]
  invoicesCurrency*: Option[string]
  currencySymbol*: Option[string]
  invoicesDateFormatted*: Option[string]
  paidInvoices*: Option[Table[string, ChargeInvoiceRows_invoices_value_paid_invoices_value]] ## This is optional when invoices_paid = 1 this array will show

func `%`*(v: InvoicesPaid): JsonNode =
  result = case v:
    of InvoicesPaid.`0`: %"0"
    of InvoicesPaid.`1`: %"1"
func `$`*(v: InvoicesPaid): string =
  result = case v:
    of InvoicesPaid.`0`: $("0")
    of InvoicesPaid.`1`: $("1")

proc to*(node: JsonNode, T: typedesc[InvoicesPaid]): InvoicesPaid =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum InvoicesPaid, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("0"):
    return InvoicesPaid.`0`
  of $("1"):
    return InvoicesPaid.`1`
  else:
    raise newException(ValueError, "Invalid enum value for InvoicesPaid: " & strVal)


# Custom JSON deserialization for ChargeInvoiceRowsInvoicesValue with custom field names
proc to*(node: JsonNode, T: typedesc[ChargeInvoiceRowsInvoicesValue]): ChargeInvoiceRowsInvoicesValue =
  result = ChargeInvoiceRowsInvoicesValue()
  if node.kind == JObject:
    if node.hasKey("invoices_id") and node["invoices_id"].kind != JNull:
      result.invoicesId = some(to(node["invoices_id"], typeof(result.invoicesId.get())))
    if node.hasKey("invoices_description") and node["invoices_description"].kind != JNull:
      result.invoicesDescription = some(to(node["invoices_description"], typeof(result.invoicesDescription.get())))
    if node.hasKey("invoices_amount") and node["invoices_amount"].kind != JNull:
      result.invoicesAmount = some(to(node["invoices_amount"], typeof(result.invoicesAmount.get())))
    if node.hasKey("invoices_date") and node["invoices_date"].kind != JNull:
      result.invoicesDate = some(to(node["invoices_date"], typeof(result.invoicesDate.get())))
    if node.hasKey("invoices_paid") and node["invoices_paid"].kind != JNull:
      result.invoicesPaid = some(to(node["invoices_paid"], InvoicesPaid))
    if node.hasKey("invoices_due_date") and node["invoices_due_date"].kind != JNull:
      result.invoicesDueDate = some(to(node["invoices_due_date"], typeof(result.invoicesDueDate.get())))
    if node.hasKey("invoices_currency") and node["invoices_currency"].kind != JNull:
      result.invoicesCurrency = some(to(node["invoices_currency"], typeof(result.invoicesCurrency.get())))
    if node.hasKey("currency_symbol") and node["currency_symbol"].kind != JNull:
      result.currencySymbol = some(to(node["currency_symbol"], typeof(result.currencySymbol.get())))
    if node.hasKey("invoices_date_formatted") and node["invoices_date_formatted"].kind != JNull:
      result.invoicesDateFormatted = some(to(node["invoices_date_formatted"], typeof(result.invoicesDateFormatted.get())))
    if node.hasKey("paid_invoices") and node["paid_invoices"].kind != JNull:
      result.paidInvoices = some(to(node["paid_invoices"], typeof(result.paidInvoices.get())))

# Custom JSON serialization for ChargeInvoiceRowsInvoicesValue with custom field names
proc `%`*(obj: ChargeInvoiceRowsInvoicesValue): JsonNode =
  result = newJObject()
  if obj.invoicesId.isSome():
    result["invoices_id"] = %obj.invoicesId.get()
  if obj.invoicesDescription.isSome():
    result["invoices_description"] = %obj.invoicesDescription.get()
  if obj.invoicesAmount.isSome():
    result["invoices_amount"] = %obj.invoicesAmount.get()
  if obj.invoicesDate.isSome():
    result["invoices_date"] = %obj.invoicesDate.get()
  if obj.invoicesPaid.isSome():
    result["invoices_paid"] = %obj.invoicesPaid.get()
  if obj.invoicesDueDate.isSome():
    result["invoices_due_date"] = %obj.invoicesDueDate.get()
  if obj.invoicesCurrency.isSome():
    result["invoices_currency"] = %obj.invoicesCurrency.get()
  if obj.currencySymbol.isSome():
    result["currency_symbol"] = %obj.currencySymbol.get()
  if obj.invoicesDateFormatted.isSome():
    result["invoices_date_formatted"] = %obj.invoicesDateFormatted.get()
  if obj.paidInvoices.isSome():
    result["paid_invoices"] = %obj.paidInvoices.get()

