#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_server_billing_details
import model_server_client_link
import model_server_extra_info_tables
import model_server_locations
import model_server_network_info
import model_server_service_info

type Server* = object
  ## 
  ipmiAuth*: bool
  clientLinks*: seq[ServerClientLink]
  billingDetails*: ServerBillingDetails
  custCurrency*: string
  custCurrencySymbol*: string
  package*: string
  serviceExtra*: seq[string]
  locations*: Server_locations
  networkInfo*: ServerNetworkInfo
  extraInfoTables*: ServerExtraInfoTables
  serviceInfo*: ServerServiceInfo


# Custom JSON deserialization for Server with custom field names
proc to*(node: JsonNode, T: typedesc[Server]): Server =
  result = Server()
  if node.kind == JObject:
    if node.hasKey("ipmiAuth"):
      result.ipmiAuth = to(node["ipmiAuth"], bool)
    if node.hasKey("client_links"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["client_links"]
      if arrayNode.kind == JArray:
        result.clientLinks = @[]
        for item in arrayNode.items:
          result.clientLinks.add(to(item, ServerClientLink))
    if node.hasKey("billingDetails"):
      result.billingDetails = to(node["billingDetails"], ServerBillingDetails)
    if node.hasKey("custCurrency"):
      result.custCurrency = to(node["custCurrency"], string)
    if node.hasKey("custCurrencySymbol"):
      result.custCurrencySymbol = to(node["custCurrencySymbol"], string)
    if node.hasKey("package"):
      result.package = to(node["package"], string)
    if node.hasKey("serviceExtra"):
      result.serviceExtra = to(node["serviceExtra"], seq[string])
    if node.hasKey("locations"):
      result.locations = to(node["locations"], Server_locations)
    if node.hasKey("networkInfo"):
      result.networkInfo = to(node["networkInfo"], ServerNetworkInfo)
    if node.hasKey("extraInfoTables"):
      result.extraInfoTables = to(node["extraInfoTables"], ServerExtraInfoTables)
    if node.hasKey("serviceInfo"):
      result.serviceInfo = to(node["serviceInfo"], ServerServiceInfo)

# Custom JSON serialization for Server with custom field names
proc `%`*(obj: Server): JsonNode =
  result = newJObject()
  result["ipmiAuth"] = %obj.ipmiAuth
  result["client_links"] = %obj.clientLinks
  result["billingDetails"] = %obj.billingDetails
  result["custCurrency"] = %obj.custCurrency
  result["custCurrencySymbol"] = %obj.custCurrencySymbol
  result["package"] = %obj.package
  result["serviceExtra"] = %obj.serviceExtra
  result["locations"] = %obj.locations
  result["networkInfo"] = %obj.networkInfo
  result["extraInfoTables"] = %obj.extraInfoTables
  result["serviceInfo"] = %obj.serviceInfo

