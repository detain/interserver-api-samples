#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_any_type

type TicketsRow* = object
  ## Information about a single ticket.
  title*: string
  ticketmaskid*: string
  lastreplier*: string
  status*: string
  priority*: string
  totalReplies*: int
  lastactivity*: string
  departmenttitle*: string
  ticketid*: int
  canClose*: string
  attachments*: Option[JsonNode]
  statusText*: string
  checked*: bool


# Custom JSON deserialization for TicketsRow with custom field names
proc to*(node: JsonNode, T: typedesc[TicketsRow]): TicketsRow =
  result = TicketsRow()
  if node.kind == JObject:
    if node.hasKey("title"):
      result.title = to(node["title"], string)
    if node.hasKey("ticketmaskid"):
      result.ticketmaskid = to(node["ticketmaskid"], string)
    if node.hasKey("lastreplier"):
      result.lastreplier = to(node["lastreplier"], string)
    if node.hasKey("status"):
      result.status = to(node["status"], string)
    if node.hasKey("priority"):
      result.priority = to(node["priority"], string)
    if node.hasKey("total_replies"):
      result.totalReplies = to(node["total_replies"], int)
    if node.hasKey("lastactivity"):
      result.lastactivity = to(node["lastactivity"], string)
    if node.hasKey("departmenttitle"):
      result.departmenttitle = to(node["departmenttitle"], string)
    if node.hasKey("ticketid"):
      result.ticketid = to(node["ticketid"], int)
    if node.hasKey("can_close"):
      result.canClose = to(node["can_close"], string)
    if node.hasKey("attachments") and node["attachments"].kind != JNull:
      result.attachments = some(to(node["attachments"], typeof(result.attachments.get())))
    if node.hasKey("status_text"):
      result.statusText = to(node["status_text"], string)
    if node.hasKey("checked"):
      result.checked = to(node["checked"], bool)

# Custom JSON serialization for TicketsRow with custom field names
proc `%`*(obj: TicketsRow): JsonNode =
  result = newJObject()
  result["title"] = %obj.title
  result["ticketmaskid"] = %obj.ticketmaskid
  result["lastreplier"] = %obj.lastreplier
  result["status"] = %obj.status
  result["priority"] = %obj.priority
  result["total_replies"] = %obj.totalReplies
  result["lastactivity"] = %obj.lastactivity
  result["departmenttitle"] = %obj.departmenttitle
  result["ticketid"] = %obj.ticketid
  result["can_close"] = %obj.canClose
  if obj.attachments.isSome():
    result["attachments"] = %obj.attachments.get()
  result["status_text"] = %obj.statusText
  result["checked"] = %obj.checked

