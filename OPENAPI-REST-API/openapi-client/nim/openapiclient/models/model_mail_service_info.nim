#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type MailServiceInfo* = object
  ## 
  mailId*: string ## The ID of the mail service.
  mailType*: string ## The type of mail service.
  mailCurrency*: string ## The currency of the mail service.
  mailOrderDate*: string ## The order date of the mail service.
  mailCustid*: string ## The customer ID associated with the mail service.
  mailQuota*: string ## The mail quota for the service.
  mailStatus*: string ## The status of the mail service.
  mailInvoice*: string ## The invoice ID of the mail service.
  mailUsername*: Option[string] ## The username associated with the mail service.
  mailIp*: Option[string] ## The IP address associated with the mail service.
  mailCoupon*: Option[string] ## The coupon associated with the mail service.
  mailExtra*: Option[string] ## Additional information for the mail service.
  mailServerStatus*: Option[string] ## The server status of the mail service.
  mailComment*: Option[string] ## Additional comments for the mail service.


# Custom JSON deserialization for MailServiceInfo with custom field names
proc to*(node: JsonNode, T: typedesc[MailServiceInfo]): MailServiceInfo =
  result = MailServiceInfo()
  if node.kind == JObject:
    if node.hasKey("mail_id"):
      result.mailId = to(node["mail_id"], string)
    if node.hasKey("mail_type"):
      result.mailType = to(node["mail_type"], string)
    if node.hasKey("mail_currency"):
      result.mailCurrency = to(node["mail_currency"], string)
    if node.hasKey("mail_order_date"):
      result.mailOrderDate = to(node["mail_order_date"], string)
    if node.hasKey("mail_custid"):
      result.mailCustid = to(node["mail_custid"], string)
    if node.hasKey("mail_quota"):
      result.mailQuota = to(node["mail_quota"], string)
    if node.hasKey("mail_status"):
      result.mailStatus = to(node["mail_status"], string)
    if node.hasKey("mail_invoice"):
      result.mailInvoice = to(node["mail_invoice"], string)
    if node.hasKey("mail_username") and node["mail_username"].kind != JNull:
      result.mailUsername = some(to(node["mail_username"], typeof(result.mailUsername.get())))
    if node.hasKey("mail_ip") and node["mail_ip"].kind != JNull:
      result.mailIp = some(to(node["mail_ip"], typeof(result.mailIp.get())))
    if node.hasKey("mail_coupon") and node["mail_coupon"].kind != JNull:
      result.mailCoupon = some(to(node["mail_coupon"], typeof(result.mailCoupon.get())))
    if node.hasKey("mail_extra") and node["mail_extra"].kind != JNull:
      result.mailExtra = some(to(node["mail_extra"], typeof(result.mailExtra.get())))
    if node.hasKey("mail_server_status") and node["mail_server_status"].kind != JNull:
      result.mailServerStatus = some(to(node["mail_server_status"], typeof(result.mailServerStatus.get())))
    if node.hasKey("mail_comment") and node["mail_comment"].kind != JNull:
      result.mailComment = some(to(node["mail_comment"], typeof(result.mailComment.get())))

# Custom JSON serialization for MailServiceInfo with custom field names
proc `%`*(obj: MailServiceInfo): JsonNode =
  result = newJObject()
  result["mail_id"] = %obj.mailId
  result["mail_type"] = %obj.mailType
  result["mail_currency"] = %obj.mailCurrency
  result["mail_order_date"] = %obj.mailOrderDate
  result["mail_custid"] = %obj.mailCustid
  result["mail_quota"] = %obj.mailQuota
  result["mail_status"] = %obj.mailStatus
  result["mail_invoice"] = %obj.mailInvoice
  if obj.mailUsername.isSome():
    result["mail_username"] = %obj.mailUsername.get()
  if obj.mailIp.isSome():
    result["mail_ip"] = %obj.mailIp.get()
  if obj.mailCoupon.isSome():
    result["mail_coupon"] = %obj.mailCoupon.get()
  if obj.mailExtra.isSome():
    result["mail_extra"] = %obj.mailExtra.get()
  if obj.mailServerStatus.isSome():
    result["mail_server_status"] = %obj.mailServerStatus.get()
  if obj.mailComment.isSome():
    result["mail_comment"] = %obj.mailComment.get()

