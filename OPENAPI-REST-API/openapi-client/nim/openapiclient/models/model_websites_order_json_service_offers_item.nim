#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type WebsitesOrderJsonServiceOffersItem* = object
  ## 
  serviceOfferId*: Option[string] ## Service offer ID
  serviceId*: Option[string] ## Service ID
  introCost*: Option[int] ## Introductory cost
  renewalCost*: Option[int] ## Renewal cost
  introFrequency*: Option[string] ## Introductory frequency
  renewalFrequency*: Option[string] ## Renewal frequency
  allowCoupon*: Option[string] ## Allow coupon
  serviceModule*: Option[string] ## Service module
  createdAt*: Option[string] ## Creation date
  updatedAt*: Option[string] ## Update date
  deletedAt*: Option[string] ## Deletion date
  currencySymbol*: Option[string] ## Currency symbol


# Custom JSON deserialization for WebsitesOrderJsonServiceOffersItem with custom field names
proc to*(node: JsonNode, T: typedesc[WebsitesOrderJsonServiceOffersItem]): WebsitesOrderJsonServiceOffersItem =
  result = WebsitesOrderJsonServiceOffersItem()
  if node.kind == JObject:
    if node.hasKey("service_offer_id") and node["service_offer_id"].kind != JNull:
      result.serviceOfferId = some(to(node["service_offer_id"], typeof(result.serviceOfferId.get())))
    if node.hasKey("service_id") and node["service_id"].kind != JNull:
      result.serviceId = some(to(node["service_id"], typeof(result.serviceId.get())))
    if node.hasKey("intro_cost") and node["intro_cost"].kind != JNull:
      result.introCost = some(to(node["intro_cost"], typeof(result.introCost.get())))
    if node.hasKey("renewal_cost") and node["renewal_cost"].kind != JNull:
      result.renewalCost = some(to(node["renewal_cost"], typeof(result.renewalCost.get())))
    if node.hasKey("intro_frequency") and node["intro_frequency"].kind != JNull:
      result.introFrequency = some(to(node["intro_frequency"], typeof(result.introFrequency.get())))
    if node.hasKey("renewal_frequency") and node["renewal_frequency"].kind != JNull:
      result.renewalFrequency = some(to(node["renewal_frequency"], typeof(result.renewalFrequency.get())))
    if node.hasKey("allow_coupon") and node["allow_coupon"].kind != JNull:
      result.allowCoupon = some(to(node["allow_coupon"], typeof(result.allowCoupon.get())))
    if node.hasKey("service_module") and node["service_module"].kind != JNull:
      result.serviceModule = some(to(node["service_module"], typeof(result.serviceModule.get())))
    if node.hasKey("created_at") and node["created_at"].kind != JNull:
      result.createdAt = some(to(node["created_at"], typeof(result.createdAt.get())))
    if node.hasKey("updated_at") and node["updated_at"].kind != JNull:
      result.updatedAt = some(to(node["updated_at"], typeof(result.updatedAt.get())))
    if node.hasKey("deleted_at") and node["deleted_at"].kind != JNull:
      result.deletedAt = some(to(node["deleted_at"], typeof(result.deletedAt.get())))
    if node.hasKey("currencySymbol") and node["currencySymbol"].kind != JNull:
      result.currencySymbol = some(to(node["currencySymbol"], typeof(result.currencySymbol.get())))

# Custom JSON serialization for WebsitesOrderJsonServiceOffersItem with custom field names
proc `%`*(obj: WebsitesOrderJsonServiceOffersItem): JsonNode =
  result = newJObject()
  if obj.serviceOfferId.isSome():
    result["service_offer_id"] = %obj.serviceOfferId.get()
  if obj.serviceId.isSome():
    result["service_id"] = %obj.serviceId.get()
  if obj.introCost.isSome():
    result["intro_cost"] = %obj.introCost.get()
  if obj.renewalCost.isSome():
    result["renewal_cost"] = %obj.renewalCost.get()
  if obj.introFrequency.isSome():
    result["intro_frequency"] = %obj.introFrequency.get()
  if obj.renewalFrequency.isSome():
    result["renewal_frequency"] = %obj.renewalFrequency.get()
  if obj.allowCoupon.isSome():
    result["allow_coupon"] = %obj.allowCoupon.get()
  if obj.serviceModule.isSome():
    result["service_module"] = %obj.serviceModule.get()
  if obj.createdAt.isSome():
    result["created_at"] = %obj.createdAt.get()
  if obj.updatedAt.isSome():
    result["updated_at"] = %obj.updatedAt.get()
  if obj.deletedAt.isSome():
    result["deleted_at"] = %obj.deletedAt.get()
  if obj.currencySymbol.isSome():
    result["currencySymbol"] = %obj.currencySymbol.get()

