#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type WebsitesOrderServiceOffer* = object
  ## 
  serviceOfferId*: string ## The ID of the service offer.
  serviceId*: string ## The ID of the associated service.
  introCost*: string ## The introductory cost of the service offer.
  renewalCost*: string ## The renewal cost of the service offer.
  introFrequency*: string ## The introductory frequency of the service offer.
  renewalFrequency*: string ## The renewal frequency of the service offer.
  allowCoupon*: string ## Indicates if coupons are allowed (1 for yes, 0 for no).
  serviceModule*: string ## The module of the service offer.
  createdAt*: string ## The creation timestamp of the service offer.
  updatedAt*: Option[string] ## The update timestamp of the service offer.
  deletedAt*: Option[string] ## The deletion timestamp of the service offer.


# Custom JSON deserialization for WebsitesOrderServiceOffer with custom field names
proc to*(node: JsonNode, T: typedesc[WebsitesOrderServiceOffer]): WebsitesOrderServiceOffer =
  result = WebsitesOrderServiceOffer()
  if node.kind == JObject:
    if node.hasKey("service_offer_id"):
      result.serviceOfferId = to(node["service_offer_id"], string)
    if node.hasKey("service_id"):
      result.serviceId = to(node["service_id"], string)
    if node.hasKey("intro_cost"):
      result.introCost = to(node["intro_cost"], string)
    if node.hasKey("renewal_cost"):
      result.renewalCost = to(node["renewal_cost"], string)
    if node.hasKey("intro_frequency"):
      result.introFrequency = to(node["intro_frequency"], string)
    if node.hasKey("renewal_frequency"):
      result.renewalFrequency = to(node["renewal_frequency"], string)
    if node.hasKey("allow_coupon"):
      result.allowCoupon = to(node["allow_coupon"], string)
    if node.hasKey("service_module"):
      result.serviceModule = to(node["service_module"], string)
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], string)
    if node.hasKey("updated_at") and node["updated_at"].kind != JNull:
      result.updatedAt = some(to(node["updated_at"], typeof(result.updatedAt.get())))
    if node.hasKey("deleted_at") and node["deleted_at"].kind != JNull:
      result.deletedAt = some(to(node["deleted_at"], typeof(result.deletedAt.get())))

# Custom JSON serialization for WebsitesOrderServiceOffer with custom field names
proc `%`*(obj: WebsitesOrderServiceOffer): JsonNode =
  result = newJObject()
  result["service_offer_id"] = %obj.serviceOfferId
  result["service_id"] = %obj.serviceId
  result["intro_cost"] = %obj.introCost
  result["renewal_cost"] = %obj.renewalCost
  result["intro_frequency"] = %obj.introFrequency
  result["renewal_frequency"] = %obj.renewalFrequency
  result["allow_coupon"] = %obj.allowCoupon
  result["service_module"] = %obj.serviceModule
  result["created_at"] = %obj.createdAt
  if obj.updatedAt.isSome():
    result["updated_at"] = %obj.updatedAt.get()
  if obj.deletedAt.isSome():
    result["deleted_at"] = %obj.deletedAt.get()

