#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_server_order_bandwidth_li
import model_server_order_config_ids
import model_server_order_cp_li
import model_server_order_cpu_li
import model_server_order_field_labels
import model_server_order_form_values
import model_server_order_ips_li
import model_server_order_memory_li
import model_server_order_os_li
import model_server_order_raid

type ServerOrder* = object
  ## Object representing a server order.
  formValues*: Option[ServerOrderFormValues]
  configIds*: Option[ServerOrderConfigIds]
  cpu*: Option[int] ## Number of CPUs for the server order.
  fieldLabel*: Option[ServerOrderFieldLabels]
  cpuLi*: Option[ServerOrder_cpu_li]
  memoryLi*: Option[ServerOrder_memory_li]
  bandwidthLi*: Option[ServerOrder_bandwidth_li]
  ipsLi*: Option[ServerOrder_ips_li]
  osLi*: Option[ServerOrder_os_li]
  cpLi*: Option[ServerOrder_cp_li]
  raidLi*: Option[seq[ServerOrderRAID]] ## RAID options for the server order.


# Custom JSON deserialization for ServerOrder with custom field names
proc to*(node: JsonNode, T: typedesc[ServerOrder]): ServerOrder =
  result = ServerOrder()
  if node.kind == JObject:
    if node.hasKey("form_values") and node["form_values"].kind != JNull:
      result.formValues = some(to(node["form_values"], typeof(result.formValues.get())))
    if node.hasKey("config_ids") and node["config_ids"].kind != JNull:
      result.configIds = some(to(node["config_ids"], typeof(result.configIds.get())))
    if node.hasKey("cpu") and node["cpu"].kind != JNull:
      result.cpu = some(to(node["cpu"], typeof(result.cpu.get())))
    if node.hasKey("field_label") and node["field_label"].kind != JNull:
      result.fieldLabel = some(to(node["field_label"], typeof(result.fieldLabel.get())))
    if node.hasKey("cpu_li") and node["cpu_li"].kind != JNull:
      result.cpuLi = some(to(node["cpu_li"], typeof(result.cpuLi.get())))
    if node.hasKey("memory_li") and node["memory_li"].kind != JNull:
      result.memoryLi = some(to(node["memory_li"], typeof(result.memoryLi.get())))
    if node.hasKey("bandwidth_li") and node["bandwidth_li"].kind != JNull:
      result.bandwidthLi = some(to(node["bandwidth_li"], typeof(result.bandwidthLi.get())))
    if node.hasKey("ips_li") and node["ips_li"].kind != JNull:
      result.ipsLi = some(to(node["ips_li"], typeof(result.ipsLi.get())))
    if node.hasKey("os_li") and node["os_li"].kind != JNull:
      result.osLi = some(to(node["os_li"], typeof(result.osLi.get())))
    if node.hasKey("cp_li") and node["cp_li"].kind != JNull:
      result.cpLi = some(to(node["cp_li"], typeof(result.cpLi.get())))
    if node.hasKey("raid_li") and node["raid_li"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["raid_li"]
      if arrayNode.kind == JArray:
        var arr: seq[ServerOrderRAID] = @[]
        for item in arrayNode.items:
          arr.add(to(item, ServerOrderRAID))
        result.raidLi = some(arr)

# Custom JSON serialization for ServerOrder with custom field names
proc `%`*(obj: ServerOrder): JsonNode =
  result = newJObject()
  if obj.formValues.isSome():
    result["form_values"] = %obj.formValues.get()
  if obj.configIds.isSome():
    result["config_ids"] = %obj.configIds.get()
  if obj.cpu.isSome():
    result["cpu"] = %obj.cpu.get()
  if obj.fieldLabel.isSome():
    result["field_label"] = %obj.fieldLabel.get()
  if obj.cpuLi.isSome():
    result["cpu_li"] = %obj.cpuLi.get()
  if obj.memoryLi.isSome():
    result["memory_li"] = %obj.memoryLi.get()
  if obj.bandwidthLi.isSome():
    result["bandwidth_li"] = %obj.bandwidthLi.get()
  if obj.ipsLi.isSome():
    result["ips_li"] = %obj.ipsLi.get()
  if obj.osLi.isSome():
    result["os_li"] = %obj.osLi.get()
  if obj.cpLi.isSome():
    result["cp_li"] = %obj.cpLi.get()
  if obj.raidLi.isSome():
    result["raid_li"] = %obj.raidLi.get()

