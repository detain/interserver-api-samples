#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type VpsServiceInfo* = object
  ## 
  vpsId*: Option[string] ## VPS ID
  vpsCustid*: Option[string] ## Customer ID
  vpsServer*: Option[string] ## Server ID
  vpsIp*: Option[string] ## IP address of the VPS
  vpsIpv6*: Option[string] ## IPv6 address of the VPS
  vpsVzid*: Option[string] ## VPS Virtuozzo ID
  vpsCurrency*: Option[string] ## Currency used for billing
  vpsType*: Option[string] ## VPS type
  vpsOrderDate*: Option[string] ## Date of VPS order
  vpsStatus*: Option[string] ## VPS status
  vpsInvoice*: Option[string] ## VPS invoice number
  vpsCoupon*: Option[string] ## VPS coupon code
  vpsExtra*: Option[string] ## Additional information about the VPS
  vpsHostname*: Option[string] ## VPS hostname
  vpsServerStatus*: Option[string] ## Status of the VPS server
  vpsComment*: Option[string] ## Comment associated with the VPS
  vpsSlices*: Option[string] ## Number of VPS slices
  vpsVnc*: Option[string] ## VNC address
  vpsVncPort*: Option[string] ## VNC port
  vpsRootpass*: Option[string] ## Root password of the VPS
  vpsMac*: Option[string] ## MAC address of the VPS
  vpsOs*: Option[string] ## Operating system of the VPS
  vpsVersion*: Option[string] ## Version of the operating system
  vpsLocation*: Option[string] ## Location of the VPS
  vpsPlatform*: Option[string] ## Virtualization platform
  vpsDiskused*: Option[string] ## Amount of disk space used
  vpsDiskmax*: Option[string] ## Maximum disk space available


# Custom JSON deserialization for VpsServiceInfo with custom field names
proc to*(node: JsonNode, T: typedesc[VpsServiceInfo]): VpsServiceInfo =
  result = VpsServiceInfo()
  if node.kind == JObject:
    if node.hasKey("vps_id") and node["vps_id"].kind != JNull:
      result.vpsId = some(to(node["vps_id"], typeof(result.vpsId.get())))
    if node.hasKey("vps_custid") and node["vps_custid"].kind != JNull:
      result.vpsCustid = some(to(node["vps_custid"], typeof(result.vpsCustid.get())))
    if node.hasKey("vps_server") and node["vps_server"].kind != JNull:
      result.vpsServer = some(to(node["vps_server"], typeof(result.vpsServer.get())))
    if node.hasKey("vps_ip") and node["vps_ip"].kind != JNull:
      result.vpsIp = some(to(node["vps_ip"], typeof(result.vpsIp.get())))
    if node.hasKey("vps_ipv6") and node["vps_ipv6"].kind != JNull:
      result.vpsIpv6 = some(to(node["vps_ipv6"], typeof(result.vpsIpv6.get())))
    if node.hasKey("vps_vzid") and node["vps_vzid"].kind != JNull:
      result.vpsVzid = some(to(node["vps_vzid"], typeof(result.vpsVzid.get())))
    if node.hasKey("vps_currency") and node["vps_currency"].kind != JNull:
      result.vpsCurrency = some(to(node["vps_currency"], typeof(result.vpsCurrency.get())))
    if node.hasKey("vps_type") and node["vps_type"].kind != JNull:
      result.vpsType = some(to(node["vps_type"], typeof(result.vpsType.get())))
    if node.hasKey("vps_order_date") and node["vps_order_date"].kind != JNull:
      result.vpsOrderDate = some(to(node["vps_order_date"], typeof(result.vpsOrderDate.get())))
    if node.hasKey("vps_status") and node["vps_status"].kind != JNull:
      result.vpsStatus = some(to(node["vps_status"], typeof(result.vpsStatus.get())))
    if node.hasKey("vps_invoice") and node["vps_invoice"].kind != JNull:
      result.vpsInvoice = some(to(node["vps_invoice"], typeof(result.vpsInvoice.get())))
    if node.hasKey("vps_coupon") and node["vps_coupon"].kind != JNull:
      result.vpsCoupon = some(to(node["vps_coupon"], typeof(result.vpsCoupon.get())))
    if node.hasKey("vps_extra") and node["vps_extra"].kind != JNull:
      result.vpsExtra = some(to(node["vps_extra"], typeof(result.vpsExtra.get())))
    if node.hasKey("vps_hostname") and node["vps_hostname"].kind != JNull:
      result.vpsHostname = some(to(node["vps_hostname"], typeof(result.vpsHostname.get())))
    if node.hasKey("vps_server_status") and node["vps_server_status"].kind != JNull:
      result.vpsServerStatus = some(to(node["vps_server_status"], typeof(result.vpsServerStatus.get())))
    if node.hasKey("vps_comment") and node["vps_comment"].kind != JNull:
      result.vpsComment = some(to(node["vps_comment"], typeof(result.vpsComment.get())))
    if node.hasKey("vps_slices") and node["vps_slices"].kind != JNull:
      result.vpsSlices = some(to(node["vps_slices"], typeof(result.vpsSlices.get())))
    if node.hasKey("vps_vnc") and node["vps_vnc"].kind != JNull:
      result.vpsVnc = some(to(node["vps_vnc"], typeof(result.vpsVnc.get())))
    if node.hasKey("vps_vnc_port") and node["vps_vnc_port"].kind != JNull:
      result.vpsVncPort = some(to(node["vps_vnc_port"], typeof(result.vpsVncPort.get())))
    if node.hasKey("vps_rootpass") and node["vps_rootpass"].kind != JNull:
      result.vpsRootpass = some(to(node["vps_rootpass"], typeof(result.vpsRootpass.get())))
    if node.hasKey("vps_mac") and node["vps_mac"].kind != JNull:
      result.vpsMac = some(to(node["vps_mac"], typeof(result.vpsMac.get())))
    if node.hasKey("vps_os") and node["vps_os"].kind != JNull:
      result.vpsOs = some(to(node["vps_os"], typeof(result.vpsOs.get())))
    if node.hasKey("vps_version") and node["vps_version"].kind != JNull:
      result.vpsVersion = some(to(node["vps_version"], typeof(result.vpsVersion.get())))
    if node.hasKey("vps_location") and node["vps_location"].kind != JNull:
      result.vpsLocation = some(to(node["vps_location"], typeof(result.vpsLocation.get())))
    if node.hasKey("vps_platform") and node["vps_platform"].kind != JNull:
      result.vpsPlatform = some(to(node["vps_platform"], typeof(result.vpsPlatform.get())))
    if node.hasKey("vps_diskused") and node["vps_diskused"].kind != JNull:
      result.vpsDiskused = some(to(node["vps_diskused"], typeof(result.vpsDiskused.get())))
    if node.hasKey("vps_diskmax") and node["vps_diskmax"].kind != JNull:
      result.vpsDiskmax = some(to(node["vps_diskmax"], typeof(result.vpsDiskmax.get())))

# Custom JSON serialization for VpsServiceInfo with custom field names
proc `%`*(obj: VpsServiceInfo): JsonNode =
  result = newJObject()
  if obj.vpsId.isSome():
    result["vps_id"] = %obj.vpsId.get()
  if obj.vpsCustid.isSome():
    result["vps_custid"] = %obj.vpsCustid.get()
  if obj.vpsServer.isSome():
    result["vps_server"] = %obj.vpsServer.get()
  if obj.vpsIp.isSome():
    result["vps_ip"] = %obj.vpsIp.get()
  if obj.vpsIpv6.isSome():
    result["vps_ipv6"] = %obj.vpsIpv6.get()
  if obj.vpsVzid.isSome():
    result["vps_vzid"] = %obj.vpsVzid.get()
  if obj.vpsCurrency.isSome():
    result["vps_currency"] = %obj.vpsCurrency.get()
  if obj.vpsType.isSome():
    result["vps_type"] = %obj.vpsType.get()
  if obj.vpsOrderDate.isSome():
    result["vps_order_date"] = %obj.vpsOrderDate.get()
  if obj.vpsStatus.isSome():
    result["vps_status"] = %obj.vpsStatus.get()
  if obj.vpsInvoice.isSome():
    result["vps_invoice"] = %obj.vpsInvoice.get()
  if obj.vpsCoupon.isSome():
    result["vps_coupon"] = %obj.vpsCoupon.get()
  if obj.vpsExtra.isSome():
    result["vps_extra"] = %obj.vpsExtra.get()
  if obj.vpsHostname.isSome():
    result["vps_hostname"] = %obj.vpsHostname.get()
  if obj.vpsServerStatus.isSome():
    result["vps_server_status"] = %obj.vpsServerStatus.get()
  if obj.vpsComment.isSome():
    result["vps_comment"] = %obj.vpsComment.get()
  if obj.vpsSlices.isSome():
    result["vps_slices"] = %obj.vpsSlices.get()
  if obj.vpsVnc.isSome():
    result["vps_vnc"] = %obj.vpsVnc.get()
  if obj.vpsVncPort.isSome():
    result["vps_vnc_port"] = %obj.vpsVncPort.get()
  if obj.vpsRootpass.isSome():
    result["vps_rootpass"] = %obj.vpsRootpass.get()
  if obj.vpsMac.isSome():
    result["vps_mac"] = %obj.vpsMac.get()
  if obj.vpsOs.isSome():
    result["vps_os"] = %obj.vpsOs.get()
  if obj.vpsVersion.isSome():
    result["vps_version"] = %obj.vpsVersion.get()
  if obj.vpsLocation.isSome():
    result["vps_location"] = %obj.vpsLocation.get()
  if obj.vpsPlatform.isSome():
    result["vps_platform"] = %obj.vpsPlatform.get()
  if obj.vpsDiskused.isSome():
    result["vps_diskused"] = %obj.vpsDiskused.get()
  if obj.vpsDiskmax.isSome():
    result["vps_diskmax"] = %obj.vpsDiskmax.get()

