#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_object

type DomainSearchResponse* = object
  ## Lookup and suggestion results returned for a domain search.
  success*: Option[bool] ## Indicates whether the registrar search succeeded.
  responseText*: Option[string] ## Human-readable status text from the registrar.
  responseTime*: Option[string] ## Response time as reported by the registrar.
  lookup*: Option[seq[JsonNode]] ## Availability lookup results for queried domains.
  suggest*: Option[seq[JsonNode]] ## Suggested alternative domains and availability data.
  tlds*: Option[seq[string]] ## TLDs evaluated during the search.


# Custom JSON deserialization for DomainSearchResponse with custom field names
proc to*(node: JsonNode, T: typedesc[DomainSearchResponse]): DomainSearchResponse =
  result = DomainSearchResponse()
  if node.kind == JObject:
    if node.hasKey("success") and node["success"].kind != JNull:
      result.success = some(to(node["success"], typeof(result.success.get())))
    if node.hasKey("response_text") and node["response_text"].kind != JNull:
      result.responseText = some(to(node["response_text"], typeof(result.responseText.get())))
    if node.hasKey("response_time") and node["response_time"].kind != JNull:
      result.responseTime = some(to(node["response_time"], typeof(result.responseTime.get())))
    if node.hasKey("lookup") and node["lookup"].kind != JNull:
      result.lookup = some(to(node["lookup"], typeof(result.lookup.get())))
    if node.hasKey("suggest") and node["suggest"].kind != JNull:
      result.suggest = some(to(node["suggest"], typeof(result.suggest.get())))
    if node.hasKey("tlds") and node["tlds"].kind != JNull:
      result.tlds = some(to(node["tlds"], typeof(result.tlds.get())))

# Custom JSON serialization for DomainSearchResponse with custom field names
proc `%`*(obj: DomainSearchResponse): JsonNode =
  result = newJObject()
  if obj.success.isSome():
    result["success"] = %obj.success.get()
  if obj.responseText.isSome():
    result["response_text"] = %obj.responseText.get()
  if obj.responseTime.isSome():
    result["response_time"] = %obj.responseTime.get()
  if obj.lookup.isSome():
    result["lookup"] = %obj.lookup.get()
  if obj.suggest.isSome():
    result["suggest"] = %obj.suggest.get()
  if obj.tlds.isSome():
    result["tlds"] = %obj.tlds.get()

