#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type WebsiteServiceMaster* = object
  ## 
  websiteId*: Option[string] ## Website ID for the service master
  websiteName*: Option[string] ## Website name for the service master
  websiteIp*: Option[string] ## IP address for the service master
  websiteType*: Option[string] ## Website type for the service master
  websiteAvailable*: Option[string] ## Availability status for the service master
  websiteHdsize*: Option[string] ## Hard drive size for the service master
  websiteHdfree*: Option[string] ## Free hard drive space for the service master
  websiteLoad*: Option[string] ## Load for the service master
  websiteLastUpdate*: Option[string] ## Last update date for the service master
  websiteMaxSites*: Option[string] ## Maximum number of sites for the service master
  websiteOrder*: Option[string] ## Order number for the service master
  websitePartitions*: Option[string] ## Partitions for the service master
  websiteDns1*: Option[string] ## DNS server 1 for the service master
  websiteDns2*: Option[string] ## DNS server 2 for the service master


# Custom JSON deserialization for WebsiteServiceMaster with custom field names
proc to*(node: JsonNode, T: typedesc[WebsiteServiceMaster]): WebsiteServiceMaster =
  result = WebsiteServiceMaster()
  if node.kind == JObject:
    if node.hasKey("website_id") and node["website_id"].kind != JNull:
      result.websiteId = some(to(node["website_id"], typeof(result.websiteId.get())))
    if node.hasKey("website_name") and node["website_name"].kind != JNull:
      result.websiteName = some(to(node["website_name"], typeof(result.websiteName.get())))
    if node.hasKey("website_ip") and node["website_ip"].kind != JNull:
      result.websiteIp = some(to(node["website_ip"], typeof(result.websiteIp.get())))
    if node.hasKey("website_type") and node["website_type"].kind != JNull:
      result.websiteType = some(to(node["website_type"], typeof(result.websiteType.get())))
    if node.hasKey("website_available") and node["website_available"].kind != JNull:
      result.websiteAvailable = some(to(node["website_available"], typeof(result.websiteAvailable.get())))
    if node.hasKey("website_hdsize") and node["website_hdsize"].kind != JNull:
      result.websiteHdsize = some(to(node["website_hdsize"], typeof(result.websiteHdsize.get())))
    if node.hasKey("website_hdfree") and node["website_hdfree"].kind != JNull:
      result.websiteHdfree = some(to(node["website_hdfree"], typeof(result.websiteHdfree.get())))
    if node.hasKey("website_load") and node["website_load"].kind != JNull:
      result.websiteLoad = some(to(node["website_load"], typeof(result.websiteLoad.get())))
    if node.hasKey("website_last_update") and node["website_last_update"].kind != JNull:
      result.websiteLastUpdate = some(to(node["website_last_update"], typeof(result.websiteLastUpdate.get())))
    if node.hasKey("website_max_sites") and node["website_max_sites"].kind != JNull:
      result.websiteMaxSites = some(to(node["website_max_sites"], typeof(result.websiteMaxSites.get())))
    if node.hasKey("website_order") and node["website_order"].kind != JNull:
      result.websiteOrder = some(to(node["website_order"], typeof(result.websiteOrder.get())))
    if node.hasKey("website_partitions") and node["website_partitions"].kind != JNull:
      result.websitePartitions = some(to(node["website_partitions"], typeof(result.websitePartitions.get())))
    if node.hasKey("website_dns1") and node["website_dns1"].kind != JNull:
      result.websiteDns1 = some(to(node["website_dns1"], typeof(result.websiteDns1.get())))
    if node.hasKey("website_dns2") and node["website_dns2"].kind != JNull:
      result.websiteDns2 = some(to(node["website_dns2"], typeof(result.websiteDns2.get())))

# Custom JSON serialization for WebsiteServiceMaster with custom field names
proc `%`*(obj: WebsiteServiceMaster): JsonNode =
  result = newJObject()
  if obj.websiteId.isSome():
    result["website_id"] = %obj.websiteId.get()
  if obj.websiteName.isSome():
    result["website_name"] = %obj.websiteName.get()
  if obj.websiteIp.isSome():
    result["website_ip"] = %obj.websiteIp.get()
  if obj.websiteType.isSome():
    result["website_type"] = %obj.websiteType.get()
  if obj.websiteAvailable.isSome():
    result["website_available"] = %obj.websiteAvailable.get()
  if obj.websiteHdsize.isSome():
    result["website_hdsize"] = %obj.websiteHdsize.get()
  if obj.websiteHdfree.isSome():
    result["website_hdfree"] = %obj.websiteHdfree.get()
  if obj.websiteLoad.isSome():
    result["website_load"] = %obj.websiteLoad.get()
  if obj.websiteLastUpdate.isSome():
    result["website_last_update"] = %obj.websiteLastUpdate.get()
  if obj.websiteMaxSites.isSome():
    result["website_max_sites"] = %obj.websiteMaxSites.get()
  if obj.websiteOrder.isSome():
    result["website_order"] = %obj.websiteOrder.get()
  if obj.websitePartitions.isSome():
    result["website_partitions"] = %obj.websitePartitions.get()
  if obj.websiteDns1.isSome():
    result["website_dns1"] = %obj.websiteDns1.get()
  if obj.websiteDns2.isSome():
    result["website_dns2"] = %obj.websiteDns2.get()

