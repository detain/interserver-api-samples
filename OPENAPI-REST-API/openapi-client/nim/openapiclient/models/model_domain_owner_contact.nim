#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type DomainOwnerContact* = object
  ## Contact details for the registered domain owner.
  postalCode*: Option[string]
  orgName*: Option[string]
  country*: Option[string]
  status*: Option[string]
  state*: Option[string]
  firstName*: Option[string]
  lastName*: Option[string]
  address3*: Option[string]
  fax*: Option[string]
  address2*: Option[string]
  email*: Option[string]
  city*: Option[string]
  phone*: Option[string]
  address1*: Option[string]


# Custom JSON deserialization for DomainOwnerContact with custom field names
proc to*(node: JsonNode, T: typedesc[DomainOwnerContact]): DomainOwnerContact =
  result = DomainOwnerContact()
  if node.kind == JObject:
    if node.hasKey("postal_code") and node["postal_code"].kind != JNull:
      result.postalCode = some(to(node["postal_code"], typeof(result.postalCode.get())))
    if node.hasKey("org_name") and node["org_name"].kind != JNull:
      result.orgName = some(to(node["org_name"], typeof(result.orgName.get())))
    if node.hasKey("country") and node["country"].kind != JNull:
      result.country = some(to(node["country"], typeof(result.country.get())))
    if node.hasKey("status") and node["status"].kind != JNull:
      result.status = some(to(node["status"], typeof(result.status.get())))
    if node.hasKey("state") and node["state"].kind != JNull:
      result.state = some(to(node["state"], typeof(result.state.get())))
    if node.hasKey("first_name") and node["first_name"].kind != JNull:
      result.firstName = some(to(node["first_name"], typeof(result.firstName.get())))
    if node.hasKey("last_name") and node["last_name"].kind != JNull:
      result.lastName = some(to(node["last_name"], typeof(result.lastName.get())))
    if node.hasKey("address3") and node["address3"].kind != JNull:
      result.address3 = some(to(node["address3"], typeof(result.address3.get())))
    if node.hasKey("fax") and node["fax"].kind != JNull:
      result.fax = some(to(node["fax"], typeof(result.fax.get())))
    if node.hasKey("address2") and node["address2"].kind != JNull:
      result.address2 = some(to(node["address2"], typeof(result.address2.get())))
    if node.hasKey("email") and node["email"].kind != JNull:
      result.email = some(to(node["email"], typeof(result.email.get())))
    if node.hasKey("city") and node["city"].kind != JNull:
      result.city = some(to(node["city"], typeof(result.city.get())))
    if node.hasKey("phone") and node["phone"].kind != JNull:
      result.phone = some(to(node["phone"], typeof(result.phone.get())))
    if node.hasKey("address1") and node["address1"].kind != JNull:
      result.address1 = some(to(node["address1"], typeof(result.address1.get())))

# Custom JSON serialization for DomainOwnerContact with custom field names
proc `%`*(obj: DomainOwnerContact): JsonNode =
  result = newJObject()
  if obj.postalCode.isSome():
    result["postal_code"] = %obj.postalCode.get()
  if obj.orgName.isSome():
    result["org_name"] = %obj.orgName.get()
  if obj.country.isSome():
    result["country"] = %obj.country.get()
  if obj.status.isSome():
    result["status"] = %obj.status.get()
  if obj.state.isSome():
    result["state"] = %obj.state.get()
  if obj.firstName.isSome():
    result["first_name"] = %obj.firstName.get()
  if obj.lastName.isSome():
    result["last_name"] = %obj.lastName.get()
  if obj.address3.isSome():
    result["address3"] = %obj.address3.get()
  if obj.fax.isSome():
    result["fax"] = %obj.fax.get()
  if obj.address2.isSome():
    result["address2"] = %obj.address2.get()
  if obj.email.isSome():
    result["email"] = %obj.email.get()
  if obj.city.isSome():
    result["city"] = %obj.city.get()
  if obj.phone.isSome():
    result["phone"] = %obj.phone.get()
  if obj.address1.isSome():
    result["address1"] = %obj.address1.get()

