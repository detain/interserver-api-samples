#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_quickserver_billing_details
import model_quickserver_client_link
import model_quickserver_extra_info_tables
import model_quickserver_service_extra
import model_quickserver_service_info
import model_quickserver_service_master

type Quickserver* = object
  ## 
  serviceInfo*: QuickserverServiceInfo
  clientLinks*: seq[QuickserverClientLink]
  billingDetails*: QuickserverBillingDetails
  custCurrency*: string ## Currency of the customer
  custCurrencySymbol*: string ## Currency symbol of the customer
  serviceMaster*: QuickserverServiceMaster
  package*: string ## Package name
  osTemplate*: string ## Operating system template
  serviceExtra*: Quickserver_serviceExtra
  extraInfoTables*: Quickserver_extraInfoTables
  cpuGraphData*: string ## CPU graph data
  bandwidthXaxis*: string ## Bandwidth x-axis data
  bandwidthYaxis*: string ## Bandwidth y-axis data
  module*: string ## Module information
  token*: string ## Authentication token
  serviceDiskUsed*: string ## Used disk space
  serviceDiskTotal*: string ## Total disk space
  diskPercentage*: float ## Disk usage percentage
  memory*: string ## Memory information
  hdd*: string ## HDD information
  serviceOverviewExtra*: seq[string]


# Custom JSON deserialization for Quickserver with custom field names
proc to*(node: JsonNode, T: typedesc[Quickserver]): Quickserver =
  result = Quickserver()
  if node.kind == JObject:
    if node.hasKey("serviceInfo"):
      result.serviceInfo = to(node["serviceInfo"], QuickserverServiceInfo)
    if node.hasKey("client_links"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["client_links"]
      if arrayNode.kind == JArray:
        result.clientLinks = @[]
        for item in arrayNode.items:
          result.clientLinks.add(to(item, QuickserverClientLink))
    if node.hasKey("billingDetails"):
      result.billingDetails = to(node["billingDetails"], QuickserverBillingDetails)
    if node.hasKey("custCurrency"):
      result.custCurrency = to(node["custCurrency"], string)
    if node.hasKey("custCurrencySymbol"):
      result.custCurrencySymbol = to(node["custCurrencySymbol"], string)
    if node.hasKey("serviceMaster"):
      result.serviceMaster = to(node["serviceMaster"], QuickserverServiceMaster)
    if node.hasKey("package"):
      result.package = to(node["package"], string)
    if node.hasKey("os_template"):
      result.osTemplate = to(node["os_template"], string)
    if node.hasKey("serviceExtra"):
      result.serviceExtra = to(node["serviceExtra"], Quickserver_serviceExtra)
    if node.hasKey("extraInfoTables"):
      result.extraInfoTables = to(node["extraInfoTables"], Quickserver_extraInfoTables)
    if node.hasKey("cpu_graph_data"):
      result.cpuGraphData = to(node["cpu_graph_data"], string)
    if node.hasKey("bandwidth_xaxis"):
      result.bandwidthXaxis = to(node["bandwidth_xaxis"], string)
    if node.hasKey("bandwidth_yaxis"):
      result.bandwidthYaxis = to(node["bandwidth_yaxis"], string)
    if node.hasKey("module"):
      result.module = to(node["module"], string)
    if node.hasKey("token"):
      result.token = to(node["token"], string)
    if node.hasKey("service_disk_used"):
      result.serviceDiskUsed = to(node["service_disk_used"], string)
    if node.hasKey("service_disk_total"):
      result.serviceDiskTotal = to(node["service_disk_total"], string)
    if node.hasKey("disk_percentage"):
      result.diskPercentage = to(node["disk_percentage"], float)
    if node.hasKey("memory"):
      result.memory = to(node["memory"], string)
    if node.hasKey("hdd"):
      result.hdd = to(node["hdd"], string)
    if node.hasKey("service_overview_extra"):
      result.serviceOverviewExtra = to(node["service_overview_extra"], seq[string])

# Custom JSON serialization for Quickserver with custom field names
proc `%`*(obj: Quickserver): JsonNode =
  result = newJObject()
  result["serviceInfo"] = %obj.serviceInfo
  result["client_links"] = %obj.clientLinks
  result["billingDetails"] = %obj.billingDetails
  result["custCurrency"] = %obj.custCurrency
  result["custCurrencySymbol"] = %obj.custCurrencySymbol
  result["serviceMaster"] = %obj.serviceMaster
  result["package"] = %obj.package
  result["os_template"] = %obj.osTemplate
  result["serviceExtra"] = %obj.serviceExtra
  result["extraInfoTables"] = %obj.extraInfoTables
  result["cpu_graph_data"] = %obj.cpuGraphData
  result["bandwidth_xaxis"] = %obj.bandwidthXaxis
  result["bandwidth_yaxis"] = %obj.bandwidthYaxis
  result["module"] = %obj.module
  result["token"] = %obj.token
  result["service_disk_used"] = %obj.serviceDiskUsed
  result["service_disk_total"] = %obj.serviceDiskTotal
  result["disk_percentage"] = %obj.diskPercentage
  result["memory"] = %obj.memory
  result["hdd"] = %obj.hdd
  result["service_overview_extra"] = %obj.serviceOverviewExtra

