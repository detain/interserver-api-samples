#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type AccountInfoPost* = object
  ## Request to update account information.
  name*: string ## Your name.
  address*: string ## Your address.
  city*: string ## Your city.
  state*: string ## Your state.
  zip*: string ## Your ZIP code.
  country*: string ## Your country.
  phone*: string ## Your phone number.
  company*: Option[string] ## Your company name.
  address2*: Option[string] ## Additional address information.
  locale*: Option[string] ## Your preferred locale.
  emailInvoices*: Option[string] ## Your email for invoice notifications.
  emailAbuse*: Option[string] ## Your email for abuse notifications.
  disableReset*: Option[bool] ## Set to `true` to disable account resets, or `false` to enable them.
  disableReinstall*: Option[bool] ## Set to `true` to disable server reinstalls, or `false` to enable them.
  disableServerNotifications*: Option[bool] ## Set to `true` to disable server notifications, or `false` to enable them.
  disableEmailNotifications*: Option[bool] ## Set to `true` to disable email notifications, or `false` to enable them.
  gstin*: Option[string] ## Your GST identification number (if applicable).


# Custom JSON deserialization for AccountInfoPost with custom field names
proc to*(node: JsonNode, T: typedesc[AccountInfoPost]): AccountInfoPost =
  result = AccountInfoPost()
  if node.kind == JObject:
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("address"):
      result.address = to(node["address"], string)
    if node.hasKey("city"):
      result.city = to(node["city"], string)
    if node.hasKey("state"):
      result.state = to(node["state"], string)
    if node.hasKey("zip"):
      result.zip = to(node["zip"], string)
    if node.hasKey("country"):
      result.country = to(node["country"], string)
    if node.hasKey("phone"):
      result.phone = to(node["phone"], string)
    if node.hasKey("company") and node["company"].kind != JNull:
      result.company = some(to(node["company"], typeof(result.company.get())))
    if node.hasKey("address2") and node["address2"].kind != JNull:
      result.address2 = some(to(node["address2"], typeof(result.address2.get())))
    if node.hasKey("locale") and node["locale"].kind != JNull:
      result.locale = some(to(node["locale"], typeof(result.locale.get())))
    if node.hasKey("email_invoices") and node["email_invoices"].kind != JNull:
      result.emailInvoices = some(to(node["email_invoices"], typeof(result.emailInvoices.get())))
    if node.hasKey("email_abuse") and node["email_abuse"].kind != JNull:
      result.emailAbuse = some(to(node["email_abuse"], typeof(result.emailAbuse.get())))
    if node.hasKey("disable_reset") and node["disable_reset"].kind != JNull:
      result.disableReset = some(to(node["disable_reset"], typeof(result.disableReset.get())))
    if node.hasKey("disable_reinstall") and node["disable_reinstall"].kind != JNull:
      result.disableReinstall = some(to(node["disable_reinstall"], typeof(result.disableReinstall.get())))
    if node.hasKey("disable_server_notifications") and node["disable_server_notifications"].kind != JNull:
      result.disableServerNotifications = some(to(node["disable_server_notifications"], typeof(result.disableServerNotifications.get())))
    if node.hasKey("disable_email_notifications") and node["disable_email_notifications"].kind != JNull:
      result.disableEmailNotifications = some(to(node["disable_email_notifications"], typeof(result.disableEmailNotifications.get())))
    if node.hasKey("gstin") and node["gstin"].kind != JNull:
      result.gstin = some(to(node["gstin"], typeof(result.gstin.get())))

# Custom JSON serialization for AccountInfoPost with custom field names
proc `%`*(obj: AccountInfoPost): JsonNode =
  result = newJObject()
  result["name"] = %obj.name
  result["address"] = %obj.address
  result["city"] = %obj.city
  result["state"] = %obj.state
  result["zip"] = %obj.zip
  result["country"] = %obj.country
  result["phone"] = %obj.phone
  if obj.company.isSome():
    result["company"] = %obj.company.get()
  if obj.address2.isSome():
    result["address2"] = %obj.address2.get()
  if obj.locale.isSome():
    result["locale"] = %obj.locale.get()
  if obj.emailInvoices.isSome():
    result["email_invoices"] = %obj.emailInvoices.get()
  if obj.emailAbuse.isSome():
    result["email_abuse"] = %obj.emailAbuse.get()
  if obj.disableReset.isSome():
    result["disable_reset"] = %obj.disableReset.get()
  if obj.disableReinstall.isSome():
    result["disable_reinstall"] = %obj.disableReinstall.get()
  if obj.disableServerNotifications.isSome():
    result["disable_server_notifications"] = %obj.disableServerNotifications.get()
  if obj.disableEmailNotifications.isSome():
    result["disable_email_notifications"] = %obj.disableEmailNotifications.get()
  if obj.gstin.isSome():
    result["gstin"] = %obj.gstin.get()

