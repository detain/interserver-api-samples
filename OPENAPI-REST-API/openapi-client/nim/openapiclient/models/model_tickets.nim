#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_tickets_count_array
import model_tickets_row

type Tickets* = object
  ## A listing of support tickets.
  ima*: string
  custid*: string
  view*: string
  currentPage*: int
  limit*: int
  sortcol*: int
  sortdir*: int
  rowsOffset*: int
  tickets*: seq[TicketsRow]
  pages*: int
  rowsTotal*: int
  inboxCount*: int
  countArray*: Tickets_countArray
  viewText*: string


# Custom JSON deserialization for Tickets with custom field names
proc to*(node: JsonNode, T: typedesc[Tickets]): Tickets =
  result = Tickets()
  if node.kind == JObject:
    if node.hasKey("ima"):
      result.ima = to(node["ima"], string)
    if node.hasKey("custid"):
      result.custid = to(node["custid"], string)
    if node.hasKey("view"):
      result.view = to(node["view"], string)
    if node.hasKey("currentPage"):
      result.currentPage = to(node["currentPage"], int)
    if node.hasKey("limit"):
      result.limit = to(node["limit"], int)
    if node.hasKey("sortcol"):
      result.sortcol = to(node["sortcol"], int)
    if node.hasKey("sortdir"):
      result.sortdir = to(node["sortdir"], int)
    if node.hasKey("rowsOffset"):
      result.rowsOffset = to(node["rowsOffset"], int)
    if node.hasKey("tickets"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["tickets"]
      if arrayNode.kind == JArray:
        result.tickets = @[]
        for item in arrayNode.items:
          result.tickets.add(to(item, TicketsRow))
    if node.hasKey("pages"):
      result.pages = to(node["pages"], int)
    if node.hasKey("rowsTotal"):
      result.rowsTotal = to(node["rowsTotal"], int)
    if node.hasKey("inboxCount"):
      result.inboxCount = to(node["inboxCount"], int)
    if node.hasKey("countArray"):
      result.countArray = to(node["countArray"], Tickets_countArray)
    if node.hasKey("viewText"):
      result.viewText = to(node["viewText"], string)

# Custom JSON serialization for Tickets with custom field names
proc `%`*(obj: Tickets): JsonNode =
  result = newJObject()
  result["ima"] = %obj.ima
  result["custid"] = %obj.custid
  result["view"] = %obj.view
  result["currentPage"] = %obj.currentPage
  result["limit"] = %obj.limit
  result["sortcol"] = %obj.sortcol
  result["sortdir"] = %obj.sortdir
  result["rowsOffset"] = %obj.rowsOffset
  result["tickets"] = %obj.tickets
  result["pages"] = %obj.pages
  result["rowsTotal"] = %obj.rowsTotal
  result["inboxCount"] = %obj.inboxCount
  result["countArray"] = %obj.countArray
  result["viewText"] = %obj.viewText

