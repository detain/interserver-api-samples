#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type LicenseRow* = object
  ## A result row from the `Licenses` `GET` request.
  licenseId*: Option[string] ## The id of the license.
  licenseHostname*: Option[string] ## The hostname of the license.
  licenseIp*: Option[string] ## The ip of the license.
  servicesName*: Option[string] ## The services name of the license.
  cost*: Option[string] ## The cost of the license.
  licenseStatus*: Option[string] ## The status of the license.
  invoicesPaid*: Option[string] ## The invoices paid of the license.
  invoicesDate*: Option[string] ## The invoices date of the license.


# Custom JSON deserialization for LicenseRow with custom field names
proc to*(node: JsonNode, T: typedesc[LicenseRow]): LicenseRow =
  result = LicenseRow()
  if node.kind == JObject:
    if node.hasKey("license_id") and node["license_id"].kind != JNull:
      result.licenseId = some(to(node["license_id"], typeof(result.licenseId.get())))
    if node.hasKey("license_hostname") and node["license_hostname"].kind != JNull:
      result.licenseHostname = some(to(node["license_hostname"], typeof(result.licenseHostname.get())))
    if node.hasKey("license_ip") and node["license_ip"].kind != JNull:
      result.licenseIp = some(to(node["license_ip"], typeof(result.licenseIp.get())))
    if node.hasKey("services_name") and node["services_name"].kind != JNull:
      result.servicesName = some(to(node["services_name"], typeof(result.servicesName.get())))
    if node.hasKey("cost") and node["cost"].kind != JNull:
      result.cost = some(to(node["cost"], typeof(result.cost.get())))
    if node.hasKey("license_status") and node["license_status"].kind != JNull:
      result.licenseStatus = some(to(node["license_status"], typeof(result.licenseStatus.get())))
    if node.hasKey("invoices_paid") and node["invoices_paid"].kind != JNull:
      result.invoicesPaid = some(to(node["invoices_paid"], typeof(result.invoicesPaid.get())))
    if node.hasKey("invoices_date") and node["invoices_date"].kind != JNull:
      result.invoicesDate = some(to(node["invoices_date"], typeof(result.invoicesDate.get())))

# Custom JSON serialization for LicenseRow with custom field names
proc `%`*(obj: LicenseRow): JsonNode =
  result = newJObject()
  if obj.licenseId.isSome():
    result["license_id"] = %obj.licenseId.get()
  if obj.licenseHostname.isSome():
    result["license_hostname"] = %obj.licenseHostname.get()
  if obj.licenseIp.isSome():
    result["license_ip"] = %obj.licenseIp.get()
  if obj.servicesName.isSome():
    result["services_name"] = %obj.servicesName.get()
  if obj.cost.isSome():
    result["cost"] = %obj.cost.get()
  if obj.licenseStatus.isSome():
    result["license_status"] = %obj.licenseStatus.get()
  if obj.invoicesPaid.isSome():
    result["invoices_paid"] = %obj.invoicesPaid.get()
  if obj.invoicesDate.isSome():
    result["invoices_date"] = %obj.invoicesDate.get()

