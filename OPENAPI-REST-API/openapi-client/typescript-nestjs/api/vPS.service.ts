/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
/* tslint:disable:no-unused-variable member-ordering */

import { HttpService, Injectable, Optional } from '@nestjs/common';
import type { AxiosRequestConfig, AxiosResponse } from 'axios';
import { Observable, from, of, switchMap } from 'rxjs';
import { ChargeInvoiceRows } from '../model/chargeInvoiceRows';
import { DownloadQsBackup200Response } from '../model/downloadQsBackup200Response';
import { DownloadQsBackupRequest } from '../model/downloadQsBackupRequest';
import { GetAccountInfo401Response } from '../model/getAccountInfo401Response';
import { QueueResponse } from '../model/queueResponse';
import { RestoreRequest } from '../model/restoreRequest';
import { ReverseDnsEntries } from '../model/reverseDnsEntries';
import { ServiceOrderPostResponse } from '../model/serviceOrderPostResponse';
import { SuccessTextResponse } from '../model/successTextResponse';
import { TextResponse } from '../model/textResponse';
import { VPSCancel200Response } from '../model/vPSCancel200Response';
import { Vps } from '../model/vps';
import { VpsBackupRows } from '../model/vpsBackupRows';
import { VpsOrder } from '../model/vpsOrder';
import { VpsOrderPostRequest } from '../model/vpsOrderPostRequest';
import { VpsOrderPutRequest } from '../model/vpsOrderPutRequest';
import { VpsOrderPutResponse } from '../model/vpsOrderPutResponse';
import { VpsRow } from '../model/vpsRow';
import { VpsTemplatesList } from '../model/vpsTemplatesList';
import { VpsTrafficResponse } from '../model/vpsTrafficResponse';
import { Configuration } from '../configuration';
import { COLLECTION_FORMATS } from '../variables';


@Injectable()
export class VPSService {

    protected basePath = 'https://my.interserver.net/apiv2';
    public defaultHeaders: Record<string,string> = {};
    public configuration = new Configuration();
    protected httpClient: HttpService;

    constructor(httpClient: HttpService, @Optional() configuration: Configuration) {
        this.configuration = configuration || this.configuration;
        this.basePath = configuration?.basePath || this.basePath;
        this.httpClient = configuration?.httpClient || httpClient;
    }

    /**
     * @param consumes string[] mime-types
     * @return true: consumes contains 'multipart/form-data', false: otherwise
     */
    private canConsumeForm(consumes: string[]): boolean {
        const form = 'multipart/form-data';
        return consumes.includes(form);
    }

    /**
     * Place VPS Order
     * Places an order for a new VPS. Use &#x60;PUT /vps/order&#x60; to validate the order first.
     * @param vpsOrderPostRequest 
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [addVpsOpts.config] Override http request option.
     */
    public addVps(vpsOrderPostRequest?: VpsOrderPostRequest, addVpsOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ServiceOrderPostResponse>>;
    public addVps(vpsOrderPostRequest?: VpsOrderPostRequest, addVpsOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json',
            'multipart/form-data'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<ServiceOrderPostResponse>(`${this.basePath}/vps/order`,
                    vpsOrderPostRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...addVpsOpts?.config,
                        headers: {...headers, ...addVpsOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Delete VPS Backup
     * Permanently removes the specified backup file from storage. Use &#x60;GET /vps/{id}/backups&#x60; to list available backup filenames before deleting.
     * @param id VPS ID number
     * @param file The backup filename to delete.
     * @param all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given VPS.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [deleteVpsBackupOpts.config] Override http request option.
     */
    public deleteVpsBackup(id: number, file: string, all?: '0' | '1', deleteVpsBackupOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<SuccessTextResponse>>;
    public deleteVpsBackup(id: number, file: string, all?: '0' | '1', deleteVpsBackupOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling deleteVpsBackup.');
        }

        if (file === null || file === undefined) {
            throw new Error('Required parameter file was null or undefined when calling deleteVpsBackup.');
        }

        let queryParameters = new URLSearchParams();
        if (all !== undefined && all !== null) {
            queryParameters.append('all', <any>all);
        }
        if (file !== undefined && file !== null) {
            queryParameters.append('file', <any>file);
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.delete<SuccessTextResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/backups`,
                    {
                        params: queryParameters,
                        withCredentials: this.configuration.withCredentials,
                        ...deleteVpsBackupOpts?.config,
                        headers: {...headers, ...deleteVpsBackupOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Blocks SMTP
     * Blocks outbound SMTP (port 25) on the VPS to prevent spam. Useful for VPS that do not need to send email.
     * @param id VPS ID number.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [doVpsBlockSmtpOpts.config] Override http request option.
     */
    public doVpsBlockSmtp(id: number, doVpsBlockSmtpOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<QueueResponse>>;
    public doVpsBlockSmtp(id: number, doVpsBlockSmtpOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling doVpsBlockSmtp.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<QueueResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/block_smtp`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...doVpsBlockSmtpOpts?.config,
                        headers: {...headers, ...doVpsBlockSmtpOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Disable CD Drive
     * Disables the virtual CD drive on the VPS.
     * @param id VPS ID number.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [doVpsDisableCdOpts.config] Override http request option.
     */
    public doVpsDisableCd(id: number, doVpsDisableCdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<QueueResponse>>;
    public doVpsDisableCd(id: number, doVpsDisableCdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling doVpsDisableCd.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<QueueResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/disable_cd`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...doVpsDisableCdOpts?.config,
                        headers: {...headers, ...doVpsDisableCdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Disable Quotas
     * Disables disk quota enforcement on the VPS.
     * @param id VPS ID number.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [doVpsDisableQuotaOpts.config] Override http request option.
     */
    public doVpsDisableQuota(id: number, doVpsDisableQuotaOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<QueueResponse>>;
    public doVpsDisableQuota(id: number, doVpsDisableQuotaOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling doVpsDisableQuota.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<QueueResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/disable_quota`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...doVpsDisableQuotaOpts?.config,
                        headers: {...headers, ...doVpsDisableQuotaOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Eject CD Drive
     * Ejects the virtual CD from the VPS CD drive.
     * @param id VPS ID number.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [doVpsEjectCdOpts.config] Override http request option.
     */
    public doVpsEjectCd(id: number, doVpsEjectCdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<QueueResponse>>;
    public doVpsEjectCd(id: number, doVpsEjectCdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling doVpsEjectCd.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<QueueResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/eject_cd`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...doVpsEjectCdOpts?.config,
                        headers: {...headers, ...doVpsEjectCdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Enable Quotas
     * Enables disk quota enforcement on the VPS.
     * @param id VPS ID number.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [doVpsEnableQuotaOpts.config] Override http request option.
     */
    public doVpsEnableQuota(id: number, doVpsEnableQuotaOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<QueueResponse>>;
    public doVpsEnableQuota(id: number, doVpsEnableQuotaOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling doVpsEnableQuota.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<QueueResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/enable_quota`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...doVpsEnableQuotaOpts?.config,
                        headers: {...headers, ...doVpsEnableQuotaOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Restart VPS
     * Restarts the VPS.
     * @param id VPS ID number.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [doVpsRestartOpts.config] Override http request option.
     */
    public doVpsRestart(id: number, doVpsRestartOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<QueueResponse>>;
    public doVpsRestart(id: number, doVpsRestartOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling doVpsRestart.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<QueueResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/restart`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...doVpsRestartOpts?.config,
                        headers: {...headers, ...doVpsRestartOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Start VPS
     * Powers on the VPS.
     * @param id VPS ID number.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [doVpsStartOpts.config] Override http request option.
     */
    public doVpsStart(id: number, doVpsStartOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<QueueResponse>>;
    public doVpsStart(id: number, doVpsStartOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling doVpsStart.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<QueueResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/start`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...doVpsStartOpts?.config,
                        headers: {...headers, ...doVpsStartOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Stop VPS
     * Powers off the VPS.
     * @param id VPS ID number.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [doVpsStopOpts.config] Override http request option.
     */
    public doVpsStop(id: number, doVpsStopOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<QueueResponse>>;
    public doVpsStop(id: number, doVpsStopOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling doVpsStop.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<QueueResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/stop`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...doVpsStopOpts?.config,
                        headers: {...headers, ...doVpsStopOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Download VPS Backup
     * Generates a pre-signed download URL for the specified backup file. The URL is valid for 24 hours. Use &#x60;GET /vps/{id}/backups&#x60; to list available backup filenames.
     * @param id VPS ID number
     * @param downloadQsBackupRequest 
     * @param all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given VPS.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [downloadVpsBackupOpts.config] Override http request option.
     */
    public downloadVpsBackup(id: number, downloadQsBackupRequest: DownloadQsBackupRequest, all?: '0' | '1', downloadVpsBackupOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<DownloadQsBackup200Response>>;
    public downloadVpsBackup(id: number, downloadQsBackupRequest: DownloadQsBackupRequest, all?: '0' | '1', downloadVpsBackupOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling downloadVpsBackup.');
        }

        if (downloadQsBackupRequest === null || downloadQsBackupRequest === undefined) {
            throw new Error('Required parameter downloadQsBackupRequest was null or undefined when calling downloadVpsBackup.');
        }

        let queryParameters = new URLSearchParams();
        if (all !== undefined && all !== null) {
            queryParameters.append('all', <any>all);
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json',
            'multipart/form-data'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.patch<DownloadQsBackup200Response>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/backups`,
                    downloadQsBackupRequest,
                    {
                        params: queryParameters,
                        withCredentials: this.configuration.withCredentials,
                        ...downloadVpsBackupOpts?.config,
                        headers: {...headers, ...downloadVpsBackupOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * VPS Ordering Information
     * Retrieves available VPS configurations, OS templates, and pricing for ordering.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getNewVpsOpts.config] Override http request option.
     */
    public getNewVps(getNewVpsOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<VpsOrder>>;
    public getNewVps(getNewVpsOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<VpsOrder>(`${this.basePath}/vps/order`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getNewVpsOpts?.config,
                        headers: {...headers, ...getNewVpsOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get VPS Backups List
     * Returns the available backups for the VPS across all storage systems (Swift, MinIO, ZFS). Use the backup &#x60;name&#x60; value with &#x60;PATCH /vps/{id}/backups&#x60; to download or &#x60;DELETE /vps/{id}/backups&#x60; to remove a backup. Use &#x60;POST /vps/{id}/restore&#x60; to restore from a backup.
     * @param id VPS ID number
     * @param all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given VPS.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getVpsBackupsOpts.config] Override http request option.
     */
    public getVpsBackups(id: number, all?: '0' | '1', getVpsBackupsOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<VpsBackupRows>>;
    public getVpsBackups(id: number, all?: '0' | '1', getVpsBackupsOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getVpsBackups.');
        }

        let queryParameters = new URLSearchParams();
        if (all !== undefined && all !== null) {
            queryParameters.append('all', <any>all);
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<VpsBackupRows>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/backups`,
                    {
                        params: queryParameters,
                        withCredentials: this.configuration.withCredentials,
                        ...getVpsBackupsOpts?.config,
                        headers: {...headers, ...getVpsBackupsOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * HD Space Addon Info
     * Returns available hard drive space addon options and pricing for the VPS.
     * @param id VPS ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getVpsBuyHdSpaceOpts.config] Override http request option.
     */
    public getVpsBuyHdSpace(id: number, getVpsBuyHdSpaceOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public getVpsBuyHdSpace(id: number, getVpsBuyHdSpaceOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getVpsBuyHdSpace.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<any>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/buy_hd_space`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getVpsBuyHdSpaceOpts?.config,
                        headers: {...headers, ...getVpsBuyHdSpaceOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Additional IP Addon Info
     * Returns available additional IP address options and pricing for the VPS.
     * @param id VPS ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getVpsBuyIpOpts.config] Override http request option.
     */
    public getVpsBuyIp(id: number, getVpsBuyIpOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public getVpsBuyIp(id: number, getVpsBuyIpOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getVpsBuyIp.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<any>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/buy_ip`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getVpsBuyIpOpts?.config,
                        headers: {...headers, ...getVpsBuyIpOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get Timezone Info
     * Returns the list of available timezones that can be set on the VPS.
     * @param id VPS ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getVpsChangeTimezoneOpts.config] Override http request option.
     */
    public getVpsChangeTimezone(id: number, getVpsChangeTimezoneOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<Array<string>>>;
    public getVpsChangeTimezone(id: number, getVpsChangeTimezoneOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getVpsChangeTimezone.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<Array<string>>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/change_timezone`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getVpsChangeTimezoneOpts?.config,
                        headers: {...headers, ...getVpsChangeTimezoneOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get VPS Order
     * Returns detailed information about a specific VPS including its IPs, hostname, OS, and resource allocation.
     * @param id VPS ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getVpsInfoOpts.config] Override http request option.
     */
    public getVpsInfo(id: number, getVpsInfoOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<Vps>>;
    public getVpsInfo(id: number, getVpsInfoOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getVpsInfo.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<Vps>(`${this.basePath}/vps/${encodeURIComponent(String(id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getVpsInfoOpts?.config,
                        headers: {...headers, ...getVpsInfoOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get VPS Invoices
     * Returns the billing invoices associated with this VPS.
     * @param id VPS ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getVpsInvoicesOpts.config] Override http request option.
     */
    public getVpsInvoices(id: number, getVpsInvoicesOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ChargeInvoiceRows>>;
    public getVpsInvoices(id: number, getVpsInvoicesOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getVpsInvoices.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<ChargeInvoiceRows>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/invoices`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getVpsInvoicesOpts?.config,
                        headers: {...headers, ...getVpsInvoicesOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * List VPS Orders
     * Returns all VPS services on the account with their current status and configuration.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getVpsListOpts.config] Override http request option.
     */
    public getVpsList(getVpsListOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<Array<VpsRow>>>;
    public getVpsList(getVpsListOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<Array<VpsRow>>(`${this.basePath}/vps`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getVpsListOpts?.config,
                        headers: {...headers, ...getVpsListOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * VPS Reinstall OS Options
     * Returns the list of available OS templates for reinstalling the VPS.
     * @param id VPS ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getVpsReinstallOsOpts.config] Override http request option.
     */
    public getVpsReinstallOs(id: number, getVpsReinstallOsOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<VpsTemplatesList>>;
    public getVpsReinstallOs(id: number, getVpsReinstallOsOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getVpsReinstallOs.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<VpsTemplatesList>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/reinstall_os`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getVpsReinstallOsOpts?.config,
                        headers: {...headers, ...getVpsReinstallOsOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Reverse DNS Info
     * Returns the current reverse DNS (PTR record) entries for the VPS IP addresses.
     * @param id VPS ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getVpsReverseDnsOpts.config] Override http request option.
     */
    public getVpsReverseDns(id: number, getVpsReverseDnsOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ReverseDnsEntries>>;
    public getVpsReverseDns(id: number, getVpsReverseDnsOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getVpsReverseDns.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<ReverseDnsEntries>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/reverse_dns`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getVpsReverseDnsOpts?.config,
                        headers: {...headers, ...getVpsReverseDnsOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * VNC Setup Info
     * Returns the current VNC connection information for the VPS.
     * @param id VPS ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getVpsSetupVncOpts.config] Override http request option.
     */
    public getVpsSetupVnc(id: number, getVpsSetupVncOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public getVpsSetupVnc(id: number, getVpsSetupVncOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getVpsSetupVnc.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<any>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/setup_vnc`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getVpsSetupVncOpts?.config,
                        headers: {...headers, ...getVpsSetupVncOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Slice Upgrade Info
     * Returns available slice upgrade options and pricing for the VPS.
     * @param id VPS ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getVpsSlicesOpts.config] Override http request option.
     */
    public getVpsSlices(id: number, getVpsSlicesOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public getVpsSlices(id: number, getVpsSlicesOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getVpsSlices.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<any>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/slices`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getVpsSlicesOpts?.config,
                        headers: {...headers, ...getVpsSlicesOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get Traffic Usage
     * Returns bandwidth traffic usage data for the VPS.
     * @param id VPS ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getVpsTrafficUsageOpts.config] Override http request option.
     */
    public getVpsTrafficUsage(id: number, getVpsTrafficUsageOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<VpsTrafficResponse>>;
    public getVpsTrafficUsage(id: number, getVpsTrafficUsageOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getVpsTrafficUsage.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<VpsTrafficResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/traffic_usage`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getVpsTrafficUsageOpts?.config,
                        headers: {...headers, ...getVpsTrafficUsageOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get View Desktop Info
     * Returns remote desktop connection information for the VPS.
     * @param id VPS ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getVpsViewDesktopOpts.config] Override http request option.
     */
    public getVpsViewDesktop(id: number, getVpsViewDesktopOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public getVpsViewDesktop(id: number, getVpsViewDesktopOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getVpsViewDesktop.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<any>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/view_desktop`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getVpsViewDesktopOpts?.config,
                        headers: {...headers, ...getVpsViewDesktopOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Resend VPS Welcome Email
     * Resends the welcome email containing connection details and credentials for the VPS order.
     * @param id VPS ID
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getVpsWelcomeEmailOpts.config] Override http request option.
     */
    public getVpsWelcomeEmail(id: string, getVpsWelcomeEmailOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<SuccessTextResponse>>;
    public getVpsWelcomeEmail(id: string, getVpsWelcomeEmailOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getVpsWelcomeEmail.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<SuccessTextResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/welcome_email`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getVpsWelcomeEmailOpts?.config,
                        headers: {...headers, ...getVpsWelcomeEmailOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Start a VPS Backup
     * Creates a backup of the VPS. The backup can be downloaded or restored later via the backups endpoints.
     * @param id VPS ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [postVpsBackupOpts.config] Override http request option.
     */
    public postVpsBackup(id: number, postVpsBackupOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<QueueResponse>>;
    public postVpsBackup(id: number, postVpsBackupOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling postVpsBackup.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<QueueResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/backup`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...postVpsBackupOpts?.config,
                        headers: {...headers, ...postVpsBackupOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Purchase HD Space Addon
     * Purchases additional hard drive space for the VPS.
     * @param id VPS ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [postVpsBuyHdSpaceOpts.config] Override http request option.
     */
    public postVpsBuyHdSpace(id: number, postVpsBuyHdSpaceOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public postVpsBuyHdSpace(id: number, postVpsBuyHdSpaceOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling postVpsBuyHdSpace.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<any>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/buy_hd_space`,
                    null,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...postVpsBuyHdSpaceOpts?.config,
                        headers: {...headers, ...postVpsBuyHdSpaceOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Purchase Additional IP
     * Purchases an additional IP address for the VPS.
     * @param id VPS ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [postVpsBuyIpOpts.config] Override http request option.
     */
    public postVpsBuyIp(id: number, postVpsBuyIpOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public postVpsBuyIp(id: number, postVpsBuyIpOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling postVpsBuyIp.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<any>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/buy_ip`,
                    null,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...postVpsBuyIpOpts?.config,
                        headers: {...headers, ...postVpsBuyIpOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Update VPS Hostname
     * Changes the hostname of the VPS. This queues a background task to update the server.
     * @param id VPS ID number
     * @param hostname 
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [postVpsChangeHostnameOpts.config] Override http request option.
     */
    public postVpsChangeHostname(id: number, hostname?: string, postVpsChangeHostnameOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<QueueResponse>>;
    public postVpsChangeHostname(id: number, hostname?: string, postVpsChangeHostnameOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling postVpsChangeHostname.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'multipart/form-data',
            'application/json'
        ];

        const canConsumeForm = this.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): void; };
        let useForm = false;
        let convertFormParamsToString = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            // formParams = new HttpParams({encoder: new CustomHttpUrlEncodingCodec()});
        }

        if (hostname !== undefined) {
            formParams!.append('hostname', <any>hostname);
        }

        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<QueueResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/change_hostname`,
                    convertFormParamsToString ? formParams!.toString() : formParams!,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...postVpsChangeHostnameOpts?.config,
                        headers: {...headers, ...postVpsChangeHostnameOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Change VPS Root Password
     * Changes the root password of the VPS.
     * @param id VPS ID number
     * @param password 
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [postVpsChangeRootPasswordOpts.config] Override http request option.
     */
    public postVpsChangeRootPassword(id: number, password: string, postVpsChangeRootPasswordOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<QueueResponse>>;
    public postVpsChangeRootPassword(id: number, password: string, postVpsChangeRootPasswordOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling postVpsChangeRootPassword.');
        }

        if (password === null || password === undefined) {
            throw new Error('Required parameter password was null or undefined when calling postVpsChangeRootPassword.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'multipart/form-data',
            'application/json'
        ];

        const canConsumeForm = this.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): void; };
        let useForm = false;
        let convertFormParamsToString = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            // formParams = new HttpParams({encoder: new CustomHttpUrlEncodingCodec()});
        }

        if (password !== undefined) {
            formParams!.append('password', <any>password);
        }

        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<QueueResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/change_root_password`,
                    convertFormParamsToString ? formParams!.toString() : formParams!,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...postVpsChangeRootPasswordOpts?.config,
                        headers: {...headers, ...postVpsChangeRootPasswordOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Change VPS Timezone
     * Changes the system timezone on the VPS.
     * @param id VPS ID number
     * @param timezone The time zone
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [postVpsChangeTimezoneOpts.config] Override http request option.
     */
    public postVpsChangeTimezone(id: number, timezone: string, postVpsChangeTimezoneOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<QueueResponse>>;
    public postVpsChangeTimezone(id: number, timezone: string, postVpsChangeTimezoneOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling postVpsChangeTimezone.');
        }

        if (timezone === null || timezone === undefined) {
            throw new Error('Required parameter timezone was null or undefined when calling postVpsChangeTimezone.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'multipart/form-data',
            'application/json'
        ];

        const canConsumeForm = this.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): void; };
        let useForm = false;
        let convertFormParamsToString = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            // formParams = new HttpParams({encoder: new CustomHttpUrlEncodingCodec()});
        }

        if (timezone !== undefined) {
            formParams!.append('timezone', <any>timezone);
        }

        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<QueueResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/change_timezone`,
                    convertFormParamsToString ? formParams!.toString() : formParams!,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...postVpsChangeTimezoneOpts?.config,
                        headers: {...headers, ...postVpsChangeTimezoneOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Change Webuzo Password
     * Changes the Webuzo control panel password on the VPS.
     * @param id VPS ID number
     * @param password 
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [postVpsChangeWebuzoPasswordOpts.config] Override http request option.
     */
    public postVpsChangeWebuzoPassword(id: number, password: string, postVpsChangeWebuzoPasswordOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<QueueResponse>>;
    public postVpsChangeWebuzoPassword(id: number, password: string, postVpsChangeWebuzoPasswordOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling postVpsChangeWebuzoPassword.');
        }

        if (password === null || password === undefined) {
            throw new Error('Required parameter password was null or undefined when calling postVpsChangeWebuzoPassword.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'multipart/form-data',
            'application/json'
        ];

        const canConsumeForm = this.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): void; };
        let useForm = false;
        let convertFormParamsToString = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            // formParams = new HttpParams({encoder: new CustomHttpUrlEncodingCodec()});
        }

        if (password !== undefined) {
            formParams!.append('password', <any>password);
        }

        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<QueueResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/change_webuzo_password`,
                    convertFormParamsToString ? formParams!.toString() : formParams!,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...postVpsChangeWebuzoPasswordOpts?.config,
                        headers: {...headers, ...postVpsChangeWebuzoPasswordOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Insert CD in VPS
     * Mounts an ISO image in the VPS virtual CD drive from the given URL.
     * @param id VPS ID number
     * @param url 
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [postVpsInsertCdOpts.config] Override http request option.
     */
    public postVpsInsertCd(id: number, url?: string, postVpsInsertCdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<QueueResponse>>;
    public postVpsInsertCd(id: number, url?: string, postVpsInsertCdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling postVpsInsertCd.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'multipart/form-data',
            'application/json'
        ];

        const canConsumeForm = this.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): void; };
        let useForm = false;
        let convertFormParamsToString = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            // formParams = new HttpParams({encoder: new CustomHttpUrlEncodingCodec()});
        }

        if (url !== undefined) {
            formParams!.append('url', <any>url);
        }

        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<QueueResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/insert_cd`,
                    convertFormParamsToString ? formParams!.toString() : formParams!,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...postVpsInsertCdOpts?.config,
                        headers: {...headers, ...postVpsInsertCdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Reinstall VPS OS
     * Reinstalls the operating system on the VPS. Warning - all data will be erased.
     * @param id VPS ID number
     * @param template OS Template Filename
     * @param localPassword Password for this account.
     * @param password Password for Root / Administrator Account.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [postVpsReinstallOsOpts.config] Override http request option.
     */
    public postVpsReinstallOs(id: number, template: string, localPassword: string, password?: string, postVpsReinstallOsOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<QueueResponse>>;
    public postVpsReinstallOs(id: number, template: string, localPassword: string, password?: string, postVpsReinstallOsOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling postVpsReinstallOs.');
        }

        if (template === null || template === undefined) {
            throw new Error('Required parameter template was null or undefined when calling postVpsReinstallOs.');
        }

        if (localPassword === null || localPassword === undefined) {
            throw new Error('Required parameter localPassword was null or undefined when calling postVpsReinstallOs.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'multipart/form-data',
            'application/json'
        ];

        const canConsumeForm = this.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): void; };
        let useForm = false;
        let convertFormParamsToString = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            // formParams = new HttpParams({encoder: new CustomHttpUrlEncodingCodec()});
        }

        if (template !== undefined) {
            formParams!.append('template', <any>template);
        }

        if (password !== undefined) {
            formParams!.append('password', <any>password);
        }

        if (localPassword !== undefined) {
            formParams!.append('localPassword', <any>localPassword);
        }

        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<QueueResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/reinstall_os`,
                    convertFormParamsToString ? formParams!.toString() : formParams!,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...postVpsReinstallOsOpts?.config,
                        headers: {...headers, ...postVpsReinstallOsOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Reset VPS Password
     * Resets the root password on the VPS to a new randomly generated password.
     * @param id VPS ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [postVpsResetPasswordOpts.config] Override http request option.
     */
    public postVpsResetPassword(id: number, postVpsResetPasswordOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<QueueResponse>>;
    public postVpsResetPassword(id: number, postVpsResetPasswordOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling postVpsResetPassword.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<QueueResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/reset_password`,
                    null,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...postVpsResetPasswordOpts?.config,
                        headers: {...headers, ...postVpsResetPasswordOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Restore VPS from Backup
     * Initiates a restore of the VPS from a previously created backup. The restore operation overwrites the current disk contents. Use &#x60;GET /vps/{id}/backups&#x60; to retrieve available backup names.
     * @param id VPS ID number
     * @param restoreRequest VPS Restore request
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [postVpsRestoreOpts.config] Override http request option.
     */
    public postVpsRestore(id: number, restoreRequest: RestoreRequest, postVpsRestoreOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<QueueResponse>>;
    public postVpsRestore(id: number, restoreRequest: RestoreRequest, postVpsRestoreOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling postVpsRestore.');
        }

        if (restoreRequest === null || restoreRequest === undefined) {
            throw new Error('Required parameter restoreRequest was null or undefined when calling postVpsRestore.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json',
            'multipart/form-data'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<QueueResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/restore`,
                    restoreRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...postVpsRestoreOpts?.config,
                        headers: {...headers, ...postVpsRestoreOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Update Reverse DNS
     * Updates the reverse DNS (PTR record) entries for the VPS IP addresses.
     * @param id VPS ID number
     * @param reverseDnsEntries 
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [postVpsReverseDnsOpts.config] Override http request option.
     */
    public postVpsReverseDns(id: number, reverseDnsEntries: ReverseDnsEntries, postVpsReverseDnsOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<TextResponse>>;
    public postVpsReverseDns(id: number, reverseDnsEntries: ReverseDnsEntries, postVpsReverseDnsOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling postVpsReverseDns.');
        }

        if (reverseDnsEntries === null || reverseDnsEntries === undefined) {
            throw new Error('Required parameter reverseDnsEntries was null or undefined when calling postVpsReverseDns.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json',
            'multipart/form-data'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<TextResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/reverse_dns`,
                    reverseDnsEntries,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...postVpsReverseDnsOpts?.config,
                        headers: {...headers, ...postVpsReverseDnsOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Setup VNC
     * Sets up or refreshes the VNC console connection for the VPS.
     * @param id VPS ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [postVpsSetupVncOpts.config] Override http request option.
     */
    public postVpsSetupVnc(id: number, postVpsSetupVncOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<QueueResponse>>;
    public postVpsSetupVnc(id: number, postVpsSetupVncOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling postVpsSetupVnc.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<QueueResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/setup_vnc`,
                    null,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...postVpsSetupVncOpts?.config,
                        headers: {...headers, ...postVpsSetupVncOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Purchase Slice Upgrade
     * Purchases a slice upgrade for the VPS to increase CPU, RAM, and disk resources.
     * @param id VPS ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [postVpsSlicesOpts.config] Override http request option.
     */
    public postVpsSlices(id: number, postVpsSlicesOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public postVpsSlices(id: number, postVpsSlicesOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling postVpsSlices.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<any>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/slices`,
                    null,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...postVpsSlicesOpts?.config,
                        headers: {...headers, ...postVpsSlicesOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Update View Desktop
     * Updates or refreshes the remote desktop session for the VPS.
     * @param id VPS ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [postVpsViewDesktopOpts.config] Override http request option.
     */
    public postVpsViewDesktop(id: number, postVpsViewDesktopOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public postVpsViewDesktop(id: number, postVpsViewDesktopOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling postVpsViewDesktop.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<any>(`${this.basePath}/vps/${encodeURIComponent(String(id))}/view_desktop`,
                    null,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...postVpsViewDesktopOpts?.config,
                        headers: {...headers, ...postVpsViewDesktopOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Validate VPS Order
     * Validates a VPS order before placing it. Returns cost breakdown and any validation errors.
     * @param vpsOrderPutRequest 
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [putVpsOpts.config] Override http request option.
     */
    public putVps(vpsOrderPutRequest?: VpsOrderPutRequest, putVpsOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<VpsOrderPutResponse>>;
    public putVps(vpsOrderPutRequest?: VpsOrderPutRequest, putVpsOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json',
            'multipart/form-data'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.put<VpsOrderPutResponse>(`${this.basePath}/vps/order`,
                    vpsOrderPutRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...putVpsOpts?.config,
                        headers: {...headers, ...putVpsOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Update VPS Order
     * Updates settings on a VPS order.
     * @param id VPS ID number.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [updateVpsInfoOpts.config] Override http request option.
     */
    public updateVpsInfo(id: string, updateVpsInfoOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<SuccessTextResponse>>;
    public updateVpsInfo(id: string, updateVpsInfoOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling updateVpsInfo.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<SuccessTextResponse>(`${this.basePath}/vps/${encodeURIComponent(String(id))}`,
                    null,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...updateVpsInfoOpts?.config,
                        headers: {...headers, ...updateVpsInfoOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Cancel VPS Service
     * Cancels the VPS service. The server will be deprovisioned and billing will stop at the end of the current billing cycle.
     * @param id VPS ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [vPSCancelOpts.config] Override http request option.
     */
    public vPSCancel(id: number, vPSCancelOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<VPSCancel200Response>>;
    public vPSCancel(id: number, vPSCancelOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling vPSCancel.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.delete<VPSCancel200Response>(`${this.basePath}/vps/${encodeURIComponent(String(id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...vPSCancelOpts?.config,
                        headers: {...headers, ...vPSCancelOpts?.config?.headers},
                    }
                );
            })
        );
    }
}
