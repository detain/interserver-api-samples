/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
/* tslint:disable:no-unused-variable member-ordering */

import { HttpService, Injectable, Optional } from '@nestjs/common';
import type { AxiosRequestConfig, AxiosResponse } from 'axios';
import { Observable, from, of, switchMap } from 'rxjs';
import { BuyItNowList } from '../model/buyItNowList';
import { BuyItNowServerOrder200Response } from '../model/buyItNowServerOrder200Response';
import { ChargeInvoiceRows } from '../model/chargeInvoiceRows';
import { GetAccountInfo401Response } from '../model/getAccountInfo401Response';
import { PlaceBuyNowServerRequest } from '../model/placeBuyNowServerRequest';
import { ReverseDnsEntries } from '../model/reverseDnsEntries';
import { Server } from '../model/server';
import { ServerIpmiLiveInfo } from '../model/serverIpmiLiveInfo';
import { ServerOrder } from '../model/serverOrder';
import { ServerRow } from '../model/serverRow';
import { ServersBuyNowError } from '../model/serversBuyNowError';
import { ServersBuyNowResponse } from '../model/serversBuyNowResponse';
import { ServersCancel200Response } from '../model/serversCancel200Response';
import { SuccessTextResponse } from '../model/successTextResponse';
import { TextResponse } from '../model/textResponse';
import { Configuration } from '../configuration';
import { COLLECTION_FORMATS } from '../variables';


@Injectable()
export class ServersService {

    protected basePath = 'https://my.interserver.net/apiv2';
    public defaultHeaders: Record<string,string> = {};
    public configuration = new Configuration();
    protected httpClient: HttpService;

    constructor(httpClient: HttpService, @Optional() configuration: Configuration) {
        this.configuration = configuration || this.configuration;
        this.basePath = configuration?.basePath || this.basePath;
        this.httpClient = configuration?.httpClient || httpClient;
    }

    /**
     * @param consumes string[] mime-types
     * @return true: consumes contains 'multipart/form-data', false: otherwise
     */
    private canConsumeForm(consumes: string[]): boolean {
        const form = 'multipart/form-data';
        return consumes.includes(form);
    }

    /**
     * Place Server Order
     * Places an order for a new dedicated server. Use &#x60;PUT /servers/order&#x60; to validate the order first.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [addServerOpts.config] Override http request option.
     */
    public addServer(addServerOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public addServer(addServerOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<any>(`${this.basePath}/servers/order`,
                    null,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...addServerOpts?.config,
                        headers: {...headers, ...addServerOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get Buy Now Server Options
     * Returns the configuration options and pricing for buy-it-now dedicated servers, including available bandwidth packages, IP blocks, operating systems, control panels, and RAID configurations. Use the returned option IDs when placing an order via &#x60;POST /servers/order/buy_now_server&#x60;.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [buyItNowServerOrderOpts.config] Override http request option.
     */
    public buyItNowServerOrder(buyItNowServerOrderOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<BuyItNowServerOrder200Response>>;
    public buyItNowServerOrder(buyItNowServerOrderOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<BuyItNowServerOrder200Response>(`${this.basePath}/servers/order/buy_now_server`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...buyItNowServerOrderOpts?.config,
                        headers: {...headers, ...buyItNowServerOrderOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * List Marketplace Servers
     * Returns the list of available Rapid Deploy dedicated servers with current pricing. Each entry includes CPU, memory, disk, bandwidth, IP allocation, and location details. These servers are pre-configured and can be provisioned immediately after purchase.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getMPServersOpts.config] Override http request option.
     */
    public getMPServers(getMPServersOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<BuyItNowList>>;
    public getMPServers(getMPServersOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<BuyItNowList>(`${this.basePath}/buy_now_servers_list`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getMPServersOpts?.config,
                        headers: {...headers, ...getMPServersOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Server Ordering Information
     * Retrieves available server configurations and pricing for ordering a new dedicated server.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getNewServerOpts.config] Override http request option.
     */
    public getNewServer(getNewServerOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ServerOrder>>;
    public getNewServer(getNewServerOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<ServerOrder>(`${this.basePath}/servers/order`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getNewServerOpts?.config,
                        headers: {...headers, ...getNewServerOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get Server Order
     * Returns detailed information about a specific server including its hardware configuration, IPs, and status.
     * @param id Server ID number.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getServerInfoOpts.config] Override http request option.
     */
    public getServerInfo(id: number, getServerInfoOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<Server>>;
    public getServerInfo(id: number, getServerInfoOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getServerInfo.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<Server>(`${this.basePath}/servers/${encodeURIComponent(String(id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getServerInfoOpts?.config,
                        headers: {...headers, ...getServerInfoOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get Server Invoices
     * Returns the billing invoices associated with this dedicated server.
     * @param id Server ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getServerInvoicesOpts.config] Override http request option.
     */
    public getServerInvoices(id: number, getServerInvoicesOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ChargeInvoiceRows>>;
    public getServerInvoices(id: number, getServerInvoicesOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getServerInvoices.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<ChargeInvoiceRows>(`${this.basePath}/servers/${encodeURIComponent(String(id))}/invoices`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getServerInvoicesOpts?.config,
                        headers: {...headers, ...getServerInvoicesOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * List Servers
     * Returns all dedicated server services on the account with their current status and configuration.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getServerListOpts.config] Override http request option.
     */
    public getServerList(getServerListOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<Array<ServerRow>>>;
    public getServerList(getServerListOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<Array<ServerRow>>(`${this.basePath}/servers`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getServerListOpts?.config,
                        headers: {...headers, ...getServerListOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Reverse DNS Info
     * Returns the current reverse DNS (PTR record) entries for the server\&#39;s IP addresses.
     * @param id Server ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getServerReverseDnsOpts.config] Override http request option.
     */
    public getServerReverseDns(id: number, getServerReverseDnsOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ReverseDnsEntries>>;
    public getServerReverseDns(id: number, getServerReverseDnsOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getServerReverseDns.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<ReverseDnsEntries>(`${this.basePath}/servers/${encodeURIComponent(String(id))}/reverse_dns`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getServerReverseDnsOpts?.config,
                        headers: {...headers, ...getServerReverseDnsOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Resend Server Welcome Email
     * Resends the welcome email for the order.
     * @param id Server ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getServersWelcomeEmailOpts.config] Override http request option.
     */
    public getServersWelcomeEmail(id: number, getServersWelcomeEmailOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<SuccessTextResponse>>;
    public getServersWelcomeEmail(id: number, getServersWelcomeEmailOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getServersWelcomeEmail.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<SuccessTextResponse>(`${this.basePath}/servers/${encodeURIComponent(String(id))}/welcome_email`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getServersWelcomeEmailOpts?.config,
                        headers: {...headers, ...getServersWelcomeEmailOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Place Buy Now Server Order
     * Places an order for a buy-it-now dedicated server. Use &#x60;GET /servers/order/buy_now_server&#x60; to retrieve available server configurations and their IDs before ordering.
     * @param placeBuyNowServerRequest 
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [placeBuyNowServerOpts.config] Override http request option.
     */
    public placeBuyNowServer(placeBuyNowServerRequest?: PlaceBuyNowServerRequest, placeBuyNowServerOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ServersBuyNowResponse>>;
    public placeBuyNowServer(placeBuyNowServerRequest?: PlaceBuyNowServerRequest, placeBuyNowServerOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<ServersBuyNowResponse>(`${this.basePath}/servers/order/buy_now_server`,
                    placeBuyNowServerRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...placeBuyNowServerOpts?.config,
                        headers: {...headers, ...placeBuyNowServerOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Update Reverse DNS
     * Updates the reverse DNS (PTR record) entries for the server\&#39;s IP addresses.
     * @param id Server ID number
     * @param reverseDnsEntries 
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [postServerReverseDnsOpts.config] Override http request option.
     */
    public postServerReverseDns(id: number, reverseDnsEntries: ReverseDnsEntries, postServerReverseDnsOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<TextResponse>>;
    public postServerReverseDns(id: number, reverseDnsEntries: ReverseDnsEntries, postServerReverseDnsOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling postServerReverseDns.');
        }

        if (reverseDnsEntries === null || reverseDnsEntries === undefined) {
            throw new Error('Required parameter reverseDnsEntries was null or undefined when calling postServerReverseDns.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json',
            'multipart/form-data'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<TextResponse>(`${this.basePath}/servers/${encodeURIComponent(String(id))}/reverse_dns`,
                    reverseDnsEntries,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...postServerReverseDnsOpts?.config,
                        headers: {...headers, ...postServerReverseDnsOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Validate Server Order
     * Validates a server order before placing it. Use this to check for errors before committing to a purchase.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [putServersOpts.config] Override http request option.
     */
    public putServers(putServersOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public putServers(putServersOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.put<any>(`${this.basePath}/servers/order`,
                    null,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...putServersOpts?.config,
                        headers: {...headers, ...putServersOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Server IPMI Live Information
     * Returns the current IPMI live connection information for the server.
     * @param id Server ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [serverIpmiLiveGetOpts.config] Override http request option.
     */
    public serverIpmiLiveGet(id: number, serverIpmiLiveGetOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ServerIpmiLiveInfo>>;
    public serverIpmiLiveGet(id: number, serverIpmiLiveGetOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling serverIpmiLiveGet.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<ServerIpmiLiveInfo>(`${this.basePath}/servers/${encodeURIComponent(String(id))}/ipmi_live`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...serverIpmiLiveGetOpts?.config,
                        headers: {...headers, ...serverIpmiLiveGetOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Server IPMI Live Setup
     * Configures IPMI live access by whitelisting your current IP address for connections to the server\&#39;s IPMI management interface.
     * @param id Server ID number
     * @param ip Your IP Address you wish to connect to the IPMI system from.
     * @param asset Asset ID
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [serverIpmiLivePostOpts.config] Override http request option.
     */
    public serverIpmiLivePost(id: number, ip: string, asset?: number, serverIpmiLivePostOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ServerIpmiLiveInfo>>;
    public serverIpmiLivePost(id: number, ip: string, asset?: number, serverIpmiLivePostOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling serverIpmiLivePost.');
        }

        if (ip === null || ip === undefined) {
            throw new Error('Required parameter ip was null or undefined when calling serverIpmiLivePost.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'multipart/form-data',
            'application/json'
        ];

        const canConsumeForm = this.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): void; };
        let useForm = false;
        let convertFormParamsToString = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            // formParams = new HttpParams({encoder: new CustomHttpUrlEncodingCodec()});
        }

        if (asset !== undefined) {
            formParams!.append('asset', <any>asset);
        }

        if (ip !== undefined) {
            formParams!.append('ip', <any>ip);
        }

        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<ServerIpmiLiveInfo>(`${this.basePath}/servers/${encodeURIComponent(String(id))}/ipmi_live`,
                    convertFormParamsToString ? formParams!.toString() : formParams!,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...serverIpmiLivePostOpts?.config,
                        headers: {...headers, ...serverIpmiLivePostOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get IPMI Power Status
     * Returns the chassis power status from ipmi.
     * @param id Server ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [serverIpmiPowerGetOpts.config] Override http request option.
     */
    public serverIpmiPowerGet(id: number, serverIpmiPowerGetOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<TextResponse>>;
    public serverIpmiPowerGet(id: number, serverIpmiPowerGetOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling serverIpmiPowerGet.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<TextResponse>(`${this.basePath}/servers/${encodeURIComponent(String(id))}/ipmi_power`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...serverIpmiPowerGetOpts?.config,
                        headers: {...headers, ...serverIpmiPowerGetOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Server IPMI Power
     * Uses the IPMI interface to set the Power status on the server.
     * @param id Server ID number
     * @param action The power action to send to the ipmi controller.
     * @param asset The Asset ID
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [serverIpmiPowerPostOpts.config] Override http request option.
     */
    public serverIpmiPowerPost(id: number, action: string, asset?: number, serverIpmiPowerPostOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<TextResponse>>;
    public serverIpmiPowerPost(id: number, action: string, asset?: number, serverIpmiPowerPostOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling serverIpmiPowerPost.');
        }

        if (action === null || action === undefined) {
            throw new Error('Required parameter action was null or undefined when calling serverIpmiPowerPost.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'multipart/form-data',
            'application/json'
        ];

        const canConsumeForm = this.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): void; };
        let useForm = false;
        let convertFormParamsToString = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            // formParams = new HttpParams({encoder: new CustomHttpUrlEncodingCodec()});
        }

        if (asset !== undefined) {
            formParams!.append('asset', <any>asset);
        }

        if (action !== undefined) {
            formParams!.append('action', <any>action);
        }

        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<TextResponse>(`${this.basePath}/servers/${encodeURIComponent(String(id))}/ipmi_power`,
                    convertFormParamsToString ? formParams!.toString() : formParams!,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...serverIpmiPowerPostOpts?.config,
                        headers: {...headers, ...serverIpmiPowerPostOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Cancel Server Service
     * Cancels the dedicated server service. The server will be deprovisioned and billing will stop at the end of the current billing cycle.
     * @param id Server ID number
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [serversCancelOpts.config] Override http request option.
     */
    public serversCancel(id: number, serversCancelOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ServersCancel200Response>>;
    public serversCancel(id: number, serversCancelOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling serversCancel.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.delete<ServersCancel200Response>(`${this.basePath}/servers/${encodeURIComponent(String(id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...serversCancelOpts?.config,
                        headers: {...headers, ...serversCancelOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Update Server Order
     * Updates settings on a dedicated server order.
     * @param id Server ID number.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [updateServerInfoOpts.config] Override http request option.
     */
    public updateServerInfo(id: string, updateServerInfoOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public updateServerInfo(id: string, updateServerInfoOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling updateServerInfo.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (sessionIdCookieAuth) required
        // authentication (apiKeyAuth) required
        if (this.configuration.apiKeys?.["X-API-KEY"]) {
            headers['X-API-KEY'] = this.configuration.apiKeys["X-API-KEY"];
        }

        // authentication (sessionIdHeaderAuth) required
        if (this.configuration.apiKeys?.["sessionid"]) {
            headers['sessionid'] = this.configuration.apiKeys["sessionid"];
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<any>(`${this.basePath}/servers/${encodeURIComponent(String(id))}`,
                    null,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...updateServerInfoOpts?.config,
                        headers: {...headers, ...updateServerInfoOpts?.config?.headers},
                    }
                );
            })
        );
    }
}
