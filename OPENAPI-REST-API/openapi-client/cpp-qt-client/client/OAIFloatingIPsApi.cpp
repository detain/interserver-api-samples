/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAIFloatingIPsApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAIFloatingIPsApi::OAIFloatingIPsApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAIFloatingIPsApi::~OAIFloatingIPsApi() {
}

void OAIFloatingIPsApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("https://my.interserver.net/apiv2"),
    "Live API Endpoint",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("addFloatingIp", defaultConf);
    _serverIndices.insert("addFloatingIp", 0);
    _serverConfigs.insert("floatingIpsCancel", defaultConf);
    _serverIndices.insert("floatingIpsCancel", 0);
    _serverConfigs.insert("getFloatingIpInfo", defaultConf);
    _serverIndices.insert("getFloatingIpInfo", 0);
    _serverConfigs.insert("getFloatingIpInvoices", defaultConf);
    _serverIndices.insert("getFloatingIpInvoices", 0);
    _serverConfigs.insert("getFloatingIpsList", defaultConf);
    _serverIndices.insert("getFloatingIpsList", 0);
    _serverConfigs.insert("getFloatingIpsWelcomeEmail", defaultConf);
    _serverIndices.insert("getFloatingIpsWelcomeEmail", 0);
    _serverConfigs.insert("getNewFloatingIp", defaultConf);
    _serverIndices.insert("getNewFloatingIp", 0);
    _serverConfigs.insert("postFloatingIpsChangeIp", defaultConf);
    _serverIndices.insert("postFloatingIpsChangeIp", 0);
    _serverConfigs.insert("putFloatingIps", defaultConf);
    _serverIndices.insert("putFloatingIps", 0);
    _serverConfigs.insert("updateFloatingIpInfo", defaultConf);
    _serverIndices.insert("updateFloatingIpInfo", 0);
}

/**
 * returns 0 on success and -1, -2 or -3 on failure.
 * -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
 */
int OAIFloatingIPsApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}

/**
 * Sets the server index.
 * @param operation The id to the target operation.
 * @param serverIndex The server index.
 */
void OAIFloatingIPsApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAIFloatingIPsApi::setServerIndex(int serverIndex) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, serverIndex);
    }
}

void OAIFloatingIPsApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAIFloatingIPsApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAIFloatingIPsApi::setUsername(const QString &username) {
    _username = username;
}

void OAIFloatingIPsApi::setPassword(const QString &password) {
    _password = password;
}


void OAIFloatingIPsApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAIFloatingIPsApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAIFloatingIPsApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
 * Appends a new ServerConfiguration to the config map for a specific operation.
 * @param operation The id to the target operation.
 * @param url A string that contains the URL of the server
 * @param description A String that describes the server
 * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
 * returns the index of the new server config on success and -1 if the operation is not found
 */
int OAIFloatingIPsApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
 * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
 * @param url A string that contains the URL of the server
 * @param description A String that describes the server
 * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
 */
void OAIFloatingIPsApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
 * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
 * @param URL A string that contains the URL of the server
 * @param description A String that describes the server
 * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
 */
void OAIFloatingIPsApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAIFloatingIPsApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAIFloatingIPsApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAIFloatingIPsApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAIFloatingIPsApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAIFloatingIPsApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAIFloatingIPsApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAIFloatingIPsApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAIFloatingIPsApi::addFloatingIp() {
    QString fullPath = QString(_serverConfigs["addFloatingIp"][_serverIndices.value("addFloatingIp")].URL()+"/floating_ips/order");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIFloatingIPsApi::addFloatingIpCallback);
    connect(this, &OAIFloatingIPsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIFloatingIPsApi::addFloatingIpCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIServiceOrderPostResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT addFloatingIpSignal(output);
        Q_EMIT addFloatingIpSignalFull(worker, output);
    } else {
        Q_EMIT addFloatingIpSignalError(output, error_type, error_str);
        Q_EMIT addFloatingIpSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIFloatingIPsApi::floatingIpsCancel(const qint32 &id) {
    QString fullPath = QString(_serverConfigs["floatingIpsCancel"][_serverIndices.value("floatingIpsCancel")].URL()+"/floating_ips/{id}");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    
    {
        QString idPathParam("{");
        idPathParam.append("id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"id"+pathSuffix : pathPrefix;
        fullPath.replace(idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "DELETE");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIFloatingIPsApi::floatingIpsCancelCallback);
    connect(this, &OAIFloatingIPsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIFloatingIPsApi::floatingIpsCancelCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIFloating_ipsCancel_200_response output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT floatingIpsCancelSignal(output);
        Q_EMIT floatingIpsCancelSignalFull(worker, output);
    } else {
        Q_EMIT floatingIpsCancelSignalError(output, error_type, error_str);
        Q_EMIT floatingIpsCancelSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIFloatingIPsApi::getFloatingIpInfo(const qint32 &id) {
    QString fullPath = QString(_serverConfigs["getFloatingIpInfo"][_serverIndices.value("getFloatingIpInfo")].URL()+"/floating_ips/{id}");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    
    {
        QString idPathParam("{");
        idPathParam.append("id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"id"+pathSuffix : pathPrefix;
        fullPath.replace(idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIFloatingIPsApi::getFloatingIpInfoCallback);
    connect(this, &OAIFloatingIPsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIFloatingIPsApi::getFloatingIpInfoCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIObject output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getFloatingIpInfoSignal(output);
        Q_EMIT getFloatingIpInfoSignalFull(worker, output);
    } else {
        Q_EMIT getFloatingIpInfoSignalError(output, error_type, error_str);
        Q_EMIT getFloatingIpInfoSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIFloatingIPsApi::getFloatingIpInvoices(const qint32 &id) {
    QString fullPath = QString(_serverConfigs["getFloatingIpInvoices"][_serverIndices.value("getFloatingIpInvoices")].URL()+"/floating_ips/{id}/invoices");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    
    {
        QString idPathParam("{");
        idPathParam.append("id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"id"+pathSuffix : pathPrefix;
        fullPath.replace(idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIFloatingIPsApi::getFloatingIpInvoicesCallback);
    connect(this, &OAIFloatingIPsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIFloatingIPsApi::getFloatingIpInvoicesCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIChargeInvoiceRows output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getFloatingIpInvoicesSignal(output);
        Q_EMIT getFloatingIpInvoicesSignalFull(worker, output);
    } else {
        Q_EMIT getFloatingIpInvoicesSignalError(output, error_type, error_str);
        Q_EMIT getFloatingIpInvoicesSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIFloatingIPsApi::getFloatingIpsList() {
    QString fullPath = QString(_serverConfigs["getFloatingIpsList"][_serverIndices.value("getFloatingIpsList")].URL()+"/floating_ips");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIFloatingIPsApi::getFloatingIpsListCallback);
    connect(this, &OAIFloatingIPsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIFloatingIPsApi::getFloatingIpsListCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getFloatingIpsListSignal();
        Q_EMIT getFloatingIpsListSignalFull(worker);
    } else {
        Q_EMIT getFloatingIpsListSignalError(error_type, error_str);
        Q_EMIT getFloatingIpsListSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIFloatingIPsApi::getFloatingIpsWelcomeEmail(const qint32 &id) {
    QString fullPath = QString(_serverConfigs["getFloatingIpsWelcomeEmail"][_serverIndices.value("getFloatingIpsWelcomeEmail")].URL()+"/floating_ips/{id}/welcome_email");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    
    {
        QString idPathParam("{");
        idPathParam.append("id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"id"+pathSuffix : pathPrefix;
        fullPath.replace(idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIFloatingIPsApi::getFloatingIpsWelcomeEmailCallback);
    connect(this, &OAIFloatingIPsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIFloatingIPsApi::getFloatingIpsWelcomeEmailCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAISuccessTextResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getFloatingIpsWelcomeEmailSignal(output);
        Q_EMIT getFloatingIpsWelcomeEmailSignalFull(worker, output);
    } else {
        Q_EMIT getFloatingIpsWelcomeEmailSignalError(output, error_type, error_str);
        Q_EMIT getFloatingIpsWelcomeEmailSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIFloatingIPsApi::getNewFloatingIp() {
    QString fullPath = QString(_serverConfigs["getNewFloatingIp"][_serverIndices.value("getNewFloatingIp")].URL()+"/floating_ips/order");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIFloatingIPsApi::getNewFloatingIpCallback);
    connect(this, &OAIFloatingIPsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIFloatingIPsApi::getNewFloatingIpCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIObject output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getNewFloatingIpSignal(output);
        Q_EMIT getNewFloatingIpSignalFull(worker, output);
    } else {
        Q_EMIT getNewFloatingIpSignalError(output, error_type, error_str);
        Q_EMIT getNewFloatingIpSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIFloatingIPsApi::postFloatingIpsChangeIp(const qint32 &id, const QString &ip) {
    QString fullPath = QString(_serverConfigs["postFloatingIpsChangeIp"][_serverIndices.value("postFloatingIpsChangeIp")].URL()+"/floating_ips/{id}/change_ip");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    
    {
        QString idPathParam("{");
        idPathParam.append("id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"id"+pathSuffix : pathPrefix;
        fullPath.replace(idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    
    {
        input.add_var("ip", ::OpenAPI::toStringValue(ip));
    }

    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIFloatingIPsApi::postFloatingIpsChangeIpCallback);
    connect(this, &OAIFloatingIPsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIFloatingIPsApi::postFloatingIpsChangeIpCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAISuccessTextResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT postFloatingIpsChangeIpSignal(output);
        Q_EMIT postFloatingIpsChangeIpSignalFull(worker, output);
    } else {
        Q_EMIT postFloatingIpsChangeIpSignalError(output, error_type, error_str);
        Q_EMIT postFloatingIpsChangeIpSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIFloatingIPsApi::putFloatingIps() {
    QString fullPath = QString(_serverConfigs["putFloatingIps"][_serverIndices.value("putFloatingIps")].URL()+"/floating_ips/order");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "PUT");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIFloatingIPsApi::putFloatingIpsCallback);
    connect(this, &OAIFloatingIPsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIFloatingIPsApi::putFloatingIpsCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT putFloatingIpsSignal();
        Q_EMIT putFloatingIpsSignalFull(worker);
    } else {
        Q_EMIT putFloatingIpsSignalError(error_type, error_str);
        Q_EMIT putFloatingIpsSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIFloatingIPsApi::updateFloatingIpInfo(const QString &id) {
    QString fullPath = QString(_serverConfigs["updateFloatingIpInfo"][_serverIndices.value("updateFloatingIpInfo")].URL()+"/floating_ips/{id}");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    
    {
        QString idPathParam("{");
        idPathParam.append("id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"id"+pathSuffix : pathPrefix;
        fullPath.replace(idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIFloatingIPsApi::updateFloatingIpInfoCallback);
    connect(this, &OAIFloatingIPsApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIFloatingIPsApi::updateFloatingIpInfoCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAISuccessTextResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT updateFloatingIpInfoSignal(output);
        Q_EMIT updateFloatingIpInfoSignalFull(worker, output);
    } else {
        Q_EMIT updateFloatingIpInfoSignalError(output, error_type, error_str);
        Q_EMIT updateFloatingIpInfoSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIFloatingIPsApi::tokenAvailable() {

    oauthToken token;
    switch (_OauthMethod) {
    case OauthMethod::ImplicitFlow:
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case OauthMethod::AuthorizationFlow:
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case OauthMethod::ClientCredentialsFlow:
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case OauthMethod::ResourceOwnerPasswordFlow:
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
