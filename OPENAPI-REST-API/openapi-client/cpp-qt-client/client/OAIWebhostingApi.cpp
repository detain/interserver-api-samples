/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAIWebhostingApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAIWebhostingApi::OAIWebhostingApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAIWebhostingApi::~OAIWebhostingApi() {
}

void OAIWebhostingApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("https://my.interserver.net/apiv2"),
    "Live API Endpoint",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("addWebsite", defaultConf);
    _serverIndices.insert("addWebsite", 0);
    _serverConfigs.insert("getNewWebsite", defaultConf);
    _serverIndices.insert("getNewWebsite", 0);
    _serverConfigs.insert("getWebsiteBuyIp", defaultConf);
    _serverIndices.insert("getWebsiteBuyIp", 0);
    _serverConfigs.insert("getWebsiteInfo", defaultConf);
    _serverIndices.insert("getWebsiteInfo", 0);
    _serverConfigs.insert("getWebsiteInvoices", defaultConf);
    _serverIndices.insert("getWebsiteInvoices", 0);
    _serverConfigs.insert("getWebsiteList", defaultConf);
    _serverIndices.insert("getWebsiteList", 0);
    _serverConfigs.insert("getWebsitesBackups", defaultConf);
    _serverIndices.insert("getWebsitesBackups", 0);
    _serverConfigs.insert("getWebsitesLogin", defaultConf);
    _serverIndices.insert("getWebsitesLogin", 0);
    _serverConfigs.insert("getWebsitesWelcomeEmail", defaultConf);
    _serverIndices.insert("getWebsitesWelcomeEmail", 0);
    _serverConfigs.insert("gettWebsiteReverseDns", defaultConf);
    _serverIndices.insert("gettWebsiteReverseDns", 0);
    _serverConfigs.insert("postWebsiteBuyIp", defaultConf);
    _serverIndices.insert("postWebsiteBuyIp", 0);
    _serverConfigs.insert("postWebsiteMigration", defaultConf);
    _serverIndices.insert("postWebsiteMigration", 0);
    _serverConfigs.insert("postWebsitesReverseDns", defaultConf);
    _serverIndices.insert("postWebsitesReverseDns", 0);
    _serverConfigs.insert("putWebsites", defaultConf);
    _serverIndices.insert("putWebsites", 0);
    _serverConfigs.insert("updateWebsiteInfo", defaultConf);
    _serverIndices.insert("updateWebsiteInfo", 0);
    _serverConfigs.insert("webhostingCancel", defaultConf);
    _serverIndices.insert("webhostingCancel", 0);
}

/**
 * returns 0 on success and -1, -2 or -3 on failure.
 * -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
 */
int OAIWebhostingApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}

/**
 * Sets the server index.
 * @param operation The id to the target operation.
 * @param serverIndex The server index.
 */
void OAIWebhostingApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAIWebhostingApi::setServerIndex(int serverIndex) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, serverIndex);
    }
}

void OAIWebhostingApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAIWebhostingApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAIWebhostingApi::setUsername(const QString &username) {
    _username = username;
}

void OAIWebhostingApi::setPassword(const QString &password) {
    _password = password;
}


void OAIWebhostingApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAIWebhostingApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAIWebhostingApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
 * Appends a new ServerConfiguration to the config map for a specific operation.
 * @param operation The id to the target operation.
 * @param url A string that contains the URL of the server
 * @param description A String that describes the server
 * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
 * returns the index of the new server config on success and -1 if the operation is not found
 */
int OAIWebhostingApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
 * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
 * @param url A string that contains the URL of the server
 * @param description A String that describes the server
 * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
 */
void OAIWebhostingApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
 * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
 * @param URL A string that contains the URL of the server
 * @param description A String that describes the server
 * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
 */
void OAIWebhostingApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAIWebhostingApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAIWebhostingApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAIWebhostingApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAIWebhostingApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAIWebhostingApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAIWebhostingApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAIWebhostingApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAIWebhostingApi::addWebsite() {
    QString fullPath = QString(_serverConfigs["addWebsite"][_serverIndices.value("addWebsite")].URL()+"/websites/order");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIWebhostingApi::addWebsiteCallback);
    connect(this, &OAIWebhostingApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIWebhostingApi::addWebsiteCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIServiceOrderPostResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT addWebsiteSignal(output);
        Q_EMIT addWebsiteSignalFull(worker, output);
    } else {
        Q_EMIT addWebsiteSignalError(output, error_type, error_str);
        Q_EMIT addWebsiteSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIWebhostingApi::getNewWebsite() {
    QString fullPath = QString(_serverConfigs["getNewWebsite"][_serverIndices.value("getNewWebsite")].URL()+"/websites/order");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIWebhostingApi::getNewWebsiteCallback);
    connect(this, &OAIWebhostingApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIWebhostingApi::getNewWebsiteCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIWebsitesOrder output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getNewWebsiteSignal(output);
        Q_EMIT getNewWebsiteSignalFull(worker, output);
    } else {
        Q_EMIT getNewWebsiteSignalError(output, error_type, error_str);
        Q_EMIT getNewWebsiteSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIWebhostingApi::getWebsiteBuyIp(const qint32 &id) {
    QString fullPath = QString(_serverConfigs["getWebsiteBuyIp"][_serverIndices.value("getWebsiteBuyIp")].URL()+"/websites/{id}/buy_ip");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    
    {
        QString idPathParam("{");
        idPathParam.append("id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"id"+pathSuffix : pathPrefix;
        fullPath.replace(idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIWebhostingApi::getWebsiteBuyIpCallback);
    connect(this, &OAIWebhostingApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIWebhostingApi::getWebsiteBuyIpCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIGetWebsiteBuyIp_200_response output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getWebsiteBuyIpSignal(output);
        Q_EMIT getWebsiteBuyIpSignalFull(worker, output);
    } else {
        Q_EMIT getWebsiteBuyIpSignalError(output, error_type, error_str);
        Q_EMIT getWebsiteBuyIpSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIWebhostingApi::getWebsiteInfo(const qint32 &id) {
    QString fullPath = QString(_serverConfigs["getWebsiteInfo"][_serverIndices.value("getWebsiteInfo")].URL()+"/websites/{id}");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    
    {
        QString idPathParam("{");
        idPathParam.append("id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"id"+pathSuffix : pathPrefix;
        fullPath.replace(idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIWebhostingApi::getWebsiteInfoCallback);
    connect(this, &OAIWebhostingApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIWebhostingApi::getWebsiteInfoCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIWebsite output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getWebsiteInfoSignal(output);
        Q_EMIT getWebsiteInfoSignalFull(worker, output);
    } else {
        Q_EMIT getWebsiteInfoSignalError(output, error_type, error_str);
        Q_EMIT getWebsiteInfoSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIWebhostingApi::getWebsiteInvoices(const qint32 &id) {
    QString fullPath = QString(_serverConfigs["getWebsiteInvoices"][_serverIndices.value("getWebsiteInvoices")].URL()+"/websites/{id}/invoices");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    
    {
        QString idPathParam("{");
        idPathParam.append("id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"id"+pathSuffix : pathPrefix;
        fullPath.replace(idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIWebhostingApi::getWebsiteInvoicesCallback);
    connect(this, &OAIWebhostingApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIWebhostingApi::getWebsiteInvoicesCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIChargeInvoiceRows output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getWebsiteInvoicesSignal(output);
        Q_EMIT getWebsiteInvoicesSignalFull(worker, output);
    } else {
        Q_EMIT getWebsiteInvoicesSignalError(output, error_type, error_str);
        Q_EMIT getWebsiteInvoicesSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIWebhostingApi::getWebsiteList() {
    QString fullPath = QString(_serverConfigs["getWebsiteList"][_serverIndices.value("getWebsiteList")].URL()+"/websites");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIWebhostingApi::getWebsiteListCallback);
    connect(this, &OAIWebhostingApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIWebhostingApi::getWebsiteListCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    QList<OAIWebsiteRow> output;
    QString json(worker->response);
    QByteArray array(json.toStdString().c_str());
    QJsonDocument doc = QJsonDocument::fromJson(array);
    QJsonArray jsonArray = doc.array();
    for (QJsonValue obj : jsonArray) {
        OAIWebsiteRow val;
        ::OpenAPI::fromJsonValue(val, obj);
        output.append(val);
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getWebsiteListSignal(output);
        Q_EMIT getWebsiteListSignalFull(worker, output);
    } else {
        Q_EMIT getWebsiteListSignalError(output, error_type, error_str);
        Q_EMIT getWebsiteListSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIWebhostingApi::getWebsitesBackups(const qint32 &id) {
    QString fullPath = QString(_serverConfigs["getWebsitesBackups"][_serverIndices.value("getWebsitesBackups")].URL()+"/websites/{id}/backups");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    
    {
        QString idPathParam("{");
        idPathParam.append("id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"id"+pathSuffix : pathPrefix;
        fullPath.replace(idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIWebhostingApi::getWebsitesBackupsCallback);
    connect(this, &OAIWebhostingApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIWebhostingApi::getWebsitesBackupsCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIWebsiteBackups<OAIWebsiteBackups_inner> output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getWebsitesBackupsSignal(output);
        Q_EMIT getWebsitesBackupsSignalFull(worker, output);
    } else {
        Q_EMIT getWebsitesBackupsSignalError(output, error_type, error_str);
        Q_EMIT getWebsitesBackupsSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIWebhostingApi::getWebsitesLogin(const qint32 &id) {
    QString fullPath = QString(_serverConfigs["getWebsitesLogin"][_serverIndices.value("getWebsitesLogin")].URL()+"/websites/{id}/login");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    
    {
        QString idPathParam("{");
        idPathParam.append("id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"id"+pathSuffix : pathPrefix;
        fullPath.replace(idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIWebhostingApi::getWebsitesLoginCallback);
    connect(this, &OAIWebhostingApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIWebhostingApi::getWebsitesLoginCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIWebsiteLoginResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getWebsitesLoginSignal(output);
        Q_EMIT getWebsitesLoginSignalFull(worker, output);
    } else {
        Q_EMIT getWebsitesLoginSignalError(output, error_type, error_str);
        Q_EMIT getWebsitesLoginSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIWebhostingApi::getWebsitesWelcomeEmail(const qint32 &id) {
    QString fullPath = QString(_serverConfigs["getWebsitesWelcomeEmail"][_serverIndices.value("getWebsitesWelcomeEmail")].URL()+"/websites/{id}/welcome_email");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    
    {
        QString idPathParam("{");
        idPathParam.append("id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"id"+pathSuffix : pathPrefix;
        fullPath.replace(idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIWebhostingApi::getWebsitesWelcomeEmailCallback);
    connect(this, &OAIWebhostingApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIWebhostingApi::getWebsitesWelcomeEmailCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAISuccessTextResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getWebsitesWelcomeEmailSignal(output);
        Q_EMIT getWebsitesWelcomeEmailSignalFull(worker, output);
    } else {
        Q_EMIT getWebsitesWelcomeEmailSignalError(output, error_type, error_str);
        Q_EMIT getWebsitesWelcomeEmailSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIWebhostingApi::gettWebsiteReverseDns(const qint32 &id) {
    QString fullPath = QString(_serverConfigs["gettWebsiteReverseDns"][_serverIndices.value("gettWebsiteReverseDns")].URL()+"/websites/{id}/reverse_dns");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    
    {
        QString idPathParam("{");
        idPathParam.append("id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"id"+pathSuffix : pathPrefix;
        fullPath.replace(idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIWebhostingApi::gettWebsiteReverseDnsCallback);
    connect(this, &OAIWebhostingApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIWebhostingApi::gettWebsiteReverseDnsCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIReverseDnsEntries output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT gettWebsiteReverseDnsSignal(output);
        Q_EMIT gettWebsiteReverseDnsSignalFull(worker, output);
    } else {
        Q_EMIT gettWebsiteReverseDnsSignalError(output, error_type, error_str);
        Q_EMIT gettWebsiteReverseDnsSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIWebhostingApi::postWebsiteBuyIp(const qint32 &id, const OAIPostWebsiteBuyIp_request &oai_post_website_buy_ip_request) {
    QString fullPath = QString(_serverConfigs["postWebsiteBuyIp"][_serverIndices.value("postWebsiteBuyIp")].URL()+"/websites/{id}/buy_ip");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    
    {
        QString idPathParam("{");
        idPathParam.append("id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"id"+pathSuffix : pathPrefix;
        fullPath.replace(idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    {

        
        QByteArray output = oai_post_website_buy_ip_request.asJson().toUtf8();
        input.request_body.append(output);
    }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIWebhostingApi::postWebsiteBuyIpCallback);
    connect(this, &OAIWebhostingApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIWebhostingApi::postWebsiteBuyIpCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIPostWebsiteBuyIp_200_response output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT postWebsiteBuyIpSignal(output);
        Q_EMIT postWebsiteBuyIpSignalFull(worker, output);
    } else {
        Q_EMIT postWebsiteBuyIpSignalError(output, error_type, error_str);
        Q_EMIT postWebsiteBuyIpSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIWebhostingApi::postWebsiteMigration(const qint32 &id, const OAIPostWebsiteMigration_request &oai_post_website_migration_request) {
    QString fullPath = QString(_serverConfigs["postWebsiteMigration"][_serverIndices.value("postWebsiteMigration")].URL()+"/websites/{id}/migration");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    
    {
        QString idPathParam("{");
        idPathParam.append("id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"id"+pathSuffix : pathPrefix;
        fullPath.replace(idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    {

        
        QByteArray output = oai_post_website_migration_request.asJson().toUtf8();
        input.request_body.append(output);
    }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIWebhostingApi::postWebsiteMigrationCallback);
    connect(this, &OAIWebhostingApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIWebhostingApi::postWebsiteMigrationCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIPostWebsiteMigration_200_response output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT postWebsiteMigrationSignal(output);
        Q_EMIT postWebsiteMigrationSignalFull(worker, output);
    } else {
        Q_EMIT postWebsiteMigrationSignalError(output, error_type, error_str);
        Q_EMIT postWebsiteMigrationSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIWebhostingApi::postWebsitesReverseDns(const qint32 &id, const OAIReverseDnsEntries &oai_reverse_dns_entries) {
    QString fullPath = QString(_serverConfigs["postWebsitesReverseDns"][_serverIndices.value("postWebsitesReverseDns")].URL()+"/websites/{id}/reverse_dns");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    
    {
        QString idPathParam("{");
        idPathParam.append("id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"id"+pathSuffix : pathPrefix;
        fullPath.replace(idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    {

        
        QByteArray output = oai_reverse_dns_entries.asJson().toUtf8();
        input.request_body.append(output);
    }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIWebhostingApi::postWebsitesReverseDnsCallback);
    connect(this, &OAIWebhostingApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIWebhostingApi::postWebsitesReverseDnsCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAITextResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT postWebsitesReverseDnsSignal(output);
        Q_EMIT postWebsitesReverseDnsSignalFull(worker, output);
    } else {
        Q_EMIT postWebsitesReverseDnsSignalError(output, error_type, error_str);
        Q_EMIT postWebsitesReverseDnsSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIWebhostingApi::putWebsites() {
    QString fullPath = QString(_serverConfigs["putWebsites"][_serverIndices.value("putWebsites")].URL()+"/websites/order");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "PUT");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIWebhostingApi::putWebsitesCallback);
    connect(this, &OAIWebhostingApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIWebhostingApi::putWebsitesCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT putWebsitesSignal();
        Q_EMIT putWebsitesSignalFull(worker);
    } else {
        Q_EMIT putWebsitesSignalError(error_type, error_str);
        Q_EMIT putWebsitesSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIWebhostingApi::updateWebsiteInfo(const QString &id) {
    QString fullPath = QString(_serverConfigs["updateWebsiteInfo"][_serverIndices.value("updateWebsiteInfo")].URL()+"/websites/{id}");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    
    {
        QString idPathParam("{");
        idPathParam.append("id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"id"+pathSuffix : pathPrefix;
        fullPath.replace(idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIWebhostingApi::updateWebsiteInfoCallback);
    connect(this, &OAIWebhostingApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIWebhostingApi::updateWebsiteInfoCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAISuccessTextResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT updateWebsiteInfoSignal(output);
        Q_EMIT updateWebsiteInfoSignalFull(worker, output);
    } else {
        Q_EMIT updateWebsiteInfoSignalError(output, error_type, error_str);
        Q_EMIT updateWebsiteInfoSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIWebhostingApi::webhostingCancel(const QString &id) {
    QString fullPath = QString(_serverConfigs["webhostingCancel"][_serverIndices.value("webhostingCancel")].URL()+"/websites/{id}");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    
    {
        QString idPathParam("{");
        idPathParam.append("id").append("}");
        QString pathPrefix, pathSuffix, pathDelimiter;
        QString pathStyle = "simple";
        if (pathStyle == "")
            pathStyle = "simple";
        pathPrefix = getParamStylePrefix(pathStyle);
        pathSuffix = getParamStyleSuffix(pathStyle);
        pathDelimiter = getParamStyleDelimiter(pathStyle, "id", false);
        QString paramString = (pathStyle == "matrix") ? pathPrefix+"id"+pathSuffix : pathPrefix;
        fullPath.replace(idPathParam, paramString+QUrl::toPercentEncoding(::OpenAPI::toStringValue(id)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "DELETE");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIWebhostingApi::webhostingCancelCallback);
    connect(this, &OAIWebhostingApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIWebhostingApi::webhostingCancelCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIWebhostingCancel_200_response output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT webhostingCancelSignal(output);
        Q_EMIT webhostingCancelSignalFull(worker, output);
    } else {
        Q_EMIT webhostingCancelSignalError(output, error_type, error_str);
        Q_EMIT webhostingCancelSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIWebhostingApi::tokenAvailable() {

    oauthToken token;
    switch (_OauthMethod) {
    case OauthMethod::ImplicitFlow:
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case OauthMethod::AuthorizationFlow:
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case OauthMethod::ClientCredentialsFlow:
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case OauthMethod::ResourceOwnerPasswordFlow:
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
