/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAIPublicApi.h"
#include "OAIServerConfiguration.h"
#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAIPublicApi::OAIPublicApi(const int timeOut)
    : _timeOut(timeOut),
      _manager(nullptr),
      _isResponseCompressionEnabled(false),
      _isRequestCompressionEnabled(false) {
    initializeServerConfigs();
}

OAIPublicApi::~OAIPublicApi() {
}

void OAIPublicApi::initializeServerConfigs() {
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("https://my.interserver.net/apiv2"),
    "Live API Endpoint",
    QMap<QString, OAIServerVariable>()));
    _serverConfigs.insert("getCaptcha", defaultConf);
    _serverIndices.insert("getCaptcha", 0);
    _serverConfigs.insert("getCountries", defaultConf);
    _serverIndices.insert("getCountries", 0);
    _serverConfigs.insert("getInfo", defaultConf);
    _serverIndices.insert("getInfo", 0);
    _serverConfigs.insert("getLoginInfo", defaultConf);
    _serverIndices.insert("getLoginInfo", 0);
    _serverConfigs.insert("getMPServers", defaultConf);
    _serverIndices.insert("getMPServers", 0);
    _serverConfigs.insert("getOauthRedirect", defaultConf);
    _serverIndices.insert("getOauthRedirect", 0);
    _serverConfigs.insert("getTimezones", defaultConf);
    _serverIndices.insert("getTimezones", 0);
    _serverConfigs.insert("patchOauthTwoFactor", defaultConf);
    _serverIndices.insert("patchOauthTwoFactor", 0);
    _serverConfigs.insert("pingServer", defaultConf);
    _serverIndices.insert("pingServer", 0);
    _serverConfigs.insert("postOauthCallback", defaultConf);
    _serverIndices.insert("postOauthCallback", 0);
    _serverConfigs.insert("submitLogin", defaultConf);
    _serverIndices.insert("submitLogin", 0);
    _serverConfigs.insert("submitSignup", defaultConf);
    _serverIndices.insert("submitSignup", 0);
}

/**
 * returns 0 on success and -1, -2 or -3 on failure.
 * -1 when the variable does not exist and -2 if the value is not defined in the enum and -3 if the operation or server index is not found
 */
int OAIPublicApi::setDefaultServerValue(int serverIndex, const QString &operation, const QString &variable, const QString &value) {
    auto it = _serverConfigs.find(operation);
    if (it != _serverConfigs.end() && serverIndex < it.value().size()) {
      return _serverConfigs[operation][serverIndex].setDefaultValue(variable,value);
    }
    return -3;
}

/**
 * Sets the server index.
 * @param operation The id to the target operation.
 * @param serverIndex The server index.
 */
void OAIPublicApi::setServerIndex(const QString &operation, int serverIndex) {
    if (_serverIndices.contains(operation) && serverIndex < _serverConfigs.find(operation).value().size()) {
        _serverIndices[operation] = serverIndex;
    }
}

void OAIPublicApi::setServerIndex(int serverIndex) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, serverIndex);
    }
}

void OAIPublicApi::setApiKey(const QString &apiKeyName, const QString &apiKey) {
    _apiKeys.insert(apiKeyName, apiKey);
}

void OAIPublicApi::setBearerToken(const QString &token) {
    _bearerToken = token;
}

void OAIPublicApi::setUsername(const QString &username) {
    _username = username;
}

void OAIPublicApi::setPassword(const QString &password) {
    _password = password;
}


void OAIPublicApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAIPublicApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAIPublicApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;
}

/**
 * Appends a new ServerConfiguration to the config map for a specific operation.
 * @param operation The id to the target operation.
 * @param url A string that contains the URL of the server
 * @param description A String that describes the server
 * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
 * returns the index of the new server config on success and -1 if the operation is not found
 */
int OAIPublicApi::addServerConfiguration(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    if (_serverConfigs.contains(operation)) {
        _serverConfigs[operation].append(OAIServerConfiguration(
                    url,
                    description,
                    variables));
        return _serverConfigs[operation].size()-1;
    } else {
        return -1;
    }
}

/**
 * Appends a new ServerConfiguration to the config map for a all operations and sets the index to that server.
 * @param url A string that contains the URL of the server
 * @param description A String that describes the server
 * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
 */
void OAIPublicApi::setNewServerForAllOperations(const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    for (auto keyIt = _serverIndices.keyBegin(); keyIt != _serverIndices.keyEnd(); keyIt++) {
        setServerIndex(*keyIt, addServerConfiguration(*keyIt, url, description, variables));
    }
}

/**
 * Appends a new ServerConfiguration to the config map for an operations and sets the index to that server.
 * @param URL A string that contains the URL of the server
 * @param description A String that describes the server
 * @param variables A map between a variable name and its value. The value is used for substitution in the server's URL template.
 */
void OAIPublicApi::setNewServer(const QString &operation, const QUrl &url, const QString &description, const QMap<QString, OAIServerVariable> &variables) {
    setServerIndex(operation, addServerConfiguration(operation, url, description, variables));
}

void OAIPublicApi::addHeaders(const QString &key, const QString &value) {
    _defaultHeaders.insert(key, value);
}

void OAIPublicApi::enableRequestCompression() {
    _isRequestCompressionEnabled = true;
}

void OAIPublicApi::enableResponseCompression() {
    _isResponseCompressionEnabled = true;
}

void OAIPublicApi::abortRequests() {
    Q_EMIT abortRequestsSignal();
}

QString OAIPublicApi::getParamStylePrefix(const QString &style) {
    if (style == "matrix") {
        return ";";
    } else if (style == "label") {
        return ".";
    } else if (style == "form") {
        return "&";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "&";
    } else if (style == "pipeDelimited") {
        return "&";
    } else {
        return "none";
    }
}

QString OAIPublicApi::getParamStyleSuffix(const QString &style) {
    if (style == "matrix") {
        return "=";
    } else if (style == "label") {
        return "";
    } else if (style == "form") {
        return "=";
    } else if (style == "simple") {
        return "";
    } else if (style == "spaceDelimited") {
        return "=";
    } else if (style == "pipeDelimited") {
        return "=";
    } else {
        return "none";
    }
}

QString OAIPublicApi::getParamStyleDelimiter(const QString &style, const QString &name, bool isExplode) {

    if (style == "matrix") {
        return (isExplode) ? ";" + name + "=" : ",";

    } else if (style == "label") {
        return (isExplode) ? "." : ",";

    } else if (style == "form") {
        return (isExplode) ? "&" + name + "=" : ",";

    } else if (style == "simple") {
        return ",";
    } else if (style == "spaceDelimited") {
        return (isExplode) ? "&" + name + "=" : " ";

    } else if (style == "pipeDelimited") {
        return (isExplode) ? "&" + name + "=" : "|";

    } else if (style == "deepObject") {
        return (isExplode) ? "&" : "none";

    } else {
        return "none";
    }
}

void OAIPublicApi::getCaptcha() {
    QString fullPath = QString(_serverConfigs["getCaptcha"][_serverIndices.value("getCaptcha")].URL()+"/captcha");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIPublicApi::getCaptchaCallback);
    connect(this, &OAIPublicApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIPublicApi::getCaptchaCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAICaptchaResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getCaptchaSignal(output);
        Q_EMIT getCaptchaSignalFull(worker, output);
    } else {
        Q_EMIT getCaptchaSignalError(output, error_type, error_str);
        Q_EMIT getCaptchaSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIPublicApi::getCountries(const ::OpenAPI::OptionalParam<QString> &fetch_by) {
    QString fullPath = QString(_serverConfigs["getCountries"][_serverIndices.value("getCountries")].URL()+"/account/countries");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    QString queryPrefix, querySuffix, queryDelimiter, queryStyle;
    if (fetch_by.hasValue())
    {
        queryStyle = "form";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "fetch_by", true);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("fetch_by")).append(querySuffix).append(QUrl::toPercentEncoding(::OpenAPI::toStringValue(fetch_by.stringValue())));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIPublicApi::getCountriesCallback);
    connect(this, &OAIPublicApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIPublicApi::getCountriesCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIObject output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getCountriesSignal(output);
        Q_EMIT getCountriesSignalFull(worker, output);
    } else {
        Q_EMIT getCountriesSignalError(output, error_type, error_str);
        Q_EMIT getCountriesSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIPublicApi::getInfo() {
    QString fullPath = QString(_serverConfigs["getInfo"][_serverIndices.value("getInfo")].URL()+"/info");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIPublicApi::getInfoCallback);
    connect(this, &OAIPublicApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIPublicApi::getInfoCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIServicesInfo output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getInfoSignal(output);
        Q_EMIT getInfoSignalFull(worker, output);
    } else {
        Q_EMIT getInfoSignalError(output, error_type, error_str);
        Q_EMIT getInfoSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIPublicApi::getLoginInfo() {
    QString fullPath = QString(_serverConfigs["getLoginInfo"][_serverIndices.value("getLoginInfo")].URL()+"/login");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIPublicApi::getLoginInfoCallback);
    connect(this, &OAIPublicApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIPublicApi::getLoginInfoCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAILoginInfo output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getLoginInfoSignal(output);
        Q_EMIT getLoginInfoSignalFull(worker, output);
    } else {
        Q_EMIT getLoginInfoSignalError(output, error_type, error_str);
        Q_EMIT getLoginInfoSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIPublicApi::getMPServers() {
    QString fullPath = QString(_serverConfigs["getMPServers"][_serverIndices.value("getMPServers")].URL()+"/buy_now_servers_list");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIPublicApi::getMPServersCallback);
    connect(this, &OAIPublicApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIPublicApi::getMPServersCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIBuyItNowList<OAIBuyItNowRow> output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getMPServersSignal(output);
        Q_EMIT getMPServersSignalFull(worker, output);
    } else {
        Q_EMIT getMPServersSignalError(output, error_type, error_str);
        Q_EMIT getMPServersSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIPublicApi::getOauthRedirect(const QString &provider) {
    QString fullPath = QString(_serverConfigs["getOauthRedirect"][_serverIndices.value("getOauthRedirect")].URL()+"/oauth");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    QString queryPrefix, querySuffix, queryDelimiter, queryStyle;
    
    {
        queryStyle = "form";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "provider", true);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("provider")).append(querySuffix).append(QUrl::toPercentEncoding(::OpenAPI::toStringValue(provider)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIPublicApi::getOauthRedirectCallback);
    connect(this, &OAIPublicApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIPublicApi::getOauthRedirectCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIGetOauthRedirect_200_response output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getOauthRedirectSignal(output);
        Q_EMIT getOauthRedirectSignalFull(worker, output);
    } else {
        Q_EMIT getOauthRedirectSignalError(output, error_type, error_str);
        Q_EMIT getOauthRedirectSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIPublicApi::getTimezones() {
    QString fullPath = QString(_serverConfigs["getTimezones"][_serverIndices.value("getTimezones")].URL()+"/account/timezones");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIPublicApi::getTimezonesCallback);
    connect(this, &OAIPublicApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIPublicApi::getTimezonesCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    QList<QString> output;
    QString json(worker->response);
    QByteArray array(json.toStdString().c_str());
    QJsonDocument doc = QJsonDocument::fromJson(array);
    QJsonArray jsonArray = doc.array();
    for (QJsonValue obj : jsonArray) {
        QString val;
        ::OpenAPI::fromJsonValue(val, obj);
        output.append(val);
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT getTimezonesSignal(output);
        Q_EMIT getTimezonesSignalFull(worker, output);
    } else {
        Q_EMIT getTimezonesSignalError(output, error_type, error_str);
        Q_EMIT getTimezonesSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIPublicApi::patchOauthTwoFactor(const OAIPatchOauthTwoFactor_request &oai_patch_oauth_two_factor_request) {
    QString fullPath = QString(_serverConfigs["patchOauthTwoFactor"][_serverIndices.value("patchOauthTwoFactor")].URL()+"/oauth");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "PATCH");

    {

        
        QByteArray output = oai_patch_oauth_two_factor_request.asJson().toUtf8();
        input.request_body.append(output);
    }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIPublicApi::patchOauthTwoFactorCallback);
    connect(this, &OAIPublicApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIPublicApi::patchOauthTwoFactorCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIPatchOauthTwoFactor_200_response output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT patchOauthTwoFactorSignal(output);
        Q_EMIT patchOauthTwoFactorSignalFull(worker, output);
    } else {
        Q_EMIT patchOauthTwoFactorSignalError(output, error_type, error_str);
        Q_EMIT patchOauthTwoFactorSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIPublicApi::pingServer() {
    QString fullPath = QString(_serverConfigs["pingServer"][_serverIndices.value("pingServer")].URL()+"/ping");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");


    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIPublicApi::pingServerCallback);
    connect(this, &OAIPublicApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIPublicApi::pingServerCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    QString output;
    ::OpenAPI::fromStringValue(QString(worker->response), output);
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT pingServerSignal(output);
        Q_EMIT pingServerSignalFull(worker, output);
    } else {
        Q_EMIT pingServerSignalError(output, error_type, error_str);
        Q_EMIT pingServerSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIPublicApi::postOauthCallback(const QString &provider, const ::OpenAPI::OptionalParam<OAIPostOauthCallback_request> &oai_post_oauth_callback_request) {
    QString fullPath = QString(_serverConfigs["postOauthCallback"][_serverIndices.value("postOauthCallback")].URL()+"/oauth");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    QString queryPrefix, querySuffix, queryDelimiter, queryStyle;
    
    {
        queryStyle = "form";
        if (queryStyle == "")
            queryStyle = "form";
        queryPrefix = getParamStylePrefix(queryStyle);
        querySuffix = getParamStyleSuffix(queryStyle);
        queryDelimiter = getParamStyleDelimiter(queryStyle, "provider", true);
        if (fullPath.indexOf("?") > 0)
            fullPath.append(queryPrefix);
        else
            fullPath.append("?");

        fullPath.append(QUrl::toPercentEncoding("provider")).append(querySuffix).append(QUrl::toPercentEncoding(::OpenAPI::toStringValue(provider)));
    }
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (oai_post_oauth_callback_request.hasValue()){

        
        QByteArray output = oai_post_oauth_callback_request.value().asJson().toUtf8();
        input.request_body.append(output);
    }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIPublicApi::postOauthCallbackCallback);
    connect(this, &OAIPublicApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIPublicApi::postOauthCallbackCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAIPostOauthCallback_200_response output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT postOauthCallbackSignal(output);
        Q_EMIT postOauthCallbackSignalFull(worker, output);
    } else {
        Q_EMIT postOauthCallbackSignalError(output, error_type, error_str);
        Q_EMIT postOauthCallbackSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIPublicApi::submitLogin(const QString &login, const QString &passwd, const ::OpenAPI::OptionalParam<QString> &remember, const ::OpenAPI::OptionalParam<OAILoginSubmissionExample_g_recaptcha_response> &g_recaptcha_response, const ::OpenAPI::OptionalParam<QString> &tfa) {
    QString fullPath = QString(_serverConfigs["submitLogin"][_serverIndices.value("submitLogin")].URL()+"/login");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    
    {
        input.add_var("login", ::OpenAPI::toStringValue(login));
    }
    
    {
        input.add_var("passwd", ::OpenAPI::toStringValue(passwd));
    }
    if (remember.hasValue())
    {
        input.add_var("remember", ::OpenAPI::toStringValue(remember.value()));
    }
    if (g_recaptcha_response.hasValue())
    {
        input.add_var("g-recaptcha-response", ::OpenAPI::toStringValue(g_recaptcha_response.value()));
    }
    if (tfa.hasValue())
    {
        input.add_var("tfa", ::OpenAPI::toStringValue(tfa.value()));
    }

    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIPublicApi::submitLoginCallback);
    connect(this, &OAIPublicApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIPublicApi::submitLoginCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    OAILoginSuccessResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT submitLoginSignal(output);
        Q_EMIT submitLoginSignalFull(worker, output);
    } else {
        Q_EMIT submitLoginSignalError(output, error_type, error_str);
        Q_EMIT submitLoginSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIPublicApi::submitSignup(const ::OpenAPI::OptionalParam<OAILoginSubmissionExample> &oai_login_submission_example) {
    QString fullPath = QString(_serverConfigs["submitSignup"][_serverIndices.value("submitSignup")].URL()+"/signup");
    
    if (_apiKeys.contains("apiKeyAuth")) {
        addHeaders("apiKeyAuth",_apiKeys.find("apiKeyAuth").value());
    }
    
    if (_apiKeys.contains("sessionIdHeaderAuth")) {
        addHeaders("sessionIdHeaderAuth",_apiKeys.find("sessionIdHeaderAuth").value());
    }
    
    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "POST");

    if (oai_login_submission_example.hasValue()){

        
        QByteArray output = oai_login_submission_example.value().asJson().toUtf8();
        input.request_body.append(output);
    }
    for (auto keyValueIt = _defaultHeaders.keyValueBegin(); keyValueIt != _defaultHeaders.keyValueEnd(); keyValueIt++) {
        input.headers.insert(keyValueIt->first, keyValueIt->second);
    }


    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIPublicApi::submitSignupCallback);
    connect(this, &OAIPublicApi::abortRequestsSignal, worker, &QObject::deleteLater);
    connect(worker, &QObject::destroyed, this, [this] {
        if (findChildren<OAIHttpRequestWorker*>().count() == 0) {
            Q_EMIT allPendingRequestsCompleted();
        }
    });

    worker->execute(&input);
}

void OAIPublicApi::submitSignupCallback(OAIHttpRequestWorker *worker) {
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type != QNetworkReply::NoError) {
        error_str = QString("%1, %2").arg(worker->error_str, QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        Q_EMIT submitSignupSignal();
        Q_EMIT submitSignupSignalFull(worker);
    } else {
        Q_EMIT submitSignupSignalError(error_type, error_str);
        Q_EMIT submitSignupSignalErrorFull(worker, error_type, error_str);
    }
}

void OAIPublicApi::tokenAvailable() {

    oauthToken token;
    switch (_OauthMethod) {
    case OauthMethod::ImplicitFlow:
        token = _implicitFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _implicitFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case OauthMethod::AuthorizationFlow:
        token = _authFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _authFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case OauthMethod::ClientCredentialsFlow:
        token = _credentialFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    case OauthMethod::ResourceOwnerPasswordFlow:
        token = _passwordFlow.getToken(_latestScope.join(" "));
        if(token.isValid()){
            _latestInput.headers.insert("Authorization", "Bearer " + token.getToken());
            _latestWorker->execute(&_latestInput);
        }else{
            _credentialFlow.removeToken(_latestScope.join(" "));
            qDebug() << "Could not retrieve a valid token";
        }
        break;
    default:
        qDebug() << "No Oauth method set!";
        break;
    }
}
} // namespace OpenAPI
