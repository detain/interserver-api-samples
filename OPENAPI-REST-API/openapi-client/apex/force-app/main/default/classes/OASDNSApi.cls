/*
 * InterServer Management API
 * # Overview\n\nThe InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.\n\nThe API covers the following service categories:\n- [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management\n- [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting\n- [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers\n- [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html)\n- [Backups](https://www.interserver.net/storage/) — storage and backup services\n- Licenses — control panel and software licenses\n- [Mail](https://www.mail.baby/) — mail delivery services\n- SSL — certificate provisioning\n- Billing — invoices, payment methods, and account management\n\nFor interactive testing, see the [API documentation](/api-docs/).\n\n# Authentication\n\nMost endpoints require authentication. Two methods are supported:\n\n## API Key (Preferred)\n\nGenerate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:\n\n```\nX-API-KEY: your-api-key-here\n```\n\n## Session-Based Authentication\n\nAlternatively, authenticate by creating a session:\n\n1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier.\n2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:\n\n```\nsessionid: your-session-id-here\n```\n\nAPI key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.\n\n
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by the OAS code generator program.
 * https://github.com/OpenAPITools/openapi-generator
 * Do not edit the class manually.
 */

public class OASDNSApi {
    OASClient client;

    public OASDNSApi(OASClient client) {
        this.client = client;
    }

    public OASDNSApi() {
        this.client = new OASClient();
    }

    public OASClient getClient() {
        return this.client;
    }

    /**
     * Create DNS Domain
     * Creates a new DNS domain and assigns an initial A record pointing to the supplied IP address. The domain is immediately available on InterServer\&#39;s DNS servers. Use &#x60;/dns/{id}&#x60; to manage records after creation.
     * @param domain The domain name. (required)
     * @param ip IP Address to point the domain to. (required)
     * @throws OAS.ApiException if fails to make API call
     */
    public void addDnsDomain(Map<String, Object> params) {
        client.assertNotNull(params.get('domain'), 'domain');
        client.assertNotNull(params.get('ip'), 'ip');
        List<OAS.Param> query = new List<OAS.Param>();
        List<OAS.Param> form = new List<OAS.Param>();

        // cast form params to verify their expected type
        form.addAll(client.makeParam('domain', (String) params.get('domain')));
        form.addAll(client.makeParam('ip', (String) params.get('ip')));

        client.invoke(
            'POST', '/dns', '',
            query, form,
            new Map<String, Object>(),
            new Map<String, Object>(),
            new List<String>{ 'application/json' },
            new List<String>{ 'multipart/form-data', 'application/json' },
            new List<String> { 'sessionIdCookieAuth', 'apiKeyAuth', 'sessionIdHeaderAuth' },
            null
        );
    }
    /**
     * Add DNS Record to Domain
     * Adds a new DNS record to the specified domain. Provide the record type (A, AAAA, CNAME, MX, TXT, etc.), name, content, TTL, and priority. The record takes effect on the DNS servers immediately. Use &#x60;GET /dns/{id}&#x60; afterward to confirm the record was created.
     * @param id The DNS Domain ID. (required)
     * @param name Name part of record (required)
     * @param type  (required)
     * @param content Content of record (required)
     * @param ttl Time-to-live (optional, default to 86400)
     * @param prio Priority (optional, default to 0)
     * @throws OAS.ApiException if fails to make API call
     */
    public void addDnsRecord(Map<String, Object> params) {
        client.assertNotNull(params.get('id'), 'id');
        client.assertNotNull(params.get('name'), 'name');
        client.assertNotNull(params.get('type'), 'type');
        client.assertNotNull(params.get('content'), 'content');
        List<OAS.Param> query = new List<OAS.Param>();
        List<OAS.Param> form = new List<OAS.Param>();

        // cast form params to verify their expected type
        form.addAll(client.makeParam('name', (String) params.get('name')));
        form.addAll(client.makeParam('type', (OASDnsRecordType) params.get('type')));
        form.addAll(client.makeParam('content', (String) params.get('content')));
        form.addAll(client.makeParam('ttl', (Integer) params.get('ttl')));
        form.addAll(client.makeParam('prio', (Integer) params.get('prio')));

        client.invoke(
            'POST', '/dns/{id}', '',
            query, form,
            new Map<String, Object>{
                'id' => (String) params.get('id')
            },
            new Map<String, Object>(),
            new List<String>{ 'application/json' },
            new List<String>{ 'multipart/form-data', 'application/json' },
            new List<String> { 'sessionIdCookieAuth', 'apiKeyAuth', 'sessionIdHeaderAuth' },
            null
        );
    }
    /**
     * Delete DNS Domain
     * Deletes a DNS domain and all of its associated records from the DNS servers. This action is permanent and cannot be undone. Any services relying on these DNS records will be affected immediately.
     * @param id The DNS domain ID to delete. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain. (required)
     * @throws OAS.ApiException if fails to make API call
     */
    public void deleteDnsDomain(Map<String, Object> params) {
        client.assertNotNull(params.get('id'), 'id');
        List<OAS.Param> query = new List<OAS.Param>();
        List<OAS.Param> form = new List<OAS.Param>();

        client.invoke(
            'DELETE', '/dns/{id}', '',
            query, form,
            new Map<String, Object>{
                'id' => (String) params.get('id')
            },
            new Map<String, Object>(),
            new List<String>{ 'application/json' },
            new List<String>(),
            new List<String> { 'sessionIdCookieAuth', 'apiKeyAuth', 'sessionIdHeaderAuth' },
            null
        );
    }
    /**
     * Delete DNS Record
     * Removes a DNS record from the specified domain. The deletion takes effect on the DNS servers immediately. Use &#x60;GET /dns/{id}&#x60; to verify the record has been removed.
     * @param domainId The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain. (required)
     * @param recordId The DNS record ID within the domain. Use the record &#x60;id&#x60; from &#x60;GET /dns/{id}&#x60; to identify the record. (required)
     * @throws OAS.ApiException if fails to make API call
     */
    public void deleteDnsRecord(Map<String, Object> params) {
        client.assertNotNull(params.get('domainId'), 'domainId');
        client.assertNotNull(params.get('recordId'), 'recordId');
        List<OAS.Param> query = new List<OAS.Param>();
        List<OAS.Param> form = new List<OAS.Param>();

        client.invoke(
            'DELETE', '/dns/{domainId}/{recordId}', '',
            query, form,
            new Map<String, Object>{
                'domainId' => (Integer) params.get('domainId'),
                'recordId' => (Integer) params.get('recordId')
            },
            new Map<String, Object>(),
            new List<String>{ 'application/json' },
            new List<String>(),
            new List<String> { 'sessionIdCookieAuth', 'apiKeyAuth', 'sessionIdHeaderAuth' },
            null
        );
    }
    /**
     * List Domain DNS Records
     * Returns the full set of DNS records for the specified domain, including NS, A, AAAA, CNAME, MX, TXT, and other record types. Use the record &#x60;id&#x60; values with &#x60;/dns/{domainId}/{recordId}&#x60; to update or delete individual records.
     * @param id The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain. (required)
     * @return List<OASDnsRecord>
     * @throws OAS.ApiException if fails to make API call
     */
    public List<OASDnsRecord> getDnsDomain(Map<String, Object> params) {
        client.assertNotNull(params.get('id'), 'id');
        List<OAS.Param> query = new List<OAS.Param>();
        List<OAS.Param> form = new List<OAS.Param>();

        return (List<OASDnsRecord>) client.invoke(
            'GET', '/dns/{id}', '',
            query, form,
            new Map<String, Object>{
                'id' => (Integer) params.get('id')
            },
            new Map<String, Object>(),
            new List<String>{ 'application/json' },
            new List<String>(),
            new List<String> { 'sessionIdCookieAuth', 'apiKeyAuth', 'sessionIdHeaderAuth' },
            List<OASDnsRecord>.class
        );
    }
    /**
     * List DNS Domains
     * Returns the DNS domains on your account along with their primary A record content. Use the &#x60;id&#x60; from each entry with &#x60;/dns/{id}&#x60; to retrieve the full record set, or to add, update, and delete individual records.
     * @return List<OASDnsListItem>
     * @throws OAS.ApiException if fails to make API call
     */
    public List<OASDnsListItem> getDnsList() {
        List<OAS.Param> query = new List<OAS.Param>();
        List<OAS.Param> form = new List<OAS.Param>();

        return (List<OASDnsListItem>) client.invoke(
            'GET', '/dns', '',
            query, form,
            new Map<String, Object>(),
            new Map<String, Object>(),
            new List<String>{ 'application/json' },
            new List<String>(),
            new List<String> { 'sessionIdCookieAuth', 'apiKeyAuth', 'sessionIdHeaderAuth' },
            List<OASDnsListItem>.class
        );
    }
    /**
     * Update DNS Record
     * Updates an existing DNS record with new values. Use &#x60;GET /dns/{id}&#x60; to list records and retrieve the record IDs before updating. Changes propagate to the DNS servers immediately.
     * @param domainId The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain. (required)
     * @param recordId The DNS record ID within the domain. Use the record &#x60;id&#x60; from &#x60;GET /dns/{id}&#x60; to identify the record. (required)
     * @param name  (optional)
     * @param type  (optional)
     * @param content  (optional)
     * @param ttl  (optional)
     * @param prio  (optional)
     * @param disabled  (optional)
     * @param ordername  (optional)
     * @param auth  (optional)
     * @throws OAS.ApiException if fails to make API call
     */
    public void updateDnsRecord(Map<String, Object> params) {
        client.assertNotNull(params.get('domainId'), 'domainId');
        client.assertNotNull(params.get('recordId'), 'recordId');
        List<OAS.Param> query = new List<OAS.Param>();
        List<OAS.Param> form = new List<OAS.Param>();

        // cast form params to verify their expected type
        form.addAll(client.makeParam('name', (String) params.get('name')));
        form.addAll(client.makeParam('type', (OASDnsRecordType) params.get('type')));
        form.addAll(client.makeParam('content', (String) params.get('content')));
        form.addAll(client.makeParam('ttl', (String) params.get('ttl')));
        form.addAll(client.makeParam('prio', (String) params.get('prio')));
        form.addAll(client.makeParam('disabled', (String) params.get('disabled')));
        form.addAll(client.makeParam('ordername', (String) params.get('ordername')));
        form.addAll(client.makeParam('auth', (String) params.get('auth')));

        client.invoke(
            'POST', '/dns/{domainId}/{recordId}', '',
            query, form,
            new Map<String, Object>{
                'domainId' => (Integer) params.get('domainId'),
                'recordId' => (Integer) params.get('recordId')
            },
            new Map<String, Object>(),
            new List<String>{ 'application/json' },
            new List<String>{ 'multipart/form-data', 'application/json' },
            new List<String> { 'sessionIdCookieAuth', 'apiKeyAuth', 'sessionIdHeaderAuth' },
            null
        );
    }
}
