/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  CancelScrubIp200Response,
  GetOrderDetail200Response,
  ChargeInvoiceRows,
  EnableScrub200Response,
  CreateRule201Response,
  ScrubIpsDeleteRule200Response,
  CreateFirewallRule,
  ScrubIpFilterTypes,
  CreateFilter201Response,
  DisableScrub200Response,
  GetScrubIpDetails200Response,
  DeleteFirewallRule,
  PlaceScrubOrder201Response,
  DeleteGeoFirewallRule,
  ScrubIpsLogRowSchema,
  ScrubIpsRowSchema,
  CreateGeoFirewallRule,
  CreateFilter,
  DeleteFilter200Response,
  ScrubIpPlaceOrder,
} from './models';

/**
 * cancelScrubIp - parameters interface
 */
export interface ICancelScrubIpParams {
  id: number;
}

/**
 * createFilter - parameters interface
 */
export interface ICreateFilterParams {
  id: number;
  createFilter: CreateFilter;
}

/**
 * createGeoRule - parameters interface
 */
export interface ICreateGeoRuleParams {
  id: number;
  createGeoFirewallRule: CreateGeoFirewallRule;
}

/**
 * createRule - parameters interface
 */
export interface ICreateRuleParams {
  id: number;
  createFirewallRule: CreateFirewallRule;
}

/**
 * deleteFilter - parameters interface
 */
export interface IDeleteFilterParams {
  id: number;
  createFilter: CreateFilter;
}

/**
 * disableScrub - parameters interface
 */
export interface IDisableScrubParams {
  id: number;
}

/**
 * enableScrub - parameters interface
 */
export interface IEnableScrubParams {
  id: number;
}

/**
 * getOrderDetail - parameters interface
 */
export interface IGetOrderDetailParams {
}

/**
 * getScrubIpDetails - parameters interface
 */
export interface IGetScrubIpDetailsParams {
  id: number;
}

/**
 * getScrubIpFilterTypes - parameters interface
 */
export interface IGetScrubIpFilterTypesParams {
}

/**
 * getScrubIpInvoices - parameters interface
 */
export interface IGetScrubIpInvoicesParams {
  id: number;
}

/**
 * getScrubIpLogs - parameters interface
 */
export interface IGetScrubIpLogsParams {
  id: string;
}

/**
 * getScrubIpsList - parameters interface
 */
export interface IGetScrubIpsListParams {
}

/**
 * placeScrubOrder - parameters interface
 */
export interface IPlaceScrubOrderParams {
  scrubIpPlaceOrder: ScrubIpPlaceOrder;
}

/**
 * scrubIpsDeleteGeoRule - parameters interface
 */
export interface IScrubIpsDeleteGeoRuleParams {
  id: number;
  deleteGeoFirewallRule: DeleteGeoFirewallRule;
}

/**
 * scrubIpsDeleteRule - parameters interface
 */
export interface IScrubIpsDeleteRuleParams {
  id: number;
  deleteFirewallRule: DeleteFirewallRule;
}

/**
 * ScrubIpsApi - API class
 */
@autoinject()
export class ScrubIpsApi extends Api {

  /**
   * Creates a new ScrubIpsApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Cancel Scrub IP Service
   * Cancels the Scrub IP DDoS protection service. The protection will be removed and billing will stop at the end of the current billing cycle.
   * @param params.id ScrubIp ID number
   */
  async cancelScrubIp(params: ICancelScrubIpParams): Promise<CancelScrubIp200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('cancelScrubIp', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/scrub_ips/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Create Traffic Filter
   * Creates a traffic filter for the Scrub IP service. Filters apply predefined scrubbing profiles (e.g., DNS, HTTP) to specific destination ports. Use &#x60;GET /scrub_ips/filter_types&#x60; to list available filter types.
   * @param params.id ScrubIp ID number
   * @param params.createFilter 
   */
  async createFilter(params: ICreateFilterParams): Promise<CreateFilter201Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('createFilter', params, 'id');
    this.ensureParamIsSet('createFilter', params, 'createFilter');

    // Create URL to call
    const url = `${this.basePath}/scrub_ips/{id}/create_filter`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['createFilter'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Create Geo Firewall Rule
   * Creates a geographic-based firewall rule for the Scrub IP service. Geo rules allow you to block or allow traffic from specific countries or regions.
   * @param params.id ScrubIp ID number
   * @param params.createGeoFirewallRule 
   */
  async createGeoRule(params: ICreateGeoRuleParams): Promise<CreateRule201Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('createGeoRule', params, 'id');
    this.ensureParamIsSet('createGeoRule', params, 'createGeoFirewallRule');

    // Create URL to call
    const url = `${this.basePath}/scrub_ips/{id}/create_geo_rule`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['createGeoFirewallRule'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Create Firewall Rule
   * Creates a new firewall rule for the Scrub IP service. Rules allow you to block or allow traffic based on source IP, destination port, and protocol.
   * @param params.id ScrubIp ID number
   * @param params.createFirewallRule 
   */
  async createRule(params: ICreateRuleParams): Promise<CreateRule201Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('createRule', params, 'id');
    this.ensureParamIsSet('createRule', params, 'createFirewallRule');

    // Create URL to call
    const url = `${this.basePath}/scrub_ips/{id}/create_rule`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['createFirewallRule'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delete Traffic Filter
   * Removes an existing traffic filter from the Scrub IP service. Provide the same filter parameters used during creation to identify which filter to remove.
   * @param params.id ScrubIp ID number
   * @param params.createFilter 
   */
  async deleteFilter(params: IDeleteFilterParams): Promise<DeleteFilter200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteFilter', params, 'id');
    this.ensureParamIsSet('deleteFilter', params, 'createFilter');

    // Create URL to call
    const url = `${this.basePath}/scrub_ips/{id}/delete_filter`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['createFilter'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Disable Scrub Protection
   * Disables DDoS scrubbing protection on the IP address. Traffic will no longer be routed through the scrubbing infrastructure.
   * @param params.id ScrubIp ID number
   */
  async disableScrub(params: IDisableScrubParams): Promise<DisableScrub200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('disableScrub', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/scrub_ips/{id}/disable`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Enable Scrub Protection
   * Enables DDoS scrubbing protection on the IP address associated with this service. Traffic will be routed through the scrubbing infrastructure to filter malicious packets.
   * @param params.id ScrubIp ID number
   */
  async enableScrub(params: IEnableScrubParams): Promise<EnableScrub200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('enableScrub', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/scrub_ips/{id}/enable`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Scrub IP Ordering Information
   * Returns the available Scrub IP service plans and pricing information needed to build an order form.
   */
  async getOrderDetail(): Promise<GetOrderDetail200Response> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/scrub_ips/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Scrub IP Details
   * Returns detailed information about a Scrub IP service, including connection details, billing information, active firewall rules, and traffic filters.
   * @param params.id ScrubIp ID number
   */
  async getScrubIpDetails(params: IGetScrubIpDetailsParams): Promise<GetScrubIpDetails200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('getScrubIpDetails', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/scrub_ips/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List Scrub Filter Types
   * Returns the list of scrub filter types that can be used when creating filter rules via &#x60;/scrub_ips/{id}/create_filter&#x60;.
   */
  async getScrubIpFilterTypes(): Promise<ScrubIpFilterTypes> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/scrub_ips/filter_types`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get ScrubIp Invoices
   * Retrieves invoices associated with the scrub IP service. Use these invoices to confirm billing status or to initiate payment.
   * @param params.id ScrubIp ID number
   */
  async getScrubIpInvoices(params: IGetScrubIpInvoicesParams): Promise<ChargeInvoiceRows> {
    // Verify required parameters are set
    this.ensureParamIsSet('getScrubIpInvoices', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/scrub_ips/{id}/invoices`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Scrub IP Logs
   * Returns the activity and event logs for the Scrub IP service, including scrubbing events and configuration changes.
   * @param params.id Scrub Order ID
   */
  async getScrubIpLogs(params: IGetScrubIpLogsParams): Promise<Array<ScrubIpsLogRowSchema>> {
    // Verify required parameters are set
    this.ensureParamIsSet('getScrubIpLogs', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/scrub_ips/{id}/logs`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List Scrub IP Services
   * Returns all Scrub IP DDoS protection services on your account with their current status and associated IP addresses.
   */
  async getScrubIpsList(): Promise<Array<ScrubIpsRowSchema>> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/scrub_ips`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Place Scrub IP Order
   * Places an order for a new Scrub IP DDoS protection service. On success, an invoice is generated for payment.
   * @param params.scrubIpPlaceOrder 
   */
  async placeScrubOrder(params: IPlaceScrubOrderParams): Promise<PlaceScrubOrder201Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('placeScrubOrder', params, 'scrubIpPlaceOrder');

    // Create URL to call
    const url = `${this.basePath}/scrub_ips/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['scrubIpPlaceOrder'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delete Geo Firewall Rule
   * Removes an existing geographic-based firewall rule from the Scrub IP service. Use the &#x60;rule_id&#x60; from the service details response to identify the rule to delete.
   * @param params.id ScrubIp ID number
   * @param params.deleteGeoFirewallRule 
   */
  async scrubIpsDeleteGeoRule(params: IScrubIpsDeleteGeoRuleParams): Promise<ScrubIpsDeleteRule200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('scrubIpsDeleteGeoRule', params, 'id');
    this.ensureParamIsSet('scrubIpsDeleteGeoRule', params, 'deleteGeoFirewallRule');

    // Create URL to call
    const url = `${this.basePath}/scrub_ips/{id}/delete_geo_rule`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['deleteGeoFirewallRule'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delete Firewall Rule
   * Removes an existing firewall rule from the Scrub IP service. Use the &#x60;rule_id&#x60; from the service details response to identify the rule to delete.
   * @param params.id ScrubIp ID number
   * @param params.deleteFirewallRule 
   */
  async scrubIpsDeleteRule(params: IScrubIpsDeleteRuleParams): Promise<ScrubIpsDeleteRule200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('scrubIpsDeleteRule', params, 'id');
    this.ensureParamIsSet('scrubIpsDeleteRule', params, 'deleteFirewallRule');

    // Create URL to call
    const url = `${this.basePath}/scrub_ips/{id}/delete_rule`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['deleteFirewallRule'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

