/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  Quickserver,
  QuickserverRow,
  ChargeInvoiceRows,
  DownloadQsBackupRequest,
  SuccessTextResponse,
  QuickserverOrder,
  VpsBackupRows,
  TextResponse,
  ReverseDnsEntries,
  QueueResponse,
  DownloadQsBackup200Response,
  VpsTemplatesList,
  QuickserversCancel200Response,
  RestoreRequest,
} from './models';

/**
 * addQs - parameters interface
 */
export interface IAddQsParams {
}

/**
 * deleteQsBackup - parameters interface
 */
export interface IDeleteQsBackupParams {
  id: number;
  file: string;
  all?: '0' | '1';
}

/**
 * doQsBlockSmtp - parameters interface
 */
export interface IDoQsBlockSmtpParams {
  id: number;
}

/**
 * doQsDisableCd - parameters interface
 */
export interface IDoQsDisableCdParams {
  id: number;
}

/**
 * doQsDisableQuota - parameters interface
 */
export interface IDoQsDisableQuotaParams {
  id: number;
}

/**
 * doQsEjectCd - parameters interface
 */
export interface IDoQsEjectCdParams {
  id: number;
}

/**
 * doQsEnableQuota - parameters interface
 */
export interface IDoQsEnableQuotaParams {
  id: number;
}

/**
 * doQsRestart - parameters interface
 */
export interface IDoQsRestartParams {
  id: number;
}

/**
 * doQsStart - parameters interface
 */
export interface IDoQsStartParams {
  id: number;
}

/**
 * doQsStop - parameters interface
 */
export interface IDoQsStopParams {
  id: number;
}

/**
 * downloadQsBackup - parameters interface
 */
export interface IDownloadQsBackupParams {
  id: number;
  downloadQsBackupRequest: DownloadQsBackupRequest;
  all?: '0' | '1';
}

/**
 * getNewQs - parameters interface
 */
export interface IGetNewQsParams {
}

/**
 * getQsBackups - parameters interface
 */
export interface IGetQsBackupsParams {
  id: number;
  all?: '0' | '1';
}

/**
 * getQsChangeHostname - parameters interface
 */
export interface IGetQsChangeHostnameParams {
  id: number;
}

/**
 * getQsChangeRootPassword - parameters interface
 */
export interface IGetQsChangeRootPasswordParams {
  id: number;
}

/**
 * getQsChangeTimezone - parameters interface
 */
export interface IGetQsChangeTimezoneParams {
  id: number;
}

/**
 * getQsChangeWebuzoPassword - parameters interface
 */
export interface IGetQsChangeWebuzoPasswordParams {
  id: number;
}

/**
 * getQsInfo - parameters interface
 */
export interface IGetQsInfoParams {
  id: number;
}

/**
 * getQsInsertCd - parameters interface
 */
export interface IGetQsInsertCdParams {
  id: number;
}

/**
 * getQsInvoices - parameters interface
 */
export interface IGetQsInvoicesParams {
  id: number;
}

/**
 * getQsList - parameters interface
 */
export interface IGetQsListParams {
}

/**
 * getQsReinstallOs - parameters interface
 */
export interface IGetQsReinstallOsParams {
  id: number;
}

/**
 * getQsResetPassword - parameters interface
 */
export interface IGetQsResetPasswordParams {
  id: number;
}

/**
 * getQsReverseDns - parameters interface
 */
export interface IGetQsReverseDnsParams {
  id: number;
}

/**
 * getQsSetupVnc - parameters interface
 */
export interface IGetQsSetupVncParams {
  id: number;
}

/**
 * getQsTrafficUsage - parameters interface
 */
export interface IGetQsTrafficUsageParams {
  id: number;
}

/**
 * getQsViewDesktop - parameters interface
 */
export interface IGetQsViewDesktopParams {
  id: number;
}

/**
 * getQsWelcomeEmail - parameters interface
 */
export interface IGetQsWelcomeEmailParams {
  id: string;
}

/**
 * postQsBackup - parameters interface
 */
export interface IPostQsBackupParams {
  id: number;
}

/**
 * postQsChangeHostname - parameters interface
 */
export interface IPostQsChangeHostnameParams {
  id: number;
}

/**
 * postQsChangeRootPassword - parameters interface
 */
export interface IPostQsChangeRootPasswordParams {
  id: number;
}

/**
 * postQsChangeTimezone - parameters interface
 */
export interface IPostQsChangeTimezoneParams {
  id: number;
  timezone: string;
}

/**
 * postQsChangeWebuzoPassword - parameters interface
 */
export interface IPostQsChangeWebuzoPasswordParams {
  id: number;
}

/**
 * postQsInsertCd - parameters interface
 */
export interface IPostQsInsertCdParams {
  id: number;
}

/**
 * postQsReinstallOs - parameters interface
 */
export interface IPostQsReinstallOsParams {
  id: number;
}

/**
 * postQsResetPassword - parameters interface
 */
export interface IPostQsResetPasswordParams {
  id: number;
}

/**
 * postQsReverseDns - parameters interface
 */
export interface IPostQsReverseDnsParams {
  id: number;
  reverseDnsEntries: ReverseDnsEntries;
}

/**
 * postQsSetupVnc - parameters interface
 */
export interface IPostQsSetupVncParams {
  id: number;
}

/**
 * postQsTrafficUsage - parameters interface
 */
export interface IPostQsTrafficUsageParams {
  id: number;
}

/**
 * postQsViewDesktop - parameters interface
 */
export interface IPostQsViewDesktopParams {
  id: number;
}

/**
 * postQuickServerRestore - parameters interface
 */
export interface IPostQuickServerRestoreParams {
  id: number;
  restoreRequest: RestoreRequest;
}

/**
 * putQs - parameters interface
 */
export interface IPutQsParams {
}

/**
 * quickserversCancel - parameters interface
 */
export interface IQuickserversCancelParams {
  id: number;
}

/**
 * updateQsInfo - parameters interface
 */
export interface IUpdateQsInfoParams {
  id: string;
}

/**
 * QuickServersApi - API class
 */
@autoinject()
export class QuickServersApi extends Api {

  /**
   * Creates a new QuickServersApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Place QuickServer Order
   * Places a QuickServer order. On success, invoices are generated for payment; use &#x60;/billing/invoices/{id}&#x60; or &#x60;/pay/{method}/{invoices}&#x60; to complete payment.
   */
  async addQs(): Promise<any> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/qs/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delete QuickServer Backup
   * Permanently removes the specified backup file from storage. Use &#x60;GET /qs/{id}/backups&#x60; to list available backup filenames before deleting.
   * @param params.id QuickServer ID number
   * @param params.file The backup filename to delete.
   * @param params.all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given QuickServer.
   */
  async deleteQsBackup(params: IDeleteQsBackupParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteQsBackup', params, 'id');
    this.ensureParamIsSet('deleteQsBackup', params, 'file');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/backups`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()
      // Set query parameters
      .withParams({ 
        'all': params['all'],
        'file': params['file'],
      })

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Block QuickServer SMTP
   * Blocks outbound SMTP for the QuickServer to prevent email abuse. Use this action when responding to abuse notifications or to enforce outbound email policies.
   * @param params.id QuickServer ID number.
   */
  async doQsBlockSmtp(params: IDoQsBlockSmtpParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('doQsBlockSmtp', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/block_smtp`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Disable CD Drive
   * Disables the virtual CD drive for the QuickServer.
   * @param params.id QuickServer ID number.
   */
  async doQsDisableCd(params: IDoQsDisableCdParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('doQsDisableCd', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/disable_cd`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Disable Quotas
   * Disables disk quota enforcement for the QuickServer.
   * @param params.id QuickServer ID number.
   */
  async doQsDisableQuota(params: IDoQsDisableQuotaParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('doQsDisableQuota', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/disable_quota`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Eject CD Drive
   * Ejects the virtual CD from the QuickServer\&#39;s CD drive.
   * @param params.id QuickServer ID number.
   */
  async doQsEjectCd(params: IDoQsEjectCdParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('doQsEjectCd', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/eject_cd`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Enable Quotas
   * Enables disk quota enforcement for the QuickServer.
   * @param params.id QuickServer ID number.
   */
  async doQsEnableQuota(params: IDoQsEnableQuotaParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('doQsEnableQuota', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/enable_quota`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Restart QuickServer
   * Restarts the QuickServer. The server will be shut down and started again.
   * @param params.id QuickServer ID number.
   */
  async doQsRestart(params: IDoQsRestartParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('doQsRestart', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/restart`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Start QuickServer
   * Powers on the QuickServer.
   * @param params.id QuickServer ID number.
   */
  async doQsStart(params: IDoQsStartParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('doQsStart', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/start`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Stop QuickServer
   * Powers off the QuickServer.
   * @param params.id QuickServer ID number.
   */
  async doQsStop(params: IDoQsStopParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('doQsStop', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/stop`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Download QuickServer Backup
   * Generates a pre-signed download URL for the specified backup file. The URL is valid for 24 hours. Use &#x60;GET /qs/{id}/backups&#x60; to list available backup filenames.
   * @param params.id QuickServer ID number
   * @param params.downloadQsBackupRequest 
   * @param params.all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given QuickServer.
   */
  async downloadQsBackup(params: IDownloadQsBackupParams): Promise<DownloadQsBackup200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('downloadQsBackup', params, 'id');
    this.ensureParamIsSet('downloadQsBackup', params, 'downloadQsBackupRequest');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/backups`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPatch()
      // Set query parameters
      .withParams({ 
        'all': params['all'],
      })
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['downloadQsBackupRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get QuickServer Ordering Information
   * Returns QuickServer ordering metadata and available plans. Use these details to build the order form and to validate a plan selection.
   */
  async getNewQs(): Promise<QuickserverOrder> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/qs/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List QuickServer Backups
   * Returns the available backups for the QuickServer across all storage systems (Swift, MinIO, ZFS). Use the backup &#x60;name&#x60; value with &#x60;PATCH /qs/{id}/backups&#x60; to download or &#x60;DELETE /qs/{id}/backups&#x60; to remove a backup. Use &#x60;POST /qs/{id}/restore&#x60; to restore from a backup.
   * @param params.id QuickServer ID number
   * @param params.all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given QuickServer.
   */
  async getQsBackups(params: IGetQsBackupsParams): Promise<VpsBackupRows> {
    // Verify required parameters are set
    this.ensureParamIsSet('getQsBackups', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/backups`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'all': params['all'],
      })

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get QuickServer Hostname
   * Retrieves the current hostname and any validation requirements for changing it.
   * @param params.id QuickServer ID number
   */
  async getQsChangeHostname(params: IGetQsChangeHostnameParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('getQsChangeHostname', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/change_hostname`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Change Root Password Info
   * Retrieves instructions or metadata needed to reset the root password.
   * @param params.id QuickServer ID number
   */
  async getQsChangeRootPassword(params: IGetQsChangeRootPasswordParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('getQsChangeRootPassword', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/change_root_password`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Timezone Info
   * Returns the list of available timezones that can be set on the QuickServer.
   * @param params.id QuickServer ID number
   */
  async getQsChangeTimezone(params: IGetQsChangeTimezoneParams): Promise<Array<string>> {
    // Verify required parameters are set
    this.ensureParamIsSet('getQsChangeTimezone', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/change_timezone`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Webuzo Change Pass Info
   * Retrieves instructions or metadata for changing the Webuzo control panel password.
   * @param params.id QuickServer ID number
   */
  async getQsChangeWebuzoPassword(params: IGetQsChangeWebuzoPasswordParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('getQsChangeWebuzoPassword', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/change_webuzo_password`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get QuickServer Order
   * Returns detailed QuickServer information, including credentials, IPs, and available client actions.
   * @param params.id QuickServer ID number.
   */
  async getQsInfo(params: IGetQsInfoParams): Promise<Quickserver> {
    // Verify required parameters are set
    this.ensureParamIsSet('getQsInfo', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Insert CD Information
   * Returns available ISO images that can be mounted in the QuickServer\&#39;s virtual CD drive.
   * @param params.id QuickServer ID number
   */
  async getQsInsertCd(params: IGetQsInsertCdParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('getQsInsertCd', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/insert_cd`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get QuickServer Invoices
   * Returns the billing invoices associated with this QuickServer.
   * @param params.id QuickServer ID number
   */
  async getQsInvoices(params: IGetQsInvoicesParams): Promise<ChargeInvoiceRows> {
    // Verify required parameters are set
    this.ensureParamIsSet('getQsInvoices', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/invoices`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List QuickServers
   * Returns the QuickServer services on your account. Use the &#x60;qs_id&#x60; values with &#x60;/qs/{id}&#x60; for details or with the action endpoints (restart, backup, etc.) to manage each server.
   */
  async getQsList(): Promise<Array<QuickserverRow>> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/qs`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * QuickServer Reinstall OS Options
   * Returns the list of available operating system templates for reinstalling the QuickServer.
   * @param params.id QuickServer ID number
   */
  async getQsReinstallOs(params: IGetQsReinstallOsParams): Promise<VpsTemplatesList> {
    // Verify required parameters are set
    this.ensureParamIsSet('getQsReinstallOs', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/reinstall_os`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Reset QuickServer Password Info
   * Returns information needed before resetting the QuickServer\&#39;s root password.
   * @param params.id QuickServer ID number
   */
  async getQsResetPassword(params: IGetQsResetPasswordParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('getQsResetPassword', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/reset_password`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Reverse DNS Info
   * Returns the current reverse DNS (PTR record) entries for the QuickServer\&#39;s IP addresses.
   * @param params.id QuickServer ID number
   */
  async getQsReverseDns(params: IGetQsReverseDnsParams): Promise<ReverseDnsEntries> {
    // Verify required parameters are set
    this.ensureParamIsSet('getQsReverseDns', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/reverse_dns`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * VNC Setup Info
   * Returns the current VNC connection information for the QuickServer.
   * @param params.id QuickServer ID number
   */
  async getQsSetupVnc(params: IGetQsSetupVncParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('getQsSetupVnc', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/setup_vnc`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Traffic Usage
   * Returns bandwidth traffic usage data for the QuickServer.
   * @param params.id QuickServer ID number
   */
  async getQsTrafficUsage(params: IGetQsTrafficUsageParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('getQsTrafficUsage', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/traffic_usage`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get View Desktop Info
   * Returns remote desktop connection information for the QuickServer.
   * @param params.id QuickServer ID number
   */
  async getQsViewDesktop(params: IGetQsViewDesktopParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('getQsViewDesktop', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/view_desktop`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Resend QuickServer Welcome Email
   * Resends the welcome email containing connection details and credentials for the QuickServer order.
   * @param params.id Quickserver ID
   */
  async getQsWelcomeEmail(params: IGetQsWelcomeEmailParams): Promise<TextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('getQsWelcomeEmail', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/welcome_email`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Create QuickServer Backup
   * Creates a backup of the QuickServer. The backup can be downloaded or restored later via the backups endpoints.
   * @param params.id QuickServer ID number
   */
  async postQsBackup(params: IPostQsBackupParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postQsBackup', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/backup`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update QuickServer Hostname
   * Submits a hostname change request for the QuickServer.
   * @param params.id QuickServer ID number
   */
  async postQsChangeHostname(params: IPostQsChangeHostnameParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postQsChangeHostname', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/change_hostname`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Change Root Password
   * Triggers a root password reset for the QuickServer.
   * @param params.id QuickServer ID number
   */
  async postQsChangeRootPassword(params: IPostQsChangeRootPasswordParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postQsChangeRootPassword', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/change_root_password`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Change QuickServer Timezone
   * Changes the system timezone on the QuickServer. Use &#x60;GET /qs/{id}/change_timezone&#x60; to list available options first.
   * @param params.id QuickServer ID number
   * @param params.timezone The time zone
   */
  async postQsChangeTimezone(params: IPostQsChangeTimezoneParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postQsChangeTimezone', params, 'id');
    this.ensureParamIsSet('postQsChangeTimezone', params, 'timezone');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/change_timezone`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode form parameters
      .withHeader('content-type', 'application/x-www-form-urlencoded')
      .withContent(this.queryString({ 
        'timezone': params['timezone'],
      }))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Change Webuzo Password
   * Resets the Webuzo control panel password for the QuickServer.
   * @param params.id QuickServer ID number
   */
  async postQsChangeWebuzoPassword(params: IPostQsChangeWebuzoPasswordParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postQsChangeWebuzoPassword', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/change_webuzo_password`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Insert CD in QuickServer
   * Mounts an ISO image in the QuickServer\&#39;s virtual CD drive. Use &#x60;GET /qs/{id}/insert_cd&#x60; to list available images.
   * @param params.id QuickServer ID number
   */
  async postQsInsertCd(params: IPostQsInsertCdParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postQsInsertCd', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/insert_cd`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Reinstall QuickServer OS
   * Reinstalls the operating system on the QuickServer. Warning - this will erase all data on the server.
   * @param params.id QuickServer ID number
   */
  async postQsReinstallOs(params: IPostQsReinstallOsParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postQsReinstallOs', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/reinstall_os`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Reset QuickServer Password
   * Resets the root password on the QuickServer to a new randomly generated password.
   * @param params.id QuickServer ID number
   */
  async postQsResetPassword(params: IPostQsResetPasswordParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postQsResetPassword', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/reset_password`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update Reverse DNS
   * Updates the reverse DNS (PTR record) entries for the QuickServer\&#39;s IP addresses.
   * @param params.id QuickServer ID number
   * @param params.reverseDnsEntries 
   */
  async postQsReverseDns(params: IPostQsReverseDnsParams): Promise<TextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postQsReverseDns', params, 'id');
    this.ensureParamIsSet('postQsReverseDns', params, 'reverseDnsEntries');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/reverse_dns`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['reverseDnsEntries'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Setup VNC
   * Sets up or refreshes the VNC console connection for the QuickServer.
   * @param params.id QuickServer ID number
   */
  async postQsSetupVnc(params: IPostQsSetupVncParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postQsSetupVnc', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/setup_vnc`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Search Traffic Usage
   * Searches and filters the QuickServer\&#39;s bandwidth traffic usage data by date range.
   * @param params.id QuickServer ID number
   */
  async postQsTrafficUsage(params: IPostQsTrafficUsageParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('postQsTrafficUsage', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/traffic_usage`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update View Desktop
   * Updates or refreshes the remote desktop session for the QuickServer.
   * @param params.id QuickServer ID number
   */
  async postQsViewDesktop(params: IPostQsViewDesktopParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('postQsViewDesktop', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/view_desktop`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Restore QuickServer from Backup
   * Initiates a restore of the QuickServer from a previously created backup. The restore operation overwrites the current disk contents. Use &#x60;GET /qs/{id}/backups&#x60; to retrieve available backup names.
   * @param params.id QuickServer ID number
   * @param params.restoreRequest QuickServer Restore request
   */
  async postQuickServerRestore(params: IPostQuickServerRestoreParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postQuickServerRestore', params, 'id');
    this.ensureParamIsSet('postQuickServerRestore', params, 'restoreRequest');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}/restore`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['restoreRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Validate QuickServer Order
   * Validates a QuickServer order and returns pricing or validation errors. Use this before submitting the final order.
   */
  async putQs(): Promise<any> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/qs/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPut()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Cancel QuickServer Order
   * Cancels the QuickServer service. The server will be deprovisioned and billing will stop at the end of the current billing cycle.
   * @param params.id QuickServer ID number
   */
  async quickserversCancel(params: IQuickserversCancelParams): Promise<QuickserversCancel200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('quickserversCancel', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update QuickServer Order
   * Updates QuickServer metadata or stored settings associated with the order.
   * @param params.id QuickServer ID number.
   */
  async updateQsInfo(params: IUpdateQsInfoParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateQsInfo', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/qs/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

