/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  TicketNew,
  ViewTicketResponse,
  ReplyTicketResponseSchema,
  TicketNewResponse,
  CloseTicketResponseSchema,
  UpdateTicket,
  Tickets,
  ReplyTicketRequest,
  UpdateTicketResponseSchema,
} from './models';

/**
 * addNewTicket - parameters interface
 */
export interface IAddNewTicketParams {
  ticketNew: TicketNew;
}

/**
 * closeTicket - parameters interface
 */
export interface ICloseTicketParams {
  id: string;
}

/**
 * deleteTicketInfo - parameters interface
 */
export interface IDeleteTicketInfoParams {
  id: number;
}

/**
 * getNewTicket - parameters interface
 */
export interface IGetNewTicketParams {
}

/**
 * getTicketInfo - parameters interface
 */
export interface IGetTicketInfoParams {
  id: number;
}

/**
 * getTicketsList - parameters interface
 */
export interface IGetTicketsListParams {
  page?: number;
  period?: '30' | '90' | '365' | '1825' | 'all';
  view?: 'Open' | 'Closed' | 'On Hold' | 'In Progress';
}

/**
 * postTicketInfo - parameters interface
 */
export interface IPostTicketInfoParams {
  id: number;
}

/**
 * postTicketsList - parameters interface
 */
export interface IPostTicketsListParams {
}

/**
 * putTicketInfo - parameters interface
 */
export interface IPutTicketInfoParams {
  id: number;
}

/**
 * replyTicket - parameters interface
 */
export interface IReplyTicketParams {
  id: number;
  replyTicketRequest?: ReplyTicketRequest;
}

/**
 * updateTicketInfo - parameters interface
 */
export interface IUpdateTicketInfoParams {
  id: number;
  updateTicket?: UpdateTicket;
}

/**
 * TicketsApi - API class
 */
@autoinject()
export class TicketsApi extends Api {

  /**
   * Creates a new TicketsApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Create New Ticket
   * Creates a new support ticket. Optionally link it to a specific service by providing the service ID and module.
   * @param params.ticketNew 
   */
  async addNewTicket(params: IAddNewTicketParams): Promise<TicketNewResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('addNewTicket', params, 'ticketNew');

    // Create URL to call
    const url = `${this.basePath}/tickets/new`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['ticketNew'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Close Ticket
   * Closes the specified support ticket. Closed tickets can still be viewed but will no longer appear in the active inbox.
   * @param params.id Ticket ID
   */
  async closeTicket(params: ICloseTicketParams): Promise<CloseTicketResponseSchema> {
    // Verify required parameters are set
    this.ensureParamIsSet('closeTicket', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/tickets/{id}/close`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Close Ticket
   * Closes the support ticket.
   * @param params.id Ticket ID number.
   */
  async deleteTicketInfo(params: IDeleteTicketInfoParams): Promise<ViewTicketResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteTicketInfo', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/tickets/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Gets Information for creating a new ticket.
   * Returns the form data needed to create a new support ticket, such as available departments and service categories.
   */
  async getNewTicket(): Promise<any> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/tickets/new`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Ticket Information
   * Returns the full details of a support ticket including its history of replies.
   * @param params.id Ticket ID number.
   */
  async getTicketInfo(params: IGetTicketInfoParams): Promise<ViewTicketResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('getTicketInfo', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/tickets/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List Support Tickets
   * Returns a paginated list of support tickets on the account. Filter by status and time period.
   * @param params.page Page number for paginated results.
   * @param params.period How far back to show tickets from. Value is in days.
   * @param params.view The status of tickets to view. Possible values are Open, Closed, On Hold, and In Progress.  If not specified it will show all types.
   */
  async getTicketsList(params: IGetTicketsListParams): Promise<Tickets> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/tickets`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'page': params['page'],
        'period': params['period'],
        'view': params['view'],
      })

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Reply To Ticket
   * Adds a reply to an existing support ticket.
   * @param params.id Ticket ID number.
   */
  async postTicketInfo(params: IPostTicketInfoParams): Promise<ViewTicketResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postTicketInfo', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/tickets/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Search Support Tickets
   * Searches support tickets by email, subject, or ticket mask ID.
   */
  async postTicketsList(): Promise<Tickets> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/tickets`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update Ticket
   * Updates a support ticket\&#39;s properties such as subject or status.
   * @param params.id Ticket ID number.
   */
  async putTicketInfo(params: IPutTicketInfoParams): Promise<ViewTicketResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('putTicketInfo', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/tickets/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPut()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Reply Ticket
   * Posts a reply to an existing support ticket thread.
   * @param params.id The ticket ID number.
   * @param params.replyTicketRequest 
   */
  async replyTicket(params: IReplyTicketParams): Promise<ReplyTicketResponseSchema> {
    // Verify required parameters are set
    this.ensureParamIsSet('replyTicket', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/tickets/{id}/reply`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['replyTicketRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update Ticket
   * Updates a support ticket\&#39;s subject or body content.
   * @param params.id The ticket ID number.
   * @param params.updateTicket 
   */
  async updateTicketInfo(params: IUpdateTicketInfoParams): Promise<UpdateTicketResponseSchema> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateTicketInfo', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/tickets/{id}/update`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['updateTicket'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

