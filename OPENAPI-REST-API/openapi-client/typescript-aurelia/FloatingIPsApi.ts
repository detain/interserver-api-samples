/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  ChargeInvoiceRows,
  SuccessTextResponse,
  ServiceOrderPostResponse,
  FloatingIpsCancel200Response,
} from './models';

/**
 * addFloatingIp - parameters interface
 */
export interface IAddFloatingIpParams {
}

/**
 * floatingIpsCancel - parameters interface
 */
export interface IFloatingIpsCancelParams {
  id: number;
}

/**
 * getFloatingIpInfo - parameters interface
 */
export interface IGetFloatingIpInfoParams {
  id: number;
}

/**
 * getFloatingIpInvoices - parameters interface
 */
export interface IGetFloatingIpInvoicesParams {
  id: number;
}

/**
 * getFloatingIpsList - parameters interface
 */
export interface IGetFloatingIpsListParams {
}

/**
 * getFloatingIpsWelcomeEmail - parameters interface
 */
export interface IGetFloatingIpsWelcomeEmailParams {
  id: number;
}

/**
 * getNewFloatingIp - parameters interface
 */
export interface IGetNewFloatingIpParams {
}

/**
 * postFloatingIpsChangeIp - parameters interface
 */
export interface IPostFloatingIpsChangeIpParams {
  id: number;
  ip: string;
}

/**
 * putFloatingIps - parameters interface
 */
export interface IPutFloatingIpsParams {
}

/**
 * updateFloatingIpInfo - parameters interface
 */
export interface IUpdateFloatingIpInfoParams {
  id: string;
}

/**
 * FloatingIPsApi - API class
 */
@autoinject()
export class FloatingIPsApi extends Api {

  /**
   * Creates a new FloatingIPsApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Place Floating IP Order
   * Places an order for a new Floating IP service. Use &#x60;PUT /floating_ips/order&#x60; to validate the order first.
   */
  async addFloatingIp(): Promise<ServiceOrderPostResponse> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/floating_ips/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Cancel Floating IP
   * Cancels a Floating IP service. After cancellation the IP assignment is released and the service transitions to a canceled status. No further billing charges will be incurred.
   * @param params.id The Floating IP service ID. Use the ID from &#x60;GET /floating_ips&#x60;.
   */
  async floatingIpsCancel(params: IFloatingIpsCancelParams): Promise<FloatingIpsCancel200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('floatingIpsCancel', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/floating_ips/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * View Floating IP
   * Returns detailed information about a specific Floating IP service including its current target IP assignment.
   * @param params.id The Floating IP service ID. Use the ID from &#x60;GET /floating_ips&#x60;.
   */
  async getFloatingIpInfo(params: IGetFloatingIpInfoParams): Promise<object> {
    // Verify required parameters are set
    this.ensureParamIsSet('getFloatingIpInfo', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/floating_ips/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Floating IP Invoices
   * Returns the billing invoices associated with this Floating IP service.
   * @param params.id The Floating IP service ID. Use the ID from &#x60;GET /floating_ips&#x60;.
   */
  async getFloatingIpInvoices(params: IGetFloatingIpInvoicesParams): Promise<ChargeInvoiceRows> {
    // Verify required parameters are set
    this.ensureParamIsSet('getFloatingIpInvoices', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/floating_ips/{id}/invoices`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List Floating IPs
   * Returns all Floating IP services on the account with their current status and assignment details.
   */
  async getFloatingIpsList(): Promise<Array<object>> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/floating_ips`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Resend Floating IPs Welcome Email
   * Resends the welcome email for the Floating IP service. The email contains setup instructions and connection details.
   * @param params.id The Floating IP service ID. Use the ID from &#x60;GET /floating_ips&#x60;.
   */
  async getFloatingIpsWelcomeEmail(params: IGetFloatingIpsWelcomeEmailParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('getFloatingIpsWelcomeEmail', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/floating_ips/{id}/welcome_email`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Floating IP Ordering Information
   * Retrieves available options and pricing for ordering a new Floating IP.
   */
  async getNewFloatingIp(): Promise<object> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/floating_ips/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Change Floating IP Target
   * Changes the target IP address that the Floating IP points to. The Floating IP service must be active. Use &#x60;GET /floating_ips/{id}&#x60; to view the current target before making changes.
   * @param params.id The Floating IP service ID. Use the ID from &#x60;GET /floating_ips&#x60;.
   * @param params.ip IP Address
   */
  async postFloatingIpsChangeIp(params: IPostFloatingIpsChangeIpParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postFloatingIpsChangeIp', params, 'id');
    this.ensureParamIsSet('postFloatingIpsChangeIp', params, 'ip');

    // Create URL to call
    const url = `${this.basePath}/floating_ips/{id}/change_ip`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode form parameters
      .withHeader('content-type', 'application/x-www-form-urlencoded')
      .withContent(this.queryString({ 
        'ip': params['ip'],
      }))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Validate Floating IP Order
   * Validates a Floating IP order before placing it. Use this to check for errors before committing to a purchase.
   */
  async putFloatingIps(): Promise<any> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/floating_ips/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPut()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update Floating IP
   * Updates settings on a Floating IP service, such as its label or configuration metadata.
   * @param params.id The Floating IP service ID. Use the ID from &#x60;GET /floating_ips&#x60;.
   */
  async updateFloatingIpInfo(params: IUpdateFloatingIpInfoParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateFloatingIpInfo', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/floating_ips/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

