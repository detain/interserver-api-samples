/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  InitiatePayment200Response,
  Invoice,
  BillingPaymentMethodRequest,
  BillingInvoiceDetail,
  SuccessTextResponse,
  TextResponse,
  AffiliateBannerRow,
  MonthlyCounts,
  BillingVerifyCcRequest,
  BillingInvoiceList,
  StatusMonthlyBreakdown,
  BillingAddCcRequest,
  BillingPrepayRequest,
  AffiliateTrafficRow,
} from './models';

/**
 * addAccountCreditCard - parameters interface
 */
export interface IAddAccountCreditCardParams {
  name?: string;
  address?: string;
  city?: string;
  state?: string;
  country?: string;
  zip?: string;
  cc?: string;
  ccExp?: string;
  ccCcv2?: string;
}

/**
 * addBillingCreditCard - parameters interface
 */
export interface IAddBillingCreditCardParams {
  billingAddCcRequest: BillingAddCcRequest;
}

/**
 * addBillingPrepay - parameters interface
 */
export interface IAddBillingPrepayParams {
  billingPrepayRequest: BillingPrepayRequest;
}

/**
 * deleteAccountCreditCard - parameters interface
 */
export interface IDeleteAccountCreditCardParams {
  id: string;
}

/**
 * deleteBillingCreditCard - parameters interface
 */
export interface IDeleteBillingCreditCardParams {
  id: number;
}

/**
 * deleteBillingInvoice - parameters interface
 */
export interface IDeleteBillingInvoiceParams {
  id: number;
}

/**
 * deleteBillingPrepay - parameters interface
 */
export interface IDeleteBillingPrepayParams {
  id: number;
}

/**
 * getAffiliateBanners - parameters interface
 */
export interface IGetAffiliateBannersParams {
}

/**
 * getAffiliateRichReport - parameters interface
 */
export interface IGetAffiliateRichReportParams {
}

/**
 * getAffiliateSalesGraph - parameters interface
 */
export interface IGetAffiliateSalesGraphParams {
  days?: number;
}

/**
 * getAffiliateSalesReport - parameters interface
 */
export interface IGetAffiliateSalesReportParams {
}

/**
 * getAffiliateTrafficGraph - parameters interface
 */
export interface IGetAffiliateTrafficGraphParams {
  days?: number;
}

/**
 * getAffiliateWebTraffic - parameters interface
 */
export interface IGetAffiliateWebTrafficParams {
}

/**
 * getBillingCart - parameters interface
 */
export interface IGetBillingCartParams {
}

/**
 * getBillingCreditCardVerify - parameters interface
 */
export interface IGetBillingCreditCardVerifyParams {
  id: number;
}

/**
 * getBillingInvoice - parameters interface
 */
export interface IGetBillingInvoiceParams {
  id: number;
}

/**
 * getBillingInvoices - parameters interface
 */
export interface IGetBillingInvoicesParams {
}

/**
 * getBillingPrePays - parameters interface
 */
export interface IGetBillingPrePaysParams {
}

/**
 * getInvoices - parameters interface
 */
export interface IGetInvoicesParams {
  searchString?: string;
  skip?: number;
  limit?: number;
}

/**
 * initiatePayment - parameters interface
 */
export interface IInitiatePaymentParams {
  method: 'cc' | 'paypal' | 'prepay' | 'payssion' | 'payu' | 'ccavenue' | 'cashfree' | 'coinbase' | 'btcpay';
  invoices: string;
}

/**
 * postBillingCreditCardVerify - parameters interface
 */
export interface IPostBillingCreditCardVerifyParams {
  id: number;
  billingVerifyCcRequest: BillingVerifyCcRequest;
}

/**
 * updateAccountCreditCard - parameters interface
 */
export interface IUpdateAccountCreditCardParams {
  id: number;
}

/**
 * updateAffiliateDockSetup - parameters interface
 */
export interface IUpdateAffiliateDockSetupParams {
  affiliateDockTitle?: string;
  affiliateDockDescription?: string;
  referrerCoupon?: string;
}

/**
 * updateAffiliateLandingPage - parameters interface
 */
export interface IUpdateAffiliateLandingPageParams {
  affiliateDockTitle?: string;
  affiliateDockDescription?: string;
  referrerCoupon?: string;
}

/**
 * updateAffiliatePaymentSetup - parameters interface
 */
export interface IUpdateAffiliatePaymentSetupParams {
  affiliatePaypal?: string;
  affiliatePaymentMethod?: string;
}

/**
 * updateBillingCreditCard - parameters interface
 */
export interface IUpdateBillingCreditCardParams {
  id: number;
}

/**
 * updateBillingPaymentMethod - parameters interface
 */
export interface IUpdateBillingPaymentMethodParams {
  billingPaymentMethodRequest: BillingPaymentMethodRequest;
}

/**
 * BillingApi - API class
 */
@autoinject()
export class BillingApi extends Api {

  /**
   * Creates a new BillingApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Add Credit Card to Account
   * Adds a new credit card to the account for billing. Card details are validated and stored securely. The card may require verification before it can be used as a payment method.
   * @param params.name 
   * @param params.address 
   * @param params.city 
   * @param params.state 
   * @param params.country 
   * @param params.zip 
   * @param params.cc 
   * @param params.ccExp 
   * @param params.ccCcv2 
   */
  async addAccountCreditCard(params: IAddAccountCreditCardParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/account/creditcards`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode form parameters
      .withHeader('content-type', 'application/x-www-form-urlencoded')
      .withContent(this.queryString({ 
        'name': params['name'],
        'address': params['address'],
        'city': params['city'],
        'state': params['state'],
        'country': params['country'],
        'zip': params['zip'],
        'cc': params['cc'],
        'cc_exp': params['ccExp'],
        'cc_ccv2': params['ccCcv2'],
      }))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Add Credit Card for Billing
   * Adds a new credit card to the account for use as a payment method. If the card requires verification, the response indicates the next step. Complete verification via &#x60;GET /billing/creditcards/{id}/verify&#x60; followed by &#x60;POST /billing/creditcards/{id}/verify&#x60; before the card can be used for payments.
   * @param params.billingAddCcRequest 
   */
  async addBillingCreditCard(params: IAddBillingCreditCardParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('addBillingCreditCard', params, 'billingAddCcRequest');

    // Create URL to call
    const url = `${this.basePath}/billing/creditcards`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['billingAddCcRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Create Prepay Deposit
   * Creates a new prepay balance deposit and returns the invoice ID that must be paid to activate it. The prepay balance can then be used as a payment method for future orders. Use &#x60;/billing/invoices/{id}&#x60; to retrieve the generated invoice details.
   * @param params.billingPrepayRequest 
   */
  async addBillingPrepay(params: IAddBillingPrepayParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('addBillingPrepay', params, 'billingPrepayRequest');

    // Create URL to call
    const url = `${this.basePath}/billing/prepays`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['billingPrepayRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Remove Credit Card
   * Removes a credit card from the account. If this is the default payment method, select a new default via &#x60;/billing/payment_method&#x60; afterward.
   * @param params.id The credit card ID. Use the card ID returned from &#x60;POST /account/creditcards&#x60; or listed in &#x60;/billing/creditcards&#x60;.
   */
  async deleteAccountCreditCard(params: IDeleteAccountCreditCardParams): Promise<string> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteAccountCreditCard', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/account/creditcards/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delete Credit Card
   * Removes the selected credit card from the account. Use &#x60;/billing/payment_method&#x60; to select a new default payment method after deleting a card.
   * @param params.id The credit card ID to remove. Use IDs from &#x60;GET /billing/creditcards&#x60;.
   */
  async deleteBillingCreditCard(params: IDeleteBillingCreditCardParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteBillingCreditCard', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/billing/creditcards/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delete Invoice
   * Deletes a pending (unpaid) invoice from the account. Only invoices with a pending payment status can be deleted. Paid invoices cannot be removed. Related service records and repeat invoices are also cleaned up.
   * @param params.id The invoice ID to delete. Only unpaid invoices can be deleted.
   */
  async deleteBillingInvoice(params: IDeleteBillingInvoiceParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteBillingInvoice', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/billing/invoices/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delete Prepay Balance
   * Deletes a prepay balance from the account. The balance must have remaining funds to be eligible for deletion. Use &#x60;GET /billing/prepays&#x60; to list available prepay balances and their IDs.
   * @param params.id The prepay balance ID to delete.
   */
  async deleteBillingPrepay(params: IDeleteBillingPrepayParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteBillingPrepay', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/billing/prepays/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List Affiliate Banner Assets
   * Returns the catalog of available affiliate banner images with their dimensions. Use these assets to build marketing creatives for your affiliate campaigns. Each banner includes the image filename, width, and height for layout purposes.
   */
  async getAffiliateBanners(): Promise<Array<AffiliateBannerRow>> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/affiliate/banners`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Affiliate Performance Report
   * Returns a detailed affiliate performance report with commission totals, conversion rates, and traffic summary. Use this for a comprehensive overview of your affiliate program performance in a single request.
   */
  async getAffiliateRichReport(): Promise<TextResponse> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/affiliate/rich_report`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Affiliate Sales Graph Data
   * Returns time-series sales data for the requested number of days. Use this to render sales trend charts in an affiliate dashboard. Each data point represents aggregated sales for a time period.
   * @param params.days Number of days of sales history to include in the graph data. Determines the time window for the returned data points.
   */
  async getAffiliateSalesGraph(params: IGetAffiliateSalesGraphParams): Promise<StatusMonthlyBreakdown> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/affiliate/sales_graph`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'days': params['days'],
      })

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Affiliate Sales Report
   * Returns the affiliate sales report with commission amounts and order summaries. Use this for tabular sales data export or to reconcile commission payouts against individual referral orders.
   */
  async getAffiliateSalesReport(): Promise<TextResponse> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/affiliate/sales_report`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Affiliate Traffic Graph Data
   * Returns time-series traffic data for the requested number of days. Use this to render click and visit trend charts in an affiliate dashboard. Each data point represents aggregated traffic counts for a time period.
   * @param params.days Number of days of traffic history to include in the graph data. Determines the time window for the returned data points.
   */
  async getAffiliateTrafficGraph(params: IGetAffiliateTrafficGraphParams): Promise<MonthlyCounts> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/affiliate/traffic_graph`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'days': params['days'],
      })

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List Affiliate Web Traffic Entries
   * Returns individual web traffic log entries for affiliate referrals, including visitor IP address, referral URL, and timestamp. Use this to audit traffic sources, identify top referrers, or investigate suspicious click patterns.
   */
  async getAffiliateWebTraffic(): Promise<Array<AffiliateTrafficRow>> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/affiliate/web_traffic`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Shopping Cart Contents
   * Returns the current cart contents, available payment methods, and checkout metadata for the authenticated account. Use this to display the cart page, show totals, and determine which payment options are available before directing the user to &#x60;/pay/{method}/{invoices}&#x60;.
   */
  async getBillingCart(): Promise<object> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/billing/cart`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Credit Card Verification Requirements
   * Retrieves the verification requirements for a newly added credit card. The response indicates whether the card requires micro-charge amount confirmation or CVV validation. Use this before presenting a verification form to the user.
   * @param params.id The credit card ID to verify. Use the ID returned from &#x60;POST /billing/creditcards&#x60;.
   */
  async getBillingCreditCardVerify(params: IGetBillingCreditCardVerifyParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('getBillingCreditCardVerify', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/billing/creditcards/{id}/verify`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Invoice Details
   * Retrieves the full invoice information including line items, amounts, and payment status. Use this before redirecting to &#x60;/pay/{method}/{invoices}&#x60; so you can display the exact amount due and confirm the invoice is still unpaid.
   * @param params.id The invoice ID. Use IDs from &#x60;GET /billing/invoices&#x60; or from order responses.
   */
  async getBillingInvoice(params: IGetBillingInvoiceParams): Promise<BillingInvoiceDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('getBillingInvoice', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/billing/invoices/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List Account Invoices
   * Returns the invoice list for the account with summary totals. Use the invoice IDs from the response with &#x60;/billing/invoices/{id}&#x60; to retrieve detailed line items, or with &#x60;/pay/{method}/{invoices}&#x60; to initiate payment.
   */
  async getBillingInvoices(): Promise<BillingInvoiceList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/billing/invoices`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List Prepay Balances
   * Lists prepay balances and their associated metadata. Use this to determine whether an account has usable prepay funds before selecting &#x60;prepay&#x60; as a payment method.
   */
  async getBillingPrePays(): Promise<object> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/billing/prepays`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Invoices
   * Returns a paginated list of invoices for the authenticated account. Each invoice includes the invoice number, date, total amount, and payment status. Use the optional &#x60;searchString&#x60; parameter to filter results and &#x60;skip&#x60;/&#x60;limit&#x60; for pagination.
   * @param params.searchString pass an optional search string for looking up inventory
   * @param params.skip number of records to skip for pagination
   * @param params.limit maximum number of records to return
   */
  async getInvoices(params: IGetInvoicesParams): Promise<Array<Invoice>> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/invoices`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'searchString': params['searchString'],
        'skip': params['skip'],
        'limit': params['limit'],
      })

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Initiate Payment
   * Initiates a payment for the specified invoices using the chosen payment method. The response type determines how your client should proceed: &#x60;redirect&#x60; means send the user to the provided URL, &#x60;submit&#x60; means POST a form with the provided fields, and &#x60;single&#x60; means the payment was processed immediately. Use invoice IDs obtained from order responses or &#x60;/billing/invoices&#x60;.
   * @param params.method The payment method to use. Valid values: &#x60;cc&#x60; (credit card), &#x60;paypal&#x60;, &#x60;prepay&#x60;, &#x60;payssion&#x60;, &#x60;payu&#x60;, &#x60;ccavenue&#x60;, &#x60;cashfree&#x60;, &#x60;coinbase&#x60;, &#x60;btcpay&#x60;.
   * @param params.invoices A comma-separated list of invoice IDs to pay. These IDs are returned by order endpoints (e.g. &#x60;/backups/order&#x60;, &#x60;/vps/order&#x60;) and by &#x60;/billing/invoices&#x60;.
   */
  async initiatePayment(params: IInitiatePaymentParams): Promise<InitiatePayment200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('initiatePayment', params, 'method');
    this.ensureParamIsSet('initiatePayment', params, 'invoices');

    // Create URL to call
    const url = `${this.basePath}/pay/{method}/{invoices}`
      .replace(`{${'method'}}`, encodeURIComponent(`${params['method']}`))
      .replace(`{${'invoices'}}`, encodeURIComponent(`${params['invoices']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Submit Credit Card Verification
   * Completes the credit card verification flow by submitting the micro-charge amounts or CVV as required by &#x60;GET /billing/creditcards/{id}/verify&#x60;. A successful response means the card is verified and can be selected as a payment method via &#x60;/billing/payment_method&#x60;.
   * @param params.id The credit card ID to verify. Use the ID returned from &#x60;POST /billing/creditcards&#x60;.
   * @param params.billingVerifyCcRequest 
   */
  async postBillingCreditCardVerify(params: IPostBillingCreditCardVerifyParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postBillingCreditCardVerify', params, 'id');
    this.ensureParamIsSet('postBillingCreditCardVerify', params, 'billingVerifyCcRequest');

    // Create URL to call
    const url = `${this.basePath}/billing/creditcards/{id}/verify`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['billingVerifyCcRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update Credit Card
   * Updates an existing credit card on the account. Use this to refresh stored card metadata such as expiration date or billing address.
   * @param params.id The credit card ID. Use the card ID returned from &#x60;POST /account/creditcards&#x60; or listed in &#x60;/billing/creditcards&#x60;.
   */
  async updateAccountCreditCard(params: IUpdateAccountCreditCardParams): Promise<string> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateAccountCreditCard', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/account/creditcards/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Configure Affiliate Dock Settings
   * Updates the affiliate dock settings including the referral coupon and marketing copy. The dock is the branded landing page shown to visitors arriving via your affiliate link. Use this to customize the coupon code and promotional text.
   * @param params.affiliateDockTitle 
   * @param params.affiliateDockDescription 
   * @param params.referrerCoupon 
   */
  async updateAffiliateDockSetup(params: IUpdateAffiliateDockSetupParams): Promise<TextResponse> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/affiliate/dock_setup`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode form parameters
      .withHeader('content-type', 'application/x-www-form-urlencoded')
      .withContent(this.queryString({ 
        'affiliate_dock_title': params['affiliateDockTitle'],
        'affiliate_dock_description': params['affiliateDockDescription'],
        'referrer_coupon': params['referrerCoupon'],
      }))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Configure Affiliate Landing Page
   * Updates the affiliate landing page configuration, including the title, description, and coupon code. Visitors who arrive through your affiliate link see this customized page. Changes are published immediately.
   * @param params.affiliateDockTitle 
   * @param params.affiliateDockDescription 
   * @param params.referrerCoupon 
   */
  async updateAffiliateLandingPage(params: IUpdateAffiliateLandingPageParams): Promise<TextResponse> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/affiliate/landing_pg`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode form parameters
      .withHeader('content-type', 'application/x-www-form-urlencoded')
      .withContent(this.queryString({ 
        'affiliate_dock_title': params['affiliateDockTitle'],
        'affiliate_dock_description': params['affiliateDockDescription'],
        'referrer_coupon': params['referrerCoupon'],
      }))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Configure Affiliate Payout Preferences
   * Updates how you receive affiliate commission payouts. Choose between prepay credit applied to your account balance or PayPal disbursement. When selecting PayPal, provide the email address linked to your PayPal account.
   * @param params.affiliatePaypal 
   * @param params.affiliatePaymentMethod 
   */
  async updateAffiliatePaymentSetup(params: IUpdateAffiliatePaymentSetupParams): Promise<TextResponse> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/affiliate/payment_setup`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode form parameters
      .withHeader('content-type', 'application/x-www-form-urlencoded')
      .withContent(this.queryString({ 
        'affiliate_paypal': params['affiliatePaypal'],
        'affiliate_payment_method': params['affiliatePaymentMethod'],
      }))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update Credit Card Details
   * Updates stored credit card metadata or retrieves the masked card details. Use this to refresh card data before verification or to update billing address information associated with the card.
   * @param params.id The credit card ID. Use IDs from &#x60;GET /billing/creditcards&#x60; or the response from &#x60;POST /billing/creditcards&#x60;.
   */
  async updateBillingCreditCard(params: IUpdateBillingCreditCardParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateBillingCreditCard', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/billing/creditcards/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update Default Payment Method
   * Updates the account\&#39;s default payment method, including selecting a verified credit card as the primary payment source or switching to PayPal when available.
   * @param params.billingPaymentMethodRequest 
   */
  async updateBillingPaymentMethod(params: IUpdateBillingPaymentMethodParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateBillingPaymentMethod', params, 'billingPaymentMethodRequest');

    // Create URL to call
    const url = `${this.basePath}/billing/payment_method`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['billingPaymentMethodRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

