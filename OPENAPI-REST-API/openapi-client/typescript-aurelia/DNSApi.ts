/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  DnsRecordType,
  DnsRecord,
  DnsListItem,
} from './models';

/**
 * addDnsDomain - parameters interface
 */
export interface IAddDnsDomainParams {
  domain: string;
  ip: string;
}

/**
 * addDnsRecord - parameters interface
 */
export interface IAddDnsRecordParams {
  id: string;
  name: string;
  type: DnsRecordType;
  content: string;
  ttl?: number;
  prio?: number;
}

/**
 * deleteDnsDomain - parameters interface
 */
export interface IDeleteDnsDomainParams {
  id: string;
}

/**
 * deleteDnsRecord - parameters interface
 */
export interface IDeleteDnsRecordParams {
  domainId: number;
  recordId: number;
}

/**
 * getDnsDomain - parameters interface
 */
export interface IGetDnsDomainParams {
  id: number;
}

/**
 * getDnsList - parameters interface
 */
export interface IGetDnsListParams {
}

/**
 * updateDnsRecord - parameters interface
 */
export interface IUpdateDnsRecordParams {
  domainId: number;
  recordId: number;
  name?: string;
  type?: DnsRecordType;
  content?: string;
  ttl?: string;
  prio?: string;
  disabled?: string;
  ordername?: string;
  auth?: string;
}

/**
 * DNSApi - API class
 */
@autoinject()
export class DNSApi extends Api {

  /**
   * Creates a new DNSApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Create DNS Domain
   * Creates a new DNS domain and assigns an initial A record pointing to the supplied IP address. The domain is immediately available on InterServer\&#39;s DNS servers. Use &#x60;/dns/{id}&#x60; to manage records after creation.
   * @param params.domain The domain name.
   * @param params.ip IP Address to point the domain to.
   */
  async addDnsDomain(params: IAddDnsDomainParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('addDnsDomain', params, 'domain');
    this.ensureParamIsSet('addDnsDomain', params, 'ip');

    // Create URL to call
    const url = `${this.basePath}/dns`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode form parameters
      .withHeader('content-type', 'application/x-www-form-urlencoded')
      .withContent(this.queryString({ 
        'domain': params['domain'],
        'ip': params['ip'],
      }))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Add DNS Record to Domain
   * Adds a new DNS record to the specified domain. Provide the record type (A, AAAA, CNAME, MX, TXT, etc.), name, content, TTL, and priority. The record takes effect on the DNS servers immediately. Use &#x60;GET /dns/{id}&#x60; afterward to confirm the record was created.
   * @param params.id The DNS Domain ID.
   * @param params.name Name part of record
   * @param params.type 
   * @param params.content Content of record
   * @param params.ttl Time-to-live
   * @param params.prio Priority
   */
  async addDnsRecord(params: IAddDnsRecordParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('addDnsRecord', params, 'id');
    this.ensureParamIsSet('addDnsRecord', params, 'name');
    this.ensureParamIsSet('addDnsRecord', params, 'type');
    this.ensureParamIsSet('addDnsRecord', params, 'content');

    // Create URL to call
    const url = `${this.basePath}/dns/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode form parameters
      .withHeader('content-type', 'application/x-www-form-urlencoded')
      .withContent(this.queryString({ 
        'name': params['name'],
        'type': params['type'],
        'content': params['content'],
        'ttl': params['ttl'],
        'prio': params['prio'],
      }))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delete DNS Domain
   * Deletes a DNS domain and all of its associated records from the DNS servers. This action is permanent and cannot be undone. Any services relying on these DNS records will be affected immediately.
   * @param params.id The DNS domain ID to delete. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain.
   */
  async deleteDnsDomain(params: IDeleteDnsDomainParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteDnsDomain', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/dns/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delete DNS Record
   * Removes a DNS record from the specified domain. The deletion takes effect on the DNS servers immediately. Use &#x60;GET /dns/{id}&#x60; to verify the record has been removed.
   * @param params.domainId The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain.
   * @param params.recordId The DNS record ID within the domain. Use the record &#x60;id&#x60; from &#x60;GET /dns/{id}&#x60; to identify the record.
   */
  async deleteDnsRecord(params: IDeleteDnsRecordParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteDnsRecord', params, 'domainId');
    this.ensureParamIsSet('deleteDnsRecord', params, 'recordId');

    // Create URL to call
    const url = `${this.basePath}/dns/{domainId}/{recordId}`
      .replace(`{${'domainId'}}`, encodeURIComponent(`${params['domainId']}`))
      .replace(`{${'recordId'}}`, encodeURIComponent(`${params['recordId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List Domain DNS Records
   * Returns the full set of DNS records for the specified domain, including NS, A, AAAA, CNAME, MX, TXT, and other record types. Use the record &#x60;id&#x60; values with &#x60;/dns/{domainId}/{recordId}&#x60; to update or delete individual records.
   * @param params.id The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain.
   */
  async getDnsDomain(params: IGetDnsDomainParams): Promise<Array<DnsRecord>> {
    // Verify required parameters are set
    this.ensureParamIsSet('getDnsDomain', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/dns/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List DNS Domains
   * Returns the DNS domains on your account along with their primary A record content. Use the &#x60;id&#x60; from each entry with &#x60;/dns/{id}&#x60; to retrieve the full record set, or to add, update, and delete individual records.
   */
  async getDnsList(): Promise<Array<DnsListItem>> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/dns`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update DNS Record
   * Updates an existing DNS record with new values. Use &#x60;GET /dns/{id}&#x60; to list records and retrieve the record IDs before updating. Changes propagate to the DNS servers immediately.
   * @param params.domainId The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain.
   * @param params.recordId The DNS record ID within the domain. Use the record &#x60;id&#x60; from &#x60;GET /dns/{id}&#x60; to identify the record.
   * @param params.name 
   * @param params.type 
   * @param params.content 
   * @param params.ttl 
   * @param params.prio 
   * @param params.disabled 
   * @param params.ordername 
   * @param params.auth 
   */
  async updateDnsRecord(params: IUpdateDnsRecordParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateDnsRecord', params, 'domainId');
    this.ensureParamIsSet('updateDnsRecord', params, 'recordId');

    // Create URL to call
    const url = `${this.basePath}/dns/{domainId}/{recordId}`
      .replace(`{${'domainId'}}`, encodeURIComponent(`${params['domainId']}`))
      .replace(`{${'recordId'}}`, encodeURIComponent(`${params['recordId']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode form parameters
      .withHeader('content-type', 'application/x-www-form-urlencoded')
      .withContent(this.queryString({ 
        'name': params['name'],
        'type': params['type'],
        'content': params['content'],
        'ttl': params['ttl'],
        'prio': params['prio'],
        'disabled': params['disabled'],
        'ordername': params['ordername'],
        'auth': params['auth'],
      }))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

