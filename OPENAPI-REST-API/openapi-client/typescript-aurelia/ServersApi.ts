/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  ChargeInvoiceRows,
  PlaceBuyNowServerRequest,
  Server,
  ServerIpmiLiveInfo,
  SuccessTextResponse,
  ServersBuyNowResponse,
  ServersCancel200Response,
  TextResponse,
  ReverseDnsEntries,
  BuyItNowServerOrder200Response,
  BuyItNowList,
  ServerRow,
  ServerOrder,
} from './models';

/**
 * addServer - parameters interface
 */
export interface IAddServerParams {
}

/**
 * buyItNowServerOrder - parameters interface
 */
export interface IBuyItNowServerOrderParams {
}

/**
 * getMPServers - parameters interface
 */
export interface IGetMPServersParams {
}

/**
 * getNewServer - parameters interface
 */
export interface IGetNewServerParams {
}

/**
 * getServerInfo - parameters interface
 */
export interface IGetServerInfoParams {
  id: number;
}

/**
 * getServerInvoices - parameters interface
 */
export interface IGetServerInvoicesParams {
  id: number;
}

/**
 * getServerList - parameters interface
 */
export interface IGetServerListParams {
}

/**
 * getServerReverseDns - parameters interface
 */
export interface IGetServerReverseDnsParams {
  id: number;
}

/**
 * getServersWelcomeEmail - parameters interface
 */
export interface IGetServersWelcomeEmailParams {
  id: number;
}

/**
 * placeBuyNowServer - parameters interface
 */
export interface IPlaceBuyNowServerParams {
  placeBuyNowServerRequest?: PlaceBuyNowServerRequest;
}

/**
 * postServerReverseDns - parameters interface
 */
export interface IPostServerReverseDnsParams {
  id: number;
  reverseDnsEntries: ReverseDnsEntries;
}

/**
 * putServers - parameters interface
 */
export interface IPutServersParams {
}

/**
 * serverIpmiLiveGet - parameters interface
 */
export interface IServerIpmiLiveGetParams {
  id: number;
}

/**
 * serverIpmiLivePost - parameters interface
 */
export interface IServerIpmiLivePostParams {
  id: number;
  ip: string;
  asset?: number;
}

/**
 * serverIpmiPowerGet - parameters interface
 */
export interface IServerIpmiPowerGetParams {
  id: number;
}

/**
 * serverIpmiPowerPost - parameters interface
 */
export interface IServerIpmiPowerPostParams {
  id: number;
  action: string;
  asset?: number;
}

/**
 * serversCancel - parameters interface
 */
export interface IServersCancelParams {
  id: number;
}

/**
 * updateServerInfo - parameters interface
 */
export interface IUpdateServerInfoParams {
  id: string;
}

/**
 * ServersApi - API class
 */
@autoinject()
export class ServersApi extends Api {

  /**
   * Creates a new ServersApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Place Server Order
   * Places an order for a new dedicated server. Use &#x60;PUT /servers/order&#x60; to validate the order first.
   */
  async addServer(): Promise<any> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/servers/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Buy Now Server Options
   * Returns the configuration options and pricing for buy-it-now dedicated servers, including available bandwidth packages, IP blocks, operating systems, control panels, and RAID configurations. Use the returned option IDs when placing an order via &#x60;POST /servers/order/buy_now_server&#x60;.
   */
  async buyItNowServerOrder(): Promise<BuyItNowServerOrder200Response> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/servers/order/buy_now_server`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List Marketplace Servers
   * Returns the list of available Rapid Deploy dedicated servers with current pricing. Each entry includes CPU, memory, disk, bandwidth, IP allocation, and location details. These servers are pre-configured and can be provisioned immediately after purchase.
   */
  async getMPServers(): Promise<BuyItNowList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/buy_now_servers_list`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Server Ordering Information
   * Retrieves available server configurations and pricing for ordering a new dedicated server.
   */
  async getNewServer(): Promise<ServerOrder> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/servers/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Server Order
   * Returns detailed information about a specific server including its hardware configuration, IPs, and status.
   * @param params.id Server ID number.
   */
  async getServerInfo(params: IGetServerInfoParams): Promise<Server> {
    // Verify required parameters are set
    this.ensureParamIsSet('getServerInfo', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/servers/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Server Invoices
   * Returns the billing invoices associated with this dedicated server.
   * @param params.id Server ID number
   */
  async getServerInvoices(params: IGetServerInvoicesParams): Promise<ChargeInvoiceRows> {
    // Verify required parameters are set
    this.ensureParamIsSet('getServerInvoices', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/servers/{id}/invoices`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List Servers
   * Returns all dedicated server services on the account with their current status and configuration.
   */
  async getServerList(): Promise<Array<ServerRow>> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/servers`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Reverse DNS Info
   * Returns the current reverse DNS (PTR record) entries for the server\&#39;s IP addresses.
   * @param params.id Server ID number
   */
  async getServerReverseDns(params: IGetServerReverseDnsParams): Promise<ReverseDnsEntries> {
    // Verify required parameters are set
    this.ensureParamIsSet('getServerReverseDns', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/servers/{id}/reverse_dns`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Resend Server Welcome Email
   * Resends the welcome email for the order.
   * @param params.id Server ID number
   */
  async getServersWelcomeEmail(params: IGetServersWelcomeEmailParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('getServersWelcomeEmail', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/servers/{id}/welcome_email`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Place Buy Now Server Order
   * Places an order for a buy-it-now dedicated server. Use &#x60;GET /servers/order/buy_now_server&#x60; to retrieve available server configurations and their IDs before ordering.
   * @param params.placeBuyNowServerRequest 
   */
  async placeBuyNowServer(params: IPlaceBuyNowServerParams): Promise<ServersBuyNowResponse> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/servers/order/buy_now_server`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['placeBuyNowServerRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update Reverse DNS
   * Updates the reverse DNS (PTR record) entries for the server\&#39;s IP addresses.
   * @param params.id Server ID number
   * @param params.reverseDnsEntries 
   */
  async postServerReverseDns(params: IPostServerReverseDnsParams): Promise<TextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postServerReverseDns', params, 'id');
    this.ensureParamIsSet('postServerReverseDns', params, 'reverseDnsEntries');

    // Create URL to call
    const url = `${this.basePath}/servers/{id}/reverse_dns`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['reverseDnsEntries'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Validate Server Order
   * Validates a server order before placing it. Use this to check for errors before committing to a purchase.
   */
  async putServers(): Promise<any> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/servers/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPut()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Server IPMI Live Information
   * Returns the current IPMI live connection information for the server.
   * @param params.id Server ID number
   */
  async serverIpmiLiveGet(params: IServerIpmiLiveGetParams): Promise<ServerIpmiLiveInfo> {
    // Verify required parameters are set
    this.ensureParamIsSet('serverIpmiLiveGet', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/servers/{id}/ipmi_live`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Server IPMI Live Setup
   * Configures IPMI live access by whitelisting your current IP address for connections to the server\&#39;s IPMI management interface.
   * @param params.id Server ID number
   * @param params.ip Your IP Address you wish to connect to the IPMI system from.
   * @param params.asset Asset ID
   */
  async serverIpmiLivePost(params: IServerIpmiLivePostParams): Promise<ServerIpmiLiveInfo> {
    // Verify required parameters are set
    this.ensureParamIsSet('serverIpmiLivePost', params, 'id');
    this.ensureParamIsSet('serverIpmiLivePost', params, 'ip');

    // Create URL to call
    const url = `${this.basePath}/servers/{id}/ipmi_live`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode form parameters
      .withHeader('content-type', 'application/x-www-form-urlencoded')
      .withContent(this.queryString({ 
        'asset': params['asset'],
        'ip': params['ip'],
      }))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get IPMI Power Status
   * Returns the chassis power status from ipmi.
   * @param params.id Server ID number
   */
  async serverIpmiPowerGet(params: IServerIpmiPowerGetParams): Promise<TextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('serverIpmiPowerGet', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/servers/{id}/ipmi_power`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Server IPMI Power
   * Uses the IPMI interface to set the Power status on the server.
   * @param params.id Server ID number
   * @param params.action The power action to send to the ipmi controller.
   * @param params.asset The Asset ID
   */
  async serverIpmiPowerPost(params: IServerIpmiPowerPostParams): Promise<TextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('serverIpmiPowerPost', params, 'id');
    this.ensureParamIsSet('serverIpmiPowerPost', params, 'action');

    // Create URL to call
    const url = `${this.basePath}/servers/{id}/ipmi_power`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode form parameters
      .withHeader('content-type', 'application/x-www-form-urlencoded')
      .withContent(this.queryString({ 
        'asset': params['asset'],
        'action': params['action'],
      }))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Cancel Server Service
   * Cancels the dedicated server service. The server will be deprovisioned and billing will stop at the end of the current billing cycle.
   * @param params.id Server ID number
   */
  async serversCancel(params: IServersCancelParams): Promise<ServersCancel200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('serversCancel', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/servers/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update Server Order
   * Updates settings on a dedicated server order.
   * @param params.id Server ID number.
   */
  async updateServerInfo(params: IUpdateServerInfoParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateServerInfo', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/servers/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

