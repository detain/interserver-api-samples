/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  DomainDnssecRecords,
  ChargeInvoiceRows,
  CancelDomain200Response,
  SuccessTextResponse,
  DomainNameserverPostRequest,
  TextResponse,
  DomainRow,
  DomainContactDetails,
  DomainNameserverGetResponse,
  DomainWhoisPrivacyRequest,
  DomainLookupResponse,
  DomainSearchResponse,
  DomainOrder,
  ServiceOrderPostResponse,
  DomainNameserverPutRequest,
  DomainDnssecRequest,
  Domain,
} from './models';

/**
 * addDomain - parameters interface
 */
export interface IAddDomainParams {
}

/**
 * addDomainDnssec - parameters interface
 */
export interface IAddDomainDnssecParams {
  id: number;
  domainDnssecRequest: DomainDnssecRequest;
}

/**
 * addDomainNameserver - parameters interface
 */
export interface IAddDomainNameserverParams {
  id: number;
  domainNameserverPostRequest: DomainNameserverPostRequest;
}

/**
 * cancelDomain - parameters interface
 */
export interface ICancelDomainParams {
  id: number;
}

/**
 * deleteDomainDnssec - parameters interface
 */
export interface IDeleteDomainDnssecParams {
  id: number;
  action: string;
}

/**
 * deleteDomainNameserver - parameters interface
 */
export interface IDeleteDomainNameserverParams {
  id: number;
  index: number;
}

/**
 * getDomainContact - parameters interface
 */
export interface IGetDomainContactParams {
  id: number;
}

/**
 * getDomainDnssec - parameters interface
 */
export interface IGetDomainDnssecParams {
  id: number;
}

/**
 * getDomainInfo - parameters interface
 */
export interface IGetDomainInfoParams {
  id: number;
}

/**
 * getDomainInvoices - parameters interface
 */
export interface IGetDomainInvoicesParams {
  id: number;
}

/**
 * getDomainLookup - parameters interface
 */
export interface IGetDomainLookupParams {
  name: string;
}

/**
 * getDomainNameservers - parameters interface
 */
export interface IGetDomainNameserversParams {
  id: number;
}

/**
 * getDomainOrderFields - parameters interface
 */
export interface IGetDomainOrderFieldsParams {
  domain: string;
  regType: string;
}

/**
 * getDomainOrderSearchResults - parameters interface
 */
export interface IGetDomainOrderSearchResultsParams {
  domain: string;
}

/**
 * getDomainRenewal - parameters interface
 */
export interface IGetDomainRenewalParams {
  id: number;
}

/**
 * getDomainSearch - parameters interface
 */
export interface IGetDomainSearchParams {
  name: string;
}

/**
 * getDomainTransfer - parameters interface
 */
export interface IGetDomainTransferParams {
  id: number;
}

/**
 * getDomainWhoisPrivacy - parameters interface
 */
export interface IGetDomainWhoisPrivacyParams {
  id: number;
}

/**
 * getDomainsList - parameters interface
 */
export interface IGetDomainsListParams {
}

/**
 * getDomainsWelcomeEmail - parameters interface
 */
export interface IGetDomainsWelcomeEmailParams {
  id: number;
}

/**
 * getNewDomain - parameters interface
 */
export interface IGetNewDomainParams {
}

/**
 * patchDomains - parameters interface
 */
export interface IPatchDomainsParams {
}

/**
 * postDomainRenewal - parameters interface
 */
export interface IPostDomainRenewalParams {
  id: number;
}

/**
 * postDomainTransfer - parameters interface
 */
export interface IPostDomainTransferParams {
  id: number;
}

/**
 * putDomains - parameters interface
 */
export interface IPutDomainsParams {
}

/**
 * updateDomainContact - parameters interface
 */
export interface IUpdateDomainContactParams {
  id: number;
  domainContactDetails: DomainContactDetails;
}

/**
 * updateDomainInfo - parameters interface
 */
export interface IUpdateDomainInfoParams {
  id: string;
}

/**
 * updateDomainNameservers - parameters interface
 */
export interface IUpdateDomainNameserversParams {
  id: number;
  domainNameserverPutRequest: DomainNameserverPutRequest;
}

/**
 * updateDomainWhoisPrivacy - parameters interface
 */
export interface IUpdateDomainWhoisPrivacyParams {
  id: number;
  domainWhoisPrivacyRequest: DomainWhoisPrivacyRequest;
}

/**
 * DomainsApi - API class
 */
@autoinject()
export class DomainsApi extends Api {

  /**
   * Creates a new DomainsApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Place Domain Order
   * Places a new domain registration or transfer order. Use the results from &#x60;/domains/lookup/{name}&#x60; or &#x60;/domains/order/{domain}/{regType}&#x60; to populate the required domain fields before submitting the order.
   */
  async addDomain(): Promise<ServiceOrderPostResponse> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/domains/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Add Domain DNSSEC Records
   * Adds DNSSEC DS records to the domain registration. Provide one or more DNSSEC record entries (algorithm, digest type, digest, key tag). Registrations must be active.
   * @param params.id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;.
   * @param params.domainDnssecRequest 
   */
  async addDomainDnssec(params: IAddDomainDnssecParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('addDomainDnssec', params, 'id');
    this.ensureParamIsSet('addDomainDnssec', params, 'domainDnssecRequest');

    // Create URL to call
    const url = `${this.basePath}/domains/{id}/dnssec`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['domainDnssecRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Add Registered Nameserver
   * Adds a new registered nameserver (glue record) for the domain using the supplied hostname and IP address. Use &#x60;GET /domains/{id}/nameservers&#x60; to confirm that the new entry exists.
   * @param params.id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;.
   * @param params.domainNameserverPostRequest 
   */
  async addDomainNameserver(params: IAddDomainNameserverParams): Promise<TextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('addDomainNameserver', params, 'id');
    this.ensureParamIsSet('addDomainNameserver', params, 'domainNameserverPostRequest');

    // Create URL to call
    const url = `${this.basePath}/domains/{id}/nameservers`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['domainNameserverPostRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Cancel Domain Order
   * Cancels the domain order in the billing system. After cancellation the service will no longer auto-renew and will transition to a &#x60;canceled&#x60; status. The domain registration itself may remain active at the registrar until its current expiration date, but no further renewals will be processed. Treat the domain as inactive in downstream automation after this call succeeds.
   * @param params.id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;.
   */
  async cancelDomain(params: ICancelDomainParams): Promise<CancelDomain200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('cancelDomain', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/domains/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Remove Domain DNSSEC Records
   * Removes DNSSEC DS records from the domain registration. Use this to disable DNSSEC at the registrar level.
   * @param params.id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;.
   * @param params.action Set to &#x60;delete&#x60; to remove all DNSSEC records.
   */
  async deleteDomainDnssec(params: IDeleteDomainDnssecParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteDomainDnssec', params, 'id');
    this.ensureParamIsSet('deleteDomainDnssec', params, 'action');

    // Create URL to call
    const url = `${this.basePath}/domains/{id}/dnssec`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()
      // Set query parameters
      .withParams({ 
        'action': params['action'],
      })

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delete Registered Nameserver
   * Removes a registered nameserver (glue record) from the domain. Specify the zero-based &#x60;index&#x60; of the nameserver to remove as returned by &#x60;GET /domains/{id}/nameservers&#x60;.
   * @param params.id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;.
   * @param params.index The index of the registered nameserver from the registered nameservers list to delete.  
   */
  async deleteDomainNameserver(params: IDeleteDomainNameserverParams): Promise<TextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteDomainNameserver', params, 'id');
    this.ensureParamIsSet('deleteDomainNameserver', params, 'index');

    // Create URL to call
    const url = `${this.basePath}/domains/{id}/nameservers`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()
      // Set query parameters
      .withParams({ 
        'index': params['index'],
      })

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Domain Contact Details
   * Returns the current domain contact details that will be used for the registrant, admin, technical, and billing contact records. Use this response as the basis for any contact updates.
   * @param params.id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;.
   */
  async getDomainContact(params: IGetDomainContactParams): Promise<DomainContactDetails> {
    // Verify required parameters are set
    this.ensureParamIsSet('getDomainContact', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/domains/{id}/contact`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Domain DNSSEC Records
   * Returns the DNSSEC DS records registered with the domain. Use the record data if you need to synchronize DNSSEC settings with another registry or to prepare an update request.
   * @param params.id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;.
   */
  async getDomainDnssec(params: IGetDomainDnssecParams): Promise<DomainDnssecRecords> {
    // Verify required parameters are set
    this.ensureParamIsSet('getDomainDnssec', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/domains/{id}/dnssec`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Domain Order
   * Fetches the detailed domain service record (billing, registration, and service metadata) for the specified order ID. Use the &#x60;domain_hostname&#x60; from this response when calling &#x60;/domains/lookup/{name}&#x60; or when interacting with nameserver and DNSSEC management endpoints.
   * @param params.id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;.
   */
  async getDomainInfo(params: IGetDomainInfoParams): Promise<Domain> {
    // Verify required parameters are set
    this.ensureParamIsSet('getDomainInfo', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/domains/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Domain Invoices
   * Retrieves invoice records associated with the domain order. Use this to reconcile billing or to confirm whether renewals or add-ons (such as Whois privacy) have been invoiced.
   * @param params.id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;.
   */
  async getDomainInvoices(params: IGetDomainInvoicesParams): Promise<ChargeInvoiceRows> {
    // Verify required parameters are set
    this.ensureParamIsSet('getDomainInvoices', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/domains/{id}/invoices`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Lookup Domain Availability and Pricing
   * Checks a domain for availability and premium status, and returns pricing information for new, renewal, and transfer actions when possible. The response also includes the order field metadata you can pass to &#x60;/domains/order&#x60; when placing a registration or transfer.
   * @param params.name The full domain name to look up (for example &#x60;example.com&#x60;).
   */
  async getDomainLookup(params: IGetDomainLookupParams): Promise<DomainLookupResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('getDomainLookup', params, 'name');

    // Create URL to call
    const url = `${this.basePath}/domains/lookup/{name}`
      .replace(`{${'name'}}`, encodeURIComponent(`${params['name']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List Registered Nameservers
   * Returns the registered nameserver hosts and glue IP addresses currently configured at the registrar. Use the returned list when adding, updating, or deleting individual nameserver records.
   * @param params.id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;.
   */
  async getDomainNameservers(params: IGetDomainNameserversParams): Promise<DomainNameserverGetResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('getDomainNameservers', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/domains/{id}/nameservers`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Domain Order Fields
   * Returns the registration-type-specific form fields required to place a domain order for the given hostname. Use the returned fields to build the POST body for &#x60;POST /domains/order&#x60;.
   * @param params.domain The fully qualified domain name (e.g. &#x60;example.com&#x60;).
   * @param params.regType The registration type. Common values include &#x60;register&#x60; for new registrations and &#x60;transfer&#x60; for inbound transfers.
   */
  async getDomainOrderFields(params: IGetDomainOrderFieldsParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('getDomainOrderFields', params, 'domain');
    this.ensureParamIsSet('getDomainOrderFields', params, 'regType');

    // Create URL to call
    const url = `${this.basePath}/domains/order/{domain}/{regType}`
      .replace(`{${'domain'}}`, encodeURIComponent(`${params['domain']}`))
      .replace(`{${'regType'}}`, encodeURIComponent(`${params['regType']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Domain Order Search Results
   * Returns order data for the specified domain hostname, including availability and pricing details. Use this to preview order information before placing a registration or transfer.
   * @param params.domain The fully qualified domain name to look up (e.g. &#x60;example.com&#x60;).
   */
  async getDomainOrderSearchResults(params: IGetDomainOrderSearchResultsParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('getDomainOrderSearchResults', params, 'domain');

    // Create URL to call
    const url = `${this.basePath}/domains/order/{domain}`
      .replace(`{${'domain'}}`, encodeURIComponent(`${params['domain']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Start Domain Renewal Flow
   * Starts the domain renewal workflow for an active domain. This endpoint currently returns a status message indicating that renewal handling has been triggered.
   * @param params.id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;.
   */
  async getDomainRenewal(params: IGetDomainRenewalParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('getDomainRenewal', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/domains/{id}/renew`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Search Domain Suggestions
   * Queries the registrar for suggested domains and availability data. Use the returned &#x60;lookup&#x60; results to identify availability and then call &#x60;/domains/lookup/{name}&#x60; or &#x60;/domains/order&#x60; to obtain pricing and order fields for the chosen domain.
   * @param params.name The base domain name to search (for example &#x60;example&#x60; or &#x60;example.com&#x60;).
   */
  async getDomainSearch(params: IGetDomainSearchParams): Promise<DomainSearchResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('getDomainSearch', params, 'name');

    // Create URL to call
    const url = `${this.basePath}/domains/search/{name}`
      .replace(`{${'name'}}`, encodeURIComponent(`${params['name']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Start Domain Transfer Flow
   * Initiates the transfer workflow for a domain already in your account. Use this in coordination with &#x60;/domains/{id}/contact&#x60; to ensure registrant details are ready for the transfer request.
   * @param params.id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;.
   */
  async getDomainTransfer(params: IGetDomainTransferParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('getDomainTransfer', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/domains/{id}/transfer`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Whois Privacy Status
   * Returns status text for the Whois privacy add-on on the domain. Use this to determine whether privacy can be enabled or disabled for the current TLD before submitting updates.
   * @param params.id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;.
   */
  async getDomainWhoisPrivacy(params: IGetDomainWhoisPrivacyParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('getDomainWhoisPrivacy', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/domains/{id}/whois`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List Domain Orders
   * Returns the domain services on your account, including the billing order ID, hostname, expiration date, and status. Use the &#x60;domain_id&#x60; from this response with &#x60;GET /domains/{id}&#x60; to fetch full service details, or with &#x60;/domains/{id}/nameservers&#x60;, &#x60;/domains/{id}/contact&#x60;, and &#x60;/domains/{id}/whois&#x60; to manage registration settings.
   */
  async getDomainsList(): Promise<Array<DomainRow>> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/domains`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Resend Domain Welcome Email
   * Resends the welcome email for the domain service. The email contains registration details and management instructions.
   * @param params.id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;.
   */
  async getDomainsWelcomeEmail(params: IGetDomainsWelcomeEmailParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('getDomainsWelcomeEmail', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/domains/{id}/welcome_email`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Domain Ordering Information
   * Returns the catalog of available domain services, including TLD service IDs and base Whois privacy pricing. Use the &#x60;tldServices&#x60; map to resolve a TLD into a service ID when you build an order request for &#x60;/domains/order&#x60; (POST) or the domain-field lookup endpoints.
   */
  async getNewDomain(): Promise<DomainOrder> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/domains/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Validate Domain Order
   * Returns the domain-specific field requirements for the supplied hostname and registration type. Use this to build the form data required for &#x60;/domains/order&#x60; (POST).
   */
  async patchDomains(): Promise<any> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/domains/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPatch()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Request Domain Renewal
   * Submits a domain renewal request for the order. Use &#x60;/domains/{id}/invoices&#x60; to identify any invoices created as part of the renewal flow.
   * @param params.id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;.
   */
  async postDomainRenewal(params: IPostDomainRenewalParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postDomainRenewal', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/domains/{id}/renew`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Request Domain Transfer
   * Submits a transfer request for the domain order. Use &#x60;/domains/{id}/invoices&#x60; to identify any invoices generated as part of the transfer process.
   * @param params.id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;.
   */
  async postDomainTransfer(params: IPostDomainTransferParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postDomainTransfer', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/domains/{id}/transfer`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Domain Order Search
   * Returns validation data for a domain order request (such as availability and pricing information). This is typically used as a pre-flight check before submitting a POST order.
   */
  async putDomains(): Promise<any> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/domains/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPut()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update Domain Contact Details
   * Updates the registrant/admin contact details for the domain. Submit the same fields returned by the GET response after making the desired changes. Domain updates require the service to be active and unlocked.
   * @param params.id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;.
   * @param params.domainContactDetails 
   */
  async updateDomainContact(params: IUpdateDomainContactParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateDomainContact', params, 'id');
    this.ensureParamIsSet('updateDomainContact', params, 'domainContactDetails');

    // Create URL to call
    const url = `${this.basePath}/domains/{id}/contact`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['domainContactDetails'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update Domain Order
   * Updates the domain service record for the order. Use this for account-level changes such as updating stored registration metadata or transfer attributes.
   * @param params.id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;.
   */
  async updateDomainInfo(params: IUpdateDomainInfoParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateDomainInfo', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/domains/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Replace Nameserver Set
   * Replaces the full nameserver assignment for the domain with the provided list. This is the primary method for changing which authoritative nameservers the domain delegates to.
   * @param params.id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;.
   * @param params.domainNameserverPutRequest 
   */
  async updateDomainNameservers(params: IUpdateDomainNameserversParams): Promise<TextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateDomainNameservers', params, 'id');
    this.ensureParamIsSet('updateDomainNameservers', params, 'domainNameserverPutRequest');

    // Create URL to call
    const url = `${this.basePath}/domains/{id}/nameservers`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPut()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['domainNameserverPutRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update Whois Privacy
   * Enables or disables the Whois privacy add-on for the domain. The API may create invoices when enabling privacy; use &#x60;/domains/{id}/invoices&#x60; to track billing events.
   * @param params.id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;.
   * @param params.domainWhoisPrivacyRequest 
   */
  async updateDomainWhoisPrivacy(params: IUpdateDomainWhoisPrivacyParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateDomainWhoisPrivacy', params, 'id');
    this.ensureParamIsSet('updateDomainWhoisPrivacy', params, 'domainWhoisPrivacyRequest');

    // Create URL to call
    const url = `${this.basePath}/domains/{id}/whois`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['domainWhoisPrivacyRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

