/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  MailCancel200Response,
  MailDelistRequest,
  ChargeInvoiceRows,
  GenericResponse,
  SuccessTextResponse,
  MailStatsType,
  SendMailAdv,
  MailAlertUpdateRequest,
  DenyRuleRecord,
  DenyRuleNew,
  SendMail,
  MailDeliverabilityResponse,
  MailAlertRequest,
  MailAlertsResponse,
  MailLog,
  ServiceOrderPostResponse,
  MailOrder,
  MailDelistResponse,
  MailBlocks,
  MailRow,
  MailSchema,
  ViewMailLogStartDateParameter,
} from './models';

/**
 * addMail - parameters interface
 */
export interface IAddMailParams {
}

/**
 * addRule - parameters interface
 */
export interface IAddRuleParams {
  id: number;
  denyRuleNew: DenyRuleNew;
}

/**
 * createMailAlert - parameters interface
 */
export interface ICreateMailAlertParams {
  id: number;
  mailAlertRequest: MailAlertRequest;
}

/**
 * deleteMailAlert - parameters interface
 */
export interface IDeleteMailAlertParams {
  id: number;
  alertId: number;
}

/**
 * deleteRule - parameters interface
 */
export interface IDeleteRuleParams {
  id: number;
  rule: string;
}

/**
 * delistBlock - parameters interface
 */
export interface IDelistBlockParams {
  id: number;
  email?: string;
}

/**
 * getMailAlerts - parameters interface
 */
export interface IGetMailAlertsParams {
  id: number;
}

/**
 * getMailBlocks - parameters interface
 */
export interface IGetMailBlocksParams {
  id: number;
}

/**
 * getMailDelist - parameters interface
 */
export interface IGetMailDelistParams {
  id: number;
}

/**
 * getMailDeliverability - parameters interface
 */
export interface IGetMailDeliverabilityParams {
  id: number;
}

/**
 * getMailInfo - parameters interface
 */
export interface IGetMailInfoParams {
  id: number;
}

/**
 * getMailInvoices - parameters interface
 */
export interface IGetMailInvoicesParams {
  id: number;
}

/**
 * getMailList - parameters interface
 */
export interface IGetMailListParams {
}

/**
 * getMailWelcomeEmail - parameters interface
 */
export interface IGetMailWelcomeEmailParams {
  id: number;
}

/**
 * getNewMail - parameters interface
 */
export interface IGetNewMailParams {
}

/**
 * getRules - parameters interface
 */
export interface IGetRulesParams {
  id: number;
}

/**
 * getStats - parameters interface
 */
export interface IGetStatsParams {
  id: number;
  time?: 'all' | 'billing' | 'month' | '7d' | '24h' | '1d' | '1h';
}

/**
 * mailCancel - parameters interface
 */
export interface IMailCancelParams {
  id: number;
}

/**
 * postMailDelist - parameters interface
 */
export interface IPostMailDelistParams {
  id: number;
  mailDelistRequest: MailDelistRequest;
}

/**
 * putMail - parameters interface
 */
export interface IPutMailParams {
}

/**
 * resetMailPassword - parameters interface
 */
export interface IResetMailPasswordParams {
  id: number;
}

/**
 * sendAdvMail - parameters interface
 */
export interface ISendAdvMailParams {
  id: number;
  sendMailAdv: SendMailAdv;
}

/**
 * sendMail - parameters interface
 */
export interface ISendMailParams {
  id: number;
  sendMail: SendMail;
}

/**
 * updateMailAlert - parameters interface
 */
export interface IUpdateMailAlertParams {
  id: number;
  mailAlertUpdateRequest: MailAlertUpdateRequest;
}

/**
 * updateMailInfo - parameters interface
 */
export interface IUpdateMailInfoParams {
  id: string;
}

/**
 * viewMailLog - parameters interface
 */
export interface IViewMailLogParams {
  id: number;
  id2?: number;
  origin?: string;
  mx?: string;
  from?: string;
  to?: string;
  subject?: string;
  mailid?: string;
  messageId?: string;
  replyto?: string;
  headerfrom?: string;
  delivered?: 0 | 1;
  skip?: number;
  limit?: number;
  startDate?: ViewMailLogStartDateParameter;
  endDate?: ViewMailLogStartDateParameter;
  sort?: 'time';
  dir?: 'asc' | 'desc';
  groupby?: 'message' | 'recipient';
}

/**
 * MailApi - API class
 */
@autoinject()
export class MailApi extends Api {

  /**
   * Creates a new MailApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Place Mail Order
   * Places a Mail Baby order. On success, invoices are created for payment; use &#x60;/billing/invoices/{id}&#x60; or &#x60;/pay/{method}/{invoices}&#x60; to complete payment.
   */
  async addMail(): Promise<ServiceOrderPostResponse> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/mail/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Create Deny Rule
   * Adds a new deny rule to automatically block emails that match the specified criteria.
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   * @param params.denyRuleNew These are the fields needed to create a new email deny rule.
   */
  async addRule(params: IAddRuleParams): Promise<GenericResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('addRule', params, 'id');
    this.ensureParamIsSet('addRule', params, 'denyRuleNew');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}/rules`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['denyRuleNew'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Create Mail Alert
   * Creates a new alert for the mail service, such as delivery or quota notifications.
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   * @param params.mailAlertRequest 
   */
  async createMailAlert(params: ICreateMailAlertParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('createMailAlert', params, 'id');
    this.ensureParamIsSet('createMailAlert', params, 'mailAlertRequest');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}/alerts`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['mailAlertRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delete Mail Alert
   * Deletes an existing alert definition for the mail service.
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   * @param params.alertId Alert ID to delete.
   */
  async deleteMailAlert(params: IDeleteMailAlertParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteMailAlert', params, 'id');
    this.ensureParamIsSet('deleteMailAlert', params, 'alertId');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}/alerts`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()
      // Set query parameters
      .withParams({ 
        'alert_id': params['alertId'],
      })

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delete Deny Rule
   * Removes a deny rule from the mail service.
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   * @param params.rule The ID of the Rules entry.
   */
  async deleteRule(params: IDeleteRuleParams): Promise<GenericResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteRule', params, 'id');
    this.ensureParamIsSet('deleteRule', params, 'rule');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}/rules/{rule}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`))
      .replace(`{${'rule'}}`, encodeURIComponent(`${params['rule']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Remove Email Address from Block List
   * Removes an email address from the mail service\&#39;s block lists.
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   * @param params.email an email address
   */
  async delistBlock(params: IDelistBlockParams): Promise<GenericResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('delistBlock', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}/blocks/delete`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode form parameters
      .withHeader('content-type', 'application/x-www-form-urlencoded')
      .withContent(this.queryString({ 
        'email': params['email'],
      }))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List Mail Alerts
   * Returns the alert configuration for the mail service. Use the alert IDs from this response with PUT or DELETE to update or remove alerts.
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   */
  async getMailAlerts(params: IGetMailAlertsParams): Promise<MailAlertsResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('getMailAlerts', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}/alerts`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List Blocked Email Addresses
   * Displays a listing of the blocked email addresses
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   */
  async getMailBlocks(params: IGetMailBlocksParams): Promise<MailBlocks> {
    // Verify required parameters are set
    this.ensureParamIsSet('getMailBlocks', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}/blocks`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Delist Status
   * Returns the current blocklist and delisting information for the mail service, including recent local and trap blocks.
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   */
  async getMailDelist(params: IGetMailDelistParams): Promise<MailDelistResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('getMailDelist', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}/delist`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Deliverability Metrics
   * Returns deliverability statistics such as delivered vs. bounced counts and percentages. Use query filters to pivot the response by domain or sender.
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   */
  async getMailDeliverability(params: IGetMailDeliverabilityParams): Promise<MailDeliverabilityResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('getMailDeliverability', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}/deliverability`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Mail Order
   * Returns detailed information for the mail service, including credentials and service metadata required to configure your sending client.
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   */
  async getMailInfo(params: IGetMailInfoParams): Promise<MailSchema> {
    // Verify required parameters are set
    this.ensureParamIsSet('getMailInfo', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Mail Invoices
   * Retrieves invoices associated with the mail service. Use these invoices to validate billing status or initiate payment.
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   */
  async getMailInvoices(params: IGetMailInvoicesParams): Promise<ChargeInvoiceRows> {
    // Verify required parameters are set
    this.ensureParamIsSet('getMailInvoices', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}/invoices`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List Mail Orders
   * Returns the Mail Baby services on your account. Use the &#x60;mail_id&#x60; from this list with &#x60;/mail/{id}&#x60; to retrieve service details, and with &#x60;/mail/{id}/stats&#x60; or &#x60;/mail/{id}/log&#x60; to review delivery statistics.
   */
  async getMailList(): Promise<Array<MailRow>> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/mail`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Resend Mail Welcome Email
   * Resends the welcome email for the Mail Baby service. The email contains SMTP credentials and configuration instructions.
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   */
  async getMailWelcomeEmail(params: IGetMailWelcomeEmailParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('getMailWelcomeEmail', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}/welcome_email`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Mail Ordering Information
   * Returns available Mail Baby plans and ordering metadata. Use the service type IDs from this response when validating or placing a new mail order.
   */
  async getNewMail(): Promise<MailOrder> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/mail/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List Deny Rules
   * Returns a listing of all the deny block rules configured for this mail service.
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   */
  async getRules(params: IGetRulesParams): Promise<Array<DenyRuleRecord>> {
    // Verify required parameters are set
    this.ensureParamIsSet('getRules', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}/rules`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Mail Usage Statistics
   * Returns usage statistics for the mail service over the requested time period, including send counts, delivery rates, and quota consumption.
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   * @param params.time The timeframe for the statistics.
   */
  async getStats(params: IGetStatsParams): Promise<MailStatsType> {
    // Verify required parameters are set
    this.ensureParamIsSet('getStats', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}/stats`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'time': params['time'],
      })

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Cancel Mail
   * Cancels a Mail Baby service. After cancellation the mail credentials are deactivated and the service transitions to a canceled status. No further billing charges will be incurred.
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   */
  async mailCancel(params: IMailCancelParams): Promise<MailCancel200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('mailCancel', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delist a Blocked Sender
   * Removes an email address from blocklists for the mail service. Provide the &#x60;unblock&#x60; email address from the delist status response.
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   * @param params.mailDelistRequest 
   */
  async postMailDelist(params: IPostMailDelistParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postMailDelist', params, 'id');
    this.ensureParamIsSet('postMailDelist', params, 'mailDelistRequest');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}/delist`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['mailDelistRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Validate Mail Order
   * Validates a Mail Baby order and returns pricing or errors. Use this before placing the final order.
   */
  async putMail(): Promise<any> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/mail/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPut()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Reset Mail Password
   * Resets the Mail Baby service password and emails the new password to the account owner. Use &#x60;/mail/{id}&#x60; to retrieve updated credential data after the reset.
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   */
  async resetMailPassword(params: IResetMailPasswordParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('resetMailPassword', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}/reset_password`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Send Email with Advanced Options
   * Sends an email through one of your mail orders with support for file attachments, CC, BCC, and other advanced options. For simple single-recipient sends, use &#x60;POST /mail/{id}/send&#x60;.
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   * @param params.sendMailAdv 
   */
  async sendAdvMail(params: ISendAdvMailParams): Promise<GenericResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('sendAdvMail', params, 'id');
    this.ensureParamIsSet('sendAdvMail', params, 'sendMailAdv');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}/advsend`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['sendMailAdv'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Send Email
   * Sends an email through one of your mail orders. For multiple recipients or file attachments, use &#x60;POST /mail/{id}/advsend&#x60; instead.
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   * @param params.sendMail 
   */
  async sendMail(params: ISendMailParams): Promise<GenericResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('sendMail', params, 'id');
    this.ensureParamIsSet('sendMail', params, 'sendMail');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}/send`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['sendMail'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update Mail Alert
   * Updates an existing alert definition for the mail service. Provide the &#x60;alert_id&#x60; returned by the list response along with updated fields.
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   * @param params.mailAlertUpdateRequest 
   */
  async updateMailAlert(params: IUpdateMailAlertParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateMailAlert', params, 'id');
    this.ensureParamIsSet('updateMailAlert', params, 'mailAlertUpdateRequest');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}/alerts`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPut()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['mailAlertUpdateRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update Mail Order
   * Updates mail service metadata for the order, such as stored settings or account details.
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   */
  async updateMailInfo(params: IUpdateMailInfoParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateMailInfo', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * View Mail Log
   * Returns a paginated log of emails sent through this mail service, with optional filtering by sender, recipient, date range, and delivery status.  **Row grouping** is controlled by the &#x60;groupby&#x60; parameter.  By default (&#x60;groupby&#x3D;recipient&#x60;), the response contains one row per delivery attempt — so a single message sent to 4 recipients produces 4 rows, each with its own &#x60;recipient&#x60;, &#x60;delivered&#x60;, &#x60;response&#x60;, and &#x60;mxHostname&#x60; values.  Set &#x60;groupby&#x3D;message&#x60; to collapse to one row per message (delivery fields will reflect one arbitrary recipient).  **Pagination** is controlled by &#x60;skip&#x60; and &#x60;limit&#x60;.  The &#x60;total&#x60; in the response reflects the row count **after** grouping, so it matches the number of pages you need to fetch.  **Date filtering** accepts either a Unix timestamp (integer) or a date string parseable by PHP &#x60;strtotime()&#x60; such as &#x60;2024-01-15&#x60;, &#x60;last monday&#x60;, or &#x60;2024-01-01 00:00:00&#x60;.  Examples: &#x60;startDate&#x3D;1704067200&amp;endDate&#x3D;1706745599&#x60; or &#x60;startDate&#x3D;2024-01-01&amp;endDate&#x3D;2024-01-31&#x60;.  **Sorting** is controlled by &#x60;sort&#x60; and &#x60;dir&#x60;.  Currently the only sort key is &#x60;time&#x60; (default), which orders by internal row ID.  **Delivery status** can be filtered with the &#x60;delivered&#x60; parameter: &#x60;delivered&#x3D;1&#x60; returns only successfully delivered messages; &#x60;delivered&#x3D;0&#x60; returns messages still in queue or that failed.  **Address filtering** distinguishes between the SMTP envelope address (&#x60;from&#x60;, &#x60;to&#x60;) and message headers (&#x60;headerfrom&#x60; for the &#x60;From:&#x60; header, &#x60;replyto&#x60; for &#x60;Reply-To:&#x60;). These may differ when a message is sent on behalf of another address.  The &#x60;mailid&#x60; parameter corresponds to the &#x60;id&#x60; field in the returned &#x60;MailLogEntry&#x60; objects, **not** the &#x60;_id&#x60; field.  It also matches the transaction ID returned in the &#x60;text&#x60; field of a successful send response.  The &#x60;messageId&#x60; parameter searches the &#x60;Message-ID&#x60; email header (case-insensitive substring match). 
   * @param params.id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;.
   * @param params.id2 The numeric ID of the mail order to filter by.  When omitted, logs from the first active mail order are returned.  Obtain valid IDs from &#x60;GET /mail&#x60; or &#x60;GET /mail/{id}&#x60;.
   * @param params.origin Filter by the originating IP address from which the message was submitted to the relay.  Must be a valid IPv4 or IPv6 address.
   * @param params.mx Filter by the MX hostname the relay attempted delivery to.  For example &#x60;mx.google.com&#x60; would return messages destined for Gmail recipients. Maps to &#x60;mxHostname&#x60; in the &#x60;MailLogEntry&#x60; response.
   * @param params.from Filter by SMTP envelope &#x60;MAIL FROM&#x60; address (exact match).  This is the address the relay used for bounce handling and may differ from the &#x60;From:&#x60; message header.  For header-level filtering use &#x60;headerfrom&#x60;.
   * @param params.to Filter by SMTP envelope &#x60;RCPT TO&#x60; address (exact match).  This is the delivery address used by the relay and may differ from the &#x60;To:&#x60; header when BCC recipients are involved.
   * @param params.subject Filter by email &#x60;Subject&#x60; header (exact match).  MIME-encoded subjects are decoded automatically in the response.
   * @param params.mailid Filter by the relay-assigned mail ID string (exact match).  This corresponds to the &#x60;id&#x60; field in &#x60;MailLogEntry&#x60; and to the &#x60;text&#x60; value returned by the sending endpoints on success.  Format is an 18-19 character hexadecimal string such as &#x60;185997065c60008840&#x60;.
   * @param params.messageId Filter by the &#x60;Message-ID&#x60; email header using a substring (case-insensitive) match. The &#x60;Message-ID&#x60; is assigned by the sending mail client and is visible in the &#x60;messageId&#x60; field of &#x60;MailLogEntry&#x60;.
   * @param params.replyto Filter by the &#x60;Reply-To&#x60; message header address (exact match).  Only returns messages where this header was explicitly set.
   * @param params.headerfrom Filter by the &#x60;From&#x60; message header address (exact match).  This is the human-visible sender address and may differ from the SMTP envelope &#x60;from&#x60; parameter when sending on behalf of another address.
   * @param params.delivered Filter by delivery status.  &#x60;1&#x60; returns only messages that were successfully delivered to the destination MX.  &#x60;0&#x60; returns messages that are still queued, deferred, or failed.  Omit to return all messages regardless of delivery status.
   * @param params.skip Number of records to skip for pagination.  Use in combination with &#x60;limit&#x60; to page through large result sets.  Defaults to &#x60;0&#x60; (no skip).
   * @param params.limit Maximum number of records to return per page.  Defaults to &#x60;100&#x60;. Maximum allowed value is &#x60;10000&#x60;.  The response also includes a &#x60;total&#x60; field with the full matched count so you can calculate the number of pages.
   * @param params.startDate Earliest date to include.  Accepts either a Unix timestamp (integer seconds since epoch) or a date string parseable by &#x60;strtotime()&#x60; such as &#x60;2024-01-15&#x60; or &#x60;last monday&#x60;.  Messages with a &#x60;time&#x60; value **greater than or equal to** this value will be included.
   * @param params.endDate Latest date to include.  Accepts either a Unix timestamp (integer seconds since epoch) or a date string parseable by &#x60;strtotime()&#x60; such as &#x60;2024-01-31&#x60; or &#x60;yesterday&#x60;. Messages with a &#x60;time&#x60; value **less than or equal to** this value will be included.
   * @param params.sort Field to sort results by.  Currently only &#x60;time&#x60; is supported (sorts by internal row ID which corresponds to chronological order).
   * @param params.dir Sort direction.  &#x60;desc&#x60; returns newest first (default), &#x60;asc&#x60; returns oldest first.
   * @param params.groupby Controls how results are grouped.  &#x60;recipient&#x60; (default) returns one row per delivery attempt — a message sent to 4 recipients produces 4 rows, each with its own &#x60;recipient&#x60;, &#x60;delivered&#x60;, &#x60;response&#x60;, and delivery metadata.  &#x60;message&#x60; collapses to one row per unique message ID; delivery-level fields will reflect one arbitrary recipient per message.  The &#x60;total&#x60; count in the response matches the grouping mode.
   */
  async viewMailLog(params: IViewMailLogParams): Promise<MailLog> {
    // Verify required parameters are set
    this.ensureParamIsSet('viewMailLog', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/mail/{id}/log`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'id': params['id2'],
        'origin': params['origin'],
        'mx': params['mx'],
        'from': params['from'],
        'to': params['to'],
        'subject': params['subject'],
        'mailid': params['mailid'],
        'messageId': params['messageId'],
        'replyto': params['replyto'],
        'headerfrom': params['headerfrom'],
        'delivered': params['delivered'],
        'skip': params['skip'],
        'limit': params['limit'],
        'startDate': params['startDate'],
        'endDate': params['endDate'],
        'sort': params['sort'],
        'dir': params['dir'],
        'groupby': params['groupby'],
      })

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

