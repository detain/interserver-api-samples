/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  ChargeInvoiceRows,
  VpsRow,
  DownloadQsBackupRequest,
  SuccessTextResponse,
  VpsBackupRows,
  VPSCancel200Response,
  TextResponse,
  ReverseDnsEntries,
  VpsOrderPutRequest,
  QueueResponse,
  DownloadQsBackup200Response,
  VpsTemplatesList,
  VpsTrafficResponse,
  Vps,
  VpsOrderPutResponse,
  RestoreRequest,
  VpsOrderPostRequest,
  VpsOrder,
} from './models';

/**
 * addVps - parameters interface
 */
export interface IAddVpsParams {
  vpsOrderPostRequest?: VpsOrderPostRequest;
}

/**
 * deleteVpsBackup - parameters interface
 */
export interface IDeleteVpsBackupParams {
  id: number;
  file: string;
  all?: '0' | '1';
}

/**
 * doVpsBlockSmtp - parameters interface
 */
export interface IDoVpsBlockSmtpParams {
  id: number;
}

/**
 * doVpsDisableCd - parameters interface
 */
export interface IDoVpsDisableCdParams {
  id: number;
}

/**
 * doVpsDisableQuota - parameters interface
 */
export interface IDoVpsDisableQuotaParams {
  id: number;
}

/**
 * doVpsEjectCd - parameters interface
 */
export interface IDoVpsEjectCdParams {
  id: number;
}

/**
 * doVpsEnableQuota - parameters interface
 */
export interface IDoVpsEnableQuotaParams {
  id: number;
}

/**
 * doVpsRestart - parameters interface
 */
export interface IDoVpsRestartParams {
  id: number;
}

/**
 * doVpsStart - parameters interface
 */
export interface IDoVpsStartParams {
  id: number;
}

/**
 * doVpsStop - parameters interface
 */
export interface IDoVpsStopParams {
  id: number;
}

/**
 * downloadVpsBackup - parameters interface
 */
export interface IDownloadVpsBackupParams {
  id: number;
  downloadQsBackupRequest: DownloadQsBackupRequest;
  all?: '0' | '1';
}

/**
 * getNewVps - parameters interface
 */
export interface IGetNewVpsParams {
}

/**
 * getVpsBackups - parameters interface
 */
export interface IGetVpsBackupsParams {
  id: number;
  all?: '0' | '1';
}

/**
 * getVpsBuyHdSpace - parameters interface
 */
export interface IGetVpsBuyHdSpaceParams {
  id: number;
}

/**
 * getVpsBuyIp - parameters interface
 */
export interface IGetVpsBuyIpParams {
  id: number;
}

/**
 * getVpsChangeTimezone - parameters interface
 */
export interface IGetVpsChangeTimezoneParams {
  id: number;
}

/**
 * getVpsInfo - parameters interface
 */
export interface IGetVpsInfoParams {
  id: number;
}

/**
 * getVpsInvoices - parameters interface
 */
export interface IGetVpsInvoicesParams {
  id: number;
}

/**
 * getVpsList - parameters interface
 */
export interface IGetVpsListParams {
}

/**
 * getVpsReinstallOs - parameters interface
 */
export interface IGetVpsReinstallOsParams {
  id: number;
}

/**
 * getVpsReverseDns - parameters interface
 */
export interface IGetVpsReverseDnsParams {
  id: number;
}

/**
 * getVpsSetupVnc - parameters interface
 */
export interface IGetVpsSetupVncParams {
  id: number;
}

/**
 * getVpsSlices - parameters interface
 */
export interface IGetVpsSlicesParams {
  id: number;
}

/**
 * getVpsTrafficUsage - parameters interface
 */
export interface IGetVpsTrafficUsageParams {
  id: number;
}

/**
 * getVpsViewDesktop - parameters interface
 */
export interface IGetVpsViewDesktopParams {
  id: number;
}

/**
 * getVpsWelcomeEmail - parameters interface
 */
export interface IGetVpsWelcomeEmailParams {
  id: string;
}

/**
 * postVpsBackup - parameters interface
 */
export interface IPostVpsBackupParams {
  id: number;
}

/**
 * postVpsBuyHdSpace - parameters interface
 */
export interface IPostVpsBuyHdSpaceParams {
  id: number;
}

/**
 * postVpsBuyIp - parameters interface
 */
export interface IPostVpsBuyIpParams {
  id: number;
}

/**
 * postVpsChangeHostname - parameters interface
 */
export interface IPostVpsChangeHostnameParams {
  id: number;
  hostname?: string;
}

/**
 * postVpsChangeRootPassword - parameters interface
 */
export interface IPostVpsChangeRootPasswordParams {
  id: number;
  password: string;
}

/**
 * postVpsChangeTimezone - parameters interface
 */
export interface IPostVpsChangeTimezoneParams {
  id: number;
  timezone: string;
}

/**
 * postVpsChangeWebuzoPassword - parameters interface
 */
export interface IPostVpsChangeWebuzoPasswordParams {
  id: number;
  password: string;
}

/**
 * postVpsInsertCd - parameters interface
 */
export interface IPostVpsInsertCdParams {
  id: number;
  url?: string;
}

/**
 * postVpsReinstallOs - parameters interface
 */
export interface IPostVpsReinstallOsParams {
  id: number;
  template: string;
  localPassword: string;
  password?: string;
}

/**
 * postVpsResetPassword - parameters interface
 */
export interface IPostVpsResetPasswordParams {
  id: number;
}

/**
 * postVpsRestore - parameters interface
 */
export interface IPostVpsRestoreParams {
  id: number;
  restoreRequest: RestoreRequest;
}

/**
 * postVpsReverseDns - parameters interface
 */
export interface IPostVpsReverseDnsParams {
  id: number;
  reverseDnsEntries: ReverseDnsEntries;
}

/**
 * postVpsSetupVnc - parameters interface
 */
export interface IPostVpsSetupVncParams {
  id: number;
}

/**
 * postVpsSlices - parameters interface
 */
export interface IPostVpsSlicesParams {
  id: number;
}

/**
 * postVpsViewDesktop - parameters interface
 */
export interface IPostVpsViewDesktopParams {
  id: number;
}

/**
 * putVps - parameters interface
 */
export interface IPutVpsParams {
  vpsOrderPutRequest?: VpsOrderPutRequest;
}

/**
 * updateVpsInfo - parameters interface
 */
export interface IUpdateVpsInfoParams {
  id: string;
}

/**
 * vPSCancel - parameters interface
 */
export interface IVPSCancelParams {
  id: number;
}

/**
 * VPSApi - API class
 */
@autoinject()
export class VPSApi extends Api {

  /**
   * Creates a new VPSApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Place VPS Order
   * Places an order for a new VPS. Use &#x60;PUT /vps/order&#x60; to validate the order first.
   * @param params.vpsOrderPostRequest 
   */
  async addVps(params: IAddVpsParams): Promise<any> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/vps/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['vpsOrderPostRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Delete VPS Backup
   * Permanently removes the specified backup file from storage. Use &#x60;GET /vps/{id}/backups&#x60; to list available backup filenames before deleting.
   * @param params.id VPS ID number
   * @param params.file The backup filename to delete.
   * @param params.all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given VPS.
   */
  async deleteVpsBackup(params: IDeleteVpsBackupParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('deleteVpsBackup', params, 'id');
    this.ensureParamIsSet('deleteVpsBackup', params, 'file');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/backups`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()
      // Set query parameters
      .withParams({ 
        'all': params['all'],
        'file': params['file'],
      })

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Blocks SMTP
   * Blocks outbound SMTP (port 25) on the VPS to prevent spam. Useful for VPS that do not need to send email.
   * @param params.id VPS ID number.
   */
  async doVpsBlockSmtp(params: IDoVpsBlockSmtpParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('doVpsBlockSmtp', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/block_smtp`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Disable CD Drive
   * Disables the virtual CD drive on the VPS.
   * @param params.id VPS ID number.
   */
  async doVpsDisableCd(params: IDoVpsDisableCdParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('doVpsDisableCd', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/disable_cd`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Disable Quotas
   * Disables disk quota enforcement on the VPS.
   * @param params.id VPS ID number.
   */
  async doVpsDisableQuota(params: IDoVpsDisableQuotaParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('doVpsDisableQuota', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/disable_quota`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Eject CD Drive
   * Ejects the virtual CD from the VPS CD drive.
   * @param params.id VPS ID number.
   */
  async doVpsEjectCd(params: IDoVpsEjectCdParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('doVpsEjectCd', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/eject_cd`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Enable Quotas
   * Enables disk quota enforcement on the VPS.
   * @param params.id VPS ID number.
   */
  async doVpsEnableQuota(params: IDoVpsEnableQuotaParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('doVpsEnableQuota', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/enable_quota`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Restart VPS
   * Restarts the VPS.
   * @param params.id VPS ID number.
   */
  async doVpsRestart(params: IDoVpsRestartParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('doVpsRestart', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/restart`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Start VPS
   * Powers on the VPS.
   * @param params.id VPS ID number.
   */
  async doVpsStart(params: IDoVpsStartParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('doVpsStart', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/start`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Stop VPS
   * Powers off the VPS.
   * @param params.id VPS ID number.
   */
  async doVpsStop(params: IDoVpsStopParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('doVpsStop', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/stop`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Download VPS Backup
   * Generates a pre-signed download URL for the specified backup file. The URL is valid for 24 hours. Use &#x60;GET /vps/{id}/backups&#x60; to list available backup filenames.
   * @param params.id VPS ID number
   * @param params.downloadQsBackupRequest 
   * @param params.all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given VPS.
   */
  async downloadVpsBackup(params: IDownloadVpsBackupParams): Promise<DownloadQsBackup200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('downloadVpsBackup', params, 'id');
    this.ensureParamIsSet('downloadVpsBackup', params, 'downloadQsBackupRequest');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/backups`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPatch()
      // Set query parameters
      .withParams({ 
        'all': params['all'],
      })
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['downloadQsBackupRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * VPS Ordering Information
   * Retrieves available VPS configurations, OS templates, and pricing for ordering.
   */
  async getNewVps(): Promise<VpsOrder> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/vps/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get VPS Backups List
   * Returns the available backups for the VPS across all storage systems (Swift, MinIO, ZFS). Use the backup &#x60;name&#x60; value with &#x60;PATCH /vps/{id}/backups&#x60; to download or &#x60;DELETE /vps/{id}/backups&#x60; to remove a backup. Use &#x60;POST /vps/{id}/restore&#x60; to restore from a backup.
   * @param params.id VPS ID number
   * @param params.all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given VPS.
   */
  async getVpsBackups(params: IGetVpsBackupsParams): Promise<VpsBackupRows> {
    // Verify required parameters are set
    this.ensureParamIsSet('getVpsBackups', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/backups`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'all': params['all'],
      })

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * HD Space Addon Info
   * Returns available hard drive space addon options and pricing for the VPS.
   * @param params.id VPS ID number
   */
  async getVpsBuyHdSpace(params: IGetVpsBuyHdSpaceParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('getVpsBuyHdSpace', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/buy_hd_space`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Additional IP Addon Info
   * Returns available additional IP address options and pricing for the VPS.
   * @param params.id VPS ID number
   */
  async getVpsBuyIp(params: IGetVpsBuyIpParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('getVpsBuyIp', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/buy_ip`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Timezone Info
   * Returns the list of available timezones that can be set on the VPS.
   * @param params.id VPS ID number
   */
  async getVpsChangeTimezone(params: IGetVpsChangeTimezoneParams): Promise<Array<string>> {
    // Verify required parameters are set
    this.ensureParamIsSet('getVpsChangeTimezone', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/change_timezone`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get VPS Order
   * Returns detailed information about a specific VPS including its IPs, hostname, OS, and resource allocation.
   * @param params.id VPS ID number
   */
  async getVpsInfo(params: IGetVpsInfoParams): Promise<Vps> {
    // Verify required parameters are set
    this.ensureParamIsSet('getVpsInfo', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get VPS Invoices
   * Returns the billing invoices associated with this VPS.
   * @param params.id VPS ID number
   */
  async getVpsInvoices(params: IGetVpsInvoicesParams): Promise<ChargeInvoiceRows> {
    // Verify required parameters are set
    this.ensureParamIsSet('getVpsInvoices', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/invoices`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List VPS Orders
   * Returns all VPS services on the account with their current status and configuration.
   */
  async getVpsList(): Promise<Array<VpsRow>> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/vps`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * VPS Reinstall OS Options
   * Returns the list of available OS templates for reinstalling the VPS.
   * @param params.id VPS ID number
   */
  async getVpsReinstallOs(params: IGetVpsReinstallOsParams): Promise<VpsTemplatesList> {
    // Verify required parameters are set
    this.ensureParamIsSet('getVpsReinstallOs', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/reinstall_os`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Reverse DNS Info
   * Returns the current reverse DNS (PTR record) entries for the VPS IP addresses.
   * @param params.id VPS ID number
   */
  async getVpsReverseDns(params: IGetVpsReverseDnsParams): Promise<ReverseDnsEntries> {
    // Verify required parameters are set
    this.ensureParamIsSet('getVpsReverseDns', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/reverse_dns`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * VNC Setup Info
   * Returns the current VNC connection information for the VPS.
   * @param params.id VPS ID number
   */
  async getVpsSetupVnc(params: IGetVpsSetupVncParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('getVpsSetupVnc', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/setup_vnc`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Slice Upgrade Info
   * Returns available slice upgrade options and pricing for the VPS.
   * @param params.id VPS ID number
   */
  async getVpsSlices(params: IGetVpsSlicesParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('getVpsSlices', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/slices`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Traffic Usage
   * Returns bandwidth traffic usage data for the VPS.
   * @param params.id VPS ID number
   */
  async getVpsTrafficUsage(params: IGetVpsTrafficUsageParams): Promise<VpsTrafficResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('getVpsTrafficUsage', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/traffic_usage`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get View Desktop Info
   * Returns remote desktop connection information for the VPS.
   * @param params.id VPS ID number
   */
  async getVpsViewDesktop(params: IGetVpsViewDesktopParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('getVpsViewDesktop', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/view_desktop`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Resend VPS Welcome Email
   * Resends the welcome email containing connection details and credentials for the VPS order.
   * @param params.id VPS ID
   */
  async getVpsWelcomeEmail(params: IGetVpsWelcomeEmailParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('getVpsWelcomeEmail', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/welcome_email`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Start a VPS Backup
   * Creates a backup of the VPS. The backup can be downloaded or restored later via the backups endpoints.
   * @param params.id VPS ID number
   */
  async postVpsBackup(params: IPostVpsBackupParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postVpsBackup', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/backup`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Purchase HD Space Addon
   * Purchases additional hard drive space for the VPS.
   * @param params.id VPS ID number
   */
  async postVpsBuyHdSpace(params: IPostVpsBuyHdSpaceParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('postVpsBuyHdSpace', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/buy_hd_space`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Purchase Additional IP
   * Purchases an additional IP address for the VPS.
   * @param params.id VPS ID number
   */
  async postVpsBuyIp(params: IPostVpsBuyIpParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('postVpsBuyIp', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/buy_ip`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update VPS Hostname
   * Changes the hostname of the VPS. This queues a background task to update the server.
   * @param params.id VPS ID number
   * @param params.hostname 
   */
  async postVpsChangeHostname(params: IPostVpsChangeHostnameParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postVpsChangeHostname', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/change_hostname`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode form parameters
      .withHeader('content-type', 'application/x-www-form-urlencoded')
      .withContent(this.queryString({ 
        'hostname': params['hostname'],
      }))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Change VPS Root Password
   * Changes the root password of the VPS.
   * @param params.id VPS ID number
   * @param params.password 
   */
  async postVpsChangeRootPassword(params: IPostVpsChangeRootPasswordParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postVpsChangeRootPassword', params, 'id');
    this.ensureParamIsSet('postVpsChangeRootPassword', params, 'password');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/change_root_password`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode form parameters
      .withHeader('content-type', 'application/x-www-form-urlencoded')
      .withContent(this.queryString({ 
        'password': params['password'],
      }))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Change VPS Timezone
   * Changes the system timezone on the VPS.
   * @param params.id VPS ID number
   * @param params.timezone The time zone
   */
  async postVpsChangeTimezone(params: IPostVpsChangeTimezoneParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postVpsChangeTimezone', params, 'id');
    this.ensureParamIsSet('postVpsChangeTimezone', params, 'timezone');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/change_timezone`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode form parameters
      .withHeader('content-type', 'application/x-www-form-urlencoded')
      .withContent(this.queryString({ 
        'timezone': params['timezone'],
      }))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Change Webuzo Password
   * Changes the Webuzo control panel password on the VPS.
   * @param params.id VPS ID number
   * @param params.password 
   */
  async postVpsChangeWebuzoPassword(params: IPostVpsChangeWebuzoPasswordParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postVpsChangeWebuzoPassword', params, 'id');
    this.ensureParamIsSet('postVpsChangeWebuzoPassword', params, 'password');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/change_webuzo_password`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode form parameters
      .withHeader('content-type', 'application/x-www-form-urlencoded')
      .withContent(this.queryString({ 
        'password': params['password'],
      }))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Insert CD in VPS
   * Mounts an ISO image in the VPS virtual CD drive from the given URL.
   * @param params.id VPS ID number
   * @param params.url 
   */
  async postVpsInsertCd(params: IPostVpsInsertCdParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postVpsInsertCd', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/insert_cd`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode form parameters
      .withHeader('content-type', 'application/x-www-form-urlencoded')
      .withContent(this.queryString({ 
        'url': params['url'],
      }))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Reinstall VPS OS
   * Reinstalls the operating system on the VPS. Warning - all data will be erased.
   * @param params.id VPS ID number
   * @param params.template OS Template Filename
   * @param params.localPassword Password for this account.
   * @param params.password Password for Root / Administrator Account.
   */
  async postVpsReinstallOs(params: IPostVpsReinstallOsParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postVpsReinstallOs', params, 'id');
    this.ensureParamIsSet('postVpsReinstallOs', params, 'template');
    this.ensureParamIsSet('postVpsReinstallOs', params, 'localPassword');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/reinstall_os`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode form parameters
      .withHeader('content-type', 'application/x-www-form-urlencoded')
      .withContent(this.queryString({ 
        'template': params['template'],
        'password': params['password'],
        'localPassword': params['localPassword'],
      }))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Reset VPS Password
   * Resets the root password on the VPS to a new randomly generated password.
   * @param params.id VPS ID number
   */
  async postVpsResetPassword(params: IPostVpsResetPasswordParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postVpsResetPassword', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/reset_password`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Restore VPS from Backup
   * Initiates a restore of the VPS from a previously created backup. The restore operation overwrites the current disk contents. Use &#x60;GET /vps/{id}/backups&#x60; to retrieve available backup names.
   * @param params.id VPS ID number
   * @param params.restoreRequest VPS Restore request
   */
  async postVpsRestore(params: IPostVpsRestoreParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postVpsRestore', params, 'id');
    this.ensureParamIsSet('postVpsRestore', params, 'restoreRequest');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/restore`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['restoreRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update Reverse DNS
   * Updates the reverse DNS (PTR record) entries for the VPS IP addresses.
   * @param params.id VPS ID number
   * @param params.reverseDnsEntries 
   */
  async postVpsReverseDns(params: IPostVpsReverseDnsParams): Promise<TextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postVpsReverseDns', params, 'id');
    this.ensureParamIsSet('postVpsReverseDns', params, 'reverseDnsEntries');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/reverse_dns`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['reverseDnsEntries'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Setup VNC
   * Sets up or refreshes the VNC console connection for the VPS.
   * @param params.id VPS ID number
   */
  async postVpsSetupVnc(params: IPostVpsSetupVncParams): Promise<QueueResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postVpsSetupVnc', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/setup_vnc`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Purchase Slice Upgrade
   * Purchases a slice upgrade for the VPS to increase CPU, RAM, and disk resources.
   * @param params.id VPS ID number
   */
  async postVpsSlices(params: IPostVpsSlicesParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('postVpsSlices', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/slices`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update View Desktop
   * Updates or refreshes the remote desktop session for the VPS.
   * @param params.id VPS ID number
   */
  async postVpsViewDesktop(params: IPostVpsViewDesktopParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('postVpsViewDesktop', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}/view_desktop`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Validate VPS Order
   * Validates a VPS order before placing it. Returns cost breakdown and any validation errors.
   * @param params.vpsOrderPutRequest 
   */
  async putVps(params: IPutVpsParams): Promise<VpsOrderPutResponse> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/vps/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPut()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['vpsOrderPutRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update VPS Order
   * Updates settings on a VPS order.
   * @param params.id VPS ID number.
   */
  async updateVpsInfo(params: IUpdateVpsInfoParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateVpsInfo', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Cancel VPS Service
   * Cancels the VPS service. The server will be deprovisioned and billing will stop at the end of the current billing cycle.
   * @param params.id VPS ID number
   */
  async vPSCancel(params: IVPSCancelParams): Promise<VPSCancel200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('vPSCancel', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/vps/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

