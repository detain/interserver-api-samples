/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  GetWebsiteBuyIp200Response,
  ChargeInvoiceRows,
  WebhostingCancel200Response,
  WebsiteLoginResponse,
  SuccessTextResponse,
  PostWebsiteMigration200Response,
  Website,
  TextResponse,
  WebsiteRow,
  ReverseDnsEntries,
  PostWebsiteBuyIpRequest,
  PostWebsiteMigrationRequest,
  WebsitesOrder,
  WebsiteBackups,
  PostWebsiteBuyIp200Response,
} from './models';

/**
 * addWebsite - parameters interface
 */
export interface IAddWebsiteParams {
}

/**
 * getNewWebsite - parameters interface
 */
export interface IGetNewWebsiteParams {
}

/**
 * getWebsiteBuyIp - parameters interface
 */
export interface IGetWebsiteBuyIpParams {
  id: number;
}

/**
 * getWebsiteInfo - parameters interface
 */
export interface IGetWebsiteInfoParams {
  id: number;
}

/**
 * getWebsiteInvoices - parameters interface
 */
export interface IGetWebsiteInvoicesParams {
  id: number;
}

/**
 * getWebsiteList - parameters interface
 */
export interface IGetWebsiteListParams {
}

/**
 * getWebsitesBackups - parameters interface
 */
export interface IGetWebsitesBackupsParams {
  id: number;
}

/**
 * getWebsitesLogin - parameters interface
 */
export interface IGetWebsitesLoginParams {
  id: number;
}

/**
 * getWebsitesWelcomeEmail - parameters interface
 */
export interface IGetWebsitesWelcomeEmailParams {
  id: number;
}

/**
 * gettWebsiteReverseDns - parameters interface
 */
export interface IGettWebsiteReverseDnsParams {
  id: number;
}

/**
 * postWebsiteBuyIp - parameters interface
 */
export interface IPostWebsiteBuyIpParams {
  id: number;
  postWebsiteBuyIpRequest: PostWebsiteBuyIpRequest;
}

/**
 * postWebsiteMigration - parameters interface
 */
export interface IPostWebsiteMigrationParams {
  id: number;
  postWebsiteMigrationRequest: PostWebsiteMigrationRequest;
}

/**
 * postWebsitesReverseDns - parameters interface
 */
export interface IPostWebsitesReverseDnsParams {
  id: number;
  reverseDnsEntries: ReverseDnsEntries;
}

/**
 * putWebsites - parameters interface
 */
export interface IPutWebsitesParams {
}

/**
 * updateWebsiteInfo - parameters interface
 */
export interface IUpdateWebsiteInfoParams {
  id: string;
}

/**
 * webhostingCancel - parameters interface
 */
export interface IWebhostingCancelParams {
  id: string;
}

/**
 * WebhostingApi - API class
 */
@autoinject()
export class WebhostingApi extends Api {

  /**
   * Creates a new WebhostingApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Place Website Order
   * Places an order for a new webhosting package. Use &#x60;PUT /websites/order&#x60; to validate the order first.
   */
  async addWebsite(): Promise<any> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/websites/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Website Ordering Information
   * Retrieves available webhosting plans and pricing for ordering.
   */
  async getNewWebsite(): Promise<WebsitesOrder> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/websites/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Website IP Information
   * Returns the IP addresses assigned to the website along with their current reverse DNS hostnames. Use this information to review assignments before updating reverse DNS via &#x60;POST /websites/{id}/buy_ip&#x60;.
   * @param params.id The website service ID. Use &#x60;website_id&#x60; from &#x60;GET /websites&#x60;.
   */
  async getWebsiteBuyIp(params: IGetWebsiteBuyIpParams): Promise<GetWebsiteBuyIp200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('getWebsiteBuyIp', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/websites/{id}/buy_ip`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Website Order
   * Returns detailed information about a specific webhosting order including its domain, plan, and status.
   * @param params.id The website service ID. Use &#x60;website_id&#x60; from &#x60;GET /websites&#x60;.
   */
  async getWebsiteInfo(params: IGetWebsiteInfoParams): Promise<Website> {
    // Verify required parameters are set
    this.ensureParamIsSet('getWebsiteInfo', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/websites/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Website Invoices
   * Returns the billing invoices associated with this webhosting service.
   * @param params.id The website service ID. Use &#x60;website_id&#x60; from &#x60;GET /websites&#x60;.
   */
  async getWebsiteInvoices(params: IGetWebsiteInvoicesParams): Promise<ChargeInvoiceRows> {
    // Verify required parameters are set
    this.ensureParamIsSet('getWebsiteInvoices', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/websites/{id}/invoices`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Website Listing
   * Gets a listing of your webhosting orders and service details.
   */
  async getWebsiteList(): Promise<Array<WebsiteRow>> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/websites`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Website Backups
   * Gets a list of the backups that exist for a website and their sizes.
   * @param params.id The website service ID. Use &#x60;website_id&#x60; from &#x60;GET /websites&#x60;.
   */
  async getWebsitesBackups(params: IGetWebsitesBackupsParams): Promise<WebsiteBackups> {
    // Verify required parameters are set
    this.ensureParamIsSet('getWebsitesBackups', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/websites/{id}/backups`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Hosting Panel Auto Login
   * Returns an auto-login URL for the webhosting control panel. Use this to access cPanel or DirectAdmin without entering credentials.
   * @param params.id The website service ID. Use &#x60;website_id&#x60; from &#x60;GET /websites&#x60;.
   */
  async getWebsitesLogin(params: IGetWebsitesLoginParams): Promise<WebsiteLoginResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('getWebsitesLogin', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/websites/{id}/login`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Resend Website Welcome Email
   * Resends the welcome email containing hosting credentials and panel access details for the webhosting order.
   * @param params.id The website service ID. Use &#x60;website_id&#x60; from &#x60;GET /websites&#x60;.
   */
  async getWebsitesWelcomeEmail(params: IGetWebsitesWelcomeEmailParams): Promise<SuccessTextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('getWebsitesWelcomeEmail', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/websites/{id}/welcome_email`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get Website Reverse DNS
   * Returns the current reverse DNS (PTR record) entries for the website\&#39;s IP addresses.
   * @param params.id The website service ID. Use &#x60;website_id&#x60; from &#x60;GET /websites&#x60;.
   */
  async gettWebsiteReverseDns(params: IGettWebsiteReverseDnsParams): Promise<ReverseDnsEntries> {
    // Verify required parameters are set
    this.ensureParamIsSet('gettWebsiteReverseDns', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/websites/{id}/reverse_dns`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update Website IP DNS
   * Updates the reverse DNS hostnames for the website\&#39;s IP addresses. Provide an &#x60;ips&#x60; object mapping each IP address to its desired hostname.
   * @param params.id The website service ID. Use &#x60;website_id&#x60; from &#x60;GET /websites&#x60;.
   * @param params.postWebsiteBuyIpRequest 
   */
  async postWebsiteBuyIp(params: IPostWebsiteBuyIpParams): Promise<PostWebsiteBuyIp200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('postWebsiteBuyIp', params, 'id');
    this.ensureParamIsSet('postWebsiteBuyIp', params, 'postWebsiteBuyIpRequest');

    // Create URL to call
    const url = `${this.basePath}/websites/{id}/buy_ip`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['postWebsiteBuyIpRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Request Website Migration
   * Submits a website migration request from your current hosting provider to InterServer. Provide the credentials and details for your current host so our team can perform the migration. A support ticket is created to track the migration progress; use the returned &#x60;ticket&#x60; ID with &#x60;/tickets/{id}&#x60; to monitor status.
   * @param params.id The website service ID. Use &#x60;website_id&#x60; from &#x60;GET /websites&#x60;.
   * @param params.postWebsiteMigrationRequest 
   */
  async postWebsiteMigration(params: IPostWebsiteMigrationParams): Promise<PostWebsiteMigration200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('postWebsiteMigration', params, 'id');
    this.ensureParamIsSet('postWebsiteMigration', params, 'postWebsiteMigrationRequest');

    // Create URL to call
    const url = `${this.basePath}/websites/{id}/migration`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['postWebsiteMigrationRequest'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update Website Reverse DNS
   * Updates the reverse DNS entries for each of the IP addresses for the website.
   * @param params.id The website service ID. Use &#x60;website_id&#x60; from &#x60;GET /websites&#x60;.
   * @param params.reverseDnsEntries 
   */
  async postWebsitesReverseDns(params: IPostWebsitesReverseDnsParams): Promise<TextResponse> {
    // Verify required parameters are set
    this.ensureParamIsSet('postWebsitesReverseDns', params, 'id');
    this.ensureParamIsSet('postWebsitesReverseDns', params, 'reverseDnsEntries');

    // Create URL to call
    const url = `${this.basePath}/websites/{id}/reverse_dns`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['reverseDnsEntries'] || {}))

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Validate Webhosting Order
   * Validates a webhosting order before placing it.
   */
  async putWebsites(): Promise<any> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/websites/order`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPut()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update Website Order
   * Updates settings on a webhosting order.
   * @param params.id The website service ID. Use &#x60;website_id&#x60; from &#x60;GET /websites&#x60;.
   */
  async updateWebsiteInfo(params: IUpdateWebsiteInfoParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('updateWebsiteInfo', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/websites/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Cancel Website
   * Cancels a webhosting service. The service will be scheduled for termination and all hosted content will be removed. This action cannot be undone.
   * @param params.id The website service ID. Use &#x60;website_id&#x60; from &#x60;GET /websites&#x60;.
   */
  async webhostingCancel(params: IWebhostingCancelParams): Promise<WebhostingCancel200Response> {
    // Verify required parameters are set
    this.ensureParamIsSet('webhostingCancel', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/websites/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asDelete()

      // Authentication 'sessionIdCookieAuth' required
      // Authentication 'apiKeyAuth' required
      .withHeader('X-API-KEY', this.authStorage.getapiKeyAuth())
      // Authentication 'sessionIdHeaderAuth' required
      .withHeader('sessionid', this.authStorage.getsessionIdHeaderAuth())
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

