# #InterServer Management API
#
### Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
#
#The version of the OpenAPI document: 0.9.0
#Contact: support@interserver.net
#Generated by: https://openapi-generator.tech
#Generator version: 7.21.0
#

module OpenAPIClient
  # request to validate a vps order
  class VpsOrderPostRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # OS Distribution
    @[JSON::Field(key: "osDistro", type: String, nillable: false, emit_null: false)]
    property os_distro : String

    # Number of slices
    @[JSON::Field(key: "slices", type: Int32, default: 1, nillable: false, emit_null: false)]
    property slices : Int32

    # VPS Platform
    @[JSON::Field(key: "vpsPlatform", type: String, nillable: false, emit_null: false)]
    property vps_platform : String

    # Billing Period or Frequency
    @[JSON::Field(key: "period", type: Int32, default: 1, nillable: false, emit_null: false)]
    property period : Int32

    # Location
    @[JSON::Field(key: "location", type: Int32, default: 1, nillable: false, emit_null: false)]
    property location : Int32

    # OS Version
    @[JSON::Field(key: "osVersion", type: String, nillable: false, emit_null: false)]
    property os_version : String

    # The hostname to assign to the VPS
    @[JSON::Field(key: "hostname", type: String, default: "", nillable: false, emit_null: false)]
    property hostname : String

    # Root password to assign to the VVPS
    @[JSON::Field(key: "rootpass", type: String, nillable: false, emit_null: false)]
    property rootpass : String

    # Optional properties
    # Control Panel
    @[JSON::Field(key: "controlpanel", type: String?, nillable: true, emit_null: false)]
    property controlpanel : String?

    # Coupon
    @[JSON::Field(key: "coupon", type: String?, default: "", nillable: true, emit_null: false)]
    property coupon : String?

    # Order comments or notes
    @[JSON::Field(key: "comment", type: String?, default: "", nillable: true, emit_null: false)]
    property comment : String?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForVpsPlatform < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "vps_platform"
        @allowable_values = ["kvm", "hyperv", "kvmstorage"].map { |value| to(String, value)}
      end
    end

    class EnumAttributeValidatorForControlpanel < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "controlpanel"
        @allowable_values = ["none", "cpanel", "da"].map { |value| to(String, value)}
      end
    end


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@os_distro : String, @slices : Int32, @vps_platform : String, @period : Int32, @location : Int32, @os_version : String, @hostname : String, @rootpass : String, @controlpanel : String? = nil, @coupon : String? = nil, @comment : String? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @slices.try &.> 32
        invalid_properties.push("invalid value for \"slices\", must be smaller than or equal to 32.")
      end

      if @slices.try &.< 1
        invalid_properties.push("invalid value for \"slices\", must be greater than or equal to 1.")
      end

      vps_platform_validator = EnumAttributeValidatorForVpsPlatform.new
      if !vps_platform_validator.valid?(@vps_platform)
        message = vps_platform_validator.message
        invalid_properties.push(message)
      end

      if @period.try &.> 12
        invalid_properties.push("invalid value for \"period\", must be smaller than or equal to 12.")
      end

      if @period.try &.< 1
        invalid_properties.push("invalid value for \"period\", must be greater than or equal to 1.")
      end

      if @location.try &.> 3
        invalid_properties.push("invalid value for \"location\", must be smaller than or equal to 3.")
      end

      if @location.try &.< 1
        invalid_properties.push("invalid value for \"location\", must be greater than or equal to 1.")
      end

      controlpanel_validator = EnumAttributeValidatorForControlpanel.new
      if !controlpanel_validator.valid?(@controlpanel)
        message = controlpanel_validator.message
        invalid_properties.push(message)
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @slices.try &.> 32
      return false if @slices.try &.< 1
      vps_platform_validator = EnumAttributeValidatorForVpsPlatform.new
      return false unless vps_platform_validator.valid?(@vps_platform)
      return false if @period.try &.> 12
      return false if @period.try &.< 1
      return false if @location.try &.> 3
      return false if @location.try &.< 1
      controlpanel_validator = EnumAttributeValidatorForControlpanel.new
      return false unless controlpanel_validator.valid?(@controlpanel)
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] slices Value to be assigned
    def slices=(slices)
      if slices > 32
        raise ArgumentError.new("invalid value for \"slices\", must be smaller than or equal to 32.")
      end

      if slices < 1
        raise ArgumentError.new("invalid value for \"slices\", must be greater than or equal to 1.")
      end

      @slices = slices
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] vps_platform Object to be assigned
    def vps_platform=(vps_platform)
      validator = EnumAttributeValidatorForVpsPlatform.new
      unless validator.valid?(vps_platform)
        raise ArgumentError.new(validator.message)
      end
      @vps_platform = vps_platform
    end

    # Custom attribute writer method with validation
    # @param [Object] period Value to be assigned
    def period=(period)
      if period > 12
        raise ArgumentError.new("invalid value for \"period\", must be smaller than or equal to 12.")
      end

      if period < 1
        raise ArgumentError.new("invalid value for \"period\", must be greater than or equal to 1.")
      end

      @period = period
    end

    # Custom attribute writer method with validation
    # @param [Object] location Value to be assigned
    def location=(location)
      if location > 3
        raise ArgumentError.new("invalid value for \"location\", must be smaller than or equal to 3.")
      end

      if location < 1
        raise ArgumentError.new("invalid value for \"location\", must be greater than or equal to 1.")
      end

      @location = location
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] controlpanel Object to be assigned
    def controlpanel=(controlpanel)
      validator = EnumAttributeValidatorForControlpanel.new
      unless validator.valid?(controlpanel)
        raise ArgumentError.new(validator.message)
      end
      @controlpanel = controlpanel
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          os_distro == other.os_distro &&
          slices == other.slices &&
          vps_platform == other.vps_platform &&
          period == other.period &&
          location == other.location &&
          os_version == other.os_version &&
          hostname == other.hostname &&
          rootpass == other.rootpass &&
          controlpanel == other.controlpanel &&
          coupon == other.coupon &&
          comment == other.comment
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [os_distro, slices, vps_platform, period, location, os_version, hostname, rootpass, controlpanel, coupon, comment].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["osDistro"] = _to_h(os_distro)
      hash["slices"] = _to_h(slices)
      hash["vpsPlatform"] = _to_h(vps_platform)
      hash["period"] = _to_h(period)
      hash["location"] = _to_h(location)
      hash["osVersion"] = _to_h(os_version)
      hash["hostname"] = _to_h(hostname)
      hash["rootpass"] = _to_h(rootpass)
      hash["controlpanel"] = _to_h(controlpanel)
      hash["coupon"] = _to_h(coupon)
      hash["comment"] = _to_h(comment)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
