# #InterServer Management API
#
### Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
#
#The version of the OpenAPI document: 0.9.0
#Contact: support@interserver.net
#Generated by: https://openapi-generator.tech
#Generator version: 7.21.0
#

module OpenAPIClient
  # Core service record for a backup storage service including ID, status, quota, and billing details.
  class BackupServiceInfo
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    # Backup ID.
    @[JSON::Field(key: "backup_id", type: String?, nillable: true, emit_null: false)]
    property backup_id : String?

    # Backup server ID.
    @[JSON::Field(key: "backup_server", type: String?, nillable: true, emit_null: false)]
    property backup_server : String?

    # Backup username.
    @[JSON::Field(key: "backup_username", type: String?, nillable: true, emit_null: false)]
    property backup_username : String?

    # Backup type.
    @[JSON::Field(key: "backup_type", type: String?, nillable: true, emit_null: false)]
    property backup_type : String?

    # Backup currency.
    @[JSON::Field(key: "backup_currency", type: String?, nillable: true, emit_null: false)]
    property backup_currency : String?

    # Backup order date.
    @[JSON::Field(key: "backup_order_date", type: String?, nillable: true, emit_null: false)]
    property backup_order_date : String?

    # Backup customer ID.
    @[JSON::Field(key: "backup_custid", type: String?, nillable: true, emit_null: false)]
    property backup_custid : String?

    # Backup quota.
    @[JSON::Field(key: "backup_quota", type: String?, nillable: true, emit_null: false)]
    property backup_quota : String?

    # Backup IP address.
    @[JSON::Field(key: "backup_ip", type: String?, nillable: true, emit_null: false)]
    property backup_ip : String?

    # Backup status.
    @[JSON::Field(key: "backup_status", type: String?, nillable: true, emit_null: false)]
    property backup_status : String?

    # Backup invoice.
    @[JSON::Field(key: "backup_invoice", type: String?, nillable: true, emit_null: false)]
    property backup_invoice : String?

    # Backup coupon.
    @[JSON::Field(key: "backup_coupon", type: String?, nillable: true, emit_null: false)]
    property backup_coupon : String?

    # Backup extra information.
    @[JSON::Field(key: "backup_extra", type: String?, nillable: true, emit_null: false)]
    property backup_extra : String?

    # Backup server status.
    @[JSON::Field(key: "backup_server_status", type: String?, nillable: true, emit_null: false)]
    property backup_server_status : String?

    # Backup comment.
    @[JSON::Field(key: "backup_comment", type: String?, nillable: true, emit_null: false)]
    property backup_comment : String?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@backup_id : String? = nil, @backup_server : String? = nil, @backup_username : String? = nil, @backup_type : String? = nil, @backup_currency : String? = nil, @backup_order_date : String? = nil, @backup_custid : String? = nil, @backup_quota : String? = nil, @backup_ip : String? = nil, @backup_status : String? = nil, @backup_invoice : String? = nil, @backup_coupon : String? = nil, @backup_extra : String? = nil, @backup_server_status : String? = nil, @backup_comment : String? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      true
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          backup_id == other.backup_id &&
          backup_server == other.backup_server &&
          backup_username == other.backup_username &&
          backup_type == other.backup_type &&
          backup_currency == other.backup_currency &&
          backup_order_date == other.backup_order_date &&
          backup_custid == other.backup_custid &&
          backup_quota == other.backup_quota &&
          backup_ip == other.backup_ip &&
          backup_status == other.backup_status &&
          backup_invoice == other.backup_invoice &&
          backup_coupon == other.backup_coupon &&
          backup_extra == other.backup_extra &&
          backup_server_status == other.backup_server_status &&
          backup_comment == other.backup_comment
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [backup_id, backup_server, backup_username, backup_type, backup_currency, backup_order_date, backup_custid, backup_quota, backup_ip, backup_status, backup_invoice, backup_coupon, backup_extra, backup_server_status, backup_comment].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["backup_id"] = _to_h(backup_id)
      hash["backup_server"] = _to_h(backup_server)
      hash["backup_username"] = _to_h(backup_username)
      hash["backup_type"] = _to_h(backup_type)
      hash["backup_currency"] = _to_h(backup_currency)
      hash["backup_order_date"] = _to_h(backup_order_date)
      hash["backup_custid"] = _to_h(backup_custid)
      hash["backup_quota"] = _to_h(backup_quota)
      hash["backup_ip"] = _to_h(backup_ip)
      hash["backup_status"] = _to_h(backup_status)
      hash["backup_invoice"] = _to_h(backup_invoice)
      hash["backup_coupon"] = _to_h(backup_coupon)
      hash["backup_extra"] = _to_h(backup_extra)
      hash["backup_server_status"] = _to_h(backup_server_status)
      hash["backup_comment"] = _to_h(backup_comment)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
