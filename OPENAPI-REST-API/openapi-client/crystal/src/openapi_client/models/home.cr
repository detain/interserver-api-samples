# #InterServer Management API
#
### Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
#
#The version of the OpenAPI document: 0.9.0
#Contact: support@interserver.net
#Generated by: https://openapi-generator.tech
#Generator version: 7.21.0
#

module OpenAPIClient
  class Home
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # Last login IP.
    @[JSON::Field(key: "last_login_ip", type: String, nillable: false, emit_null: false)]
    property last_login_ip : String

    # Last login time.
    @[JSON::Field(key: "last_login", type: String, nillable: false, emit_null: false)]
    property last_login : String

    # Currency symbol.
    @[JSON::Field(key: "currency", type: String, nillable: false, emit_null: false)]
    property currency : String

    # Amount with currency.
    @[JSON::Field(key: "amount", type: String, nillable: false, emit_null: false)]
    property amount : String

    # Number of invoices.
    @[JSON::Field(key: "invoice_list", type: Int32, nillable: false, emit_null: false)]
    property invoice_list : Int32

    # Balance with currency.
    @[JSON::Field(key: "balance", type: String, nillable: false, emit_null: false)]
    property balance : String

    # Users full name.
    @[JSON::Field(key: "full_name", type: String, nillable: false, emit_null: false)]
    property full_name : String

    # User email address.
    @[JSON::Field(key: "email", type: String, nillable: false, emit_null: false)]
    property email : String

    # List of tickets.
    @[JSON::Field(key: "tickets", type: Array(String), nillable: false, emit_null: false)]
    property tickets : Array(String)

    @[JSON::Field(key: "ticketStatus", type: HomeTicketStatus, nillable: false, emit_null: false)]
    property ticket_status : HomeTicketStatus

    @[JSON::Field(key: "ticketStatusView", type: HomeTicketStatusView, nillable: false, emit_null: false)]
    property ticket_status_view : HomeTicketStatusView

    @[JSON::Field(key: "details", type: HomeDetails, nillable: false, emit_null: false)]
    property details : HomeDetails

    @[JSON::Field(key: "services", type: HomeServices, nillable: false, emit_null: false)]
    property services : HomeServices

    # Affiliate amount with currency.
    @[JSON::Field(key: "AFFILIATE_AMOUNT", type: String, nillable: false, emit_null: false)]
    property affiliate_amount : String

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@last_login_ip : String, @last_login : String, @currency : String, @amount : String, @invoice_list : Int32, @balance : String, @full_name : String, @email : String, @tickets : Array(String), @ticket_status : HomeTicketStatus, @ticket_status_view : HomeTicketStatusView, @details : HomeDetails, @services : HomeServices, @affiliate_amount : String)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      true
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          last_login_ip == other.last_login_ip &&
          last_login == other.last_login &&
          currency == other.currency &&
          amount == other.amount &&
          invoice_list == other.invoice_list &&
          balance == other.balance &&
          full_name == other.full_name &&
          email == other.email &&
          tickets == other.tickets &&
          ticket_status == other.ticket_status &&
          ticket_status_view == other.ticket_status_view &&
          details == other.details &&
          services == other.services &&
          affiliate_amount == other.affiliate_amount
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [last_login_ip, last_login, currency, amount, invoice_list, balance, full_name, email, tickets, ticket_status, ticket_status_view, details, services, affiliate_amount].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["last_login_ip"] = _to_h(last_login_ip)
      hash["last_login"] = _to_h(last_login)
      hash["currency"] = _to_h(currency)
      hash["amount"] = _to_h(amount)
      hash["invoice_list"] = _to_h(invoice_list)
      hash["balance"] = _to_h(balance)
      hash["full_name"] = _to_h(full_name)
      hash["email"] = _to_h(email)
      hash["tickets"] = _to_h(tickets)
      hash["ticketStatus"] = _to_h(ticket_status)
      hash["ticketStatusView"] = _to_h(ticket_status_view)
      hash["details"] = _to_h(details)
      hash["services"] = _to_h(services)
      hash["AFFILIATE_AMOUNT"] = _to_h(affiliate_amount)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
