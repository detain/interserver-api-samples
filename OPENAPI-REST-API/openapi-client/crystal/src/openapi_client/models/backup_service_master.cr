# #InterServer Management API
#
### Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
#
#The version of the OpenAPI document: 0.9.0
#Contact: support@interserver.net
#Generated by: https://openapi-generator.tech
#Generator version: 7.21.0
#

module OpenAPIClient
  class BackupServiceMaster
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    # Backup ID of the service master.
    @[JSON::Field(key: "backup_id", type: Int32?, nillable: true, emit_null: false)]
    property backup_id : Int32?

    # Name of the backup service.
    @[JSON::Field(key: "backup_name", type: String?, nillable: true, emit_null: false)]
    property backup_name : String?

    # IP address of the backup service.
    @[JSON::Field(key: "backup_ip", type: String?, nillable: true, emit_null: false)]
    property backup_ip : String?

    # Type of the backup service.
    @[JSON::Field(key: "backup_type", type: Int32?, nillable: true, emit_null: false)]
    property backup_type : Int32?

    # Size of the backup service's hard drive.
    @[JSON::Field(key: "backup_hdsize", type: Int32?, nillable: true, emit_null: false)]
    property backup_hdsize : Int32?

    # Amount of free space on the backup service's hard drive.
    @[JSON::Field(key: "backup_hdfree", type: Int32?, nillable: true, emit_null: false)]
    property backup_hdfree : Int32?

    # Last update timestamp of the backup service.
    @[JSON::Field(key: "backup_last_update", type: String?, nillable: true, emit_null: false)]
    property backup_last_update : String?

    # Availability status of the backup service.
    @[JSON::Field(key: "backup_available", type: Int32?, nillable: true, emit_null: false)]
    property backup_available : Int32?

    # I/O wait status of the backup service.
    @[JSON::Field(key: "backup_iowait", type: Int32?, nillable: true, emit_null: false)]
    property backup_iowait : Int32?

    # Order associated with the backup service.
    @[JSON::Field(key: "backup_order", type: Int32?, nillable: true, emit_null: false)]
    property backup_order : Int32?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@backup_id : Int32? = nil, @backup_name : String? = nil, @backup_ip : String? = nil, @backup_type : Int32? = nil, @backup_hdsize : Int32? = nil, @backup_hdfree : Int32? = nil, @backup_last_update : String? = nil, @backup_available : Int32? = nil, @backup_iowait : Int32? = nil, @backup_order : Int32? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      true
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          backup_id == other.backup_id &&
          backup_name == other.backup_name &&
          backup_ip == other.backup_ip &&
          backup_type == other.backup_type &&
          backup_hdsize == other.backup_hdsize &&
          backup_hdfree == other.backup_hdfree &&
          backup_last_update == other.backup_last_update &&
          backup_available == other.backup_available &&
          backup_iowait == other.backup_iowait &&
          backup_order == other.backup_order
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [backup_id, backup_name, backup_ip, backup_type, backup_hdsize, backup_hdfree, backup_last_update, backup_available, backup_iowait, backup_order].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["backup_id"] = _to_h(backup_id)
      hash["backup_name"] = _to_h(backup_name)
      hash["backup_ip"] = _to_h(backup_ip)
      hash["backup_type"] = _to_h(backup_type)
      hash["backup_hdsize"] = _to_h(backup_hdsize)
      hash["backup_hdfree"] = _to_h(backup_hdfree)
      hash["backup_last_update"] = _to_h(backup_last_update)
      hash["backup_available"] = _to_h(backup_available)
      hash["backup_iowait"] = _to_h(backup_iowait)
      hash["backup_order"] = _to_h(backup_order)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
