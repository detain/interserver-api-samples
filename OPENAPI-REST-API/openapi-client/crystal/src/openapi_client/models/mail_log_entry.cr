# #InterServer Management API
#
### Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
#
#The version of the OpenAPI document: 0.9.0
#Contact: support@interserver.net
#Generated by: https://openapi-generator.tech
#Generator version: 7.20.0
#

module OpenAPIClient
  # An email record
  class MailLogEntry
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # internal db id
    @[JSON::Field(key: "_id", type: Int32, nillable: false, emit_null: false)]
    property _id : Int32

    # mail id
    @[JSON::Field(key: "id", type: String, nillable: false, emit_null: false)]
    property id : String

    # from address
    @[JSON::Field(key: "from", type: String, nillable: false, emit_null: false)]
    property from : String

    # to address
    @[JSON::Field(key: "to", type: String, nillable: false, emit_null: false)]
    property to : String

    # email subject
    @[JSON::Field(key: "subject", type: String, nillable: false, emit_null: false)]
    property subject : String

    # creation date
    @[JSON::Field(key: "created", type: String, nillable: false, emit_null: false)]
    property created : String

    # creation timestamp
    @[JSON::Field(key: "time", type: Int32, nillable: false, emit_null: false)]
    property time : Int32

    # user account
    @[JSON::Field(key: "user", type: String, nillable: false, emit_null: false)]
    property user : String

    # transaction type
    @[JSON::Field(key: "transtype", type: String, nillable: false, emit_null: false)]
    property transtype : String

    # origin ip
    @[JSON::Field(key: "origin", type: String, nillable: false, emit_null: false)]
    property origin : String

    # interface name
    @[JSON::Field(key: "interface", type: String, nillable: false, emit_null: false)]
    property interface : String

    # sending zone
    @[JSON::Field(key: "sendingZone", type: String, nillable: false, emit_null: false)]
    property sending_zone : String

    # email body size in bytes
    @[JSON::Field(key: "bodySize", type: Int32, nillable: false, emit_null: false)]
    property body_size : Int32

    # index of email in the to adderess list
    @[JSON::Field(key: "seq", type: Int32, nillable: false, emit_null: false)]
    property seq : Int32

    # to address this email is being sent to
    @[JSON::Field(key: "recipient", type: String, nillable: false, emit_null: false)]
    property recipient : String

    # to address domain
    @[JSON::Field(key: "domain", type: String, nillable: false, emit_null: false)]
    property domain : String

    # locked status
    @[JSON::Field(key: "locked", type: Int32, nillable: false, emit_null: false)]
    property locked : Int32

    # lock timestamp
    @[JSON::Field(key: "lockTime", type: Int32, nillable: false, emit_null: false)]
    property lock_time : Int32

    # assigned server
    @[JSON::Field(key: "assigned", type: String, nillable: false, emit_null: false)]
    property assigned : String

    # queued timestamp
    @[JSON::Field(key: "queued", type: String, nillable: false, emit_null: false)]
    property queued : String

    # mx hostname
    @[JSON::Field(key: "mxHostname", type: String, nillable: false, emit_null: false)]
    property mx_hostname : String

    # mail delivery response
    @[JSON::Field(key: "response", type: String, nillable: false, emit_null: false)]
    property response : String

    # Optional properties
    # message id
    @[JSON::Field(key: "messageId", type: String?, nillable: true, emit_null: false)]
    property message_id : String?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@_id : Int32, @id : String, @from : String, @to : String, @subject : String, @created : String, @time : Int32, @user : String, @transtype : String, @origin : String, @interface : String, @sending_zone : String, @body_size : Int32, @seq : Int32, @recipient : String, @domain : String, @locked : Int32, @lock_time : Int32, @assigned : String, @queued : String, @mx_hostname : String, @response : String, @message_id : String? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      true
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          _id == other._id &&
          id == other.id &&
          from == other.from &&
          to == other.to &&
          subject == other.subject &&
          created == other.created &&
          time == other.time &&
          user == other.user &&
          transtype == other.transtype &&
          origin == other.origin &&
          interface == other.interface &&
          sending_zone == other.sending_zone &&
          body_size == other.body_size &&
          seq == other.seq &&
          recipient == other.recipient &&
          domain == other.domain &&
          locked == other.locked &&
          lock_time == other.lock_time &&
          assigned == other.assigned &&
          queued == other.queued &&
          mx_hostname == other.mx_hostname &&
          response == other.response &&
          message_id == other.message_id
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [_id, id, from, to, subject, created, time, user, transtype, origin, interface, sending_zone, body_size, seq, recipient, domain, locked, lock_time, assigned, queued, mx_hostname, response, message_id].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["_id"] = _to_h(_id)
      hash["id"] = _to_h(id)
      hash["from"] = _to_h(from)
      hash["to"] = _to_h(to)
      hash["subject"] = _to_h(subject)
      hash["created"] = _to_h(created)
      hash["time"] = _to_h(time)
      hash["user"] = _to_h(user)
      hash["transtype"] = _to_h(transtype)
      hash["origin"] = _to_h(origin)
      hash["interface"] = _to_h(interface)
      hash["sendingZone"] = _to_h(sending_zone)
      hash["bodySize"] = _to_h(body_size)
      hash["seq"] = _to_h(seq)
      hash["recipient"] = _to_h(recipient)
      hash["domain"] = _to_h(domain)
      hash["locked"] = _to_h(locked)
      hash["lockTime"] = _to_h(lock_time)
      hash["assigned"] = _to_h(assigned)
      hash["queued"] = _to_h(queued)
      hash["mxHostname"] = _to_h(mx_hostname)
      hash["response"] = _to_h(response)
      hash["messageId"] = _to_h(message_id)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
