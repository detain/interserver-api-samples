# #InterServer Management API
#
### Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
#
#The version of the OpenAPI document: 0.9.0
#Contact: support@interserver.net
#Generated by: https://openapi-generator.tech
#Generator version: 7.20.0
#

module OpenAPIClient
  # Schema for the WebsitesOrder object
  class WebsitesOrder
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # Step description
    @[JSON::Field(key: "step", type: String, nillable: false, emit_null: false)]
    property step : String

    # Website description
    @[JSON::Field(key: "website", type: String, nillable: false, emit_null: false)]
    property website : String

    # Period description
    @[JSON::Field(key: "period", type: Int32, nillable: false, emit_null: false)]
    property period : Int32

    # Service offer ID description
    @[JSON::Field(key: "serviceOfferId", type: Int32, nillable: false, emit_null: false)]
    property service_offer_id : Int32

    @[JSON::Field(key: "packages", type: WebsitesOrderPackages, nillable: false, emit_null: false)]
    property packages : WebsitesOrderPackages

    # Enable domain registering description
    @[JSON::Field(key: "enableDomainRegistering", type: Bool, nillable: false, emit_null: false)]
    property enable_domain_registering : Bool

    @[JSON::Field(key: "jsonServices", type: WebsitesOrderJsonServices, nillable: false, emit_null: false)]
    property json_services : WebsitesOrderJsonServices

    @[JSON::Field(key: "jsonServiceOffers", type: WebsitesOrderJsonServiceOffers, nillable: false, emit_null: false)]
    property json_service_offers : WebsitesOrderJsonServiceOffers

    @[JSON::Field(key: "serviceTypes", type: WebsitesOrderServiceTypes, nillable: false, emit_null: false)]
    property service_types : WebsitesOrderServiceTypes

    @[JSON::Field(key: "serviceOffers", type: WebsitesOrderServiceOffers, nillable: false, emit_null: false)]
    property service_offers : WebsitesOrderServiceOffers

    # Optional properties
    @[JSON::Field(key: "packges", type: WebsitesOrderPackges?, nillable: true, emit_null: false)]
    property packges : WebsitesOrderPackges?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@step : String, @website : String, @period : Int32, @service_offer_id : Int32, @packages : WebsitesOrderPackages, @enable_domain_registering : Bool, @json_services : WebsitesOrderJsonServices, @json_service_offers : WebsitesOrderJsonServiceOffers, @service_types : WebsitesOrderServiceTypes, @service_offers : WebsitesOrderServiceOffers, @packges : WebsitesOrderPackges? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      true
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          step == other.step &&
          website == other.website &&
          period == other.period &&
          service_offer_id == other.service_offer_id &&
          packages == other.packages &&
          enable_domain_registering == other.enable_domain_registering &&
          json_services == other.json_services &&
          json_service_offers == other.json_service_offers &&
          service_types == other.service_types &&
          service_offers == other.service_offers &&
          packges == other.packges
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [step, website, period, service_offer_id, packages, enable_domain_registering, json_services, json_service_offers, service_types, service_offers, packges].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["step"] = _to_h(step)
      hash["website"] = _to_h(website)
      hash["period"] = _to_h(period)
      hash["serviceOfferId"] = _to_h(service_offer_id)
      hash["packages"] = _to_h(packages)
      hash["enableDomainRegistering"] = _to_h(enable_domain_registering)
      hash["jsonServices"] = _to_h(json_services)
      hash["jsonServiceOffers"] = _to_h(json_service_offers)
      hash["serviceTypes"] = _to_h(service_types)
      hash["serviceOffers"] = _to_h(service_offers)
      hash["packges"] = _to_h(packges)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
