# #InterServer Management API
#
### Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
#
#The version of the OpenAPI document: 0.9.0
#Contact: support@interserver.net
#Generated by: https://openapi-generator.tech
#Generator version: 7.21.0
#

require "uri"

module OpenAPIClient
  class BillingApi
    property api_client : ApiClient

    def initialize(api_client = ApiClient.default)
      @api_client = api_client
    end
    # Add Credit Card to Account
    # Adds a new credit card to the account for billing. Card details are validated and stored securely. The card may require verification before it can be used as a payment method.
    # @return [SuccessTextResponse]
    def add_account_credit_card(name : String? = nil, address : String? = nil, city : String? = nil, state : String? = nil, country : String? = nil, zip : String? = nil, cc : String? = nil, cc_exp : String? = nil, cc_ccv2 : String? = nil)
      data, _status_code, _headers = add_account_credit_card_with_http_info(name, address, city, state, country, zip, cc, cc_exp, cc_ccv2)
      data
    end

    # Add Credit Card to Account
    # Adds a new credit card to the account for billing. Card details are validated and stored securely. The card may require verification before it can be used as a payment method.
    # @return [Array<(SuccessTextResponse, Integer, Hash)>] SuccessTextResponse data, response status code and response headers
    def add_account_credit_card_with_http_info(name : String? = nil, address : String? = nil, city : String? = nil, state : String? = nil, country : String? = nil, zip : String? = nil, cc : String? = nil, cc_exp : String? = nil, cc_ccv2 : String? = nil)
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.add_account_credit_card ..."}
      end
      # resource path
      local_var_path = "/account/creditcards"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["multipart/form-data", "application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new
      form_params[:"name"] = name unless name.nil?
      form_params[:"address"] = address unless address.nil?
      form_params[:"city"] = city unless city.nil?
      form_params[:"state"] = state unless state.nil?
      form_params[:"country"] = country unless country.nil?
      form_params[:"zip"] = zip unless zip.nil?
      form_params[:"cc"] = cc unless cc.nil?
      form_params[:"cc_exp"] = cc_exp unless cc_exp.nil?
      form_params[:"cc_ccv2"] = cc_ccv2 unless cc_ccv2.nil?

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :POST,
        path: local_var_path,
        operation: :"BillingApi.add_account_credit_card",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#add_account_credit_card\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return SuccessTextResponse.from_json(data), status_code, headers
    end

    # Add Credit Card for Billing
    # Adds a new credit card to the account for use as a payment method. If the card requires verification, the response indicates the next step. Complete verification via `GET /billing/creditcards/{id}/verify` followed by `POST /billing/creditcards/{id}/verify` before the card can be used for payments.
    # @param billing_add_cc_request [BillingAddCcRequest] 
    # @return [SuccessTextResponse]
    def add_billing_credit_card(billing_add_cc_request : BillingAddCcRequest)
      data, _status_code, _headers = add_billing_credit_card_with_http_info(billing_add_cc_request)
      data
    end

    # Add Credit Card for Billing
    # Adds a new credit card to the account for use as a payment method. If the card requires verification, the response indicates the next step. Complete verification via &#x60;GET /billing/creditcards/{id}/verify&#x60; followed by &#x60;POST /billing/creditcards/{id}/verify&#x60; before the card can be used for payments.
    # @param billing_add_cc_request [BillingAddCcRequest] 
    # @return [Array<(SuccessTextResponse, Integer, Hash)>] SuccessTextResponse data, response status code and response headers
    def add_billing_credit_card_with_http_info(billing_add_cc_request : BillingAddCcRequest)
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.add_billing_credit_card ..."}
      end
      # verify the required parameter "billing_add_cc_request" is set
      if @api_client.config.client_side_validation && billing_add_cc_request.nil?
        raise ArgumentError.new("Missing the required parameter 'billing_add_cc_request' when calling BillingApi.add_billing_credit_card")
      end
      # resource path
      local_var_path = "/billing/creditcards"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json", "multipart/form-data"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = billing_add_cc_request.to_json

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :POST,
        path: local_var_path,
        operation: :"BillingApi.add_billing_credit_card",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#add_billing_credit_card\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return SuccessTextResponse.from_json(data), status_code, headers
    end

    # Create Prepay Deposit
    # Creates a new prepay balance deposit and returns the invoice ID that must be paid to activate it. The prepay balance can then be used as a payment method for future orders. Use `/billing/invoices/{id}` to retrieve the generated invoice details.
    # @param billing_prepay_request [BillingPrepayRequest] 
    # @return [SuccessTextResponse]
    def add_billing_prepay(billing_prepay_request : BillingPrepayRequest)
      data, _status_code, _headers = add_billing_prepay_with_http_info(billing_prepay_request)
      data
    end

    # Create Prepay Deposit
    # Creates a new prepay balance deposit and returns the invoice ID that must be paid to activate it. The prepay balance can then be used as a payment method for future orders. Use &#x60;/billing/invoices/{id}&#x60; to retrieve the generated invoice details.
    # @param billing_prepay_request [BillingPrepayRequest] 
    # @return [Array<(SuccessTextResponse, Integer, Hash)>] SuccessTextResponse data, response status code and response headers
    def add_billing_prepay_with_http_info(billing_prepay_request : BillingPrepayRequest)
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.add_billing_prepay ..."}
      end
      # verify the required parameter "billing_prepay_request" is set
      if @api_client.config.client_side_validation && billing_prepay_request.nil?
        raise ArgumentError.new("Missing the required parameter 'billing_prepay_request' when calling BillingApi.add_billing_prepay")
      end
      # resource path
      local_var_path = "/billing/prepays"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json", "multipart/form-data"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = billing_prepay_request.to_json

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :POST,
        path: local_var_path,
        operation: :"BillingApi.add_billing_prepay",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#add_billing_prepay\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return SuccessTextResponse.from_json(data), status_code, headers
    end

    # Remove Credit Card
    # Removes a credit card from the account. If this is the default payment method, select a new default via `/billing/payment_method` afterward.
    # @param id [String] The credit card ID. Use the card ID returned from &#x60;POST /account/creditcards&#x60; or listed in &#x60;/billing/creditcards&#x60;.
    # @return [String]
    def delete_account_credit_card(id : String)
      data, _status_code, _headers = delete_account_credit_card_with_http_info(id)
      data
    end

    # Remove Credit Card
    # Removes a credit card from the account. If this is the default payment method, select a new default via &#x60;/billing/payment_method&#x60; afterward.
    # @param id [String] The credit card ID. Use the card ID returned from &#x60;POST /account/creditcards&#x60; or listed in &#x60;/billing/creditcards&#x60;.
    # @return [Array<(String, Integer, Hash)>] String data, response status code and response headers
    def delete_account_credit_card_with_http_info(id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.delete_account_credit_card ..."}
      end
      # verify the required parameter "id" is set
      if @api_client.config.client_side_validation && id.nil?
        raise ArgumentError.new("Missing the required parameter 'id' when calling BillingApi.delete_account_credit_card")
      end
      # resource path
      local_var_path = "/account/creditcards/{id}".sub("{" + "id" + "}", URI.encode_path(id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :DELETE,
        path: local_var_path,
        operation: :"BillingApi.delete_account_credit_card",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#delete_account_credit_card\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return String.from_json(data), status_code, headers
    end

    # Delete Credit Card
    # Removes the selected credit card from the account. Use `/billing/payment_method` to select a new default payment method after deleting a card.
    # @param id [Int32] The credit card ID to remove. Use IDs from &#x60;GET /billing/creditcards&#x60;.
    # @return [SuccessTextResponse]
    def delete_billing_credit_card(id : Int32)
      data, _status_code, _headers = delete_billing_credit_card_with_http_info(id)
      data
    end

    # Delete Credit Card
    # Removes the selected credit card from the account. Use &#x60;/billing/payment_method&#x60; to select a new default payment method after deleting a card.
    # @param id [Int32] The credit card ID to remove. Use IDs from &#x60;GET /billing/creditcards&#x60;.
    # @return [Array<(SuccessTextResponse, Integer, Hash)>] SuccessTextResponse data, response status code and response headers
    def delete_billing_credit_card_with_http_info(id : Int32)
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.delete_billing_credit_card ..."}
      end
      # verify the required parameter "id" is set
      if @api_client.config.client_side_validation && id.nil?
        raise ArgumentError.new("Missing the required parameter 'id' when calling BillingApi.delete_billing_credit_card")
      end
      # resource path
      local_var_path = "/billing/creditcards/{id}".sub("{" + "id" + "}", URI.encode_path(id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :DELETE,
        path: local_var_path,
        operation: :"BillingApi.delete_billing_credit_card",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#delete_billing_credit_card\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return SuccessTextResponse.from_json(data), status_code, headers
    end

    # Delete Invoice
    # Deletes a pending (unpaid) invoice from the account. Only invoices with a pending payment status can be deleted. Paid invoices cannot be removed. Related service records and repeat invoices are also cleaned up.
    # @param id [Int32] The invoice ID to delete. Only unpaid invoices can be deleted.
    # @return [SuccessTextResponse]
    def delete_billing_invoice(id : Int32)
      data, _status_code, _headers = delete_billing_invoice_with_http_info(id)
      data
    end

    # Delete Invoice
    # Deletes a pending (unpaid) invoice from the account. Only invoices with a pending payment status can be deleted. Paid invoices cannot be removed. Related service records and repeat invoices are also cleaned up.
    # @param id [Int32] The invoice ID to delete. Only unpaid invoices can be deleted.
    # @return [Array<(SuccessTextResponse, Integer, Hash)>] SuccessTextResponse data, response status code and response headers
    def delete_billing_invoice_with_http_info(id : Int32)
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.delete_billing_invoice ..."}
      end
      # verify the required parameter "id" is set
      if @api_client.config.client_side_validation && id.nil?
        raise ArgumentError.new("Missing the required parameter 'id' when calling BillingApi.delete_billing_invoice")
      end
      # resource path
      local_var_path = "/billing/invoices/{id}".sub("{" + "id" + "}", URI.encode_path(id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :DELETE,
        path: local_var_path,
        operation: :"BillingApi.delete_billing_invoice",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#delete_billing_invoice\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return SuccessTextResponse.from_json(data), status_code, headers
    end

    # Delete Prepay Balance
    # Deletes a prepay balance from the account. The balance must have remaining funds to be eligible for deletion. Use `GET /billing/prepays` to list available prepay balances and their IDs.
    # @param id [Int32] The prepay balance ID to delete.
    # @return [SuccessTextResponse]
    def delete_billing_prepay(id : Int32)
      data, _status_code, _headers = delete_billing_prepay_with_http_info(id)
      data
    end

    # Delete Prepay Balance
    # Deletes a prepay balance from the account. The balance must have remaining funds to be eligible for deletion. Use &#x60;GET /billing/prepays&#x60; to list available prepay balances and their IDs.
    # @param id [Int32] The prepay balance ID to delete.
    # @return [Array<(SuccessTextResponse, Integer, Hash)>] SuccessTextResponse data, response status code and response headers
    def delete_billing_prepay_with_http_info(id : Int32)
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.delete_billing_prepay ..."}
      end
      # verify the required parameter "id" is set
      if @api_client.config.client_side_validation && id.nil?
        raise ArgumentError.new("Missing the required parameter 'id' when calling BillingApi.delete_billing_prepay")
      end
      # resource path
      local_var_path = "/billing/prepays/{id}".sub("{" + "id" + "}", URI.encode_path(id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :DELETE,
        path: local_var_path,
        operation: :"BillingApi.delete_billing_prepay",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#delete_billing_prepay\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return SuccessTextResponse.from_json(data), status_code, headers
    end

    # List Affiliate Banner Assets
    # Returns the catalog of available affiliate banner images with their dimensions. Use these assets to build marketing creatives for your affiliate campaigns. Each banner includes the image filename, width, and height for layout purposes.
    # @return [Array(AffiliateBannerRow)]
    def get_affiliate_banners()
      data, _status_code, _headers = get_affiliate_banners_with_http_info()
      data
    end

    # List Affiliate Banner Assets
    # Returns the catalog of available affiliate banner images with their dimensions. Use these assets to build marketing creatives for your affiliate campaigns. Each banner includes the image filename, width, and height for layout purposes.
    # @return [Array<(Array(AffiliateBannerRow), Integer, Hash)>] Array(AffiliateBannerRow) data, response status code and response headers
    def get_affiliate_banners_with_http_info()
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.get_affiliate_banners ..."}
      end
      # resource path
      local_var_path = "/affiliate/banners"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"BillingApi.get_affiliate_banners",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#get_affiliate_banners\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return Array(AffiliateBannerRow).from_json(data), status_code, headers
    end

    # Get Affiliate Performance Report
    # Returns a detailed affiliate performance report with commission totals, conversion rates, and traffic summary. Use this for a comprehensive overview of your affiliate program performance in a single request.
    # @return [TextResponse]
    def get_affiliate_rich_report()
      data, _status_code, _headers = get_affiliate_rich_report_with_http_info()
      data
    end

    # Get Affiliate Performance Report
    # Returns a detailed affiliate performance report with commission totals, conversion rates, and traffic summary. Use this for a comprehensive overview of your affiliate program performance in a single request.
    # @return [Array<(TextResponse, Integer, Hash)>] TextResponse data, response status code and response headers
    def get_affiliate_rich_report_with_http_info()
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.get_affiliate_rich_report ..."}
      end
      # resource path
      local_var_path = "/affiliate/rich_report"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"BillingApi.get_affiliate_rich_report",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#get_affiliate_rich_report\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return TextResponse.from_json(data), status_code, headers
    end

    # Get Affiliate Sales Graph Data
    # Returns time-series sales data for the requested number of days. Use this to render sales trend charts in an affiliate dashboard. Each data point represents aggregated sales for a time period.
    # @return [StatusMonthlyBreakdown]
    def get_affiliate_sales_graph(days : Int32? = nil)
      data, _status_code, _headers = get_affiliate_sales_graph_with_http_info(days)
      data
    end

    # Get Affiliate Sales Graph Data
    # Returns time-series sales data for the requested number of days. Use this to render sales trend charts in an affiliate dashboard. Each data point represents aggregated sales for a time period.
    # @return [Array<(StatusMonthlyBreakdown, Integer, Hash)>] StatusMonthlyBreakdown data, response status code and response headers
    def get_affiliate_sales_graph_with_http_info(days : Int32? = nil)
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.get_affiliate_sales_graph ..."}
      end
      # resource path
      local_var_path = "/affiliate/sales_graph"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new
      query_params["days"] = days.to_s unless days.nil?

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"BillingApi.get_affiliate_sales_graph",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#get_affiliate_sales_graph\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return StatusMonthlyBreakdown.from_json(data), status_code, headers
    end

    # Get Affiliate Sales Report
    # Returns the affiliate sales report with commission amounts and order summaries. Use this for tabular sales data export or to reconcile commission payouts against individual referral orders.
    # @return [TextResponse]
    def get_affiliate_sales_report()
      data, _status_code, _headers = get_affiliate_sales_report_with_http_info()
      data
    end

    # Get Affiliate Sales Report
    # Returns the affiliate sales report with commission amounts and order summaries. Use this for tabular sales data export or to reconcile commission payouts against individual referral orders.
    # @return [Array<(TextResponse, Integer, Hash)>] TextResponse data, response status code and response headers
    def get_affiliate_sales_report_with_http_info()
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.get_affiliate_sales_report ..."}
      end
      # resource path
      local_var_path = "/affiliate/sales_report"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"BillingApi.get_affiliate_sales_report",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#get_affiliate_sales_report\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return TextResponse.from_json(data), status_code, headers
    end

    # Get Affiliate Traffic Graph Data
    # Returns time-series traffic data for the requested number of days. Use this to render click and visit trend charts in an affiliate dashboard. Each data point represents aggregated traffic counts for a time period.
    # @return [MonthlyCounts]
    def get_affiliate_traffic_graph(days : Int32? = nil)
      data, _status_code, _headers = get_affiliate_traffic_graph_with_http_info(days)
      data
    end

    # Get Affiliate Traffic Graph Data
    # Returns time-series traffic data for the requested number of days. Use this to render click and visit trend charts in an affiliate dashboard. Each data point represents aggregated traffic counts for a time period.
    # @return [Array<(MonthlyCounts, Integer, Hash)>] MonthlyCounts data, response status code and response headers
    def get_affiliate_traffic_graph_with_http_info(days : Int32? = nil)
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.get_affiliate_traffic_graph ..."}
      end
      # resource path
      local_var_path = "/affiliate/traffic_graph"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new
      query_params["days"] = days.to_s unless days.nil?

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"BillingApi.get_affiliate_traffic_graph",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#get_affiliate_traffic_graph\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return MonthlyCounts.from_json(data), status_code, headers
    end

    # List Affiliate Web Traffic Entries
    # Returns individual web traffic log entries for affiliate referrals, including visitor IP address, referral URL, and timestamp. Use this to audit traffic sources, identify top referrers, or investigate suspicious click patterns.
    # @return [Array(AffiliateTrafficRow)]
    def get_affiliate_web_traffic()
      data, _status_code, _headers = get_affiliate_web_traffic_with_http_info()
      data
    end

    # List Affiliate Web Traffic Entries
    # Returns individual web traffic log entries for affiliate referrals, including visitor IP address, referral URL, and timestamp. Use this to audit traffic sources, identify top referrers, or investigate suspicious click patterns.
    # @return [Array<(Array(AffiliateTrafficRow), Integer, Hash)>] Array(AffiliateTrafficRow) data, response status code and response headers
    def get_affiliate_web_traffic_with_http_info()
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.get_affiliate_web_traffic ..."}
      end
      # resource path
      local_var_path = "/affiliate/web_traffic"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"BillingApi.get_affiliate_web_traffic",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#get_affiliate_web_traffic\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return Array(AffiliateTrafficRow).from_json(data), status_code, headers
    end

    # Get Shopping Cart Contents
    # Returns the current cart contents, available payment methods, and checkout metadata for the authenticated account. Use this to display the cart page, show totals, and determine which payment options are available before directing the user to `/pay/{method}/{invoices}`.
    # @return [JSON::Any]
    def get_billing_cart()
      data, _status_code, _headers = get_billing_cart_with_http_info()
      data
    end

    # Get Shopping Cart Contents
    # Returns the current cart contents, available payment methods, and checkout metadata for the authenticated account. Use this to display the cart page, show totals, and determine which payment options are available before directing the user to &#x60;/pay/{method}/{invoices}&#x60;.
    # @return [Array<(JSON::Any, Integer, Hash)>] JSON::Any data, response status code and response headers
    def get_billing_cart_with_http_info()
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.get_billing_cart ..."}
      end
      # resource path
      local_var_path = "/billing/cart"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"BillingApi.get_billing_cart",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#get_billing_cart\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return JSON::Any.from_json(data), status_code, headers
    end

    # Get Credit Card Verification Requirements
    # Retrieves the verification requirements for a newly added credit card. The response indicates whether the card requires micro-charge amount confirmation or CVV validation. Use this before presenting a verification form to the user.
    # @param id [Int32] The credit card ID to verify. Use the ID returned from &#x60;POST /billing/creditcards&#x60;.
    # @return [SuccessTextResponse]
    def get_billing_credit_card_verify(id : Int32)
      data, _status_code, _headers = get_billing_credit_card_verify_with_http_info(id)
      data
    end

    # Get Credit Card Verification Requirements
    # Retrieves the verification requirements for a newly added credit card. The response indicates whether the card requires micro-charge amount confirmation or CVV validation. Use this before presenting a verification form to the user.
    # @param id [Int32] The credit card ID to verify. Use the ID returned from &#x60;POST /billing/creditcards&#x60;.
    # @return [Array<(SuccessTextResponse, Integer, Hash)>] SuccessTextResponse data, response status code and response headers
    def get_billing_credit_card_verify_with_http_info(id : Int32)
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.get_billing_credit_card_verify ..."}
      end
      # verify the required parameter "id" is set
      if @api_client.config.client_side_validation && id.nil?
        raise ArgumentError.new("Missing the required parameter 'id' when calling BillingApi.get_billing_credit_card_verify")
      end
      # resource path
      local_var_path = "/billing/creditcards/{id}/verify".sub("{" + "id" + "}", URI.encode_path(id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"BillingApi.get_billing_credit_card_verify",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#get_billing_credit_card_verify\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return SuccessTextResponse.from_json(data), status_code, headers
    end

    # Get Invoice Details
    # Retrieves the full invoice information including line items, amounts, and payment status. Use this before redirecting to `/pay/{method}/{invoices}` so you can display the exact amount due and confirm the invoice is still unpaid.
    # @param id [Int32] The invoice ID. Use IDs from &#x60;GET /billing/invoices&#x60; or from order responses.
    # @return [BillingInvoiceDetail]
    def get_billing_invoice(id : Int32)
      data, _status_code, _headers = get_billing_invoice_with_http_info(id)
      data
    end

    # Get Invoice Details
    # Retrieves the full invoice information including line items, amounts, and payment status. Use this before redirecting to &#x60;/pay/{method}/{invoices}&#x60; so you can display the exact amount due and confirm the invoice is still unpaid.
    # @param id [Int32] The invoice ID. Use IDs from &#x60;GET /billing/invoices&#x60; or from order responses.
    # @return [Array<(BillingInvoiceDetail, Integer, Hash)>] BillingInvoiceDetail data, response status code and response headers
    def get_billing_invoice_with_http_info(id : Int32)
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.get_billing_invoice ..."}
      end
      # verify the required parameter "id" is set
      if @api_client.config.client_side_validation && id.nil?
        raise ArgumentError.new("Missing the required parameter 'id' when calling BillingApi.get_billing_invoice")
      end
      # resource path
      local_var_path = "/billing/invoices/{id}".sub("{" + "id" + "}", URI.encode_path(id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"BillingApi.get_billing_invoice",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#get_billing_invoice\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return BillingInvoiceDetail.from_json(data), status_code, headers
    end

    # List Account Invoices
    # Returns the invoice list for the account with summary totals. Use the invoice IDs from the response with `/billing/invoices/{id}` to retrieve detailed line items, or with `/pay/{method}/{invoices}` to initiate payment.
    # @return [BillingInvoiceList]
    def get_billing_invoices()
      data, _status_code, _headers = get_billing_invoices_with_http_info()
      data
    end

    # List Account Invoices
    # Returns the invoice list for the account with summary totals. Use the invoice IDs from the response with &#x60;/billing/invoices/{id}&#x60; to retrieve detailed line items, or with &#x60;/pay/{method}/{invoices}&#x60; to initiate payment.
    # @return [Array<(BillingInvoiceList, Integer, Hash)>] BillingInvoiceList data, response status code and response headers
    def get_billing_invoices_with_http_info()
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.get_billing_invoices ..."}
      end
      # resource path
      local_var_path = "/billing/invoices"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"BillingApi.get_billing_invoices",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#get_billing_invoices\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return BillingInvoiceList.from_json(data), status_code, headers
    end

    # List Prepay Balances
    # Lists prepay balances and their associated metadata. Use this to determine whether an account has usable prepay funds before selecting `prepay` as a payment method.
    # @return [JSON::Any]
    def get_billing_pre_pays()
      data, _status_code, _headers = get_billing_pre_pays_with_http_info()
      data
    end

    # List Prepay Balances
    # Lists prepay balances and their associated metadata. Use this to determine whether an account has usable prepay funds before selecting &#x60;prepay&#x60; as a payment method.
    # @return [Array<(JSON::Any, Integer, Hash)>] JSON::Any data, response status code and response headers
    def get_billing_pre_pays_with_http_info()
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.get_billing_pre_pays ..."}
      end
      # resource path
      local_var_path = "/billing/prepays"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"BillingApi.get_billing_pre_pays",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#get_billing_pre_pays\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return JSON::Any.from_json(data), status_code, headers
    end

    # Get Invoices
    # Returns a paginated list of invoices for the authenticated account. Each invoice includes the invoice number, date, total amount, and payment status. Use the optional `searchString` parameter to filter results and `skip`/`limit` for pagination.
    # @return [Array(Invoice)]
    def get_invoices(search_string : String? = nil, skip : Int32? = nil, limit : Int32? = nil)
      data, _status_code, _headers = get_invoices_with_http_info(search_string, skip, limit)
      data
    end

    # Get Invoices
    # Returns a paginated list of invoices for the authenticated account. Each invoice includes the invoice number, date, total amount, and payment status. Use the optional &#x60;searchString&#x60; parameter to filter results and &#x60;skip&#x60;/&#x60;limit&#x60; for pagination.
    # @return [Array<(Array(Invoice), Integer, Hash)>] Array(Invoice) data, response status code and response headers
    def get_invoices_with_http_info(search_string : String? = nil, skip : Int32? = nil, limit : Int32? = nil)
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.get_invoices ..."}
      end
      if @api_client.config.client_side_validation && !skip.nil? && skip < 0
        raise ArgumentError.new("invalid value for \"skip\" when calling BillingApi.get_invoices, must be greater than or equal to 0.")
      end

      if @api_client.config.client_side_validation && !limit.nil? && limit > 50
        raise ArgumentError.new("invalid value for \"limit\" when calling BillingApi.get_invoices, must be smaller than or equal to 50.")
      end

      if @api_client.config.client_side_validation && !limit.nil? && limit < 0
        raise ArgumentError.new("invalid value for \"limit\" when calling BillingApi.get_invoices, must be greater than or equal to 0.")
      end

      # resource path
      local_var_path = "/invoices"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new
      query_params["searchString"] = search_string.to_s unless search_string.nil?
      query_params["skip"] = skip.to_s unless skip.nil?
      query_params["limit"] = limit.to_s unless limit.nil?

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"BillingApi.get_invoices",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#get_invoices\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return Array(Invoice).from_json(data), status_code, headers
    end

    # Initiate Payment
    # Initiates a payment for the specified invoices using the chosen payment method. The response type determines how your client should proceed: `redirect` means send the user to the provided URL, `submit` means POST a form with the provided fields, and `single` means the payment was processed immediately. Use invoice IDs obtained from order responses or `/billing/invoices`.
    # @param method [String] The payment method to use. Valid values: &#x60;cc&#x60; (credit card), &#x60;paypal&#x60;, &#x60;prepay&#x60;, &#x60;payssion&#x60;, &#x60;payu&#x60;, &#x60;ccavenue&#x60;, &#x60;cashfree&#x60;, &#x60;coinbase&#x60;, &#x60;btcpay&#x60;.
    # @param invoices [String] A comma-separated list of invoice IDs to pay. These IDs are returned by order endpoints (e.g. &#x60;/backups/order&#x60;, &#x60;/vps/order&#x60;) and by &#x60;/billing/invoices&#x60;.
    # @return [InitiatePayment200Response]
    def initiate_payment(method : String, invoices : String)
      data, _status_code, _headers = initiate_payment_with_http_info(method, invoices)
      data
    end

    # Initiate Payment
    # Initiates a payment for the specified invoices using the chosen payment method. The response type determines how your client should proceed: &#x60;redirect&#x60; means send the user to the provided URL, &#x60;submit&#x60; means POST a form with the provided fields, and &#x60;single&#x60; means the payment was processed immediately. Use invoice IDs obtained from order responses or &#x60;/billing/invoices&#x60;.
    # @param method [String] The payment method to use. Valid values: &#x60;cc&#x60; (credit card), &#x60;paypal&#x60;, &#x60;prepay&#x60;, &#x60;payssion&#x60;, &#x60;payu&#x60;, &#x60;ccavenue&#x60;, &#x60;cashfree&#x60;, &#x60;coinbase&#x60;, &#x60;btcpay&#x60;.
    # @param invoices [String] A comma-separated list of invoice IDs to pay. These IDs are returned by order endpoints (e.g. &#x60;/backups/order&#x60;, &#x60;/vps/order&#x60;) and by &#x60;/billing/invoices&#x60;.
    # @return [Array<(InitiatePayment200Response, Integer, Hash)>] InitiatePayment200Response data, response status code and response headers
    def initiate_payment_with_http_info(method : String, invoices : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.initiate_payment ..."}
      end
      # verify the required parameter "method" is set
      if @api_client.config.client_side_validation && method.nil?
        raise ArgumentError.new("Missing the required parameter 'method' when calling BillingApi.initiate_payment")
      end
      # verify enum value
      allowable_values = ["cc", "paypal", "prepay", "payssion", "payu", "ccavenue", "cashfree", "coinbase", "btcpay"]
      if @api_client.config.client_side_validation && !allowable_values.includes?(method)
        raise ArgumentError.new("invalid value for \"method\", must be one of #{allowable_values}")
      end
      # verify the required parameter "invoices" is set
      if @api_client.config.client_side_validation && invoices.nil?
        raise ArgumentError.new("Missing the required parameter 'invoices' when calling BillingApi.initiate_payment")
      end
      # resource path
      local_var_path = "/pay/{method}/{invoices}".sub("{" + "method" + "}", URI.encode_path(method.to_s)).sub("{" + "invoices" + "}", URI.encode_path(invoices.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"BillingApi.initiate_payment",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#initiate_payment\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return InitiatePayment200Response.from_json(data), status_code, headers
    end

    # Submit Credit Card Verification
    # Completes the credit card verification flow by submitting the micro-charge amounts or CVV as required by `GET /billing/creditcards/{id}/verify`. A successful response means the card is verified and can be selected as a payment method via `/billing/payment_method`.
    # @param id [Int32] The credit card ID to verify. Use the ID returned from &#x60;POST /billing/creditcards&#x60;.
    # @param billing_verify_cc_request [BillingVerifyCcRequest] 
    # @return [SuccessTextResponse]
    def post_billing_credit_card_verify(id : Int32, billing_verify_cc_request : BillingVerifyCcRequest)
      data, _status_code, _headers = post_billing_credit_card_verify_with_http_info(id, billing_verify_cc_request)
      data
    end

    # Submit Credit Card Verification
    # Completes the credit card verification flow by submitting the micro-charge amounts or CVV as required by &#x60;GET /billing/creditcards/{id}/verify&#x60;. A successful response means the card is verified and can be selected as a payment method via &#x60;/billing/payment_method&#x60;.
    # @param id [Int32] The credit card ID to verify. Use the ID returned from &#x60;POST /billing/creditcards&#x60;.
    # @param billing_verify_cc_request [BillingVerifyCcRequest] 
    # @return [Array<(SuccessTextResponse, Integer, Hash)>] SuccessTextResponse data, response status code and response headers
    def post_billing_credit_card_verify_with_http_info(id : Int32, billing_verify_cc_request : BillingVerifyCcRequest)
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.post_billing_credit_card_verify ..."}
      end
      # verify the required parameter "id" is set
      if @api_client.config.client_side_validation && id.nil?
        raise ArgumentError.new("Missing the required parameter 'id' when calling BillingApi.post_billing_credit_card_verify")
      end
      # verify the required parameter "billing_verify_cc_request" is set
      if @api_client.config.client_side_validation && billing_verify_cc_request.nil?
        raise ArgumentError.new("Missing the required parameter 'billing_verify_cc_request' when calling BillingApi.post_billing_credit_card_verify")
      end
      # resource path
      local_var_path = "/billing/creditcards/{id}/verify".sub("{" + "id" + "}", URI.encode_path(id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json", "multipart/form-data"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = billing_verify_cc_request.to_json

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :POST,
        path: local_var_path,
        operation: :"BillingApi.post_billing_credit_card_verify",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#post_billing_credit_card_verify\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return SuccessTextResponse.from_json(data), status_code, headers
    end

    # Update Credit Card
    # Updates an existing credit card on the account. Use this to refresh stored card metadata such as expiration date or billing address.
    # @param id [Int32] The credit card ID. Use the card ID returned from &#x60;POST /account/creditcards&#x60; or listed in &#x60;/billing/creditcards&#x60;.
    # @return [String]
    def update_account_credit_card(id : Int32)
      data, _status_code, _headers = update_account_credit_card_with_http_info(id)
      data
    end

    # Update Credit Card
    # Updates an existing credit card on the account. Use this to refresh stored card metadata such as expiration date or billing address.
    # @param id [Int32] The credit card ID. Use the card ID returned from &#x60;POST /account/creditcards&#x60; or listed in &#x60;/billing/creditcards&#x60;.
    # @return [Array<(String, Integer, Hash)>] String data, response status code and response headers
    def update_account_credit_card_with_http_info(id : Int32)
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.update_account_credit_card ..."}
      end
      # verify the required parameter "id" is set
      if @api_client.config.client_side_validation && id.nil?
        raise ArgumentError.new("Missing the required parameter 'id' when calling BillingApi.update_account_credit_card")
      end
      # resource path
      local_var_path = "/account/creditcards/{id}".sub("{" + "id" + "}", URI.encode_path(id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :POST,
        path: local_var_path,
        operation: :"BillingApi.update_account_credit_card",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#update_account_credit_card\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return String.from_json(data), status_code, headers
    end

    # Configure Affiliate Dock Settings
    # Updates the affiliate dock settings including the referral coupon and marketing copy. The dock is the branded landing page shown to visitors arriving via your affiliate link. Use this to customize the coupon code and promotional text.
    # @return [TextResponse]
    def update_affiliate_dock_setup(affiliate_dock_title : String? = nil, affiliate_dock_description : String? = nil, referrer_coupon : String? = nil)
      data, _status_code, _headers = update_affiliate_dock_setup_with_http_info(affiliate_dock_title, affiliate_dock_description, referrer_coupon)
      data
    end

    # Configure Affiliate Dock Settings
    # Updates the affiliate dock settings including the referral coupon and marketing copy. The dock is the branded landing page shown to visitors arriving via your affiliate link. Use this to customize the coupon code and promotional text.
    # @return [Array<(TextResponse, Integer, Hash)>] TextResponse data, response status code and response headers
    def update_affiliate_dock_setup_with_http_info(affiliate_dock_title : String? = nil, affiliate_dock_description : String? = nil, referrer_coupon : String? = nil)
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.update_affiliate_dock_setup ..."}
      end
      # resource path
      local_var_path = "/affiliate/dock_setup"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["multipart/form-data", "application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new
      form_params[:"affiliate_dock_title"] = affiliate_dock_title unless affiliate_dock_title.nil?
      form_params[:"affiliate_dock_description"] = affiliate_dock_description unless affiliate_dock_description.nil?
      form_params[:"referrer_coupon"] = referrer_coupon unless referrer_coupon.nil?

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :POST,
        path: local_var_path,
        operation: :"BillingApi.update_affiliate_dock_setup",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#update_affiliate_dock_setup\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return TextResponse.from_json(data), status_code, headers
    end

    # Configure Affiliate Landing Page
    # Updates the affiliate landing page configuration, including the title, description, and coupon code. Visitors who arrive through your affiliate link see this customized page. Changes are published immediately.
    # @return [TextResponse]
    def update_affiliate_landing_page(affiliate_dock_title : String? = nil, affiliate_dock_description : String? = nil, referrer_coupon : String? = nil)
      data, _status_code, _headers = update_affiliate_landing_page_with_http_info(affiliate_dock_title, affiliate_dock_description, referrer_coupon)
      data
    end

    # Configure Affiliate Landing Page
    # Updates the affiliate landing page configuration, including the title, description, and coupon code. Visitors who arrive through your affiliate link see this customized page. Changes are published immediately.
    # @return [Array<(TextResponse, Integer, Hash)>] TextResponse data, response status code and response headers
    def update_affiliate_landing_page_with_http_info(affiliate_dock_title : String? = nil, affiliate_dock_description : String? = nil, referrer_coupon : String? = nil)
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.update_affiliate_landing_page ..."}
      end
      # resource path
      local_var_path = "/affiliate/landing_pg"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["multipart/form-data", "application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new
      form_params[:"affiliate_dock_title"] = affiliate_dock_title unless affiliate_dock_title.nil?
      form_params[:"affiliate_dock_description"] = affiliate_dock_description unless affiliate_dock_description.nil?
      form_params[:"referrer_coupon"] = referrer_coupon unless referrer_coupon.nil?

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :POST,
        path: local_var_path,
        operation: :"BillingApi.update_affiliate_landing_page",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#update_affiliate_landing_page\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return TextResponse.from_json(data), status_code, headers
    end

    # Configure Affiliate Payout Preferences
    # Updates how you receive affiliate commission payouts. Choose between prepay credit applied to your account balance or PayPal disbursement. When selecting PayPal, provide the email address linked to your PayPal account.
    # @return [TextResponse]
    def update_affiliate_payment_setup(affiliate_paypal : String? = nil, affiliate_payment_method : String? = nil)
      data, _status_code, _headers = update_affiliate_payment_setup_with_http_info(affiliate_paypal, affiliate_payment_method)
      data
    end

    # Configure Affiliate Payout Preferences
    # Updates how you receive affiliate commission payouts. Choose between prepay credit applied to your account balance or PayPal disbursement. When selecting PayPal, provide the email address linked to your PayPal account.
    # @return [Array<(TextResponse, Integer, Hash)>] TextResponse data, response status code and response headers
    def update_affiliate_payment_setup_with_http_info(affiliate_paypal : String? = nil, affiliate_payment_method : String? = nil)
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.update_affiliate_payment_setup ..."}
      end
      # resource path
      local_var_path = "/affiliate/payment_setup"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["multipart/form-data", "application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new
      form_params[:"affiliate_paypal"] = affiliate_paypal unless affiliate_paypal.nil?
      form_params[:"affiliate_payment_method"] = affiliate_payment_method unless affiliate_payment_method.nil?

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :POST,
        path: local_var_path,
        operation: :"BillingApi.update_affiliate_payment_setup",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#update_affiliate_payment_setup\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return TextResponse.from_json(data), status_code, headers
    end

    # Update Credit Card Details
    # Updates stored credit card metadata or retrieves the masked card details. Use this to refresh card data before verification or to update billing address information associated with the card.
    # @param id [Int32] The credit card ID. Use IDs from &#x60;GET /billing/creditcards&#x60; or the response from &#x60;POST /billing/creditcards&#x60;.
    # @return [SuccessTextResponse]
    def update_billing_credit_card(id : Int32)
      data, _status_code, _headers = update_billing_credit_card_with_http_info(id)
      data
    end

    # Update Credit Card Details
    # Updates stored credit card metadata or retrieves the masked card details. Use this to refresh card data before verification or to update billing address information associated with the card.
    # @param id [Int32] The credit card ID. Use IDs from &#x60;GET /billing/creditcards&#x60; or the response from &#x60;POST /billing/creditcards&#x60;.
    # @return [Array<(SuccessTextResponse, Integer, Hash)>] SuccessTextResponse data, response status code and response headers
    def update_billing_credit_card_with_http_info(id : Int32)
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.update_billing_credit_card ..."}
      end
      # verify the required parameter "id" is set
      if @api_client.config.client_side_validation && id.nil?
        raise ArgumentError.new("Missing the required parameter 'id' when calling BillingApi.update_billing_credit_card")
      end
      # resource path
      local_var_path = "/billing/creditcards/{id}".sub("{" + "id" + "}", URI.encode_path(id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :POST,
        path: local_var_path,
        operation: :"BillingApi.update_billing_credit_card",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#update_billing_credit_card\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return SuccessTextResponse.from_json(data), status_code, headers
    end

    # Update Default Payment Method
    # Updates the account's default payment method, including selecting a verified credit card as the primary payment source or switching to PayPal when available.
    # @param billing_payment_method_request [BillingPaymentMethodRequest] 
    # @return [SuccessTextResponse]
    def update_billing_payment_method(billing_payment_method_request : BillingPaymentMethodRequest)
      data, _status_code, _headers = update_billing_payment_method_with_http_info(billing_payment_method_request)
      data
    end

    # Update Default Payment Method
    # Updates the account&#39;s default payment method, including selecting a verified credit card as the primary payment source or switching to PayPal when available.
    # @param billing_payment_method_request [BillingPaymentMethodRequest] 
    # @return [Array<(SuccessTextResponse, Integer, Hash)>] SuccessTextResponse data, response status code and response headers
    def update_billing_payment_method_with_http_info(billing_payment_method_request : BillingPaymentMethodRequest)
      if @api_client.config.debugging
        Log.debug {"Calling API: BillingApi.update_billing_payment_method ..."}
      end
      # verify the required parameter "billing_payment_method_request" is set
      if @api_client.config.client_side_validation && billing_payment_method_request.nil?
        raise ArgumentError.new("Missing the required parameter 'billing_payment_method_request' when calling BillingApi.update_billing_payment_method")
      end
      # resource path
      local_var_path = "/billing/payment_method"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json", "multipart/form-data"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = billing_payment_method_request.to_json

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :POST,
        path: local_var_path,
        operation: :"BillingApi.update_billing_payment_method",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BillingApi#update_billing_payment_method\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return SuccessTextResponse.from_json(data), status_code, headers
    end
  end
end
