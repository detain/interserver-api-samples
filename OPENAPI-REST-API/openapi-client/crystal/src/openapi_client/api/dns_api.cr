# #InterServer Management API
#
### Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
#
#The version of the OpenAPI document: 0.9.0
#Contact: support@interserver.net
#Generated by: https://openapi-generator.tech
#Generator version: 7.20.0
#

require "uri"

module OpenAPIClient
  class DNSApi
    property api_client : ApiClient

    def initialize(api_client = ApiClient.default)
      @api_client = api_client
    end
    # Create DNS Domain
    # Creates a new DNS domain and assigns an initial A record pointing to the supplied IP address. The domain is immediately available on InterServer's DNS servers. Use `/dns/{id}` to manage records after creation.
    # @param domain [String] The domain name.
    # @param ip [String] IP Address to point the domain to.
    # @return [nil]
    def add_dns_domain(domain : String, ip : String)
      add_dns_domain_with_http_info(domain, ip)
      nil
    end

    # Create DNS Domain
    # Creates a new DNS domain and assigns an initial A record pointing to the supplied IP address. The domain is immediately available on InterServer&#39;s DNS servers. Use &#x60;/dns/{id}&#x60; to manage records after creation.
    # @param domain [String] The domain name.
    # @param ip [String] IP Address to point the domain to.
    # @return [Array<(nil, Integer, Hash)>] nil, response status code and response headers
    def add_dns_domain_with_http_info(domain : String, ip : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: DNSApi.add_dns_domain ..."}
      end
      # verify the required parameter "domain" is set
      if @api_client.config.client_side_validation && domain.nil?
        raise ArgumentError.new("Missing the required parameter 'domain' when calling DNSApi.add_dns_domain")
      end
      # verify the required parameter "ip" is set
      if @api_client.config.client_side_validation && ip.nil?
        raise ArgumentError.new("Missing the required parameter 'ip' when calling DNSApi.add_dns_domain")
      end
      # resource path
      local_var_path = "/dns"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["multipart/form-data", "application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new
      form_params[:"domain"] = domain unless domain.nil?
      form_params[:"ip"] = ip unless ip.nil?

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :POST,
        path: local_var_path,
        operation: :"DNSApi.add_dns_domain",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: DNSApi#add_dns_domain\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return nil, status_code, headers
    end

    # Add DNS Record to Domain
    # Adds a new DNS record to the specified domain. Provide the record type (A, AAAA, CNAME, MX, TXT, etc.), name, content, TTL, and priority. The record takes effect on the DNS servers immediately. Use `GET /dns/{id}` afterward to confirm the record was created.
    # @param id [String] The DNS Domain ID.
    # @param name [String] Name part of record
    # @param _type [DnsRecordType] 
    # @param content [String] Content of record
    # @return [nil]
    def add_dns_record(id : String, name : String, _type : DnsRecordType, content : String, ttl : Int32? = nil, prio : Int32? = nil)
      add_dns_record_with_http_info(id, name, _type, content, ttl, prio)
      nil
    end

    # Add DNS Record to Domain
    # Adds a new DNS record to the specified domain. Provide the record type (A, AAAA, CNAME, MX, TXT, etc.), name, content, TTL, and priority. The record takes effect on the DNS servers immediately. Use &#x60;GET /dns/{id}&#x60; afterward to confirm the record was created.
    # @param id [String] The DNS Domain ID.
    # @param name [String] Name part of record
    # @param _type [DnsRecordType] 
    # @param content [String] Content of record
    # @return [Array<(nil, Integer, Hash)>] nil, response status code and response headers
    def add_dns_record_with_http_info(id : String, name : String, _type : DnsRecordType, content : String, ttl : Int32? = nil, prio : Int32? = nil)
      if @api_client.config.debugging
        Log.debug {"Calling API: DNSApi.add_dns_record ..."}
      end
      # verify the required parameter "id" is set
      if @api_client.config.client_side_validation && id.nil?
        raise ArgumentError.new("Missing the required parameter 'id' when calling DNSApi.add_dns_record")
      end
      # verify the required parameter "name" is set
      if @api_client.config.client_side_validation && name.nil?
        raise ArgumentError.new("Missing the required parameter 'name' when calling DNSApi.add_dns_record")
      end
      # verify the required parameter "_type" is set
      if @api_client.config.client_side_validation && _type.nil?
        raise ArgumentError.new("Missing the required parameter '_type' when calling DNSApi.add_dns_record")
      end
      # verify the required parameter "content" is set
      if @api_client.config.client_side_validation && content.nil?
        raise ArgumentError.new("Missing the required parameter 'content' when calling DNSApi.add_dns_record")
      end
      # resource path
      local_var_path = "/dns/{id}".sub("{" + "id" + "}", URI.encode_path(id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["multipart/form-data", "application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new
      form_params[:"name"] = name unless name.nil?
      form_params[:"type"] = _type unless _type.nil?
      form_params[:"content"] = content unless content.nil?
      form_params[:"ttl"] = ttl unless ttl.nil?
      form_params[:"prio"] = prio unless prio.nil?

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :POST,
        path: local_var_path,
        operation: :"DNSApi.add_dns_record",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: DNSApi#add_dns_record\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return nil, status_code, headers
    end

    # Delete DNS Domain
    # Deletes a DNS domain and all of its associated records from the DNS servers. This action is permanent and cannot be undone. Any services relying on these DNS records will be affected immediately.
    # @param id [String] The DNS domain ID to delete. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain.
    # @return [nil]
    def delete_dns_domain(id : String)
      delete_dns_domain_with_http_info(id)
      nil
    end

    # Delete DNS Domain
    # Deletes a DNS domain and all of its associated records from the DNS servers. This action is permanent and cannot be undone. Any services relying on these DNS records will be affected immediately.
    # @param id [String] The DNS domain ID to delete. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain.
    # @return [Array<(nil, Integer, Hash)>] nil, response status code and response headers
    def delete_dns_domain_with_http_info(id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: DNSApi.delete_dns_domain ..."}
      end
      # verify the required parameter "id" is set
      if @api_client.config.client_side_validation && id.nil?
        raise ArgumentError.new("Missing the required parameter 'id' when calling DNSApi.delete_dns_domain")
      end
      # resource path
      local_var_path = "/dns/{id}".sub("{" + "id" + "}", URI.encode_path(id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :DELETE,
        path: local_var_path,
        operation: :"DNSApi.delete_dns_domain",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: DNSApi#delete_dns_domain\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return nil, status_code, headers
    end

    # Delete DNS Record
    # Removes a DNS record from the specified domain. The deletion takes effect on the DNS servers immediately. Use `GET /dns/{id}` to verify the record has been removed.
    # @param domain_id [Int32] The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain.
    # @param record_id [Int32] The DNS record ID within the domain. Use the record &#x60;id&#x60; from &#x60;GET /dns/{id}&#x60; to identify the record.
    # @return [nil]
    def delete_dns_record(domain_id : Int32, record_id : Int32)
      delete_dns_record_with_http_info(domain_id, record_id)
      nil
    end

    # Delete DNS Record
    # Removes a DNS record from the specified domain. The deletion takes effect on the DNS servers immediately. Use &#x60;GET /dns/{id}&#x60; to verify the record has been removed.
    # @param domain_id [Int32] The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain.
    # @param record_id [Int32] The DNS record ID within the domain. Use the record &#x60;id&#x60; from &#x60;GET /dns/{id}&#x60; to identify the record.
    # @return [Array<(nil, Integer, Hash)>] nil, response status code and response headers
    def delete_dns_record_with_http_info(domain_id : Int32, record_id : Int32)
      if @api_client.config.debugging
        Log.debug {"Calling API: DNSApi.delete_dns_record ..."}
      end
      # verify the required parameter "domain_id" is set
      if @api_client.config.client_side_validation && domain_id.nil?
        raise ArgumentError.new("Missing the required parameter 'domain_id' when calling DNSApi.delete_dns_record")
      end
      # verify the required parameter "record_id" is set
      if @api_client.config.client_side_validation && record_id.nil?
        raise ArgumentError.new("Missing the required parameter 'record_id' when calling DNSApi.delete_dns_record")
      end
      # resource path
      local_var_path = "/dns/{domainId}/{recordId}".sub("{" + "domainId" + "}", URI.encode_path(domain_id.to_s)).sub("{" + "recordId" + "}", URI.encode_path(record_id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :DELETE,
        path: local_var_path,
        operation: :"DNSApi.delete_dns_record",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: DNSApi#delete_dns_record\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return nil, status_code, headers
    end

    # List Domain DNS Records
    # Returns the full set of DNS records for the specified domain, including NS, A, AAAA, CNAME, MX, TXT, and other record types. Use the record `id` values with `/dns/{domainId}/{recordId}` to update or delete individual records.
    # @param id [Int32] The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain.
    # @return [Array(DnsRecord)]
    def get_dns_domain(id : Int32)
      data, _status_code, _headers = get_dns_domain_with_http_info(id)
      data
    end

    # List Domain DNS Records
    # Returns the full set of DNS records for the specified domain, including NS, A, AAAA, CNAME, MX, TXT, and other record types. Use the record &#x60;id&#x60; values with &#x60;/dns/{domainId}/{recordId}&#x60; to update or delete individual records.
    # @param id [Int32] The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain.
    # @return [Array<(Array(DnsRecord), Integer, Hash)>] Array(DnsRecord) data, response status code and response headers
    def get_dns_domain_with_http_info(id : Int32)
      if @api_client.config.debugging
        Log.debug {"Calling API: DNSApi.get_dns_domain ..."}
      end
      # verify the required parameter "id" is set
      if @api_client.config.client_side_validation && id.nil?
        raise ArgumentError.new("Missing the required parameter 'id' when calling DNSApi.get_dns_domain")
      end
      # resource path
      local_var_path = "/dns/{id}".sub("{" + "id" + "}", URI.encode_path(id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"DNSApi.get_dns_domain",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: DNSApi#get_dns_domain\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return Array(DnsRecord).from_json(data), status_code, headers
    end

    # List DNS Domains
    # Returns the DNS domains on your account along with their primary A record content. Use the `id` from each entry with `/dns/{id}` to retrieve the full record set, or to add, update, and delete individual records.
    # @return [Array(DnsListItem)]
    def get_dns_list()
      data, _status_code, _headers = get_dns_list_with_http_info()
      data
    end

    # List DNS Domains
    # Returns the DNS domains on your account along with their primary A record content. Use the &#x60;id&#x60; from each entry with &#x60;/dns/{id}&#x60; to retrieve the full record set, or to add, update, and delete individual records.
    # @return [Array<(Array(DnsListItem), Integer, Hash)>] Array(DnsListItem) data, response status code and response headers
    def get_dns_list_with_http_info()
      if @api_client.config.debugging
        Log.debug {"Calling API: DNSApi.get_dns_list ..."}
      end
      # resource path
      local_var_path = "/dns"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"DNSApi.get_dns_list",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: DNSApi#get_dns_list\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return Array(DnsListItem).from_json(data), status_code, headers
    end

    # Update DNS Record
    # Updates an existing DNS record with new values. Use `GET /dns/{id}` to list records and retrieve the record IDs before updating. Changes propagate to the DNS servers immediately.
    # @param domain_id [Int32] The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain.
    # @param record_id [Int32] The DNS record ID within the domain. Use the record &#x60;id&#x60; from &#x60;GET /dns/{id}&#x60; to identify the record.
    # @return [nil]
    def update_dns_record(domain_id : Int32, record_id : Int32, name : String? = nil, _type : DnsRecordType? = nil, content : String? = nil, ttl : String? = nil, prio : String? = nil, disabled : String? = nil, ordername : String? = nil, auth : String? = nil)
      update_dns_record_with_http_info(domain_id, record_id, name, _type, content, ttl, prio, disabled, ordername, auth)
      nil
    end

    # Update DNS Record
    # Updates an existing DNS record with new values. Use &#x60;GET /dns/{id}&#x60; to list records and retrieve the record IDs before updating. Changes propagate to the DNS servers immediately.
    # @param domain_id [Int32] The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain.
    # @param record_id [Int32] The DNS record ID within the domain. Use the record &#x60;id&#x60; from &#x60;GET /dns/{id}&#x60; to identify the record.
    # @return [Array<(nil, Integer, Hash)>] nil, response status code and response headers
    def update_dns_record_with_http_info(domain_id : Int32, record_id : Int32, name : String? = nil, _type : DnsRecordType? = nil, content : String? = nil, ttl : String? = nil, prio : String? = nil, disabled : String? = nil, ordername : String? = nil, auth : String? = nil)
      if @api_client.config.debugging
        Log.debug {"Calling API: DNSApi.update_dns_record ..."}
      end
      # verify the required parameter "domain_id" is set
      if @api_client.config.client_side_validation && domain_id.nil?
        raise ArgumentError.new("Missing the required parameter 'domain_id' when calling DNSApi.update_dns_record")
      end
      # verify the required parameter "record_id" is set
      if @api_client.config.client_side_validation && record_id.nil?
        raise ArgumentError.new("Missing the required parameter 'record_id' when calling DNSApi.update_dns_record")
      end
      # resource path
      local_var_path = "/dns/{domainId}/{recordId}".sub("{" + "domainId" + "}", URI.encode_path(domain_id.to_s)).sub("{" + "recordId" + "}", URI.encode_path(record_id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["multipart/form-data", "application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new
      form_params[:"name"] = name unless name.nil?
      form_params[:"type"] = _type unless _type.nil?
      form_params[:"content"] = content unless content.nil?
      form_params[:"ttl"] = ttl unless ttl.nil?
      form_params[:"prio"] = prio unless prio.nil?
      form_params[:"disabled"] = disabled unless disabled.nil?
      form_params[:"ordername"] = ordername unless ordername.nil?
      form_params[:"auth"] = auth unless auth.nil?

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :POST,
        path: local_var_path,
        operation: :"DNSApi.update_dns_record",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: DNSApi#update_dns_record\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return nil, status_code, headers
    end
  end
end
