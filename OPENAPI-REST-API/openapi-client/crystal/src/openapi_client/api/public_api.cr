# #InterServer Management API
#
### Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
#
#The version of the OpenAPI document: 0.9.0
#Contact: support@interserver.net
#Generated by: https://openapi-generator.tech
#Generator version: 7.20.0
#

require "uri"

module OpenAPIClient
  class PublicApi
    property api_client : ApiClient

    def initialize(api_client = ApiClient.default)
      @api_client = api_client
    end
    # Get Captcha Challenge
    # Returns a base64-encoded JPEG captcha image for human verification. Display this image to the user and submit their response along with the registration or order form. No authentication is required for this endpoint.
    # @return [CaptchaResponse]
    def get_captcha()
      data, _status_code, _headers = get_captcha_with_http_info()
      data
    end

    # Get Captcha Challenge
    # Returns a base64-encoded JPEG captcha image for human verification. Display this image to the user and submit their response along with the registration or order form. No authentication is required for this endpoint.
    # @return [Array<(CaptchaResponse, Integer, Hash)>] CaptchaResponse data, response status code and response headers
    def get_captcha_with_http_info()
      if @api_client.config.debugging
        Log.debug {"Calling API: PublicApi.get_captcha ..."}
      end
      # resource path
      local_var_path = "/captcha"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"PublicApi.get_captcha",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: PublicApi#get_captcha\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return CaptchaResponse.from_json(data), status_code, headers
    end

    # Get Countries
    # Returns a map of country codes to country names. Use the optional `fetch_by` parameter to select the key format: `iso2` (default, two-letter codes), `iso3` (three-letter codes), or `numcode` (numeric codes). Use these values when populating country fields in account registration or domain contact forms.
    # @return [JSON::Any]
    def get_countries(fetch_by : String? = nil)
      data, _status_code, _headers = get_countries_with_http_info(fetch_by)
      data
    end

    # Get Countries
    # Returns a map of country codes to country names. Use the optional &#x60;fetch_by&#x60; parameter to select the key format: &#x60;iso2&#x60; (default, two-letter codes), &#x60;iso3&#x60; (three-letter codes), or &#x60;numcode&#x60; (numeric codes). Use these values when populating country fields in account registration or domain contact forms.
    # @return [Array<(JSON::Any, Integer, Hash)>] JSON::Any data, response status code and response headers
    def get_countries_with_http_info(fetch_by : String? = nil)
      if @api_client.config.debugging
        Log.debug {"Calling API: PublicApi.get_countries ..."}
      end
      allowable_values = ["iso2", "iso3", "numcode"]
      if @api_client.config.client_side_validation && fetch_by && !allowable_values.includes?(fetch_by)
        raise ArgumentError.new("invalid value for \"fetch_by\", must be one of #{allowable_values}")
      end
      # resource path
      local_var_path = "/account/countries"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new
      query_params["fetch_by"] = fetch_by.to_s unless fetch_by.nil?

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"PublicApi.get_countries",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: PublicApi#get_countries\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return JSON::Any.from_json(data), status_code, headers
    end

    # Get Server Info
    # Returns metadata about the API server's available modules, service packages, categories, and types. This public endpoint does not require authentication and is useful for discovering supported services before placing orders.
    # @return [ServicesInfo]
    def get_info()
      data, _status_code, _headers = get_info_with_http_info()
      data
    end

    # Get Server Info
    # Returns metadata about the API server&#39;s available modules, service packages, categories, and types. This public endpoint does not require authentication and is useful for discovering supported services before placing orders.
    # @return [Array<(ServicesInfo, Integer, Hash)>] ServicesInfo data, response status code and response headers
    def get_info_with_http_info()
      if @api_client.config.debugging
        Log.debug {"Calling API: PublicApi.get_info ..."}
      end
      # resource path
      local_var_path = "/info"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"PublicApi.get_info",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: PublicApi#get_info\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return ServicesInfo.from_json(data), status_code, headers
    end

    # Get Login Info
    # Gets the various pieces of information useful for generating a login page.
    # @return [LoginInfo]
    def get_login_info()
      data, _status_code, _headers = get_login_info_with_http_info()
      data
    end

    # Get Login Info
    # Gets the various pieces of information useful for generating a login page.
    # @return [Array<(LoginInfo, Integer, Hash)>] LoginInfo data, response status code and response headers
    def get_login_info_with_http_info()
      if @api_client.config.debugging
        Log.debug {"Calling API: PublicApi.get_login_info ..."}
      end
      # resource path
      local_var_path = "/login"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"PublicApi.get_login_info",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: PublicApi#get_login_info\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return LoginInfo.from_json(data), status_code, headers
    end

    # List Marketplace Servers
    # Returns the list of available Rapid Deploy dedicated servers with current pricing. Each entry includes CPU, memory, disk, bandwidth, IP allocation, and location details. These servers are pre-configured and can be provisioned immediately after purchase.
    # @return [BuyItNowList]
    def get_mp_servers()
      data, _status_code, _headers = get_mp_servers_with_http_info()
      data
    end

    # List Marketplace Servers
    # Returns the list of available Rapid Deploy dedicated servers with current pricing. Each entry includes CPU, memory, disk, bandwidth, IP allocation, and location details. These servers are pre-configured and can be provisioned immediately after purchase.
    # @return [Array<(BuyItNowList, Integer, Hash)>] BuyItNowList data, response status code and response headers
    def get_mp_servers_with_http_info()
      if @api_client.config.debugging
        Log.debug {"Calling API: PublicApi.get_mp_servers ..."}
      end
      # resource path
      local_var_path = "/buy_now_servers_list"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"PublicApi.get_mp_servers",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: PublicApi#get_mp_servers\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return BuyItNowList.from_json(data), status_code, headers
    end

    # Get OAuth Redirect URL
    # Returns the OAuth redirect URL for the specified provider. Redirect the user to this URL to begin the OAuth authentication flow. After the user authorizes, the provider will redirect back to your callback endpoint where you should call `POST /oauth`.
    # @param provider [String] The OAuth provider name (e.g. &#x60;Google&#x60;).
    # @return [GetOauthRedirect200Response]
    def get_oauth_redirect(provider : String)
      data, _status_code, _headers = get_oauth_redirect_with_http_info(provider)
      data
    end

    # Get OAuth Redirect URL
    # Returns the OAuth redirect URL for the specified provider. Redirect the user to this URL to begin the OAuth authentication flow. After the user authorizes, the provider will redirect back to your callback endpoint where you should call &#x60;POST /oauth&#x60;.
    # @param provider [String] The OAuth provider name (e.g. &#x60;Google&#x60;).
    # @return [Array<(GetOauthRedirect200Response, Integer, Hash)>] GetOauthRedirect200Response data, response status code and response headers
    def get_oauth_redirect_with_http_info(provider : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: PublicApi.get_oauth_redirect ..."}
      end
      # verify the required parameter "provider" is set
      if @api_client.config.client_side_validation && provider.nil?
        raise ArgumentError.new("Missing the required parameter 'provider' when calling PublicApi.get_oauth_redirect")
      end
      # resource path
      local_var_path = "/oauth"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new
      query_params["provider"] = provider.to_s unless provider.nil?

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"PublicApi.get_oauth_redirect",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: PublicApi#get_oauth_redirect\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return GetOauthRedirect200Response.from_json(data), status_code, headers
    end

    # Get Available Timezones
    # Returns the complete list of available timezone identifiers (e.g. `America/New_York`, `UTC`). Use these values when updating timezone settings on your account or on VPS and QuickServer services via `/vps/{id}/change_timezone` or `/qs/{id}/change_timezone`.
    # @return [Array(String)]
    def get_timezones()
      data, _status_code, _headers = get_timezones_with_http_info()
      data
    end

    # Get Available Timezones
    # Returns the complete list of available timezone identifiers (e.g. &#x60;America/New_York&#x60;, &#x60;UTC&#x60;). Use these values when updating timezone settings on your account or on VPS and QuickServer services via &#x60;/vps/{id}/change_timezone&#x60; or &#x60;/qs/{id}/change_timezone&#x60;.
    # @return [Array<(Array(String), Integer, Hash)>] Array(String) data, response status code and response headers
    def get_timezones_with_http_info()
      if @api_client.config.debugging
        Log.debug {"Calling API: PublicApi.get_timezones ..."}
      end
      # resource path
      local_var_path = "/account/timezones"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"PublicApi.get_timezones",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: PublicApi#get_timezones\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return Array(String).from_json(data), status_code, headers
    end

    # Complete OAuth Two-Factor Verification
    # Completes the OAuth login by verifying a two-factor authentication code. Use the `account_id` returned by `POST /oauth` when the response contained `error_code: \"2fa_required\"`.
    # @param patch_oauth_two_factor_request [PatchOauthTwoFactorRequest] 
    # @return [PatchOauthTwoFactor200Response]
    def patch_oauth_two_factor(patch_oauth_two_factor_request : PatchOauthTwoFactorRequest)
      data, _status_code, _headers = patch_oauth_two_factor_with_http_info(patch_oauth_two_factor_request)
      data
    end

    # Complete OAuth Two-Factor Verification
    # Completes the OAuth login by verifying a two-factor authentication code. Use the &#x60;account_id&#x60; returned by &#x60;POST /oauth&#x60; when the response contained &#x60;error_code: \&quot;2fa_required\&quot;&#x60;.
    # @param patch_oauth_two_factor_request [PatchOauthTwoFactorRequest] 
    # @return [Array<(PatchOauthTwoFactor200Response, Integer, Hash)>] PatchOauthTwoFactor200Response data, response status code and response headers
    def patch_oauth_two_factor_with_http_info(patch_oauth_two_factor_request : PatchOauthTwoFactorRequest)
      if @api_client.config.debugging
        Log.debug {"Calling API: PublicApi.patch_oauth_two_factor ..."}
      end
      # verify the required parameter "patch_oauth_two_factor_request" is set
      if @api_client.config.client_side_validation && patch_oauth_two_factor_request.nil?
        raise ArgumentError.new("Missing the required parameter 'patch_oauth_two_factor_request' when calling PublicApi.patch_oauth_two_factor")
      end
      # resource path
      local_var_path = "/oauth"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json", "multipart/form-data"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = patch_oauth_two_factor_request.to_json

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :PATCH,
        path: local_var_path,
        operation: :"PublicApi.patch_oauth_two_factor",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: PublicApi#patch_oauth_two_factor\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return PatchOauthTwoFactor200Response.from_json(data), status_code, headers
    end

    # Ping Server
    # Performs a single ping/pong request to let you know if the server is up.
    # @return [String]
    def ping_server()
      data, _status_code, _headers = ping_server_with_http_info()
      data
    end

    # Ping Server
    # Performs a single ping/pong request to let you know if the server is up.
    # @return [Array<(String, Integer, Hash)>] String data, response status code and response headers
    def ping_server_with_http_info()
      if @api_client.config.debugging
        Log.debug {"Calling API: PublicApi.ping_server ..."}
      end
      # resource path
      local_var_path = "/ping"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"PublicApi.ping_server",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: PublicApi#ping_server\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return String.from_json(data), status_code, headers
    end

    # OAuth Callback
    # Handles the OAuth callback after the user authorizes with the provider. Returns the login status. If the account has two-factor authentication enabled, the response will include `error_code: \"2fa_required\"` and an `account_id`; use `PATCH /oauth` with the 2FA code and account ID to complete login.
    # @param provider [String] The OAuth provider name (e.g. &#x60;Google&#x60;).
    # @return [PostOauthCallback200Response]
    def post_oauth_callback(provider : String, post_oauth_callback_request : PostOauthCallbackRequest? = nil)
      data, _status_code, _headers = post_oauth_callback_with_http_info(provider, post_oauth_callback_request)
      data
    end

    # OAuth Callback
    # Handles the OAuth callback after the user authorizes with the provider. Returns the login status. If the account has two-factor authentication enabled, the response will include &#x60;error_code: \&quot;2fa_required\&quot;&#x60; and an &#x60;account_id&#x60;; use &#x60;PATCH /oauth&#x60; with the 2FA code and account ID to complete login.
    # @param provider [String] The OAuth provider name (e.g. &#x60;Google&#x60;).
    # @return [Array<(PostOauthCallback200Response, Integer, Hash)>] PostOauthCallback200Response data, response status code and response headers
    def post_oauth_callback_with_http_info(provider : String, post_oauth_callback_request : PostOauthCallbackRequest? = nil)
      if @api_client.config.debugging
        Log.debug {"Calling API: PublicApi.post_oauth_callback ..."}
      end
      # verify the required parameter "provider" is set
      if @api_client.config.client_side_validation && provider.nil?
        raise ArgumentError.new("Missing the required parameter 'provider' when calling PublicApi.post_oauth_callback")
      end
      # resource path
      local_var_path = "/oauth"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new
      query_params["provider"] = provider.to_s unless provider.nil?

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json", "multipart/form-data"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = post_oauth_callback_request.to_json

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :POST,
        path: local_var_path,
        operation: :"PublicApi.post_oauth_callback",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: PublicApi#post_oauth_callback\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return PostOauthCallback200Response.from_json(data), status_code, headers
    end

    # Submit Login Information
    # Authenticates using the provided login credentials and returns a session token. Include the returned session ID in the `sessionid` header on subsequent requests. If authentication fails, a `402` error is returned.
    # @param login [String] 
    # @param passwd [String] 
    # @return [LoginSuccessResponse]
    def submit_login(login : String, passwd : String, remember : String? = nil, g_recaptcha_response : LoginSubmissionExampleGRecaptchaResponse? = nil, tfa : String? = nil)
      data, _status_code, _headers = submit_login_with_http_info(login, passwd, remember, g_recaptcha_response, tfa)
      data
    end

    # Submit Login Information
    # Authenticates using the provided login credentials and returns a session token. Include the returned session ID in the &#x60;sessionid&#x60; header on subsequent requests. If authentication fails, a &#x60;402&#x60; error is returned.
    # @param login [String] 
    # @param passwd [String] 
    # @return [Array<(LoginSuccessResponse, Integer, Hash)>] LoginSuccessResponse data, response status code and response headers
    def submit_login_with_http_info(login : String, passwd : String, remember : String? = nil, g_recaptcha_response : LoginSubmissionExampleGRecaptchaResponse? = nil, tfa : String? = nil)
      if @api_client.config.debugging
        Log.debug {"Calling API: PublicApi.submit_login ..."}
      end
      # verify the required parameter "login" is set
      if @api_client.config.client_side_validation && login.nil?
        raise ArgumentError.new("Missing the required parameter 'login' when calling PublicApi.submit_login")
      end
      # verify the required parameter "passwd" is set
      if @api_client.config.client_side_validation && passwd.nil?
        raise ArgumentError.new("Missing the required parameter 'passwd' when calling PublicApi.submit_login")
      end
      # resource path
      local_var_path = "/login"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["multipart/form-data", "application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new
      form_params[:"login"] = login unless login.nil?
      form_params[:"passwd"] = passwd unless passwd.nil?
      form_params[:"remember"] = remember unless remember.nil?
      form_params[:"g-recaptcha-response"] = g_recaptcha_response unless g_recaptcha_response.nil?
      form_params[:"tfa"] = tfa unless tfa.nil?

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :POST,
        path: local_var_path,
        operation: :"PublicApi.submit_login",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: PublicApi#submit_login\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return LoginSuccessResponse.from_json(data), status_code, headers
    end

    # Submit Signup Information
    # Creates a new account in our system using the provided information.
    # @return [nil]
    def submit_signup(login_submission_example : LoginSubmissionExample? = nil)
      submit_signup_with_http_info(login_submission_example)
      nil
    end

    # Submit Signup Information
    # Creates a new account in our system using the provided information.
    # @return [Array<(nil, Integer, Hash)>] nil, response status code and response headers
    def submit_signup_with_http_info(login_submission_example : LoginSubmissionExample? = nil)
      if @api_client.config.debugging
        Log.debug {"Calling API: PublicApi.submit_signup ..."}
      end
      # resource path
      local_var_path = "/signup"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = login_submission_example.to_json

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :POST,
        path: local_var_path,
        operation: :"PublicApi.submit_signup",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: PublicApi#submit_signup\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return nil, status_code, headers
    end
  end
end
