# #InterServer Management API
#
### Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
#
#The version of the OpenAPI document: 0.9.0
#Contact: support@interserver.net
#Generated by: https://openapi-generator.tech
#Generator version: 7.21.0
#

require "uri"

module OpenAPIClient
  class BackupsApi
    property api_client : ApiClient

    def initialize(api_client = ApiClient.default)
      @api_client = api_client
    end
    # Place Backup Order
    # Places a new backup storage order and generates an invoice. On success, the response includes invoice IDs that can be used with `/billing/invoices/{id}` to view the invoice or `/pay/{method}/{invoices}` to complete payment. The service is provisioned after payment is confirmed.
    # @return [BackupOrderPostResponse]
    def add_backup(validate_only : Bool? = nil, service_type : Int32? = nil, coupon : String? = nil)
      data, _status_code, _headers = add_backup_with_http_info(validate_only, service_type, coupon)
      data
    end

    # Place Backup Order
    # Places a new backup storage order and generates an invoice. On success, the response includes invoice IDs that can be used with &#x60;/billing/invoices/{id}&#x60; to view the invoice or &#x60;/pay/{method}/{invoices}&#x60; to complete payment. The service is provisioned after payment is confirmed.
    # @return [Array<(BackupOrderPostResponse, Integer, Hash)>] BackupOrderPostResponse data, response status code and response headers
    def add_backup_with_http_info(validate_only : Bool? = nil, service_type : Int32? = nil, coupon : String? = nil)
      if @api_client.config.debugging
        Log.debug {"Calling API: BackupsApi.add_backup ..."}
      end
      # resource path
      local_var_path = "/backups/order"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["multipart/form-data", "application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new
      form_params[:"validateOnly"] = validate_only unless validate_only.nil?
      form_params[:"serviceType"] = service_type unless service_type.nil?
      form_params[:"coupon"] = coupon unless coupon.nil?

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :POST,
        path: local_var_path,
        operation: :"BackupsApi.add_backup",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BackupsApi#add_backup\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return BackupOrderPostResponse.from_json(data), status_code, headers
    end

    # Cancel Backup Service
    # Cancels the specified backup storage service. The service remains accessible until the end of the current billing period. This action cannot be undone; a new order must be placed to restore service.
    # @param id [Int32] The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service.
    # @return [CancelBackup200Response]
    def cancel_backup(id : Int32)
      data, _status_code, _headers = cancel_backup_with_http_info(id)
      data
    end

    # Cancel Backup Service
    # Cancels the specified backup storage service. The service remains accessible until the end of the current billing period. This action cannot be undone; a new order must be placed to restore service.
    # @param id [Int32] The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service.
    # @return [Array<(CancelBackup200Response, Integer, Hash)>] CancelBackup200Response data, response status code and response headers
    def cancel_backup_with_http_info(id : Int32)
      if @api_client.config.debugging
        Log.debug {"Calling API: BackupsApi.cancel_backup ..."}
      end
      # verify the required parameter "id" is set
      if @api_client.config.client_side_validation && id.nil?
        raise ArgumentError.new("Missing the required parameter 'id' when calling BackupsApi.cancel_backup")
      end
      # resource path
      local_var_path = "/backups/{id}".sub("{" + "id" + "}", URI.encode_path(id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :DELETE,
        path: local_var_path,
        operation: :"BackupsApi.cancel_backup",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BackupsApi#cancel_backup\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return CancelBackup200Response.from_json(data), status_code, headers
    end

    # Get Backup Service Details
    # Returns detailed service information for the specified backup storage order, including `backup_username`, `backup_ip`, `backup_status`, and `backup_quota` in `serviceInfo`. Also returns `client_links`, `billingDetails`, `extraInfoTables`, `package`, and `custCurrency`.
    # @param id [Int32] The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service.
    # @return [Backup]
    def get_backup_info(id : Int32)
      data, _status_code, _headers = get_backup_info_with_http_info(id)
      data
    end

    # Get Backup Service Details
    # Returns detailed service information for the specified backup storage order, including &#x60;backup_username&#x60;, &#x60;backup_ip&#x60;, &#x60;backup_status&#x60;, and &#x60;backup_quota&#x60; in &#x60;serviceInfo&#x60;. Also returns &#x60;client_links&#x60;, &#x60;billingDetails&#x60;, &#x60;extraInfoTables&#x60;, &#x60;package&#x60;, and &#x60;custCurrency&#x60;.
    # @param id [Int32] The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service.
    # @return [Array<(Backup, Integer, Hash)>] Backup data, response status code and response headers
    def get_backup_info_with_http_info(id : Int32)
      if @api_client.config.debugging
        Log.debug {"Calling API: BackupsApi.get_backup_info ..."}
      end
      # verify the required parameter "id" is set
      if @api_client.config.client_side_validation && id.nil?
        raise ArgumentError.new("Missing the required parameter 'id' when calling BackupsApi.get_backup_info")
      end
      # resource path
      local_var_path = "/backups/{id}".sub("{" + "id" + "}", URI.encode_path(id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"BackupsApi.get_backup_info",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BackupsApi#get_backup_info\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return Backup.from_json(data), status_code, headers
    end

    # Get Backup Order Invoices
    # Retrieves invoices associated with the backup storage order. Use this to confirm billing status or locate invoices for payment.
    # @param id [Int32] The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service.
    # @return [ChargeInvoiceRows]
    def get_backup_invoices(id : Int32)
      data, _status_code, _headers = get_backup_invoices_with_http_info(id)
      data
    end

    # Get Backup Order Invoices
    # Retrieves invoices associated with the backup storage order. Use this to confirm billing status or locate invoices for payment.
    # @param id [Int32] The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service.
    # @return [Array<(ChargeInvoiceRows, Integer, Hash)>] ChargeInvoiceRows data, response status code and response headers
    def get_backup_invoices_with_http_info(id : Int32)
      if @api_client.config.debugging
        Log.debug {"Calling API: BackupsApi.get_backup_invoices ..."}
      end
      # verify the required parameter "id" is set
      if @api_client.config.client_side_validation && id.nil?
        raise ArgumentError.new("Missing the required parameter 'id' when calling BackupsApi.get_backup_invoices")
      end
      # resource path
      local_var_path = "/backups/{id}/invoices".sub("{" + "id" + "}", URI.encode_path(id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"BackupsApi.get_backup_invoices",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BackupsApi#get_backup_invoices\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return ChargeInvoiceRows.from_json(data), status_code, headers
    end

    # Get Backup Storage Panel Login
    # Creates and returns a login session URL for the backup storage panel. The returned session URL can be used to redirect the user directly into the storage management interface without requiring separate credentials.
    # @param id [Int32] The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service.
    # @return [BackupLoginResponse]
    def get_backup_login(id : Int32)
      data, _status_code, _headers = get_backup_login_with_http_info(id)
      data
    end

    # Get Backup Storage Panel Login
    # Creates and returns a login session URL for the backup storage panel. The returned session URL can be used to redirect the user directly into the storage management interface without requiring separate credentials.
    # @param id [Int32] The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service.
    # @return [Array<(BackupLoginResponse, Integer, Hash)>] BackupLoginResponse data, response status code and response headers
    def get_backup_login_with_http_info(id : Int32)
      if @api_client.config.debugging
        Log.debug {"Calling API: BackupsApi.get_backup_login ..."}
      end
      # verify the required parameter "id" is set
      if @api_client.config.client_side_validation && id.nil?
        raise ArgumentError.new("Missing the required parameter 'id' when calling BackupsApi.get_backup_login")
      end
      # resource path
      local_var_path = "/backups/{id}/login".sub("{" + "id" + "}", URI.encode_path(id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"BackupsApi.get_backup_login",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BackupsApi#get_backup_login\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return BackupLoginResponse.from_json(data), status_code, headers
    end

    # List Backup Services
    # Returns all backup storage services on your account. Each entry includes the `backup_id`, `backup_username`, `backup_ip`, `backup_status`, and `backup_quota`. Use the `backup_id` with `/backups/{id}` to retrieve full service details or `/backups/{id}/login` to obtain a storage panel session.
    # @return [Array(BackupRow)]
    def get_backups_list()
      data, _status_code, _headers = get_backups_list_with_http_info()
      data
    end

    # List Backup Services
    # Returns all backup storage services on your account. Each entry includes the &#x60;backup_id&#x60;, &#x60;backup_username&#x60;, &#x60;backup_ip&#x60;, &#x60;backup_status&#x60;, and &#x60;backup_quota&#x60;. Use the &#x60;backup_id&#x60; with &#x60;/backups/{id}&#x60; to retrieve full service details or &#x60;/backups/{id}/login&#x60; to obtain a storage panel session.
    # @return [Array<(Array(BackupRow), Integer, Hash)>] Array(BackupRow) data, response status code and response headers
    def get_backups_list_with_http_info()
      if @api_client.config.debugging
        Log.debug {"Calling API: BackupsApi.get_backups_list ..."}
      end
      # resource path
      local_var_path = "/backups"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"BackupsApi.get_backups_list",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BackupsApi#get_backups_list\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return Array(BackupRow).from_json(data), status_code, headers
    end

    # Resend Backup Welcome Email
    # Resends the welcome email for the specified backup service. The email contains connection credentials and setup instructions. Use this when the original welcome email was lost or never received.
    # @param id [Int32] The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service.
    # @return [SuccessTextResponse]
    def get_backups_welcome_email(id : Int32)
      data, _status_code, _headers = get_backups_welcome_email_with_http_info(id)
      data
    end

    # Resend Backup Welcome Email
    # Resends the welcome email for the specified backup service. The email contains connection credentials and setup instructions. Use this when the original welcome email was lost or never received.
    # @param id [Int32] The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service.
    # @return [Array<(SuccessTextResponse, Integer, Hash)>] SuccessTextResponse data, response status code and response headers
    def get_backups_welcome_email_with_http_info(id : Int32)
      if @api_client.config.debugging
        Log.debug {"Calling API: BackupsApi.get_backups_welcome_email ..."}
      end
      # verify the required parameter "id" is set
      if @api_client.config.client_side_validation && id.nil?
        raise ArgumentError.new("Missing the required parameter 'id' when calling BackupsApi.get_backups_welcome_email")
      end
      # resource path
      local_var_path = "/backups/{id}/welcome_email".sub("{" + "id" + "}", URI.encode_path(id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"BackupsApi.get_backups_welcome_email",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BackupsApi#get_backups_welcome_email\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return SuccessTextResponse.from_json(data), status_code, headers
    end

    # Get Backup Order Form Data
    # Returns available backup storage plans, pricing tiers, and form metadata needed to build an order form. Use the service type IDs from this response when submitting a validation request via `PUT /backups/order` or placing an order via `POST /backups/order`.
    # @return [BackupsOrder]
    def get_new_backup()
      data, _status_code, _headers = get_new_backup_with_http_info()
      data
    end

    # Get Backup Order Form Data
    # Returns available backup storage plans, pricing tiers, and form metadata needed to build an order form. Use the service type IDs from this response when submitting a validation request via &#x60;PUT /backups/order&#x60; or placing an order via &#x60;POST /backups/order&#x60;.
    # @return [Array<(BackupsOrder, Integer, Hash)>] BackupsOrder data, response status code and response headers
    def get_new_backup_with_http_info()
      if @api_client.config.debugging
        Log.debug {"Calling API: BackupsApi.get_new_backup ..."}
      end
      # resource path
      local_var_path = "/backups/order"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :GET,
        path: local_var_path,
        operation: :"BackupsApi.get_new_backup",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BackupsApi#get_new_backup\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return BackupsOrder.from_json(data), status_code, headers
    end

    # Update Backup Information
    # Updates backup storage service metadata, such as stored credentials or settings for the order.
    # @param id [Int32] The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service.
    # @return [nil]
    def update_backup_info(id : Int32)
      update_backup_info_with_http_info(id)
      nil
    end

    # Update Backup Information
    # Updates backup storage service metadata, such as stored credentials or settings for the order.
    # @param id [Int32] The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service.
    # @return [Array<(nil, Integer, Hash)>] nil, response status code and response headers
    def update_backup_info_with_http_info(id : Int32)
      if @api_client.config.debugging
        Log.debug {"Calling API: BackupsApi.update_backup_info ..."}
      end
      # verify the required parameter "id" is set
      if @api_client.config.client_side_validation && id.nil?
        raise ArgumentError.new("Missing the required parameter 'id' when calling BackupsApi.update_backup_info")
      end
      # resource path
      local_var_path = "/backups/{id}".sub("{" + "id" + "}", URI.encode_path(id.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :POST,
        path: local_var_path,
        operation: :"BackupsApi.update_backup_info",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BackupsApi#update_backup_info\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return nil, status_code, headers
    end

    # Validate Backup Order
    # Validates a backup storage order without placing it, returning calculated pricing and any validation errors. Use this to display a confirmation screen with the final price before submitting the order via `POST /backups/order`.
    # @return [BackupOrderPutResponse]
    def validate_backup_order(validate_only : Bool? = nil, service_type : Int32? = nil, coupon : String? = nil)
      data, _status_code, _headers = validate_backup_order_with_http_info(validate_only, service_type, coupon)
      data
    end

    # Validate Backup Order
    # Validates a backup storage order without placing it, returning calculated pricing and any validation errors. Use this to display a confirmation screen with the final price before submitting the order via &#x60;POST /backups/order&#x60;.
    # @return [Array<(BackupOrderPutResponse, Integer, Hash)>] BackupOrderPutResponse data, response status code and response headers
    def validate_backup_order_with_http_info(validate_only : Bool? = nil, service_type : Int32? = nil, coupon : String? = nil)
      if @api_client.config.debugging
        Log.debug {"Calling API: BackupsApi.validate_backup_order ..."}
      end
      # resource path
      local_var_path = "/backups/order"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String | Array(String)).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["multipart/form-data", "application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new
      form_params[:"validateOnly"] = validate_only unless validate_only.nil?
      form_params[:"serviceType"] = service_type unless service_type.nil?
      form_params[:"coupon"] = coupon unless coupon.nil?

      # http body (model)
      post_body = nil

      # auth_names
      auth_names = ["sessionIdCookieAuth", "apiKeyAuth", "sessionIdHeaderAuth"]

      data, status_code, headers = @api_client.call_api(
        http_method: :PUT,
        path: local_var_path,
        operation: :"BackupsApi.validate_backup_order",
        post_body: post_body,
        auth_names: auth_names,
        header_params: header_params,
        query_params: query_params,
        cookie_params: cookie_params,
        form_params: form_params
      )

      if @api_client.config.debugging
        Log.debug {"API called: BackupsApi#validate_backup_order\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end

      return BackupOrderPutResponse.from_json(data), status_code, headers
    end
  end
end
