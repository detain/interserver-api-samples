// tslint:disable
/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import type { Observable } from 'rxjs';
import type { AjaxResponse } from 'rxjs/ajax';
import { BaseAPI, throwIfNullOrUndefined, encodeURI } from '../runtime';
import type { OperationOpts, HttpHeaders } from '../runtime';
import type {
    DnsListItem,
    DnsRecord,
    DnsRecordType,
    GetAccountInfo401Response,
} from '../models';

export interface AddDnsDomainRequest {
    domain: string;
    ip: string;
}

export interface AddDnsRecordRequest {
    id: string;
    name: string;
    type: DnsRecordType;
    content: string;
    ttl?: number;
    prio?: number;
}

export interface DeleteDnsDomainRequest {
    id: string;
}

export interface DeleteDnsRecordRequest {
    domainId: number;
    recordId: number;
}

export interface GetDnsDomainRequest {
    id: number;
}

export interface UpdateDnsRecordRequest {
    domainId: number;
    recordId: number;
    name?: string;
    type?: DnsRecordType;
    content?: string;
    ttl?: string;
    prio?: string;
    disabled?: string;
    ordername?: string;
    auth?: string;
}

/**
 * no description
 */
export class DNSApi extends BaseAPI {

    /**
     * Creates a new DNS domain and assigns an initial A record pointing to the supplied IP address. The domain is immediately available on InterServer\'s DNS servers. Use `/dns/{id}` to manage records after creation.
     * Create DNS Domain
     */
    addDnsDomain({ domain, ip }: AddDnsDomainRequest): Observable<void>
    addDnsDomain({ domain, ip }: AddDnsDomainRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>>
    addDnsDomain({ domain, ip }: AddDnsDomainRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>> {
        throwIfNullOrUndefined(domain, 'domain', 'addDnsDomain');
        throwIfNullOrUndefined(ip, 'ip', 'addDnsDomain');

        const headers: HttpHeaders = {
            ...(this.configuration.apiKey && { 'X-API-KEY': this.configuration.apiKey('X-API-KEY') }), // apiKeyAuth authentication
            ...(this.configuration.apiKey && { 'sessionid': this.configuration.apiKey('sessionid') }), // sessionIdHeaderAuth authentication
        };

        const formData = new FormData();
        if (domain !== undefined) { formData.append('domain', domain as any); }
        if (ip !== undefined) { formData.append('ip', ip as any); }

        return this.request<void>({
            url: '/dns',
            method: 'POST',
            headers,
            body: formData,
        }, opts?.responseOpts);
    };

    /**
     * Adds a new DNS record to the specified domain. Provide the record type (A, AAAA, CNAME, MX, TXT, etc.), name, content, TTL, and priority. The record takes effect on the DNS servers immediately. Use `GET /dns/{id}` afterward to confirm the record was created.
     * Add DNS Record to Domain
     */
    addDnsRecord({ id, name, type, content, ttl, prio }: AddDnsRecordRequest): Observable<void>
    addDnsRecord({ id, name, type, content, ttl, prio }: AddDnsRecordRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>>
    addDnsRecord({ id, name, type, content, ttl, prio }: AddDnsRecordRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>> {
        throwIfNullOrUndefined(id, 'id', 'addDnsRecord');
        throwIfNullOrUndefined(name, 'name', 'addDnsRecord');
        throwIfNullOrUndefined(type, 'type', 'addDnsRecord');
        throwIfNullOrUndefined(content, 'content', 'addDnsRecord');

        const headers: HttpHeaders = {
            ...(this.configuration.apiKey && { 'X-API-KEY': this.configuration.apiKey('X-API-KEY') }), // apiKeyAuth authentication
            ...(this.configuration.apiKey && { 'sessionid': this.configuration.apiKey('sessionid') }), // sessionIdHeaderAuth authentication
        };

        const formData = new FormData();
        if (name !== undefined) { formData.append('name', name as any); }
        if (type !== undefined) { formData.append('type', type as any); }
        if (content !== undefined) { formData.append('content', content as any); }
        if (ttl !== undefined) { formData.append('ttl', ttl as any); }
        if (prio !== undefined) { formData.append('prio', prio as any); }

        return this.request<void>({
            url: '/dns/{id}'.replace('{id}', encodeURI(id)),
            method: 'POST',
            headers,
            body: formData,
        }, opts?.responseOpts);
    };

    /**
     * Deletes a DNS domain and all of its associated records from the DNS servers. This action is permanent and cannot be undone. Any services relying on these DNS records will be affected immediately.
     * Delete DNS Domain
     */
    deleteDnsDomain({ id }: DeleteDnsDomainRequest): Observable<void>
    deleteDnsDomain({ id }: DeleteDnsDomainRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>>
    deleteDnsDomain({ id }: DeleteDnsDomainRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>> {
        throwIfNullOrUndefined(id, 'id', 'deleteDnsDomain');

        const headers: HttpHeaders = {
            ...(this.configuration.apiKey && { 'X-API-KEY': this.configuration.apiKey('X-API-KEY') }), // apiKeyAuth authentication
            ...(this.configuration.apiKey && { 'sessionid': this.configuration.apiKey('sessionid') }), // sessionIdHeaderAuth authentication
        };

        return this.request<void>({
            url: '/dns/{id}'.replace('{id}', encodeURI(id)),
            method: 'DELETE',
            headers,
        }, opts?.responseOpts);
    };

    /**
     * Removes a DNS record from the specified domain. The deletion takes effect on the DNS servers immediately. Use `GET /dns/{id}` to verify the record has been removed.
     * Delete DNS Record
     */
    deleteDnsRecord({ domainId, recordId }: DeleteDnsRecordRequest): Observable<void>
    deleteDnsRecord({ domainId, recordId }: DeleteDnsRecordRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>>
    deleteDnsRecord({ domainId, recordId }: DeleteDnsRecordRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>> {
        throwIfNullOrUndefined(domainId, 'domainId', 'deleteDnsRecord');
        throwIfNullOrUndefined(recordId, 'recordId', 'deleteDnsRecord');

        const headers: HttpHeaders = {
            ...(this.configuration.apiKey && { 'X-API-KEY': this.configuration.apiKey('X-API-KEY') }), // apiKeyAuth authentication
            ...(this.configuration.apiKey && { 'sessionid': this.configuration.apiKey('sessionid') }), // sessionIdHeaderAuth authentication
        };

        return this.request<void>({
            url: '/dns/{domainId}/{recordId}'.replace('{domainId}', encodeURI(domainId)).replace('{recordId}', encodeURI(recordId)),
            method: 'DELETE',
            headers,
        }, opts?.responseOpts);
    };

    /**
     * Returns the full set of DNS records for the specified domain, including NS, A, AAAA, CNAME, MX, TXT, and other record types. Use the record `id` values with `/dns/{domainId}/{recordId}` to update or delete individual records.
     * List Domain DNS Records
     */
    getDnsDomain({ id }: GetDnsDomainRequest): Observable<Array<DnsRecord>>
    getDnsDomain({ id }: GetDnsDomainRequest, opts?: OperationOpts): Observable<AjaxResponse<Array<DnsRecord>>>
    getDnsDomain({ id }: GetDnsDomainRequest, opts?: OperationOpts): Observable<Array<DnsRecord> | AjaxResponse<Array<DnsRecord>>> {
        throwIfNullOrUndefined(id, 'id', 'getDnsDomain');

        const headers: HttpHeaders = {
            ...(this.configuration.apiKey && { 'X-API-KEY': this.configuration.apiKey('X-API-KEY') }), // apiKeyAuth authentication
            ...(this.configuration.apiKey && { 'sessionid': this.configuration.apiKey('sessionid') }), // sessionIdHeaderAuth authentication
        };

        return this.request<Array<DnsRecord>>({
            url: '/dns/{id}'.replace('{id}', encodeURI(id)),
            method: 'GET',
            headers,
        }, opts?.responseOpts);
    };

    /**
     * Returns the DNS domains on your account along with their primary A record content. Use the `id` from each entry with `/dns/{id}` to retrieve the full record set, or to add, update, and delete individual records.
     * List DNS Domains
     */
    getDnsList(): Observable<Array<DnsListItem>>
    getDnsList(opts?: OperationOpts): Observable<AjaxResponse<Array<DnsListItem>>>
    getDnsList(opts?: OperationOpts): Observable<Array<DnsListItem> | AjaxResponse<Array<DnsListItem>>> {
        const headers: HttpHeaders = {
            ...(this.configuration.apiKey && { 'X-API-KEY': this.configuration.apiKey('X-API-KEY') }), // apiKeyAuth authentication
            ...(this.configuration.apiKey && { 'sessionid': this.configuration.apiKey('sessionid') }), // sessionIdHeaderAuth authentication
        };

        return this.request<Array<DnsListItem>>({
            url: '/dns',
            method: 'GET',
            headers,
        }, opts?.responseOpts);
    };

    /**
     * Updates an existing DNS record with new values. Use `GET /dns/{id}` to list records and retrieve the record IDs before updating. Changes propagate to the DNS servers immediately.
     * Update DNS Record
     */
    updateDnsRecord({ domainId, recordId, name, type, content, ttl, prio, disabled, ordername, auth }: UpdateDnsRecordRequest): Observable<void>
    updateDnsRecord({ domainId, recordId, name, type, content, ttl, prio, disabled, ordername, auth }: UpdateDnsRecordRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>>
    updateDnsRecord({ domainId, recordId, name, type, content, ttl, prio, disabled, ordername, auth }: UpdateDnsRecordRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>> {
        throwIfNullOrUndefined(domainId, 'domainId', 'updateDnsRecord');
        throwIfNullOrUndefined(recordId, 'recordId', 'updateDnsRecord');

        const headers: HttpHeaders = {
            ...(this.configuration.apiKey && { 'X-API-KEY': this.configuration.apiKey('X-API-KEY') }), // apiKeyAuth authentication
            ...(this.configuration.apiKey && { 'sessionid': this.configuration.apiKey('sessionid') }), // sessionIdHeaderAuth authentication
        };

        const formData = new FormData();
        if (name !== undefined) { formData.append('name', name as any); }
        if (type !== undefined) { formData.append('type', type as any); }
        if (content !== undefined) { formData.append('content', content as any); }
        if (ttl !== undefined) { formData.append('ttl', ttl as any); }
        if (prio !== undefined) { formData.append('prio', prio as any); }
        if (disabled !== undefined) { formData.append('disabled', disabled as any); }
        if (ordername !== undefined) { formData.append('ordername', ordername as any); }
        if (auth !== undefined) { formData.append('auth', auth as any); }

        return this.request<void>({
            url: '/dns/{domainId}/{recordId}'.replace('{domainId}', encodeURI(domainId)).replace('{recordId}', encodeURI(recordId)),
            method: 'POST',
            headers,
            body: formData,
        }, opts?.responseOpts);
    };

}
