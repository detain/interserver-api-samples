/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The VpsRow model module.
 * @module model/VpsRow
 * @version 0.9.0
 */
class VpsRow {
    /**
     * Constructs a new <code>VpsRow</code>.
     * A result row from the &#x60;Vps&#x60; &#x60;GET&#x60; request.
     * @alias module:model/VpsRow
     * @param vps_id {String} The id of the vps.
     * @param vps_name {String} The name of the vps.
     * @param repeat_invoices_cost {String} The repeat invoices cost of the vps.
     * @param vps_hostname {String} The hostname of the vps.
     * @param vps_ip {String} The ip of the vps.
     * @param vps_status {String} The status of the vps.
     * @param services_name {String} The services name of the vps.
     * @param vps_comment {String} The comment of the vps.
     */
    constructor(vps_id, vps_name, repeat_invoices_cost, vps_hostname, vps_ip, vps_status, services_name, vps_comment) { 
        
        VpsRow.initialize(this, vps_id, vps_name, repeat_invoices_cost, vps_hostname, vps_ip, vps_status, services_name, vps_comment);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, vps_id, vps_name, repeat_invoices_cost, vps_hostname, vps_ip, vps_status, services_name, vps_comment) { 
        obj['vps_id'] = vps_id;
        obj['vps_name'] = vps_name;
        obj['repeat_invoices_cost'] = repeat_invoices_cost;
        obj['vps_hostname'] = vps_hostname;
        obj['vps_ip'] = vps_ip;
        obj['vps_status'] = vps_status;
        obj['services_name'] = services_name;
        obj['vps_comment'] = vps_comment;
    }

    /**
     * Constructs a <code>VpsRow</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VpsRow} obj Optional instance to populate.
     * @return {module:model/VpsRow} The populated <code>VpsRow</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VpsRow();

            if (data.hasOwnProperty('vps_id')) {
                obj['vps_id'] = ApiClient.convertToType(data['vps_id'], 'String');
            }
            if (data.hasOwnProperty('vps_name')) {
                obj['vps_name'] = ApiClient.convertToType(data['vps_name'], 'String');
            }
            if (data.hasOwnProperty('repeat_invoices_cost')) {
                obj['repeat_invoices_cost'] = ApiClient.convertToType(data['repeat_invoices_cost'], 'String');
            }
            if (data.hasOwnProperty('vps_hostname')) {
                obj['vps_hostname'] = ApiClient.convertToType(data['vps_hostname'], 'String');
            }
            if (data.hasOwnProperty('vps_ip')) {
                obj['vps_ip'] = ApiClient.convertToType(data['vps_ip'], 'String');
            }
            if (data.hasOwnProperty('vps_status')) {
                obj['vps_status'] = ApiClient.convertToType(data['vps_status'], 'String');
            }
            if (data.hasOwnProperty('services_name')) {
                obj['services_name'] = ApiClient.convertToType(data['services_name'], 'String');
            }
            if (data.hasOwnProperty('vps_comment')) {
                obj['vps_comment'] = ApiClient.convertToType(data['vps_comment'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VpsRow</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VpsRow</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of VpsRow.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['vps_id'] && !(typeof data['vps_id'] === 'string' || data['vps_id'] instanceof String)) {
            throw new Error("Expected the field `vps_id` to be a primitive type in the JSON string but got " + data['vps_id']);
        }
        // ensure the json data is a string
        if (data['vps_name'] && !(typeof data['vps_name'] === 'string' || data['vps_name'] instanceof String)) {
            throw new Error("Expected the field `vps_name` to be a primitive type in the JSON string but got " + data['vps_name']);
        }
        // ensure the json data is a string
        if (data['repeat_invoices_cost'] && !(typeof data['repeat_invoices_cost'] === 'string' || data['repeat_invoices_cost'] instanceof String)) {
            throw new Error("Expected the field `repeat_invoices_cost` to be a primitive type in the JSON string but got " + data['repeat_invoices_cost']);
        }
        // ensure the json data is a string
        if (data['vps_hostname'] && !(typeof data['vps_hostname'] === 'string' || data['vps_hostname'] instanceof String)) {
            throw new Error("Expected the field `vps_hostname` to be a primitive type in the JSON string but got " + data['vps_hostname']);
        }
        // ensure the json data is a string
        if (data['vps_ip'] && !(typeof data['vps_ip'] === 'string' || data['vps_ip'] instanceof String)) {
            throw new Error("Expected the field `vps_ip` to be a primitive type in the JSON string but got " + data['vps_ip']);
        }
        // ensure the json data is a string
        if (data['vps_status'] && !(typeof data['vps_status'] === 'string' || data['vps_status'] instanceof String)) {
            throw new Error("Expected the field `vps_status` to be a primitive type in the JSON string but got " + data['vps_status']);
        }
        // ensure the json data is a string
        if (data['services_name'] && !(typeof data['services_name'] === 'string' || data['services_name'] instanceof String)) {
            throw new Error("Expected the field `services_name` to be a primitive type in the JSON string but got " + data['services_name']);
        }
        // ensure the json data is a string
        if (data['vps_comment'] && !(typeof data['vps_comment'] === 'string' || data['vps_comment'] instanceof String)) {
            throw new Error("Expected the field `vps_comment` to be a primitive type in the JSON string but got " + data['vps_comment']);
        }

        return true;
    }


}

VpsRow.RequiredProperties = ["vps_id", "vps_name", "repeat_invoices_cost", "vps_hostname", "vps_ip", "vps_status", "services_name", "vps_comment"];

/**
 * The id of the vps.
 * @member {String} vps_id
 */
VpsRow.prototype['vps_id'] = undefined;

/**
 * The name of the vps.
 * @member {String} vps_name
 */
VpsRow.prototype['vps_name'] = undefined;

/**
 * The repeat invoices cost of the vps.
 * @member {String} repeat_invoices_cost
 */
VpsRow.prototype['repeat_invoices_cost'] = undefined;

/**
 * The hostname of the vps.
 * @member {String} vps_hostname
 */
VpsRow.prototype['vps_hostname'] = undefined;

/**
 * The ip of the vps.
 * @member {String} vps_ip
 */
VpsRow.prototype['vps_ip'] = undefined;

/**
 * The status of the vps.
 * @member {String} vps_status
 */
VpsRow.prototype['vps_status'] = undefined;

/**
 * The services name of the vps.
 * @member {String} services_name
 */
VpsRow.prototype['services_name'] = undefined;

/**
 * The comment of the vps.
 * @member {String} vps_comment
 */
VpsRow.prototype['vps_comment'] = undefined;






export default VpsRow;

