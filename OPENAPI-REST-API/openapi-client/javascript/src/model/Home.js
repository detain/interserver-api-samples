/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import HomeDetails from './HomeDetails';
import HomeServices from './HomeServices';
import HomeTicketStatus from './HomeTicketStatus';
import HomeTicketStatusView from './HomeTicketStatusView';

/**
 * The Home model module.
 * @module model/Home
 * @version 0.9.0
 */
class Home {
    /**
     * Constructs a new <code>Home</code>.
     * @alias module:model/Home
     * @param last_login_ip {String} Last login IP.
     * @param last_login {String} Last login time.
     * @param currency {String} Currency symbol.
     * @param amount {String} Amount with currency.
     * @param invoice_list {Number} Number of invoices.
     * @param balance {String} Balance with currency.
     * @param full_name {String} Users full name.
     * @param email {String} User email address.
     * @param tickets {Array.<String>} List of tickets.
     * @param ticketStatus {module:model/HomeTicketStatus} 
     * @param ticketStatusView {module:model/HomeTicketStatusView} 
     * @param details {module:model/HomeDetails} 
     * @param services {module:model/HomeServices} 
     * @param AFFILIATE_AMOUNT {String} Affiliate amount with currency.
     */
    constructor(last_login_ip, last_login, currency, amount, invoice_list, balance, full_name, email, tickets, ticketStatus, ticketStatusView, details, services, AFFILIATE_AMOUNT) { 
        
        Home.initialize(this, last_login_ip, last_login, currency, amount, invoice_list, balance, full_name, email, tickets, ticketStatus, ticketStatusView, details, services, AFFILIATE_AMOUNT);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, last_login_ip, last_login, currency, amount, invoice_list, balance, full_name, email, tickets, ticketStatus, ticketStatusView, details, services, AFFILIATE_AMOUNT) { 
        obj['last_login_ip'] = last_login_ip;
        obj['last_login'] = last_login;
        obj['currency'] = currency;
        obj['amount'] = amount;
        obj['invoice_list'] = invoice_list;
        obj['balance'] = balance;
        obj['full_name'] = full_name;
        obj['email'] = email;
        obj['tickets'] = tickets;
        obj['ticketStatus'] = ticketStatus;
        obj['ticketStatusView'] = ticketStatusView;
        obj['details'] = details;
        obj['services'] = services;
        obj['AFFILIATE_AMOUNT'] = AFFILIATE_AMOUNT;
    }

    /**
     * Constructs a <code>Home</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Home} obj Optional instance to populate.
     * @return {module:model/Home} The populated <code>Home</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Home();

            if (data.hasOwnProperty('last_login_ip')) {
                obj['last_login_ip'] = ApiClient.convertToType(data['last_login_ip'], 'String');
            }
            if (data.hasOwnProperty('last_login')) {
                obj['last_login'] = ApiClient.convertToType(data['last_login'], 'String');
            }
            if (data.hasOwnProperty('currency')) {
                obj['currency'] = ApiClient.convertToType(data['currency'], 'String');
            }
            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'String');
            }
            if (data.hasOwnProperty('invoice_list')) {
                obj['invoice_list'] = ApiClient.convertToType(data['invoice_list'], 'Number');
            }
            if (data.hasOwnProperty('balance')) {
                obj['balance'] = ApiClient.convertToType(data['balance'], 'String');
            }
            if (data.hasOwnProperty('full_name')) {
                obj['full_name'] = ApiClient.convertToType(data['full_name'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('tickets')) {
                obj['tickets'] = ApiClient.convertToType(data['tickets'], ['String']);
            }
            if (data.hasOwnProperty('ticketStatus')) {
                obj['ticketStatus'] = HomeTicketStatus.constructFromObject(data['ticketStatus']);
            }
            if (data.hasOwnProperty('ticketStatusView')) {
                obj['ticketStatusView'] = HomeTicketStatusView.constructFromObject(data['ticketStatusView']);
            }
            if (data.hasOwnProperty('details')) {
                obj['details'] = HomeDetails.constructFromObject(data['details']);
            }
            if (data.hasOwnProperty('services')) {
                obj['services'] = HomeServices.constructFromObject(data['services']);
            }
            if (data.hasOwnProperty('AFFILIATE_AMOUNT')) {
                obj['AFFILIATE_AMOUNT'] = ApiClient.convertToType(data['AFFILIATE_AMOUNT'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Home</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Home</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Home.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['last_login_ip'] && !(typeof data['last_login_ip'] === 'string' || data['last_login_ip'] instanceof String)) {
            throw new Error("Expected the field `last_login_ip` to be a primitive type in the JSON string but got " + data['last_login_ip']);
        }
        // ensure the json data is a string
        if (data['last_login'] && !(typeof data['last_login'] === 'string' || data['last_login'] instanceof String)) {
            throw new Error("Expected the field `last_login` to be a primitive type in the JSON string but got " + data['last_login']);
        }
        // ensure the json data is a string
        if (data['currency'] && !(typeof data['currency'] === 'string' || data['currency'] instanceof String)) {
            throw new Error("Expected the field `currency` to be a primitive type in the JSON string but got " + data['currency']);
        }
        // ensure the json data is a string
        if (data['amount'] && !(typeof data['amount'] === 'string' || data['amount'] instanceof String)) {
            throw new Error("Expected the field `amount` to be a primitive type in the JSON string but got " + data['amount']);
        }
        // ensure the json data is a string
        if (data['balance'] && !(typeof data['balance'] === 'string' || data['balance'] instanceof String)) {
            throw new Error("Expected the field `balance` to be a primitive type in the JSON string but got " + data['balance']);
        }
        // ensure the json data is a string
        if (data['full_name'] && !(typeof data['full_name'] === 'string' || data['full_name'] instanceof String)) {
            throw new Error("Expected the field `full_name` to be a primitive type in the JSON string but got " + data['full_name']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['tickets'])) {
            throw new Error("Expected the field `tickets` to be an array in the JSON data but got " + data['tickets']);
        }
        // validate the optional field `ticketStatus`
        if (data['ticketStatus']) { // data not null
          HomeTicketStatus.validateJSON(data['ticketStatus']);
        }
        // validate the optional field `ticketStatusView`
        if (data['ticketStatusView']) { // data not null
          HomeTicketStatusView.validateJSON(data['ticketStatusView']);
        }
        // validate the optional field `details`
        if (data['details']) { // data not null
          HomeDetails.validateJSON(data['details']);
        }
        // validate the optional field `services`
        if (data['services']) { // data not null
          HomeServices.validateJSON(data['services']);
        }
        // ensure the json data is a string
        if (data['AFFILIATE_AMOUNT'] && !(typeof data['AFFILIATE_AMOUNT'] === 'string' || data['AFFILIATE_AMOUNT'] instanceof String)) {
            throw new Error("Expected the field `AFFILIATE_AMOUNT` to be a primitive type in the JSON string but got " + data['AFFILIATE_AMOUNT']);
        }

        return true;
    }


}

Home.RequiredProperties = ["last_login_ip", "last_login", "currency", "amount", "invoice_list", "balance", "full_name", "email", "tickets", "ticketStatus", "ticketStatusView", "details", "services", "AFFILIATE_AMOUNT"];

/**
 * Last login IP.
 * @member {String} last_login_ip
 */
Home.prototype['last_login_ip'] = undefined;

/**
 * Last login time.
 * @member {String} last_login
 */
Home.prototype['last_login'] = undefined;

/**
 * Currency symbol.
 * @member {String} currency
 */
Home.prototype['currency'] = undefined;

/**
 * Amount with currency.
 * @member {String} amount
 */
Home.prototype['amount'] = undefined;

/**
 * Number of invoices.
 * @member {Number} invoice_list
 */
Home.prototype['invoice_list'] = undefined;

/**
 * Balance with currency.
 * @member {String} balance
 */
Home.prototype['balance'] = undefined;

/**
 * Users full name.
 * @member {String} full_name
 */
Home.prototype['full_name'] = undefined;

/**
 * User email address.
 * @member {String} email
 */
Home.prototype['email'] = undefined;

/**
 * List of tickets.
 * @member {Array.<String>} tickets
 */
Home.prototype['tickets'] = undefined;

/**
 * @member {module:model/HomeTicketStatus} ticketStatus
 */
Home.prototype['ticketStatus'] = undefined;

/**
 * @member {module:model/HomeTicketStatusView} ticketStatusView
 */
Home.prototype['ticketStatusView'] = undefined;

/**
 * @member {module:model/HomeDetails} details
 */
Home.prototype['details'] = undefined;

/**
 * @member {module:model/HomeServices} services
 */
Home.prototype['services'] = undefined;

/**
 * Affiliate amount with currency.
 * @member {String} AFFILIATE_AMOUNT
 */
Home.prototype['AFFILIATE_AMOUNT'] = undefined;






export default Home;

