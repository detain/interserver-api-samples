/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ServerRow model module.
 * @module model/ServerRow
 * @version 0.9.0
 */
class ServerRow {
    /**
     * Constructs a new <code>ServerRow</code>.
     * A result row from the &#x60;Servers&#x60; &#x60;GET&#x60; request.
     * @alias module:model/ServerRow
     * @param server_id {String} The id of the server.
     * @param account_lid {String} The account lid of the server.
     * @param server_hostname {String} The hostname of the server.
     * @param server_status {String} The status of the server.
     */
    constructor(server_id, account_lid, server_hostname, server_status) { 
        
        ServerRow.initialize(this, server_id, account_lid, server_hostname, server_status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, server_id, account_lid, server_hostname, server_status) { 
        obj['server_id'] = server_id;
        obj['account_lid'] = account_lid;
        obj['server_hostname'] = server_hostname;
        obj['server_status'] = server_status;
    }

    /**
     * Constructs a <code>ServerRow</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ServerRow} obj Optional instance to populate.
     * @return {module:model/ServerRow} The populated <code>ServerRow</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ServerRow();

            if (data.hasOwnProperty('server_id')) {
                obj['server_id'] = ApiClient.convertToType(data['server_id'], 'String');
            }
            if (data.hasOwnProperty('account_lid')) {
                obj['account_lid'] = ApiClient.convertToType(data['account_lid'], 'String');
            }
            if (data.hasOwnProperty('server_hostname')) {
                obj['server_hostname'] = ApiClient.convertToType(data['server_hostname'], 'String');
            }
            if (data.hasOwnProperty('server_status')) {
                obj['server_status'] = ApiClient.convertToType(data['server_status'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ServerRow</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ServerRow</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ServerRow.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['server_id'] && !(typeof data['server_id'] === 'string' || data['server_id'] instanceof String)) {
            throw new Error("Expected the field `server_id` to be a primitive type in the JSON string but got " + data['server_id']);
        }
        // ensure the json data is a string
        if (data['account_lid'] && !(typeof data['account_lid'] === 'string' || data['account_lid'] instanceof String)) {
            throw new Error("Expected the field `account_lid` to be a primitive type in the JSON string but got " + data['account_lid']);
        }
        // ensure the json data is a string
        if (data['server_hostname'] && !(typeof data['server_hostname'] === 'string' || data['server_hostname'] instanceof String)) {
            throw new Error("Expected the field `server_hostname` to be a primitive type in the JSON string but got " + data['server_hostname']);
        }
        // ensure the json data is a string
        if (data['server_status'] && !(typeof data['server_status'] === 'string' || data['server_status'] instanceof String)) {
            throw new Error("Expected the field `server_status` to be a primitive type in the JSON string but got " + data['server_status']);
        }

        return true;
    }


}

ServerRow.RequiredProperties = ["server_id", "account_lid", "server_hostname", "server_status"];

/**
 * The id of the server.
 * @member {String} server_id
 */
ServerRow.prototype['server_id'] = undefined;

/**
 * The account lid of the server.
 * @member {String} account_lid
 */
ServerRow.prototype['account_lid'] = undefined;

/**
 * The hostname of the server.
 * @member {String} server_hostname
 */
ServerRow.prototype['server_hostname'] = undefined;

/**
 * The status of the server.
 * @member {String} server_status
 */
ServerRow.prototype['server_status'] = undefined;






export default ServerRow;

