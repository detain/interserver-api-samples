/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import LoginServiceCounts from './LoginServiceCounts';

/**
 * The LoginInfo model module.
 * @module model/LoginInfo
 * @version 0.9.0
 */
class LoginInfo {
    /**
     * Constructs a new <code>LoginInfo</code>.
     * Basic information useful for rendering a login page.
     * @alias module:model/LoginInfo
     * @param captcha {String} A base64 encoded image to use for rendering the alternateive captcha.
     * @param counts {module:model/LoginServiceCounts} 
     */
    constructor(captcha, counts) { 
        
        LoginInfo.initialize(this, captcha, counts);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, captcha, counts) { 
        obj['captcha'] = captcha;
        obj['counts'] = counts;
    }

    /**
     * Constructs a <code>LoginInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LoginInfo} obj Optional instance to populate.
     * @return {module:model/LoginInfo} The populated <code>LoginInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LoginInfo();

            if (data.hasOwnProperty('captcha')) {
                obj['captcha'] = ApiClient.convertToType(data['captcha'], 'String');
            }
            if (data.hasOwnProperty('counts')) {
                obj['counts'] = LoginServiceCounts.constructFromObject(data['counts']);
            }
            if (data.hasOwnProperty('logo')) {
                obj['logo'] = ApiClient.convertToType(data['logo'], 'String');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LoginInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LoginInfo</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of LoginInfo.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['captcha'] && !(typeof data['captcha'] === 'string' || data['captcha'] instanceof String)) {
            throw new Error("Expected the field `captcha` to be a primitive type in the JSON string but got " + data['captcha']);
        }
        // validate the optional field `counts`
        if (data['counts']) { // data not null
          LoginServiceCounts.validateJSON(data['counts']);
        }
        // ensure the json data is a string
        if (data['logo'] && !(typeof data['logo'] === 'string' || data['logo'] instanceof String)) {
            throw new Error("Expected the field `logo` to be a primitive type in the JSON string but got " + data['logo']);
        }
        // ensure the json data is a string
        if (data['language'] && !(typeof data['language'] === 'string' || data['language'] instanceof String)) {
            throw new Error("Expected the field `language` to be a primitive type in the JSON string but got " + data['language']);
        }

        return true;
    }


}

LoginInfo.RequiredProperties = ["captcha", "counts"];

/**
 * A base64 encoded image to use for rendering the alternateive captcha.
 * @member {String} captcha
 */
LoginInfo.prototype['captcha'] = undefined;

/**
 * @member {module:model/LoginServiceCounts} counts
 */
LoginInfo.prototype['counts'] = undefined;

/**
 * A logo image url.
 * @member {String} logo
 */
LoginInfo.prototype['logo'] = undefined;

/**
 * The desired langauge to render the site with.
 * @member {String} language
 */
LoginInfo.prototype['language'] = undefined;






export default LoginInfo;

