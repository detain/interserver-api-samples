/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The BackupRow model module.
 * @module model/BackupRow
 * @version 0.9.0
 */
class BackupRow {
    /**
     * Constructs a new <code>BackupRow</code>.
     * A result row from the &#x60;Backups&#x60; &#x60;GET&#x60; request.
     * @alias module:model/BackupRow
     */
    constructor() { 
        
        BackupRow.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>BackupRow</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BackupRow} obj Optional instance to populate.
     * @return {module:model/BackupRow} The populated <code>BackupRow</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BackupRow();

            if (data.hasOwnProperty('backup_id')) {
                obj['backup_id'] = ApiClient.convertToType(data['backup_id'], 'String');
            }
            if (data.hasOwnProperty('backup_name')) {
                obj['backup_name'] = ApiClient.convertToType(data['backup_name'], 'String');
            }
            if (data.hasOwnProperty('backup_cost')) {
                obj['backup_cost'] = ApiClient.convertToType(data['backup_cost'], 'String');
            }
            if (data.hasOwnProperty('backup_username')) {
                obj['backup_username'] = ApiClient.convertToType(data['backup_username'], 'String');
            }
            if (data.hasOwnProperty('backup_status')) {
                obj['backup_status'] = ApiClient.convertToType(data['backup_status'], 'String');
            }
            if (data.hasOwnProperty('services_name')) {
                obj['services_name'] = ApiClient.convertToType(data['services_name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BackupRow</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BackupRow</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['backup_id'] && !(typeof data['backup_id'] === 'string' || data['backup_id'] instanceof String)) {
            throw new Error("Expected the field `backup_id` to be a primitive type in the JSON string but got " + data['backup_id']);
        }
        // ensure the json data is a string
        if (data['backup_name'] && !(typeof data['backup_name'] === 'string' || data['backup_name'] instanceof String)) {
            throw new Error("Expected the field `backup_name` to be a primitive type in the JSON string but got " + data['backup_name']);
        }
        // ensure the json data is a string
        if (data['backup_cost'] && !(typeof data['backup_cost'] === 'string' || data['backup_cost'] instanceof String)) {
            throw new Error("Expected the field `backup_cost` to be a primitive type in the JSON string but got " + data['backup_cost']);
        }
        // ensure the json data is a string
        if (data['backup_username'] && !(typeof data['backup_username'] === 'string' || data['backup_username'] instanceof String)) {
            throw new Error("Expected the field `backup_username` to be a primitive type in the JSON string but got " + data['backup_username']);
        }
        // ensure the json data is a string
        if (data['backup_status'] && !(typeof data['backup_status'] === 'string' || data['backup_status'] instanceof String)) {
            throw new Error("Expected the field `backup_status` to be a primitive type in the JSON string but got " + data['backup_status']);
        }
        // ensure the json data is a string
        if (data['services_name'] && !(typeof data['services_name'] === 'string' || data['services_name'] instanceof String)) {
            throw new Error("Expected the field `services_name` to be a primitive type in the JSON string but got " + data['services_name']);
        }

        return true;
    }


}



/**
 * The id of the backup.
 * @member {String} backup_id
 */
BackupRow.prototype['backup_id'] = undefined;

/**
 * The name of the backup.
 * @member {String} backup_name
 */
BackupRow.prototype['backup_name'] = undefined;

/**
 * The cost of the backup.
 * @member {String} backup_cost
 */
BackupRow.prototype['backup_cost'] = undefined;

/**
 * The username of the backup.
 * @member {String} backup_username
 */
BackupRow.prototype['backup_username'] = undefined;

/**
 * The status of the backup.
 * @member {String} backup_status
 */
BackupRow.prototype['backup_status'] = undefined;

/**
 * The services name of the backup.
 * @member {String} services_name
 */
BackupRow.prototype['services_name'] = undefined;






export default BackupRow;

