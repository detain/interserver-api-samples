/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package org.openapitools.client.model

import com.github.plokhotnyuk.jsoniter_scala.circe.JsoniterScalaCodec.*
import com.github.plokhotnyuk.jsoniter_scala.macros.named

case class ServerAsset(
  /* Unique identifier for the asset. */
  @named("id") id: Int,
  /* Order identifier for the asset. */
  @named("order_id") order_id: String,
  /* Hostname associated with the asset. */
  @named("hostname") hostname: String,
  /* Status of the asset. */
  @named("status") status: String,
  /* Primary IPv4 address of the asset. */
  @named("primary_ipv4") primary_ipv4: String,
  /* Primary IPv6 address of the asset. */
  @named("primary_ipv6") primary_ipv6: String,
  /* Datacenter identifier for the asset. */
  @named("datacenter") datacenter: String,
  /* Type identifier for the asset. */
  @named("type_id") type_id: String,
  /* Asset tag associated with the asset. */
  @named("asset_tag") asset_tag: String,
  /* Rack identifier for the asset. */
  @named("rack") rack: String,
  /* Row identifier for the asset. */
  @named("row") row: String,
  /* Column identifier for the asset. */
  @named("col") col: String,
  /* Starting unit identifier for the asset. */
  @named("unit_start") unit_start: String,
  /* Ending unit identifier for the asset. */
  @named("unit_end") unit_end: String,
  /* Subunit identifier for the asset. */
  @named("unit_sub") unit_sub: String,
  /* IPMI MAC address associated with the asset. */
  @named("ipmi_mac") ipmi_mac: String,
  /* IPMI IP address associated with the asset. */
  @named("ipmi_ip") ipmi_ip: String,
  /* IPMI working status associated with the asset. */
  @named("ipmi_working") ipmi_working: String,
  /* Company associated with the asset. */
  @named("company") company: String,
  /* Comments associated with the asset. */
  @named("comments") comments: String,
  /* Make of the asset. */
  @named("make") make: String,
  /* Model of the asset. */
  @named("model") model: String,
  /* Description of the asset. */
  @named("description") description: String,
  /* Customer identifier for the asset. */
  @named("customer_id") customer_id: String,
  /* External identifier for the asset. */
  @named("external_id") external_id: String,
  /* Billing status of the asset. */
  @named("billing_status") billing_status: String,
  /* Overdue status of the asset. */
  @named("overdue") overdue: String,
  /* Asset identifier for the asset. */
  @named("asset_id") asset_id: String,
  /* Name of the asset. */
  @named("asset_name") asset_name: String,
  /* Rack identifier for the asset. */
  @named("rack_id") rack_id: String,
  /* Rack name associated with the asset. */
  @named("rack_name") rack_name: String,
  /* Location of the rack associated with the asset. */
  @named("rack_location") rack_location: String,
  /* Size of the rack associated with the asset. */
  @named("rack_size") rack_size: String,
  /* X-coordinate of the asset within the rack. */
  @named("rack_x") rack_x: String,
  /* Y-coordinate of the asset within the rack. */
  @named("rack_y") rack_y: String,
  /* List of switchports associated with the asset. */
  @named("switchports") switchports: Seq[Int],
  /* List of VLANs associated with the asset. */
  @named("vlans") vlans: Seq[String],
  /* List of IPv6 VLANs associated with the asset. */
  @named("vlans6") vlans6: Seq[String],
  @named("lease") lease: ServerLease,
  /* MAC address associated with the asset. */
  @named("mac") mac: Option[io.circe.Json] = scala.None,
  /* IPMI admin username associated with the asset. */
  @named("ipmi_admin_username") ipmi_admin_username: Option[io.circe.Json] = scala.None,
  /* IPMI admin password associated with the asset. */
  @named("ipmi_admin_password") ipmi_admin_password: Option[io.circe.Json] = scala.None,
  /* IPMI client username associated with the asset. */
  @named("ipmi_client_username") ipmi_client_username: Option[io.circe.Json] = scala.None,
  /* IPMI client password associated with the asset. */
  @named("ipmi_client_password") ipmi_client_password: Option[io.circe.Json] = scala.None,
  /* IPMI update status associated with the asset. */
  @named("ipmi_updated") ipmi_updated: Option[io.circe.Json] = scala.None,
  /* Timestamp of asset creation. */
  @named("create_timestamp") create_timestamp: Option[io.circe.Json] = scala.None,
  /* Timestamp of asset update. */
  @named("update_timestamp") update_timestamp: Option[io.circe.Json] = scala.None,
  /* Comment associated with the asset. */
  @named("comment") comment: Option[io.circe.Json] = scala.None
)

