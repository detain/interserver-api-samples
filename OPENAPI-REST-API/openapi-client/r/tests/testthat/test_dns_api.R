# Automatically generated by openapi-generator (https://openapi-generator.tech)
# Please update as you see appropriate

context("Test DNSApi")

api_instance <- DNSApi$new()

test_that("AddDnsDomain", {
  # tests for AddDnsDomain
  # base path: https://my.interserver.net/apiv2
  # Create DNS Domain
  # Creates a new DNS domain and assigns an initial A record pointing to the supplied IP address. The domain is immediately available on InterServer&#39;s DNS servers. Use &#x60;/dns/{id}&#x60; to manage records after creation.
  # @param domain character The domain name.
  # @param ip character IP Address to point the domain to.
  # @return [Void]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("AddDnsRecord", {
  # tests for AddDnsRecord
  # base path: https://my.interserver.net/apiv2
  # Add DNS Record to Domain
  # Adds a new DNS record to the specified domain. Provide the record type (A, AAAA, CNAME, MX, TXT, etc.), name, content, TTL, and priority. The record takes effect on the DNS servers immediately. Use &#x60;GET /dns/{id}&#x60; afterward to confirm the record was created.
  # @param id character The DNS Domain ID.
  # @param name character Name part of record
  # @param type DnsRecordType 
  # @param content character Content of record
  # @param ttl integer Time-to-live (optional)
  # @param prio integer Priority (optional)
  # @return [Void]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("DeleteDnsDomain", {
  # tests for DeleteDnsDomain
  # base path: https://my.interserver.net/apiv2
  # Delete DNS Domain
  # Deletes a DNS domain and all of its associated records from the DNS servers. This action is permanent and cannot be undone. Any services relying on these DNS records will be affected immediately.
  # @param id character The DNS domain ID to delete. Use the `id` from `GET /dns` to identify the domain.
  # @return [Void]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("DeleteDnsRecord", {
  # tests for DeleteDnsRecord
  # base path: https://my.interserver.net/apiv2
  # Delete DNS Record
  # Removes a DNS record from the specified domain. The deletion takes effect on the DNS servers immediately. Use &#x60;GET /dns/{id}&#x60; to verify the record has been removed.
  # @param domain_id integer The DNS domain ID. Use the `id` from `GET /dns` to identify the domain.
  # @param record_id integer The DNS record ID within the domain. Use the record `id` from `GET /dns/{id}` to identify the record.
  # @return [Void]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetDnsDomain", {
  # tests for GetDnsDomain
  # base path: https://my.interserver.net/apiv2
  # List Domain DNS Records
  # Returns the full set of DNS records for the specified domain, including NS, A, AAAA, CNAME, MX, TXT, and other record types. Use the record &#x60;id&#x60; values with &#x60;/dns/{domainId}/{recordId}&#x60; to update or delete individual records.
  # @param id integer The DNS domain ID. Use the `id` from `GET /dns` to identify the domain.
  # @return [array[DnsRecord]]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("GetDnsList", {
  # tests for GetDnsList
  # base path: https://my.interserver.net/apiv2
  # List DNS Domains
  # Returns the DNS domains on your account along with their primary A record content. Use the &#x60;id&#x60; from each entry with &#x60;/dns/{id}&#x60; to retrieve the full record set, or to add, update, and delete individual records.
  # @return [array[DnsListItem]]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})

test_that("UpdateDnsRecord", {
  # tests for UpdateDnsRecord
  # base path: https://my.interserver.net/apiv2
  # Update DNS Record
  # Updates an existing DNS record with new values. Use &#x60;GET /dns/{id}&#x60; to list records and retrieve the record IDs before updating. Changes propagate to the DNS servers immediately.
  # @param domain_id integer The DNS domain ID. Use the `id` from `GET /dns` to identify the domain.
  # @param record_id integer The DNS record ID within the domain. Use the record `id` from `GET /dns/{id}` to identify the record.
  # @param name character  (optional)
  # @param type DnsRecordType  (optional)
  # @param content character  (optional)
  # @param ttl character  (optional)
  # @param prio character  (optional)
  # @param disabled character  (optional)
  # @param ordername character  (optional)
  # @param auth character  (optional)
  # @return [Void]

  # uncomment below to test the operation
  #expect_equal(result, "EXPECTED_RESULT")
})
