/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * OpenAPI spec version: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator
 * https://github.com/OpenAPITools/openapi-generator
 * Do not edit the class manually.
 */

#pragma once

#include "OpenAPIBaseModel.h"

namespace OpenAPI
{

/*
 * OpenAPIServerServiceInfo
 *
 * 
 */
class OPENAPI_API OpenAPIServerServiceInfo : public Model
{
public:
    virtual ~OpenAPIServerServiceInfo() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* The ID of the server. */
	TOptional<FString> ServerId;
	/* The hostname of the server. */
	TOptional<FString> ServerHostname;
	/* The customer ID associated with the server. */
	TOptional<FString> ServerCustid;
	/* The type of the server. */
	TOptional<FString> ServerType;
	/* The currency used for billing. */
	TOptional<FString> ServerCurrency;
	/* The date when the server was ordered. */
	TOptional<FString> ServerOrderDate;
	/* The invoice number for the server. */
	TOptional<FString> ServerInvoice;
	/* The coupon associated with the server. */
	TOptional<FString> ServerCoupon;
	/* The status of the server. */
	TOptional<FString> ServerStatus;
	/* The root of the server. */
	TOptional<FString> ServerRoot;
	/* The dedicated tag of the server. */
	TOptional<FString> ServerDedicatedTag;
	/* The custom tag of the server. */
	TOptional<FString> ServerCustomTag;
	/* Comments related to the server. */
	TOptional<FString> ServerComment;
	/* The initial billing amount for the server. */
	TOptional<FString> ServerInitialBill;
	/* The hardware information of the server. */
	TOptional<FString> ServerHardware;
	/* The number of IPs associated with the server. */
	TOptional<FString> ServerIps;
	/* The monthly billing amount for the server. */
	TOptional<FString> ServerMonthlyBill;
	/* The setup status of the server. */
	TOptional<FString> ServerSetup;
	/* Discount information for the server. */
	TOptional<FString> ServerDiscount;
	/* The reputation of the server. */
	TOptional<FString> ServerRep;
	/* The date related to the server. */
	TOptional<FString> ServerDate;
	/* The total cost of the server. */
	TOptional<FString> ServerTotalCost;
	/* The location of the server. */
	TOptional<FString> ServerLocation;
	/* The ordered hardware for the server. */
	TOptional<FString> ServerHardwareOrdered;
	/* The billed amount for the server. */
	TOptional<FString> ServerBilled;
	/* Indicates whether a welcome email was sent. */
	TOptional<FString> ServerWelcomeEmail;
	/* The number of dedicated CPUs for the server. */
	TOptional<FString> ServerDedicatedCpu;
	/* The amount of dedicated memory for the server. */
	TOptional<FString> ServerDedicatedMemory;
	/* The size of the first dedicated hard drive. */
	TOptional<FString> ServerDedicatedHd1;
	/* The size of the second dedicated hard drive. */
	TOptional<FString> ServerDedicatedHd2;
	/* The bandwidth of the server. */
	TOptional<FString> ServerDedicatedBandwidth;
	/* The number of dedicated IPs for the server. */
	TOptional<FString> ServerDedicatedIps;
	/* The operating system of the server. */
	TOptional<FString> ServerDedicatedOs;
	/* The control panel of the server. */
	TOptional<FString> ServerDedicatedCp;
	/* The RAID configuration of the server. */
	TOptional<FString> ServerDedicatedRaid;
	/* Additional information about the server. */
	TOptional<FString> ServerExtra;
};

}
