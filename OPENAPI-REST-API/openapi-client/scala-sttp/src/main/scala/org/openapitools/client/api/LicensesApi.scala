/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package org.openapitools.client.api

import org.openapitools.client.model.ChargeInvoiceRows
import org.openapitools.client.model.GetAccountInfo401Response
import org.openapitools.client.model.IpObject
import org.openapitools.client.model.License
import org.openapitools.client.model.LicenseRow
import org.openapitools.client.model.LicensesCancel200Response
import org.openapitools.client.model.LicensesOrder
import org.openapitools.client.model.ServiceOrderPostResponse
import org.openapitools.client.model.SuccessTextResponse
import org.openapitools.client.core.JsonSupport._
import sttp.client3._
import sttp.model.Method

object LicensesApi {
  def apply(baseUrl: String = "https://my.interserver.net/apiv2") = new LicensesApi(baseUrl)
}

class LicensesApi(baseUrl: String) {

  /**
   * Places an order for a new software license. Use `PUT /licenses/order` to validate the order first.
   * 
   * Expected answers:
   *   code 200 : ServiceOrderPostResponse (Order placed successfully. Use the invoice ID to proceed to payment via `/pay/{method}/{invoices}` or view the invoice at `/billing/invoices/{id}`.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   */
  def addLicense(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(
): Request[Either[ResponseException[String, Exception], ServiceOrderPostResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/licenses/order")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[ServiceOrderPostResponse])

  /**
   * Returns detailed information about a specific license including its type, IP assignment, and status.
   * 
   * Expected answers:
   *   code 200 : License (License information.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id The license service ID. Use `license_id` from `GET /licenses`.
   */
  def getLicenseInfo(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], License], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/licenses/${id}")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[License])

  /**
   * Returns the billing invoices associated with this license service.
   * 
   * Expected answers:
   *   code 200 : ChargeInvoiceRows (Get Invoices response)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id The license service ID. Use `license_id` from `GET /licenses`.
   */
  def getLicenseInvoices(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], ChargeInvoiceRows], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/licenses/${id}/invoices")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[ChargeInvoiceRows])

  /**
   * Returns all software license services on the account with their current status and IP assignments.
   * 
   * Expected answers:
   *   code 200 : Seq[LicenseRow] (The listing of `Licenses` services on your account.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   */
  def getLicenseList(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(
): Request[Either[ResponseException[String, Exception], Seq[LicenseRow]], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/licenses")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[Seq[LicenseRow]])

  /**
   * Returns the available license types and pricing for a specific license category. Use the category tags from `GET /licenses/order` to identify valid values.
   * 
   * Expected answers:
   *   code 200 :  (License types and pricing for the specified category.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param catTag The license category tag (e.g. `cpanel`, `plesk`). Obtain valid values from the `GET /licenses/order` response.
   */
  def getLicenseOrderCatTagInfo(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(catTag: String
): Request[Either[ResponseException[String, Exception], Unit], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/licenses/order/${catTag}")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asString.mapWithMetadata(ResponseAs.deserializeRightWithError(_ => Right(()))))

  /**
   * Resends the welcome email for the license service. The email contains the license key and activation instructions.
   * 
   * Expected answers:
   *   code 200 : SuccessTextResponse (A response indicating the operation completed successfully with a text message.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id The license service ID. Use `license_id` from `GET /licenses`.
   */
  def getLicensesWelcomeEmail(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], SuccessTextResponse], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/licenses/${id}/welcome_email")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[SuccessTextResponse])

  /**
   * Retrieves available license types, categories, and pricing for ordering a new license.
   * 
   * Expected answers:
   *   code 200 : LicensesOrder (License ordering information.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   */
  def getNewLicense(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(
): Request[Either[ResponseException[String, Exception], LicensesOrder], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/licenses/order")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[LicensesOrder])

  /**
   * Cancels a license service. After cancellation the license key is deactivated and the service transitions to a canceled status. No further billing charges will be incurred.
   * 
   * Expected answers:
   *   code 200 : LicensesCancel200Response (License Cancel)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id The license service ID. Use `license_id` from `GET /licenses`.
   */
  def licensesCancel(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], LicensesCancel200Response], Any] =
    basicRequest
      .method(Method.DELETE, uri"$baseUrl/licenses/${id}")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[LicensesCancel200Response])

  /**
   * Changes the IP address associated with the license. The service must be active. Use `GET /licenses/{id}` to view the current IP assignment before making changes.
   * 
   * Expected answers:
   *   code 200 : SuccessTextResponse (A response indicating the operation completed successfully with a text message.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id The license service ID. Use `license_id` from `GET /licenses`.
   * @param ipObject 
   */
  def postLicenseChangeIp(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int, ipObject: IpObject
): Request[Either[ResponseException[String, Exception], SuccessTextResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/licenses/${id}/change_ip")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .body(ipObject)
      .response(asJson[SuccessTextResponse])

  /**
   * Validates a license order before placing it. Use this to check for errors before committing to a purchase.
   * 
   * Expected answers:
   *   code 200 :  (Validate Licenses order response)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   */
  def putLicenses(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(
): Request[Either[ResponseException[String, Exception], Unit], Any] =
    basicRequest
      .method(Method.PUT, uri"$baseUrl/licenses/order")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asString.mapWithMetadata(ResponseAs.deserializeRightWithError(_ => Right(()))))

  /**
   * Updates settings on a license service such as its assigned IP.
   * 
   * Expected answers:
   *   code 200 : SuccessTextResponse (A response indicating the operation completed successfully with a text message.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id The license service ID. Use `license_id` from `GET /licenses`.
   */
  def updateLicenseInfo(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: String
): Request[Either[ResponseException[String, Exception], SuccessTextResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/licenses/${id}")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[SuccessTextResponse])

}
