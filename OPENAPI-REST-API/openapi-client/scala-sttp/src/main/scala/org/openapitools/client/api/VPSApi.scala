/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package org.openapitools.client.api

import org.openapitools.client.model.ChargeInvoiceRows
import org.openapitools.client.model.DownloadQsBackup200Response
import org.openapitools.client.model.DownloadQsBackupRequest
import org.openapitools.client.model.GetAccountInfo401Response
import org.openapitools.client.model.QueueResponse
import org.openapitools.client.model.RestoreRequest
import org.openapitools.client.model.ReverseDnsEntries
import org.openapitools.client.model.SuccessTextResponse
import org.openapitools.client.model.TextResponse
import org.openapitools.client.model.VPSCancel200Response
import org.openapitools.client.model.Vps
import org.openapitools.client.model.VpsBackupRows
import org.openapitools.client.model.VpsOrder
import org.openapitools.client.model.VpsOrderPostRequest
import org.openapitools.client.model.VpsOrderPutRequest
import org.openapitools.client.model.VpsOrderPutResponse
import org.openapitools.client.model.VpsRow
import org.openapitools.client.model.VpsTemplatesList
import org.openapitools.client.model.VpsTrafficResponse
import org.openapitools.client.core.JsonSupport._
import sttp.client3._
import sttp.model.Method

object VPSApi {
  def apply(baseUrl: String = "https://my.interserver.net/apiv2") = new VPSApi(baseUrl)
}

class VPSApi(baseUrl: String) {

  /**
   * Places an order for a new VPS. Use `PUT /vps/order` to validate the order first.
   * 
   * Expected answers:
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   *   code 0 :  (Default response)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param vpsOrderPostRequest 
   */
  def addVps(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(vpsOrderPostRequest: Option[VpsOrderPostRequest] = None
): Request[Either[ResponseException[String, Exception], Unit], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/vps/order")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .body(vpsOrderPostRequest)
      .response(asString.mapWithMetadata(ResponseAs.deserializeRightWithError(_ => Right(()))))

  /**
   * Permanently removes the specified backup file from storage. Use `GET /vps/{id}/backups` to list available backup filenames before deleting.
   * 
   * Expected answers:
   *   code 200 : SuccessTextResponse (A response indicating the operation completed successfully with a text message.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   * @param file The backup filename to delete.
   * @param all Set to `1` to list all backups across all services, not just the ones for the given VPS.
   */
  def deleteVpsBackup(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int, file: String, all: Option[String] = None
): Request[Either[ResponseException[String, Exception], SuccessTextResponse], Any] =
    basicRequest
      .method(Method.DELETE, uri"$baseUrl/vps/${id}/backups?all=${ all }&file=${ file }")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[SuccessTextResponse])

  /**
   * Blocks outbound SMTP (port 25) on the VPS to prevent spam. Useful for VPS that do not need to send email.
   * 
   * Expected answers:
   *   code 200 : QueueResponse (Response message from sending a service queue.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number.
   */
  def doVpsBlockSmtp(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], QueueResponse], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/block_smtp")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[QueueResponse])

  /**
   * Disables the virtual CD drive on the VPS.
   * 
   * Expected answers:
   *   code 200 : QueueResponse (Response message from sending a service queue.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number.
   */
  def doVpsDisableCd(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], QueueResponse], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/disable_cd")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[QueueResponse])

  /**
   * Disables disk quota enforcement on the VPS.
   * 
   * Expected answers:
   *   code 200 : QueueResponse (Response message from sending a service queue.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number.
   */
  def doVpsDisableQuota(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], QueueResponse], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/disable_quota")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[QueueResponse])

  /**
   * Ejects the virtual CD from the VPS CD drive.
   * 
   * Expected answers:
   *   code 200 : QueueResponse (Response message from sending a service queue.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number.
   */
  def doVpsEjectCd(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], QueueResponse], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/eject_cd")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[QueueResponse])

  /**
   * Enables disk quota enforcement on the VPS.
   * 
   * Expected answers:
   *   code 200 : QueueResponse (Response message from sending a service queue.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number.
   */
  def doVpsEnableQuota(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], QueueResponse], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/enable_quota")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[QueueResponse])

  /**
   * Restarts the VPS.
   * 
   * Expected answers:
   *   code 200 : QueueResponse (Response message from sending a service queue.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number.
   */
  def doVpsRestart(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], QueueResponse], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/restart")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[QueueResponse])

  /**
   * Powers on the VPS.
   * 
   * Expected answers:
   *   code 200 : QueueResponse (Response message from sending a service queue.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number.
   */
  def doVpsStart(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], QueueResponse], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/start")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[QueueResponse])

  /**
   * Powers off the VPS.
   * 
   * Expected answers:
   *   code 200 : QueueResponse (Response message from sending a service queue.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number.
   */
  def doVpsStop(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], QueueResponse], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/stop")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[QueueResponse])

  /**
   * Generates a pre-signed download URL for the specified backup file. The URL is valid for 24 hours. Use `GET /vps/{id}/backups` to list available backup filenames.
   * 
   * Expected answers:
   *   code 200 : DownloadQsBackup200Response (Download URL for the backup file.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   * @param downloadQsBackupRequest 
   * @param all Set to `1` to list all backups across all services, not just the ones for the given VPS.
   */
  def downloadVpsBackup(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int, downloadQsBackupRequest: DownloadQsBackupRequest, all: Option[String] = None
): Request[Either[ResponseException[String, Exception], DownloadQsBackup200Response], Any] =
    basicRequest
      .method(Method.PATCH, uri"$baseUrl/vps/${id}/backups?all=${ all }")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .body(downloadQsBackupRequest)
      .response(asJson[DownloadQsBackup200Response])

  /**
   * Retrieves available VPS configurations, OS templates, and pricing for ordering.
   * 
   * Expected answers:
   *   code 200 : VpsOrder (VPS Order information)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   */
  def getNewVps(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(
): Request[Either[ResponseException[String, Exception], VpsOrder], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/order")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[VpsOrder])

  /**
   * Returns the available backups for the VPS across all storage systems (Swift, MinIO, ZFS). Use the backup `name` value with `PATCH /vps/{id}/backups` to download or `DELETE /vps/{id}/backups` to remove a backup. Use `POST /vps/{id}/restore` to restore from a backup.
   * 
   * Expected answers:
   *   code 200 : VpsBackupRows (The listing of the available backups)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   * @param all Set to `1` to list all backups across all services, not just the ones for the given VPS.
   */
  def getVpsBackups(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int, all: Option[String] = None
): Request[Either[ResponseException[String, Exception], VpsBackupRows], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/backups?all=${ all }")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[VpsBackupRows])

  /**
   * Returns available hard drive space addon options and pricing for the VPS.
   * 
   * Expected answers:
   *   code 200 :  (Get VPS Buy HD Space information)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   */
  def getVpsBuyHdSpace(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], Unit], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/buy_hd_space")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asString.mapWithMetadata(ResponseAs.deserializeRightWithError(_ => Right(()))))

  /**
   * Returns available additional IP address options and pricing for the VPS.
   * 
   * Expected answers:
   *   code 200 :  (VPS Buy IP information)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   */
  def getVpsBuyIp(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], Unit], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/buy_ip")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asString.mapWithMetadata(ResponseAs.deserializeRightWithError(_ => Right(()))))

  /**
   * Returns the list of available timezones that can be set on the VPS.
   * 
   * Expected answers:
   *   code 200 : Seq[String] (VPS Change Timezone info response)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   */
  def getVpsChangeTimezone(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], Seq[String]], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/change_timezone")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[Seq[String]])

  /**
   * Returns detailed information about a specific VPS including its IPs, hostname, OS, and resource allocation.
   * 
   * Expected answers:
   *   code 200 : Vps (The VPS Information)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   */
  def getVpsInfo(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], Vps], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[Vps])

  /**
   * Returns the billing invoices associated with this VPS.
   * 
   * Expected answers:
   *   code 200 : ChargeInvoiceRows (Get VPS Invoices response)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   */
  def getVpsInvoices(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], ChargeInvoiceRows], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/invoices")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[ChargeInvoiceRows])

  /**
   * Returns all VPS services on the account with their current status and configuration.
   * 
   * Expected answers:
   *   code 200 : Seq[VpsRow] (The listing of `Vps` services on your account.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   */
  def getVpsList(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(
): Request[Either[ResponseException[String, Exception], Seq[VpsRow]], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[Seq[VpsRow]])

  /**
   * Returns the list of available OS templates for reinstalling the VPS.
   * 
   * Expected answers:
   *   code 200 : VpsTemplatesList (VPS Reinstall OS info response)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   */
  def getVpsReinstallOs(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], VpsTemplatesList], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/reinstall_os")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[VpsTemplatesList])

  /**
   * Returns the current reverse DNS (PTR record) entries for the VPS IP addresses.
   * 
   * Expected answers:
   *   code 200 : ReverseDnsEntries (VPS Reverse DNS info response)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   */
  def getVpsReverseDns(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], ReverseDnsEntries], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/reverse_dns")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[ReverseDnsEntries])

  /**
   * Returns the current VNC connection information for the VPS.
   * 
   * Expected answers:
   *   code 200 :  (Get VPS Setup VNC Information)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   */
  def getVpsSetupVnc(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], Unit], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/setup_vnc")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asString.mapWithMetadata(ResponseAs.deserializeRightWithError(_ => Right(()))))

  /**
   * Returns available slice upgrade options and pricing for the VPS.
   * 
   * Expected answers:
   *   code 200 :  (VPS Slices information)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   */
  def getVpsSlices(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], Unit], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/slices")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asString.mapWithMetadata(ResponseAs.deserializeRightWithError(_ => Right(()))))

  /**
   * Returns bandwidth traffic usage data for the VPS.
   * 
   * Expected answers:
   *   code 200 : VpsTrafficResponse (Get VPS Traffic usage)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   */
  def getVpsTrafficUsage(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], VpsTrafficResponse], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/traffic_usage")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[VpsTrafficResponse])

  /**
   * Returns remote desktop connection information for the VPS.
   * 
   * Expected answers:
   *   code 200 :  (Get VPS View Desktop Information)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   */
  def getVpsViewDesktop(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], Unit], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/view_desktop")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asString.mapWithMetadata(ResponseAs.deserializeRightWithError(_ => Right(()))))

  /**
   * Resends the welcome email containing connection details and credentials for the VPS order.
   * 
   * Expected answers:
   *   code 200 : SuccessTextResponse (A response indicating the operation completed successfully with a text message.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID
   */
  def getVpsWelcomeEmail(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: String
): Request[Either[ResponseException[String, Exception], SuccessTextResponse], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/welcome_email")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[SuccessTextResponse])

  /**
   * Creates a backup of the VPS. The backup can be downloaded or restored later via the backups endpoints.
   * 
   * Expected answers:
   *   code 200 : QueueResponse (Response message from sending a service queue.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   */
  def postVpsBackup(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], QueueResponse], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/vps/${id}/backup")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[QueueResponse])

  /**
   * Purchases additional hard drive space for the VPS.
   * 
   * Expected answers:
   *   code 200 :  (Post Buy HD Space for VPS response)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   */
  def postVpsBuyHdSpace(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], Unit], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/vps/${id}/buy_hd_space")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asString.mapWithMetadata(ResponseAs.deserializeRightWithError(_ => Right(()))))

  /**
   * Purchases an additional IP address for the VPS.
   * 
   * Expected answers:
   *   code 200 :  (Submit VPS Buy IP form response)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   */
  def postVpsBuyIp(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], Unit], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/vps/${id}/buy_ip")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asString.mapWithMetadata(ResponseAs.deserializeRightWithError(_ => Right(()))))

  /**
   * Changes the hostname of the VPS. This queues a background task to update the server.
   * 
   * Expected answers:
   *   code 200 : QueueResponse (Response message from sending a service queue.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   * @param hostname 
   */
  def postVpsChangeHostname(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int, hostname: Option[String] = None
): Request[Either[ResponseException[String, Exception], QueueResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/vps/${id}/change_hostname")
      .contentType("multipart/form-data")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .multipartBody(Seq(
                hostname.map(multipart("hostname", _))

      ).flatten)
      .response(asJson[QueueResponse])

  /**
   * Changes the root password of the VPS.
   * 
   * Expected answers:
   *   code 200 : QueueResponse (Response message from sending a service queue.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   * @param password 
   */
  def postVpsChangeRootPassword(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int, password: String
): Request[Either[ResponseException[String, Exception], QueueResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/vps/${id}/change_root_password")
      .contentType("multipart/form-data")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .multipartBody(Seq(
                multipart("password", password)

      ).flatten)
      .response(asJson[QueueResponse])

  /**
   * Changes the system timezone on the VPS.
   * 
   * Expected answers:
   *   code 200 : QueueResponse (Response message from sending a service queue.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   * @param timezone The time zone
   */
  def postVpsChangeTimezone(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int, timezone: String
): Request[Either[ResponseException[String, Exception], QueueResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/vps/${id}/change_timezone")
      .contentType("multipart/form-data")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .multipartBody(Seq(
                multipart("timezone", timezone)

      ).flatten)
      .response(asJson[QueueResponse])

  /**
   * Changes the Webuzo control panel password on the VPS.
   * 
   * Expected answers:
   *   code 200 : QueueResponse (Response message from sending a service queue.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   * @param password 
   */
  def postVpsChangeWebuzoPassword(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int, password: String
): Request[Either[ResponseException[String, Exception], QueueResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/vps/${id}/change_webuzo_password")
      .contentType("multipart/form-data")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .multipartBody(Seq(
                multipart("password", password)

      ).flatten)
      .response(asJson[QueueResponse])

  /**
   * Mounts an ISO image in the VPS virtual CD drive from the given URL.
   * 
   * Expected answers:
   *   code 200 : QueueResponse (Response message from sending a service queue.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   * @param url 
   */
  def postVpsInsertCd(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int, url: Option[String] = None
): Request[Either[ResponseException[String, Exception], QueueResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/vps/${id}/insert_cd")
      .contentType("multipart/form-data")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .multipartBody(Seq(
                url.map(multipart("url", _))

      ).flatten)
      .response(asJson[QueueResponse])

  /**
   * Reinstalls the operating system on the VPS. Warning - all data will be erased.
   * 
   * Expected answers:
   *   code 200 : QueueResponse (Response message from sending a service queue.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   * @param template OS Template Filename
   * @param localPassword Password for this account.
   * @param password Password for Root / Administrator Account.
   */
  def postVpsReinstallOs(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int, template: String, localPassword: String, password: Option[String] = None
): Request[Either[ResponseException[String, Exception], QueueResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/vps/${id}/reinstall_os")
      .contentType("multipart/form-data")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .multipartBody(Seq(
                multipart("template", template)
, 
                password.map(multipart("password", _))
, 
                multipart("localPassword", localPassword)

      ).flatten)
      .response(asJson[QueueResponse])

  /**
   * Resets the root password on the VPS to a new randomly generated password.
   * 
   * Expected answers:
   *   code 200 : QueueResponse (Response message from sending a service queue.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   */
  def postVpsResetPassword(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], QueueResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/vps/${id}/reset_password")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[QueueResponse])

  /**
   * Initiates a restore of the VPS from a previously created backup. The restore operation overwrites the current disk contents. Use `GET /vps/{id}/backups` to retrieve available backup names.
   * 
   * Expected answers:
   *   code 200 : QueueResponse (Response message from sending a service queue.)
   *   code 400 : TextResponse (Response with a text message field.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   * @param restoreRequest VPS Restore request
   */
  def postVpsRestore(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int, restoreRequest: RestoreRequest
): Request[Either[ResponseException[String, Exception], QueueResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/vps/${id}/restore")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .body(restoreRequest)
      .response(asJson[QueueResponse])

  /**
   * Updates the reverse DNS (PTR record) entries for the VPS IP addresses.
   * 
   * Expected answers:
   *   code 200 : TextResponse (Update VPS Reverse DNS response)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   * @param reverseDnsEntries 
   */
  def postVpsReverseDns(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int, reverseDnsEntries: ReverseDnsEntries
): Request[Either[ResponseException[String, Exception], TextResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/vps/${id}/reverse_dns")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .body(reverseDnsEntries)
      .response(asJson[TextResponse])

  /**
   * Sets up or refreshes the VNC console connection for the VPS.
   * 
   * Expected answers:
   *   code 200 : QueueResponse (Response message from sending a service queue.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   */
  def postVpsSetupVnc(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], QueueResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/vps/${id}/setup_vnc")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[QueueResponse])

  /**
   * Purchases a slice upgrade for the VPS to increase CPU, RAM, and disk resources.
   * 
   * Expected answers:
   *   code 200 :  (Update VPS slices response)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   */
  def postVpsSlices(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], Unit], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/vps/${id}/slices")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asString.mapWithMetadata(ResponseAs.deserializeRightWithError(_ => Right(()))))

  /**
   * Updates or refreshes the remote desktop session for the VPS.
   * 
   * Expected answers:
   *   code 200 :  (Submit VPS View Desktop Information)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   */
  def postVpsViewDesktop(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], Unit], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/vps/${id}/view_desktop")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asString.mapWithMetadata(ResponseAs.deserializeRightWithError(_ => Right(()))))

  /**
   * Validates a VPS order before placing it. Returns cost breakdown and any validation errors.
   * 
   * Expected answers:
   *   code 200 : VpsOrderPutResponse (Validate VPS order response.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param vpsOrderPutRequest 
   */
  def putVps(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(vpsOrderPutRequest: Option[VpsOrderPutRequest] = None
): Request[Either[ResponseException[String, Exception], VpsOrderPutResponse], Any] =
    basicRequest
      .method(Method.PUT, uri"$baseUrl/vps/order")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .body(vpsOrderPutRequest)
      .response(asJson[VpsOrderPutResponse])

  /**
   * Updates settings on a VPS order.
   * 
   * Expected answers:
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   *   code 0 :  (Default response)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number.
   */
  def updateVpsInfo(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: String
): Request[Either[ResponseException[String, Exception], Unit], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/vps/${id}")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asString.mapWithMetadata(ResponseAs.deserializeRightWithError(_ => Right(()))))

  /**
   * Cancels the VPS service. The server will be deprovisioned and billing will stop at the end of the current billing cycle.
   * 
   * Expected answers:
   *   code 200 : VPSCancel200Response (VPS Cancel)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param id VPS ID number
   */
  def vPSCancel(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(id: Int
): Request[Either[ResponseException[String, Exception], VPSCancel200Response], Any] =
    basicRequest
      .method(Method.DELETE, uri"$baseUrl/vps/${id}")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[VPSCancel200Response])

}
