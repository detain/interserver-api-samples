/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package org.openapitools.client.api

import org.openapitools.client.model.AccountInfo
import org.openapitools.client.model.GenericResponse
import org.openapitools.client.model.GetAccountInfo401Response
import org.openapitools.client.model.GetAccountTfaSetup200Response
import org.openapitools.client.model.Home
import org.openapitools.client.model.SearchAutocompleteResponse
import org.openapitools.client.model.SuccessTextResponse
import org.openapitools.client.model.TextResponse
import org.openapitools.client.core.JsonSupport._
import sttp.client3._
import sttp.model.Method

object AccountApi {
  def apply(baseUrl: String = "https://my.interserver.net/apiv2") = new AccountApi(baseUrl)
}

class AccountApi(baseUrl: String) {

  /**
   * Changes the login username for the account. The new username must be unique across all accounts. After changing, use the new username for all future logins.
   * 
   * Expected answers:
   *   code 200 : TextResponse (Response with a text message field.)
   *   code 400 : GetAccountInfo401Response (The specified resource was not found)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   */
  def changeAccountUsername(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(
): Request[Either[ResponseException[String, Exception], TextResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/account/username")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[TextResponse])

  /**
   * Unlinks a third-party OAuth provider from the account. After unlinking, that provider can no longer be used for login.
   * 
   * Expected answers:
   *   code 200 : SuccessTextResponse (A response indicating the operation completed successfully with a text message.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param name 
   */
  def deleteAccountOauthName(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(name: String
): Request[Either[ResponseException[String, Exception], SuccessTextResponse], Any] =
    basicRequest
      .method(Method.DELETE, uri"$baseUrl/account/oauth/${name}")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[SuccessTextResponse])

  /**
   * Disables two-factor authentication on the account. After disabling, the account will only require password-based authentication.
   * 
   * Expected answers:
   *   code 200 : SuccessTextResponse (A response indicating the operation completed successfully with a text message.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   */
  def deleteAccountTfa(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(
): Request[Either[ResponseException[String, Exception], SuccessTextResponse], Any] =
    basicRequest
      .method(Method.DELETE, uri"$baseUrl/account/2fa")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[SuccessTextResponse])

  /**
   * Removes an IP address range from the account's access restriction list. If this is the last range, IP limiting is effectively disabled and the account becomes accessible from any IP address.
   * 
   * Expected answers:
   *   code 200 : GenericResponse (IP Range removed.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   */
  def deleteIpLimit(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(
): Request[Either[ResponseException[String, Exception], GenericResponse], Any] =
    basicRequest
      .method(Method.PATCH, uri"$baseUrl/account/iplimits")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[GenericResponse])

  /**
   * Returns the full account profile including contact information, billing address, and security settings. Use this to populate account management forms or verify account state before making changes with `POST /account`.
   * 
   * Expected answers:
   *   code 200 : AccountInfo (Your account information.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   */
  def getAccountInfo(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(
): Request[Either[ResponseException[String, Exception], AccountInfo], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/account")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[AccountInfo])

  /**
   * Returns the TOTP secret key needed to configure a two-factor authentication app (e.g. Google Authenticator). Present the `2fa_google_key` as a QR code or display the `2fa_google_split` value for manual entry. After setup, verify with `POST /account/2fa`.
   * 
   * Expected answers:
   *   code 200 : GetAccountTfaSetup200Response (Two-factor authentication setup data.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   */
  def getAccountTfaSetup(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(
): Request[Either[ResponseException[String, Exception], GetAccountTfaSetup200Response], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/account/2fa")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[GetAccountTfaSetup200Response])

  /**
   * Returns an overview of the account suitable for a dashboard home page, including service counts, recent activity, and account-level alerts. Requires an authenticated session.
   * 
   * Expected answers:
   *   code 200 : Home (General information for use on the home page.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   */
  def getHome(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(
): Request[Either[ResponseException[String, Exception], Home], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/home")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[Home])

  /**
   * Returns autocomplete results for the account's services and records. Use this endpoint to power global search experiences in the client portal.
   * 
   * Expected answers:
   *   code 200 : SearchAutocompleteResponse (Search autocomplete results for the account.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   */
  def getSearch(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(
): Request[Either[ResponseException[String, Exception], SearchAutocompleteResponse], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/search")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[SearchAutocompleteResponse])

  /**
   * Invalidates the current session. After calling this endpoint, the session token can no longer be used for authenticated requests. The client should discard the stored session ID.
   * 
   * Expected answers:
   *   code 200 : SuccessTextResponse (A response indicating the operation completed successfully with a text message.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   */
  def logout(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(
): Request[Either[ResponseException[String, Exception], SuccessTextResponse], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/logout")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[SuccessTextResponse])

  /**
   * Logs out of the specified OAuth provider session.
   * 
   * Expected answers:
   *   code 200 : SuccessTextResponse (A response indicating the operation completed successfully with a text message.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param name 
   */
  def logoutAccountOauth(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(name: String
): Request[Either[ResponseException[String, Exception], SuccessTextResponse], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/account/oauth/${name}/logout")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[SuccessTextResponse])

  /**
   * Generates a new API key for the account. The previous key is immediately invalidated. Store the new key securely as it cannot be retrieved later.
   * 
   * Expected answers:
   *   code 200 : SuccessTextResponse (A response indicating the operation completed successfully with a text message.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   */
  def updateAccountApiKey(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(
): Request[Either[ResponseException[String, Exception], SuccessTextResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/account/apikey")
      .contentType("application/json")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .response(asJson[SuccessTextResponse])

  /**
   * Updates account-level feature flags that control service capabilities. These flags can disable password reset, OS reinstall, or other potentially destructive operations across your services. Changes take effect immediately.
   * 
   * Expected answers:
   *   code 200 : SuccessTextResponse (A response indicating the operation completed successfully with a text message.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   *   code 422 : TextResponse (Request was valid but did not contain updatable values.)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param disableReset 
   * @param disableReinstall 
   */
  def updateAccountFeatures(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(disableReset: Option[Int] = None, disableReinstall: Option[Int] = None
): Request[Either[ResponseException[String, Exception], SuccessTextResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/account/features")
      .contentType("multipart/form-data")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .multipartBody(Seq(
                disableReset.map(multipart("disable_reset", _))
, 
                disableReinstall.map(multipart("disable_reinstall", _))

      ).flatten)
      .response(asJson[SuccessTextResponse])

  /**
   * Updates the stored contact and billing information on your account. Submit only the fields you want to change. Validation errors are returned as a 422 response with field-level messages.
   * 
   * Expected answers:
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   *   code 422 : TextResponse (Validation error while updating account data.)
   *   code 0 :  (Default response)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param name Your name.
   * @param address Your address.
   * @param city Your city.
   * @param state Your state.
   * @param zip Your ZIP code.
   * @param country Your country.
   * @param phone Your phone number.
   * @param company Your company name.
   * @param address2 Additional address information.
   * @param locale Your preferred locale.
   * @param emailInvoices Your email for invoice notifications.
   * @param emailAbuse Your email for abuse notifications.
   * @param disableReset Set to `true` to disable account resets, or `false` to enable them.
   * @param disableReinstall Set to `true` to disable server reinstalls, or `false` to enable them.
   * @param disableServerNotifications Set to `true` to disable server notifications, or `false` to enable them.
   * @param disableEmailNotifications Set to `true` to disable email notifications, or `false` to enable them.
   * @param gstin Your GST identification number (if applicable).
   */
  def updateAccountInfo(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(name: String, address: String, city: String, state: String, zip: String, country: String, phone: String, company: Option[String] = None, address2: Option[String] = None, locale: Option[String] = None, emailInvoices: Option[String] = None, emailAbuse: Option[String] = None, disableReset: Option[Boolean] = None, disableReinstall: Option[Boolean] = None, disableServerNotifications: Option[Boolean] = None, disableEmailNotifications: Option[Boolean] = None, gstin: Option[String] = None
): Request[Either[ResponseException[String, Exception], Unit], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/account")
      .contentType("multipart/form-data")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .multipartBody(Seq(
                multipart("name", name)
, 
                company.map(multipart("company", _))
, 
                multipart("address", address)
, 
                address2.map(multipart("address2", _))
, 
                multipart("city", city)
, 
                multipart("state", state)
, 
                multipart("zip", zip)
, 
                multipart("country", country)
, 
                multipart("phone", phone)
, 
                locale.map(multipart("locale", _))
, 
                emailInvoices.map(multipart("email_invoices", _))
, 
                emailAbuse.map(multipart("email_abuse", _))
, 
                disableReset.map(multipart("disable_reset", _))
, 
                disableReinstall.map(multipart("disable_reinstall", _))
, 
                disableServerNotifications.map(multipart("disable_server_notifications", _))
, 
                disableEmailNotifications.map(multipart("disable_email_notifications", _))
, 
                gstin.map(multipart("gstin", _))

      ).flatten)
      .response(asString.mapWithMetadata(ResponseAs.deserializeRightWithError(_ => Right(()))))

  /**
   * Adds an IP address range to the account's access restriction list. Once IP limiting is active, only requests originating from allowed ranges can access the account. Provide the start and end of the range in dotted-quad notation.
   * 
   * Expected answers:
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   *   code 422 : TextResponse (IP limit payload contains an invalid address.)
   *   code 0 :  (Default response)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param start The begining (or first) IP address in the range.
   * @param end The ending (or last) IP address in the range.
   */
  def updateAccountIpLimits(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(start: String, end: String
): Request[Either[ResponseException[String, Exception], Unit], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/account/iplimits")
      .contentType("multipart/form-data")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .multipartBody(Seq(
                multipart("start", start)
, 
                multipart("end", end)

      ).flatten)
      .response(asString.mapWithMetadata(ResponseAs.deserializeRightWithError(_ => Right(()))))

  /**
   * Changes the account login password. The current password must be provided for verification. After a successful change, existing API keys remain valid but active sessions may require re-authentication.
   * 
   * Expected answers:
   *   code 200 : TextResponse (Response with a text message field.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param password 
   */
  def updateAccountPassword(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(password: String
): Request[Either[ResponseException[String, Exception], TextResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/account/password")
      .contentType("multipart/form-data")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .multipartBody(Seq(
                multipart("password", password)

      ).flatten)
      .response(asJson[TextResponse])

  /**
   * Updates the SSH public key stored on the account. This key can be automatically installed on new VPS and server orders.
   * 
   * Expected answers:
   *   code 200 : SuccessTextResponse (A response indicating the operation completed successfully with a text message.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param sshKey 
   */
  def updateAccountSshKey(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(sshKey: Option[String] = None
): Request[Either[ResponseException[String, Exception], SuccessTextResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/account/sshkey")
      .contentType("multipart/form-data")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .multipartBody(Seq(
                sshKey.map(multipart("ssh_key", _))

      ).flatten)
      .response(asJson[SuccessTextResponse])

  /**
   * Verifies the TOTP code from your authenticator app and enables two-factor authentication on the account. Use `GET /account/2fa` first to retrieve the secret key for app setup.
   * 
   * Expected answers:
   *   code 200 : SuccessTextResponse (A response indicating the operation completed successfully with a text message.)
   *   code 401 : GetAccountInfo401Response (Unauthorized)
   *   code 422 : TextResponse (Provided two-factor verification code was invalid.)
   * 
   * Available security schemes:
   *   sessionIdCookieAuth (apiKey)
   *   apiKeyAuth (apiKey)
   *   sessionIdHeaderAuth (apiKey)
   * 
   * @param `2faGoogleCode` The 6-digit verification code from your authenticator app.
   */
  def updateAccountTfa(apiKeyCookie: String, apiKeyHeader: String, apiKeyHeader: String)(`2faGoogleCode`: String
): Request[Either[ResponseException[String, Exception], SuccessTextResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/account/2fa")
      .contentType("multipart/form-data")
      .cookie("sessionid", apiKeyCookie)
      .header("X-API-KEY", apiKeyHeader)
      .header("sessionid", apiKeyHeader)
      .multipartBody(Seq(
                multipart("2fa_google_code", `2faGoogleCode`)

      ).flatten)
      .response(asJson[SuccessTextResponse])

}
