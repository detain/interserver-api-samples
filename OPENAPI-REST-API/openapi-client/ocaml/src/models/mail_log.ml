(*
 * This file has been generated by the OCamlClientCodegen generator for openapi-generator.
 *
 * Generated by: https://openapi-generator.tech
 *
 * Schema Mail_log.t : Paginated mail log response.  Contains the full matched count (`total`) plus a page of `MailLogEntry` records.  The `total` reflects the grouping mode: with `groupby=recipient` it counts delivery attempts, with `groupby=message` it counts unique messages.
 *)



    
        type t = {
                              total: int32
                  
                  
                
                
                ; [@key "total"]
                    (** Total number of log entries that match the supplied filters, regardless of `skip` and `limit`.  Reflects the `groupby` mode. *)
                                      skip: int32
                  
                  
                
                
                ; [@key "skip"]
                    (** The `skip` value used for this page (echoed from the request). *)
                                      limit: int32
                  
                  
                
                
                ; [@key "limit"]
                    (** The `limit` value used for this page (echoed from the request). *)
                                      emails: Mail_log_entry.t list
                
                
                ; [@key "emails"]
                        } [@@deriving yojson { strict = false }, show, eq ];;
        
        (** Paginated mail log response.  Contains the full matched count (`total`) plus a page of `MailLogEntry` records.  The `total` reflects the grouping mode: with `groupby=recipient` it counts delivery attempts, with `groupby=message` it counts unique messages. *)
        let create (total : int32) (skip : int32) (limit : int32) (emails : Mail_log_entry.t list) : t = {
            total = total;
            skip = skip;
            limit = limit;
            emails = emails;
        }
    

