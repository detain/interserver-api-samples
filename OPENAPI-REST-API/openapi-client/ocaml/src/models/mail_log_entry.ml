(*
 * This file has been generated by the OCamlClientCodegen generator for openapi-generator.
 *
 * Generated by: https://openapi-generator.tech
 *
 * Schema Mail_log_entry.t : A single email record in the mail log.  Combines data from the message store (envelope metadata), the queue release table (delivery status and response), and the sender delivery table (MX routing details).  When `groupby=recipient` each row represents one delivery attempt; when `groupby=message` delivery fields reflect one arbitrary recipient.
 *)



    
        type t = {
                              _id: int32
                  
                  
                
                
                ; [@key "_id"]
                    (** Internal auto-increment database row ID. *)
                                      id: string
                  
                  
                
                
                ; [@key "id"]
                    (** The relay-assigned mail ID (18-19 hex characters).  Matches the `mailid` filter parameter and the `text` value returned by send endpoints. *)
                                      from: string
                  
                  
                
                
                ; [@key "from"]
                    (** SMTP envelope `MAIL FROM` address. *)
                                      _to: string
                  
                  
                
                
                ; [@key "to"]
                    (** SMTP envelope `RCPT TO` address. *)
                                      created: string
                  
                  
                
                
                ; [@key "created"]
                    (** Human-readable creation timestamp in `YYYY-MM-DD HH:MM:SS` format. *)
                                      time: int32
                  
                  
                
                
                ; [@key "time"]
                    (** Unix timestamp of message acceptance.  Corresponds to the `startDate` and `endDate` filter parameters. *)
                                      user: string
                  
                  
                
                
                ; [@key "user"]
                    (** The SMTP AUTH username used to submit the message (e.g. `mb5658`). *)
                                      transtype: string
                  
                  
                
                
                ; [@key "transtype"]
                    (** SMTP transaction type negotiated with the relay. *)
                                      origin: string
                  
                  
                
                
                ; [@key "origin"]
                    (** IP address of the client that submitted the message to the relay. *)
                                      interface: string
                  
                  
                
                
                ; [@key "interface"]
                    (** Relay interface name that accepted the message. *)
                                      subject: string
                  
                   option [@default None]
                
                
                ; [@key "subject"]
                    (** The `Subject` header value.  MIME-encoded subjects (UTF-8, ISO-8859, US-ASCII) are automatically decoded. *)
                                      message_id: string
                  
                   option [@default None]
                
                
                ; [@key "messageId"]
                    (** The `Message-ID` header value.  Can be used with the `messageId` filter for subsequent lookups. *)
                                      sending_zone: string
                  
                   option [@default None]
                
                
                ; [@key "sendingZone"]
                    (** The sending zone assigned by the relay for outbound delivery. *)
                                      body_size: int32
                  
                   option [@default None]
                
                
                ; [@key "bodySize"]
                    (** Size of the message body in bytes. *)
                                      seq: int32
                  
                   option [@default None]
                
                
                ; [@key "seq"]
                    (** Sequence index of this recipient in a multi-recipient message. Starts at 1. *)
                                      delivered: int32
                  
                   option [@default None]
                
                
                ; [@key "delivered"]
                    (** Delivery status flag.  `1` = successfully delivered to destination MX. `0` = queued, deferred, or failed.  `null` = delivery not yet attempted. *)
                                      code: int32
                  
                   option [@default None]
                
                
                ; [@key "code"]
                    (** The SMTP response code from the destination MX server (e.g. `250`). *)
                                      recipient: string
                  
                   option [@default None]
                
                
                ; [@key "recipient"]
                    (** The specific recipient address this delivery record is for. *)
                                      response: string
                  
                   option [@default None]
                
                
                ; [@key "response"]
                    (** The full SMTP response string received from the destination MX server. *)
                                      domain: string
                  
                   option [@default None]
                
                
                ; [@key "domain"]
                    (** The destination domain for this delivery attempt. *)
                                      locked: int32
                  
                   option [@default None]
                
                
                ; [@key "locked"]
                    (** Whether the queue entry is currently locked for delivery processing. *)
                                      lock_time: string
                  
                   option [@default None]
                
                
                ; [@key "lockTime"]
                    (** Millisecond-precision timestamp of the last queue lock acquisition. *)
                                      assigned: string
                  
                   option [@default None]
                
                
                ; [@key "assigned"]
                    (** The relay server node assigned to deliver this message. *)
                                      queued: string
                  
                   option [@default None]
                
                
                ; [@key "queued"]
                    (** ISO 8601 timestamp when the message was placed into the delivery queue. *)
                                      mx_hostname: string
                  
                   option [@default None]
                
                
                ; [@key "mxHostname"]
                    (** The MX hostname the relay connected to for delivery.  Corresponds to the `mx` filter parameter. *)
                } [@@deriving yojson { strict = false }, show, eq ];;
        
        (** A single email record in the mail log.  Combines data from the message store (envelope metadata), the queue release table (delivery status and response), and the sender delivery table (MX routing details).  When `groupby=recipient` each row represents one delivery attempt; when `groupby=message` delivery fields reflect one arbitrary recipient. *)
        let create (_id : int32) (id : string) (from : string) (_to : string) (created : string) (time : int32) (user : string) (transtype : string) (origin : string) (interface : string) : t = {
            _id = _id;
            id = id;
            from = from;
            _to = _to;
            created = created;
            time = time;
            user = user;
            transtype = transtype;
            origin = origin;
            interface = interface;
            subject = None;
            message_id = None;
            sending_zone = None;
            body_size = None;
            seq = None;
            delivered = None;
            code = None;
            recipient = None;
            response = None;
            domain = None;
            locked = None;
            lock_time = None;
            assigned = None;
            queued = None;
            mx_hostname = None;
        }
    

