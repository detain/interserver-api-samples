// tslint:disable
/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import { HttpMethods, QueryConfig, ResponseBody, ResponseText } from 'redux-query';
import * as runtime from '../runtime';
import {
    CloseTicketResponseSchema,
    CloseTicketResponseSchemaFromJSON,
    CloseTicketResponseSchemaToJSON,
    GetAccountInfo401Response,
    GetAccountInfo401ResponseFromJSON,
    GetAccountInfo401ResponseToJSON,
    ReplyTicketRequest,
    ReplyTicketRequestFromJSON,
    ReplyTicketRequestToJSON,
    ReplyTicketResponseSchema,
    ReplyTicketResponseSchemaFromJSON,
    ReplyTicketResponseSchemaToJSON,
    TicketNew,
    TicketNewFromJSON,
    TicketNewToJSON,
    TicketNewResponse,
    TicketNewResponseFromJSON,
    TicketNewResponseToJSON,
    Tickets,
    TicketsFromJSON,
    TicketsToJSON,
    UpdateTicket,
    UpdateTicketFromJSON,
    UpdateTicketToJSON,
    UpdateTicketResponseSchema,
    UpdateTicketResponseSchemaFromJSON,
    UpdateTicketResponseSchemaToJSON,
    ViewTicketResponse,
    ViewTicketResponseFromJSON,
    ViewTicketResponseToJSON,
} from '../models';

export interface AddNewTicketRequest {
    ticketNew: TicketNew;
}

export interface CloseTicketRequest {
    id: string;
}

export interface DeleteTicketInfoRequest {
    id: number;
}

export interface GetTicketInfoRequest {
    id: number;
}

export interface GetTicketsListRequest {
    page?: number;
    period?: GetTicketsListPeriodEnum;
    view?: GetTicketsListViewEnum;
}

export interface PostTicketInfoRequest {
    id: number;
}

export interface PutTicketInfoRequest {
    id: number;
}

export interface ReplyTicketRequest {
    id: number;
    replyTicketRequest?: ReplyTicketRequest;
}

export interface UpdateTicketInfoRequest {
    id: number;
    updateTicket?: UpdateTicket;
}


/**
 * Creates a new support ticket. Optionally link it to a specific service by providing the service ID and module.
 * Create New Ticket
 */
function addNewTicketRaw<T>(requestParameters: AddNewTicketRequest, requestConfig: runtime.TypedQueryConfig<T, TicketNewResponse> = {}): QueryConfig<T> {
    if (requestParameters.ticketNew === null || requestParameters.ticketNew === undefined) {
        throw new runtime.RequiredError('ticketNew','Required parameter requestParameters.ticketNew was null or undefined when calling addNewTicket.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/tickets/new`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters || TicketNewToJSON(requestParameters.ticketNew),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(TicketNewResponseFromJSON(body), text);
    }

    return config;
}

/**
* Creates a new support ticket. Optionally link it to a specific service by providing the service ID and module.
* Create New Ticket
*/
export function addNewTicket<T>(requestParameters: AddNewTicketRequest, requestConfig?: runtime.TypedQueryConfig<T, TicketNewResponse>): QueryConfig<T> {
    return addNewTicketRaw(requestParameters, requestConfig);
}

/**
 * Closes the specified support ticket. Closed tickets can still be viewed but will no longer appear in the active inbox.
 * Close Ticket
 */
function closeTicketRaw<T>(requestParameters: CloseTicketRequest, requestConfig: runtime.TypedQueryConfig<T, CloseTicketResponseSchema> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling closeTicket.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/tickets/{id}/close`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(CloseTicketResponseSchemaFromJSON(body), text);
    }

    return config;
}

/**
* Closes the specified support ticket. Closed tickets can still be viewed but will no longer appear in the active inbox.
* Close Ticket
*/
export function closeTicket<T>(requestParameters: CloseTicketRequest, requestConfig?: runtime.TypedQueryConfig<T, CloseTicketResponseSchema>): QueryConfig<T> {
    return closeTicketRaw(requestParameters, requestConfig);
}

/**
 * Closes the support ticket.
 * Close Ticket
 */
function deleteTicketInfoRaw<T>(requestParameters: DeleteTicketInfoRequest, requestConfig: runtime.TypedQueryConfig<T, ViewTicketResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling deleteTicketInfo.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/tickets/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'DELETE',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(ViewTicketResponseFromJSON(body), text);
    }

    return config;
}

/**
* Closes the support ticket.
* Close Ticket
*/
export function deleteTicketInfo<T>(requestParameters: DeleteTicketInfoRequest, requestConfig?: runtime.TypedQueryConfig<T, ViewTicketResponse>): QueryConfig<T> {
    return deleteTicketInfoRaw(requestParameters, requestConfig);
}

/**
 * Returns the form data needed to create a new support ticket, such as available departments and service categories.
 * Gets Information for creating a new ticket.
 */
function getNewTicketRaw<T>( requestConfig: runtime.TypedQueryConfig<T, void> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/tickets/new`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* Returns the form data needed to create a new support ticket, such as available departments and service categories.
* Gets Information for creating a new ticket.
*/
export function getNewTicket<T>( requestConfig?: runtime.TypedQueryConfig<T, void>): QueryConfig<T> {
    return getNewTicketRaw( requestConfig);
}

/**
 * Returns the full details of a support ticket including its history of replies.
 * Get Ticket Information
 */
function getTicketInfoRaw<T>(requestParameters: GetTicketInfoRequest, requestConfig: runtime.TypedQueryConfig<T, ViewTicketResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getTicketInfo.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/tickets/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(ViewTicketResponseFromJSON(body), text);
    }

    return config;
}

/**
* Returns the full details of a support ticket including its history of replies.
* Get Ticket Information
*/
export function getTicketInfo<T>(requestParameters: GetTicketInfoRequest, requestConfig?: runtime.TypedQueryConfig<T, ViewTicketResponse>): QueryConfig<T> {
    return getTicketInfoRaw(requestParameters, requestConfig);
}

/**
 * Returns a paginated list of support tickets on the account. Filter by status and time period.
 * List Support Tickets
 */
function getTicketsListRaw<T>(requestParameters: GetTicketsListRequest, requestConfig: runtime.TypedQueryConfig<T, Tickets> = {}): QueryConfig<T> {
    let queryParameters = null;

    queryParameters = {};


    if (requestParameters.page !== undefined) {
        queryParameters['page'] = requestParameters.page;
    }


    if (requestParameters.period !== undefined) {
        queryParameters['period'] = requestParameters.period;
    }


    if (requestParameters.view !== undefined) {
        queryParameters['view'] = requestParameters.view;
    }

    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/tickets`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(TicketsFromJSON(body), text);
    }

    return config;
}

/**
* Returns a paginated list of support tickets on the account. Filter by status and time period.
* List Support Tickets
*/
export function getTicketsList<T>(requestParameters: GetTicketsListRequest, requestConfig?: runtime.TypedQueryConfig<T, Tickets>): QueryConfig<T> {
    return getTicketsListRaw(requestParameters, requestConfig);
}

/**
 * Adds a reply to an existing support ticket.
 * Reply To Ticket
 */
function postTicketInfoRaw<T>(requestParameters: PostTicketInfoRequest, requestConfig: runtime.TypedQueryConfig<T, ViewTicketResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling postTicketInfo.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/tickets/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(ViewTicketResponseFromJSON(body), text);
    }

    return config;
}

/**
* Adds a reply to an existing support ticket.
* Reply To Ticket
*/
export function postTicketInfo<T>(requestParameters: PostTicketInfoRequest, requestConfig?: runtime.TypedQueryConfig<T, ViewTicketResponse>): QueryConfig<T> {
    return postTicketInfoRaw(requestParameters, requestConfig);
}

/**
 * Searches support tickets by email, subject, or ticket mask ID.
 * Search Support Tickets
 */
function postTicketsListRaw<T>( requestConfig: runtime.TypedQueryConfig<T, Tickets> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/tickets`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(TicketsFromJSON(body), text);
    }

    return config;
}

/**
* Searches support tickets by email, subject, or ticket mask ID.
* Search Support Tickets
*/
export function postTicketsList<T>( requestConfig?: runtime.TypedQueryConfig<T, Tickets>): QueryConfig<T> {
    return postTicketsListRaw( requestConfig);
}

/**
 * Updates a support ticket\'s properties such as subject or status.
 * Update Ticket
 */
function putTicketInfoRaw<T>(requestParameters: PutTicketInfoRequest, requestConfig: runtime.TypedQueryConfig<T, ViewTicketResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling putTicketInfo.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/tickets/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'PUT',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(ViewTicketResponseFromJSON(body), text);
    }

    return config;
}

/**
* Updates a support ticket\'s properties such as subject or status.
* Update Ticket
*/
export function putTicketInfo<T>(requestParameters: PutTicketInfoRequest, requestConfig?: runtime.TypedQueryConfig<T, ViewTicketResponse>): QueryConfig<T> {
    return putTicketInfoRaw(requestParameters, requestConfig);
}

/**
 * Posts a reply to an existing support ticket thread.
 * Reply Ticket
 */
function replyTicketRaw<T>(requestParameters: ReplyTicketRequest, requestConfig: runtime.TypedQueryConfig<T, ReplyTicketResponseSchema> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling replyTicket.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/tickets/{id}/reply`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters || ReplyTicketRequestToJSON(requestParameters.replyTicketRequest),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(ReplyTicketResponseSchemaFromJSON(body), text);
    }

    return config;
}

/**
* Posts a reply to an existing support ticket thread.
* Reply Ticket
*/
export function replyTicket<T>(requestParameters: ReplyTicketRequest, requestConfig?: runtime.TypedQueryConfig<T, ReplyTicketResponseSchema>): QueryConfig<T> {
    return replyTicketRaw(requestParameters, requestConfig);
}

/**
 * Updates a support ticket\'s subject or body content.
 * Update Ticket
 */
function updateTicketInfoRaw<T>(requestParameters: UpdateTicketInfoRequest, requestConfig: runtime.TypedQueryConfig<T, UpdateTicketResponseSchema> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling updateTicketInfo.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/tickets/{id}/update`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters || UpdateTicketToJSON(requestParameters.updateTicket),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(UpdateTicketResponseSchemaFromJSON(body), text);
    }

    return config;
}

/**
* Updates a support ticket\'s subject or body content.
* Update Ticket
*/
export function updateTicketInfo<T>(requestParameters: UpdateTicketInfoRequest, requestConfig?: runtime.TypedQueryConfig<T, UpdateTicketResponseSchema>): QueryConfig<T> {
    return updateTicketInfoRaw(requestParameters, requestConfig);
}


/**
    * @export
    * @enum {string}
    */
export enum GetTicketsListPeriodEnum {
    _30 = '30',
    _90 = '90',
    _365 = '365',
    _1825 = '1825',
    All = 'all'
}
/**
    * @export
    * @enum {string}
    */
export enum GetTicketsListViewEnum {
    Open = 'Open',
    Closed = 'Closed',
    OnHold = 'On Hold',
    InProgress = 'In Progress'
}
