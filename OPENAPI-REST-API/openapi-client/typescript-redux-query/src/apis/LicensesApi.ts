// tslint:disable
/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import { HttpMethods, QueryConfig, ResponseBody, ResponseText } from 'redux-query';
import * as runtime from '../runtime';
import {
    ChargeInvoiceRows,
    ChargeInvoiceRowsFromJSON,
    ChargeInvoiceRowsToJSON,
    GetAccountInfo401Response,
    GetAccountInfo401ResponseFromJSON,
    GetAccountInfo401ResponseToJSON,
    IpObject,
    IpObjectFromJSON,
    IpObjectToJSON,
    License,
    LicenseFromJSON,
    LicenseToJSON,
    LicenseRow,
    LicenseRowFromJSON,
    LicenseRowToJSON,
    LicensesCancel200Response,
    LicensesCancel200ResponseFromJSON,
    LicensesCancel200ResponseToJSON,
    LicensesOrder,
    LicensesOrderFromJSON,
    LicensesOrderToJSON,
    ServiceOrderPostResponse,
    ServiceOrderPostResponseFromJSON,
    ServiceOrderPostResponseToJSON,
    SuccessTextResponse,
    SuccessTextResponseFromJSON,
    SuccessTextResponseToJSON,
} from '../models';

export interface GetLicenseInfoRequest {
    id: number;
}

export interface GetLicenseInvoicesRequest {
    id: number;
}

export interface GetLicenseOrderCatTagInfoRequest {
    catTag: string;
}

export interface GetLicensesWelcomeEmailRequest {
    id: number;
}

export interface LicensesCancelRequest {
    id: number;
}

export interface PostLicenseChangeIpRequest {
    id: number;
    ipObject: IpObject;
}

export interface UpdateLicenseInfoRequest {
    id: string;
}


/**
 * Places an order for a new software license. Use `PUT /licenses/order` to validate the order first.
 * Place License Order
 */
function addLicenseRaw<T>( requestConfig: runtime.TypedQueryConfig<T, ServiceOrderPostResponse> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/licenses/order`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(ServiceOrderPostResponseFromJSON(body), text);
    }

    return config;
}

/**
* Places an order for a new software license. Use `PUT /licenses/order` to validate the order first.
* Place License Order
*/
export function addLicense<T>( requestConfig?: runtime.TypedQueryConfig<T, ServiceOrderPostResponse>): QueryConfig<T> {
    return addLicenseRaw( requestConfig);
}

/**
 * Returns detailed information about a specific license including its type, IP assignment, and status.
 * Get License
 */
function getLicenseInfoRaw<T>(requestParameters: GetLicenseInfoRequest, requestConfig: runtime.TypedQueryConfig<T, License> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getLicenseInfo.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/licenses/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(LicenseFromJSON(body), text);
    }

    return config;
}

/**
* Returns detailed information about a specific license including its type, IP assignment, and status.
* Get License
*/
export function getLicenseInfo<T>(requestParameters: GetLicenseInfoRequest, requestConfig?: runtime.TypedQueryConfig<T, License>): QueryConfig<T> {
    return getLicenseInfoRaw(requestParameters, requestConfig);
}

/**
 * Returns the billing invoices associated with this license service.
 * Get License Invoices
 */
function getLicenseInvoicesRaw<T>(requestParameters: GetLicenseInvoicesRequest, requestConfig: runtime.TypedQueryConfig<T, ChargeInvoiceRows> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getLicenseInvoices.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/licenses/{id}/invoices`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(ChargeInvoiceRowsFromJSON(body), text);
    }

    return config;
}

/**
* Returns the billing invoices associated with this license service.
* Get License Invoices
*/
export function getLicenseInvoices<T>(requestParameters: GetLicenseInvoicesRequest, requestConfig?: runtime.TypedQueryConfig<T, ChargeInvoiceRows>): QueryConfig<T> {
    return getLicenseInvoicesRaw(requestParameters, requestConfig);
}

/**
 * Returns all software license services on the account with their current status and IP assignments.
 * List Licenses
 */
function getLicenseListRaw<T>( requestConfig: runtime.TypedQueryConfig<T, Array<LicenseRow>> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/licenses`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(body.map(LicenseRowFromJSON), text);
    }

    return config;
}

/**
* Returns all software license services on the account with their current status and IP assignments.
* List Licenses
*/
export function getLicenseList<T>( requestConfig?: runtime.TypedQueryConfig<T, Array<LicenseRow>>): QueryConfig<T> {
    return getLicenseListRaw( requestConfig);
}

/**
 * Returns the available license types and pricing for a specific license category. Use the category tags from `GET /licenses/order` to identify valid values.
 * Get License Order Information for Category
 */
function getLicenseOrderCatTagInfoRaw<T>(requestParameters: GetLicenseOrderCatTagInfoRequest, requestConfig: runtime.TypedQueryConfig<T, void> = {}): QueryConfig<T> {
    if (requestParameters.catTag === null || requestParameters.catTag === undefined) {
        throw new runtime.RequiredError('catTag','Required parameter requestParameters.catTag was null or undefined when calling getLicenseOrderCatTagInfo.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/licenses/order/{catTag}`.replace(`{${"catTag"}}`, encodeURIComponent(String(requestParameters.catTag))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* Returns the available license types and pricing for a specific license category. Use the category tags from `GET /licenses/order` to identify valid values.
* Get License Order Information for Category
*/
export function getLicenseOrderCatTagInfo<T>(requestParameters: GetLicenseOrderCatTagInfoRequest, requestConfig?: runtime.TypedQueryConfig<T, void>): QueryConfig<T> {
    return getLicenseOrderCatTagInfoRaw(requestParameters, requestConfig);
}

/**
 * Resends the welcome email for the license service. The email contains the license key and activation instructions.
 * Resend License Welcome Email
 */
function getLicensesWelcomeEmailRaw<T>(requestParameters: GetLicensesWelcomeEmailRequest, requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getLicensesWelcomeEmail.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/licenses/{id}/welcome_email`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Resends the welcome email for the license service. The email contains the license key and activation instructions.
* Resend License Welcome Email
*/
export function getLicensesWelcomeEmail<T>(requestParameters: GetLicensesWelcomeEmailRequest, requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return getLicensesWelcomeEmailRaw(requestParameters, requestConfig);
}

/**
 * Retrieves available license types, categories, and pricing for ordering a new license.
 * Get License Order Information
 */
function getNewLicenseRaw<T>( requestConfig: runtime.TypedQueryConfig<T, LicensesOrder> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/licenses/order`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(LicensesOrderFromJSON(body), text);
    }

    return config;
}

/**
* Retrieves available license types, categories, and pricing for ordering a new license.
* Get License Order Information
*/
export function getNewLicense<T>( requestConfig?: runtime.TypedQueryConfig<T, LicensesOrder>): QueryConfig<T> {
    return getNewLicenseRaw( requestConfig);
}

/**
 * Cancels a license service. After cancellation the license key is deactivated and the service transitions to a canceled status. No further billing charges will be incurred.
 * Cancel License
 */
function licensesCancelRaw<T>(requestParameters: LicensesCancelRequest, requestConfig: runtime.TypedQueryConfig<T, LicensesCancel200Response> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling licensesCancel.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/licenses/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'DELETE',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(LicensesCancel200ResponseFromJSON(body), text);
    }

    return config;
}

/**
* Cancels a license service. After cancellation the license key is deactivated and the service transitions to a canceled status. No further billing charges will be incurred.
* Cancel License
*/
export function licensesCancel<T>(requestParameters: LicensesCancelRequest, requestConfig?: runtime.TypedQueryConfig<T, LicensesCancel200Response>): QueryConfig<T> {
    return licensesCancelRaw(requestParameters, requestConfig);
}

/**
 * Changes the IP address associated with the license. The service must be active. Use `GET /licenses/{id}` to view the current IP assignment before making changes.
 * Change License IP
 */
function postLicenseChangeIpRaw<T>(requestParameters: PostLicenseChangeIpRequest, requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling postLicenseChangeIp.');
    }

    if (requestParameters.ipObject === null || requestParameters.ipObject === undefined) {
        throw new runtime.RequiredError('ipObject','Required parameter requestParameters.ipObject was null or undefined when calling postLicenseChangeIp.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/licenses/{id}/change_ip`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters || IpObjectToJSON(requestParameters.ipObject),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Changes the IP address associated with the license. The service must be active. Use `GET /licenses/{id}` to view the current IP assignment before making changes.
* Change License IP
*/
export function postLicenseChangeIp<T>(requestParameters: PostLicenseChangeIpRequest, requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return postLicenseChangeIpRaw(requestParameters, requestConfig);
}

/**
 * Validates a license order before placing it. Use this to check for errors before committing to a purchase.
 * Validate License Order
 */
function putLicensesRaw<T>( requestConfig: runtime.TypedQueryConfig<T, void> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/licenses/order`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'PUT',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* Validates a license order before placing it. Use this to check for errors before committing to a purchase.
* Validate License Order
*/
export function putLicenses<T>( requestConfig?: runtime.TypedQueryConfig<T, void>): QueryConfig<T> {
    return putLicensesRaw( requestConfig);
}

/**
 * Updates settings on a license service such as its assigned IP.
 * Update License
 */
function updateLicenseInfoRaw<T>(requestParameters: UpdateLicenseInfoRequest, requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling updateLicenseInfo.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/licenses/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Updates settings on a license service such as its assigned IP.
* Update License
*/
export function updateLicenseInfo<T>(requestParameters: UpdateLicenseInfoRequest, requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return updateLicenseInfoRaw(requestParameters, requestConfig);
}

