// tslint:disable
/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import { HttpMethods, QueryConfig, ResponseBody, ResponseText } from 'redux-query';
import * as runtime from '../runtime';
import {
    BuyItNowList,
    BuyItNowListFromJSON,
    BuyItNowListToJSON,
    BuyItNowServerOrder200Response,
    BuyItNowServerOrder200ResponseFromJSON,
    BuyItNowServerOrder200ResponseToJSON,
    ChargeInvoiceRows,
    ChargeInvoiceRowsFromJSON,
    ChargeInvoiceRowsToJSON,
    GetAccountInfo401Response,
    GetAccountInfo401ResponseFromJSON,
    GetAccountInfo401ResponseToJSON,
    PlaceBuyNowServerRequest,
    PlaceBuyNowServerRequestFromJSON,
    PlaceBuyNowServerRequestToJSON,
    ReverseDnsEntries,
    ReverseDnsEntriesFromJSON,
    ReverseDnsEntriesToJSON,
    Server,
    ServerFromJSON,
    ServerToJSON,
    ServerIpmiLiveInfo,
    ServerIpmiLiveInfoFromJSON,
    ServerIpmiLiveInfoToJSON,
    ServerOrder,
    ServerOrderFromJSON,
    ServerOrderToJSON,
    ServerRow,
    ServerRowFromJSON,
    ServerRowToJSON,
    ServersBuyNowError,
    ServersBuyNowErrorFromJSON,
    ServersBuyNowErrorToJSON,
    ServersBuyNowResponse,
    ServersBuyNowResponseFromJSON,
    ServersBuyNowResponseToJSON,
    ServersCancel200Response,
    ServersCancel200ResponseFromJSON,
    ServersCancel200ResponseToJSON,
    SuccessTextResponse,
    SuccessTextResponseFromJSON,
    SuccessTextResponseToJSON,
    TextResponse,
    TextResponseFromJSON,
    TextResponseToJSON,
} from '../models';

export interface GetServerInfoRequest {
    id: number;
}

export interface GetServerInvoicesRequest {
    id: number;
}

export interface GetServerReverseDnsRequest {
    id: number;
}

export interface GetServersWelcomeEmailRequest {
    id: number;
}

export interface PlaceBuyNowServerRequest {
    placeBuyNowServerRequest?: PlaceBuyNowServerRequest;
}

export interface PostServerReverseDnsRequest {
    id: number;
    reverseDnsEntries: ReverseDnsEntries;
}

export interface ServerIpmiLiveGetRequest {
    id: number;
}

export interface ServerIpmiLivePostRequest {
    id: number;
    ip: string;
    asset?: number;
}

export interface ServerIpmiPowerGetRequest {
    id: number;
}

export interface ServerIpmiPowerPostRequest {
    id: number;
    action: ServerIpmiPowerPostActionEnum;
    asset?: number;
}

export interface ServersCancelRequest {
    id: number;
}

export interface UpdateServerInfoRequest {
    id: string;
}


/**
 * Places an order for a new dedicated server. Use `PUT /servers/order` to validate the order first.
 * Place Server Order
 */
function addServerRaw<T>( requestConfig: runtime.TypedQueryConfig<T, void> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/servers/order`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* Places an order for a new dedicated server. Use `PUT /servers/order` to validate the order first.
* Place Server Order
*/
export function addServer<T>( requestConfig?: runtime.TypedQueryConfig<T, void>): QueryConfig<T> {
    return addServerRaw( requestConfig);
}

/**
 * Returns the configuration options and pricing for buy-it-now dedicated servers, including available bandwidth packages, IP blocks, operating systems, control panels, and RAID configurations. Use the returned option IDs when placing an order via `POST /servers/order/buy_now_server`.
 * Get Buy Now Server Options
 */
function buyItNowServerOrderRaw<T>( requestConfig: runtime.TypedQueryConfig<T, BuyItNowServerOrder200Response> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/servers/order/buy_now_server`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(BuyItNowServerOrder200ResponseFromJSON(body), text);
    }

    return config;
}

/**
* Returns the configuration options and pricing for buy-it-now dedicated servers, including available bandwidth packages, IP blocks, operating systems, control panels, and RAID configurations. Use the returned option IDs when placing an order via `POST /servers/order/buy_now_server`.
* Get Buy Now Server Options
*/
export function buyItNowServerOrder<T>( requestConfig?: runtime.TypedQueryConfig<T, BuyItNowServerOrder200Response>): QueryConfig<T> {
    return buyItNowServerOrderRaw( requestConfig);
}

/**
 * Returns the list of available Rapid Deploy dedicated servers with current pricing. Each entry includes CPU, memory, disk, bandwidth, IP allocation, and location details. These servers are pre-configured and can be provisioned immediately after purchase.
 * List Marketplace Servers
 */
function getMPServersRaw<T>( requestConfig: runtime.TypedQueryConfig<T, BuyItNowList> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/buy_now_servers_list`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(BuyItNowListFromJSON(body), text);
    }

    return config;
}

/**
* Returns the list of available Rapid Deploy dedicated servers with current pricing. Each entry includes CPU, memory, disk, bandwidth, IP allocation, and location details. These servers are pre-configured and can be provisioned immediately after purchase.
* List Marketplace Servers
*/
export function getMPServers<T>( requestConfig?: runtime.TypedQueryConfig<T, BuyItNowList>): QueryConfig<T> {
    return getMPServersRaw( requestConfig);
}

/**
 * Retrieves available server configurations and pricing for ordering a new dedicated server.
 * Server Ordering Information
 */
function getNewServerRaw<T>( requestConfig: runtime.TypedQueryConfig<T, ServerOrder> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/servers/order`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(ServerOrderFromJSON(body), text);
    }

    return config;
}

/**
* Retrieves available server configurations and pricing for ordering a new dedicated server.
* Server Ordering Information
*/
export function getNewServer<T>( requestConfig?: runtime.TypedQueryConfig<T, ServerOrder>): QueryConfig<T> {
    return getNewServerRaw( requestConfig);
}

/**
 * Returns detailed information about a specific server including its hardware configuration, IPs, and status.
 * Get Server Order
 */
function getServerInfoRaw<T>(requestParameters: GetServerInfoRequest, requestConfig: runtime.TypedQueryConfig<T, Server> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getServerInfo.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/servers/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(ServerFromJSON(body), text);
    }

    return config;
}

/**
* Returns detailed information about a specific server including its hardware configuration, IPs, and status.
* Get Server Order
*/
export function getServerInfo<T>(requestParameters: GetServerInfoRequest, requestConfig?: runtime.TypedQueryConfig<T, Server>): QueryConfig<T> {
    return getServerInfoRaw(requestParameters, requestConfig);
}

/**
 * Returns the billing invoices associated with this dedicated server.
 * Get Server Invoices
 */
function getServerInvoicesRaw<T>(requestParameters: GetServerInvoicesRequest, requestConfig: runtime.TypedQueryConfig<T, ChargeInvoiceRows> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getServerInvoices.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/servers/{id}/invoices`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(ChargeInvoiceRowsFromJSON(body), text);
    }

    return config;
}

/**
* Returns the billing invoices associated with this dedicated server.
* Get Server Invoices
*/
export function getServerInvoices<T>(requestParameters: GetServerInvoicesRequest, requestConfig?: runtime.TypedQueryConfig<T, ChargeInvoiceRows>): QueryConfig<T> {
    return getServerInvoicesRaw(requestParameters, requestConfig);
}

/**
 * Returns all dedicated server services on the account with their current status and configuration.
 * List Servers
 */
function getServerListRaw<T>( requestConfig: runtime.TypedQueryConfig<T, Array<ServerRow>> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/servers`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(body.map(ServerRowFromJSON), text);
    }

    return config;
}

/**
* Returns all dedicated server services on the account with their current status and configuration.
* List Servers
*/
export function getServerList<T>( requestConfig?: runtime.TypedQueryConfig<T, Array<ServerRow>>): QueryConfig<T> {
    return getServerListRaw( requestConfig);
}

/**
 * Returns the current reverse DNS (PTR record) entries for the server\'s IP addresses.
 * Reverse DNS Info
 */
function getServerReverseDnsRaw<T>(requestParameters: GetServerReverseDnsRequest, requestConfig: runtime.TypedQueryConfig<T, ReverseDnsEntries> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getServerReverseDns.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/servers/{id}/reverse_dns`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(ReverseDnsEntriesFromJSON(body), text);
    }

    return config;
}

/**
* Returns the current reverse DNS (PTR record) entries for the server\'s IP addresses.
* Reverse DNS Info
*/
export function getServerReverseDns<T>(requestParameters: GetServerReverseDnsRequest, requestConfig?: runtime.TypedQueryConfig<T, ReverseDnsEntries>): QueryConfig<T> {
    return getServerReverseDnsRaw(requestParameters, requestConfig);
}

/**
 * Resends the welcome email for the order.
 * Resend Server Welcome Email
 */
function getServersWelcomeEmailRaw<T>(requestParameters: GetServersWelcomeEmailRequest, requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getServersWelcomeEmail.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/servers/{id}/welcome_email`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Resends the welcome email for the order.
* Resend Server Welcome Email
*/
export function getServersWelcomeEmail<T>(requestParameters: GetServersWelcomeEmailRequest, requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return getServersWelcomeEmailRaw(requestParameters, requestConfig);
}

/**
 * Places an order for a buy-it-now dedicated server. Use `GET /servers/order/buy_now_server` to retrieve available server configurations and their IDs before ordering.
 * Place Buy Now Server Order
 */
function placeBuyNowServerRaw<T>(requestParameters: PlaceBuyNowServerRequest, requestConfig: runtime.TypedQueryConfig<T, ServersBuyNowResponse> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/servers/order/buy_now_server`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters || PlaceBuyNowServerRequestToJSON(requestParameters.placeBuyNowServerRequest),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(ServersBuyNowResponseFromJSON(body), text);
    }

    return config;
}

/**
* Places an order for a buy-it-now dedicated server. Use `GET /servers/order/buy_now_server` to retrieve available server configurations and their IDs before ordering.
* Place Buy Now Server Order
*/
export function placeBuyNowServer<T>(requestParameters: PlaceBuyNowServerRequest, requestConfig?: runtime.TypedQueryConfig<T, ServersBuyNowResponse>): QueryConfig<T> {
    return placeBuyNowServerRaw(requestParameters, requestConfig);
}

/**
 * Updates the reverse DNS (PTR record) entries for the server\'s IP addresses.
 * Update Reverse DNS
 */
function postServerReverseDnsRaw<T>(requestParameters: PostServerReverseDnsRequest, requestConfig: runtime.TypedQueryConfig<T, TextResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling postServerReverseDns.');
    }

    if (requestParameters.reverseDnsEntries === null || requestParameters.reverseDnsEntries === undefined) {
        throw new runtime.RequiredError('reverseDnsEntries','Required parameter requestParameters.reverseDnsEntries was null or undefined when calling postServerReverseDns.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/servers/{id}/reverse_dns`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters || ReverseDnsEntriesToJSON(requestParameters.reverseDnsEntries),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(TextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Updates the reverse DNS (PTR record) entries for the server\'s IP addresses.
* Update Reverse DNS
*/
export function postServerReverseDns<T>(requestParameters: PostServerReverseDnsRequest, requestConfig?: runtime.TypedQueryConfig<T, TextResponse>): QueryConfig<T> {
    return postServerReverseDnsRaw(requestParameters, requestConfig);
}

/**
 * Validates a server order before placing it. Use this to check for errors before committing to a purchase.
 * Validate Server Order
 */
function putServersRaw<T>( requestConfig: runtime.TypedQueryConfig<T, void> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/servers/order`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'PUT',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* Validates a server order before placing it. Use this to check for errors before committing to a purchase.
* Validate Server Order
*/
export function putServers<T>( requestConfig?: runtime.TypedQueryConfig<T, void>): QueryConfig<T> {
    return putServersRaw( requestConfig);
}

/**
 * Returns the current IPMI live connection information for the server.
 * Server IPMI Live Information
 */
function serverIpmiLiveGetRaw<T>(requestParameters: ServerIpmiLiveGetRequest, requestConfig: runtime.TypedQueryConfig<T, ServerIpmiLiveInfo> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling serverIpmiLiveGet.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/servers/{id}/ipmi_live`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(ServerIpmiLiveInfoFromJSON(body), text);
    }

    return config;
}

/**
* Returns the current IPMI live connection information for the server.
* Server IPMI Live Information
*/
export function serverIpmiLiveGet<T>(requestParameters: ServerIpmiLiveGetRequest, requestConfig?: runtime.TypedQueryConfig<T, ServerIpmiLiveInfo>): QueryConfig<T> {
    return serverIpmiLiveGetRaw(requestParameters, requestConfig);
}

/**
 * Configures IPMI live access by whitelisting your current IP address for connections to the server\'s IPMI management interface.
 * Server IPMI Live Setup
 */
function serverIpmiLivePostRaw<T>(requestParameters: ServerIpmiLivePostRequest, requestConfig: runtime.TypedQueryConfig<T, ServerIpmiLiveInfo> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling serverIpmiLivePost.');
    }

    if (requestParameters.ip === null || requestParameters.ip === undefined) {
        throw new runtime.RequiredError('ip','Required parameter requestParameters.ip was null or undefined when calling serverIpmiLivePost.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const formData = new FormData();
    if (requestParameters.asset !== undefined) {
        formData.append('asset', requestParameters.asset as any);
    }

    if (requestParameters.ip !== undefined) {
        formData.append('ip', requestParameters.ip as any);
    }

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/servers/{id}/ipmi_live`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: formData,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(ServerIpmiLiveInfoFromJSON(body), text);
    }

    return config;
}

/**
* Configures IPMI live access by whitelisting your current IP address for connections to the server\'s IPMI management interface.
* Server IPMI Live Setup
*/
export function serverIpmiLivePost<T>(requestParameters: ServerIpmiLivePostRequest, requestConfig?: runtime.TypedQueryConfig<T, ServerIpmiLiveInfo>): QueryConfig<T> {
    return serverIpmiLivePostRaw(requestParameters, requestConfig);
}

/**
 * Returns the chassis power status from ipmi.
 * Get IPMI Power Status
 */
function serverIpmiPowerGetRaw<T>(requestParameters: ServerIpmiPowerGetRequest, requestConfig: runtime.TypedQueryConfig<T, TextResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling serverIpmiPowerGet.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/servers/{id}/ipmi_power`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(TextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Returns the chassis power status from ipmi.
* Get IPMI Power Status
*/
export function serverIpmiPowerGet<T>(requestParameters: ServerIpmiPowerGetRequest, requestConfig?: runtime.TypedQueryConfig<T, TextResponse>): QueryConfig<T> {
    return serverIpmiPowerGetRaw(requestParameters, requestConfig);
}

/**
 * Uses the IPMI interface to set the Power status on the server.
 * Server IPMI Power
 */
function serverIpmiPowerPostRaw<T>(requestParameters: ServerIpmiPowerPostRequest, requestConfig: runtime.TypedQueryConfig<T, TextResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling serverIpmiPowerPost.');
    }

    if (requestParameters.action === null || requestParameters.action === undefined) {
        throw new runtime.RequiredError('action','Required parameter requestParameters.action was null or undefined when calling serverIpmiPowerPost.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const formData = new FormData();
    if (requestParameters.asset !== undefined) {
        formData.append('asset', requestParameters.asset as any);
    }

    if (requestParameters.action !== undefined) {
        formData.append('action', requestParameters.action as any);
    }

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/servers/{id}/ipmi_power`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: formData,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(TextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Uses the IPMI interface to set the Power status on the server.
* Server IPMI Power
*/
export function serverIpmiPowerPost<T>(requestParameters: ServerIpmiPowerPostRequest, requestConfig?: runtime.TypedQueryConfig<T, TextResponse>): QueryConfig<T> {
    return serverIpmiPowerPostRaw(requestParameters, requestConfig);
}

/**
 * Cancels the dedicated server service. The server will be deprovisioned and billing will stop at the end of the current billing cycle.
 * Cancel Server Service
 */
function serversCancelRaw<T>(requestParameters: ServersCancelRequest, requestConfig: runtime.TypedQueryConfig<T, ServersCancel200Response> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling serversCancel.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/servers/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'DELETE',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(ServersCancel200ResponseFromJSON(body), text);
    }

    return config;
}

/**
* Cancels the dedicated server service. The server will be deprovisioned and billing will stop at the end of the current billing cycle.
* Cancel Server Service
*/
export function serversCancel<T>(requestParameters: ServersCancelRequest, requestConfig?: runtime.TypedQueryConfig<T, ServersCancel200Response>): QueryConfig<T> {
    return serversCancelRaw(requestParameters, requestConfig);
}

/**
 * Updates settings on a dedicated server order.
 * Update Server Order
 */
function updateServerInfoRaw<T>(requestParameters: UpdateServerInfoRequest, requestConfig: runtime.TypedQueryConfig<T, void> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling updateServerInfo.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/servers/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* Updates settings on a dedicated server order.
* Update Server Order
*/
export function updateServerInfo<T>(requestParameters: UpdateServerInfoRequest, requestConfig?: runtime.TypedQueryConfig<T, void>): QueryConfig<T> {
    return updateServerInfoRaw(requestParameters, requestConfig);
}


/**
    * @export
    * @enum {string}
    */
export enum ServerIpmiPowerPostActionEnum {
    Cycle = 'cycle',
    Reset = 'reset',
    On = 'on',
    Off = 'off',
    Soft = 'soft'
}
