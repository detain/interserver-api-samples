// tslint:disable
/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import { HttpMethods, QueryConfig, ResponseBody, ResponseText } from 'redux-query';
import * as runtime from '../runtime';
import {
    AccountInfo,
    AccountInfoFromJSON,
    AccountInfoToJSON,
    GenericResponse,
    GenericResponseFromJSON,
    GenericResponseToJSON,
    GetAccountInfo401Response,
    GetAccountInfo401ResponseFromJSON,
    GetAccountInfo401ResponseToJSON,
    GetAccountTfaSetup200Response,
    GetAccountTfaSetup200ResponseFromJSON,
    GetAccountTfaSetup200ResponseToJSON,
    Home,
    HomeFromJSON,
    HomeToJSON,
    SearchAutocompleteResponse,
    SearchAutocompleteResponseFromJSON,
    SearchAutocompleteResponseToJSON,
    SuccessTextResponse,
    SuccessTextResponseFromJSON,
    SuccessTextResponseToJSON,
    TextResponse,
    TextResponseFromJSON,
    TextResponseToJSON,
} from '../models';

export interface DeleteAccountOauthNameRequest {
    name: string;
}

export interface LogoutAccountOauthRequest {
    name: string;
}

export interface UpdateAccountFeaturesRequest {
    disableReset?: number;
    disableReinstall?: number;
}

export interface UpdateAccountInfoRequest {
    name: string;
    address: string;
    city: string;
    state: string;
    zip: string;
    country: string;
    phone: string;
    company?: string;
    address2?: string;
    locale?: string;
    emailInvoices?: string;
    emailAbuse?: string;
    disableReset?: boolean;
    disableReinstall?: boolean;
    disableServerNotifications?: boolean;
    disableEmailNotifications?: boolean;
    gstin?: string;
}

export interface UpdateAccountIpLimitsRequest {
    start: string;
    end: string;
}

export interface UpdateAccountPasswordRequest {
    password: string;
}

export interface UpdateAccountSshKeyRequest {
    sshKey?: string;
}

export interface UpdateAccountTfaRequest {
    _2faGoogleCode: string;
}


/**
 * Changes the login username for the account. The new username must be unique across all accounts. After changing, use the new username for all future logins.
 * Change Account Username
 */
function changeAccountUsernameRaw<T>( requestConfig: runtime.TypedQueryConfig<T, TextResponse> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/account/username`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(TextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Changes the login username for the account. The new username must be unique across all accounts. After changing, use the new username for all future logins.
* Change Account Username
*/
export function changeAccountUsername<T>( requestConfig?: runtime.TypedQueryConfig<T, TextResponse>): QueryConfig<T> {
    return changeAccountUsernameRaw( requestConfig);
}

/**
 * Unlinks a third-party OAuth provider from the account. After unlinking, that provider can no longer be used for login.
 * Unlink OAuth Account
 */
function deleteAccountOauthNameRaw<T>(requestParameters: DeleteAccountOauthNameRequest, requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    if (requestParameters.name === null || requestParameters.name === undefined) {
        throw new runtime.RequiredError('name','Required parameter requestParameters.name was null or undefined when calling deleteAccountOauthName.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/account/oauth/{name}`.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters.name))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'DELETE',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Unlinks a third-party OAuth provider from the account. After unlinking, that provider can no longer be used for login.
* Unlink OAuth Account
*/
export function deleteAccountOauthName<T>(requestParameters: DeleteAccountOauthNameRequest, requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return deleteAccountOauthNameRaw(requestParameters, requestConfig);
}

/**
 * Disables two-factor authentication on the account. After disabling, the account will only require password-based authentication.
 * Disable Two-Factor Authentication
 */
function deleteAccountTfaRaw<T>( requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/account/2fa`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'DELETE',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Disables two-factor authentication on the account. After disabling, the account will only require password-based authentication.
* Disable Two-Factor Authentication
*/
export function deleteAccountTfa<T>( requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return deleteAccountTfaRaw( requestConfig);
}

/**
 * Removes an IP address range from the account\'s access restriction list. If this is the last range, IP limiting is effectively disabled and the account becomes accessible from any IP address.
 * Remove IP Access Restriction
 */
function deleteIpLimitRaw<T>( requestConfig: runtime.TypedQueryConfig<T, GenericResponse> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/account/iplimits`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'PATCH',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(GenericResponseFromJSON(body), text);
    }

    return config;
}

/**
* Removes an IP address range from the account\'s access restriction list. If this is the last range, IP limiting is effectively disabled and the account becomes accessible from any IP address.
* Remove IP Access Restriction
*/
export function deleteIpLimit<T>( requestConfig?: runtime.TypedQueryConfig<T, GenericResponse>): QueryConfig<T> {
    return deleteIpLimitRaw( requestConfig);
}

/**
 * Returns the full account profile including contact information, billing address, and security settings. Use this to populate account management forms or verify account state before making changes with `POST /account`.
 * Retrieve Account Details
 */
function getAccountInfoRaw<T>( requestConfig: runtime.TypedQueryConfig<T, AccountInfo> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/account`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(AccountInfoFromJSON(body), text);
    }

    return config;
}

/**
* Returns the full account profile including contact information, billing address, and security settings. Use this to populate account management forms or verify account state before making changes with `POST /account`.
* Retrieve Account Details
*/
export function getAccountInfo<T>( requestConfig?: runtime.TypedQueryConfig<T, AccountInfo>): QueryConfig<T> {
    return getAccountInfoRaw( requestConfig);
}

/**
 * Returns the TOTP secret key needed to configure a two-factor authentication app (e.g. Google Authenticator). Present the `2fa_google_key` as a QR code or display the `2fa_google_split` value for manual entry. After setup, verify with `POST /account/2fa`.
 * Get Two-Factor Setup Data
 */
function getAccountTfaSetupRaw<T>( requestConfig: runtime.TypedQueryConfig<T, GetAccountTfaSetup200Response> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/account/2fa`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(GetAccountTfaSetup200ResponseFromJSON(body), text);
    }

    return config;
}

/**
* Returns the TOTP secret key needed to configure a two-factor authentication app (e.g. Google Authenticator). Present the `2fa_google_key` as a QR code or display the `2fa_google_split` value for manual entry. After setup, verify with `POST /account/2fa`.
* Get Two-Factor Setup Data
*/
export function getAccountTfaSetup<T>( requestConfig?: runtime.TypedQueryConfig<T, GetAccountTfaSetup200Response>): QueryConfig<T> {
    return getAccountTfaSetupRaw( requestConfig);
}

/**
 * Returns an overview of the account suitable for a dashboard home page, including service counts, recent activity, and account-level alerts. Requires an authenticated session.
 * Get Home Data
 */
function getHomeRaw<T>( requestConfig: runtime.TypedQueryConfig<T, Home> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/home`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(HomeFromJSON(body), text);
    }

    return config;
}

/**
* Returns an overview of the account suitable for a dashboard home page, including service counts, recent activity, and account-level alerts. Requires an authenticated session.
* Get Home Data
*/
export function getHome<T>( requestConfig?: runtime.TypedQueryConfig<T, Home>): QueryConfig<T> {
    return getHomeRaw( requestConfig);
}

/**
 * Returns autocomplete results for the account\'s services and records. Use this endpoint to power global search experiences in the client portal.
 * Search Autocomplete
 */
function getSearchRaw<T>( requestConfig: runtime.TypedQueryConfig<T, SearchAutocompleteResponse> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/search`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SearchAutocompleteResponseFromJSON(body), text);
    }

    return config;
}

/**
* Returns autocomplete results for the account\'s services and records. Use this endpoint to power global search experiences in the client portal.
* Search Autocomplete
*/
export function getSearch<T>( requestConfig?: runtime.TypedQueryConfig<T, SearchAutocompleteResponse>): QueryConfig<T> {
    return getSearchRaw( requestConfig);
}

/**
 * Invalidates the current session. After calling this endpoint, the session token can no longer be used for authenticated requests. The client should discard the stored session ID.
 * Log Out
 */
function logoutRaw<T>( requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/logout`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Invalidates the current session. After calling this endpoint, the session token can no longer be used for authenticated requests. The client should discard the stored session ID.
* Log Out
*/
export function logout<T>( requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return logoutRaw( requestConfig);
}

/**
 * Logs out of the specified OAuth provider session.
 * Logout of OAuth
 */
function logoutAccountOauthRaw<T>(requestParameters: LogoutAccountOauthRequest, requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    if (requestParameters.name === null || requestParameters.name === undefined) {
        throw new runtime.RequiredError('name','Required parameter requestParameters.name was null or undefined when calling logoutAccountOauth.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/account/oauth/{name}/logout`.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters.name))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Logs out of the specified OAuth provider session.
* Logout of OAuth
*/
export function logoutAccountOauth<T>(requestParameters: LogoutAccountOauthRequest, requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return logoutAccountOauthRaw(requestParameters, requestConfig);
}

/**
 * Generates a new API key for the account. The previous key is immediately invalidated. Store the new key securely as it cannot be retrieved later.
 * Generate New API Key
 */
function updateAccountApiKeyRaw<T>( requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/account/apikey`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Generates a new API key for the account. The previous key is immediately invalidated. Store the new key securely as it cannot be retrieved later.
* Generate New API Key
*/
export function updateAccountApiKey<T>( requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return updateAccountApiKeyRaw( requestConfig);
}

/**
 * Updates account-level feature flags that control service capabilities. These flags can disable password reset, OS reinstall, or other potentially destructive operations across your services. Changes take effect immediately.
 * Update Account Feature Flags
 */
function updateAccountFeaturesRaw<T>(requestParameters: UpdateAccountFeaturesRequest, requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const formData = new FormData();
    if (requestParameters.disableReset !== undefined) {
        formData.append('disable_reset', requestParameters.disableReset as any);
    }

    if (requestParameters.disableReinstall !== undefined) {
        formData.append('disable_reinstall', requestParameters.disableReinstall as any);
    }

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/account/features`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: formData,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Updates account-level feature flags that control service capabilities. These flags can disable password reset, OS reinstall, or other potentially destructive operations across your services. Changes take effect immediately.
* Update Account Feature Flags
*/
export function updateAccountFeatures<T>(requestParameters: UpdateAccountFeaturesRequest, requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return updateAccountFeaturesRaw(requestParameters, requestConfig);
}

/**
 * Updates the stored contact and billing information on your account. Submit only the fields you want to change. Validation errors are returned as a 422 response with field-level messages.
 * Update Account Information
 */
function updateAccountInfoRaw<T>(requestParameters: UpdateAccountInfoRequest, requestConfig: runtime.TypedQueryConfig<T, void> = {}): QueryConfig<T> {
    if (requestParameters.name === null || requestParameters.name === undefined) {
        throw new runtime.RequiredError('name','Required parameter requestParameters.name was null or undefined when calling updateAccountInfo.');
    }

    if (requestParameters.address === null || requestParameters.address === undefined) {
        throw new runtime.RequiredError('address','Required parameter requestParameters.address was null or undefined when calling updateAccountInfo.');
    }

    if (requestParameters.city === null || requestParameters.city === undefined) {
        throw new runtime.RequiredError('city','Required parameter requestParameters.city was null or undefined when calling updateAccountInfo.');
    }

    if (requestParameters.state === null || requestParameters.state === undefined) {
        throw new runtime.RequiredError('state','Required parameter requestParameters.state was null or undefined when calling updateAccountInfo.');
    }

    if (requestParameters.zip === null || requestParameters.zip === undefined) {
        throw new runtime.RequiredError('zip','Required parameter requestParameters.zip was null or undefined when calling updateAccountInfo.');
    }

    if (requestParameters.country === null || requestParameters.country === undefined) {
        throw new runtime.RequiredError('country','Required parameter requestParameters.country was null or undefined when calling updateAccountInfo.');
    }

    if (requestParameters.phone === null || requestParameters.phone === undefined) {
        throw new runtime.RequiredError('phone','Required parameter requestParameters.phone was null or undefined when calling updateAccountInfo.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const formData = new FormData();
    if (requestParameters.name !== undefined) {
        formData.append('name', requestParameters.name as any);
    }

    if (requestParameters.company !== undefined) {
        formData.append('company', requestParameters.company as any);
    }

    if (requestParameters.address !== undefined) {
        formData.append('address', requestParameters.address as any);
    }

    if (requestParameters.address2 !== undefined) {
        formData.append('address2', requestParameters.address2 as any);
    }

    if (requestParameters.city !== undefined) {
        formData.append('city', requestParameters.city as any);
    }

    if (requestParameters.state !== undefined) {
        formData.append('state', requestParameters.state as any);
    }

    if (requestParameters.zip !== undefined) {
        formData.append('zip', requestParameters.zip as any);
    }

    if (requestParameters.country !== undefined) {
        formData.append('country', requestParameters.country as any);
    }

    if (requestParameters.phone !== undefined) {
        formData.append('phone', requestParameters.phone as any);
    }

    if (requestParameters.locale !== undefined) {
        formData.append('locale', requestParameters.locale as any);
    }

    if (requestParameters.emailInvoices !== undefined) {
        formData.append('email_invoices', requestParameters.emailInvoices as any);
    }

    if (requestParameters.emailAbuse !== undefined) {
        formData.append('email_abuse', requestParameters.emailAbuse as any);
    }

    if (requestParameters.disableReset !== undefined) {
        formData.append('disable_reset', requestParameters.disableReset as any);
    }

    if (requestParameters.disableReinstall !== undefined) {
        formData.append('disable_reinstall', requestParameters.disableReinstall as any);
    }

    if (requestParameters.disableServerNotifications !== undefined) {
        formData.append('disable_server_notifications', requestParameters.disableServerNotifications as any);
    }

    if (requestParameters.disableEmailNotifications !== undefined) {
        formData.append('disable_email_notifications', requestParameters.disableEmailNotifications as any);
    }

    if (requestParameters.gstin !== undefined) {
        formData.append('gstin', requestParameters.gstin as any);
    }

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/account`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: formData,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* Updates the stored contact and billing information on your account. Submit only the fields you want to change. Validation errors are returned as a 422 response with field-level messages.
* Update Account Information
*/
export function updateAccountInfo<T>(requestParameters: UpdateAccountInfoRequest, requestConfig?: runtime.TypedQueryConfig<T, void>): QueryConfig<T> {
    return updateAccountInfoRaw(requestParameters, requestConfig);
}

/**
 * Adds an IP address range to the account\'s access restriction list. Once IP limiting is active, only requests originating from allowed ranges can access the account. Provide the start and end of the range in dotted-quad notation.
 * Add IP Access Restriction
 */
function updateAccountIpLimitsRaw<T>(requestParameters: UpdateAccountIpLimitsRequest, requestConfig: runtime.TypedQueryConfig<T, void> = {}): QueryConfig<T> {
    if (requestParameters.start === null || requestParameters.start === undefined) {
        throw new runtime.RequiredError('start','Required parameter requestParameters.start was null or undefined when calling updateAccountIpLimits.');
    }

    if (requestParameters.end === null || requestParameters.end === undefined) {
        throw new runtime.RequiredError('end','Required parameter requestParameters.end was null or undefined when calling updateAccountIpLimits.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const formData = new FormData();
    if (requestParameters.start !== undefined) {
        formData.append('start', requestParameters.start as any);
    }

    if (requestParameters.end !== undefined) {
        formData.append('end', requestParameters.end as any);
    }

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/account/iplimits`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: formData,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* Adds an IP address range to the account\'s access restriction list. Once IP limiting is active, only requests originating from allowed ranges can access the account. Provide the start and end of the range in dotted-quad notation.
* Add IP Access Restriction
*/
export function updateAccountIpLimits<T>(requestParameters: UpdateAccountIpLimitsRequest, requestConfig?: runtime.TypedQueryConfig<T, void>): QueryConfig<T> {
    return updateAccountIpLimitsRaw(requestParameters, requestConfig);
}

/**
 * Changes the account login password. The current password must be provided for verification. After a successful change, existing API keys remain valid but active sessions may require re-authentication.
 * Change Account Password
 */
function updateAccountPasswordRaw<T>(requestParameters: UpdateAccountPasswordRequest, requestConfig: runtime.TypedQueryConfig<T, TextResponse> = {}): QueryConfig<T> {
    if (requestParameters.password === null || requestParameters.password === undefined) {
        throw new runtime.RequiredError('password','Required parameter requestParameters.password was null or undefined when calling updateAccountPassword.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const formData = new FormData();
    if (requestParameters.password !== undefined) {
        formData.append('password', requestParameters.password as any);
    }

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/account/password`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: formData,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(TextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Changes the account login password. The current password must be provided for verification. After a successful change, existing API keys remain valid but active sessions may require re-authentication.
* Change Account Password
*/
export function updateAccountPassword<T>(requestParameters: UpdateAccountPasswordRequest, requestConfig?: runtime.TypedQueryConfig<T, TextResponse>): QueryConfig<T> {
    return updateAccountPasswordRaw(requestParameters, requestConfig);
}

/**
 * Updates the SSH public key stored on the account. This key can be automatically installed on new VPS and server orders.
 * Update SSH Keys
 */
function updateAccountSshKeyRaw<T>(requestParameters: UpdateAccountSshKeyRequest, requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const formData = new FormData();
    if (requestParameters.sshKey !== undefined) {
        formData.append('ssh_key', requestParameters.sshKey as any);
    }

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/account/sshkey`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: formData,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Updates the SSH public key stored on the account. This key can be automatically installed on new VPS and server orders.
* Update SSH Keys
*/
export function updateAccountSshKey<T>(requestParameters: UpdateAccountSshKeyRequest, requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return updateAccountSshKeyRaw(requestParameters, requestConfig);
}

/**
 * Verifies the TOTP code from your authenticator app and enables two-factor authentication on the account. Use `GET /account/2fa` first to retrieve the secret key for app setup.
 * Enable Two-Factor Authentication
 */
function updateAccountTfaRaw<T>(requestParameters: UpdateAccountTfaRequest, requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    if (requestParameters._2faGoogleCode === null || requestParameters._2faGoogleCode === undefined) {
        throw new runtime.RequiredError('_2faGoogleCode','Required parameter requestParameters._2faGoogleCode was null or undefined when calling updateAccountTfa.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const formData = new FormData();
    if (requestParameters._2faGoogleCode !== undefined) {
        formData.append('2fa_google_code', requestParameters._2faGoogleCode as any);
    }

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/account/2fa`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: formData,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Verifies the TOTP code from your authenticator app and enables two-factor authentication on the account. Use `GET /account/2fa` first to retrieve the secret key for app setup.
* Enable Two-Factor Authentication
*/
export function updateAccountTfa<T>(requestParameters: UpdateAccountTfaRequest, requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return updateAccountTfaRaw(requestParameters, requestConfig);
}

