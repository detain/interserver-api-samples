// tslint:disable
/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import { HttpMethods, QueryConfig, ResponseBody, ResponseText } from 'redux-query';
import * as runtime from '../runtime';
import {
    BuyItNowList,
    BuyItNowListFromJSON,
    BuyItNowListToJSON,
    CaptchaResponse,
    CaptchaResponseFromJSON,
    CaptchaResponseToJSON,
    GetAccountInfo401Response,
    GetAccountInfo401ResponseFromJSON,
    GetAccountInfo401ResponseToJSON,
    GetOauthRedirect200Response,
    GetOauthRedirect200ResponseFromJSON,
    GetOauthRedirect200ResponseToJSON,
    LoginErrorResponse,
    LoginErrorResponseFromJSON,
    LoginErrorResponseToJSON,
    LoginInfo,
    LoginInfoFromJSON,
    LoginInfoToJSON,
    LoginSubmissionExample,
    LoginSubmissionExampleFromJSON,
    LoginSubmissionExampleToJSON,
    LoginSubmissionExampleGRecaptchaResponse,
    LoginSubmissionExampleGRecaptchaResponseFromJSON,
    LoginSubmissionExampleGRecaptchaResponseToJSON,
    LoginSuccessResponse,
    LoginSuccessResponseFromJSON,
    LoginSuccessResponseToJSON,
    PatchOauthTwoFactor200Response,
    PatchOauthTwoFactor200ResponseFromJSON,
    PatchOauthTwoFactor200ResponseToJSON,
    PatchOauthTwoFactorRequest,
    PatchOauthTwoFactorRequestFromJSON,
    PatchOauthTwoFactorRequestToJSON,
    PostOauthCallback200Response,
    PostOauthCallback200ResponseFromJSON,
    PostOauthCallback200ResponseToJSON,
    PostOauthCallbackRequest,
    PostOauthCallbackRequestFromJSON,
    PostOauthCallbackRequestToJSON,
    ServicesInfo,
    ServicesInfoFromJSON,
    ServicesInfoToJSON,
} from '../models';

export interface GetCountriesRequest {
    fetchBy?: GetCountriesFetchByEnum;
}

export interface GetOauthRedirectRequest {
    provider: string;
}

export interface PatchOauthTwoFactorRequest {
    patchOauthTwoFactorRequest: PatchOauthTwoFactorRequest;
}

export interface PostOauthCallbackRequest {
    provider: string;
    postOauthCallbackRequest?: PostOauthCallbackRequest;
}

export interface SubmitLoginRequest {
    login: string;
    passwd: string;
    remember?: string;
    gRecaptchaResponse?: LoginSubmissionExampleGRecaptchaResponse;
    tfa?: string;
}

export interface SubmitSignupRequest {
    loginSubmissionExample?: LoginSubmissionExample;
}


/**
 * Returns a base64-encoded JPEG captcha image for human verification. Display this image to the user and submit their response along with the registration or order form. No authentication is required for this endpoint.
 * Get Captcha Challenge
 */
function getCaptchaRaw<T>( requestConfig: runtime.TypedQueryConfig<T, CaptchaResponse> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/captcha`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(CaptchaResponseFromJSON(body), text);
    }

    return config;
}

/**
* Returns a base64-encoded JPEG captcha image for human verification. Display this image to the user and submit their response along with the registration or order form. No authentication is required for this endpoint.
* Get Captcha Challenge
*/
export function getCaptcha<T>( requestConfig?: runtime.TypedQueryConfig<T, CaptchaResponse>): QueryConfig<T> {
    return getCaptchaRaw( requestConfig);
}

/**
 * Returns a map of country codes to country names. Use the optional `fetch_by` parameter to select the key format: `iso2` (default, two-letter codes), `iso3` (three-letter codes), or `numcode` (numeric codes). Use these values when populating country fields in account registration or domain contact forms.
 * Get Countries
 */
function getCountriesRaw<T>(requestParameters: GetCountriesRequest, requestConfig: runtime.TypedQueryConfig<T, object> = {}): QueryConfig<T> {
    let queryParameters = null;

    queryParameters = {};


    if (requestParameters.fetchBy !== undefined) {
        queryParameters['fetch_by'] = requestParameters.fetchBy;
    }

    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/account/countries`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* Returns a map of country codes to country names. Use the optional `fetch_by` parameter to select the key format: `iso2` (default, two-letter codes), `iso3` (three-letter codes), or `numcode` (numeric codes). Use these values when populating country fields in account registration or domain contact forms.
* Get Countries
*/
export function getCountries<T>(requestParameters: GetCountriesRequest, requestConfig?: runtime.TypedQueryConfig<T, object>): QueryConfig<T> {
    return getCountriesRaw(requestParameters, requestConfig);
}

/**
 * Returns metadata about the API server\'s available modules, service packages, categories, and types. This public endpoint does not require authentication and is useful for discovering supported services before placing orders.
 * Get Server Info
 */
function getInfoRaw<T>( requestConfig: runtime.TypedQueryConfig<T, ServicesInfo> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/info`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(ServicesInfoFromJSON(body), text);
    }

    return config;
}

/**
* Returns metadata about the API server\'s available modules, service packages, categories, and types. This public endpoint does not require authentication and is useful for discovering supported services before placing orders.
* Get Server Info
*/
export function getInfo<T>( requestConfig?: runtime.TypedQueryConfig<T, ServicesInfo>): QueryConfig<T> {
    return getInfoRaw( requestConfig);
}

/**
 * Gets the various pieces of information useful for generating a login page.
 * Get Login Info
 */
function getLoginInfoRaw<T>( requestConfig: runtime.TypedQueryConfig<T, LoginInfo> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/login`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(LoginInfoFromJSON(body), text);
    }

    return config;
}

/**
* Gets the various pieces of information useful for generating a login page.
* Get Login Info
*/
export function getLoginInfo<T>( requestConfig?: runtime.TypedQueryConfig<T, LoginInfo>): QueryConfig<T> {
    return getLoginInfoRaw( requestConfig);
}

/**
 * Returns the list of available Rapid Deploy dedicated servers with current pricing. Each entry includes CPU, memory, disk, bandwidth, IP allocation, and location details. These servers are pre-configured and can be provisioned immediately after purchase.
 * List Marketplace Servers
 */
function getMPServersRaw<T>( requestConfig: runtime.TypedQueryConfig<T, BuyItNowList> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/buy_now_servers_list`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(BuyItNowListFromJSON(body), text);
    }

    return config;
}

/**
* Returns the list of available Rapid Deploy dedicated servers with current pricing. Each entry includes CPU, memory, disk, bandwidth, IP allocation, and location details. These servers are pre-configured and can be provisioned immediately after purchase.
* List Marketplace Servers
*/
export function getMPServers<T>( requestConfig?: runtime.TypedQueryConfig<T, BuyItNowList>): QueryConfig<T> {
    return getMPServersRaw( requestConfig);
}

/**
 * Returns the OAuth redirect URL for the specified provider. Redirect the user to this URL to begin the OAuth authentication flow. After the user authorizes, the provider will redirect back to your callback endpoint where you should call `POST /oauth`.
 * Get OAuth Redirect URL
 */
function getOauthRedirectRaw<T>(requestParameters: GetOauthRedirectRequest, requestConfig: runtime.TypedQueryConfig<T, GetOauthRedirect200Response> = {}): QueryConfig<T> {
    if (requestParameters.provider === null || requestParameters.provider === undefined) {
        throw new runtime.RequiredError('provider','Required parameter requestParameters.provider was null or undefined when calling getOauthRedirect.');
    }

    let queryParameters = null;

    queryParameters = {};


    if (requestParameters.provider !== undefined) {
        queryParameters['provider'] = requestParameters.provider;
    }

    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/oauth`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(GetOauthRedirect200ResponseFromJSON(body), text);
    }

    return config;
}

/**
* Returns the OAuth redirect URL for the specified provider. Redirect the user to this URL to begin the OAuth authentication flow. After the user authorizes, the provider will redirect back to your callback endpoint where you should call `POST /oauth`.
* Get OAuth Redirect URL
*/
export function getOauthRedirect<T>(requestParameters: GetOauthRedirectRequest, requestConfig?: runtime.TypedQueryConfig<T, GetOauthRedirect200Response>): QueryConfig<T> {
    return getOauthRedirectRaw(requestParameters, requestConfig);
}

/**
 * Returns the complete list of available timezone identifiers (e.g. `America/New_York`, `UTC`). Use these values when updating timezone settings on your account or on VPS and QuickServer services via `/vps/{id}/change_timezone` or `/qs/{id}/change_timezone`.
 * Get Available Timezones
 */
function getTimezonesRaw<T>( requestConfig: runtime.TypedQueryConfig<T, Array<string>> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/account/timezones`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* Returns the complete list of available timezone identifiers (e.g. `America/New_York`, `UTC`). Use these values when updating timezone settings on your account or on VPS and QuickServer services via `/vps/{id}/change_timezone` or `/qs/{id}/change_timezone`.
* Get Available Timezones
*/
export function getTimezones<T>( requestConfig?: runtime.TypedQueryConfig<T, Array<string>>): QueryConfig<T> {
    return getTimezonesRaw( requestConfig);
}

/**
 * Completes the OAuth login by verifying a two-factor authentication code. Use the `account_id` returned by `POST /oauth` when the response contained `error_code: \"2fa_required\"`.
 * Complete OAuth Two-Factor Verification
 */
function patchOauthTwoFactorRaw<T>(requestParameters: PatchOauthTwoFactorRequest, requestConfig: runtime.TypedQueryConfig<T, PatchOauthTwoFactor200Response> = {}): QueryConfig<T> {
    if (requestParameters.patchOauthTwoFactorRequest === null || requestParameters.patchOauthTwoFactorRequest === undefined) {
        throw new runtime.RequiredError('patchOauthTwoFactorRequest','Required parameter requestParameters.patchOauthTwoFactorRequest was null or undefined when calling patchOauthTwoFactor.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/oauth`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'PATCH',
            headers: headerParameters,
        },
        body: queryParameters || PatchOauthTwoFactorRequestToJSON(requestParameters.patchOauthTwoFactorRequest),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(PatchOauthTwoFactor200ResponseFromJSON(body), text);
    }

    return config;
}

/**
* Completes the OAuth login by verifying a two-factor authentication code. Use the `account_id` returned by `POST /oauth` when the response contained `error_code: \"2fa_required\"`.
* Complete OAuth Two-Factor Verification
*/
export function patchOauthTwoFactor<T>(requestParameters: PatchOauthTwoFactorRequest, requestConfig?: runtime.TypedQueryConfig<T, PatchOauthTwoFactor200Response>): QueryConfig<T> {
    return patchOauthTwoFactorRaw(requestParameters, requestConfig);
}

/**
 * Performs a single ping/pong request to let you know if the server is up.
 * Ping Server
 */
function pingServerRaw<T>( requestConfig: runtime.TypedQueryConfig<T, string> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/ping`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        throw "OH NO";
    }

    return config;
}

/**
* Performs a single ping/pong request to let you know if the server is up.
* Ping Server
*/
export function pingServer<T>( requestConfig?: runtime.TypedQueryConfig<T, string>): QueryConfig<T> {
    return pingServerRaw( requestConfig);
}

/**
 * Handles the OAuth callback after the user authorizes with the provider. Returns the login status. If the account has two-factor authentication enabled, the response will include `error_code: \"2fa_required\"` and an `account_id`; use `PATCH /oauth` with the 2FA code and account ID to complete login.
 * OAuth Callback
 */
function postOauthCallbackRaw<T>(requestParameters: PostOauthCallbackRequest, requestConfig: runtime.TypedQueryConfig<T, PostOauthCallback200Response> = {}): QueryConfig<T> {
    if (requestParameters.provider === null || requestParameters.provider === undefined) {
        throw new runtime.RequiredError('provider','Required parameter requestParameters.provider was null or undefined when calling postOauthCallback.');
    }

    let queryParameters = null;

    queryParameters = {};


    if (requestParameters.provider !== undefined) {
        queryParameters['provider'] = requestParameters.provider;
    }

    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/oauth`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters || PostOauthCallbackRequestToJSON(requestParameters.postOauthCallbackRequest),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(PostOauthCallback200ResponseFromJSON(body), text);
    }

    return config;
}

/**
* Handles the OAuth callback after the user authorizes with the provider. Returns the login status. If the account has two-factor authentication enabled, the response will include `error_code: \"2fa_required\"` and an `account_id`; use `PATCH /oauth` with the 2FA code and account ID to complete login.
* OAuth Callback
*/
export function postOauthCallback<T>(requestParameters: PostOauthCallbackRequest, requestConfig?: runtime.TypedQueryConfig<T, PostOauthCallback200Response>): QueryConfig<T> {
    return postOauthCallbackRaw(requestParameters, requestConfig);
}

/**
 * Authenticates using the provided login credentials and returns a session token. Include the returned session ID in the `sessionid` header on subsequent requests. If authentication fails, a `402` error is returned.
 * Submit Login Information
 */
function submitLoginRaw<T>(requestParameters: SubmitLoginRequest, requestConfig: runtime.TypedQueryConfig<T, LoginSuccessResponse> = {}): QueryConfig<T> {
    if (requestParameters.login === null || requestParameters.login === undefined) {
        throw new runtime.RequiredError('login','Required parameter requestParameters.login was null or undefined when calling submitLogin.');
    }

    if (requestParameters.passwd === null || requestParameters.passwd === undefined) {
        throw new runtime.RequiredError('passwd','Required parameter requestParameters.passwd was null or undefined when calling submitLogin.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const formData = new FormData();
    if (requestParameters.login !== undefined) {
        formData.append('login', requestParameters.login as any);
    }

    if (requestParameters.passwd !== undefined) {
        formData.append('passwd', requestParameters.passwd as any);
    }

    if (requestParameters.remember !== undefined) {
        formData.append('remember', requestParameters.remember as any);
    }

    if (requestParameters.gRecaptchaResponse !== undefined) {
        formData.append('g-recaptcha-response', requestParameters.gRecaptchaResponse as any);
    }

    if (requestParameters.tfa !== undefined) {
        formData.append('tfa', requestParameters.tfa as any);
    }

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/login`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: formData,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(LoginSuccessResponseFromJSON(body), text);
    }

    return config;
}

/**
* Authenticates using the provided login credentials and returns a session token. Include the returned session ID in the `sessionid` header on subsequent requests. If authentication fails, a `402` error is returned.
* Submit Login Information
*/
export function submitLogin<T>(requestParameters: SubmitLoginRequest, requestConfig?: runtime.TypedQueryConfig<T, LoginSuccessResponse>): QueryConfig<T> {
    return submitLoginRaw(requestParameters, requestConfig);
}

/**
 * Creates a new account in our system using the provided information.
 * Submit Signup Information
 */
function submitSignupRaw<T>(requestParameters: SubmitSignupRequest, requestConfig: runtime.TypedQueryConfig<T, void> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/signup`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters || LoginSubmissionExampleToJSON(requestParameters.loginSubmissionExample),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* Creates a new account in our system using the provided information.
* Submit Signup Information
*/
export function submitSignup<T>(requestParameters: SubmitSignupRequest, requestConfig?: runtime.TypedQueryConfig<T, void>): QueryConfig<T> {
    return submitSignupRaw(requestParameters, requestConfig);
}


/**
    * @export
    * @enum {string}
    */
export enum GetCountriesFetchByEnum {
    Iso2 = 'iso2',
    Iso3 = 'iso3',
    Numcode = 'numcode'
}
