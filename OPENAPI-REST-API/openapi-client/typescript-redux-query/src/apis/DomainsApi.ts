// tslint:disable
/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import { HttpMethods, QueryConfig, ResponseBody, ResponseText } from 'redux-query';
import * as runtime from '../runtime';
import {
    CancelDomain200Response,
    CancelDomain200ResponseFromJSON,
    CancelDomain200ResponseToJSON,
    ChargeInvoiceRows,
    ChargeInvoiceRowsFromJSON,
    ChargeInvoiceRowsToJSON,
    Domain,
    DomainFromJSON,
    DomainToJSON,
    DomainContactDetails,
    DomainContactDetailsFromJSON,
    DomainContactDetailsToJSON,
    DomainDnssecRecords,
    DomainDnssecRecordsFromJSON,
    DomainDnssecRecordsToJSON,
    DomainDnssecRequest,
    DomainDnssecRequestFromJSON,
    DomainDnssecRequestToJSON,
    DomainLookupResponse,
    DomainLookupResponseFromJSON,
    DomainLookupResponseToJSON,
    DomainNameserverGetResponse,
    DomainNameserverGetResponseFromJSON,
    DomainNameserverGetResponseToJSON,
    DomainNameserverPostRequest,
    DomainNameserverPostRequestFromJSON,
    DomainNameserverPostRequestToJSON,
    DomainNameserverPutRequest,
    DomainNameserverPutRequestFromJSON,
    DomainNameserverPutRequestToJSON,
    DomainOrder,
    DomainOrderFromJSON,
    DomainOrderToJSON,
    DomainRow,
    DomainRowFromJSON,
    DomainRowToJSON,
    DomainSearchResponse,
    DomainSearchResponseFromJSON,
    DomainSearchResponseToJSON,
    DomainWhoisPrivacyRequest,
    DomainWhoisPrivacyRequestFromJSON,
    DomainWhoisPrivacyRequestToJSON,
    GetAccountInfo401Response,
    GetAccountInfo401ResponseFromJSON,
    GetAccountInfo401ResponseToJSON,
    SuccessTextResponse,
    SuccessTextResponseFromJSON,
    SuccessTextResponseToJSON,
    TextResponse,
    TextResponseFromJSON,
    TextResponseToJSON,
} from '../models';

export interface AddDomainDnssecRequest {
    id: number;
    domainDnssecRequest: DomainDnssecRequest;
}

export interface AddDomainNameserverRequest {
    id: number;
    domainNameserverPostRequest: DomainNameserverPostRequest;
}

export interface CancelDomainRequest {
    id: number;
}

export interface DeleteDomainDnssecRequest {
    id: number;
    action: string;
}

export interface DeleteDomainNameserverRequest {
    id: number;
    index: number;
}

export interface GetDomainContactRequest {
    id: number;
}

export interface GetDomainDnssecRequest {
    id: number;
}

export interface GetDomainInfoRequest {
    id: number;
}

export interface GetDomainInvoicesRequest {
    id: number;
}

export interface GetDomainLookupRequest {
    name: string;
}

export interface GetDomainNameserversRequest {
    id: number;
}

export interface GetDomainOrderFieldsRequest {
    domain: string;
    regType: string;
}

export interface GetDomainOrderSearchResultsRequest {
    domain: string;
}

export interface GetDomainRenewalRequest {
    id: number;
}

export interface GetDomainSearchRequest {
    name: string;
}

export interface GetDomainTransferRequest {
    id: number;
}

export interface GetDomainWhoisPrivacyRequest {
    id: number;
}

export interface GetDomainsWelcomeEmailRequest {
    id: number;
}

export interface PostDomainRenewalRequest {
    id: number;
}

export interface PostDomainTransferRequest {
    id: number;
}

export interface UpdateDomainContactRequest {
    id: number;
    domainContactDetails: DomainContactDetails;
}

export interface UpdateDomainInfoRequest {
    id: string;
}

export interface UpdateDomainNameserversRequest {
    id: number;
    domainNameserverPutRequest: DomainNameserverPutRequest;
}

export interface UpdateDomainWhoisPrivacyRequest {
    id: number;
    domainWhoisPrivacyRequest: DomainWhoisPrivacyRequest;
}


/**
 * Places a new domain registration or transfer order. Use the results from `/domains/lookup/{name}` or `/domains/order/{domain}/{regType}` to populate the required domain fields before submitting the order.
 * Place Domain Order
 */
function addDomainRaw<T>( requestConfig: runtime.TypedQueryConfig<T, void> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/order`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* Places a new domain registration or transfer order. Use the results from `/domains/lookup/{name}` or `/domains/order/{domain}/{regType}` to populate the required domain fields before submitting the order.
* Place Domain Order
*/
export function addDomain<T>( requestConfig?: runtime.TypedQueryConfig<T, void>): QueryConfig<T> {
    return addDomainRaw( requestConfig);
}

/**
 * Adds DNSSEC DS records to the domain registration. Provide one or more DNSSEC record entries (algorithm, digest type, digest, key tag). Registrations must be active.
 * Add Domain DNSSEC Records
 */
function addDomainDnssecRaw<T>(requestParameters: AddDomainDnssecRequest, requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling addDomainDnssec.');
    }

    if (requestParameters.domainDnssecRequest === null || requestParameters.domainDnssecRequest === undefined) {
        throw new runtime.RequiredError('domainDnssecRequest','Required parameter requestParameters.domainDnssecRequest was null or undefined when calling addDomainDnssec.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/{id}/dnssec`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters || DomainDnssecRequestToJSON(requestParameters.domainDnssecRequest),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Adds DNSSEC DS records to the domain registration. Provide one or more DNSSEC record entries (algorithm, digest type, digest, key tag). Registrations must be active.
* Add Domain DNSSEC Records
*/
export function addDomainDnssec<T>(requestParameters: AddDomainDnssecRequest, requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return addDomainDnssecRaw(requestParameters, requestConfig);
}

/**
 * Adds a new registered nameserver (glue record) for the domain using the supplied hostname and IP address. Use `GET /domains/{id}/nameservers` to confirm that the new entry exists.
 * Add Registered Nameserver
 */
function addDomainNameserverRaw<T>(requestParameters: AddDomainNameserverRequest, requestConfig: runtime.TypedQueryConfig<T, TextResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling addDomainNameserver.');
    }

    if (requestParameters.domainNameserverPostRequest === null || requestParameters.domainNameserverPostRequest === undefined) {
        throw new runtime.RequiredError('domainNameserverPostRequest','Required parameter requestParameters.domainNameserverPostRequest was null or undefined when calling addDomainNameserver.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/{id}/nameservers`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters || DomainNameserverPostRequestToJSON(requestParameters.domainNameserverPostRequest),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(TextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Adds a new registered nameserver (glue record) for the domain using the supplied hostname and IP address. Use `GET /domains/{id}/nameservers` to confirm that the new entry exists.
* Add Registered Nameserver
*/
export function addDomainNameserver<T>(requestParameters: AddDomainNameserverRequest, requestConfig?: runtime.TypedQueryConfig<T, TextResponse>): QueryConfig<T> {
    return addDomainNameserverRaw(requestParameters, requestConfig);
}

/**
 * Cancels the domain order in the billing system. After cancellation the service will no longer auto-renew and will transition to a `canceled` status. The domain registration itself may remain active at the registrar until its current expiration date, but no further renewals will be processed. Treat the domain as inactive in downstream automation after this call succeeds.
 * Cancel Domain Order
 */
function cancelDomainRaw<T>(requestParameters: CancelDomainRequest, requestConfig: runtime.TypedQueryConfig<T, CancelDomain200Response> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling cancelDomain.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'DELETE',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(CancelDomain200ResponseFromJSON(body), text);
    }

    return config;
}

/**
* Cancels the domain order in the billing system. After cancellation the service will no longer auto-renew and will transition to a `canceled` status. The domain registration itself may remain active at the registrar until its current expiration date, but no further renewals will be processed. Treat the domain as inactive in downstream automation after this call succeeds.
* Cancel Domain Order
*/
export function cancelDomain<T>(requestParameters: CancelDomainRequest, requestConfig?: runtime.TypedQueryConfig<T, CancelDomain200Response>): QueryConfig<T> {
    return cancelDomainRaw(requestParameters, requestConfig);
}

/**
 * Removes DNSSEC DS records from the domain registration. Use this to disable DNSSEC at the registrar level.
 * Remove Domain DNSSEC Records
 */
function deleteDomainDnssecRaw<T>(requestParameters: DeleteDomainDnssecRequest, requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling deleteDomainDnssec.');
    }

    if (requestParameters.action === null || requestParameters.action === undefined) {
        throw new runtime.RequiredError('action','Required parameter requestParameters.action was null or undefined when calling deleteDomainDnssec.');
    }

    let queryParameters = null;

    queryParameters = {};


    if (requestParameters.action !== undefined) {
        queryParameters['action'] = requestParameters.action;
    }

    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/{id}/dnssec`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'DELETE',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Removes DNSSEC DS records from the domain registration. Use this to disable DNSSEC at the registrar level.
* Remove Domain DNSSEC Records
*/
export function deleteDomainDnssec<T>(requestParameters: DeleteDomainDnssecRequest, requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return deleteDomainDnssecRaw(requestParameters, requestConfig);
}

/**
 * Removes a registered nameserver (glue record) from the domain. Specify the zero-based `index` of the nameserver to remove as returned by `GET /domains/{id}/nameservers`.
 * Delete Registered Nameserver
 */
function deleteDomainNameserverRaw<T>(requestParameters: DeleteDomainNameserverRequest, requestConfig: runtime.TypedQueryConfig<T, TextResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling deleteDomainNameserver.');
    }

    if (requestParameters.index === null || requestParameters.index === undefined) {
        throw new runtime.RequiredError('index','Required parameter requestParameters.index was null or undefined when calling deleteDomainNameserver.');
    }

    let queryParameters = null;

    queryParameters = {};


    if (requestParameters.index !== undefined) {
        queryParameters['index'] = requestParameters.index;
    }

    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/{id}/nameservers`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'DELETE',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(TextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Removes a registered nameserver (glue record) from the domain. Specify the zero-based `index` of the nameserver to remove as returned by `GET /domains/{id}/nameservers`.
* Delete Registered Nameserver
*/
export function deleteDomainNameserver<T>(requestParameters: DeleteDomainNameserverRequest, requestConfig?: runtime.TypedQueryConfig<T, TextResponse>): QueryConfig<T> {
    return deleteDomainNameserverRaw(requestParameters, requestConfig);
}

/**
 * Returns the current domain contact details that will be used for the registrant, admin, technical, and billing contact records. Use this response as the basis for any contact updates.
 * Get Domain Contact Details
 */
function getDomainContactRaw<T>(requestParameters: GetDomainContactRequest, requestConfig: runtime.TypedQueryConfig<T, DomainContactDetails> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getDomainContact.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/{id}/contact`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(DomainContactDetailsFromJSON(body), text);
    }

    return config;
}

/**
* Returns the current domain contact details that will be used for the registrant, admin, technical, and billing contact records. Use this response as the basis for any contact updates.
* Get Domain Contact Details
*/
export function getDomainContact<T>(requestParameters: GetDomainContactRequest, requestConfig?: runtime.TypedQueryConfig<T, DomainContactDetails>): QueryConfig<T> {
    return getDomainContactRaw(requestParameters, requestConfig);
}

/**
 * Returns the DNSSEC DS records registered with the domain. Use the record data if you need to synchronize DNSSEC settings with another registry or to prepare an update request.
 * Get Domain DNSSEC Records
 */
function getDomainDnssecRaw<T>(requestParameters: GetDomainDnssecRequest, requestConfig: runtime.TypedQueryConfig<T, DomainDnssecRecords> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getDomainDnssec.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/{id}/dnssec`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(DomainDnssecRecordsFromJSON(body), text);
    }

    return config;
}

/**
* Returns the DNSSEC DS records registered with the domain. Use the record data if you need to synchronize DNSSEC settings with another registry or to prepare an update request.
* Get Domain DNSSEC Records
*/
export function getDomainDnssec<T>(requestParameters: GetDomainDnssecRequest, requestConfig?: runtime.TypedQueryConfig<T, DomainDnssecRecords>): QueryConfig<T> {
    return getDomainDnssecRaw(requestParameters, requestConfig);
}

/**
 * Fetches the detailed domain service record (billing, registration, and service metadata) for the specified order ID. Use the `domain_hostname` from this response when calling `/domains/lookup/{name}` or when interacting with nameserver and DNSSEC management endpoints.
 * Get Domain Order
 */
function getDomainInfoRaw<T>(requestParameters: GetDomainInfoRequest, requestConfig: runtime.TypedQueryConfig<T, Domain> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getDomainInfo.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(DomainFromJSON(body), text);
    }

    return config;
}

/**
* Fetches the detailed domain service record (billing, registration, and service metadata) for the specified order ID. Use the `domain_hostname` from this response when calling `/domains/lookup/{name}` or when interacting with nameserver and DNSSEC management endpoints.
* Get Domain Order
*/
export function getDomainInfo<T>(requestParameters: GetDomainInfoRequest, requestConfig?: runtime.TypedQueryConfig<T, Domain>): QueryConfig<T> {
    return getDomainInfoRaw(requestParameters, requestConfig);
}

/**
 * Retrieves invoice records associated with the domain order. Use this to reconcile billing or to confirm whether renewals or add-ons (such as Whois privacy) have been invoiced.
 * Get Domain Invoices
 */
function getDomainInvoicesRaw<T>(requestParameters: GetDomainInvoicesRequest, requestConfig: runtime.TypedQueryConfig<T, ChargeInvoiceRows> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getDomainInvoices.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/{id}/invoices`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(ChargeInvoiceRowsFromJSON(body), text);
    }

    return config;
}

/**
* Retrieves invoice records associated with the domain order. Use this to reconcile billing or to confirm whether renewals or add-ons (such as Whois privacy) have been invoiced.
* Get Domain Invoices
*/
export function getDomainInvoices<T>(requestParameters: GetDomainInvoicesRequest, requestConfig?: runtime.TypedQueryConfig<T, ChargeInvoiceRows>): QueryConfig<T> {
    return getDomainInvoicesRaw(requestParameters, requestConfig);
}

/**
 * Checks a domain for availability and premium status, and returns pricing information for new, renewal, and transfer actions when possible. The response also includes the order field metadata you can pass to `/domains/order` when placing a registration or transfer.
 * Lookup Domain Availability and Pricing
 */
function getDomainLookupRaw<T>(requestParameters: GetDomainLookupRequest, requestConfig: runtime.TypedQueryConfig<T, DomainLookupResponse> = {}): QueryConfig<T> {
    if (requestParameters.name === null || requestParameters.name === undefined) {
        throw new runtime.RequiredError('name','Required parameter requestParameters.name was null or undefined when calling getDomainLookup.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/lookup/{name}`.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters.name))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(DomainLookupResponseFromJSON(body), text);
    }

    return config;
}

/**
* Checks a domain for availability and premium status, and returns pricing information for new, renewal, and transfer actions when possible. The response also includes the order field metadata you can pass to `/domains/order` when placing a registration or transfer.
* Lookup Domain Availability and Pricing
*/
export function getDomainLookup<T>(requestParameters: GetDomainLookupRequest, requestConfig?: runtime.TypedQueryConfig<T, DomainLookupResponse>): QueryConfig<T> {
    return getDomainLookupRaw(requestParameters, requestConfig);
}

/**
 * Returns the registered nameserver hosts and glue IP addresses currently configured at the registrar. Use the returned list when adding, updating, or deleting individual nameserver records.
 * List Registered Nameservers
 */
function getDomainNameserversRaw<T>(requestParameters: GetDomainNameserversRequest, requestConfig: runtime.TypedQueryConfig<T, DomainNameserverGetResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getDomainNameservers.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/{id}/nameservers`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(DomainNameserverGetResponseFromJSON(body), text);
    }

    return config;
}

/**
* Returns the registered nameserver hosts and glue IP addresses currently configured at the registrar. Use the returned list when adding, updating, or deleting individual nameserver records.
* List Registered Nameservers
*/
export function getDomainNameservers<T>(requestParameters: GetDomainNameserversRequest, requestConfig?: runtime.TypedQueryConfig<T, DomainNameserverGetResponse>): QueryConfig<T> {
    return getDomainNameserversRaw(requestParameters, requestConfig);
}

/**
 * Returns the registration-type-specific form fields required to place a domain order for the given hostname. Use the returned fields to build the POST body for `POST /domains/order`.
 * Get Domain Order Fields
 */
function getDomainOrderFieldsRaw<T>(requestParameters: GetDomainOrderFieldsRequest, requestConfig: runtime.TypedQueryConfig<T, void> = {}): QueryConfig<T> {
    if (requestParameters.domain === null || requestParameters.domain === undefined) {
        throw new runtime.RequiredError('domain','Required parameter requestParameters.domain was null or undefined when calling getDomainOrderFields.');
    }

    if (requestParameters.regType === null || requestParameters.regType === undefined) {
        throw new runtime.RequiredError('regType','Required parameter requestParameters.regType was null or undefined when calling getDomainOrderFields.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/order/{domain}/{regType}`.replace(`{${"domain"}}`, encodeURIComponent(String(requestParameters.domain))).replace(`{${"regType"}}`, encodeURIComponent(String(requestParameters.regType))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* Returns the registration-type-specific form fields required to place a domain order for the given hostname. Use the returned fields to build the POST body for `POST /domains/order`.
* Get Domain Order Fields
*/
export function getDomainOrderFields<T>(requestParameters: GetDomainOrderFieldsRequest, requestConfig?: runtime.TypedQueryConfig<T, void>): QueryConfig<T> {
    return getDomainOrderFieldsRaw(requestParameters, requestConfig);
}

/**
 * Returns order data for the specified domain hostname, including availability and pricing details. Use this to preview order information before placing a registration or transfer.
 * Get Domain Order Search Results
 */
function getDomainOrderSearchResultsRaw<T>(requestParameters: GetDomainOrderSearchResultsRequest, requestConfig: runtime.TypedQueryConfig<T, void> = {}): QueryConfig<T> {
    if (requestParameters.domain === null || requestParameters.domain === undefined) {
        throw new runtime.RequiredError('domain','Required parameter requestParameters.domain was null or undefined when calling getDomainOrderSearchResults.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/order/{domain}`.replace(`{${"domain"}}`, encodeURIComponent(String(requestParameters.domain))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* Returns order data for the specified domain hostname, including availability and pricing details. Use this to preview order information before placing a registration or transfer.
* Get Domain Order Search Results
*/
export function getDomainOrderSearchResults<T>(requestParameters: GetDomainOrderSearchResultsRequest, requestConfig?: runtime.TypedQueryConfig<T, void>): QueryConfig<T> {
    return getDomainOrderSearchResultsRaw(requestParameters, requestConfig);
}

/**
 * Starts the domain renewal workflow for an active domain. This endpoint currently returns a status message indicating that renewal handling has been triggered.
 * Start Domain Renewal Flow
 */
function getDomainRenewalRaw<T>(requestParameters: GetDomainRenewalRequest, requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getDomainRenewal.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/{id}/renew`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Starts the domain renewal workflow for an active domain. This endpoint currently returns a status message indicating that renewal handling has been triggered.
* Start Domain Renewal Flow
*/
export function getDomainRenewal<T>(requestParameters: GetDomainRenewalRequest, requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return getDomainRenewalRaw(requestParameters, requestConfig);
}

/**
 * Queries the registrar for suggested domains and availability data. Use the returned `lookup` results to identify availability and then call `/domains/lookup/{name}` or `/domains/order` to obtain pricing and order fields for the chosen domain.
 * Search Domain Suggestions
 */
function getDomainSearchRaw<T>(requestParameters: GetDomainSearchRequest, requestConfig: runtime.TypedQueryConfig<T, DomainSearchResponse> = {}): QueryConfig<T> {
    if (requestParameters.name === null || requestParameters.name === undefined) {
        throw new runtime.RequiredError('name','Required parameter requestParameters.name was null or undefined when calling getDomainSearch.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/search/{name}`.replace(`{${"name"}}`, encodeURIComponent(String(requestParameters.name))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(DomainSearchResponseFromJSON(body), text);
    }

    return config;
}

/**
* Queries the registrar for suggested domains and availability data. Use the returned `lookup` results to identify availability and then call `/domains/lookup/{name}` or `/domains/order` to obtain pricing and order fields for the chosen domain.
* Search Domain Suggestions
*/
export function getDomainSearch<T>(requestParameters: GetDomainSearchRequest, requestConfig?: runtime.TypedQueryConfig<T, DomainSearchResponse>): QueryConfig<T> {
    return getDomainSearchRaw(requestParameters, requestConfig);
}

/**
 * Initiates the transfer workflow for a domain already in your account. Use this in coordination with `/domains/{id}/contact` to ensure registrant details are ready for the transfer request.
 * Start Domain Transfer Flow
 */
function getDomainTransferRaw<T>(requestParameters: GetDomainTransferRequest, requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getDomainTransfer.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/{id}/transfer`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Initiates the transfer workflow for a domain already in your account. Use this in coordination with `/domains/{id}/contact` to ensure registrant details are ready for the transfer request.
* Start Domain Transfer Flow
*/
export function getDomainTransfer<T>(requestParameters: GetDomainTransferRequest, requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return getDomainTransferRaw(requestParameters, requestConfig);
}

/**
 * Returns status text for the Whois privacy add-on on the domain. Use this to determine whether privacy can be enabled or disabled for the current TLD before submitting updates.
 * Get Whois Privacy Status
 */
function getDomainWhoisPrivacyRaw<T>(requestParameters: GetDomainWhoisPrivacyRequest, requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getDomainWhoisPrivacy.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/{id}/whois`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Returns status text for the Whois privacy add-on on the domain. Use this to determine whether privacy can be enabled or disabled for the current TLD before submitting updates.
* Get Whois Privacy Status
*/
export function getDomainWhoisPrivacy<T>(requestParameters: GetDomainWhoisPrivacyRequest, requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return getDomainWhoisPrivacyRaw(requestParameters, requestConfig);
}

/**
 * Returns the domain services on your account, including the billing order ID, hostname, expiration date, and status. Use the `domain_id` from this response with `GET /domains/{id}` to fetch full service details, or with `/domains/{id}/nameservers`, `/domains/{id}/contact`, and `/domains/{id}/whois` to manage registration settings.
 * List Domain Orders
 */
function getDomainsListRaw<T>( requestConfig: runtime.TypedQueryConfig<T, Array<DomainRow>> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(body.map(DomainRowFromJSON), text);
    }

    return config;
}

/**
* Returns the domain services on your account, including the billing order ID, hostname, expiration date, and status. Use the `domain_id` from this response with `GET /domains/{id}` to fetch full service details, or with `/domains/{id}/nameservers`, `/domains/{id}/contact`, and `/domains/{id}/whois` to manage registration settings.
* List Domain Orders
*/
export function getDomainsList<T>( requestConfig?: runtime.TypedQueryConfig<T, Array<DomainRow>>): QueryConfig<T> {
    return getDomainsListRaw( requestConfig);
}

/**
 * Resends the welcome email for the domain service. The email contains registration details and management instructions.
 * Resend Domain Welcome Email
 */
function getDomainsWelcomeEmailRaw<T>(requestParameters: GetDomainsWelcomeEmailRequest, requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling getDomainsWelcomeEmail.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/{id}/welcome_email`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Resends the welcome email for the domain service. The email contains registration details and management instructions.
* Resend Domain Welcome Email
*/
export function getDomainsWelcomeEmail<T>(requestParameters: GetDomainsWelcomeEmailRequest, requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return getDomainsWelcomeEmailRaw(requestParameters, requestConfig);
}

/**
 * Returns the catalog of available domain services, including TLD service IDs and base Whois privacy pricing. Use the `tldServices` map to resolve a TLD into a service ID when you build an order request for `/domains/order` (POST) or the domain-field lookup endpoints.
 * Get Domain Ordering Information
 */
function getNewDomainRaw<T>( requestConfig: runtime.TypedQueryConfig<T, DomainOrder> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/order`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(DomainOrderFromJSON(body), text);
    }

    return config;
}

/**
* Returns the catalog of available domain services, including TLD service IDs and base Whois privacy pricing. Use the `tldServices` map to resolve a TLD into a service ID when you build an order request for `/domains/order` (POST) or the domain-field lookup endpoints.
* Get Domain Ordering Information
*/
export function getNewDomain<T>( requestConfig?: runtime.TypedQueryConfig<T, DomainOrder>): QueryConfig<T> {
    return getNewDomainRaw( requestConfig);
}

/**
 * Returns the domain-specific field requirements for the supplied hostname and registration type. Use this to build the form data required for `/domains/order` (POST).
 * Validate Domain Order
 */
function patchDomainsRaw<T>( requestConfig: runtime.TypedQueryConfig<T, void> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/order`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'PATCH',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* Returns the domain-specific field requirements for the supplied hostname and registration type. Use this to build the form data required for `/domains/order` (POST).
* Validate Domain Order
*/
export function patchDomains<T>( requestConfig?: runtime.TypedQueryConfig<T, void>): QueryConfig<T> {
    return patchDomainsRaw( requestConfig);
}

/**
 * Submits a domain renewal request for the order. Use `/domains/{id}/invoices` to identify any invoices created as part of the renewal flow.
 * Request Domain Renewal
 */
function postDomainRenewalRaw<T>(requestParameters: PostDomainRenewalRequest, requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling postDomainRenewal.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/{id}/renew`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Submits a domain renewal request for the order. Use `/domains/{id}/invoices` to identify any invoices created as part of the renewal flow.
* Request Domain Renewal
*/
export function postDomainRenewal<T>(requestParameters: PostDomainRenewalRequest, requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return postDomainRenewalRaw(requestParameters, requestConfig);
}

/**
 * Submits a transfer request for the domain order. Use `/domains/{id}/invoices` to identify any invoices generated as part of the transfer process.
 * Request Domain Transfer
 */
function postDomainTransferRaw<T>(requestParameters: PostDomainTransferRequest, requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling postDomainTransfer.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/{id}/transfer`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Submits a transfer request for the domain order. Use `/domains/{id}/invoices` to identify any invoices generated as part of the transfer process.
* Request Domain Transfer
*/
export function postDomainTransfer<T>(requestParameters: PostDomainTransferRequest, requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return postDomainTransferRaw(requestParameters, requestConfig);
}

/**
 * Returns validation data for a domain order request (such as availability and pricing information). This is typically used as a pre-flight check before submitting a POST order.
 * Domain Order Search
 */
function putDomainsRaw<T>( requestConfig: runtime.TypedQueryConfig<T, void> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/order`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'PUT',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* Returns validation data for a domain order request (such as availability and pricing information). This is typically used as a pre-flight check before submitting a POST order.
* Domain Order Search
*/
export function putDomains<T>( requestConfig?: runtime.TypedQueryConfig<T, void>): QueryConfig<T> {
    return putDomainsRaw( requestConfig);
}

/**
 * Updates the registrant/admin contact details for the domain. Submit the same fields returned by the GET response after making the desired changes. Domain updates require the service to be active and unlocked.
 * Update Domain Contact Details
 */
function updateDomainContactRaw<T>(requestParameters: UpdateDomainContactRequest, requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling updateDomainContact.');
    }

    if (requestParameters.domainContactDetails === null || requestParameters.domainContactDetails === undefined) {
        throw new runtime.RequiredError('domainContactDetails','Required parameter requestParameters.domainContactDetails was null or undefined when calling updateDomainContact.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/{id}/contact`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters || DomainContactDetailsToJSON(requestParameters.domainContactDetails),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Updates the registrant/admin contact details for the domain. Submit the same fields returned by the GET response after making the desired changes. Domain updates require the service to be active and unlocked.
* Update Domain Contact Details
*/
export function updateDomainContact<T>(requestParameters: UpdateDomainContactRequest, requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return updateDomainContactRaw(requestParameters, requestConfig);
}

/**
 * Updates the domain service record for the order. Use this for account-level changes such as updating stored registration metadata or transfer attributes.
 * Update Domain Order
 */
function updateDomainInfoRaw<T>(requestParameters: UpdateDomainInfoRequest, requestConfig: runtime.TypedQueryConfig<T, void> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling updateDomainInfo.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
* Updates the domain service record for the order. Use this for account-level changes such as updating stored registration metadata or transfer attributes.
* Update Domain Order
*/
export function updateDomainInfo<T>(requestParameters: UpdateDomainInfoRequest, requestConfig?: runtime.TypedQueryConfig<T, void>): QueryConfig<T> {
    return updateDomainInfoRaw(requestParameters, requestConfig);
}

/**
 * Replaces the full nameserver assignment for the domain with the provided list. This is the primary method for changing which authoritative nameservers the domain delegates to.
 * Replace Nameserver Set
 */
function updateDomainNameserversRaw<T>(requestParameters: UpdateDomainNameserversRequest, requestConfig: runtime.TypedQueryConfig<T, TextResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling updateDomainNameservers.');
    }

    if (requestParameters.domainNameserverPutRequest === null || requestParameters.domainNameserverPutRequest === undefined) {
        throw new runtime.RequiredError('domainNameserverPutRequest','Required parameter requestParameters.domainNameserverPutRequest was null or undefined when calling updateDomainNameservers.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/{id}/nameservers`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'PUT',
            headers: headerParameters,
        },
        body: queryParameters || DomainNameserverPutRequestToJSON(requestParameters.domainNameserverPutRequest),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(TextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Replaces the full nameserver assignment for the domain with the provided list. This is the primary method for changing which authoritative nameservers the domain delegates to.
* Replace Nameserver Set
*/
export function updateDomainNameservers<T>(requestParameters: UpdateDomainNameserversRequest, requestConfig?: runtime.TypedQueryConfig<T, TextResponse>): QueryConfig<T> {
    return updateDomainNameserversRaw(requestParameters, requestConfig);
}

/**
 * Enables or disables the Whois privacy add-on for the domain. The API may create invoices when enabling privacy; use `/domains/{id}/invoices` to track billing events.
 * Update Whois Privacy
 */
function updateDomainWhoisPrivacyRaw<T>(requestParameters: UpdateDomainWhoisPrivacyRequest, requestConfig: runtime.TypedQueryConfig<T, SuccessTextResponse> = {}): QueryConfig<T> {
    if (requestParameters.id === null || requestParameters.id === undefined) {
        throw new runtime.RequiredError('id','Required parameter requestParameters.id was null or undefined when calling updateDomainWhoisPrivacy.');
    }

    if (requestParameters.domainWhoisPrivacyRequest === null || requestParameters.domainWhoisPrivacyRequest === undefined) {
        throw new runtime.RequiredError('domainWhoisPrivacyRequest','Required parameter requestParameters.domainWhoisPrivacyRequest was null or undefined when calling updateDomainWhoisPrivacy.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    meta.authType = ['api_key', 'header'];
    meta.authType = ['api_key', 'header'];
    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/domains/{id}/whois`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters.id))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters || DomainWhoisPrivacyRequestToJSON(requestParameters.domainWhoisPrivacyRequest),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(SuccessTextResponseFromJSON(body), text);
    }

    return config;
}

/**
* Enables or disables the Whois privacy add-on for the domain. The API may create invoices when enabling privacy; use `/domains/{id}/invoices` to track billing events.
* Update Whois Privacy
*/
export function updateDomainWhoisPrivacy<T>(requestParameters: UpdateDomainWhoisPrivacyRequest, requestConfig?: runtime.TypedQueryConfig<T, SuccessTextResponse>): QueryConfig<T> {
    return updateDomainWhoisPrivacyRaw(requestParameters, requestConfig);
}

