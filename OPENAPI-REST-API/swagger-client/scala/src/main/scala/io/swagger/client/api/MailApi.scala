/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * OpenAPI spec version: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */
package io.swagger.client.api

import java.text.SimpleDateFormat

import io.swagger.client.model.ChargeInvoiceRows
import io.swagger.client.model.DenyRuleNew
import io.swagger.client.model.DenyRuleRecord
import io.swagger.client.model.EmailAddress
import io.swagger.client.model.EmailAddressName
import io.swagger.client.model.GenericResponse
import io.swagger.client.model.MailAlertRequest
import io.swagger.client.model.MailAlertUpdateRequest
import io.swagger.client.model.MailAlertsResponse
import io.swagger.client.model.MailAttachment
import io.swagger.client.model.MailBlocks
import io.swagger.client.model.MailDelistRequest
import io.swagger.client.model.MailDelistResponse
import io.swagger.client.model.MailDeliverabilityResponse
import io.swagger.client.model.MailLog
import io.swagger.client.model.MailOrder
import io.swagger.client.model.MailRow
import io.swagger.client.model.MailSchema
import io.swagger.client.model.MailStatsType
import io.swagger.client.model.SendMail
import io.swagger.client.model.SendMailAdv
import io.swagger.client.model.SuccessTextResponse
import io.swagger.client.model.endDate
import io.swagger.client.model.inline_response_200_8
import io.swagger.client.model.inline_response_401
import io.swagger.client.model.startDate
import io.swagger.client.{ApiInvoker, ApiException}

import com.sun.jersey.multipart.FormDataMultiPart
import com.sun.jersey.multipart.file.FileDataBodyPart

import javax.ws.rs.core.MediaType

import java.io.File
import java.util.Date
import java.util.TimeZone

import scala.collection.mutable.HashMap

import com.wordnik.swagger.client._
import scala.concurrent.Future
import collection.mutable

import java.net.URI

import com.wordnik.swagger.client.ClientResponseReaders.Json4sFormatsReader._
import com.wordnik.swagger.client.RequestWriters.Json4sFormatsWriter._

import scala.concurrent.ExecutionContext.Implicits.global
import scala.concurrent._
import scala.concurrent.duration._
import scala.util.{Failure, Success, Try}

import org.json4s._

class MailApi(
  val defBasePath: String = "https://my.interserver.net/apiv2",
  defApiInvoker: ApiInvoker = ApiInvoker
) {
  private lazy val dateTimeFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSSZ")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  private val dateFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  implicit val formats = new org.json4s.DefaultFormats {
    override def dateFormatter = dateTimeFormatter
  }
  implicit val stringReader: ClientResponseReader[String] = ClientResponseReaders.StringReader
  implicit val unitReader: ClientResponseReader[Unit] = ClientResponseReaders.UnitReader
  implicit val jvalueReader: ClientResponseReader[JValue] = ClientResponseReaders.JValueReader
  implicit val jsonReader: ClientResponseReader[Nothing] = JsonFormatsReader
  implicit val stringWriter: RequestWriter[String] = RequestWriters.StringWriter
  implicit val jsonWriter: RequestWriter[Nothing] = JsonFormatsWriter

  var basePath: String = defBasePath
  var apiInvoker: ApiInvoker = defApiInvoker

  def addHeader(key: String, value: String): mutable.HashMap[String, String] = {
    apiInvoker.defaultHeaders += key -> value
  }

  val config: SwaggerConfig = SwaggerConfig.forUrl(new URI(defBasePath))
  val client = new RestClient(config)
  val helper = new MailApiAsyncHelper(client, config)

  /**
   * Place Mail Order
   * Places a Mail Baby order. On success, invoices are created for payment; use &#x60;/billing/invoices/{id}&#x60; or &#x60;/pay/{method}/{invoices}&#x60; to complete payment.
   *
   * @return void
   */
  def addMail() = {
    val await = Try(Await.result(addMailAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Place Mail Order asynchronously
   * Places a Mail Baby order. On success, invoices are created for payment; use &#x60;/billing/invoices/{id}&#x60; or &#x60;/pay/{method}/{invoices}&#x60; to complete payment.
   *
   * @return Future(void)
   */
  def addMailAsync() = {
      helper.addMail()
  }

  /**
   * Create Deny Rule
   * Adds a new deny rule to automatically block emails that match the specified criteria.
   *
   * @param body These are the fields needed to create a new email deny rule. 
   * @param user  
   * @param &#x60;type&#x60;  
   * @param data  
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return GenericResponse
   */
  def addRule(body: DenyRuleNew, user: String, &#x60;type&#x60;: String, data: String, id: Integer): Option[GenericResponse] = {
    val await = Try(Await.result(addRuleAsync(body, user, &#x60;type&#x60;, data, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Create Deny Rule asynchronously
   * Adds a new deny rule to automatically block emails that match the specified criteria.
   *
   * @param body These are the fields needed to create a new email deny rule. 
   * @param user  
   * @param &#x60;type&#x60;  
   * @param data  
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return Future(GenericResponse)
   */
  def addRuleAsync(body: DenyRuleNew, user: String, &#x60;type&#x60;: String, data: String, id: Integer): Future[GenericResponse] = {
      helper.addRule(body, user, &#x60;type&#x60;, data, id)
  }

  /**
   * Create Mail Alert
   * Creates a new alert for the mail service, such as delivery or quota notifications.
   *
   * @param body  
   * @param &#x60;type&#x60;  
   * @param value  
   * @param to  
   * @param enabled  
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return SuccessTextResponse
   */
  def createMailAlert(body: MailAlertRequest, &#x60;type&#x60;: String, value: String, to: String, enabled: String, id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(createMailAlertAsync(body, &#x60;type&#x60;, value, to, enabled, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Create Mail Alert asynchronously
   * Creates a new alert for the mail service, such as delivery or quota notifications.
   *
   * @param body  
   * @param &#x60;type&#x60;  
   * @param value  
   * @param to  
   * @param enabled  
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return Future(SuccessTextResponse)
   */
  def createMailAlertAsync(body: MailAlertRequest, &#x60;type&#x60;: String, value: String, to: String, enabled: String, id: Integer): Future[SuccessTextResponse] = {
      helper.createMailAlert(body, &#x60;type&#x60;, value, to, enabled, id)
  }

  /**
   * Delete Mail Alert
   * Deletes an existing alert definition for the mail service.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @param alert_id Alert ID to delete. 
   * @return SuccessTextResponse
   */
  def deleteMailAlert(id: Integer, alert_id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(deleteMailAlertAsync(id, alert_id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Delete Mail Alert asynchronously
   * Deletes an existing alert definition for the mail service.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @param alert_id Alert ID to delete. 
   * @return Future(SuccessTextResponse)
   */
  def deleteMailAlertAsync(id: Integer, alert_id: Integer): Future[SuccessTextResponse] = {
      helper.deleteMailAlert(id, alert_id)
  }

  /**
   * Delete Deny Rule
   * Removes a deny rule from the mail service.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @param rule The ID of the Rules entry. 
   * @return GenericResponse
   */
  def deleteRule(id: Integer, rule: String): Option[GenericResponse] = {
    val await = Try(Await.result(deleteRuleAsync(id, rule), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Delete Deny Rule asynchronously
   * Removes a deny rule from the mail service.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @param rule The ID of the Rules entry. 
   * @return Future(GenericResponse)
   */
  def deleteRuleAsync(id: Integer, rule: String): Future[GenericResponse] = {
      helper.deleteRule(id, rule)
  }

  /**
   * Remove Email Address from Block List
   * Removes an email address from the mail service&#x27;s block lists.
   *
   * @param email  
   * @param body  
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return GenericResponse
   */
  def delistBlock(email: String, body: EmailAddress, id: Integer): Option[GenericResponse] = {
    val await = Try(Await.result(delistBlockAsync(email, body, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Remove Email Address from Block List asynchronously
   * Removes an email address from the mail service&#x27;s block lists.
   *
   * @param email  
   * @param body  
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return Future(GenericResponse)
   */
  def delistBlockAsync(email: String, body: EmailAddress, id: Integer): Future[GenericResponse] = {
      helper.delistBlock(email, body, id)
  }

  /**
   * List Mail Alerts
   * Returns the alert configuration for the mail service. Use the alert IDs from this response with PUT or DELETE to update or remove alerts.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return MailAlertsResponse
   */
  def getMailAlerts(id: Integer): Option[MailAlertsResponse] = {
    val await = Try(Await.result(getMailAlertsAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List Mail Alerts asynchronously
   * Returns the alert configuration for the mail service. Use the alert IDs from this response with PUT or DELETE to update or remove alerts.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return Future(MailAlertsResponse)
   */
  def getMailAlertsAsync(id: Integer): Future[MailAlertsResponse] = {
      helper.getMailAlerts(id)
  }

  /**
   * List Blocked Email Addresses
   * Displays a listing of the blocked email addresses
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return MailBlocks
   */
  def getMailBlocks(id: Integer): Option[MailBlocks] = {
    val await = Try(Await.result(getMailBlocksAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List Blocked Email Addresses asynchronously
   * Displays a listing of the blocked email addresses
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return Future(MailBlocks)
   */
  def getMailBlocksAsync(id: Integer): Future[MailBlocks] = {
      helper.getMailBlocks(id)
  }

  /**
   * Get Delist Status
   * Returns the current blocklist and delisting information for the mail service, including recent local and trap blocks.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return MailDelistResponse
   */
  def getMailDelist(id: Integer): Option[MailDelistResponse] = {
    val await = Try(Await.result(getMailDelistAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Delist Status asynchronously
   * Returns the current blocklist and delisting information for the mail service, including recent local and trap blocks.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return Future(MailDelistResponse)
   */
  def getMailDelistAsync(id: Integer): Future[MailDelistResponse] = {
      helper.getMailDelist(id)
  }

  /**
   * Get Deliverability Metrics
   * Returns deliverability statistics such as delivered vs. bounced counts and percentages. Use query filters to pivot the response by domain or sender.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return MailDeliverabilityResponse
   */
  def getMailDeliverability(id: Integer): Option[MailDeliverabilityResponse] = {
    val await = Try(Await.result(getMailDeliverabilityAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Deliverability Metrics asynchronously
   * Returns deliverability statistics such as delivered vs. bounced counts and percentages. Use query filters to pivot the response by domain or sender.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return Future(MailDeliverabilityResponse)
   */
  def getMailDeliverabilityAsync(id: Integer): Future[MailDeliverabilityResponse] = {
      helper.getMailDeliverability(id)
  }

  /**
   * Get Mail Order
   * Returns detailed information for the mail service, including credentials and service metadata required to configure your sending client.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return MailSchema
   */
  def getMailInfo(id: Integer): Option[MailSchema] = {
    val await = Try(Await.result(getMailInfoAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Mail Order asynchronously
   * Returns detailed information for the mail service, including credentials and service metadata required to configure your sending client.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return Future(MailSchema)
   */
  def getMailInfoAsync(id: Integer): Future[MailSchema] = {
      helper.getMailInfo(id)
  }

  /**
   * Get Mail Invoices
   * Retrieves invoices associated with the mail service. Use these invoices to validate billing status or initiate payment.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return ChargeInvoiceRows
   */
  def getMailInvoices(id: Integer): Option[ChargeInvoiceRows] = {
    val await = Try(Await.result(getMailInvoicesAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Mail Invoices asynchronously
   * Retrieves invoices associated with the mail service. Use these invoices to validate billing status or initiate payment.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return Future(ChargeInvoiceRows)
   */
  def getMailInvoicesAsync(id: Integer): Future[ChargeInvoiceRows] = {
      helper.getMailInvoices(id)
  }

  /**
   * List Mail Orders
   * Returns the Mail Baby services on your account. Use the &#x60;mail_id&#x60; from this list with &#x60;/mail/{id}&#x60; to retrieve service details, and with &#x60;/mail/{id}/stats&#x60; or &#x60;/mail/{id}/log&#x60; to review delivery statistics.
   *
   * @return List[MailRow]
   */
  def getMailList(): Option[List[MailRow]] = {
    val await = Try(Await.result(getMailListAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List Mail Orders asynchronously
   * Returns the Mail Baby services on your account. Use the &#x60;mail_id&#x60; from this list with &#x60;/mail/{id}&#x60; to retrieve service details, and with &#x60;/mail/{id}/stats&#x60; or &#x60;/mail/{id}/log&#x60; to review delivery statistics.
   *
   * @return Future(List[MailRow])
   */
  def getMailListAsync(): Future[List[MailRow]] = {
      helper.getMailList()
  }

  /**
   * Resend Mail Welcome Email
   * Resends the welcome email for the Mail Baby service. The email contains SMTP credentials and configuration instructions.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return SuccessTextResponse
   */
  def getMailWelcomeEmail(id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(getMailWelcomeEmailAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Resend Mail Welcome Email asynchronously
   * Resends the welcome email for the Mail Baby service. The email contains SMTP credentials and configuration instructions.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return Future(SuccessTextResponse)
   */
  def getMailWelcomeEmailAsync(id: Integer): Future[SuccessTextResponse] = {
      helper.getMailWelcomeEmail(id)
  }

  /**
   * Get Mail Ordering Information
   * Returns available Mail Baby plans and ordering metadata. Use the service type IDs from this response when validating or placing a new mail order.
   *
   * @return MailOrder
   */
  def getNewMail(): Option[MailOrder] = {
    val await = Try(Await.result(getNewMailAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Mail Ordering Information asynchronously
   * Returns available Mail Baby plans and ordering metadata. Use the service type IDs from this response when validating or placing a new mail order.
   *
   * @return Future(MailOrder)
   */
  def getNewMailAsync(): Future[MailOrder] = {
      helper.getNewMail()
  }

  /**
   * List Deny Rules
   * Returns a listing of all the deny block rules configured for this mail service.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return List[DenyRuleRecord]
   */
  def getRules(id: Integer): Option[List[DenyRuleRecord]] = {
    val await = Try(Await.result(getRulesAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List Deny Rules asynchronously
   * Returns a listing of all the deny block rules configured for this mail service.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return Future(List[DenyRuleRecord])
   */
  def getRulesAsync(id: Integer): Future[List[DenyRuleRecord]] = {
      helper.getRules(id)
  }

  /**
   * Get Mail Usage Statistics
   * Returns usage statistics for the mail service over the requested time period, including send counts, delivery rates, and quota consumption.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @param time The timeframe for the statistics. (optional)
   * @return MailStatsType
   */
  def getStats(id: Integer, time: Option[String] = None): Option[MailStatsType] = {
    val await = Try(Await.result(getStatsAsync(id, time), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Mail Usage Statistics asynchronously
   * Returns usage statistics for the mail service over the requested time period, including send counts, delivery rates, and quota consumption.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @param time The timeframe for the statistics. (optional)
   * @return Future(MailStatsType)
   */
  def getStatsAsync(id: Integer, time: Option[String] = None): Future[MailStatsType] = {
      helper.getStats(id, time)
  }

  /**
   * Cancel Mail
   * Cancels a Mail Baby service. After cancellation the mail credentials are deactivated and the service transitions to a canceled status. No further billing charges will be incurred.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return inline_response_200_8
   */
  def mailCancel(id: Integer): Option[inline_response_200_8] = {
    val await = Try(Await.result(mailCancelAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Cancel Mail asynchronously
   * Cancels a Mail Baby service. After cancellation the mail credentials are deactivated and the service transitions to a canceled status. No further billing charges will be incurred.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return Future(inline_response_200_8)
   */
  def mailCancelAsync(id: Integer): Future[inline_response_200_8] = {
      helper.mailCancel(id)
  }

  /**
   * Delist a Blocked Sender
   * Removes an email address from blocklists for the mail service. Provide the &#x60;unblock&#x60; email address from the delist status response.
   *
   * @param body  
   * @param unblock  
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return SuccessTextResponse
   */
  def postMailDelist(body: MailDelistRequest, unblock: String, id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(postMailDelistAsync(body, unblock, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Delist a Blocked Sender asynchronously
   * Removes an email address from blocklists for the mail service. Provide the &#x60;unblock&#x60; email address from the delist status response.
   *
   * @param body  
   * @param unblock  
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return Future(SuccessTextResponse)
   */
  def postMailDelistAsync(body: MailDelistRequest, unblock: String, id: Integer): Future[SuccessTextResponse] = {
      helper.postMailDelist(body, unblock, id)
  }

  /**
   * Validate Mail Order
   * Validates a Mail Baby order and returns pricing or errors. Use this before placing the final order.
   *
   * @return void
   */
  def putMail() = {
    val await = Try(Await.result(putMailAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Validate Mail Order asynchronously
   * Validates a Mail Baby order and returns pricing or errors. Use this before placing the final order.
   *
   * @return Future(void)
   */
  def putMailAsync() = {
      helper.putMail()
  }

  /**
   * Reset Mail Password
   * Resets the Mail Baby service password and emails the new password to the account owner. Use &#x60;/mail/{id}&#x60; to retrieve updated credential data after the reset.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return SuccessTextResponse
   */
  def resetMailPassword(id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(resetMailPasswordAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Reset Mail Password asynchronously
   * Resets the Mail Baby service password and emails the new password to the account owner. Use &#x60;/mail/{id}&#x60; to retrieve updated credential data after the reset.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return Future(SuccessTextResponse)
   */
  def resetMailPasswordAsync(id: Integer): Future[SuccessTextResponse] = {
      helper.resetMailPassword(id)
  }

  /**
   * Send Email with Advanced Options
   * Sends an email through one of your mail orders with support for file attachments, CC, BCC, and other advanced options. For simple single-recipient sends, use &#x60;POST /mail/{id}/send&#x60;.
   *
   * @param body  
   * @param subject  
   * @param body  
   * @param from  
   * @param to  
   * @param replyto  
   * @param cc  
   * @param bcc  
   * @param attachments  
   * @param id  
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return GenericResponse
   */
  def sendAdvMail(body: SendMailAdv, subject: String, body: String, from: EmailAddressName, to: List[EmailAddressName], replyto: List[EmailAddressName], cc: List[EmailAddressName], bcc: List[EmailAddressName], attachments: List[MailAttachment], id: Long, id: Integer): Option[GenericResponse] = {
    val await = Try(Await.result(sendAdvMailAsync(body, subject, body, from, to, replyto, cc, bcc, attachments, id, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Send Email with Advanced Options asynchronously
   * Sends an email through one of your mail orders with support for file attachments, CC, BCC, and other advanced options. For simple single-recipient sends, use &#x60;POST /mail/{id}/send&#x60;.
   *
   * @param body  
   * @param subject  
   * @param body  
   * @param from  
   * @param to  
   * @param replyto  
   * @param cc  
   * @param bcc  
   * @param attachments  
   * @param id  
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return Future(GenericResponse)
   */
  def sendAdvMailAsync(body: SendMailAdv, subject: String, body: String, from: EmailAddressName, to: List[EmailAddressName], replyto: List[EmailAddressName], cc: List[EmailAddressName], bcc: List[EmailAddressName], attachments: List[MailAttachment], id: Long, id: Integer): Future[GenericResponse] = {
      helper.sendAdvMail(body, subject, body, from, to, replyto, cc, bcc, attachments, id, id)
  }

  /**
   * Send Email
   * Sends an email through one of your mail orders. For multiple recipients or file attachments, use &#x60;POST /mail/{id}/advsend&#x60; instead.
   *
   * @param body  
   * @param to  
   * @param from  
   * @param subject  
   * @param body  
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return GenericResponse
   */
  def sendMail(body: SendMail, to: String, from: String, subject: String, body: String, id: Integer): Option[GenericResponse] = {
    val await = Try(Await.result(sendMailAsync(body, to, from, subject, body, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Send Email asynchronously
   * Sends an email through one of your mail orders. For multiple recipients or file attachments, use &#x60;POST /mail/{id}/advsend&#x60; instead.
   *
   * @param body  
   * @param to  
   * @param from  
   * @param subject  
   * @param body  
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return Future(GenericResponse)
   */
  def sendMailAsync(body: SendMail, to: String, from: String, subject: String, body: String, id: Integer): Future[GenericResponse] = {
      helper.sendMail(body, to, from, subject, body, id)
  }

  /**
   * Update Mail Alert
   * Updates an existing alert definition for the mail service. Provide the &#x60;alert_id&#x60; returned by the list response along with updated fields.
   *
   * @param body  
   * @param alert_id  
   * @param &#x60;type&#x60;  
   * @param value  
   * @param to  
   * @param enabled  
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return SuccessTextResponse
   */
  def updateMailAlert(body: MailAlertUpdateRequest, alert_id: Integer, &#x60;type&#x60;: String, value: String, to: String, enabled: String, id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(updateMailAlertAsync(body, alert_id, &#x60;type&#x60;, value, to, enabled, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update Mail Alert asynchronously
   * Updates an existing alert definition for the mail service. Provide the &#x60;alert_id&#x60; returned by the list response along with updated fields.
   *
   * @param body  
   * @param alert_id  
   * @param &#x60;type&#x60;  
   * @param value  
   * @param to  
   * @param enabled  
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return Future(SuccessTextResponse)
   */
  def updateMailAlertAsync(body: MailAlertUpdateRequest, alert_id: Integer, &#x60;type&#x60;: String, value: String, to: String, enabled: String, id: Integer): Future[SuccessTextResponse] = {
      helper.updateMailAlert(body, alert_id, &#x60;type&#x60;, value, to, enabled, id)
  }

  /**
   * Update Mail Order
   * Updates mail service metadata for the order, such as stored settings or account details.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return void
   */
  def updateMailInfo(id: String) = {
    val await = Try(Await.result(updateMailInfoAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update Mail Order asynchronously
   * Updates mail service metadata for the order, such as stored settings or account details.
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @return Future(void)
   */
  def updateMailInfoAsync(id: String) = {
      helper.updateMailInfo(id)
  }

  /**
   * View Mail Log
   * Returns a paginated log of emails sent through this mail service, with optional filtering by sender, recipient, date range, and delivery status.  **Row grouping** is controlled by the &#x60;groupby&#x60; parameter.  By default (&#x60;groupby&#x3D;recipient&#x60;), the response contains one row per delivery attempt — so a single message sent to 4 recipients produces 4 rows, each with its own &#x60;recipient&#x60;, &#x60;delivered&#x60;, &#x60;response&#x60;, and &#x60;mxHostname&#x60; values.  Set &#x60;groupby&#x3D;message&#x60; to collapse to one row per message (delivery fields will reflect one arbitrary recipient).  **Pagination** is controlled by &#x60;skip&#x60; and &#x60;limit&#x60;.  The &#x60;total&#x60; in the response reflects the row count **after** grouping, so it matches the number of pages you need to fetch.  **Date filtering** accepts either a Unix timestamp (integer) or a date string parseable by PHP &#x60;strtotime()&#x60; such as &#x60;2024-01-15&#x60;, &#x60;last monday&#x60;, or &#x60;2024-01-01 00:00:00&#x60;.  Examples: &#x60;startDate&#x3D;1704067200&amp;endDate&#x3D;1706745599&#x60; or &#x60;startDate&#x3D;2024-01-01&amp;endDate&#x3D;2024-01-31&#x60;.  **Sorting** is controlled by &#x60;sort&#x60; and &#x60;dir&#x60;.  Currently the only sort key is &#x60;time&#x60; (default), which orders by internal row ID.  **Delivery status** can be filtered with the &#x60;delivered&#x60; parameter: &#x60;delivered&#x3D;1&#x60; returns only successfully delivered messages; &#x60;delivered&#x3D;0&#x60; returns messages still in queue or that failed.  **Address filtering** distinguishes between the SMTP envelope address (&#x60;from&#x60;, &#x60;to&#x60;) and message headers (&#x60;headerfrom&#x60; for the &#x60;From:&#x60; header, &#x60;replyto&#x60; for &#x60;Reply-To:&#x60;). These may differ when a message is sent on behalf of another address.  The &#x60;mailid&#x60; parameter corresponds to the &#x60;id&#x60; field in the returned &#x60;MailLogEntry&#x60; objects, **not** the &#x60;_id&#x60; field.  It also matches the transaction ID returned in the &#x60;text&#x60; field of a successful send response.  The &#x60;messageId&#x60; parameter searches the &#x60;Message-ID&#x60; email header (case-insensitive substring match). 
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @param id The numeric ID of the mail order to filter by.  When omitted, logs from the first active mail order are returned.  Obtain valid IDs from &#x60;GET /mail&#x60; or &#x60;GET /mail/{id}&#x60;. (optional)
   * @param origin Filter by the originating IP address from which the message was submitted to the relay.  Must be a valid IPv4 or IPv6 address. (optional)
   * @param mx Filter by the MX hostname the relay attempted delivery to.  For example &#x60;mx.google.com&#x60; would return messages destined for Gmail recipients. Maps to &#x60;mxHostname&#x60; in the &#x60;MailLogEntry&#x60; response. (optional)
   * @param from Filter by SMTP envelope &#x60;MAIL FROM&#x60; address (exact match).  This is the address the relay used for bounce handling and may differ from the &#x60;From:&#x60; message header.  For header-level filtering use &#x60;headerfrom&#x60;. (optional)
   * @param to Filter by SMTP envelope &#x60;RCPT TO&#x60; address (exact match).  This is the delivery address used by the relay and may differ from the &#x60;To:&#x60; header when BCC recipients are involved. (optional)
   * @param subject Filter by email &#x60;Subject&#x60; header (exact match).  MIME-encoded subjects are decoded automatically in the response. (optional)
   * @param mailid Filter by the relay-assigned mail ID string (exact match).  This corresponds to the &#x60;id&#x60; field in &#x60;MailLogEntry&#x60; and to the &#x60;text&#x60; value returned by the sending endpoints on success.  Format is an 18-19 character hexadecimal string such as &#x60;185997065c60008840&#x60;. (optional)
   * @param messageId Filter by the &#x60;Message-ID&#x60; email header using a substring (case-insensitive) match.  The &#x60;Message-ID&#x60; is assigned by the sending mail client and is visible in the &#x60;messageId&#x60; field of &#x60;MailLogEntry&#x60;. (optional)
   * @param replyto Filter by the &#x60;Reply-To&#x60; message header address (exact match).  Only returns messages where this header was explicitly set. (optional)
   * @param headerfrom Filter by the &#x60;From&#x60; message header address (exact match).  This is the human-visible sender address and may differ from the SMTP envelope &#x60;from&#x60; parameter when sending on behalf of another address. (optional)
   * @param delivered Filter by delivery status.  &#x60;1&#x60; returns only messages that were successfully delivered to the destination MX.  &#x60;0&#x60; returns messages that are still queued, deferred, or failed.  Omit to return all messages regardless of delivery status. (optional)
   * @param skip Number of records to skip for pagination.  Use in combination with &#x60;limit&#x60; to page through large result sets.  Defaults to &#x60;0&#x60; (no skip). (optional, default to 0)
   * @param limit Maximum number of records to return per page.  Defaults to &#x60;100&#x60;. Maximum allowed value is &#x60;10000&#x60;.  The response also includes a &#x60;total&#x60; field with the full matched count so you can calculate the number of pages. (optional, default to 100)
   * @param startDate Earliest date to include.  Accepts either a Unix timestamp (integer seconds since epoch) or a date string parseable by &#x60;strtotime()&#x60; such as &#x60;2024-01-15&#x60; or &#x60;last monday&#x60;.  Messages with a &#x60;time&#x60; value **greater than or equal to** this value will be included. (optional)
   * @param endDate Latest date to include.  Accepts either a Unix timestamp (integer seconds since epoch) or a date string parseable by &#x60;strtotime()&#x60; such as &#x60;2024-01-31&#x60; or &#x60;yesterday&#x60;.  Messages with a &#x60;time&#x60; value **less than or equal to** this value will be included. (optional)
   * @param sort Field to sort results by.  Currently only &#x60;time&#x60; is supported (sorts by internal row ID which corresponds to chronological order). (optional, default to time)
   * @param dir Sort direction.  &#x60;desc&#x60; returns newest first (default), &#x60;asc&#x60; returns oldest first. (optional, default to desc)
   * @param groupby Controls how results are grouped.  &#x60;recipient&#x60; (default) returns one row per delivery attempt — a message sent to 4 recipients produces 4 rows, each with its own &#x60;recipient&#x60;, &#x60;delivered&#x60;, &#x60;response&#x60;, and delivery metadata.  &#x60;message&#x60; collapses to one row per unique message ID; delivery-level fields will reflect one arbitrary recipient per message.  The &#x60;total&#x60; count in the response matches the grouping mode. (optional, default to recipient)
   * @return MailLog
   */
  def viewMailLog(id: Integer, id: Option[Long] = None, origin: Option[String] = None, mx: Option[String] = None, from: Option[String] = None, to: Option[String] = None, subject: Option[String] = None, mailid: Option[String] = None, messageId: Option[String] = None, replyto: Option[String] = None, headerfrom: Option[String] = None, delivered: Option[Integer] = None, skip: Option[Integer] = Option(0), limit: Option[Integer] = Option(100), startDate: Option[startDate] = None, endDate: Option[endDate] = None, sort: Option[String] = Option("time"), dir: Option[String] = Option("desc"), groupby: Option[String] = Option("recipient")): Option[MailLog] = {
    val await = Try(Await.result(viewMailLogAsync(id, id, origin, mx, from, to, subject, mailid, messageId, replyto, headerfrom, delivered, skip, limit, startDate, endDate, sort, dir, groupby), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * View Mail Log asynchronously
   * Returns a paginated log of emails sent through this mail service, with optional filtering by sender, recipient, date range, and delivery status.  **Row grouping** is controlled by the &#x60;groupby&#x60; parameter.  By default (&#x60;groupby&#x3D;recipient&#x60;), the response contains one row per delivery attempt — so a single message sent to 4 recipients produces 4 rows, each with its own &#x60;recipient&#x60;, &#x60;delivered&#x60;, &#x60;response&#x60;, and &#x60;mxHostname&#x60; values.  Set &#x60;groupby&#x3D;message&#x60; to collapse to one row per message (delivery fields will reflect one arbitrary recipient).  **Pagination** is controlled by &#x60;skip&#x60; and &#x60;limit&#x60;.  The &#x60;total&#x60; in the response reflects the row count **after** grouping, so it matches the number of pages you need to fetch.  **Date filtering** accepts either a Unix timestamp (integer) or a date string parseable by PHP &#x60;strtotime()&#x60; such as &#x60;2024-01-15&#x60;, &#x60;last monday&#x60;, or &#x60;2024-01-01 00:00:00&#x60;.  Examples: &#x60;startDate&#x3D;1704067200&amp;endDate&#x3D;1706745599&#x60; or &#x60;startDate&#x3D;2024-01-01&amp;endDate&#x3D;2024-01-31&#x60;.  **Sorting** is controlled by &#x60;sort&#x60; and &#x60;dir&#x60;.  Currently the only sort key is &#x60;time&#x60; (default), which orders by internal row ID.  **Delivery status** can be filtered with the &#x60;delivered&#x60; parameter: &#x60;delivered&#x3D;1&#x60; returns only successfully delivered messages; &#x60;delivered&#x3D;0&#x60; returns messages still in queue or that failed.  **Address filtering** distinguishes between the SMTP envelope address (&#x60;from&#x60;, &#x60;to&#x60;) and message headers (&#x60;headerfrom&#x60; for the &#x60;From:&#x60; header, &#x60;replyto&#x60; for &#x60;Reply-To:&#x60;). These may differ when a message is sent on behalf of another address.  The &#x60;mailid&#x60; parameter corresponds to the &#x60;id&#x60; field in the returned &#x60;MailLogEntry&#x60; objects, **not** the &#x60;_id&#x60; field.  It also matches the transaction ID returned in the &#x60;text&#x60; field of a successful send response.  The &#x60;messageId&#x60; parameter searches the &#x60;Message-ID&#x60; email header (case-insensitive substring match). 
   *
   * @param id The mail service ID. Use &#x60;mail_id&#x60; from &#x60;GET /mail&#x60;. 
   * @param id The numeric ID of the mail order to filter by.  When omitted, logs from the first active mail order are returned.  Obtain valid IDs from &#x60;GET /mail&#x60; or &#x60;GET /mail/{id}&#x60;. (optional)
   * @param origin Filter by the originating IP address from which the message was submitted to the relay.  Must be a valid IPv4 or IPv6 address. (optional)
   * @param mx Filter by the MX hostname the relay attempted delivery to.  For example &#x60;mx.google.com&#x60; would return messages destined for Gmail recipients. Maps to &#x60;mxHostname&#x60; in the &#x60;MailLogEntry&#x60; response. (optional)
   * @param from Filter by SMTP envelope &#x60;MAIL FROM&#x60; address (exact match).  This is the address the relay used for bounce handling and may differ from the &#x60;From:&#x60; message header.  For header-level filtering use &#x60;headerfrom&#x60;. (optional)
   * @param to Filter by SMTP envelope &#x60;RCPT TO&#x60; address (exact match).  This is the delivery address used by the relay and may differ from the &#x60;To:&#x60; header when BCC recipients are involved. (optional)
   * @param subject Filter by email &#x60;Subject&#x60; header (exact match).  MIME-encoded subjects are decoded automatically in the response. (optional)
   * @param mailid Filter by the relay-assigned mail ID string (exact match).  This corresponds to the &#x60;id&#x60; field in &#x60;MailLogEntry&#x60; and to the &#x60;text&#x60; value returned by the sending endpoints on success.  Format is an 18-19 character hexadecimal string such as &#x60;185997065c60008840&#x60;. (optional)
   * @param messageId Filter by the &#x60;Message-ID&#x60; email header using a substring (case-insensitive) match.  The &#x60;Message-ID&#x60; is assigned by the sending mail client and is visible in the &#x60;messageId&#x60; field of &#x60;MailLogEntry&#x60;. (optional)
   * @param replyto Filter by the &#x60;Reply-To&#x60; message header address (exact match).  Only returns messages where this header was explicitly set. (optional)
   * @param headerfrom Filter by the &#x60;From&#x60; message header address (exact match).  This is the human-visible sender address and may differ from the SMTP envelope &#x60;from&#x60; parameter when sending on behalf of another address. (optional)
   * @param delivered Filter by delivery status.  &#x60;1&#x60; returns only messages that were successfully delivered to the destination MX.  &#x60;0&#x60; returns messages that are still queued, deferred, or failed.  Omit to return all messages regardless of delivery status. (optional)
   * @param skip Number of records to skip for pagination.  Use in combination with &#x60;limit&#x60; to page through large result sets.  Defaults to &#x60;0&#x60; (no skip). (optional, default to 0)
   * @param limit Maximum number of records to return per page.  Defaults to &#x60;100&#x60;. Maximum allowed value is &#x60;10000&#x60;.  The response also includes a &#x60;total&#x60; field with the full matched count so you can calculate the number of pages. (optional, default to 100)
   * @param startDate Earliest date to include.  Accepts either a Unix timestamp (integer seconds since epoch) or a date string parseable by &#x60;strtotime()&#x60; such as &#x60;2024-01-15&#x60; or &#x60;last monday&#x60;.  Messages with a &#x60;time&#x60; value **greater than or equal to** this value will be included. (optional)
   * @param endDate Latest date to include.  Accepts either a Unix timestamp (integer seconds since epoch) or a date string parseable by &#x60;strtotime()&#x60; such as &#x60;2024-01-31&#x60; or &#x60;yesterday&#x60;.  Messages with a &#x60;time&#x60; value **less than or equal to** this value will be included. (optional)
   * @param sort Field to sort results by.  Currently only &#x60;time&#x60; is supported (sorts by internal row ID which corresponds to chronological order). (optional, default to time)
   * @param dir Sort direction.  &#x60;desc&#x60; returns newest first (default), &#x60;asc&#x60; returns oldest first. (optional, default to desc)
   * @param groupby Controls how results are grouped.  &#x60;recipient&#x60; (default) returns one row per delivery attempt — a message sent to 4 recipients produces 4 rows, each with its own &#x60;recipient&#x60;, &#x60;delivered&#x60;, &#x60;response&#x60;, and delivery metadata.  &#x60;message&#x60; collapses to one row per unique message ID; delivery-level fields will reflect one arbitrary recipient per message.  The &#x60;total&#x60; count in the response matches the grouping mode. (optional, default to recipient)
   * @return Future(MailLog)
   */
  def viewMailLogAsync(id: Integer, id: Option[Long] = None, origin: Option[String] = None, mx: Option[String] = None, from: Option[String] = None, to: Option[String] = None, subject: Option[String] = None, mailid: Option[String] = None, messageId: Option[String] = None, replyto: Option[String] = None, headerfrom: Option[String] = None, delivered: Option[Integer] = None, skip: Option[Integer] = Option(0), limit: Option[Integer] = Option(100), startDate: Option[startDate] = None, endDate: Option[endDate] = None, sort: Option[String] = Option("time"), dir: Option[String] = Option("desc"), groupby: Option[String] = Option("recipient")): Future[MailLog] = {
      helper.viewMailLog(id, id, origin, mx, from, to, subject, mailid, messageId, replyto, headerfrom, delivered, skip, limit, startDate, endDate, sort, dir, groupby)
  }

}

class MailApiAsyncHelper(client: TransportClient, config: SwaggerConfig) extends ApiClient(client, config) {

  def addMail()(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/mail/order"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def addRule(body: DenyRuleNew,
    user: String,
    &#x60;type&#x60;: String,
    data: String,
    id: Integer)(implicit reader: ClientResponseReader[GenericResponse], writer: RequestWriter[DenyRuleNew]): Future[GenericResponse] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}/rules")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling MailApi->addRule")
    if (user == null) throw new Exception("Missing required parameter 'user' when calling MailApi->addRule")

    if (&#x60;type&#x60; == null) throw new Exception("Missing required parameter '&#x60;type&#x60;' when calling MailApi->addRule")

    if (data == null) throw new Exception("Missing required parameter 'data' when calling MailApi->addRule")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def createMailAlert(body: MailAlertRequest,
    &#x60;type&#x60;: String,
    value: String,
    to: String,
    enabled: String,
    id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse], writer: RequestWriter[MailAlertRequest]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}/alerts")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling MailApi->createMailAlert")
    if (&#x60;type&#x60; == null) throw new Exception("Missing required parameter '&#x60;type&#x60;' when calling MailApi->createMailAlert")

    if (value == null) throw new Exception("Missing required parameter 'value' when calling MailApi->createMailAlert")

    if (to == null) throw new Exception("Missing required parameter 'to' when calling MailApi->createMailAlert")

    if (enabled == null) throw new Exception("Missing required parameter 'enabled' when calling MailApi->createMailAlert")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def deleteMailAlert(id: Integer,
    alert_id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}/alerts")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    queryParams += "alert_id" -> alert_id.toString

    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def deleteRule(id: Integer,
    rule: String)(implicit reader: ClientResponseReader[GenericResponse]): Future[GenericResponse] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}/rules/{rule}")
      replaceAll("\\{" + "id" + "\\}", id.toString)
      replaceAll("\\{" + "rule" + "\\}", rule.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (rule == null) throw new Exception("Missing required parameter 'rule' when calling MailApi->deleteRule")


    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def delistBlock(email: String,
    body: EmailAddress,
    id: Integer)(implicit reader: ClientResponseReader[GenericResponse], writer: RequestWriter[EmailAddress]): Future[GenericResponse] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}/blocks/delete")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (email == null) throw new Exception("Missing required parameter 'email' when calling MailApi->delistBlock")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling MailApi->delistBlock")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getMailAlerts(id: Integer)(implicit reader: ClientResponseReader[MailAlertsResponse]): Future[MailAlertsResponse] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}/alerts")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getMailBlocks(id: Integer)(implicit reader: ClientResponseReader[MailBlocks]): Future[MailBlocks] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}/blocks")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getMailDelist(id: Integer)(implicit reader: ClientResponseReader[MailDelistResponse]): Future[MailDelistResponse] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}/delist")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getMailDeliverability(id: Integer)(implicit reader: ClientResponseReader[MailDeliverabilityResponse]): Future[MailDeliverabilityResponse] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}/deliverability")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getMailInfo(id: Integer)(implicit reader: ClientResponseReader[MailSchema]): Future[MailSchema] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getMailInvoices(id: Integer)(implicit reader: ClientResponseReader[ChargeInvoiceRows]): Future[ChargeInvoiceRows] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}/invoices")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getMailList()(implicit reader: ClientResponseReader[List[MailRow]]): Future[List[MailRow]] = {
    // create path and map variables
    val path = (addFmt("/mail"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getMailWelcomeEmail(id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}/welcome_email")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getNewMail()(implicit reader: ClientResponseReader[MailOrder]): Future[MailOrder] = {
    // create path and map variables
    val path = (addFmt("/mail/order"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getRules(id: Integer)(implicit reader: ClientResponseReader[List[DenyRuleRecord]]): Future[List[DenyRuleRecord]] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}/rules")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getStats(id: Integer,
    time: Option[String] = None
    )(implicit reader: ClientResponseReader[MailStatsType]): Future[MailStatsType] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}/stats")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    time match {
      case Some(param) => queryParams += "time" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def mailCancel(id: Integer)(implicit reader: ClientResponseReader[inline_response_200_8]): Future[inline_response_200_8] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postMailDelist(body: MailDelistRequest,
    unblock: String,
    id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse], writer: RequestWriter[MailDelistRequest]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}/delist")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling MailApi->postMailDelist")
    if (unblock == null) throw new Exception("Missing required parameter 'unblock' when calling MailApi->postMailDelist")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def putMail()(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/mail/order"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("PUT", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def resetMailPassword(id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}/reset_password")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def sendAdvMail(body: SendMailAdv,
    subject: String,
    body: String,
    from: EmailAddressName,
    to: List[EmailAddressName],
    replyto: List[EmailAddressName],
    cc: List[EmailAddressName],
    bcc: List[EmailAddressName],
    attachments: List[MailAttachment],
    id: Long,
    id: Integer)(implicit reader: ClientResponseReader[GenericResponse], writer: RequestWriter[SendMailAdv]): Future[GenericResponse] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}/advsend")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling MailApi->sendAdvMail")
    if (subject == null) throw new Exception("Missing required parameter 'subject' when calling MailApi->sendAdvMail")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling MailApi->sendAdvMail")

    if (from == null) throw new Exception("Missing required parameter 'from' when calling MailApi->sendAdvMail")
    if (to == null) throw new Exception("Missing required parameter 'to' when calling MailApi->sendAdvMail")
    if (replyto == null) throw new Exception("Missing required parameter 'replyto' when calling MailApi->sendAdvMail")
    if (cc == null) throw new Exception("Missing required parameter 'cc' when calling MailApi->sendAdvMail")
    if (bcc == null) throw new Exception("Missing required parameter 'bcc' when calling MailApi->sendAdvMail")
    if (attachments == null) throw new Exception("Missing required parameter 'attachments' when calling MailApi->sendAdvMail")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def sendMail(body: SendMail,
    to: String,
    from: String,
    subject: String,
    body: String,
    id: Integer)(implicit reader: ClientResponseReader[GenericResponse], writer: RequestWriter[SendMail]): Future[GenericResponse] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}/send")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling MailApi->sendMail")
    if (to == null) throw new Exception("Missing required parameter 'to' when calling MailApi->sendMail")

    if (from == null) throw new Exception("Missing required parameter 'from' when calling MailApi->sendMail")

    if (subject == null) throw new Exception("Missing required parameter 'subject' when calling MailApi->sendMail")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling MailApi->sendMail")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateMailAlert(body: MailAlertUpdateRequest,
    alert_id: Integer,
    &#x60;type&#x60;: String,
    value: String,
    to: String,
    enabled: String,
    id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse], writer: RequestWriter[MailAlertUpdateRequest]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}/alerts")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling MailApi->updateMailAlert")
    if (&#x60;type&#x60; == null) throw new Exception("Missing required parameter '&#x60;type&#x60;' when calling MailApi->updateMailAlert")

    if (value == null) throw new Exception("Missing required parameter 'value' when calling MailApi->updateMailAlert")

    if (to == null) throw new Exception("Missing required parameter 'to' when calling MailApi->updateMailAlert")

    if (enabled == null) throw new Exception("Missing required parameter 'enabled' when calling MailApi->updateMailAlert")


    val resFuture = client.submit("PUT", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateMailInfo(id: String)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (id == null) throw new Exception("Missing required parameter 'id' when calling MailApi->updateMailInfo")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def viewMailLog(id: Integer,
    id: Option[Long] = None,
    origin: Option[String] = None,
    mx: Option[String] = None,
    from: Option[String] = None,
    to: Option[String] = None,
    subject: Option[String] = None,
    mailid: Option[String] = None,
    messageId: Option[String] = None,
    replyto: Option[String] = None,
    headerfrom: Option[String] = None,
    delivered: Option[Integer] = None,
    skip: Option[Integer] = Option(0),
    limit: Option[Integer] = Option(100),
    startDate: Option[startDate] = None,
    endDate: Option[endDate] = None,
    sort: Option[String] = Option("time"),
    dir: Option[String] = Option("desc"),
    groupby: Option[String] = Option("recipient")
    )(implicit reader: ClientResponseReader[MailLog]): Future[MailLog] = {
    // create path and map variables
    val path = (addFmt("/mail/{id}/log")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    id match {
      case Some(param) => queryParams += "id" -> param.toString
      case _ => queryParams
    }
    origin match {
      case Some(param) => queryParams += "origin" -> param.toString
      case _ => queryParams
    }
    mx match {
      case Some(param) => queryParams += "mx" -> param.toString
      case _ => queryParams
    }
    from match {
      case Some(param) => queryParams += "from" -> param.toString
      case _ => queryParams
    }
    to match {
      case Some(param) => queryParams += "to" -> param.toString
      case _ => queryParams
    }
    subject match {
      case Some(param) => queryParams += "subject" -> param.toString
      case _ => queryParams
    }
    mailid match {
      case Some(param) => queryParams += "mailid" -> param.toString
      case _ => queryParams
    }
    messageId match {
      case Some(param) => queryParams += "messageId" -> param.toString
      case _ => queryParams
    }
    replyto match {
      case Some(param) => queryParams += "replyto" -> param.toString
      case _ => queryParams
    }
    headerfrom match {
      case Some(param) => queryParams += "headerfrom" -> param.toString
      case _ => queryParams
    }
    delivered match {
      case Some(param) => queryParams += "delivered" -> param.toString
      case _ => queryParams
    }
    skip match {
      case Some(param) => queryParams += "skip" -> param.toString
      case _ => queryParams
    }
    limit match {
      case Some(param) => queryParams += "limit" -> param.toString
      case _ => queryParams
    }
    startDate match {
      case Some(param) => queryParams += "startDate" -> param.toString
      case _ => queryParams
    }
    endDate match {
      case Some(param) => queryParams += "endDate" -> param.toString
      case _ => queryParams
    }
    sort match {
      case Some(param) => queryParams += "sort" -> param.toString
      case _ => queryParams
    }
    dir match {
      case Some(param) => queryParams += "dir" -> param.toString
      case _ => queryParams
    }
    groupby match {
      case Some(param) => queryParams += "groupby" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }


}
