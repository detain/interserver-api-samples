/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * OpenAPI spec version: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */
package io.swagger.client.api

import java.text.SimpleDateFormat

import io.swagger.client.model.AffiliateBannerRow
import io.swagger.client.model.AffiliateDockSetup
import io.swagger.client.model.AffiliatePaymentSetup
import io.swagger.client.model.AffiliateTrafficRow
import java.math.BigDecimal
import io.swagger.client.model.BillingAddCcRequest
import io.swagger.client.model.BillingInvoiceDetail
import io.swagger.client.model.BillingInvoiceList
import io.swagger.client.model.BillingPaymentMethodRequest
import io.swagger.client.model.BillingPrepayRequest
import io.swagger.client.model.BillingVerifyCcRequest
import io.swagger.client.model.Invoice
import io.swagger.client.model.MonthlyCounts
import io.swagger.client.model.StatusMonthlyBreakdown
import io.swagger.client.model.SuccessTextResponse
import io.swagger.client.model.TextResponse
import io.swagger.client.model.inline_response_200_9
import io.swagger.client.model.inline_response_401
import io.swagger.client.{ApiInvoker, ApiException}

import com.sun.jersey.multipart.FormDataMultiPart
import com.sun.jersey.multipart.file.FileDataBodyPart

import javax.ws.rs.core.MediaType

import java.io.File
import java.util.Date
import java.util.TimeZone

import scala.collection.mutable.HashMap

import com.wordnik.swagger.client._
import scala.concurrent.Future
import collection.mutable

import java.net.URI

import com.wordnik.swagger.client.ClientResponseReaders.Json4sFormatsReader._
import com.wordnik.swagger.client.RequestWriters.Json4sFormatsWriter._

import scala.concurrent.ExecutionContext.Implicits.global
import scala.concurrent._
import scala.concurrent.duration._
import scala.util.{Failure, Success, Try}

import org.json4s._

class BillingApi(
  val defBasePath: String = "https://my.interserver.net/apiv2",
  defApiInvoker: ApiInvoker = ApiInvoker
) {
  private lazy val dateTimeFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSSZ")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  private val dateFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  implicit val formats = new org.json4s.DefaultFormats {
    override def dateFormatter = dateTimeFormatter
  }
  implicit val stringReader: ClientResponseReader[String] = ClientResponseReaders.StringReader
  implicit val unitReader: ClientResponseReader[Unit] = ClientResponseReaders.UnitReader
  implicit val jvalueReader: ClientResponseReader[JValue] = ClientResponseReaders.JValueReader
  implicit val jsonReader: ClientResponseReader[Nothing] = JsonFormatsReader
  implicit val stringWriter: RequestWriter[String] = RequestWriters.StringWriter
  implicit val jsonWriter: RequestWriter[Nothing] = JsonFormatsWriter

  var basePath: String = defBasePath
  var apiInvoker: ApiInvoker = defApiInvoker

  def addHeader(key: String, value: String): mutable.HashMap[String, String] = {
    apiInvoker.defaultHeaders += key -> value
  }

  val config: SwaggerConfig = SwaggerConfig.forUrl(new URI(defBasePath))
  val client = new RestClient(config)
  val helper = new BillingApiAsyncHelper(client, config)

  /**
   * Add Credit Card to Account
   * Adds a new credit card to the account for billing. Card details are validated and stored securely. The card may require verification before it can be used as a payment method.
   *
   * @param name  
   * @param address  
   * @param city  
   * @param state  
   * @param country  
   * @param zip  
   * @param cc  
   * @param cc_exp  
   * @param cc_ccv2  
   * @param body  
   * @return SuccessTextResponse
   */
  def addAccountCreditCard(name: String, address: String, city: String, state: String, country: String, zip: String, cc: String, cc_exp: String, cc_ccv2: String, body: BillingAddCcRequest): Option[SuccessTextResponse] = {
    val await = Try(Await.result(addAccountCreditCardAsync(name, address, city, state, country, zip, cc, cc_exp, cc_ccv2, body), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Add Credit Card to Account asynchronously
   * Adds a new credit card to the account for billing. Card details are validated and stored securely. The card may require verification before it can be used as a payment method.
   *
   * @param name  
   * @param address  
   * @param city  
   * @param state  
   * @param country  
   * @param zip  
   * @param cc  
   * @param cc_exp  
   * @param cc_ccv2  
   * @param body  
   * @return Future(SuccessTextResponse)
   */
  def addAccountCreditCardAsync(name: String, address: String, city: String, state: String, country: String, zip: String, cc: String, cc_exp: String, cc_ccv2: String, body: BillingAddCcRequest): Future[SuccessTextResponse] = {
      helper.addAccountCreditCard(name, address, city, state, country, zip, cc, cc_exp, cc_ccv2, body)
  }

  /**
   * Add Credit Card for Billing
   * Adds a new credit card to the account for use as a payment method. If the card requires verification, the response indicates the next step. Complete verification via &#x60;GET /billing/creditcards/{id}/verify&#x60; followed by &#x60;POST /billing/creditcards/{id}/verify&#x60; before the card can be used for payments.
   *
   * @param body  
   * @param name  
   * @param address  
   * @param city  
   * @param state  
   * @param country  
   * @param zip  
   * @param cc  
   * @param cc_exp  
   * @param cc_ccv2  
   * @return SuccessTextResponse
   */
  def addBillingCreditCard(body: BillingAddCcRequest, name: String, address: String, city: String, state: String, country: String, zip: String, cc: String, cc_exp: String, cc_ccv2: String): Option[SuccessTextResponse] = {
    val await = Try(Await.result(addBillingCreditCardAsync(body, name, address, city, state, country, zip, cc, cc_exp, cc_ccv2), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Add Credit Card for Billing asynchronously
   * Adds a new credit card to the account for use as a payment method. If the card requires verification, the response indicates the next step. Complete verification via &#x60;GET /billing/creditcards/{id}/verify&#x60; followed by &#x60;POST /billing/creditcards/{id}/verify&#x60; before the card can be used for payments.
   *
   * @param body  
   * @param name  
   * @param address  
   * @param city  
   * @param state  
   * @param country  
   * @param zip  
   * @param cc  
   * @param cc_exp  
   * @param cc_ccv2  
   * @return Future(SuccessTextResponse)
   */
  def addBillingCreditCardAsync(body: BillingAddCcRequest, name: String, address: String, city: String, state: String, country: String, zip: String, cc: String, cc_exp: String, cc_ccv2: String): Future[SuccessTextResponse] = {
      helper.addBillingCreditCard(body, name, address, city, state, country, zip, cc, cc_exp, cc_ccv2)
  }

  /**
   * Create Prepay Deposit
   * Creates a new prepay balance deposit and returns the invoice ID that must be paid to activate it. The prepay balance can then be used as a payment method for future orders. Use &#x60;/billing/invoices/{id}&#x60; to retrieve the generated invoice details.
   *
   * @param body  
   * @param module  
   * @param amount  
   * @param automatic_use  
   * @return SuccessTextResponse
   */
  def addBillingPrepay(body: BillingPrepayRequest, module: String, amount: BigDecimal, automatic_use: String): Option[SuccessTextResponse] = {
    val await = Try(Await.result(addBillingPrepayAsync(body, module, amount, automatic_use), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Create Prepay Deposit asynchronously
   * Creates a new prepay balance deposit and returns the invoice ID that must be paid to activate it. The prepay balance can then be used as a payment method for future orders. Use &#x60;/billing/invoices/{id}&#x60; to retrieve the generated invoice details.
   *
   * @param body  
   * @param module  
   * @param amount  
   * @param automatic_use  
   * @return Future(SuccessTextResponse)
   */
  def addBillingPrepayAsync(body: BillingPrepayRequest, module: String, amount: BigDecimal, automatic_use: String): Future[SuccessTextResponse] = {
      helper.addBillingPrepay(body, module, amount, automatic_use)
  }

  /**
   * Remove Credit Card
   * Removes a credit card from the account. If this is the default payment method, select a new default via &#x60;/billing/payment_method&#x60; afterward.
   *
   * @param id The credit card ID. Use the card ID returned from &#x60;POST /account/creditcards&#x60; or listed in &#x60;/billing/creditcards&#x60;. 
   * @return void
   */
  def deleteAccountCreditCard(id: String) = {
    val await = Try(Await.result(deleteAccountCreditCardAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Remove Credit Card asynchronously
   * Removes a credit card from the account. If this is the default payment method, select a new default via &#x60;/billing/payment_method&#x60; afterward.
   *
   * @param id The credit card ID. Use the card ID returned from &#x60;POST /account/creditcards&#x60; or listed in &#x60;/billing/creditcards&#x60;. 
   * @return Future(void)
   */
  def deleteAccountCreditCardAsync(id: String) = {
      helper.deleteAccountCreditCard(id)
  }

  /**
   * Delete Credit Card
   * Removes the selected credit card from the account. Use &#x60;/billing/payment_method&#x60; to select a new default payment method after deleting a card.
   *
   * @param id The credit card ID to remove. Use IDs from &#x60;GET /billing/creditcards&#x60;. 
   * @return SuccessTextResponse
   */
  def deleteBillingCreditCard(id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(deleteBillingCreditCardAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Delete Credit Card asynchronously
   * Removes the selected credit card from the account. Use &#x60;/billing/payment_method&#x60; to select a new default payment method after deleting a card.
   *
   * @param id The credit card ID to remove. Use IDs from &#x60;GET /billing/creditcards&#x60;. 
   * @return Future(SuccessTextResponse)
   */
  def deleteBillingCreditCardAsync(id: Integer): Future[SuccessTextResponse] = {
      helper.deleteBillingCreditCard(id)
  }

  /**
   * Delete Invoice
   * Deletes a pending (unpaid) invoice from the account. Only invoices with a pending payment status can be deleted. Paid invoices cannot be removed. Related service records and repeat invoices are also cleaned up.
   *
   * @param id The invoice ID to delete. Only unpaid invoices can be deleted. 
   * @return SuccessTextResponse
   */
  def deleteBillingInvoice(id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(deleteBillingInvoiceAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Delete Invoice asynchronously
   * Deletes a pending (unpaid) invoice from the account. Only invoices with a pending payment status can be deleted. Paid invoices cannot be removed. Related service records and repeat invoices are also cleaned up.
   *
   * @param id The invoice ID to delete. Only unpaid invoices can be deleted. 
   * @return Future(SuccessTextResponse)
   */
  def deleteBillingInvoiceAsync(id: Integer): Future[SuccessTextResponse] = {
      helper.deleteBillingInvoice(id)
  }

  /**
   * Delete Prepay Balance
   * Deletes a prepay balance from the account. The balance must have remaining funds to be eligible for deletion. Use &#x60;GET /billing/prepays&#x60; to list available prepay balances and their IDs.
   *
   * @param id The prepay balance ID to delete. 
   * @return SuccessTextResponse
   */
  def deleteBillingPrepay(id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(deleteBillingPrepayAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Delete Prepay Balance asynchronously
   * Deletes a prepay balance from the account. The balance must have remaining funds to be eligible for deletion. Use &#x60;GET /billing/prepays&#x60; to list available prepay balances and their IDs.
   *
   * @param id The prepay balance ID to delete. 
   * @return Future(SuccessTextResponse)
   */
  def deleteBillingPrepayAsync(id: Integer): Future[SuccessTextResponse] = {
      helper.deleteBillingPrepay(id)
  }

  /**
   * List Affiliate Banner Assets
   * Returns the catalog of available affiliate banner images with their dimensions. Use these assets to build marketing creatives for your affiliate campaigns. Each banner includes the image filename, width, and height for layout purposes.
   *
   * @return List[AffiliateBannerRow]
   */
  def getAffiliateBanners(): Option[List[AffiliateBannerRow]] = {
    val await = Try(Await.result(getAffiliateBannersAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List Affiliate Banner Assets asynchronously
   * Returns the catalog of available affiliate banner images with their dimensions. Use these assets to build marketing creatives for your affiliate campaigns. Each banner includes the image filename, width, and height for layout purposes.
   *
   * @return Future(List[AffiliateBannerRow])
   */
  def getAffiliateBannersAsync(): Future[List[AffiliateBannerRow]] = {
      helper.getAffiliateBanners()
  }

  /**
   * Get Affiliate Performance Report
   * Returns a detailed affiliate performance report with commission totals, conversion rates, and traffic summary. Use this for a comprehensive overview of your affiliate program performance in a single request.
   *
   * @return TextResponse
   */
  def getAffiliateRichReport(): Option[TextResponse] = {
    val await = Try(Await.result(getAffiliateRichReportAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Affiliate Performance Report asynchronously
   * Returns a detailed affiliate performance report with commission totals, conversion rates, and traffic summary. Use this for a comprehensive overview of your affiliate program performance in a single request.
   *
   * @return Future(TextResponse)
   */
  def getAffiliateRichReportAsync(): Future[TextResponse] = {
      helper.getAffiliateRichReport()
  }

  /**
   * Get Affiliate Sales Graph Data
   * Returns time-series sales data for the requested number of days. Use this to render sales trend charts in an affiliate dashboard. Each data point represents aggregated sales for a time period.
   *
   * @param days Number of days of sales history to include in the graph data. Determines the time window for the returned data points. (optional)
   * @return StatusMonthlyBreakdown
   */
  def getAffiliateSalesGraph(days: Option[Integer] = None): Option[StatusMonthlyBreakdown] = {
    val await = Try(Await.result(getAffiliateSalesGraphAsync(days), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Affiliate Sales Graph Data asynchronously
   * Returns time-series sales data for the requested number of days. Use this to render sales trend charts in an affiliate dashboard. Each data point represents aggregated sales for a time period.
   *
   * @param days Number of days of sales history to include in the graph data. Determines the time window for the returned data points. (optional)
   * @return Future(StatusMonthlyBreakdown)
   */
  def getAffiliateSalesGraphAsync(days: Option[Integer] = None): Future[StatusMonthlyBreakdown] = {
      helper.getAffiliateSalesGraph(days)
  }

  /**
   * Get Affiliate Sales Report
   * Returns the affiliate sales report with commission amounts and order summaries. Use this for tabular sales data export or to reconcile commission payouts against individual referral orders.
   *
   * @return TextResponse
   */
  def getAffiliateSalesReport(): Option[TextResponse] = {
    val await = Try(Await.result(getAffiliateSalesReportAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Affiliate Sales Report asynchronously
   * Returns the affiliate sales report with commission amounts and order summaries. Use this for tabular sales data export or to reconcile commission payouts against individual referral orders.
   *
   * @return Future(TextResponse)
   */
  def getAffiliateSalesReportAsync(): Future[TextResponse] = {
      helper.getAffiliateSalesReport()
  }

  /**
   * Get Affiliate Traffic Graph Data
   * Returns time-series traffic data for the requested number of days. Use this to render click and visit trend charts in an affiliate dashboard. Each data point represents aggregated traffic counts for a time period.
   *
   * @param days Number of days of traffic history to include in the graph data. Determines the time window for the returned data points. (optional)
   * @return MonthlyCounts
   */
  def getAffiliateTrafficGraph(days: Option[Integer] = None): Option[MonthlyCounts] = {
    val await = Try(Await.result(getAffiliateTrafficGraphAsync(days), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Affiliate Traffic Graph Data asynchronously
   * Returns time-series traffic data for the requested number of days. Use this to render click and visit trend charts in an affiliate dashboard. Each data point represents aggregated traffic counts for a time period.
   *
   * @param days Number of days of traffic history to include in the graph data. Determines the time window for the returned data points. (optional)
   * @return Future(MonthlyCounts)
   */
  def getAffiliateTrafficGraphAsync(days: Option[Integer] = None): Future[MonthlyCounts] = {
      helper.getAffiliateTrafficGraph(days)
  }

  /**
   * List Affiliate Web Traffic Entries
   * Returns individual web traffic log entries for affiliate referrals, including visitor IP address, referral URL, and timestamp. Use this to audit traffic sources, identify top referrers, or investigate suspicious click patterns.
   *
   * @return List[AffiliateTrafficRow]
   */
  def getAffiliateWebTraffic(): Option[List[AffiliateTrafficRow]] = {
    val await = Try(Await.result(getAffiliateWebTrafficAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List Affiliate Web Traffic Entries asynchronously
   * Returns individual web traffic log entries for affiliate referrals, including visitor IP address, referral URL, and timestamp. Use this to audit traffic sources, identify top referrers, or investigate suspicious click patterns.
   *
   * @return Future(List[AffiliateTrafficRow])
   */
  def getAffiliateWebTrafficAsync(): Future[List[AffiliateTrafficRow]] = {
      helper.getAffiliateWebTraffic()
  }

  /**
   * Get Shopping Cart Contents
   * Returns the current cart contents, available payment methods, and checkout metadata for the authenticated account. Use this to display the cart page, show totals, and determine which payment options are available before directing the user to &#x60;/pay/{method}/{invoices}&#x60;.
   *
   * @return void
   */
  def getBillingCart() = {
    val await = Try(Await.result(getBillingCartAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Shopping Cart Contents asynchronously
   * Returns the current cart contents, available payment methods, and checkout metadata for the authenticated account. Use this to display the cart page, show totals, and determine which payment options are available before directing the user to &#x60;/pay/{method}/{invoices}&#x60;.
   *
   * @return Future(void)
   */
  def getBillingCartAsync() = {
      helper.getBillingCart()
  }

  /**
   * Get Credit Card Verification Requirements
   * Retrieves the verification requirements for a newly added credit card. The response indicates whether the card requires micro-charge amount confirmation or CVV validation. Use this before presenting a verification form to the user.
   *
   * @param id The credit card ID to verify. Use the ID returned from &#x60;POST /billing/creditcards&#x60;. 
   * @return SuccessTextResponse
   */
  def getBillingCreditCardVerify(id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(getBillingCreditCardVerifyAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Credit Card Verification Requirements asynchronously
   * Retrieves the verification requirements for a newly added credit card. The response indicates whether the card requires micro-charge amount confirmation or CVV validation. Use this before presenting a verification form to the user.
   *
   * @param id The credit card ID to verify. Use the ID returned from &#x60;POST /billing/creditcards&#x60;. 
   * @return Future(SuccessTextResponse)
   */
  def getBillingCreditCardVerifyAsync(id: Integer): Future[SuccessTextResponse] = {
      helper.getBillingCreditCardVerify(id)
  }

  /**
   * Get Invoice Details
   * Retrieves the full invoice information including line items, amounts, and payment status. Use this before redirecting to &#x60;/pay/{method}/{invoices}&#x60; so you can display the exact amount due and confirm the invoice is still unpaid.
   *
   * @param id The invoice ID. Use IDs from &#x60;GET /billing/invoices&#x60; or from order responses. 
   * @return BillingInvoiceDetail
   */
  def getBillingInvoice(id: Integer): Option[BillingInvoiceDetail] = {
    val await = Try(Await.result(getBillingInvoiceAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Invoice Details asynchronously
   * Retrieves the full invoice information including line items, amounts, and payment status. Use this before redirecting to &#x60;/pay/{method}/{invoices}&#x60; so you can display the exact amount due and confirm the invoice is still unpaid.
   *
   * @param id The invoice ID. Use IDs from &#x60;GET /billing/invoices&#x60; or from order responses. 
   * @return Future(BillingInvoiceDetail)
   */
  def getBillingInvoiceAsync(id: Integer): Future[BillingInvoiceDetail] = {
      helper.getBillingInvoice(id)
  }

  /**
   * List Account Invoices
   * Returns the invoice list for the account with summary totals. Use the invoice IDs from the response with &#x60;/billing/invoices/{id}&#x60; to retrieve detailed line items, or with &#x60;/pay/{method}/{invoices}&#x60; to initiate payment.
   *
   * @return BillingInvoiceList
   */
  def getBillingInvoices(): Option[BillingInvoiceList] = {
    val await = Try(Await.result(getBillingInvoicesAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List Account Invoices asynchronously
   * Returns the invoice list for the account with summary totals. Use the invoice IDs from the response with &#x60;/billing/invoices/{id}&#x60; to retrieve detailed line items, or with &#x60;/pay/{method}/{invoices}&#x60; to initiate payment.
   *
   * @return Future(BillingInvoiceList)
   */
  def getBillingInvoicesAsync(): Future[BillingInvoiceList] = {
      helper.getBillingInvoices()
  }

  /**
   * List Prepay Balances
   * Lists prepay balances and their associated metadata. Use this to determine whether an account has usable prepay funds before selecting &#x60;prepay&#x60; as a payment method.
   *
   * @return void
   */
  def getBillingPrePays() = {
    val await = Try(Await.result(getBillingPrePaysAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List Prepay Balances asynchronously
   * Lists prepay balances and their associated metadata. Use this to determine whether an account has usable prepay funds before selecting &#x60;prepay&#x60; as a payment method.
   *
   * @return Future(void)
   */
  def getBillingPrePaysAsync() = {
      helper.getBillingPrePays()
  }

  /**
   * Get Invoices
   * Returns a paginated list of invoices for the authenticated account. Each invoice includes the invoice number, date, total amount, and payment status. Use the optional &#x60;searchString&#x60; parameter to filter results and &#x60;skip&#x60;/&#x60;limit&#x60; for pagination.
   *
   * @param searchString pass an optional search string for looking up inventory (optional)
   * @param skip number of records to skip for pagination (optional)
   * @param limit maximum number of records to return (optional)
   * @return List[Invoice]
   */
  def getInvoices(searchString: Option[String] = None, skip: Option[Integer] = None, limit: Option[Integer] = None): Option[List[Invoice]] = {
    val await = Try(Await.result(getInvoicesAsync(searchString, skip, limit), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Invoices asynchronously
   * Returns a paginated list of invoices for the authenticated account. Each invoice includes the invoice number, date, total amount, and payment status. Use the optional &#x60;searchString&#x60; parameter to filter results and &#x60;skip&#x60;/&#x60;limit&#x60; for pagination.
   *
   * @param searchString pass an optional search string for looking up inventory (optional)
   * @param skip number of records to skip for pagination (optional)
   * @param limit maximum number of records to return (optional)
   * @return Future(List[Invoice])
   */
  def getInvoicesAsync(searchString: Option[String] = None, skip: Option[Integer] = None, limit: Option[Integer] = None): Future[List[Invoice]] = {
      helper.getInvoices(searchString, skip, limit)
  }

  /**
   * Initiate Payment
   * Initiates a payment for the specified invoices using the chosen payment method. The response type determines how your client should proceed: &#x60;redirect&#x60; means send the user to the provided URL, &#x60;submit&#x60; means POST a form with the provided fields, and &#x60;single&#x60; means the payment was processed immediately. Use invoice IDs obtained from order responses or &#x60;/billing/invoices&#x60;.
   *
   * @param method The payment method to use. Valid values: &#x60;cc&#x60; (credit card), &#x60;paypal&#x60;, &#x60;prepay&#x60;, &#x60;payssion&#x60;, &#x60;payu&#x60;, &#x60;ccavenue&#x60;, &#x60;cashfree&#x60;, &#x60;coinbase&#x60;, &#x60;btcpay&#x60;. 
   * @param invoices A comma-separated list of invoice IDs to pay. These IDs are returned by order endpoints (e.g. &#x60;/backups/order&#x60;, &#x60;/vps/order&#x60;) and by &#x60;/billing/invoices&#x60;. 
   * @return inline_response_200_9
   */
  def initiatePayment(method: String, invoices: String): Option[inline_response_200_9] = {
    val await = Try(Await.result(initiatePaymentAsync(method, invoices), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Initiate Payment asynchronously
   * Initiates a payment for the specified invoices using the chosen payment method. The response type determines how your client should proceed: &#x60;redirect&#x60; means send the user to the provided URL, &#x60;submit&#x60; means POST a form with the provided fields, and &#x60;single&#x60; means the payment was processed immediately. Use invoice IDs obtained from order responses or &#x60;/billing/invoices&#x60;.
   *
   * @param method The payment method to use. Valid values: &#x60;cc&#x60; (credit card), &#x60;paypal&#x60;, &#x60;prepay&#x60;, &#x60;payssion&#x60;, &#x60;payu&#x60;, &#x60;ccavenue&#x60;, &#x60;cashfree&#x60;, &#x60;coinbase&#x60;, &#x60;btcpay&#x60;. 
   * @param invoices A comma-separated list of invoice IDs to pay. These IDs are returned by order endpoints (e.g. &#x60;/backups/order&#x60;, &#x60;/vps/order&#x60;) and by &#x60;/billing/invoices&#x60;. 
   * @return Future(inline_response_200_9)
   */
  def initiatePaymentAsync(method: String, invoices: String): Future[inline_response_200_9] = {
      helper.initiatePayment(method, invoices)
  }

  /**
   * Submit Credit Card Verification
   * Completes the credit card verification flow by submitting the micro-charge amounts or CVV as required by &#x60;GET /billing/creditcards/{id}/verify&#x60;. A successful response means the card is verified and can be selected as a payment method via &#x60;/billing/payment_method&#x60;.
   *
   * @param body  
   * @param idx  
   * @param cc_ccv2  
   * @param cc_amount1  
   * @param cc_amount2  
   * @param terms  
   * @param id The credit card ID to verify. Use the ID returned from &#x60;POST /billing/creditcards&#x60;. 
   * @return SuccessTextResponse
   */
  def postBillingCreditCardVerify(body: BillingVerifyCcRequest, idx: Integer, cc_ccv2: String, cc_amount1: String, cc_amount2: String, terms: Boolean, id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(postBillingCreditCardVerifyAsync(body, idx, cc_ccv2, cc_amount1, cc_amount2, terms, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Submit Credit Card Verification asynchronously
   * Completes the credit card verification flow by submitting the micro-charge amounts or CVV as required by &#x60;GET /billing/creditcards/{id}/verify&#x60;. A successful response means the card is verified and can be selected as a payment method via &#x60;/billing/payment_method&#x60;.
   *
   * @param body  
   * @param idx  
   * @param cc_ccv2  
   * @param cc_amount1  
   * @param cc_amount2  
   * @param terms  
   * @param id The credit card ID to verify. Use the ID returned from &#x60;POST /billing/creditcards&#x60;. 
   * @return Future(SuccessTextResponse)
   */
  def postBillingCreditCardVerifyAsync(body: BillingVerifyCcRequest, idx: Integer, cc_ccv2: String, cc_amount1: String, cc_amount2: String, terms: Boolean, id: Integer): Future[SuccessTextResponse] = {
      helper.postBillingCreditCardVerify(body, idx, cc_ccv2, cc_amount1, cc_amount2, terms, id)
  }

  /**
   * Update Credit Card
   * Updates an existing credit card on the account. Use this to refresh stored card metadata such as expiration date or billing address.
   *
   * @param id The credit card ID. Use the card ID returned from &#x60;POST /account/creditcards&#x60; or listed in &#x60;/billing/creditcards&#x60;. 
   * @return void
   */
  def updateAccountCreditCard(id: Integer) = {
    val await = Try(Await.result(updateAccountCreditCardAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update Credit Card asynchronously
   * Updates an existing credit card on the account. Use this to refresh stored card metadata such as expiration date or billing address.
   *
   * @param id The credit card ID. Use the card ID returned from &#x60;POST /account/creditcards&#x60; or listed in &#x60;/billing/creditcards&#x60;. 
   * @return Future(void)
   */
  def updateAccountCreditCardAsync(id: Integer) = {
      helper.updateAccountCreditCard(id)
  }

  /**
   * Configure Affiliate Dock Settings
   * Updates the affiliate dock settings including the referral coupon and marketing copy. The dock is the branded landing page shown to visitors arriving via your affiliate link. Use this to customize the coupon code and promotional text.
   *
   * @param affiliate_dock_title  
   * @param affiliate_dock_description  
   * @param referrer_coupon  
   * @param body  
   * @return TextResponse
   */
  def updateAffiliateDockSetup(affiliate_dock_title: String, affiliate_dock_description: String, referrer_coupon: String, body: AffiliateDockSetup): Option[TextResponse] = {
    val await = Try(Await.result(updateAffiliateDockSetupAsync(affiliate_dock_title, affiliate_dock_description, referrer_coupon, body), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Configure Affiliate Dock Settings asynchronously
   * Updates the affiliate dock settings including the referral coupon and marketing copy. The dock is the branded landing page shown to visitors arriving via your affiliate link. Use this to customize the coupon code and promotional text.
   *
   * @param affiliate_dock_title  
   * @param affiliate_dock_description  
   * @param referrer_coupon  
   * @param body  
   * @return Future(TextResponse)
   */
  def updateAffiliateDockSetupAsync(affiliate_dock_title: String, affiliate_dock_description: String, referrer_coupon: String, body: AffiliateDockSetup): Future[TextResponse] = {
      helper.updateAffiliateDockSetup(affiliate_dock_title, affiliate_dock_description, referrer_coupon, body)
  }

  /**
   * Configure Affiliate Landing Page
   * Updates the affiliate landing page configuration, including the title, description, and coupon code. Visitors who arrive through your affiliate link see this customized page. Changes are published immediately.
   *
   * @param affiliate_dock_title  
   * @param affiliate_dock_description  
   * @param referrer_coupon  
   * @param body  
   * @return TextResponse
   */
  def updateAffiliateLandingPage(affiliate_dock_title: String, affiliate_dock_description: String, referrer_coupon: String, body: AffiliateDockSetup): Option[TextResponse] = {
    val await = Try(Await.result(updateAffiliateLandingPageAsync(affiliate_dock_title, affiliate_dock_description, referrer_coupon, body), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Configure Affiliate Landing Page asynchronously
   * Updates the affiliate landing page configuration, including the title, description, and coupon code. Visitors who arrive through your affiliate link see this customized page. Changes are published immediately.
   *
   * @param affiliate_dock_title  
   * @param affiliate_dock_description  
   * @param referrer_coupon  
   * @param body  
   * @return Future(TextResponse)
   */
  def updateAffiliateLandingPageAsync(affiliate_dock_title: String, affiliate_dock_description: String, referrer_coupon: String, body: AffiliateDockSetup): Future[TextResponse] = {
      helper.updateAffiliateLandingPage(affiliate_dock_title, affiliate_dock_description, referrer_coupon, body)
  }

  /**
   * Configure Affiliate Payout Preferences
   * Updates how you receive affiliate commission payouts. Choose between prepay credit applied to your account balance or PayPal disbursement. When selecting PayPal, provide the email address linked to your PayPal account.
   *
   * @param affiliate_paypal  
   * @param affiliate_payment_method  
   * @param body  
   * @return TextResponse
   */
  def updateAffiliatePaymentSetup(affiliate_paypal: String, affiliate_payment_method: String, body: AffiliatePaymentSetup): Option[TextResponse] = {
    val await = Try(Await.result(updateAffiliatePaymentSetupAsync(affiliate_paypal, affiliate_payment_method, body), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Configure Affiliate Payout Preferences asynchronously
   * Updates how you receive affiliate commission payouts. Choose between prepay credit applied to your account balance or PayPal disbursement. When selecting PayPal, provide the email address linked to your PayPal account.
   *
   * @param affiliate_paypal  
   * @param affiliate_payment_method  
   * @param body  
   * @return Future(TextResponse)
   */
  def updateAffiliatePaymentSetupAsync(affiliate_paypal: String, affiliate_payment_method: String, body: AffiliatePaymentSetup): Future[TextResponse] = {
      helper.updateAffiliatePaymentSetup(affiliate_paypal, affiliate_payment_method, body)
  }

  /**
   * Update Credit Card Details
   * Updates stored credit card metadata or retrieves the masked card details. Use this to refresh card data before verification or to update billing address information associated with the card.
   *
   * @param id The credit card ID. Use IDs from &#x60;GET /billing/creditcards&#x60; or the response from &#x60;POST /billing/creditcards&#x60;. 
   * @return SuccessTextResponse
   */
  def updateBillingCreditCard(id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(updateBillingCreditCardAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update Credit Card Details asynchronously
   * Updates stored credit card metadata or retrieves the masked card details. Use this to refresh card data before verification or to update billing address information associated with the card.
   *
   * @param id The credit card ID. Use IDs from &#x60;GET /billing/creditcards&#x60; or the response from &#x60;POST /billing/creditcards&#x60;. 
   * @return Future(SuccessTextResponse)
   */
  def updateBillingCreditCardAsync(id: Integer): Future[SuccessTextResponse] = {
      helper.updateBillingCreditCard(id)
  }

  /**
   * Update Default Payment Method
   * Updates the account&#x27;s default payment method, including selecting a verified credit card as the primary payment source or switching to PayPal when available.
   *
   * @param body  
   * @param payment_method  
   * @param cc_auto  
   * @return SuccessTextResponse
   */
  def updateBillingPaymentMethod(body: BillingPaymentMethodRequest, payment_method: String, cc_auto: String): Option[SuccessTextResponse] = {
    val await = Try(Await.result(updateBillingPaymentMethodAsync(body, payment_method, cc_auto), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update Default Payment Method asynchronously
   * Updates the account&#x27;s default payment method, including selecting a verified credit card as the primary payment source or switching to PayPal when available.
   *
   * @param body  
   * @param payment_method  
   * @param cc_auto  
   * @return Future(SuccessTextResponse)
   */
  def updateBillingPaymentMethodAsync(body: BillingPaymentMethodRequest, payment_method: String, cc_auto: String): Future[SuccessTextResponse] = {
      helper.updateBillingPaymentMethod(body, payment_method, cc_auto)
  }

}

class BillingApiAsyncHelper(client: TransportClient, config: SwaggerConfig) extends ApiClient(client, config) {

  def addAccountCreditCard(name: String,
    address: String,
    city: String,
    state: String,
    country: String,
    zip: String,
    cc: String,
    cc_exp: String,
    cc_ccv2: String,
    body: BillingAddCcRequest)(implicit reader: ClientResponseReader[SuccessTextResponse], writer: RequestWriter[BillingAddCcRequest]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/account/creditcards"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (name == null) throw new Exception("Missing required parameter 'name' when calling BillingApi->addAccountCreditCard")

    if (address == null) throw new Exception("Missing required parameter 'address' when calling BillingApi->addAccountCreditCard")

    if (city == null) throw new Exception("Missing required parameter 'city' when calling BillingApi->addAccountCreditCard")

    if (state == null) throw new Exception("Missing required parameter 'state' when calling BillingApi->addAccountCreditCard")

    if (country == null) throw new Exception("Missing required parameter 'country' when calling BillingApi->addAccountCreditCard")

    if (zip == null) throw new Exception("Missing required parameter 'zip' when calling BillingApi->addAccountCreditCard")

    if (cc == null) throw new Exception("Missing required parameter 'cc' when calling BillingApi->addAccountCreditCard")

    if (cc_exp == null) throw new Exception("Missing required parameter 'cc_exp' when calling BillingApi->addAccountCreditCard")

    if (cc_ccv2 == null) throw new Exception("Missing required parameter 'cc_ccv2' when calling BillingApi->addAccountCreditCard")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling BillingApi->addAccountCreditCard")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def addBillingCreditCard(body: BillingAddCcRequest,
    name: String,
    address: String,
    city: String,
    state: String,
    country: String,
    zip: String,
    cc: String,
    cc_exp: String,
    cc_ccv2: String)(implicit reader: ClientResponseReader[SuccessTextResponse], writer: RequestWriter[BillingAddCcRequest]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/billing/creditcards"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling BillingApi->addBillingCreditCard")
    if (name == null) throw new Exception("Missing required parameter 'name' when calling BillingApi->addBillingCreditCard")

    if (address == null) throw new Exception("Missing required parameter 'address' when calling BillingApi->addBillingCreditCard")

    if (city == null) throw new Exception("Missing required parameter 'city' when calling BillingApi->addBillingCreditCard")

    if (state == null) throw new Exception("Missing required parameter 'state' when calling BillingApi->addBillingCreditCard")

    if (country == null) throw new Exception("Missing required parameter 'country' when calling BillingApi->addBillingCreditCard")

    if (zip == null) throw new Exception("Missing required parameter 'zip' when calling BillingApi->addBillingCreditCard")

    if (cc == null) throw new Exception("Missing required parameter 'cc' when calling BillingApi->addBillingCreditCard")

    if (cc_exp == null) throw new Exception("Missing required parameter 'cc_exp' when calling BillingApi->addBillingCreditCard")

    if (cc_ccv2 == null) throw new Exception("Missing required parameter 'cc_ccv2' when calling BillingApi->addBillingCreditCard")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def addBillingPrepay(body: BillingPrepayRequest,
    module: String,
    amount: BigDecimal,
    automatic_use: String)(implicit reader: ClientResponseReader[SuccessTextResponse], writer: RequestWriter[BillingPrepayRequest]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/billing/prepays"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling BillingApi->addBillingPrepay")
    if (module == null) throw new Exception("Missing required parameter 'module' when calling BillingApi->addBillingPrepay")

    if (automatic_use == null) throw new Exception("Missing required parameter 'automatic_use' when calling BillingApi->addBillingPrepay")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def deleteAccountCreditCard(id: String)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/account/creditcards/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (id == null) throw new Exception("Missing required parameter 'id' when calling BillingApi->deleteAccountCreditCard")


    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def deleteBillingCreditCard(id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/billing/creditcards/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def deleteBillingInvoice(id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/billing/invoices/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def deleteBillingPrepay(id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/billing/prepays/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getAffiliateBanners()(implicit reader: ClientResponseReader[List[AffiliateBannerRow]]): Future[List[AffiliateBannerRow]] = {
    // create path and map variables
    val path = (addFmt("/affiliate/banners"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getAffiliateRichReport()(implicit reader: ClientResponseReader[TextResponse]): Future[TextResponse] = {
    // create path and map variables
    val path = (addFmt("/affiliate/rich_report"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getAffiliateSalesGraph(days: Option[Integer] = None
    )(implicit reader: ClientResponseReader[StatusMonthlyBreakdown]): Future[StatusMonthlyBreakdown] = {
    // create path and map variables
    val path = (addFmt("/affiliate/sales_graph"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    days match {
      case Some(param) => queryParams += "days" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getAffiliateSalesReport()(implicit reader: ClientResponseReader[TextResponse]): Future[TextResponse] = {
    // create path and map variables
    val path = (addFmt("/affiliate/sales_report"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getAffiliateTrafficGraph(days: Option[Integer] = None
    )(implicit reader: ClientResponseReader[MonthlyCounts]): Future[MonthlyCounts] = {
    // create path and map variables
    val path = (addFmt("/affiliate/traffic_graph"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    days match {
      case Some(param) => queryParams += "days" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getAffiliateWebTraffic()(implicit reader: ClientResponseReader[List[AffiliateTrafficRow]]): Future[List[AffiliateTrafficRow]] = {
    // create path and map variables
    val path = (addFmt("/affiliate/web_traffic"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getBillingCart()(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/billing/cart"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getBillingCreditCardVerify(id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/billing/creditcards/{id}/verify")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getBillingInvoice(id: Integer)(implicit reader: ClientResponseReader[BillingInvoiceDetail]): Future[BillingInvoiceDetail] = {
    // create path and map variables
    val path = (addFmt("/billing/invoices/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getBillingInvoices()(implicit reader: ClientResponseReader[BillingInvoiceList]): Future[BillingInvoiceList] = {
    // create path and map variables
    val path = (addFmt("/billing/invoices"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getBillingPrePays()(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/billing/prepays"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getInvoices(searchString: Option[String] = None,
    skip: Option[Integer] = None,
    limit: Option[Integer] = None
    )(implicit reader: ClientResponseReader[List[Invoice]]): Future[List[Invoice]] = {
    // create path and map variables
    val path = (addFmt("/invoices"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    searchString match {
      case Some(param) => queryParams += "searchString" -> param.toString
      case _ => queryParams
    }
    skip match {
      case Some(param) => queryParams += "skip" -> param.toString
      case _ => queryParams
    }
    limit match {
      case Some(param) => queryParams += "limit" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def initiatePayment(method: String,
    invoices: String)(implicit reader: ClientResponseReader[inline_response_200_9]): Future[inline_response_200_9] = {
    // create path and map variables
    val path = (addFmt("/pay/{method}/{invoices}")
      replaceAll("\\{" + "method" + "\\}", method.toString)
      replaceAll("\\{" + "invoices" + "\\}", invoices.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (method == null) throw new Exception("Missing required parameter 'method' when calling BillingApi->initiatePayment")

    if (invoices == null) throw new Exception("Missing required parameter 'invoices' when calling BillingApi->initiatePayment")


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postBillingCreditCardVerify(body: BillingVerifyCcRequest,
    idx: Integer,
    cc_ccv2: String,
    cc_amount1: String,
    cc_amount2: String,
    terms: Boolean,
    id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse], writer: RequestWriter[BillingVerifyCcRequest]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/billing/creditcards/{id}/verify")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling BillingApi->postBillingCreditCardVerify")
    if (cc_ccv2 == null) throw new Exception("Missing required parameter 'cc_ccv2' when calling BillingApi->postBillingCreditCardVerify")

    if (cc_amount1 == null) throw new Exception("Missing required parameter 'cc_amount1' when calling BillingApi->postBillingCreditCardVerify")

    if (cc_amount2 == null) throw new Exception("Missing required parameter 'cc_amount2' when calling BillingApi->postBillingCreditCardVerify")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateAccountCreditCard(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/account/creditcards/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateAffiliateDockSetup(affiliate_dock_title: String,
    affiliate_dock_description: String,
    referrer_coupon: String,
    body: AffiliateDockSetup)(implicit reader: ClientResponseReader[TextResponse], writer: RequestWriter[AffiliateDockSetup]): Future[TextResponse] = {
    // create path and map variables
    val path = (addFmt("/affiliate/dock_setup"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (affiliate_dock_title == null) throw new Exception("Missing required parameter 'affiliate_dock_title' when calling BillingApi->updateAffiliateDockSetup")

    if (affiliate_dock_description == null) throw new Exception("Missing required parameter 'affiliate_dock_description' when calling BillingApi->updateAffiliateDockSetup")

    if (referrer_coupon == null) throw new Exception("Missing required parameter 'referrer_coupon' when calling BillingApi->updateAffiliateDockSetup")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling BillingApi->updateAffiliateDockSetup")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateAffiliateLandingPage(affiliate_dock_title: String,
    affiliate_dock_description: String,
    referrer_coupon: String,
    body: AffiliateDockSetup)(implicit reader: ClientResponseReader[TextResponse], writer: RequestWriter[AffiliateDockSetup]): Future[TextResponse] = {
    // create path and map variables
    val path = (addFmt("/affiliate/landing_pg"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (affiliate_dock_title == null) throw new Exception("Missing required parameter 'affiliate_dock_title' when calling BillingApi->updateAffiliateLandingPage")

    if (affiliate_dock_description == null) throw new Exception("Missing required parameter 'affiliate_dock_description' when calling BillingApi->updateAffiliateLandingPage")

    if (referrer_coupon == null) throw new Exception("Missing required parameter 'referrer_coupon' when calling BillingApi->updateAffiliateLandingPage")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling BillingApi->updateAffiliateLandingPage")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateAffiliatePaymentSetup(affiliate_paypal: String,
    affiliate_payment_method: String,
    body: AffiliatePaymentSetup)(implicit reader: ClientResponseReader[TextResponse], writer: RequestWriter[AffiliatePaymentSetup]): Future[TextResponse] = {
    // create path and map variables
    val path = (addFmt("/affiliate/payment_setup"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (affiliate_paypal == null) throw new Exception("Missing required parameter 'affiliate_paypal' when calling BillingApi->updateAffiliatePaymentSetup")

    if (affiliate_payment_method == null) throw new Exception("Missing required parameter 'affiliate_payment_method' when calling BillingApi->updateAffiliatePaymentSetup")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling BillingApi->updateAffiliatePaymentSetup")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateBillingCreditCard(id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/billing/creditcards/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateBillingPaymentMethod(body: BillingPaymentMethodRequest,
    payment_method: String,
    cc_auto: String)(implicit reader: ClientResponseReader[SuccessTextResponse], writer: RequestWriter[BillingPaymentMethodRequest]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/billing/payment_method"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling BillingApi->updateBillingPaymentMethod")
    if (payment_method == null) throw new Exception("Missing required parameter 'payment_method' when calling BillingApi->updateBillingPaymentMethod")

    if (cc_auto == null) throw new Exception("Missing required parameter 'cc_auto' when calling BillingApi->updateBillingPaymentMethod")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }


}
