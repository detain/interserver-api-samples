/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * OpenAPI spec version: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */
package io.swagger.client.api

import java.text.SimpleDateFormat

import io.swagger.client.model.ChargeInvoiceRows
import io.swagger.client.model.IdBackupsBody
import io.swagger.client.model.QueueResponse
import io.swagger.client.model.Quickserver
import io.swagger.client.model.QuickserverOrder
import io.swagger.client.model.QuickserverRow
import io.swagger.client.model.RestoreRequest
import io.swagger.client.model.ReverseDnsEntries
import io.swagger.client.model.SuccessTextResponse
import io.swagger.client.model.TextResponse
import io.swagger.client.model.TimezoneUpdate
import io.swagger.client.model.VpsBackupRows
import io.swagger.client.model.VpsTemplatesList
import io.swagger.client.model.inline_response_200_10
import io.swagger.client.model.inline_response_200_11
import io.swagger.client.model.inline_response_401
import io.swagger.client.{ApiInvoker, ApiException}

import com.sun.jersey.multipart.FormDataMultiPart
import com.sun.jersey.multipart.file.FileDataBodyPart

import javax.ws.rs.core.MediaType

import java.io.File
import java.util.Date
import java.util.TimeZone

import scala.collection.mutable.HashMap

import com.wordnik.swagger.client._
import scala.concurrent.Future
import collection.mutable

import java.net.URI

import com.wordnik.swagger.client.ClientResponseReaders.Json4sFormatsReader._
import com.wordnik.swagger.client.RequestWriters.Json4sFormatsWriter._

import scala.concurrent.ExecutionContext.Implicits.global
import scala.concurrent._
import scala.concurrent.duration._
import scala.util.{Failure, Success, Try}

import org.json4s._

class QuickServersApi(
  val defBasePath: String = "https://my.interserver.net/apiv2",
  defApiInvoker: ApiInvoker = ApiInvoker
) {
  private lazy val dateTimeFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSSZ")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  private val dateFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  implicit val formats = new org.json4s.DefaultFormats {
    override def dateFormatter = dateTimeFormatter
  }
  implicit val stringReader: ClientResponseReader[String] = ClientResponseReaders.StringReader
  implicit val unitReader: ClientResponseReader[Unit] = ClientResponseReaders.UnitReader
  implicit val jvalueReader: ClientResponseReader[JValue] = ClientResponseReaders.JValueReader
  implicit val jsonReader: ClientResponseReader[Nothing] = JsonFormatsReader
  implicit val stringWriter: RequestWriter[String] = RequestWriters.StringWriter
  implicit val jsonWriter: RequestWriter[Nothing] = JsonFormatsWriter

  var basePath: String = defBasePath
  var apiInvoker: ApiInvoker = defApiInvoker

  def addHeader(key: String, value: String): mutable.HashMap[String, String] = {
    apiInvoker.defaultHeaders += key -> value
  }

  val config: SwaggerConfig = SwaggerConfig.forUrl(new URI(defBasePath))
  val client = new RestClient(config)
  val helper = new QuickServersApiAsyncHelper(client, config)

  /**
   * Place QuickServer Order
   * Places a QuickServer order. On success, invoices are generated for payment; use &#x60;/billing/invoices/{id}&#x60; or &#x60;/pay/{method}/{invoices}&#x60; to complete payment.
   *
   * @return void
   */
  def addQs() = {
    val await = Try(Await.result(addQsAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Place QuickServer Order asynchronously
   * Places a QuickServer order. On success, invoices are generated for payment; use &#x60;/billing/invoices/{id}&#x60; or &#x60;/pay/{method}/{invoices}&#x60; to complete payment.
   *
   * @return Future(void)
   */
  def addQsAsync() = {
      helper.addQs()
  }

  /**
   * Delete QuickServer Backup
   * Permanently removes the specified backup file from storage. Use &#x60;GET /qs/{id}/backups&#x60; to list available backup filenames before deleting.
   *
   * @param id QuickServer ID number 
   * @param file The backup filename to delete. 
   * @param all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given QuickServer. (optional)
   * @return SuccessTextResponse
   */
  def deleteQsBackup(id: Integer, file: String, all: Option[String] = None): Option[SuccessTextResponse] = {
    val await = Try(Await.result(deleteQsBackupAsync(id, file, all), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Delete QuickServer Backup asynchronously
   * Permanently removes the specified backup file from storage. Use &#x60;GET /qs/{id}/backups&#x60; to list available backup filenames before deleting.
   *
   * @param id QuickServer ID number 
   * @param file The backup filename to delete. 
   * @param all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given QuickServer. (optional)
   * @return Future(SuccessTextResponse)
   */
  def deleteQsBackupAsync(id: Integer, file: String, all: Option[String] = None): Future[SuccessTextResponse] = {
      helper.deleteQsBackup(id, file, all)
  }

  /**
   * Block QuickServer SMTP
   * Blocks outbound SMTP for the QuickServer to prevent email abuse. Use this action when responding to abuse notifications or to enforce outbound email policies.
   *
   * @param id QuickServer ID number. 
   * @return QueueResponse
   */
  def doQsBlockSmtp(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(doQsBlockSmtpAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Block QuickServer SMTP asynchronously
   * Blocks outbound SMTP for the QuickServer to prevent email abuse. Use this action when responding to abuse notifications or to enforce outbound email policies.
   *
   * @param id QuickServer ID number. 
   * @return Future(QueueResponse)
   */
  def doQsBlockSmtpAsync(id: Integer): Future[QueueResponse] = {
      helper.doQsBlockSmtp(id)
  }

  /**
   * Disable CD Drive
   * Disables the virtual CD drive for the QuickServer.
   *
   * @param id QuickServer ID number. 
   * @return QueueResponse
   */
  def doQsDisableCd(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(doQsDisableCdAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Disable CD Drive asynchronously
   * Disables the virtual CD drive for the QuickServer.
   *
   * @param id QuickServer ID number. 
   * @return Future(QueueResponse)
   */
  def doQsDisableCdAsync(id: Integer): Future[QueueResponse] = {
      helper.doQsDisableCd(id)
  }

  /**
   * Disable Quotas
   * Disables disk quota enforcement for the QuickServer.
   *
   * @param id QuickServer ID number. 
   * @return QueueResponse
   */
  def doQsDisableQuota(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(doQsDisableQuotaAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Disable Quotas asynchronously
   * Disables disk quota enforcement for the QuickServer.
   *
   * @param id QuickServer ID number. 
   * @return Future(QueueResponse)
   */
  def doQsDisableQuotaAsync(id: Integer): Future[QueueResponse] = {
      helper.doQsDisableQuota(id)
  }

  /**
   * Eject CD Drive
   * Ejects the virtual CD from the QuickServer&#x27;s CD drive.
   *
   * @param id QuickServer ID number. 
   * @return QueueResponse
   */
  def doQsEjectCd(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(doQsEjectCdAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Eject CD Drive asynchronously
   * Ejects the virtual CD from the QuickServer&#x27;s CD drive.
   *
   * @param id QuickServer ID number. 
   * @return Future(QueueResponse)
   */
  def doQsEjectCdAsync(id: Integer): Future[QueueResponse] = {
      helper.doQsEjectCd(id)
  }

  /**
   * Enable Quotas
   * Enables disk quota enforcement for the QuickServer.
   *
   * @param id QuickServer ID number. 
   * @return QueueResponse
   */
  def doQsEnableQuota(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(doQsEnableQuotaAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Enable Quotas asynchronously
   * Enables disk quota enforcement for the QuickServer.
   *
   * @param id QuickServer ID number. 
   * @return Future(QueueResponse)
   */
  def doQsEnableQuotaAsync(id: Integer): Future[QueueResponse] = {
      helper.doQsEnableQuota(id)
  }

  /**
   * Restart QuickServer
   * Restarts the QuickServer. The server will be shut down and started again.
   *
   * @param id QuickServer ID number. 
   * @return QueueResponse
   */
  def doQsRestart(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(doQsRestartAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Restart QuickServer asynchronously
   * Restarts the QuickServer. The server will be shut down and started again.
   *
   * @param id QuickServer ID number. 
   * @return Future(QueueResponse)
   */
  def doQsRestartAsync(id: Integer): Future[QueueResponse] = {
      helper.doQsRestart(id)
  }

  /**
   * Start QuickServer
   * Powers on the QuickServer.
   *
   * @param id QuickServer ID number. 
   * @return QueueResponse
   */
  def doQsStart(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(doQsStartAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Start QuickServer asynchronously
   * Powers on the QuickServer.
   *
   * @param id QuickServer ID number. 
   * @return Future(QueueResponse)
   */
  def doQsStartAsync(id: Integer): Future[QueueResponse] = {
      helper.doQsStart(id)
  }

  /**
   * Stop QuickServer
   * Powers off the QuickServer.
   *
   * @param id QuickServer ID number. 
   * @return QueueResponse
   */
  def doQsStop(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(doQsStopAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Stop QuickServer asynchronously
   * Powers off the QuickServer.
   *
   * @param id QuickServer ID number. 
   * @return Future(QueueResponse)
   */
  def doQsStopAsync(id: Integer): Future[QueueResponse] = {
      helper.doQsStop(id)
  }

  /**
   * Download QuickServer Backup
   * Generates a pre-signed download URL for the specified backup file. The URL is valid for 24 hours. Use &#x60;GET /qs/{id}/backups&#x60; to list available backup filenames.
   *
   * @param body  
   * @param file  
   * @param id QuickServer ID number 
   * @param all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given QuickServer. (optional)
   * @return inline_response_200_11
   */
  def downloadQsBackup(body: IdBackupsBody, file: String, id: Integer, all: Option[String] = None): Option[inline_response_200_11] = {
    val await = Try(Await.result(downloadQsBackupAsync(body, file, id, all), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Download QuickServer Backup asynchronously
   * Generates a pre-signed download URL for the specified backup file. The URL is valid for 24 hours. Use &#x60;GET /qs/{id}/backups&#x60; to list available backup filenames.
   *
   * @param body  
   * @param file  
   * @param id QuickServer ID number 
   * @param all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given QuickServer. (optional)
   * @return Future(inline_response_200_11)
   */
  def downloadQsBackupAsync(body: IdBackupsBody, file: String, id: Integer, all: Option[String] = None): Future[inline_response_200_11] = {
      helper.downloadQsBackup(body, file, id, all)
  }

  /**
   * Get QuickServer Ordering Information
   * Returns QuickServer ordering metadata and available plans. Use these details to build the order form and to validate a plan selection.
   *
   * @return QuickserverOrder
   */
  def getNewQs(): Option[QuickserverOrder] = {
    val await = Try(Await.result(getNewQsAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get QuickServer Ordering Information asynchronously
   * Returns QuickServer ordering metadata and available plans. Use these details to build the order form and to validate a plan selection.
   *
   * @return Future(QuickserverOrder)
   */
  def getNewQsAsync(): Future[QuickserverOrder] = {
      helper.getNewQs()
  }

  /**
   * List QuickServer Backups
   * Returns the available backups for the QuickServer across all storage systems (Swift, MinIO, ZFS). Use the backup &#x60;name&#x60; value with &#x60;PATCH /qs/{id}/backups&#x60; to download or &#x60;DELETE /qs/{id}/backups&#x60; to remove a backup. Use &#x60;POST /qs/{id}/restore&#x60; to restore from a backup.
   *
   * @param id QuickServer ID number 
   * @param all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given QuickServer. (optional)
   * @return VpsBackupRows
   */
  def getQsBackups(id: Integer, all: Option[String] = None): Option[VpsBackupRows] = {
    val await = Try(Await.result(getQsBackupsAsync(id, all), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List QuickServer Backups asynchronously
   * Returns the available backups for the QuickServer across all storage systems (Swift, MinIO, ZFS). Use the backup &#x60;name&#x60; value with &#x60;PATCH /qs/{id}/backups&#x60; to download or &#x60;DELETE /qs/{id}/backups&#x60; to remove a backup. Use &#x60;POST /qs/{id}/restore&#x60; to restore from a backup.
   *
   * @param id QuickServer ID number 
   * @param all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given QuickServer. (optional)
   * @return Future(VpsBackupRows)
   */
  def getQsBackupsAsync(id: Integer, all: Option[String] = None): Future[VpsBackupRows] = {
      helper.getQsBackups(id, all)
  }

  /**
   * Get QuickServer Hostname
   * Retrieves the current hostname and any validation requirements for changing it.
   *
   * @param id QuickServer ID number 
   * @return void
   */
  def getQsChangeHostname(id: Integer) = {
    val await = Try(Await.result(getQsChangeHostnameAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get QuickServer Hostname asynchronously
   * Retrieves the current hostname and any validation requirements for changing it.
   *
   * @param id QuickServer ID number 
   * @return Future(void)
   */
  def getQsChangeHostnameAsync(id: Integer) = {
      helper.getQsChangeHostname(id)
  }

  /**
   * Get Change Root Password Info
   * Retrieves instructions or metadata needed to reset the root password.
   *
   * @param id QuickServer ID number 
   * @return void
   */
  def getQsChangeRootPassword(id: Integer) = {
    val await = Try(Await.result(getQsChangeRootPasswordAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Change Root Password Info asynchronously
   * Retrieves instructions or metadata needed to reset the root password.
   *
   * @param id QuickServer ID number 
   * @return Future(void)
   */
  def getQsChangeRootPasswordAsync(id: Integer) = {
      helper.getQsChangeRootPassword(id)
  }

  /**
   * Get Timezone Info
   * Returns the list of available timezones that can be set on the QuickServer.
   *
   * @param id QuickServer ID number 
   * @return List[String]
   */
  def getQsChangeTimezone(id: Integer): Option[List[String]] = {
    val await = Try(Await.result(getQsChangeTimezoneAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Timezone Info asynchronously
   * Returns the list of available timezones that can be set on the QuickServer.
   *
   * @param id QuickServer ID number 
   * @return Future(List[String])
   */
  def getQsChangeTimezoneAsync(id: Integer): Future[List[String]] = {
      helper.getQsChangeTimezone(id)
  }

  /**
   * Webuzo Change Pass Info
   * Retrieves instructions or metadata for changing the Webuzo control panel password.
   *
   * @param id QuickServer ID number 
   * @return void
   */
  def getQsChangeWebuzoPassword(id: Integer) = {
    val await = Try(Await.result(getQsChangeWebuzoPasswordAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Webuzo Change Pass Info asynchronously
   * Retrieves instructions or metadata for changing the Webuzo control panel password.
   *
   * @param id QuickServer ID number 
   * @return Future(void)
   */
  def getQsChangeWebuzoPasswordAsync(id: Integer) = {
      helper.getQsChangeWebuzoPassword(id)
  }

  /**
   * Get QuickServer Order
   * Returns detailed QuickServer information, including credentials, IPs, and available client actions.
   *
   * @param id QuickServer ID number. 
   * @return Quickserver
   */
  def getQsInfo(id: Integer): Option[Quickserver] = {
    val await = Try(Await.result(getQsInfoAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get QuickServer Order asynchronously
   * Returns detailed QuickServer information, including credentials, IPs, and available client actions.
   *
   * @param id QuickServer ID number. 
   * @return Future(Quickserver)
   */
  def getQsInfoAsync(id: Integer): Future[Quickserver] = {
      helper.getQsInfo(id)
  }

  /**
   * Insert CD Information
   * Returns available ISO images that can be mounted in the QuickServer&#x27;s virtual CD drive.
   *
   * @param id QuickServer ID number 
   * @return void
   */
  def getQsInsertCd(id: Integer) = {
    val await = Try(Await.result(getQsInsertCdAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Insert CD Information asynchronously
   * Returns available ISO images that can be mounted in the QuickServer&#x27;s virtual CD drive.
   *
   * @param id QuickServer ID number 
   * @return Future(void)
   */
  def getQsInsertCdAsync(id: Integer) = {
      helper.getQsInsertCd(id)
  }

  /**
   * Get QuickServer Invoices
   * Returns the billing invoices associated with this QuickServer.
   *
   * @param id QuickServer ID number 
   * @return ChargeInvoiceRows
   */
  def getQsInvoices(id: Integer): Option[ChargeInvoiceRows] = {
    val await = Try(Await.result(getQsInvoicesAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get QuickServer Invoices asynchronously
   * Returns the billing invoices associated with this QuickServer.
   *
   * @param id QuickServer ID number 
   * @return Future(ChargeInvoiceRows)
   */
  def getQsInvoicesAsync(id: Integer): Future[ChargeInvoiceRows] = {
      helper.getQsInvoices(id)
  }

  /**
   * List QuickServers
   * Returns the QuickServer services on your account. Use the &#x60;qs_id&#x60; values with &#x60;/qs/{id}&#x60; for details or with the action endpoints (restart, backup, etc.) to manage each server.
   *
   * @return List[QuickserverRow]
   */
  def getQsList(): Option[List[QuickserverRow]] = {
    val await = Try(Await.result(getQsListAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List QuickServers asynchronously
   * Returns the QuickServer services on your account. Use the &#x60;qs_id&#x60; values with &#x60;/qs/{id}&#x60; for details or with the action endpoints (restart, backup, etc.) to manage each server.
   *
   * @return Future(List[QuickserverRow])
   */
  def getQsListAsync(): Future[List[QuickserverRow]] = {
      helper.getQsList()
  }

  /**
   * QuickServer Reinstall OS Options
   * Returns the list of available operating system templates for reinstalling the QuickServer.
   *
   * @param id QuickServer ID number 
   * @return VpsTemplatesList
   */
  def getQsReinstallOs(id: Integer): Option[VpsTemplatesList] = {
    val await = Try(Await.result(getQsReinstallOsAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * QuickServer Reinstall OS Options asynchronously
   * Returns the list of available operating system templates for reinstalling the QuickServer.
   *
   * @param id QuickServer ID number 
   * @return Future(VpsTemplatesList)
   */
  def getQsReinstallOsAsync(id: Integer): Future[VpsTemplatesList] = {
      helper.getQsReinstallOs(id)
  }

  /**
   * Reset QuickServer Password Info
   * Returns information needed before resetting the QuickServer&#x27;s root password.
   *
   * @param id QuickServer ID number 
   * @return void
   */
  def getQsResetPassword(id: Integer) = {
    val await = Try(Await.result(getQsResetPasswordAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Reset QuickServer Password Info asynchronously
   * Returns information needed before resetting the QuickServer&#x27;s root password.
   *
   * @param id QuickServer ID number 
   * @return Future(void)
   */
  def getQsResetPasswordAsync(id: Integer) = {
      helper.getQsResetPassword(id)
  }

  /**
   * Reverse DNS Info
   * Returns the current reverse DNS (PTR record) entries for the QuickServer&#x27;s IP addresses.
   *
   * @param id QuickServer ID number 
   * @return ReverseDnsEntries
   */
  def getQsReverseDns(id: Integer): Option[ReverseDnsEntries] = {
    val await = Try(Await.result(getQsReverseDnsAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Reverse DNS Info asynchronously
   * Returns the current reverse DNS (PTR record) entries for the QuickServer&#x27;s IP addresses.
   *
   * @param id QuickServer ID number 
   * @return Future(ReverseDnsEntries)
   */
  def getQsReverseDnsAsync(id: Integer): Future[ReverseDnsEntries] = {
      helper.getQsReverseDns(id)
  }

  /**
   * VNC Setup Info
   * Returns the current VNC connection information for the QuickServer.
   *
   * @param id QuickServer ID number 
   * @return void
   */
  def getQsSetupVnc(id: Integer) = {
    val await = Try(Await.result(getQsSetupVncAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * VNC Setup Info asynchronously
   * Returns the current VNC connection information for the QuickServer.
   *
   * @param id QuickServer ID number 
   * @return Future(void)
   */
  def getQsSetupVncAsync(id: Integer) = {
      helper.getQsSetupVnc(id)
  }

  /**
   * Get Traffic Usage
   * Returns bandwidth traffic usage data for the QuickServer.
   *
   * @param id QuickServer ID number 
   * @return void
   */
  def getQsTrafficUsage(id: Integer) = {
    val await = Try(Await.result(getQsTrafficUsageAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Traffic Usage asynchronously
   * Returns bandwidth traffic usage data for the QuickServer.
   *
   * @param id QuickServer ID number 
   * @return Future(void)
   */
  def getQsTrafficUsageAsync(id: Integer) = {
      helper.getQsTrafficUsage(id)
  }

  /**
   * Get View Desktop Info
   * Returns remote desktop connection information for the QuickServer.
   *
   * @param id QuickServer ID number 
   * @return void
   */
  def getQsViewDesktop(id: Integer) = {
    val await = Try(Await.result(getQsViewDesktopAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get View Desktop Info asynchronously
   * Returns remote desktop connection information for the QuickServer.
   *
   * @param id QuickServer ID number 
   * @return Future(void)
   */
  def getQsViewDesktopAsync(id: Integer) = {
      helper.getQsViewDesktop(id)
  }

  /**
   * Resend QuickServer Welcome Email
   * Resends the welcome email containing connection details and credentials for the QuickServer order.
   *
   * @param id Quickserver ID 
   * @return TextResponse
   */
  def getQsWelcomeEmail(id: String): Option[TextResponse] = {
    val await = Try(Await.result(getQsWelcomeEmailAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Resend QuickServer Welcome Email asynchronously
   * Resends the welcome email containing connection details and credentials for the QuickServer order.
   *
   * @param id Quickserver ID 
   * @return Future(TextResponse)
   */
  def getQsWelcomeEmailAsync(id: String): Future[TextResponse] = {
      helper.getQsWelcomeEmail(id)
  }

  /**
   * Create QuickServer Backup
   * Creates a backup of the QuickServer. The backup can be downloaded or restored later via the backups endpoints.
   *
   * @param id QuickServer ID number 
   * @return QueueResponse
   */
  def postQsBackup(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(postQsBackupAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Create QuickServer Backup asynchronously
   * Creates a backup of the QuickServer. The backup can be downloaded or restored later via the backups endpoints.
   *
   * @param id QuickServer ID number 
   * @return Future(QueueResponse)
   */
  def postQsBackupAsync(id: Integer): Future[QueueResponse] = {
      helper.postQsBackup(id)
  }

  /**
   * Update QuickServer Hostname
   * Submits a hostname change request for the QuickServer.
   *
   * @param id QuickServer ID number 
   * @return QueueResponse
   */
  def postQsChangeHostname(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(postQsChangeHostnameAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update QuickServer Hostname asynchronously
   * Submits a hostname change request for the QuickServer.
   *
   * @param id QuickServer ID number 
   * @return Future(QueueResponse)
   */
  def postQsChangeHostnameAsync(id: Integer): Future[QueueResponse] = {
      helper.postQsChangeHostname(id)
  }

  /**
   * Change Root Password
   * Triggers a root password reset for the QuickServer.
   *
   * @param id QuickServer ID number 
   * @return QueueResponse
   */
  def postQsChangeRootPassword(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(postQsChangeRootPasswordAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Change Root Password asynchronously
   * Triggers a root password reset for the QuickServer.
   *
   * @param id QuickServer ID number 
   * @return Future(QueueResponse)
   */
  def postQsChangeRootPasswordAsync(id: Integer): Future[QueueResponse] = {
      helper.postQsChangeRootPassword(id)
  }

  /**
   * Change QuickServer Timezone
   * Changes the system timezone on the QuickServer. Use &#x60;GET /qs/{id}/change_timezone&#x60; to list available options first.
   *
   * @param timezone  
   * @param body  
   * @param id QuickServer ID number 
   * @return QueueResponse
   */
  def postQsChangeTimezone(timezone: String, body: TimezoneUpdate, id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(postQsChangeTimezoneAsync(timezone, body, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Change QuickServer Timezone asynchronously
   * Changes the system timezone on the QuickServer. Use &#x60;GET /qs/{id}/change_timezone&#x60; to list available options first.
   *
   * @param timezone  
   * @param body  
   * @param id QuickServer ID number 
   * @return Future(QueueResponse)
   */
  def postQsChangeTimezoneAsync(timezone: String, body: TimezoneUpdate, id: Integer): Future[QueueResponse] = {
      helper.postQsChangeTimezone(timezone, body, id)
  }

  /**
   * Change Webuzo Password
   * Resets the Webuzo control panel password for the QuickServer.
   *
   * @param id QuickServer ID number 
   * @return QueueResponse
   */
  def postQsChangeWebuzoPassword(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(postQsChangeWebuzoPasswordAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Change Webuzo Password asynchronously
   * Resets the Webuzo control panel password for the QuickServer.
   *
   * @param id QuickServer ID number 
   * @return Future(QueueResponse)
   */
  def postQsChangeWebuzoPasswordAsync(id: Integer): Future[QueueResponse] = {
      helper.postQsChangeWebuzoPassword(id)
  }

  /**
   * Insert CD in QuickServer
   * Mounts an ISO image in the QuickServer&#x27;s virtual CD drive. Use &#x60;GET /qs/{id}/insert_cd&#x60; to list available images.
   *
   * @param id QuickServer ID number 
   * @return QueueResponse
   */
  def postQsInsertCd(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(postQsInsertCdAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Insert CD in QuickServer asynchronously
   * Mounts an ISO image in the QuickServer&#x27;s virtual CD drive. Use &#x60;GET /qs/{id}/insert_cd&#x60; to list available images.
   *
   * @param id QuickServer ID number 
   * @return Future(QueueResponse)
   */
  def postQsInsertCdAsync(id: Integer): Future[QueueResponse] = {
      helper.postQsInsertCd(id)
  }

  /**
   * Reinstall QuickServer OS
   * Reinstalls the operating system on the QuickServer. Warning - this will erase all data on the server.
   *
   * @param id QuickServer ID number 
   * @return QueueResponse
   */
  def postQsReinstallOs(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(postQsReinstallOsAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Reinstall QuickServer OS asynchronously
   * Reinstalls the operating system on the QuickServer. Warning - this will erase all data on the server.
   *
   * @param id QuickServer ID number 
   * @return Future(QueueResponse)
   */
  def postQsReinstallOsAsync(id: Integer): Future[QueueResponse] = {
      helper.postQsReinstallOs(id)
  }

  /**
   * Reset QuickServer Password
   * Resets the root password on the QuickServer to a new randomly generated password.
   *
   * @param id QuickServer ID number 
   * @return QueueResponse
   */
  def postQsResetPassword(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(postQsResetPasswordAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Reset QuickServer Password asynchronously
   * Resets the root password on the QuickServer to a new randomly generated password.
   *
   * @param id QuickServer ID number 
   * @return Future(QueueResponse)
   */
  def postQsResetPasswordAsync(id: Integer): Future[QueueResponse] = {
      helper.postQsResetPassword(id)
  }

  /**
   * Update Reverse DNS
   * Updates the reverse DNS (PTR record) entries for the QuickServer&#x27;s IP addresses.
   *
   * @param body  
   * @param ips  
   * @param id QuickServer ID number 
   * @return TextResponse
   */
  def postQsReverseDns(body: ReverseDnsEntries, ips: Map[String, Any], id: Integer): Option[TextResponse] = {
    val await = Try(Await.result(postQsReverseDnsAsync(body, ips, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update Reverse DNS asynchronously
   * Updates the reverse DNS (PTR record) entries for the QuickServer&#x27;s IP addresses.
   *
   * @param body  
   * @param ips  
   * @param id QuickServer ID number 
   * @return Future(TextResponse)
   */
  def postQsReverseDnsAsync(body: ReverseDnsEntries, ips: Map[String, Any], id: Integer): Future[TextResponse] = {
      helper.postQsReverseDns(body, ips, id)
  }

  /**
   * Setup VNC
   * Sets up or refreshes the VNC console connection for the QuickServer.
   *
   * @param id QuickServer ID number 
   * @return QueueResponse
   */
  def postQsSetupVnc(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(postQsSetupVncAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Setup VNC asynchronously
   * Sets up or refreshes the VNC console connection for the QuickServer.
   *
   * @param id QuickServer ID number 
   * @return Future(QueueResponse)
   */
  def postQsSetupVncAsync(id: Integer): Future[QueueResponse] = {
      helper.postQsSetupVnc(id)
  }

  /**
   * Search Traffic Usage
   * Searches and filters the QuickServer&#x27;s bandwidth traffic usage data by date range.
   *
   * @param id QuickServer ID number 
   * @return void
   */
  def postQsTrafficUsage(id: Integer) = {
    val await = Try(Await.result(postQsTrafficUsageAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Search Traffic Usage asynchronously
   * Searches and filters the QuickServer&#x27;s bandwidth traffic usage data by date range.
   *
   * @param id QuickServer ID number 
   * @return Future(void)
   */
  def postQsTrafficUsageAsync(id: Integer) = {
      helper.postQsTrafficUsage(id)
  }

  /**
   * Update View Desktop
   * Updates or refreshes the remote desktop session for the QuickServer.
   *
   * @param id QuickServer ID number 
   * @return void
   */
  def postQsViewDesktop(id: Integer) = {
    val await = Try(Await.result(postQsViewDesktopAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update View Desktop asynchronously
   * Updates or refreshes the remote desktop session for the QuickServer.
   *
   * @param id QuickServer ID number 
   * @return Future(void)
   */
  def postQsViewDesktopAsync(id: Integer) = {
      helper.postQsViewDesktop(id)
  }

  /**
   * Restore QuickServer from Backup
   * Initiates a restore of the QuickServer from a previously created backup. The restore operation overwrites the current disk contents. Use &#x60;GET /qs/{id}/backups&#x60; to retrieve available backup names.
   *
   * @param body QuickServer Restore request 
   * @param backup  
   * @param password  
   * @param id QuickServer ID number 
   * @return QueueResponse
   */
  def postQuickServerRestore(body: RestoreRequest, backup: String, password: String, id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(postQuickServerRestoreAsync(body, backup, password, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Restore QuickServer from Backup asynchronously
   * Initiates a restore of the QuickServer from a previously created backup. The restore operation overwrites the current disk contents. Use &#x60;GET /qs/{id}/backups&#x60; to retrieve available backup names.
   *
   * @param body QuickServer Restore request 
   * @param backup  
   * @param password  
   * @param id QuickServer ID number 
   * @return Future(QueueResponse)
   */
  def postQuickServerRestoreAsync(body: RestoreRequest, backup: String, password: String, id: Integer): Future[QueueResponse] = {
      helper.postQuickServerRestore(body, backup, password, id)
  }

  /**
   * Validate QuickServer Order
   * Validates a QuickServer order and returns pricing or validation errors. Use this before submitting the final order.
   *
   * @return void
   */
  def putQs() = {
    val await = Try(Await.result(putQsAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Validate QuickServer Order asynchronously
   * Validates a QuickServer order and returns pricing or validation errors. Use this before submitting the final order.
   *
   * @return Future(void)
   */
  def putQsAsync() = {
      helper.putQs()
  }

  /**
   * Cancel QuickServer Order
   * Cancels the QuickServer service. The server will be deprovisioned and billing will stop at the end of the current billing cycle.
   *
   * @param id QuickServer ID number 
   * @return inline_response_200_10
   */
  def quickserversCancel(id: Integer): Option[inline_response_200_10] = {
    val await = Try(Await.result(quickserversCancelAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Cancel QuickServer Order asynchronously
   * Cancels the QuickServer service. The server will be deprovisioned and billing will stop at the end of the current billing cycle.
   *
   * @param id QuickServer ID number 
   * @return Future(inline_response_200_10)
   */
  def quickserversCancelAsync(id: Integer): Future[inline_response_200_10] = {
      helper.quickserversCancel(id)
  }

  /**
   * Update QuickServer Order
   * Updates QuickServer metadata or stored settings associated with the order.
   *
   * @param id QuickServer ID number. 
   * @return void
   */
  def updateQsInfo(id: String) = {
    val await = Try(Await.result(updateQsInfoAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update QuickServer Order asynchronously
   * Updates QuickServer metadata or stored settings associated with the order.
   *
   * @param id QuickServer ID number. 
   * @return Future(void)
   */
  def updateQsInfoAsync(id: String) = {
      helper.updateQsInfo(id)
  }

}

class QuickServersApiAsyncHelper(client: TransportClient, config: SwaggerConfig) extends ApiClient(client, config) {

  def addQs()(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/qs/order"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def deleteQsBackup(id: Integer,
    file: String,
    all: Option[String] = None
    )(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/backups")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (file == null) throw new Exception("Missing required parameter 'file' when calling QuickServersApi->deleteQsBackup")

    all match {
      case Some(param) => queryParams += "all" -> param.toString
      case _ => queryParams
    }
    queryParams += "file" -> file.toString

    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def doQsBlockSmtp(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/block_smtp")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def doQsDisableCd(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/disable_cd")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def doQsDisableQuota(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/disable_quota")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def doQsEjectCd(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/eject_cd")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def doQsEnableQuota(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/enable_quota")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def doQsRestart(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/restart")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def doQsStart(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/start")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def doQsStop(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/stop")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def downloadQsBackup(body: IdBackupsBody,
    file: String,
    id: Integer,
    all: Option[String] = None
    )(implicit reader: ClientResponseReader[inline_response_200_11], writer: RequestWriter[IdBackupsBody]): Future[inline_response_200_11] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/backups")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling QuickServersApi->downloadQsBackup")
    if (file == null) throw new Exception("Missing required parameter 'file' when calling QuickServersApi->downloadQsBackup")

    all match {
      case Some(param) => queryParams += "all" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("PATCH", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getNewQs()(implicit reader: ClientResponseReader[QuickserverOrder]): Future[QuickserverOrder] = {
    // create path and map variables
    val path = (addFmt("/qs/order"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getQsBackups(id: Integer,
    all: Option[String] = None
    )(implicit reader: ClientResponseReader[VpsBackupRows]): Future[VpsBackupRows] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/backups")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    all match {
      case Some(param) => queryParams += "all" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getQsChangeHostname(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/change_hostname")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getQsChangeRootPassword(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/change_root_password")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getQsChangeTimezone(id: Integer)(implicit reader: ClientResponseReader[List[String]]): Future[List[String]] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/change_timezone")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getQsChangeWebuzoPassword(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/change_webuzo_password")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getQsInfo(id: Integer)(implicit reader: ClientResponseReader[Quickserver]): Future[Quickserver] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getQsInsertCd(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/insert_cd")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getQsInvoices(id: Integer)(implicit reader: ClientResponseReader[ChargeInvoiceRows]): Future[ChargeInvoiceRows] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/invoices")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getQsList()(implicit reader: ClientResponseReader[List[QuickserverRow]]): Future[List[QuickserverRow]] = {
    // create path and map variables
    val path = (addFmt("/qs"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getQsReinstallOs(id: Integer)(implicit reader: ClientResponseReader[VpsTemplatesList]): Future[VpsTemplatesList] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/reinstall_os")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getQsResetPassword(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/reset_password")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getQsReverseDns(id: Integer)(implicit reader: ClientResponseReader[ReverseDnsEntries]): Future[ReverseDnsEntries] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/reverse_dns")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getQsSetupVnc(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/setup_vnc")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getQsTrafficUsage(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/traffic_usage")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getQsViewDesktop(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/view_desktop")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getQsWelcomeEmail(id: String)(implicit reader: ClientResponseReader[TextResponse]): Future[TextResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/welcome_email")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (id == null) throw new Exception("Missing required parameter 'id' when calling QuickServersApi->getQsWelcomeEmail")


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postQsBackup(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/backup")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postQsChangeHostname(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/change_hostname")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postQsChangeRootPassword(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/change_root_password")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postQsChangeTimezone(timezone: String,
    body: TimezoneUpdate,
    id: Integer)(implicit reader: ClientResponseReader[QueueResponse], writer: RequestWriter[TimezoneUpdate]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/change_timezone")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (timezone == null) throw new Exception("Missing required parameter 'timezone' when calling QuickServersApi->postQsChangeTimezone")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling QuickServersApi->postQsChangeTimezone")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postQsChangeWebuzoPassword(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/change_webuzo_password")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postQsInsertCd(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/insert_cd")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postQsReinstallOs(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/reinstall_os")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postQsResetPassword(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/reset_password")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postQsReverseDns(body: ReverseDnsEntries,
    ips: Map[String, Any],
    id: Integer)(implicit reader: ClientResponseReader[TextResponse], writer: RequestWriter[ReverseDnsEntries]): Future[TextResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/reverse_dns")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling QuickServersApi->postQsReverseDns")
    if (ips == null) throw new Exception("Missing required parameter 'ips' when calling QuickServersApi->postQsReverseDns")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postQsSetupVnc(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/setup_vnc")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postQsTrafficUsage(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/traffic_usage")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postQsViewDesktop(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/view_desktop")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postQuickServerRestore(body: RestoreRequest,
    backup: String,
    password: String,
    id: Integer)(implicit reader: ClientResponseReader[QueueResponse], writer: RequestWriter[RestoreRequest]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}/restore")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling QuickServersApi->postQuickServerRestore")
    if (backup == null) throw new Exception("Missing required parameter 'backup' when calling QuickServersApi->postQuickServerRestore")

    if (password == null) throw new Exception("Missing required parameter 'password' when calling QuickServersApi->postQuickServerRestore")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def putQs()(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/qs/order"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("PUT", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def quickserversCancel(id: Integer)(implicit reader: ClientResponseReader[inline_response_200_10]): Future[inline_response_200_10] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateQsInfo(id: String)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/qs/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (id == null) throw new Exception("Missing required parameter 'id' when calling QuickServersApi->updateQsInfo")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }


}
