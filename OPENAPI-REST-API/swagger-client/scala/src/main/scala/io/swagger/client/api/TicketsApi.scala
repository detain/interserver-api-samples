/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * OpenAPI spec version: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */
package io.swagger.client.api

import java.text.SimpleDateFormat

import java.math.BigDecimal
import io.swagger.client.model.CloseTicketResponseSchema
import io.swagger.client.model.ReplyTicketRequest
import io.swagger.client.model.ReplyTicketResponseSchema
import io.swagger.client.model.TicketNew
import io.swagger.client.model.TicketNewResponse
import io.swagger.client.model.Tickets
import io.swagger.client.model.UpdateTicket
import io.swagger.client.model.UpdateTicketResponseSchema
import io.swagger.client.model.ViewTicketResponse
import io.swagger.client.model.inline_response_401
import io.swagger.client.{ApiInvoker, ApiException}

import com.sun.jersey.multipart.FormDataMultiPart
import com.sun.jersey.multipart.file.FileDataBodyPart

import javax.ws.rs.core.MediaType

import java.io.File
import java.util.Date
import java.util.TimeZone

import scala.collection.mutable.HashMap

import com.wordnik.swagger.client._
import scala.concurrent.Future
import collection.mutable

import java.net.URI

import com.wordnik.swagger.client.ClientResponseReaders.Json4sFormatsReader._
import com.wordnik.swagger.client.RequestWriters.Json4sFormatsWriter._

import scala.concurrent.ExecutionContext.Implicits.global
import scala.concurrent._
import scala.concurrent.duration._
import scala.util.{Failure, Success, Try}

import org.json4s._

class TicketsApi(
  val defBasePath: String = "https://my.interserver.net/apiv2",
  defApiInvoker: ApiInvoker = ApiInvoker
) {
  private lazy val dateTimeFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSSZ")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  private val dateFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  implicit val formats = new org.json4s.DefaultFormats {
    override def dateFormatter = dateTimeFormatter
  }
  implicit val stringReader: ClientResponseReader[String] = ClientResponseReaders.StringReader
  implicit val unitReader: ClientResponseReader[Unit] = ClientResponseReaders.UnitReader
  implicit val jvalueReader: ClientResponseReader[JValue] = ClientResponseReaders.JValueReader
  implicit val jsonReader: ClientResponseReader[Nothing] = JsonFormatsReader
  implicit val stringWriter: RequestWriter[String] = RequestWriters.StringWriter
  implicit val jsonWriter: RequestWriter[Nothing] = JsonFormatsWriter

  var basePath: String = defBasePath
  var apiInvoker: ApiInvoker = defApiInvoker

  def addHeader(key: String, value: String): mutable.HashMap[String, String] = {
    apiInvoker.defaultHeaders += key -> value
  }

  val config: SwaggerConfig = SwaggerConfig.forUrl(new URI(defBasePath))
  val client = new RestClient(config)
  val helper = new TicketsApiAsyncHelper(client, config)

  /**
   * Create New Ticket
   * Creates a new support ticket. Optionally link it to a specific service by providing the service ID and module.
   *
   * @param body  
   * @param subject  
   * @param body  
   * @param service_id  
   * @param service_module  
   * @return TicketNewResponse
   */
  def addNewTicket(body: TicketNew, subject: String, body: String, service_id: Integer, service_module: String): Option[TicketNewResponse] = {
    val await = Try(Await.result(addNewTicketAsync(body, subject, body, service_id, service_module), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Create New Ticket asynchronously
   * Creates a new support ticket. Optionally link it to a specific service by providing the service ID and module.
   *
   * @param body  
   * @param subject  
   * @param body  
   * @param service_id  
   * @param service_module  
   * @return Future(TicketNewResponse)
   */
  def addNewTicketAsync(body: TicketNew, subject: String, body: String, service_id: Integer, service_module: String): Future[TicketNewResponse] = {
      helper.addNewTicket(body, subject, body, service_id, service_module)
  }

  /**
   * Close Ticket
   * Closes the specified support ticket. Closed tickets can still be viewed but will no longer appear in the active inbox.
   *
   * @param id Ticket ID 
   * @return CloseTicketResponseSchema
   */
  def closeTicket(id: String): Option[CloseTicketResponseSchema] = {
    val await = Try(Await.result(closeTicketAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Close Ticket asynchronously
   * Closes the specified support ticket. Closed tickets can still be viewed but will no longer appear in the active inbox.
   *
   * @param id Ticket ID 
   * @return Future(CloseTicketResponseSchema)
   */
  def closeTicketAsync(id: String): Future[CloseTicketResponseSchema] = {
      helper.closeTicket(id)
  }

  /**
   * Close Ticket
   * Closes the support ticket.
   *
   * @param id Ticket ID number. 
   * @return ViewTicketResponse
   */
  def deleteTicketInfo(id: BigDecimal): Option[ViewTicketResponse] = {
    val await = Try(Await.result(deleteTicketInfoAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Close Ticket asynchronously
   * Closes the support ticket.
   *
   * @param id Ticket ID number. 
   * @return Future(ViewTicketResponse)
   */
  def deleteTicketInfoAsync(id: BigDecimal): Future[ViewTicketResponse] = {
      helper.deleteTicketInfo(id)
  }

  /**
   * Gets Information for creating a new ticket.
   * Returns the form data needed to create a new support ticket, such as available departments and service categories.
   *
   * @return void
   */
  def getNewTicket() = {
    val await = Try(Await.result(getNewTicketAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Gets Information for creating a new ticket. asynchronously
   * Returns the form data needed to create a new support ticket, such as available departments and service categories.
   *
   * @return Future(void)
   */
  def getNewTicketAsync() = {
      helper.getNewTicket()
  }

  /**
   * Get Ticket Information
   * Returns the full details of a support ticket including its history of replies.
   *
   * @param id Ticket ID number. 
   * @return ViewTicketResponse
   */
  def getTicketInfo(id: BigDecimal): Option[ViewTicketResponse] = {
    val await = Try(Await.result(getTicketInfoAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Ticket Information asynchronously
   * Returns the full details of a support ticket including its history of replies.
   *
   * @param id Ticket ID number. 
   * @return Future(ViewTicketResponse)
   */
  def getTicketInfoAsync(id: BigDecimal): Future[ViewTicketResponse] = {
      helper.getTicketInfo(id)
  }

  /**
   * List Support Tickets
   * Returns a paginated list of support tickets on the account. Filter by status and time period.
   *
   * @param page Page number for paginated results. (optional, default to 1)
   * @param period How far back to show tickets from. Value is in days. (optional, default to 30)
   * @param view The status of tickets to view. Possible values are Open, Closed, On Hold, and In Progress.  If not specified it will show all types. (optional)
   * @return Tickets
   */
  def getTicketsList(page: Option[Integer] = Option(1), period: Option[String] = Option("30"), view: Option[String] = None): Option[Tickets] = {
    val await = Try(Await.result(getTicketsListAsync(page, period, view), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List Support Tickets asynchronously
   * Returns a paginated list of support tickets on the account. Filter by status and time period.
   *
   * @param page Page number for paginated results. (optional, default to 1)
   * @param period How far back to show tickets from. Value is in days. (optional, default to 30)
   * @param view The status of tickets to view. Possible values are Open, Closed, On Hold, and In Progress.  If not specified it will show all types. (optional)
   * @return Future(Tickets)
   */
  def getTicketsListAsync(page: Option[Integer] = Option(1), period: Option[String] = Option("30"), view: Option[String] = None): Future[Tickets] = {
      helper.getTicketsList(page, period, view)
  }

  /**
   * Reply To Ticket
   * Adds a reply to an existing support ticket.
   *
   * @param id Ticket ID number. 
   * @return ViewTicketResponse
   */
  def postTicketInfo(id: BigDecimal): Option[ViewTicketResponse] = {
    val await = Try(Await.result(postTicketInfoAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Reply To Ticket asynchronously
   * Adds a reply to an existing support ticket.
   *
   * @param id Ticket ID number. 
   * @return Future(ViewTicketResponse)
   */
  def postTicketInfoAsync(id: BigDecimal): Future[ViewTicketResponse] = {
      helper.postTicketInfo(id)
  }

  /**
   * Search Support Tickets
   * Searches support tickets by email, subject, or ticket mask ID.
   *
   * @return Tickets
   */
  def postTicketsList(): Option[Tickets] = {
    val await = Try(Await.result(postTicketsListAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Search Support Tickets asynchronously
   * Searches support tickets by email, subject, or ticket mask ID.
   *
   * @return Future(Tickets)
   */
  def postTicketsListAsync(): Future[Tickets] = {
      helper.postTicketsList()
  }

  /**
   * Update Ticket
   * Updates a support ticket&#x27;s properties such as subject or status.
   *
   * @param id Ticket ID number. 
   * @return ViewTicketResponse
   */
  def putTicketInfo(id: BigDecimal): Option[ViewTicketResponse] = {
    val await = Try(Await.result(putTicketInfoAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update Ticket asynchronously
   * Updates a support ticket&#x27;s properties such as subject or status.
   *
   * @param id Ticket ID number. 
   * @return Future(ViewTicketResponse)
   */
  def putTicketInfoAsync(id: BigDecimal): Future[ViewTicketResponse] = {
      helper.putTicketInfo(id)
  }

  /**
   * Reply Ticket
   * Posts a reply to an existing support ticket thread.
   *
   * @param id The ticket ID number. 
   * @param body  (optional)
   * @param content  (optional)
   * @return ReplyTicketResponseSchema
   */
  def replyTicket(id: BigDecimal, body: Option[ReplyTicketRequest] = None, content: Option[String] = None): Option[ReplyTicketResponseSchema] = {
    val await = Try(Await.result(replyTicketAsync(id, body, content), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Reply Ticket asynchronously
   * Posts a reply to an existing support ticket thread.
   *
   * @param id The ticket ID number. 
   * @param body  (optional)
   * @param content  (optional)
   * @return Future(ReplyTicketResponseSchema)
   */
  def replyTicketAsync(id: BigDecimal, body: Option[ReplyTicketRequest] = None, content: Option[String] = None): Future[ReplyTicketResponseSchema] = {
      helper.replyTicket(id, body, content)
  }

  /**
   * Update Ticket
   * Updates a support ticket&#x27;s subject or body content.
   *
   * @param id The ticket ID number. 
   * @param body  (optional)
   * @param ip  (optional)
   * @param ip_address  (optional)
   * @param customer_server_access  (optional)
   * @param root_password  (optional)
   * @param sudo_username  (optional)
   * @param sudo_password  (optional)
   * @param port  (optional)
   * @return UpdateTicketResponseSchema
   */
  def updateTicketInfo(id: BigDecimal, body: Option[UpdateTicket] = None, ip: Option[String] = None, ip_address: Option[String] = None, customer_server_access: Option[String] = None, root_password: Option[String] = None, sudo_username: Option[String] = None, sudo_password: Option[String] = None, port: Option[Integer] = None): Option[UpdateTicketResponseSchema] = {
    val await = Try(Await.result(updateTicketInfoAsync(id, body, ip, ip_address, customer_server_access, root_password, sudo_username, sudo_password, port), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update Ticket asynchronously
   * Updates a support ticket&#x27;s subject or body content.
   *
   * @param id The ticket ID number. 
   * @param body  (optional)
   * @param ip  (optional)
   * @param ip_address  (optional)
   * @param customer_server_access  (optional)
   * @param root_password  (optional)
   * @param sudo_username  (optional)
   * @param sudo_password  (optional)
   * @param port  (optional)
   * @return Future(UpdateTicketResponseSchema)
   */
  def updateTicketInfoAsync(id: BigDecimal, body: Option[UpdateTicket] = None, ip: Option[String] = None, ip_address: Option[String] = None, customer_server_access: Option[String] = None, root_password: Option[String] = None, sudo_username: Option[String] = None, sudo_password: Option[String] = None, port: Option[Integer] = None): Future[UpdateTicketResponseSchema] = {
      helper.updateTicketInfo(id, body, ip, ip_address, customer_server_access, root_password, sudo_username, sudo_password, port)
  }

}

class TicketsApiAsyncHelper(client: TransportClient, config: SwaggerConfig) extends ApiClient(client, config) {

  def addNewTicket(body: TicketNew,
    subject: String,
    body: String,
    service_id: Integer,
    service_module: String)(implicit reader: ClientResponseReader[TicketNewResponse], writer: RequestWriter[TicketNew]): Future[TicketNewResponse] = {
    // create path and map variables
    val path = (addFmt("/tickets/new"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling TicketsApi->addNewTicket")
    if (subject == null) throw new Exception("Missing required parameter 'subject' when calling TicketsApi->addNewTicket")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling TicketsApi->addNewTicket")

    if (service_module == null) throw new Exception("Missing required parameter 'service_module' when calling TicketsApi->addNewTicket")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def closeTicket(id: String)(implicit reader: ClientResponseReader[CloseTicketResponseSchema]): Future[CloseTicketResponseSchema] = {
    // create path and map variables
    val path = (addFmt("/tickets/{id}/close")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (id == null) throw new Exception("Missing required parameter 'id' when calling TicketsApi->closeTicket")


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def deleteTicketInfo(id: BigDecimal)(implicit reader: ClientResponseReader[ViewTicketResponse]): Future[ViewTicketResponse] = {
    // create path and map variables
    val path = (addFmt("/tickets/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getNewTicket()(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/tickets/new"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getTicketInfo(id: BigDecimal)(implicit reader: ClientResponseReader[ViewTicketResponse]): Future[ViewTicketResponse] = {
    // create path and map variables
    val path = (addFmt("/tickets/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getTicketsList(page: Option[Integer] = Option(1),
    period: Option[String] = Option("30"),
    view: Option[String] = None
    )(implicit reader: ClientResponseReader[Tickets]): Future[Tickets] = {
    // create path and map variables
    val path = (addFmt("/tickets"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    page match {
      case Some(param) => queryParams += "page" -> param.toString
      case _ => queryParams
    }
    period match {
      case Some(param) => queryParams += "period" -> param.toString
      case _ => queryParams
    }
    view match {
      case Some(param) => queryParams += "view" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postTicketInfo(id: BigDecimal)(implicit reader: ClientResponseReader[ViewTicketResponse]): Future[ViewTicketResponse] = {
    // create path and map variables
    val path = (addFmt("/tickets/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postTicketsList()(implicit reader: ClientResponseReader[Tickets]): Future[Tickets] = {
    // create path and map variables
    val path = (addFmt("/tickets"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def putTicketInfo(id: BigDecimal)(implicit reader: ClientResponseReader[ViewTicketResponse]): Future[ViewTicketResponse] = {
    // create path and map variables
    val path = (addFmt("/tickets/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("PUT", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def replyTicket(id: BigDecimal,
    body: Option[ReplyTicketRequest] = None,
    content: Option[String] = None
    )(implicit reader: ClientResponseReader[ReplyTicketResponseSchema], writer: RequestWriter[Option[ReplyTicketRequest]]): Future[ReplyTicketResponseSchema] = {
    // create path and map variables
    val path = (addFmt("/tickets/{id}/reply")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateTicketInfo(id: BigDecimal,
    body: Option[UpdateTicket] = None,
    ip: Option[String] = None,
    ip_address: Option[String] = None,
    customer_server_access: Option[String] = None,
    root_password: Option[String] = None,
    sudo_username: Option[String] = None,
    sudo_password: Option[String] = None,
    port: Option[Integer] = None
    )(implicit reader: ClientResponseReader[UpdateTicketResponseSchema], writer: RequestWriter[Option[UpdateTicket]]): Future[UpdateTicketResponseSchema] = {
    // create path and map variables
    val path = (addFmt("/tickets/{id}/update")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }


}
