/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * OpenAPI spec version: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */
package io.swagger.client.api

import java.text.SimpleDateFormat

import io.swagger.client.model.ChargeInvoiceRows
import io.swagger.client.model.HostnameObject
import io.swagger.client.model.IdBackupsBody2
import io.swagger.client.model.PasswordRequest
import io.swagger.client.model.QueueResponse
import io.swagger.client.model.RestoreRequest
import io.swagger.client.model.ReverseDnsEntries
import io.swagger.client.model.ServiceOrderPostResponse
import io.swagger.client.model.SuccessTextResponse
import io.swagger.client.model.TemplateRequest
import io.swagger.client.model.TextResponse
import io.swagger.client.model.TimezoneUpdate
import io.swagger.client.model.UrlRequest
import io.swagger.client.model.Vps
import io.swagger.client.model.VpsBackupRows
import io.swagger.client.model.VpsOrder
import io.swagger.client.model.VpsOrderPostRequest
import io.swagger.client.model.VpsOrderPutRequest
import io.swagger.client.model.VpsOrderPutResponse
import io.swagger.client.model.VpsRow
import io.swagger.client.model.VpsTemplatesList
import io.swagger.client.model.VpsTrafficResponse
import io.swagger.client.model.inline_response_200_11
import io.swagger.client.model.inline_response_200_22
import io.swagger.client.model.inline_response_401
import io.swagger.client.{ApiInvoker, ApiException}

import com.sun.jersey.multipart.FormDataMultiPart
import com.sun.jersey.multipart.file.FileDataBodyPart

import javax.ws.rs.core.MediaType

import java.io.File
import java.util.Date
import java.util.TimeZone

import scala.collection.mutable.HashMap

import com.wordnik.swagger.client._
import scala.concurrent.Future
import collection.mutable

import java.net.URI

import com.wordnik.swagger.client.ClientResponseReaders.Json4sFormatsReader._
import com.wordnik.swagger.client.RequestWriters.Json4sFormatsWriter._

import scala.concurrent.ExecutionContext.Implicits.global
import scala.concurrent._
import scala.concurrent.duration._
import scala.util.{Failure, Success, Try}

import org.json4s._

class VPSApi(
  val defBasePath: String = "https://my.interserver.net/apiv2",
  defApiInvoker: ApiInvoker = ApiInvoker
) {
  private lazy val dateTimeFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSSZ")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  private val dateFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  implicit val formats = new org.json4s.DefaultFormats {
    override def dateFormatter = dateTimeFormatter
  }
  implicit val stringReader: ClientResponseReader[String] = ClientResponseReaders.StringReader
  implicit val unitReader: ClientResponseReader[Unit] = ClientResponseReaders.UnitReader
  implicit val jvalueReader: ClientResponseReader[JValue] = ClientResponseReaders.JValueReader
  implicit val jsonReader: ClientResponseReader[Nothing] = JsonFormatsReader
  implicit val stringWriter: RequestWriter[String] = RequestWriters.StringWriter
  implicit val jsonWriter: RequestWriter[Nothing] = JsonFormatsWriter

  var basePath: String = defBasePath
  var apiInvoker: ApiInvoker = defApiInvoker

  def addHeader(key: String, value: String): mutable.HashMap[String, String] = {
    apiInvoker.defaultHeaders += key -> value
  }

  val config: SwaggerConfig = SwaggerConfig.forUrl(new URI(defBasePath))
  val client = new RestClient(config)
  val helper = new VPSApiAsyncHelper(client, config)

  /**
   * Place VPS Order
   * Places an order for a new VPS. Use &#x60;PUT /vps/order&#x60; to validate the order first.
   *
   * @param body  (optional)
   * @param osDistro  (optional)
   * @param slices  (optional)
   * @param vpsPlatform  (optional)
   * @param controlpanel  (optional)
   * @param period  (optional)
   * @param location  (optional)
   * @param osVersion  (optional)
   * @param hostname  (optional)
   * @param coupon  (optional)
   * @param rootpass  (optional)
   * @param comment  (optional)
   * @return ServiceOrderPostResponse
   */
  def addVps(body: Option[VpsOrderPostRequest] = None, osDistro: Option[String] = None, slices: Option[Integer] = None, vpsPlatform: Option[String] = None, controlpanel: Option[String] = None, period: Option[Integer] = None, location: Option[Integer] = None, osVersion: Option[String] = None, hostname: Option[String] = None, coupon: Option[String] = None, rootpass: Option[String] = None, comment: Option[String] = None): Option[ServiceOrderPostResponse] = {
    val await = Try(Await.result(addVpsAsync(body, osDistro, slices, vpsPlatform, controlpanel, period, location, osVersion, hostname, coupon, rootpass, comment), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Place VPS Order asynchronously
   * Places an order for a new VPS. Use &#x60;PUT /vps/order&#x60; to validate the order first.
   *
   * @param body  (optional)
   * @param osDistro  (optional)
   * @param slices  (optional)
   * @param vpsPlatform  (optional)
   * @param controlpanel  (optional)
   * @param period  (optional)
   * @param location  (optional)
   * @param osVersion  (optional)
   * @param hostname  (optional)
   * @param coupon  (optional)
   * @param rootpass  (optional)
   * @param comment  (optional)
   * @return Future(ServiceOrderPostResponse)
   */
  def addVpsAsync(body: Option[VpsOrderPostRequest] = None, osDistro: Option[String] = None, slices: Option[Integer] = None, vpsPlatform: Option[String] = None, controlpanel: Option[String] = None, period: Option[Integer] = None, location: Option[Integer] = None, osVersion: Option[String] = None, hostname: Option[String] = None, coupon: Option[String] = None, rootpass: Option[String] = None, comment: Option[String] = None): Future[ServiceOrderPostResponse] = {
      helper.addVps(body, osDistro, slices, vpsPlatform, controlpanel, period, location, osVersion, hostname, coupon, rootpass, comment)
  }

  /**
   * Delete VPS Backup
   * Permanently removes the specified backup file from storage. Use &#x60;GET /vps/{id}/backups&#x60; to list available backup filenames before deleting.
   *
   * @param id VPS ID number 
   * @param file The backup filename to delete. 
   * @param all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given VPS. (optional)
   * @return SuccessTextResponse
   */
  def deleteVpsBackup(id: Integer, file: String, all: Option[String] = None): Option[SuccessTextResponse] = {
    val await = Try(Await.result(deleteVpsBackupAsync(id, file, all), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Delete VPS Backup asynchronously
   * Permanently removes the specified backup file from storage. Use &#x60;GET /vps/{id}/backups&#x60; to list available backup filenames before deleting.
   *
   * @param id VPS ID number 
   * @param file The backup filename to delete. 
   * @param all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given VPS. (optional)
   * @return Future(SuccessTextResponse)
   */
  def deleteVpsBackupAsync(id: Integer, file: String, all: Option[String] = None): Future[SuccessTextResponse] = {
      helper.deleteVpsBackup(id, file, all)
  }

  /**
   * Blocks SMTP
   * Blocks outbound SMTP (port 25) on the VPS to prevent spam. Useful for VPS that do not need to send email.
   *
   * @param id VPS ID number. 
   * @return QueueResponse
   */
  def doVpsBlockSmtp(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(doVpsBlockSmtpAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Blocks SMTP asynchronously
   * Blocks outbound SMTP (port 25) on the VPS to prevent spam. Useful for VPS that do not need to send email.
   *
   * @param id VPS ID number. 
   * @return Future(QueueResponse)
   */
  def doVpsBlockSmtpAsync(id: Integer): Future[QueueResponse] = {
      helper.doVpsBlockSmtp(id)
  }

  /**
   * Disable CD Drive
   * Disables the virtual CD drive on the VPS.
   *
   * @param id VPS ID number. 
   * @return QueueResponse
   */
  def doVpsDisableCd(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(doVpsDisableCdAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Disable CD Drive asynchronously
   * Disables the virtual CD drive on the VPS.
   *
   * @param id VPS ID number. 
   * @return Future(QueueResponse)
   */
  def doVpsDisableCdAsync(id: Integer): Future[QueueResponse] = {
      helper.doVpsDisableCd(id)
  }

  /**
   * Disable Quotas
   * Disables disk quota enforcement on the VPS.
   *
   * @param id VPS ID number. 
   * @return QueueResponse
   */
  def doVpsDisableQuota(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(doVpsDisableQuotaAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Disable Quotas asynchronously
   * Disables disk quota enforcement on the VPS.
   *
   * @param id VPS ID number. 
   * @return Future(QueueResponse)
   */
  def doVpsDisableQuotaAsync(id: Integer): Future[QueueResponse] = {
      helper.doVpsDisableQuota(id)
  }

  /**
   * Eject CD Drive
   * Ejects the virtual CD from the VPS CD drive.
   *
   * @param id VPS ID number. 
   * @return QueueResponse
   */
  def doVpsEjectCd(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(doVpsEjectCdAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Eject CD Drive asynchronously
   * Ejects the virtual CD from the VPS CD drive.
   *
   * @param id VPS ID number. 
   * @return Future(QueueResponse)
   */
  def doVpsEjectCdAsync(id: Integer): Future[QueueResponse] = {
      helper.doVpsEjectCd(id)
  }

  /**
   * Enable Quotas
   * Enables disk quota enforcement on the VPS.
   *
   * @param id VPS ID number. 
   * @return QueueResponse
   */
  def doVpsEnableQuota(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(doVpsEnableQuotaAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Enable Quotas asynchronously
   * Enables disk quota enforcement on the VPS.
   *
   * @param id VPS ID number. 
   * @return Future(QueueResponse)
   */
  def doVpsEnableQuotaAsync(id: Integer): Future[QueueResponse] = {
      helper.doVpsEnableQuota(id)
  }

  /**
   * Restart VPS
   * Restarts the VPS.
   *
   * @param id VPS ID number. 
   * @return QueueResponse
   */
  def doVpsRestart(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(doVpsRestartAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Restart VPS asynchronously
   * Restarts the VPS.
   *
   * @param id VPS ID number. 
   * @return Future(QueueResponse)
   */
  def doVpsRestartAsync(id: Integer): Future[QueueResponse] = {
      helper.doVpsRestart(id)
  }

  /**
   * Start VPS
   * Powers on the VPS.
   *
   * @param id VPS ID number. 
   * @return QueueResponse
   */
  def doVpsStart(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(doVpsStartAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Start VPS asynchronously
   * Powers on the VPS.
   *
   * @param id VPS ID number. 
   * @return Future(QueueResponse)
   */
  def doVpsStartAsync(id: Integer): Future[QueueResponse] = {
      helper.doVpsStart(id)
  }

  /**
   * Stop VPS
   * Powers off the VPS.
   *
   * @param id VPS ID number. 
   * @return QueueResponse
   */
  def doVpsStop(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(doVpsStopAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Stop VPS asynchronously
   * Powers off the VPS.
   *
   * @param id VPS ID number. 
   * @return Future(QueueResponse)
   */
  def doVpsStopAsync(id: Integer): Future[QueueResponse] = {
      helper.doVpsStop(id)
  }

  /**
   * Download VPS Backup
   * Generates a pre-signed download URL for the specified backup file. The URL is valid for 24 hours. Use &#x60;GET /vps/{id}/backups&#x60; to list available backup filenames.
   *
   * @param body  
   * @param file  
   * @param id VPS ID number 
   * @param all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given VPS. (optional)
   * @return inline_response_200_11
   */
  def downloadVpsBackup(body: IdBackupsBody2, file: String, id: Integer, all: Option[String] = None): Option[inline_response_200_11] = {
    val await = Try(Await.result(downloadVpsBackupAsync(body, file, id, all), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Download VPS Backup asynchronously
   * Generates a pre-signed download URL for the specified backup file. The URL is valid for 24 hours. Use &#x60;GET /vps/{id}/backups&#x60; to list available backup filenames.
   *
   * @param body  
   * @param file  
   * @param id VPS ID number 
   * @param all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given VPS. (optional)
   * @return Future(inline_response_200_11)
   */
  def downloadVpsBackupAsync(body: IdBackupsBody2, file: String, id: Integer, all: Option[String] = None): Future[inline_response_200_11] = {
      helper.downloadVpsBackup(body, file, id, all)
  }

  /**
   * VPS Ordering Information
   * Retrieves available VPS configurations, OS templates, and pricing for ordering.
   *
   * @return VpsOrder
   */
  def getNewVps(): Option[VpsOrder] = {
    val await = Try(Await.result(getNewVpsAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * VPS Ordering Information asynchronously
   * Retrieves available VPS configurations, OS templates, and pricing for ordering.
   *
   * @return Future(VpsOrder)
   */
  def getNewVpsAsync(): Future[VpsOrder] = {
      helper.getNewVps()
  }

  /**
   * Get VPS Backups List
   * Returns the available backups for the VPS across all storage systems (Swift, MinIO, ZFS). Use the backup &#x60;name&#x60; value with &#x60;PATCH /vps/{id}/backups&#x60; to download or &#x60;DELETE /vps/{id}/backups&#x60; to remove a backup. Use &#x60;POST /vps/{id}/restore&#x60; to restore from a backup.
   *
   * @param id VPS ID number 
   * @param all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given VPS. (optional)
   * @return VpsBackupRows
   */
  def getVpsBackups(id: Integer, all: Option[String] = None): Option[VpsBackupRows] = {
    val await = Try(Await.result(getVpsBackupsAsync(id, all), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get VPS Backups List asynchronously
   * Returns the available backups for the VPS across all storage systems (Swift, MinIO, ZFS). Use the backup &#x60;name&#x60; value with &#x60;PATCH /vps/{id}/backups&#x60; to download or &#x60;DELETE /vps/{id}/backups&#x60; to remove a backup. Use &#x60;POST /vps/{id}/restore&#x60; to restore from a backup.
   *
   * @param id VPS ID number 
   * @param all Set to &#x60;1&#x60; to list all backups across all services, not just the ones for the given VPS. (optional)
   * @return Future(VpsBackupRows)
   */
  def getVpsBackupsAsync(id: Integer, all: Option[String] = None): Future[VpsBackupRows] = {
      helper.getVpsBackups(id, all)
  }

  /**
   * HD Space Addon Info
   * Returns available hard drive space addon options and pricing for the VPS.
   *
   * @param id VPS ID number 
   * @return void
   */
  def getVpsBuyHdSpace(id: Integer) = {
    val await = Try(Await.result(getVpsBuyHdSpaceAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * HD Space Addon Info asynchronously
   * Returns available hard drive space addon options and pricing for the VPS.
   *
   * @param id VPS ID number 
   * @return Future(void)
   */
  def getVpsBuyHdSpaceAsync(id: Integer) = {
      helper.getVpsBuyHdSpace(id)
  }

  /**
   * Additional IP Addon Info
   * Returns available additional IP address options and pricing for the VPS.
   *
   * @param id VPS ID number 
   * @return void
   */
  def getVpsBuyIp(id: Integer) = {
    val await = Try(Await.result(getVpsBuyIpAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Additional IP Addon Info asynchronously
   * Returns available additional IP address options and pricing for the VPS.
   *
   * @param id VPS ID number 
   * @return Future(void)
   */
  def getVpsBuyIpAsync(id: Integer) = {
      helper.getVpsBuyIp(id)
  }

  /**
   * Get Timezone Info
   * Returns the list of available timezones that can be set on the VPS.
   *
   * @param id VPS ID number 
   * @return List[String]
   */
  def getVpsChangeTimezone(id: Integer): Option[List[String]] = {
    val await = Try(Await.result(getVpsChangeTimezoneAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Timezone Info asynchronously
   * Returns the list of available timezones that can be set on the VPS.
   *
   * @param id VPS ID number 
   * @return Future(List[String])
   */
  def getVpsChangeTimezoneAsync(id: Integer): Future[List[String]] = {
      helper.getVpsChangeTimezone(id)
  }

  /**
   * Get VPS Order
   * Returns detailed information about a specific VPS including its IPs, hostname, OS, and resource allocation.
   *
   * @param id VPS ID number 
   * @return Vps
   */
  def getVpsInfo(id: Integer): Option[Vps] = {
    val await = Try(Await.result(getVpsInfoAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get VPS Order asynchronously
   * Returns detailed information about a specific VPS including its IPs, hostname, OS, and resource allocation.
   *
   * @param id VPS ID number 
   * @return Future(Vps)
   */
  def getVpsInfoAsync(id: Integer): Future[Vps] = {
      helper.getVpsInfo(id)
  }

  /**
   * Get VPS Invoices
   * Returns the billing invoices associated with this VPS.
   *
   * @param id VPS ID number 
   * @return ChargeInvoiceRows
   */
  def getVpsInvoices(id: Integer): Option[ChargeInvoiceRows] = {
    val await = Try(Await.result(getVpsInvoicesAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get VPS Invoices asynchronously
   * Returns the billing invoices associated with this VPS.
   *
   * @param id VPS ID number 
   * @return Future(ChargeInvoiceRows)
   */
  def getVpsInvoicesAsync(id: Integer): Future[ChargeInvoiceRows] = {
      helper.getVpsInvoices(id)
  }

  /**
   * List VPS Orders
   * Returns all VPS services on the account with their current status and configuration.
   *
   * @return List[VpsRow]
   */
  def getVpsList(): Option[List[VpsRow]] = {
    val await = Try(Await.result(getVpsListAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List VPS Orders asynchronously
   * Returns all VPS services on the account with their current status and configuration.
   *
   * @return Future(List[VpsRow])
   */
  def getVpsListAsync(): Future[List[VpsRow]] = {
      helper.getVpsList()
  }

  /**
   * VPS Reinstall OS Options
   * Returns the list of available OS templates for reinstalling the VPS.
   *
   * @param id VPS ID number 
   * @return VpsTemplatesList
   */
  def getVpsReinstallOs(id: Integer): Option[VpsTemplatesList] = {
    val await = Try(Await.result(getVpsReinstallOsAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * VPS Reinstall OS Options asynchronously
   * Returns the list of available OS templates for reinstalling the VPS.
   *
   * @param id VPS ID number 
   * @return Future(VpsTemplatesList)
   */
  def getVpsReinstallOsAsync(id: Integer): Future[VpsTemplatesList] = {
      helper.getVpsReinstallOs(id)
  }

  /**
   * Reverse DNS Info
   * Returns the current reverse DNS (PTR record) entries for the VPS IP addresses.
   *
   * @param id VPS ID number 
   * @return ReverseDnsEntries
   */
  def getVpsReverseDns(id: Integer): Option[ReverseDnsEntries] = {
    val await = Try(Await.result(getVpsReverseDnsAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Reverse DNS Info asynchronously
   * Returns the current reverse DNS (PTR record) entries for the VPS IP addresses.
   *
   * @param id VPS ID number 
   * @return Future(ReverseDnsEntries)
   */
  def getVpsReverseDnsAsync(id: Integer): Future[ReverseDnsEntries] = {
      helper.getVpsReverseDns(id)
  }

  /**
   * VNC Setup Info
   * Returns the current VNC connection information for the VPS.
   *
   * @param id VPS ID number 
   * @return void
   */
  def getVpsSetupVnc(id: Integer) = {
    val await = Try(Await.result(getVpsSetupVncAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * VNC Setup Info asynchronously
   * Returns the current VNC connection information for the VPS.
   *
   * @param id VPS ID number 
   * @return Future(void)
   */
  def getVpsSetupVncAsync(id: Integer) = {
      helper.getVpsSetupVnc(id)
  }

  /**
   * Slice Upgrade Info
   * Returns available slice upgrade options and pricing for the VPS.
   *
   * @param id VPS ID number 
   * @return void
   */
  def getVpsSlices(id: Integer) = {
    val await = Try(Await.result(getVpsSlicesAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Slice Upgrade Info asynchronously
   * Returns available slice upgrade options and pricing for the VPS.
   *
   * @param id VPS ID number 
   * @return Future(void)
   */
  def getVpsSlicesAsync(id: Integer) = {
      helper.getVpsSlices(id)
  }

  /**
   * Get Traffic Usage
   * Returns bandwidth traffic usage data for the VPS.
   *
   * @param id VPS ID number 
   * @return VpsTrafficResponse
   */
  def getVpsTrafficUsage(id: Integer): Option[VpsTrafficResponse] = {
    val await = Try(Await.result(getVpsTrafficUsageAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Traffic Usage asynchronously
   * Returns bandwidth traffic usage data for the VPS.
   *
   * @param id VPS ID number 
   * @return Future(VpsTrafficResponse)
   */
  def getVpsTrafficUsageAsync(id: Integer): Future[VpsTrafficResponse] = {
      helper.getVpsTrafficUsage(id)
  }

  /**
   * Get View Desktop Info
   * Returns remote desktop connection information for the VPS.
   *
   * @param id VPS ID number 
   * @return void
   */
  def getVpsViewDesktop(id: Integer) = {
    val await = Try(Await.result(getVpsViewDesktopAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get View Desktop Info asynchronously
   * Returns remote desktop connection information for the VPS.
   *
   * @param id VPS ID number 
   * @return Future(void)
   */
  def getVpsViewDesktopAsync(id: Integer) = {
      helper.getVpsViewDesktop(id)
  }

  /**
   * Resend VPS Welcome Email
   * Resends the welcome email containing connection details and credentials for the VPS order.
   *
   * @param id VPS ID 
   * @return SuccessTextResponse
   */
  def getVpsWelcomeEmail(id: String): Option[SuccessTextResponse] = {
    val await = Try(Await.result(getVpsWelcomeEmailAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Resend VPS Welcome Email asynchronously
   * Resends the welcome email containing connection details and credentials for the VPS order.
   *
   * @param id VPS ID 
   * @return Future(SuccessTextResponse)
   */
  def getVpsWelcomeEmailAsync(id: String): Future[SuccessTextResponse] = {
      helper.getVpsWelcomeEmail(id)
  }

  /**
   * Start a VPS Backup
   * Creates a backup of the VPS. The backup can be downloaded or restored later via the backups endpoints.
   *
   * @param id VPS ID number 
   * @return QueueResponse
   */
  def postVpsBackup(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(postVpsBackupAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Start a VPS Backup asynchronously
   * Creates a backup of the VPS. The backup can be downloaded or restored later via the backups endpoints.
   *
   * @param id VPS ID number 
   * @return Future(QueueResponse)
   */
  def postVpsBackupAsync(id: Integer): Future[QueueResponse] = {
      helper.postVpsBackup(id)
  }

  /**
   * Purchase HD Space Addon
   * Purchases additional hard drive space for the VPS.
   *
   * @param id VPS ID number 
   * @return void
   */
  def postVpsBuyHdSpace(id: Integer) = {
    val await = Try(Await.result(postVpsBuyHdSpaceAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Purchase HD Space Addon asynchronously
   * Purchases additional hard drive space for the VPS.
   *
   * @param id VPS ID number 
   * @return Future(void)
   */
  def postVpsBuyHdSpaceAsync(id: Integer) = {
      helper.postVpsBuyHdSpace(id)
  }

  /**
   * Purchase Additional IP
   * Purchases an additional IP address for the VPS.
   *
   * @param id VPS ID number 
   * @return void
   */
  def postVpsBuyIp(id: Integer) = {
    val await = Try(Await.result(postVpsBuyIpAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Purchase Additional IP asynchronously
   * Purchases an additional IP address for the VPS.
   *
   * @param id VPS ID number 
   * @return Future(void)
   */
  def postVpsBuyIpAsync(id: Integer) = {
      helper.postVpsBuyIp(id)
  }

  /**
   * Update VPS Hostname
   * Changes the hostname of the VPS. This queues a background task to update the server.
   *
   * @param hostname  
   * @param body  
   * @param id VPS ID number 
   * @return QueueResponse
   */
  def postVpsChangeHostname(hostname: String, body: HostnameObject, id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(postVpsChangeHostnameAsync(hostname, body, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update VPS Hostname asynchronously
   * Changes the hostname of the VPS. This queues a background task to update the server.
   *
   * @param hostname  
   * @param body  
   * @param id VPS ID number 
   * @return Future(QueueResponse)
   */
  def postVpsChangeHostnameAsync(hostname: String, body: HostnameObject, id: Integer): Future[QueueResponse] = {
      helper.postVpsChangeHostname(hostname, body, id)
  }

  /**
   * Change VPS Root Password
   * Changes the root password of the VPS.
   *
   * @param password  
   * @param body  
   * @param id VPS ID number 
   * @return QueueResponse
   */
  def postVpsChangeRootPassword(password: String, body: PasswordRequest, id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(postVpsChangeRootPasswordAsync(password, body, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Change VPS Root Password asynchronously
   * Changes the root password of the VPS.
   *
   * @param password  
   * @param body  
   * @param id VPS ID number 
   * @return Future(QueueResponse)
   */
  def postVpsChangeRootPasswordAsync(password: String, body: PasswordRequest, id: Integer): Future[QueueResponse] = {
      helper.postVpsChangeRootPassword(password, body, id)
  }

  /**
   * Change VPS Timezone
   * Changes the system timezone on the VPS.
   *
   * @param timezone  
   * @param body  
   * @param id VPS ID number 
   * @return QueueResponse
   */
  def postVpsChangeTimezone(timezone: String, body: TimezoneUpdate, id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(postVpsChangeTimezoneAsync(timezone, body, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Change VPS Timezone asynchronously
   * Changes the system timezone on the VPS.
   *
   * @param timezone  
   * @param body  
   * @param id VPS ID number 
   * @return Future(QueueResponse)
   */
  def postVpsChangeTimezoneAsync(timezone: String, body: TimezoneUpdate, id: Integer): Future[QueueResponse] = {
      helper.postVpsChangeTimezone(timezone, body, id)
  }

  /**
   * Change Webuzo Password
   * Changes the Webuzo control panel password on the VPS.
   *
   * @param password  
   * @param body  
   * @param id VPS ID number 
   * @return QueueResponse
   */
  def postVpsChangeWebuzoPassword(password: String, body: PasswordRequest, id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(postVpsChangeWebuzoPasswordAsync(password, body, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Change Webuzo Password asynchronously
   * Changes the Webuzo control panel password on the VPS.
   *
   * @param password  
   * @param body  
   * @param id VPS ID number 
   * @return Future(QueueResponse)
   */
  def postVpsChangeWebuzoPasswordAsync(password: String, body: PasswordRequest, id: Integer): Future[QueueResponse] = {
      helper.postVpsChangeWebuzoPassword(password, body, id)
  }

  /**
   * Insert CD in VPS
   * Mounts an ISO image in the VPS virtual CD drive from the given URL.
   *
   * @param url  
   * @param body  
   * @param id VPS ID number 
   * @return QueueResponse
   */
  def postVpsInsertCd(url: String, body: UrlRequest, id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(postVpsInsertCdAsync(url, body, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Insert CD in VPS asynchronously
   * Mounts an ISO image in the VPS virtual CD drive from the given URL.
   *
   * @param url  
   * @param body  
   * @param id VPS ID number 
   * @return Future(QueueResponse)
   */
  def postVpsInsertCdAsync(url: String, body: UrlRequest, id: Integer): Future[QueueResponse] = {
      helper.postVpsInsertCd(url, body, id)
  }

  /**
   * Reinstall VPS OS
   * Reinstalls the operating system on the VPS. Warning - all data will be erased.
   *
   * @param template  
   * @param password  
   * @param localPassword  
   * @param body  
   * @param id VPS ID number 
   * @return QueueResponse
   */
  def postVpsReinstallOs(template: String, password: String, localPassword: String, body: TemplateRequest, id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(postVpsReinstallOsAsync(template, password, localPassword, body, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Reinstall VPS OS asynchronously
   * Reinstalls the operating system on the VPS. Warning - all data will be erased.
   *
   * @param template  
   * @param password  
   * @param localPassword  
   * @param body  
   * @param id VPS ID number 
   * @return Future(QueueResponse)
   */
  def postVpsReinstallOsAsync(template: String, password: String, localPassword: String, body: TemplateRequest, id: Integer): Future[QueueResponse] = {
      helper.postVpsReinstallOs(template, password, localPassword, body, id)
  }

  /**
   * Reset VPS Password
   * Resets the root password on the VPS to a new randomly generated password.
   *
   * @param id VPS ID number 
   * @return QueueResponse
   */
  def postVpsResetPassword(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(postVpsResetPasswordAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Reset VPS Password asynchronously
   * Resets the root password on the VPS to a new randomly generated password.
   *
   * @param id VPS ID number 
   * @return Future(QueueResponse)
   */
  def postVpsResetPasswordAsync(id: Integer): Future[QueueResponse] = {
      helper.postVpsResetPassword(id)
  }

  /**
   * Restore VPS from Backup
   * Initiates a restore of the VPS from a previously created backup. The restore operation overwrites the current disk contents. Use &#x60;GET /vps/{id}/backups&#x60; to retrieve available backup names.
   *
   * @param body VPS Restore request 
   * @param backup  
   * @param password  
   * @param id VPS ID number 
   * @return QueueResponse
   */
  def postVpsRestore(body: RestoreRequest, backup: String, password: String, id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(postVpsRestoreAsync(body, backup, password, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Restore VPS from Backup asynchronously
   * Initiates a restore of the VPS from a previously created backup. The restore operation overwrites the current disk contents. Use &#x60;GET /vps/{id}/backups&#x60; to retrieve available backup names.
   *
   * @param body VPS Restore request 
   * @param backup  
   * @param password  
   * @param id VPS ID number 
   * @return Future(QueueResponse)
   */
  def postVpsRestoreAsync(body: RestoreRequest, backup: String, password: String, id: Integer): Future[QueueResponse] = {
      helper.postVpsRestore(body, backup, password, id)
  }

  /**
   * Update Reverse DNS
   * Updates the reverse DNS (PTR record) entries for the VPS IP addresses.
   *
   * @param body  
   * @param ips  
   * @param id VPS ID number 
   * @return TextResponse
   */
  def postVpsReverseDns(body: ReverseDnsEntries, ips: Map[String, Any], id: Integer): Option[TextResponse] = {
    val await = Try(Await.result(postVpsReverseDnsAsync(body, ips, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update Reverse DNS asynchronously
   * Updates the reverse DNS (PTR record) entries for the VPS IP addresses.
   *
   * @param body  
   * @param ips  
   * @param id VPS ID number 
   * @return Future(TextResponse)
   */
  def postVpsReverseDnsAsync(body: ReverseDnsEntries, ips: Map[String, Any], id: Integer): Future[TextResponse] = {
      helper.postVpsReverseDns(body, ips, id)
  }

  /**
   * Setup VNC
   * Sets up or refreshes the VNC console connection for the VPS.
   *
   * @param id VPS ID number 
   * @return QueueResponse
   */
  def postVpsSetupVnc(id: Integer): Option[QueueResponse] = {
    val await = Try(Await.result(postVpsSetupVncAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Setup VNC asynchronously
   * Sets up or refreshes the VNC console connection for the VPS.
   *
   * @param id VPS ID number 
   * @return Future(QueueResponse)
   */
  def postVpsSetupVncAsync(id: Integer): Future[QueueResponse] = {
      helper.postVpsSetupVnc(id)
  }

  /**
   * Purchase Slice Upgrade
   * Purchases a slice upgrade for the VPS to increase CPU, RAM, and disk resources.
   *
   * @param id VPS ID number 
   * @return void
   */
  def postVpsSlices(id: Integer) = {
    val await = Try(Await.result(postVpsSlicesAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Purchase Slice Upgrade asynchronously
   * Purchases a slice upgrade for the VPS to increase CPU, RAM, and disk resources.
   *
   * @param id VPS ID number 
   * @return Future(void)
   */
  def postVpsSlicesAsync(id: Integer) = {
      helper.postVpsSlices(id)
  }

  /**
   * Update View Desktop
   * Updates or refreshes the remote desktop session for the VPS.
   *
   * @param id VPS ID number 
   * @return void
   */
  def postVpsViewDesktop(id: Integer) = {
    val await = Try(Await.result(postVpsViewDesktopAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update View Desktop asynchronously
   * Updates or refreshes the remote desktop session for the VPS.
   *
   * @param id VPS ID number 
   * @return Future(void)
   */
  def postVpsViewDesktopAsync(id: Integer) = {
      helper.postVpsViewDesktop(id)
  }

  /**
   * Validate VPS Order
   * Validates a VPS order before placing it. Returns cost breakdown and any validation errors.
   *
   * @param body  (optional)
   * @param osDistro  (optional)
   * @param slices  (optional)
   * @param vpsPlatform  (optional)
   * @param controlpanel  (optional)
   * @param period  (optional)
   * @param location  (optional)
   * @param osVersion  (optional)
   * @param hostname  (optional)
   * @param coupon  (optional)
   * @param rootpass  (optional)
   * @param comment  (optional)
   * @return VpsOrderPutResponse
   */
  def putVps(body: Option[VpsOrderPutRequest] = None, osDistro: Option[String] = None, slices: Option[Integer] = None, vpsPlatform: Option[String] = None, controlpanel: Option[String] = None, period: Option[Integer] = None, location: Option[Integer] = None, osVersion: Option[String] = None, hostname: Option[String] = None, coupon: Option[String] = None, rootpass: Option[String] = None, comment: Option[String] = None): Option[VpsOrderPutResponse] = {
    val await = Try(Await.result(putVpsAsync(body, osDistro, slices, vpsPlatform, controlpanel, period, location, osVersion, hostname, coupon, rootpass, comment), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Validate VPS Order asynchronously
   * Validates a VPS order before placing it. Returns cost breakdown and any validation errors.
   *
   * @param body  (optional)
   * @param osDistro  (optional)
   * @param slices  (optional)
   * @param vpsPlatform  (optional)
   * @param controlpanel  (optional)
   * @param period  (optional)
   * @param location  (optional)
   * @param osVersion  (optional)
   * @param hostname  (optional)
   * @param coupon  (optional)
   * @param rootpass  (optional)
   * @param comment  (optional)
   * @return Future(VpsOrderPutResponse)
   */
  def putVpsAsync(body: Option[VpsOrderPutRequest] = None, osDistro: Option[String] = None, slices: Option[Integer] = None, vpsPlatform: Option[String] = None, controlpanel: Option[String] = None, period: Option[Integer] = None, location: Option[Integer] = None, osVersion: Option[String] = None, hostname: Option[String] = None, coupon: Option[String] = None, rootpass: Option[String] = None, comment: Option[String] = None): Future[VpsOrderPutResponse] = {
      helper.putVps(body, osDistro, slices, vpsPlatform, controlpanel, period, location, osVersion, hostname, coupon, rootpass, comment)
  }

  /**
   * Update VPS Order
   * Updates settings on a VPS order.
   *
   * @param id VPS ID number. 
   * @return SuccessTextResponse
   */
  def updateVpsInfo(id: String): Option[SuccessTextResponse] = {
    val await = Try(Await.result(updateVpsInfoAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update VPS Order asynchronously
   * Updates settings on a VPS order.
   *
   * @param id VPS ID number. 
   * @return Future(SuccessTextResponse)
   */
  def updateVpsInfoAsync(id: String): Future[SuccessTextResponse] = {
      helper.updateVpsInfo(id)
  }

  /**
   * Cancel VPS Service
   * Cancels the VPS service. The server will be deprovisioned and billing will stop at the end of the current billing cycle.
   *
   * @param id VPS ID number 
   * @return inline_response_200_22
   */
  def vPSCancel(id: Integer): Option[inline_response_200_22] = {
    val await = Try(Await.result(vPSCancelAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Cancel VPS Service asynchronously
   * Cancels the VPS service. The server will be deprovisioned and billing will stop at the end of the current billing cycle.
   *
   * @param id VPS ID number 
   * @return Future(inline_response_200_22)
   */
  def vPSCancelAsync(id: Integer): Future[inline_response_200_22] = {
      helper.vPSCancel(id)
  }

}

class VPSApiAsyncHelper(client: TransportClient, config: SwaggerConfig) extends ApiClient(client, config) {

  def addVps(body: Option[VpsOrderPostRequest] = None,
    osDistro: Option[String] = None,
    slices: Option[Integer] = None,
    vpsPlatform: Option[String] = None,
    controlpanel: Option[String] = None,
    period: Option[Integer] = None,
    location: Option[Integer] = None,
    osVersion: Option[String] = None,
    hostname: Option[String] = None,
    coupon: Option[String] = None,
    rootpass: Option[String] = None,
    comment: Option[String] = None
    )(implicit reader: ClientResponseReader[ServiceOrderPostResponse], writer: RequestWriter[Option[VpsOrderPostRequest]]): Future[ServiceOrderPostResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/order"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def deleteVpsBackup(id: Integer,
    file: String,
    all: Option[String] = None
    )(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/backups")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (file == null) throw new Exception("Missing required parameter 'file' when calling VPSApi->deleteVpsBackup")

    all match {
      case Some(param) => queryParams += "all" -> param.toString
      case _ => queryParams
    }
    queryParams += "file" -> file.toString

    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def doVpsBlockSmtp(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/block_smtp")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def doVpsDisableCd(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/disable_cd")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def doVpsDisableQuota(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/disable_quota")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def doVpsEjectCd(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/eject_cd")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def doVpsEnableQuota(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/enable_quota")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def doVpsRestart(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/restart")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def doVpsStart(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/start")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def doVpsStop(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/stop")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def downloadVpsBackup(body: IdBackupsBody2,
    file: String,
    id: Integer,
    all: Option[String] = None
    )(implicit reader: ClientResponseReader[inline_response_200_11], writer: RequestWriter[IdBackupsBody2]): Future[inline_response_200_11] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/backups")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling VPSApi->downloadVpsBackup")
    if (file == null) throw new Exception("Missing required parameter 'file' when calling VPSApi->downloadVpsBackup")

    all match {
      case Some(param) => queryParams += "all" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("PATCH", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getNewVps()(implicit reader: ClientResponseReader[VpsOrder]): Future[VpsOrder] = {
    // create path and map variables
    val path = (addFmt("/vps/order"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getVpsBackups(id: Integer,
    all: Option[String] = None
    )(implicit reader: ClientResponseReader[VpsBackupRows]): Future[VpsBackupRows] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/backups")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    all match {
      case Some(param) => queryParams += "all" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getVpsBuyHdSpace(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/buy_hd_space")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getVpsBuyIp(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/buy_ip")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getVpsChangeTimezone(id: Integer)(implicit reader: ClientResponseReader[List[String]]): Future[List[String]] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/change_timezone")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getVpsInfo(id: Integer)(implicit reader: ClientResponseReader[Vps]): Future[Vps] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getVpsInvoices(id: Integer)(implicit reader: ClientResponseReader[ChargeInvoiceRows]): Future[ChargeInvoiceRows] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/invoices")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getVpsList()(implicit reader: ClientResponseReader[List[VpsRow]]): Future[List[VpsRow]] = {
    // create path and map variables
    val path = (addFmt("/vps"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getVpsReinstallOs(id: Integer)(implicit reader: ClientResponseReader[VpsTemplatesList]): Future[VpsTemplatesList] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/reinstall_os")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getVpsReverseDns(id: Integer)(implicit reader: ClientResponseReader[ReverseDnsEntries]): Future[ReverseDnsEntries] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/reverse_dns")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getVpsSetupVnc(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/setup_vnc")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getVpsSlices(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/slices")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getVpsTrafficUsage(id: Integer)(implicit reader: ClientResponseReader[VpsTrafficResponse]): Future[VpsTrafficResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/traffic_usage")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getVpsViewDesktop(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/view_desktop")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getVpsWelcomeEmail(id: String)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/welcome_email")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (id == null) throw new Exception("Missing required parameter 'id' when calling VPSApi->getVpsWelcomeEmail")


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postVpsBackup(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/backup")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postVpsBuyHdSpace(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/buy_hd_space")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postVpsBuyIp(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/buy_ip")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postVpsChangeHostname(hostname: String,
    body: HostnameObject,
    id: Integer)(implicit reader: ClientResponseReader[QueueResponse], writer: RequestWriter[HostnameObject]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/change_hostname")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (hostname == null) throw new Exception("Missing required parameter 'hostname' when calling VPSApi->postVpsChangeHostname")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling VPSApi->postVpsChangeHostname")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postVpsChangeRootPassword(password: String,
    body: PasswordRequest,
    id: Integer)(implicit reader: ClientResponseReader[QueueResponse], writer: RequestWriter[PasswordRequest]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/change_root_password")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (password == null) throw new Exception("Missing required parameter 'password' when calling VPSApi->postVpsChangeRootPassword")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling VPSApi->postVpsChangeRootPassword")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postVpsChangeTimezone(timezone: String,
    body: TimezoneUpdate,
    id: Integer)(implicit reader: ClientResponseReader[QueueResponse], writer: RequestWriter[TimezoneUpdate]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/change_timezone")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (timezone == null) throw new Exception("Missing required parameter 'timezone' when calling VPSApi->postVpsChangeTimezone")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling VPSApi->postVpsChangeTimezone")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postVpsChangeWebuzoPassword(password: String,
    body: PasswordRequest,
    id: Integer)(implicit reader: ClientResponseReader[QueueResponse], writer: RequestWriter[PasswordRequest]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/change_webuzo_password")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (password == null) throw new Exception("Missing required parameter 'password' when calling VPSApi->postVpsChangeWebuzoPassword")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling VPSApi->postVpsChangeWebuzoPassword")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postVpsInsertCd(url: String,
    body: UrlRequest,
    id: Integer)(implicit reader: ClientResponseReader[QueueResponse], writer: RequestWriter[UrlRequest]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/insert_cd")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (url == null) throw new Exception("Missing required parameter 'url' when calling VPSApi->postVpsInsertCd")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling VPSApi->postVpsInsertCd")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postVpsReinstallOs(template: String,
    password: String,
    localPassword: String,
    body: TemplateRequest,
    id: Integer)(implicit reader: ClientResponseReader[QueueResponse], writer: RequestWriter[TemplateRequest]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/reinstall_os")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (template == null) throw new Exception("Missing required parameter 'template' when calling VPSApi->postVpsReinstallOs")

    if (password == null) throw new Exception("Missing required parameter 'password' when calling VPSApi->postVpsReinstallOs")
    if (localPassword == null) throw new Exception("Missing required parameter 'localPassword' when calling VPSApi->postVpsReinstallOs")
    if (body == null) throw new Exception("Missing required parameter 'body' when calling VPSApi->postVpsReinstallOs")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postVpsResetPassword(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/reset_password")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postVpsRestore(body: RestoreRequest,
    backup: String,
    password: String,
    id: Integer)(implicit reader: ClientResponseReader[QueueResponse], writer: RequestWriter[RestoreRequest]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/restore")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling VPSApi->postVpsRestore")
    if (backup == null) throw new Exception("Missing required parameter 'backup' when calling VPSApi->postVpsRestore")

    if (password == null) throw new Exception("Missing required parameter 'password' when calling VPSApi->postVpsRestore")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postVpsReverseDns(body: ReverseDnsEntries,
    ips: Map[String, Any],
    id: Integer)(implicit reader: ClientResponseReader[TextResponse], writer: RequestWriter[ReverseDnsEntries]): Future[TextResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/reverse_dns")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling VPSApi->postVpsReverseDns")
    if (ips == null) throw new Exception("Missing required parameter 'ips' when calling VPSApi->postVpsReverseDns")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postVpsSetupVnc(id: Integer)(implicit reader: ClientResponseReader[QueueResponse]): Future[QueueResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/setup_vnc")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postVpsSlices(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/slices")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postVpsViewDesktop(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}/view_desktop")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def putVps(body: Option[VpsOrderPutRequest] = None,
    osDistro: Option[String] = None,
    slices: Option[Integer] = None,
    vpsPlatform: Option[String] = None,
    controlpanel: Option[String] = None,
    period: Option[Integer] = None,
    location: Option[Integer] = None,
    osVersion: Option[String] = None,
    hostname: Option[String] = None,
    coupon: Option[String] = None,
    rootpass: Option[String] = None,
    comment: Option[String] = None
    )(implicit reader: ClientResponseReader[VpsOrderPutResponse], writer: RequestWriter[Option[VpsOrderPutRequest]]): Future[VpsOrderPutResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/order"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("PUT", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateVpsInfo(id: String)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (id == null) throw new Exception("Missing required parameter 'id' when calling VPSApi->updateVpsInfo")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def vPSCancel(id: Integer)(implicit reader: ClientResponseReader[inline_response_200_22]): Future[inline_response_200_22] = {
    // create path and map variables
    val path = (addFmt("/vps/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }


}
