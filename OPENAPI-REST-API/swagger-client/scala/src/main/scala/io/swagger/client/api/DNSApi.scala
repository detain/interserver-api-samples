/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * OpenAPI spec version: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */
package io.swagger.client.api

import java.text.SimpleDateFormat

import io.swagger.client.model.DnsListItem
import io.swagger.client.model.DnsNewDomain
import io.swagger.client.model.DnsNewRecord
import io.swagger.client.model.DnsRecord
import io.swagger.client.model.DnsRecordType
import io.swagger.client.model.DnsUpdateRecord
import io.swagger.client.model.SuccessTextResponse
import io.swagger.client.model.inline_response_401
import io.swagger.client.{ApiInvoker, ApiException}

import com.sun.jersey.multipart.FormDataMultiPart
import com.sun.jersey.multipart.file.FileDataBodyPart

import javax.ws.rs.core.MediaType

import java.io.File
import java.util.Date
import java.util.TimeZone

import scala.collection.mutable.HashMap

import com.wordnik.swagger.client._
import scala.concurrent.Future
import collection.mutable

import java.net.URI

import com.wordnik.swagger.client.ClientResponseReaders.Json4sFormatsReader._
import com.wordnik.swagger.client.RequestWriters.Json4sFormatsWriter._

import scala.concurrent.ExecutionContext.Implicits.global
import scala.concurrent._
import scala.concurrent.duration._
import scala.util.{Failure, Success, Try}

import org.json4s._

class DNSApi(
  val defBasePath: String = "https://my.interserver.net/apiv2",
  defApiInvoker: ApiInvoker = ApiInvoker
) {
  private lazy val dateTimeFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSSZ")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  private val dateFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  implicit val formats = new org.json4s.DefaultFormats {
    override def dateFormatter = dateTimeFormatter
  }
  implicit val stringReader: ClientResponseReader[String] = ClientResponseReaders.StringReader
  implicit val unitReader: ClientResponseReader[Unit] = ClientResponseReaders.UnitReader
  implicit val jvalueReader: ClientResponseReader[JValue] = ClientResponseReaders.JValueReader
  implicit val jsonReader: ClientResponseReader[Nothing] = JsonFormatsReader
  implicit val stringWriter: RequestWriter[String] = RequestWriters.StringWriter
  implicit val jsonWriter: RequestWriter[Nothing] = JsonFormatsWriter

  var basePath: String = defBasePath
  var apiInvoker: ApiInvoker = defApiInvoker

  def addHeader(key: String, value: String): mutable.HashMap[String, String] = {
    apiInvoker.defaultHeaders += key -> value
  }

  val config: SwaggerConfig = SwaggerConfig.forUrl(new URI(defBasePath))
  val client = new RestClient(config)
  val helper = new DNSApiAsyncHelper(client, config)

  /**
   * Create DNS Domain
   * Creates a new DNS domain and assigns an initial A record pointing to the supplied IP address. The domain is immediately available on InterServer&#x27;s DNS servers. Use &#x60;/dns/{id}&#x60; to manage records after creation.
   *
   * @param domain  
   * @param ip  
   * @param body  
   * @return SuccessTextResponse
   */
  def addDnsDomain(domain: String, ip: String, body: DnsNewDomain): Option[SuccessTextResponse] = {
    val await = Try(Await.result(addDnsDomainAsync(domain, ip, body), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Create DNS Domain asynchronously
   * Creates a new DNS domain and assigns an initial A record pointing to the supplied IP address. The domain is immediately available on InterServer&#x27;s DNS servers. Use &#x60;/dns/{id}&#x60; to manage records after creation.
   *
   * @param domain  
   * @param ip  
   * @param body  
   * @return Future(SuccessTextResponse)
   */
  def addDnsDomainAsync(domain: String, ip: String, body: DnsNewDomain): Future[SuccessTextResponse] = {
      helper.addDnsDomain(domain, ip, body)
  }

  /**
   * Add DNS Record to Domain
   * Adds a new DNS record to the specified domain. Provide the record type (A, AAAA, CNAME, MX, TXT, etc.), name, content, TTL, and priority. The record takes effect on the DNS servers immediately. Use &#x60;GET /dns/{id}&#x60; afterward to confirm the record was created.
   *
   * @param name  
   * @param &#x60;type&#x60;  
   * @param content  
   * @param ttl  
   * @param prio  
   * @param body  
   * @param id The DNS Domain ID. 
   * @return void
   */
  def addDnsRecord(name: String, &#x60;type&#x60;: DnsRecordType, content: String, ttl: Integer, prio: Integer, body: DnsNewRecord, id: String) = {
    val await = Try(Await.result(addDnsRecordAsync(name, &#x60;type&#x60;, content, ttl, prio, body, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Add DNS Record to Domain asynchronously
   * Adds a new DNS record to the specified domain. Provide the record type (A, AAAA, CNAME, MX, TXT, etc.), name, content, TTL, and priority. The record takes effect on the DNS servers immediately. Use &#x60;GET /dns/{id}&#x60; afterward to confirm the record was created.
   *
   * @param name  
   * @param &#x60;type&#x60;  
   * @param content  
   * @param ttl  
   * @param prio  
   * @param body  
   * @param id The DNS Domain ID. 
   * @return Future(void)
   */
  def addDnsRecordAsync(name: String, &#x60;type&#x60;: DnsRecordType, content: String, ttl: Integer, prio: Integer, body: DnsNewRecord, id: String) = {
      helper.addDnsRecord(name, &#x60;type&#x60;, content, ttl, prio, body, id)
  }

  /**
   * Delete DNS Domain
   * Deletes a DNS domain and all of its associated records from the DNS servers. This action is permanent and cannot be undone. Any services relying on these DNS records will be affected immediately.
   *
   * @param id The DNS domain ID to delete. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain. 
   * @return SuccessTextResponse
   */
  def deleteDnsDomain(id: String): Option[SuccessTextResponse] = {
    val await = Try(Await.result(deleteDnsDomainAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Delete DNS Domain asynchronously
   * Deletes a DNS domain and all of its associated records from the DNS servers. This action is permanent and cannot be undone. Any services relying on these DNS records will be affected immediately.
   *
   * @param id The DNS domain ID to delete. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain. 
   * @return Future(SuccessTextResponse)
   */
  def deleteDnsDomainAsync(id: String): Future[SuccessTextResponse] = {
      helper.deleteDnsDomain(id)
  }

  /**
   * Delete DNS Record
   * Removes a DNS record from the specified domain. The deletion takes effect on the DNS servers immediately. Use &#x60;GET /dns/{id}&#x60; to verify the record has been removed.
   *
   * @param domainId The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain. 
   * @param recordId The DNS record ID within the domain. Use the record &#x60;id&#x60; from &#x60;GET /dns/{id}&#x60; to identify the record. 
   * @return SuccessTextResponse
   */
  def deleteDnsRecord(domainId: Integer, recordId: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(deleteDnsRecordAsync(domainId, recordId), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Delete DNS Record asynchronously
   * Removes a DNS record from the specified domain. The deletion takes effect on the DNS servers immediately. Use &#x60;GET /dns/{id}&#x60; to verify the record has been removed.
   *
   * @param domainId The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain. 
   * @param recordId The DNS record ID within the domain. Use the record &#x60;id&#x60; from &#x60;GET /dns/{id}&#x60; to identify the record. 
   * @return Future(SuccessTextResponse)
   */
  def deleteDnsRecordAsync(domainId: Integer, recordId: Integer): Future[SuccessTextResponse] = {
      helper.deleteDnsRecord(domainId, recordId)
  }

  /**
   * List Domain DNS Records
   * Returns the full set of DNS records for the specified domain, including NS, A, AAAA, CNAME, MX, TXT, and other record types. Use the record &#x60;id&#x60; values with &#x60;/dns/{domainId}/{recordId}&#x60; to update or delete individual records.
   *
   * @param id The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain. 
   * @return List[DnsRecord]
   */
  def getDnsDomain(id: Integer): Option[List[DnsRecord]] = {
    val await = Try(Await.result(getDnsDomainAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List Domain DNS Records asynchronously
   * Returns the full set of DNS records for the specified domain, including NS, A, AAAA, CNAME, MX, TXT, and other record types. Use the record &#x60;id&#x60; values with &#x60;/dns/{domainId}/{recordId}&#x60; to update or delete individual records.
   *
   * @param id The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain. 
   * @return Future(List[DnsRecord])
   */
  def getDnsDomainAsync(id: Integer): Future[List[DnsRecord]] = {
      helper.getDnsDomain(id)
  }

  /**
   * List DNS Domains
   * Returns the DNS domains on your account along with their primary A record content. Use the &#x60;id&#x60; from each entry with &#x60;/dns/{id}&#x60; to retrieve the full record set, or to add, update, and delete individual records.
   *
   * @return List[DnsListItem]
   */
  def getDnsList(): Option[List[DnsListItem]] = {
    val await = Try(Await.result(getDnsListAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List DNS Domains asynchronously
   * Returns the DNS domains on your account along with their primary A record content. Use the &#x60;id&#x60; from each entry with &#x60;/dns/{id}&#x60; to retrieve the full record set, or to add, update, and delete individual records.
   *
   * @return Future(List[DnsListItem])
   */
  def getDnsListAsync(): Future[List[DnsListItem]] = {
      helper.getDnsList()
  }

  /**
   * Update DNS Record
   * Updates an existing DNS record with new values. Use &#x60;GET /dns/{id}&#x60; to list records and retrieve the record IDs before updating. Changes propagate to the DNS servers immediately.
   *
   * @param name  
   * @param &#x60;type&#x60;  
   * @param content  
   * @param ttl  
   * @param prio  
   * @param disabled  
   * @param ordername  
   * @param auth  
   * @param body The request data to update a dns record. 
   * @param domainId The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain. 
   * @param recordId The DNS record ID within the domain. Use the record &#x60;id&#x60; from &#x60;GET /dns/{id}&#x60; to identify the record. 
   * @return SuccessTextResponse
   */
  def updateDnsRecord(name: String, &#x60;type&#x60;: DnsRecordType, content: String, ttl: String, prio: String, disabled: String, ordername: String, auth: String, body: DnsUpdateRecord, domainId: Integer, recordId: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(updateDnsRecordAsync(name, &#x60;type&#x60;, content, ttl, prio, disabled, ordername, auth, body, domainId, recordId), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update DNS Record asynchronously
   * Updates an existing DNS record with new values. Use &#x60;GET /dns/{id}&#x60; to list records and retrieve the record IDs before updating. Changes propagate to the DNS servers immediately.
   *
   * @param name  
   * @param &#x60;type&#x60;  
   * @param content  
   * @param ttl  
   * @param prio  
   * @param disabled  
   * @param ordername  
   * @param auth  
   * @param body The request data to update a dns record. 
   * @param domainId The DNS domain ID. Use the &#x60;id&#x60; from &#x60;GET /dns&#x60; to identify the domain. 
   * @param recordId The DNS record ID within the domain. Use the record &#x60;id&#x60; from &#x60;GET /dns/{id}&#x60; to identify the record. 
   * @return Future(SuccessTextResponse)
   */
  def updateDnsRecordAsync(name: String, &#x60;type&#x60;: DnsRecordType, content: String, ttl: String, prio: String, disabled: String, ordername: String, auth: String, body: DnsUpdateRecord, domainId: Integer, recordId: Integer): Future[SuccessTextResponse] = {
      helper.updateDnsRecord(name, &#x60;type&#x60;, content, ttl, prio, disabled, ordername, auth, body, domainId, recordId)
  }

}

class DNSApiAsyncHelper(client: TransportClient, config: SwaggerConfig) extends ApiClient(client, config) {

  def addDnsDomain(domain: String,
    ip: String,
    body: DnsNewDomain)(implicit reader: ClientResponseReader[SuccessTextResponse], writer: RequestWriter[DnsNewDomain]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/dns"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (domain == null) throw new Exception("Missing required parameter 'domain' when calling DNSApi->addDnsDomain")

    if (ip == null) throw new Exception("Missing required parameter 'ip' when calling DNSApi->addDnsDomain")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling DNSApi->addDnsDomain")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def addDnsRecord(name: String,
    &#x60;type&#x60;: DnsRecordType,
    content: String,
    ttl: Integer,
    prio: Integer,
    body: DnsNewRecord,
    id: String)(implicit reader: ClientResponseReader[Unit], writer: RequestWriter[DnsNewRecord]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/dns/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (name == null) throw new Exception("Missing required parameter 'name' when calling DNSApi->addDnsRecord")

    if (&#x60;type&#x60; == null) throw new Exception("Missing required parameter '&#x60;type&#x60;' when calling DNSApi->addDnsRecord")
    if (content == null) throw new Exception("Missing required parameter 'content' when calling DNSApi->addDnsRecord")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling DNSApi->addDnsRecord")
    if (id == null) throw new Exception("Missing required parameter 'id' when calling DNSApi->addDnsRecord")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def deleteDnsDomain(id: String)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/dns/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (id == null) throw new Exception("Missing required parameter 'id' when calling DNSApi->deleteDnsDomain")


    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def deleteDnsRecord(domainId: Integer,
    recordId: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/dns/{domainId}/{recordId}")
      replaceAll("\\{" + "domainId" + "\\}", domainId.toString)
      replaceAll("\\{" + "recordId" + "\\}", recordId.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getDnsDomain(id: Integer)(implicit reader: ClientResponseReader[List[DnsRecord]]): Future[List[DnsRecord]] = {
    // create path and map variables
    val path = (addFmt("/dns/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getDnsList()(implicit reader: ClientResponseReader[List[DnsListItem]]): Future[List[DnsListItem]] = {
    // create path and map variables
    val path = (addFmt("/dns"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateDnsRecord(name: String,
    &#x60;type&#x60;: DnsRecordType,
    content: String,
    ttl: String,
    prio: String,
    disabled: String,
    ordername: String,
    auth: String,
    body: DnsUpdateRecord,
    domainId: Integer,
    recordId: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse], writer: RequestWriter[DnsUpdateRecord]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/dns/{domainId}/{recordId}")
      replaceAll("\\{" + "domainId" + "\\}", domainId.toString)
      replaceAll("\\{" + "recordId" + "\\}", recordId.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (name == null) throw new Exception("Missing required parameter 'name' when calling DNSApi->updateDnsRecord")

    if (&#x60;type&#x60; == null) throw new Exception("Missing required parameter '&#x60;type&#x60;' when calling DNSApi->updateDnsRecord")
    if (content == null) throw new Exception("Missing required parameter 'content' when calling DNSApi->updateDnsRecord")

    if (ttl == null) throw new Exception("Missing required parameter 'ttl' when calling DNSApi->updateDnsRecord")

    if (prio == null) throw new Exception("Missing required parameter 'prio' when calling DNSApi->updateDnsRecord")

    if (disabled == null) throw new Exception("Missing required parameter 'disabled' when calling DNSApi->updateDnsRecord")

    if (ordername == null) throw new Exception("Missing required parameter 'ordername' when calling DNSApi->updateDnsRecord")

    if (auth == null) throw new Exception("Missing required parameter 'auth' when calling DNSApi->updateDnsRecord")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling DNSApi->updateDnsRecord")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }


}
