/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * OpenAPI spec version: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */
package io.swagger.client.api

import java.text.SimpleDateFormat

import io.swagger.client.model.BuyItNowList
import io.swagger.client.model.ChargeInvoiceRows
import io.swagger.client.model.OrderBuyNowServerBody
import io.swagger.client.model.ReverseDnsEntries
import io.swagger.client.model.Server
import io.swagger.client.model.ServerIpmiLiveInfo
import io.swagger.client.model.ServerIpmiLiveRequest
import io.swagger.client.model.ServerIpmiPowerRequest
import io.swagger.client.model.ServerOrder
import io.swagger.client.model.ServerRow
import io.swagger.client.model.ServersBuyNowError
import io.swagger.client.model.ServersBuyNowResponse
import io.swagger.client.model.SuccessTextResponse
import io.swagger.client.model.TextResponse
import io.swagger.client.model.inline_response_200_19
import io.swagger.client.model.inline_response_200_26
import io.swagger.client.model.inline_response_401
import io.swagger.client.{ApiInvoker, ApiException}

import com.sun.jersey.multipart.FormDataMultiPart
import com.sun.jersey.multipart.file.FileDataBodyPart

import javax.ws.rs.core.MediaType

import java.io.File
import java.util.Date
import java.util.TimeZone

import scala.collection.mutable.HashMap

import com.wordnik.swagger.client._
import scala.concurrent.Future
import collection.mutable

import java.net.URI

import com.wordnik.swagger.client.ClientResponseReaders.Json4sFormatsReader._
import com.wordnik.swagger.client.RequestWriters.Json4sFormatsWriter._

import scala.concurrent.ExecutionContext.Implicits.global
import scala.concurrent._
import scala.concurrent.duration._
import scala.util.{Failure, Success, Try}

import org.json4s._

class ServersApi(
  val defBasePath: String = "https://my.interserver.net/apiv2",
  defApiInvoker: ApiInvoker = ApiInvoker
) {
  private lazy val dateTimeFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSSZ")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  private val dateFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  implicit val formats = new org.json4s.DefaultFormats {
    override def dateFormatter = dateTimeFormatter
  }
  implicit val stringReader: ClientResponseReader[String] = ClientResponseReaders.StringReader
  implicit val unitReader: ClientResponseReader[Unit] = ClientResponseReaders.UnitReader
  implicit val jvalueReader: ClientResponseReader[JValue] = ClientResponseReaders.JValueReader
  implicit val jsonReader: ClientResponseReader[Nothing] = JsonFormatsReader
  implicit val stringWriter: RequestWriter[String] = RequestWriters.StringWriter
  implicit val jsonWriter: RequestWriter[Nothing] = JsonFormatsWriter

  var basePath: String = defBasePath
  var apiInvoker: ApiInvoker = defApiInvoker

  def addHeader(key: String, value: String): mutable.HashMap[String, String] = {
    apiInvoker.defaultHeaders += key -> value
  }

  val config: SwaggerConfig = SwaggerConfig.forUrl(new URI(defBasePath))
  val client = new RestClient(config)
  val helper = new ServersApiAsyncHelper(client, config)

  /**
   * Place Server Order
   * Places an order for a new dedicated server. Use &#x60;PUT /servers/order&#x60; to validate the order first.
   *
   * @return void
   */
  def addServer() = {
    val await = Try(Await.result(addServerAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Place Server Order asynchronously
   * Places an order for a new dedicated server. Use &#x60;PUT /servers/order&#x60; to validate the order first.
   *
   * @return Future(void)
   */
  def addServerAsync() = {
      helper.addServer()
  }

  /**
   * Get Buy Now Server Options
   * Returns the configuration options and pricing for buy-it-now dedicated servers, including available bandwidth packages, IP blocks, operating systems, control panels, and RAID configurations. Use the returned option IDs when placing an order via &#x60;POST /servers/order/buy_now_server&#x60;.
   *
   * @return inline_response_200_26
   */
  def buyItNowServerOrder(): Option[inline_response_200_26] = {
    val await = Try(Await.result(buyItNowServerOrderAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Buy Now Server Options asynchronously
   * Returns the configuration options and pricing for buy-it-now dedicated servers, including available bandwidth packages, IP blocks, operating systems, control panels, and RAID configurations. Use the returned option IDs when placing an order via &#x60;POST /servers/order/buy_now_server&#x60;.
   *
   * @return Future(inline_response_200_26)
   */
  def buyItNowServerOrderAsync(): Future[inline_response_200_26] = {
      helper.buyItNowServerOrder()
  }

  /**
   * List Marketplace Servers
   * Returns the list of available Rapid Deploy dedicated servers with current pricing. Each entry includes CPU, memory, disk, bandwidth, IP allocation, and location details. These servers are pre-configured and can be provisioned immediately after purchase.
   *
   * @return BuyItNowList
   */
  def getMPServers(): Option[BuyItNowList] = {
    val await = Try(Await.result(getMPServersAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List Marketplace Servers asynchronously
   * Returns the list of available Rapid Deploy dedicated servers with current pricing. Each entry includes CPU, memory, disk, bandwidth, IP allocation, and location details. These servers are pre-configured and can be provisioned immediately after purchase.
   *
   * @return Future(BuyItNowList)
   */
  def getMPServersAsync(): Future[BuyItNowList] = {
      helper.getMPServers()
  }

  /**
   * Server Ordering Information
   * Retrieves available server configurations and pricing for ordering a new dedicated server.
   *
   * @return ServerOrder
   */
  def getNewServer(): Option[ServerOrder] = {
    val await = Try(Await.result(getNewServerAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Server Ordering Information asynchronously
   * Retrieves available server configurations and pricing for ordering a new dedicated server.
   *
   * @return Future(ServerOrder)
   */
  def getNewServerAsync(): Future[ServerOrder] = {
      helper.getNewServer()
  }

  /**
   * Get Server Order
   * Returns detailed information about a specific server including its hardware configuration, IPs, and status.
   *
   * @param id Server ID number. 
   * @return Server
   */
  def getServerInfo(id: Integer): Option[Server] = {
    val await = Try(Await.result(getServerInfoAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Server Order asynchronously
   * Returns detailed information about a specific server including its hardware configuration, IPs, and status.
   *
   * @param id Server ID number. 
   * @return Future(Server)
   */
  def getServerInfoAsync(id: Integer): Future[Server] = {
      helper.getServerInfo(id)
  }

  /**
   * Get Server Invoices
   * Returns the billing invoices associated with this dedicated server.
   *
   * @param id Server ID number 
   * @return ChargeInvoiceRows
   */
  def getServerInvoices(id: Integer): Option[ChargeInvoiceRows] = {
    val await = Try(Await.result(getServerInvoicesAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Server Invoices asynchronously
   * Returns the billing invoices associated with this dedicated server.
   *
   * @param id Server ID number 
   * @return Future(ChargeInvoiceRows)
   */
  def getServerInvoicesAsync(id: Integer): Future[ChargeInvoiceRows] = {
      helper.getServerInvoices(id)
  }

  /**
   * List Servers
   * Returns all dedicated server services on the account with their current status and configuration.
   *
   * @return List[ServerRow]
   */
  def getServerList(): Option[List[ServerRow]] = {
    val await = Try(Await.result(getServerListAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List Servers asynchronously
   * Returns all dedicated server services on the account with their current status and configuration.
   *
   * @return Future(List[ServerRow])
   */
  def getServerListAsync(): Future[List[ServerRow]] = {
      helper.getServerList()
  }

  /**
   * Reverse DNS Info
   * Returns the current reverse DNS (PTR record) entries for the server&#x27;s IP addresses.
   *
   * @param id Server ID number 
   * @return ReverseDnsEntries
   */
  def getServerReverseDns(id: Integer): Option[ReverseDnsEntries] = {
    val await = Try(Await.result(getServerReverseDnsAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Reverse DNS Info asynchronously
   * Returns the current reverse DNS (PTR record) entries for the server&#x27;s IP addresses.
   *
   * @param id Server ID number 
   * @return Future(ReverseDnsEntries)
   */
  def getServerReverseDnsAsync(id: Integer): Future[ReverseDnsEntries] = {
      helper.getServerReverseDns(id)
  }

  /**
   * Resend Server Welcome Email
   * Resends the welcome email for the order.
   *
   * @param id Server ID number 
   * @return SuccessTextResponse
   */
  def getServersWelcomeEmail(id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(getServersWelcomeEmailAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Resend Server Welcome Email asynchronously
   * Resends the welcome email for the order.
   *
   * @param id Server ID number 
   * @return Future(SuccessTextResponse)
   */
  def getServersWelcomeEmailAsync(id: Integer): Future[SuccessTextResponse] = {
      helper.getServersWelcomeEmail(id)
  }

  /**
   * Place Buy Now Server Order
   * Places an order for a buy-it-now dedicated server. Use &#x60;GET /servers/order/buy_now_server&#x60; to retrieve available server configurations and their IDs before ordering.
   *
   * @param body  (optional)
   * @return ServersBuyNowResponse
   */
  def placeBuyNowServer(body: Option[OrderBuyNowServerBody] = None): Option[ServersBuyNowResponse] = {
    val await = Try(Await.result(placeBuyNowServerAsync(body), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Place Buy Now Server Order asynchronously
   * Places an order for a buy-it-now dedicated server. Use &#x60;GET /servers/order/buy_now_server&#x60; to retrieve available server configurations and their IDs before ordering.
   *
   * @param body  (optional)
   * @return Future(ServersBuyNowResponse)
   */
  def placeBuyNowServerAsync(body: Option[OrderBuyNowServerBody] = None): Future[ServersBuyNowResponse] = {
      helper.placeBuyNowServer(body)
  }

  /**
   * Update Reverse DNS
   * Updates the reverse DNS (PTR record) entries for the server&#x27;s IP addresses.
   *
   * @param body  
   * @param ips  
   * @param id Server ID number 
   * @return TextResponse
   */
  def postServerReverseDns(body: ReverseDnsEntries, ips: Map[String, Any], id: Integer): Option[TextResponse] = {
    val await = Try(Await.result(postServerReverseDnsAsync(body, ips, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update Reverse DNS asynchronously
   * Updates the reverse DNS (PTR record) entries for the server&#x27;s IP addresses.
   *
   * @param body  
   * @param ips  
   * @param id Server ID number 
   * @return Future(TextResponse)
   */
  def postServerReverseDnsAsync(body: ReverseDnsEntries, ips: Map[String, Any], id: Integer): Future[TextResponse] = {
      helper.postServerReverseDns(body, ips, id)
  }

  /**
   * Validate Server Order
   * Validates a server order before placing it. Use this to check for errors before committing to a purchase.
   *
   * @return void
   */
  def putServers() = {
    val await = Try(Await.result(putServersAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Validate Server Order asynchronously
   * Validates a server order before placing it. Use this to check for errors before committing to a purchase.
   *
   * @return Future(void)
   */
  def putServersAsync() = {
      helper.putServers()
  }

  /**
   * Server IPMI Live Information
   * Returns the current IPMI live connection information for the server.
   *
   * @param id Server ID number 
   * @return ServerIpmiLiveInfo
   */
  def serverIpmiLiveGet(id: Integer): Option[ServerIpmiLiveInfo] = {
    val await = Try(Await.result(serverIpmiLiveGetAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Server IPMI Live Information asynchronously
   * Returns the current IPMI live connection information for the server.
   *
   * @param id Server ID number 
   * @return Future(ServerIpmiLiveInfo)
   */
  def serverIpmiLiveGetAsync(id: Integer): Future[ServerIpmiLiveInfo] = {
      helper.serverIpmiLiveGet(id)
  }

  /**
   * Server IPMI Live Setup
   * Configures IPMI live access by whitelisting your current IP address for connections to the server&#x27;s IPMI management interface.
   *
   * @param asset  
   * @param ip  
   * @param body  
   * @param id Server ID number 
   * @return ServerIpmiLiveInfo
   */
  def serverIpmiLivePost(asset: Integer, ip: String, body: ServerIpmiLiveRequest, id: Integer): Option[ServerIpmiLiveInfo] = {
    val await = Try(Await.result(serverIpmiLivePostAsync(asset, ip, body, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Server IPMI Live Setup asynchronously
   * Configures IPMI live access by whitelisting your current IP address for connections to the server&#x27;s IPMI management interface.
   *
   * @param asset  
   * @param ip  
   * @param body  
   * @param id Server ID number 
   * @return Future(ServerIpmiLiveInfo)
   */
  def serverIpmiLivePostAsync(asset: Integer, ip: String, body: ServerIpmiLiveRequest, id: Integer): Future[ServerIpmiLiveInfo] = {
      helper.serverIpmiLivePost(asset, ip, body, id)
  }

  /**
   * Get IPMI Power Status
   * Returns the chassis power status from ipmi.
   *
   * @param id Server ID number 
   * @return TextResponse
   */
  def serverIpmiPowerGet(id: Integer): Option[TextResponse] = {
    val await = Try(Await.result(serverIpmiPowerGetAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get IPMI Power Status asynchronously
   * Returns the chassis power status from ipmi.
   *
   * @param id Server ID number 
   * @return Future(TextResponse)
   */
  def serverIpmiPowerGetAsync(id: Integer): Future[TextResponse] = {
      helper.serverIpmiPowerGet(id)
  }

  /**
   * Server IPMI Power
   * Uses the IPMI interface to set the Power status on the server.
   *
   * @param asset  
   * @param action  
   * @param body  
   * @param id Server ID number 
   * @return TextResponse
   */
  def serverIpmiPowerPost(asset: Integer, action: String, body: ServerIpmiPowerRequest, id: Integer): Option[TextResponse] = {
    val await = Try(Await.result(serverIpmiPowerPostAsync(asset, action, body, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Server IPMI Power asynchronously
   * Uses the IPMI interface to set the Power status on the server.
   *
   * @param asset  
   * @param action  
   * @param body  
   * @param id Server ID number 
   * @return Future(TextResponse)
   */
  def serverIpmiPowerPostAsync(asset: Integer, action: String, body: ServerIpmiPowerRequest, id: Integer): Future[TextResponse] = {
      helper.serverIpmiPowerPost(asset, action, body, id)
  }

  /**
   * Cancel Server Service
   * Cancels the dedicated server service. The server will be deprovisioned and billing will stop at the end of the current billing cycle.
   *
   * @param id Server ID number 
   * @return inline_response_200_19
   */
  def serversCancel(id: Integer): Option[inline_response_200_19] = {
    val await = Try(Await.result(serversCancelAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Cancel Server Service asynchronously
   * Cancels the dedicated server service. The server will be deprovisioned and billing will stop at the end of the current billing cycle.
   *
   * @param id Server ID number 
   * @return Future(inline_response_200_19)
   */
  def serversCancelAsync(id: Integer): Future[inline_response_200_19] = {
      helper.serversCancel(id)
  }

  /**
   * Update Server Order
   * Updates settings on a dedicated server order.
   *
   * @param id Server ID number. 
   * @return void
   */
  def updateServerInfo(id: String) = {
    val await = Try(Await.result(updateServerInfoAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update Server Order asynchronously
   * Updates settings on a dedicated server order.
   *
   * @param id Server ID number. 
   * @return Future(void)
   */
  def updateServerInfoAsync(id: String) = {
      helper.updateServerInfo(id)
  }

}

class ServersApiAsyncHelper(client: TransportClient, config: SwaggerConfig) extends ApiClient(client, config) {

  def addServer()(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/servers/order"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def buyItNowServerOrder()(implicit reader: ClientResponseReader[inline_response_200_26]): Future[inline_response_200_26] = {
    // create path and map variables
    val path = (addFmt("/servers/order/buy_now_server"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getMPServers()(implicit reader: ClientResponseReader[BuyItNowList]): Future[BuyItNowList] = {
    // create path and map variables
    val path = (addFmt("/buy_now_servers_list"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getNewServer()(implicit reader: ClientResponseReader[ServerOrder]): Future[ServerOrder] = {
    // create path and map variables
    val path = (addFmt("/servers/order"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getServerInfo(id: Integer)(implicit reader: ClientResponseReader[Server]): Future[Server] = {
    // create path and map variables
    val path = (addFmt("/servers/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getServerInvoices(id: Integer)(implicit reader: ClientResponseReader[ChargeInvoiceRows]): Future[ChargeInvoiceRows] = {
    // create path and map variables
    val path = (addFmt("/servers/{id}/invoices")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getServerList()(implicit reader: ClientResponseReader[List[ServerRow]]): Future[List[ServerRow]] = {
    // create path and map variables
    val path = (addFmt("/servers"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getServerReverseDns(id: Integer)(implicit reader: ClientResponseReader[ReverseDnsEntries]): Future[ReverseDnsEntries] = {
    // create path and map variables
    val path = (addFmt("/servers/{id}/reverse_dns")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getServersWelcomeEmail(id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/servers/{id}/welcome_email")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def placeBuyNowServer(body: Option[OrderBuyNowServerBody] = None
    )(implicit reader: ClientResponseReader[ServersBuyNowResponse], writer: RequestWriter[Option[OrderBuyNowServerBody]]): Future[ServersBuyNowResponse] = {
    // create path and map variables
    val path = (addFmt("/servers/order/buy_now_server"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postServerReverseDns(body: ReverseDnsEntries,
    ips: Map[String, Any],
    id: Integer)(implicit reader: ClientResponseReader[TextResponse], writer: RequestWriter[ReverseDnsEntries]): Future[TextResponse] = {
    // create path and map variables
    val path = (addFmt("/servers/{id}/reverse_dns")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling ServersApi->postServerReverseDns")
    if (ips == null) throw new Exception("Missing required parameter 'ips' when calling ServersApi->postServerReverseDns")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def putServers()(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/servers/order"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("PUT", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def serverIpmiLiveGet(id: Integer)(implicit reader: ClientResponseReader[ServerIpmiLiveInfo]): Future[ServerIpmiLiveInfo] = {
    // create path and map variables
    val path = (addFmt("/servers/{id}/ipmi_live")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def serverIpmiLivePost(asset: Integer,
    ip: String,
    body: ServerIpmiLiveRequest,
    id: Integer)(implicit reader: ClientResponseReader[ServerIpmiLiveInfo], writer: RequestWriter[ServerIpmiLiveRequest]): Future[ServerIpmiLiveInfo] = {
    // create path and map variables
    val path = (addFmt("/servers/{id}/ipmi_live")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (ip == null) throw new Exception("Missing required parameter 'ip' when calling ServersApi->serverIpmiLivePost")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling ServersApi->serverIpmiLivePost")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def serverIpmiPowerGet(id: Integer)(implicit reader: ClientResponseReader[TextResponse]): Future[TextResponse] = {
    // create path and map variables
    val path = (addFmt("/servers/{id}/ipmi_power")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def serverIpmiPowerPost(asset: Integer,
    action: String,
    body: ServerIpmiPowerRequest,
    id: Integer)(implicit reader: ClientResponseReader[TextResponse], writer: RequestWriter[ServerIpmiPowerRequest]): Future[TextResponse] = {
    // create path and map variables
    val path = (addFmt("/servers/{id}/ipmi_power")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (action == null) throw new Exception("Missing required parameter 'action' when calling ServersApi->serverIpmiPowerPost")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling ServersApi->serverIpmiPowerPost")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def serversCancel(id: Integer)(implicit reader: ClientResponseReader[inline_response_200_19]): Future[inline_response_200_19] = {
    // create path and map variables
    val path = (addFmt("/servers/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateServerInfo(id: String)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/servers/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (id == null) throw new Exception("Missing required parameter 'id' when calling ServersApi->updateServerInfo")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }


}
