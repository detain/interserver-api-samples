/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * OpenAPI spec version: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */
package io.swagger.client.api

import java.text.SimpleDateFormat

import io.swagger.client.model.Account2faBody1
import io.swagger.client.model.AccountFeatures
import io.swagger.client.model.AccountInfo
import io.swagger.client.model.AccountInfoPost
import io.swagger.client.model.AccountSshKey
import io.swagger.client.model.GenericResponse
import io.swagger.client.model.Home
import io.swagger.client.model.IpLimitRange
import io.swagger.client.model.PasswordRequest
import io.swagger.client.model.SearchAutocompleteResponse
import io.swagger.client.model.SuccessTextResponse
import io.swagger.client.model.TextResponse
import io.swagger.client.model.inline_response_200
import io.swagger.client.model.inline_response_401
import io.swagger.client.{ApiInvoker, ApiException}

import com.sun.jersey.multipart.FormDataMultiPart
import com.sun.jersey.multipart.file.FileDataBodyPart

import javax.ws.rs.core.MediaType

import java.io.File
import java.util.Date
import java.util.TimeZone

import scala.collection.mutable.HashMap

import com.wordnik.swagger.client._
import scala.concurrent.Future
import collection.mutable

import java.net.URI

import com.wordnik.swagger.client.ClientResponseReaders.Json4sFormatsReader._
import com.wordnik.swagger.client.RequestWriters.Json4sFormatsWriter._

import scala.concurrent.ExecutionContext.Implicits.global
import scala.concurrent._
import scala.concurrent.duration._
import scala.util.{Failure, Success, Try}

import org.json4s._

class AccountApi(
  val defBasePath: String = "https://my.interserver.net/apiv2",
  defApiInvoker: ApiInvoker = ApiInvoker
) {
  private lazy val dateTimeFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSSZ")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  private val dateFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  implicit val formats = new org.json4s.DefaultFormats {
    override def dateFormatter = dateTimeFormatter
  }
  implicit val stringReader: ClientResponseReader[String] = ClientResponseReaders.StringReader
  implicit val unitReader: ClientResponseReader[Unit] = ClientResponseReaders.UnitReader
  implicit val jvalueReader: ClientResponseReader[JValue] = ClientResponseReaders.JValueReader
  implicit val jsonReader: ClientResponseReader[Nothing] = JsonFormatsReader
  implicit val stringWriter: RequestWriter[String] = RequestWriters.StringWriter
  implicit val jsonWriter: RequestWriter[Nothing] = JsonFormatsWriter

  var basePath: String = defBasePath
  var apiInvoker: ApiInvoker = defApiInvoker

  def addHeader(key: String, value: String): mutable.HashMap[String, String] = {
    apiInvoker.defaultHeaders += key -> value
  }

  val config: SwaggerConfig = SwaggerConfig.forUrl(new URI(defBasePath))
  val client = new RestClient(config)
  val helper = new AccountApiAsyncHelper(client, config)

  /**
   * Change Account Username
   * Changes the login username for the account. The new username must be unique across all accounts. After changing, use the new username for all future logins.
   *
   * @return TextResponse
   */
  def changeAccountUsername(): Option[TextResponse] = {
    val await = Try(Await.result(changeAccountUsernameAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Change Account Username asynchronously
   * Changes the login username for the account. The new username must be unique across all accounts. After changing, use the new username for all future logins.
   *
   * @return Future(TextResponse)
   */
  def changeAccountUsernameAsync(): Future[TextResponse] = {
      helper.changeAccountUsername()
  }

  /**
   * Unlink OAuth Account
   * Unlinks a third-party OAuth provider from the account. After unlinking, that provider can no longer be used for login.
   *
   * @param name  
   * @return SuccessTextResponse
   */
  def deleteAccountOauthName(name: String): Option[SuccessTextResponse] = {
    val await = Try(Await.result(deleteAccountOauthNameAsync(name), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Unlink OAuth Account asynchronously
   * Unlinks a third-party OAuth provider from the account. After unlinking, that provider can no longer be used for login.
   *
   * @param name  
   * @return Future(SuccessTextResponse)
   */
  def deleteAccountOauthNameAsync(name: String): Future[SuccessTextResponse] = {
      helper.deleteAccountOauthName(name)
  }

  /**
   * Disable Two-Factor Authentication
   * Disables two-factor authentication on the account. After disabling, the account will only require password-based authentication.
   *
   * @return SuccessTextResponse
   */
  def deleteAccountTfa(): Option[SuccessTextResponse] = {
    val await = Try(Await.result(deleteAccountTfaAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Disable Two-Factor Authentication asynchronously
   * Disables two-factor authentication on the account. After disabling, the account will only require password-based authentication.
   *
   * @return Future(SuccessTextResponse)
   */
  def deleteAccountTfaAsync(): Future[SuccessTextResponse] = {
      helper.deleteAccountTfa()
  }

  /**
   * Remove IP Access Restriction
   * Removes an IP address range from the account&#x27;s access restriction list. If this is the last range, IP limiting is effectively disabled and the account becomes accessible from any IP address.
   *
   * @return GenericResponse
   */
  def deleteIpLimit(): Option[GenericResponse] = {
    val await = Try(Await.result(deleteIpLimitAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Remove IP Access Restriction asynchronously
   * Removes an IP address range from the account&#x27;s access restriction list. If this is the last range, IP limiting is effectively disabled and the account becomes accessible from any IP address.
   *
   * @return Future(GenericResponse)
   */
  def deleteIpLimitAsync(): Future[GenericResponse] = {
      helper.deleteIpLimit()
  }

  /**
   * Retrieve Account Details
   * Returns the full account profile including contact information, billing address, and security settings. Use this to populate account management forms or verify account state before making changes with &#x60;POST /account&#x60;.
   *
   * @return AccountInfo
   */
  def getAccountInfo(): Option[AccountInfo] = {
    val await = Try(Await.result(getAccountInfoAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Retrieve Account Details asynchronously
   * Returns the full account profile including contact information, billing address, and security settings. Use this to populate account management forms or verify account state before making changes with &#x60;POST /account&#x60;.
   *
   * @return Future(AccountInfo)
   */
  def getAccountInfoAsync(): Future[AccountInfo] = {
      helper.getAccountInfo()
  }

  /**
   * Get Two-Factor Setup Data
   * Returns the TOTP secret key needed to configure a two-factor authentication app (e.g. Google Authenticator). Present the &#x60;2fa_google_key&#x60; as a QR code or display the &#x60;2fa_google_split&#x60; value for manual entry. After setup, verify with &#x60;POST /account/2fa&#x60;.
   *
   * @return inline_response_200
   */
  def getAccountTfaSetup(): Option[inline_response_200] = {
    val await = Try(Await.result(getAccountTfaSetupAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Two-Factor Setup Data asynchronously
   * Returns the TOTP secret key needed to configure a two-factor authentication app (e.g. Google Authenticator). Present the &#x60;2fa_google_key&#x60; as a QR code or display the &#x60;2fa_google_split&#x60; value for manual entry. After setup, verify with &#x60;POST /account/2fa&#x60;.
   *
   * @return Future(inline_response_200)
   */
  def getAccountTfaSetupAsync(): Future[inline_response_200] = {
      helper.getAccountTfaSetup()
  }

  /**
   * Get Home Data
   * Returns an overview of the account suitable for a dashboard home page, including service counts, recent activity, and account-level alerts. Requires an authenticated session.
   *
   * @return Home
   */
  def getHome(): Option[Home] = {
    val await = Try(Await.result(getHomeAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Home Data asynchronously
   * Returns an overview of the account suitable for a dashboard home page, including service counts, recent activity, and account-level alerts. Requires an authenticated session.
   *
   * @return Future(Home)
   */
  def getHomeAsync(): Future[Home] = {
      helper.getHome()
  }

  /**
   * Search Autocomplete
   * Returns autocomplete results for the account&#x27;s services and records. Use this endpoint to power global search experiences in the client portal.
   *
   * @return SearchAutocompleteResponse
   */
  def getSearch(): Option[SearchAutocompleteResponse] = {
    val await = Try(Await.result(getSearchAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Search Autocomplete asynchronously
   * Returns autocomplete results for the account&#x27;s services and records. Use this endpoint to power global search experiences in the client portal.
   *
   * @return Future(SearchAutocompleteResponse)
   */
  def getSearchAsync(): Future[SearchAutocompleteResponse] = {
      helper.getSearch()
  }

  /**
   * Log Out
   * Invalidates the current session. After calling this endpoint, the session token can no longer be used for authenticated requests. The client should discard the stored session ID.
   *
   * @return SuccessTextResponse
   */
  def logout(): Option[SuccessTextResponse] = {
    val await = Try(Await.result(logoutAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Log Out asynchronously
   * Invalidates the current session. After calling this endpoint, the session token can no longer be used for authenticated requests. The client should discard the stored session ID.
   *
   * @return Future(SuccessTextResponse)
   */
  def logoutAsync(): Future[SuccessTextResponse] = {
      helper.logout()
  }

  /**
   * Logout of OAuth
   * Logs out of the specified OAuth provider session.
   *
   * @param name  
   * @return SuccessTextResponse
   */
  def logoutAccountOauth(name: String): Option[SuccessTextResponse] = {
    val await = Try(Await.result(logoutAccountOauthAsync(name), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Logout of OAuth asynchronously
   * Logs out of the specified OAuth provider session.
   *
   * @param name  
   * @return Future(SuccessTextResponse)
   */
  def logoutAccountOauthAsync(name: String): Future[SuccessTextResponse] = {
      helper.logoutAccountOauth(name)
  }

  /**
   * Generate New API Key
   * Generates a new API key for the account. The previous key is immediately invalidated. Store the new key securely as it cannot be retrieved later.
   *
   * @return SuccessTextResponse
   */
  def updateAccountApiKey(): Option[SuccessTextResponse] = {
    val await = Try(Await.result(updateAccountApiKeyAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Generate New API Key asynchronously
   * Generates a new API key for the account. The previous key is immediately invalidated. Store the new key securely as it cannot be retrieved later.
   *
   * @return Future(SuccessTextResponse)
   */
  def updateAccountApiKeyAsync(): Future[SuccessTextResponse] = {
      helper.updateAccountApiKey()
  }

  /**
   * Update Account Feature Flags
   * Updates account-level feature flags that control service capabilities. These flags can disable password reset, OS reinstall, or other potentially destructive operations across your services. Changes take effect immediately.
   *
   * @param disable_reset  
   * @param disable_reinstall  
   * @param body  
   * @return SuccessTextResponse
   */
  def updateAccountFeatures(disable_reset: Integer, disable_reinstall: Integer, body: AccountFeatures): Option[SuccessTextResponse] = {
    val await = Try(Await.result(updateAccountFeaturesAsync(disable_reset, disable_reinstall, body), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update Account Feature Flags asynchronously
   * Updates account-level feature flags that control service capabilities. These flags can disable password reset, OS reinstall, or other potentially destructive operations across your services. Changes take effect immediately.
   *
   * @param disable_reset  
   * @param disable_reinstall  
   * @param body  
   * @return Future(SuccessTextResponse)
   */
  def updateAccountFeaturesAsync(disable_reset: Integer, disable_reinstall: Integer, body: AccountFeatures): Future[SuccessTextResponse] = {
      helper.updateAccountFeatures(disable_reset, disable_reinstall, body)
  }

  /**
   * Update Account Information
   * Updates the stored contact and billing information on your account. Submit only the fields you want to change. Validation errors are returned as a 422 response with field-level messages.
   *
   * @param name  
   * @param company  
   * @param address  
   * @param address2  
   * @param city  
   * @param state  
   * @param zip  
   * @param country  
   * @param phone  
   * @param locale  
   * @param email_invoices  
   * @param email_abuse  
   * @param disable_reset  
   * @param disable_reinstall  
   * @param disable_server_notifications  
   * @param disable_email_notifications  
   * @param gstin  
   * @param body  
   * @return SuccessTextResponse
   */
  def updateAccountInfo(name: String, company: String, address: String, address2: String, city: String, state: String, zip: String, country: String, phone: String, locale: String, email_invoices: String, email_abuse: String, disable_reset: Boolean, disable_reinstall: Boolean, disable_server_notifications: Boolean, disable_email_notifications: Boolean, gstin: String, body: AccountInfoPost): Option[SuccessTextResponse] = {
    val await = Try(Await.result(updateAccountInfoAsync(name, company, address, address2, city, state, zip, country, phone, locale, email_invoices, email_abuse, disable_reset, disable_reinstall, disable_server_notifications, disable_email_notifications, gstin, body), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update Account Information asynchronously
   * Updates the stored contact and billing information on your account. Submit only the fields you want to change. Validation errors are returned as a 422 response with field-level messages.
   *
   * @param name  
   * @param company  
   * @param address  
   * @param address2  
   * @param city  
   * @param state  
   * @param zip  
   * @param country  
   * @param phone  
   * @param locale  
   * @param email_invoices  
   * @param email_abuse  
   * @param disable_reset  
   * @param disable_reinstall  
   * @param disable_server_notifications  
   * @param disable_email_notifications  
   * @param gstin  
   * @param body  
   * @return Future(SuccessTextResponse)
   */
  def updateAccountInfoAsync(name: String, company: String, address: String, address2: String, city: String, state: String, zip: String, country: String, phone: String, locale: String, email_invoices: String, email_abuse: String, disable_reset: Boolean, disable_reinstall: Boolean, disable_server_notifications: Boolean, disable_email_notifications: Boolean, gstin: String, body: AccountInfoPost): Future[SuccessTextResponse] = {
      helper.updateAccountInfo(name, company, address, address2, city, state, zip, country, phone, locale, email_invoices, email_abuse, disable_reset, disable_reinstall, disable_server_notifications, disable_email_notifications, gstin, body)
  }

  /**
   * Add IP Access Restriction
   * Adds an IP address range to the account&#x27;s access restriction list. Once IP limiting is active, only requests originating from allowed ranges can access the account. Provide the start and end of the range in dotted-quad notation.
   *
   * @param start  
   * @param end  
   * @param body The lower and upper bounds of an ip range. 
   * @return SuccessTextResponse
   */
  def updateAccountIpLimits(start: String, end: String, body: IpLimitRange): Option[SuccessTextResponse] = {
    val await = Try(Await.result(updateAccountIpLimitsAsync(start, end, body), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Add IP Access Restriction asynchronously
   * Adds an IP address range to the account&#x27;s access restriction list. Once IP limiting is active, only requests originating from allowed ranges can access the account. Provide the start and end of the range in dotted-quad notation.
   *
   * @param start  
   * @param end  
   * @param body The lower and upper bounds of an ip range. 
   * @return Future(SuccessTextResponse)
   */
  def updateAccountIpLimitsAsync(start: String, end: String, body: IpLimitRange): Future[SuccessTextResponse] = {
      helper.updateAccountIpLimits(start, end, body)
  }

  /**
   * Change Account Password
   * Changes the account login password. The current password must be provided for verification. After a successful change, existing API keys remain valid but active sessions may require re-authentication.
   *
   * @param password  
   * @param body  
   * @return TextResponse
   */
  def updateAccountPassword(password: String, body: PasswordRequest): Option[TextResponse] = {
    val await = Try(Await.result(updateAccountPasswordAsync(password, body), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Change Account Password asynchronously
   * Changes the account login password. The current password must be provided for verification. After a successful change, existing API keys remain valid but active sessions may require re-authentication.
   *
   * @param password  
   * @param body  
   * @return Future(TextResponse)
   */
  def updateAccountPasswordAsync(password: String, body: PasswordRequest): Future[TextResponse] = {
      helper.updateAccountPassword(password, body)
  }

  /**
   * Update SSH Keys
   * Updates the SSH public key stored on the account. This key can be automatically installed on new VPS and server orders.
   *
   * @param ssh_key  
   * @param body  
   * @return SuccessTextResponse
   */
  def updateAccountSshKey(ssh_key: String, body: AccountSshKey): Option[SuccessTextResponse] = {
    val await = Try(Await.result(updateAccountSshKeyAsync(ssh_key, body), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update SSH Keys asynchronously
   * Updates the SSH public key stored on the account. This key can be automatically installed on new VPS and server orders.
   *
   * @param ssh_key  
   * @param body  
   * @return Future(SuccessTextResponse)
   */
  def updateAccountSshKeyAsync(ssh_key: String, body: AccountSshKey): Future[SuccessTextResponse] = {
      helper.updateAccountSshKey(ssh_key, body)
  }

  /**
   * Enable Two-Factor Authentication
   * Verifies the TOTP code from your authenticator app and enables two-factor authentication on the account. Use &#x60;GET /account/2fa&#x60; first to retrieve the secret key for app setup.
   *
   * @param &#x60;2fa_google_code&#x60;  
   * @param body  
   * @return SuccessTextResponse
   */
  def updateAccountTfa(&#x60;2fa_google_code&#x60;: String, body: Account2faBody1): Option[SuccessTextResponse] = {
    val await = Try(Await.result(updateAccountTfaAsync(&#x60;2fa_google_code&#x60;, body), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Enable Two-Factor Authentication asynchronously
   * Verifies the TOTP code from your authenticator app and enables two-factor authentication on the account. Use &#x60;GET /account/2fa&#x60; first to retrieve the secret key for app setup.
   *
   * @param &#x60;2fa_google_code&#x60;  
   * @param body  
   * @return Future(SuccessTextResponse)
   */
  def updateAccountTfaAsync(&#x60;2fa_google_code&#x60;: String, body: Account2faBody1): Future[SuccessTextResponse] = {
      helper.updateAccountTfa(&#x60;2fa_google_code&#x60;, body)
  }

}

class AccountApiAsyncHelper(client: TransportClient, config: SwaggerConfig) extends ApiClient(client, config) {

  def changeAccountUsername()(implicit reader: ClientResponseReader[TextResponse]): Future[TextResponse] = {
    // create path and map variables
    val path = (addFmt("/account/username"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def deleteAccountOauthName(name: String)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/account/oauth/{name}")
      replaceAll("\\{" + "name" + "\\}", name.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (name == null) throw new Exception("Missing required parameter 'name' when calling AccountApi->deleteAccountOauthName")


    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def deleteAccountTfa()(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/account/2fa"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def deleteIpLimit()(implicit reader: ClientResponseReader[GenericResponse]): Future[GenericResponse] = {
    // create path and map variables
    val path = (addFmt("/account/iplimits"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("PATCH", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getAccountInfo()(implicit reader: ClientResponseReader[AccountInfo]): Future[AccountInfo] = {
    // create path and map variables
    val path = (addFmt("/account"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getAccountTfaSetup()(implicit reader: ClientResponseReader[inline_response_200]): Future[inline_response_200] = {
    // create path and map variables
    val path = (addFmt("/account/2fa"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getHome()(implicit reader: ClientResponseReader[Home]): Future[Home] = {
    // create path and map variables
    val path = (addFmt("/home"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getSearch()(implicit reader: ClientResponseReader[SearchAutocompleteResponse]): Future[SearchAutocompleteResponse] = {
    // create path and map variables
    val path = (addFmt("/search"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def logout()(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/logout"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def logoutAccountOauth(name: String)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/account/oauth/{name}/logout")
      replaceAll("\\{" + "name" + "\\}", name.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (name == null) throw new Exception("Missing required parameter 'name' when calling AccountApi->logoutAccountOauth")


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateAccountApiKey()(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/account/apikey"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateAccountFeatures(disable_reset: Integer,
    disable_reinstall: Integer,
    body: AccountFeatures)(implicit reader: ClientResponseReader[SuccessTextResponse], writer: RequestWriter[AccountFeatures]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/account/features"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling AccountApi->updateAccountFeatures")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateAccountInfo(name: String,
    company: String,
    address: String,
    address2: String,
    city: String,
    state: String,
    zip: String,
    country: String,
    phone: String,
    locale: String,
    email_invoices: String,
    email_abuse: String,
    disable_reset: Boolean,
    disable_reinstall: Boolean,
    disable_server_notifications: Boolean,
    disable_email_notifications: Boolean,
    gstin: String,
    body: AccountInfoPost)(implicit reader: ClientResponseReader[SuccessTextResponse], writer: RequestWriter[AccountInfoPost]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/account"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (name == null) throw new Exception("Missing required parameter 'name' when calling AccountApi->updateAccountInfo")

    if (company == null) throw new Exception("Missing required parameter 'company' when calling AccountApi->updateAccountInfo")

    if (address == null) throw new Exception("Missing required parameter 'address' when calling AccountApi->updateAccountInfo")

    if (address2 == null) throw new Exception("Missing required parameter 'address2' when calling AccountApi->updateAccountInfo")

    if (city == null) throw new Exception("Missing required parameter 'city' when calling AccountApi->updateAccountInfo")

    if (state == null) throw new Exception("Missing required parameter 'state' when calling AccountApi->updateAccountInfo")

    if (zip == null) throw new Exception("Missing required parameter 'zip' when calling AccountApi->updateAccountInfo")

    if (country == null) throw new Exception("Missing required parameter 'country' when calling AccountApi->updateAccountInfo")

    if (phone == null) throw new Exception("Missing required parameter 'phone' when calling AccountApi->updateAccountInfo")

    if (locale == null) throw new Exception("Missing required parameter 'locale' when calling AccountApi->updateAccountInfo")

    if (email_invoices == null) throw new Exception("Missing required parameter 'email_invoices' when calling AccountApi->updateAccountInfo")

    if (email_abuse == null) throw new Exception("Missing required parameter 'email_abuse' when calling AccountApi->updateAccountInfo")

    if (gstin == null) throw new Exception("Missing required parameter 'gstin' when calling AccountApi->updateAccountInfo")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling AccountApi->updateAccountInfo")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateAccountIpLimits(start: String,
    end: String,
    body: IpLimitRange)(implicit reader: ClientResponseReader[SuccessTextResponse], writer: RequestWriter[IpLimitRange]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/account/iplimits"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (start == null) throw new Exception("Missing required parameter 'start' when calling AccountApi->updateAccountIpLimits")

    if (end == null) throw new Exception("Missing required parameter 'end' when calling AccountApi->updateAccountIpLimits")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling AccountApi->updateAccountIpLimits")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateAccountPassword(password: String,
    body: PasswordRequest)(implicit reader: ClientResponseReader[TextResponse], writer: RequestWriter[PasswordRequest]): Future[TextResponse] = {
    // create path and map variables
    val path = (addFmt("/account/password"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (password == null) throw new Exception("Missing required parameter 'password' when calling AccountApi->updateAccountPassword")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling AccountApi->updateAccountPassword")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateAccountSshKey(ssh_key: String,
    body: AccountSshKey)(implicit reader: ClientResponseReader[SuccessTextResponse], writer: RequestWriter[AccountSshKey]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/account/sshkey"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (ssh_key == null) throw new Exception("Missing required parameter 'ssh_key' when calling AccountApi->updateAccountSshKey")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling AccountApi->updateAccountSshKey")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateAccountTfa(&#x60;2fa_google_code&#x60;: String,
    body: Account2faBody1)(implicit reader: ClientResponseReader[SuccessTextResponse], writer: RequestWriter[Account2faBody1]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/account/2fa"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (&#x60;2fa_google_code&#x60; == null) throw new Exception("Missing required parameter '&#x60;2fa_google_code&#x60;' when calling AccountApi->updateAccountTfa")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling AccountApi->updateAccountTfa")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }


}
