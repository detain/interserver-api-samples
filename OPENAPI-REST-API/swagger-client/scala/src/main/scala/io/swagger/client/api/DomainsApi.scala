/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * OpenAPI spec version: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */
package io.swagger.client.api

import java.text.SimpleDateFormat

import io.swagger.client.model.ChargeInvoiceRows
import io.swagger.client.model.Domain
import io.swagger.client.model.DomainContactDetails
import io.swagger.client.model.DomainDnssecRecords
import io.swagger.client.model.DomainDnssecRequest
import io.swagger.client.model.DomainLookupResponse
import io.swagger.client.model.DomainNameserverGetResponse
import io.swagger.client.model.DomainNameserverPostRequest
import io.swagger.client.model.DomainNameserverPutRequest
import io.swagger.client.model.DomainOrder
import io.swagger.client.model.DomainRow
import io.swagger.client.model.DomainSearchResponse
import io.swagger.client.model.DomainWhoisPrivacyRequest
import io.swagger.client.model.ServiceOrderPostResponse
import io.swagger.client.model.SuccessTextResponse
import io.swagger.client.model.TextResponse
import io.swagger.client.model.inline_response_200_2
import io.swagger.client.model.inline_response_401
import io.swagger.client.{ApiInvoker, ApiException}

import com.sun.jersey.multipart.FormDataMultiPart
import com.sun.jersey.multipart.file.FileDataBodyPart

import javax.ws.rs.core.MediaType

import java.io.File
import java.util.Date
import java.util.TimeZone

import scala.collection.mutable.HashMap

import com.wordnik.swagger.client._
import scala.concurrent.Future
import collection.mutable

import java.net.URI

import com.wordnik.swagger.client.ClientResponseReaders.Json4sFormatsReader._
import com.wordnik.swagger.client.RequestWriters.Json4sFormatsWriter._

import scala.concurrent.ExecutionContext.Implicits.global
import scala.concurrent._
import scala.concurrent.duration._
import scala.util.{Failure, Success, Try}

import org.json4s._

class DomainsApi(
  val defBasePath: String = "https://my.interserver.net/apiv2",
  defApiInvoker: ApiInvoker = ApiInvoker
) {
  private lazy val dateTimeFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSSZ")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  private val dateFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  implicit val formats = new org.json4s.DefaultFormats {
    override def dateFormatter = dateTimeFormatter
  }
  implicit val stringReader: ClientResponseReader[String] = ClientResponseReaders.StringReader
  implicit val unitReader: ClientResponseReader[Unit] = ClientResponseReaders.UnitReader
  implicit val jvalueReader: ClientResponseReader[JValue] = ClientResponseReaders.JValueReader
  implicit val jsonReader: ClientResponseReader[Nothing] = JsonFormatsReader
  implicit val stringWriter: RequestWriter[String] = RequestWriters.StringWriter
  implicit val jsonWriter: RequestWriter[Nothing] = JsonFormatsWriter

  var basePath: String = defBasePath
  var apiInvoker: ApiInvoker = defApiInvoker

  def addHeader(key: String, value: String): mutable.HashMap[String, String] = {
    apiInvoker.defaultHeaders += key -> value
  }

  val config: SwaggerConfig = SwaggerConfig.forUrl(new URI(defBasePath))
  val client = new RestClient(config)
  val helper = new DomainsApiAsyncHelper(client, config)

  /**
   * Place Domain Order
   * Places a new domain registration or transfer order. Use the results from &#x60;/domains/lookup/{name}&#x60; or &#x60;/domains/order/{domain}/{regType}&#x60; to populate the required domain fields before submitting the order.
   *
   * @return ServiceOrderPostResponse
   */
  def addDomain(): Option[ServiceOrderPostResponse] = {
    val await = Try(Await.result(addDomainAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Place Domain Order asynchronously
   * Places a new domain registration or transfer order. Use the results from &#x60;/domains/lookup/{name}&#x60; or &#x60;/domains/order/{domain}/{regType}&#x60; to populate the required domain fields before submitting the order.
   *
   * @return Future(ServiceOrderPostResponse)
   */
  def addDomainAsync(): Future[ServiceOrderPostResponse] = {
      helper.addDomain()
  }

  /**
   * Add Domain DNSSEC Records
   * Adds DNSSEC DS records to the domain registration. Provide one or more DNSSEC record entries (algorithm, digest type, digest, key tag). Registrations must be active.
   *
   * @param body  
   * @param algorithm  
   * @param digest_type  
   * @param digest  
   * @param key_tag  
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return SuccessTextResponse
   */
  def addDomainDnssec(body: DomainDnssecRequest, algorithm: List[Integer], digest_type: List[Integer], digest: List[String], key_tag: List[Integer], id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(addDomainDnssecAsync(body, algorithm, digest_type, digest, key_tag, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Add Domain DNSSEC Records asynchronously
   * Adds DNSSEC DS records to the domain registration. Provide one or more DNSSEC record entries (algorithm, digest type, digest, key tag). Registrations must be active.
   *
   * @param body  
   * @param algorithm  
   * @param digest_type  
   * @param digest  
   * @param key_tag  
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return Future(SuccessTextResponse)
   */
  def addDomainDnssecAsync(body: DomainDnssecRequest, algorithm: List[Integer], digest_type: List[Integer], digest: List[String], key_tag: List[Integer], id: Integer): Future[SuccessTextResponse] = {
      helper.addDomainDnssec(body, algorithm, digest_type, digest, key_tag, id)
  }

  /**
   * Add Registered Nameserver
   * Adds a new registered nameserver (glue record) for the domain using the supplied hostname and IP address. Use &#x60;GET /domains/{id}/nameservers&#x60; to confirm that the new entry exists.
   *
   * @param body  
   * @param name  
   * @param ipAddress  
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return TextResponse
   */
  def addDomainNameserver(body: DomainNameserverPostRequest, name: String, ipAddress: String, id: Integer): Option[TextResponse] = {
    val await = Try(Await.result(addDomainNameserverAsync(body, name, ipAddress, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Add Registered Nameserver asynchronously
   * Adds a new registered nameserver (glue record) for the domain using the supplied hostname and IP address. Use &#x60;GET /domains/{id}/nameservers&#x60; to confirm that the new entry exists.
   *
   * @param body  
   * @param name  
   * @param ipAddress  
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return Future(TextResponse)
   */
  def addDomainNameserverAsync(body: DomainNameserverPostRequest, name: String, ipAddress: String, id: Integer): Future[TextResponse] = {
      helper.addDomainNameserver(body, name, ipAddress, id)
  }

  /**
   * Cancel Domain Order
   * Cancels the domain order in the billing system. After cancellation the service will no longer auto-renew and will transition to a &#x60;canceled&#x60; status. The domain registration itself may remain active at the registrar until its current expiration date, but no further renewals will be processed. Treat the domain as inactive in downstream automation after this call succeeds.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return inline_response_200_2
   */
  def cancelDomain(id: Integer): Option[inline_response_200_2] = {
    val await = Try(Await.result(cancelDomainAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Cancel Domain Order asynchronously
   * Cancels the domain order in the billing system. After cancellation the service will no longer auto-renew and will transition to a &#x60;canceled&#x60; status. The domain registration itself may remain active at the registrar until its current expiration date, but no further renewals will be processed. Treat the domain as inactive in downstream automation after this call succeeds.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return Future(inline_response_200_2)
   */
  def cancelDomainAsync(id: Integer): Future[inline_response_200_2] = {
      helper.cancelDomain(id)
  }

  /**
   * Remove Domain DNSSEC Records
   * Removes DNSSEC DS records from the domain registration. Use this to disable DNSSEC at the registrar level.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @param action Set to &#x60;delete&#x60; to remove all DNSSEC records. 
   * @return SuccessTextResponse
   */
  def deleteDomainDnssec(id: Integer, action: String): Option[SuccessTextResponse] = {
    val await = Try(Await.result(deleteDomainDnssecAsync(id, action), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Remove Domain DNSSEC Records asynchronously
   * Removes DNSSEC DS records from the domain registration. Use this to disable DNSSEC at the registrar level.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @param action Set to &#x60;delete&#x60; to remove all DNSSEC records. 
   * @return Future(SuccessTextResponse)
   */
  def deleteDomainDnssecAsync(id: Integer, action: String): Future[SuccessTextResponse] = {
      helper.deleteDomainDnssec(id, action)
  }

  /**
   * Delete Registered Nameserver
   * Removes a registered nameserver (glue record) from the domain. Specify the zero-based &#x60;index&#x60; of the nameserver to remove as returned by &#x60;GET /domains/{id}/nameservers&#x60;.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @param index The index of the registered nameserver from the registered nameservers list to delete.   
   * @return TextResponse
   */
  def deleteDomainNameserver(id: Integer, index: Integer): Option[TextResponse] = {
    val await = Try(Await.result(deleteDomainNameserverAsync(id, index), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Delete Registered Nameserver asynchronously
   * Removes a registered nameserver (glue record) from the domain. Specify the zero-based &#x60;index&#x60; of the nameserver to remove as returned by &#x60;GET /domains/{id}/nameservers&#x60;.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @param index The index of the registered nameserver from the registered nameservers list to delete.   
   * @return Future(TextResponse)
   */
  def deleteDomainNameserverAsync(id: Integer, index: Integer): Future[TextResponse] = {
      helper.deleteDomainNameserver(id, index)
  }

  /**
   * Get Domain Contact Details
   * Returns the current domain contact details that will be used for the registrant, admin, technical, and billing contact records. Use this response as the basis for any contact updates.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return DomainContactDetails
   */
  def getDomainContact(id: Integer): Option[DomainContactDetails] = {
    val await = Try(Await.result(getDomainContactAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Domain Contact Details asynchronously
   * Returns the current domain contact details that will be used for the registrant, admin, technical, and billing contact records. Use this response as the basis for any contact updates.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return Future(DomainContactDetails)
   */
  def getDomainContactAsync(id: Integer): Future[DomainContactDetails] = {
      helper.getDomainContact(id)
  }

  /**
   * Get Domain DNSSEC Records
   * Returns the DNSSEC DS records registered with the domain. Use the record data if you need to synchronize DNSSEC settings with another registry or to prepare an update request.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return DomainDnssecRecords
   */
  def getDomainDnssec(id: Integer): Option[DomainDnssecRecords] = {
    val await = Try(Await.result(getDomainDnssecAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Domain DNSSEC Records asynchronously
   * Returns the DNSSEC DS records registered with the domain. Use the record data if you need to synchronize DNSSEC settings with another registry or to prepare an update request.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return Future(DomainDnssecRecords)
   */
  def getDomainDnssecAsync(id: Integer): Future[DomainDnssecRecords] = {
      helper.getDomainDnssec(id)
  }

  /**
   * Get Domain Order
   * Fetches the detailed domain service record (billing, registration, and service metadata) for the specified order ID. Use the &#x60;domain_hostname&#x60; from this response when calling &#x60;/domains/lookup/{name}&#x60; or when interacting with nameserver and DNSSEC management endpoints.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return Domain
   */
  def getDomainInfo(id: Integer): Option[Domain] = {
    val await = Try(Await.result(getDomainInfoAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Domain Order asynchronously
   * Fetches the detailed domain service record (billing, registration, and service metadata) for the specified order ID. Use the &#x60;domain_hostname&#x60; from this response when calling &#x60;/domains/lookup/{name}&#x60; or when interacting with nameserver and DNSSEC management endpoints.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return Future(Domain)
   */
  def getDomainInfoAsync(id: Integer): Future[Domain] = {
      helper.getDomainInfo(id)
  }

  /**
   * Get Domain Invoices
   * Retrieves invoice records associated with the domain order. Use this to reconcile billing or to confirm whether renewals or add-ons (such as Whois privacy) have been invoiced.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return ChargeInvoiceRows
   */
  def getDomainInvoices(id: Integer): Option[ChargeInvoiceRows] = {
    val await = Try(Await.result(getDomainInvoicesAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Domain Invoices asynchronously
   * Retrieves invoice records associated with the domain order. Use this to reconcile billing or to confirm whether renewals or add-ons (such as Whois privacy) have been invoiced.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return Future(ChargeInvoiceRows)
   */
  def getDomainInvoicesAsync(id: Integer): Future[ChargeInvoiceRows] = {
      helper.getDomainInvoices(id)
  }

  /**
   * Lookup Domain Availability and Pricing
   * Checks a domain for availability and premium status, and returns pricing information for new, renewal, and transfer actions when possible. The response also includes the order field metadata you can pass to &#x60;/domains/order&#x60; when placing a registration or transfer.
   *
   * @param name The full domain name to look up (for example &#x60;example.com&#x60;). 
   * @return DomainLookupResponse
   */
  def getDomainLookup(name: String): Option[DomainLookupResponse] = {
    val await = Try(Await.result(getDomainLookupAsync(name), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Lookup Domain Availability and Pricing asynchronously
   * Checks a domain for availability and premium status, and returns pricing information for new, renewal, and transfer actions when possible. The response also includes the order field metadata you can pass to &#x60;/domains/order&#x60; when placing a registration or transfer.
   *
   * @param name The full domain name to look up (for example &#x60;example.com&#x60;). 
   * @return Future(DomainLookupResponse)
   */
  def getDomainLookupAsync(name: String): Future[DomainLookupResponse] = {
      helper.getDomainLookup(name)
  }

  /**
   * List Registered Nameservers
   * Returns the registered nameserver hosts and glue IP addresses currently configured at the registrar. Use the returned list when adding, updating, or deleting individual nameserver records.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return DomainNameserverGetResponse
   */
  def getDomainNameservers(id: Integer): Option[DomainNameserverGetResponse] = {
    val await = Try(Await.result(getDomainNameserversAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List Registered Nameservers asynchronously
   * Returns the registered nameserver hosts and glue IP addresses currently configured at the registrar. Use the returned list when adding, updating, or deleting individual nameserver records.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return Future(DomainNameserverGetResponse)
   */
  def getDomainNameserversAsync(id: Integer): Future[DomainNameserverGetResponse] = {
      helper.getDomainNameservers(id)
  }

  /**
   * Get Domain Order Fields
   * Returns the registration-type-specific form fields required to place a domain order for the given hostname. Use the returned fields to build the POST body for &#x60;POST /domains/order&#x60;.
   *
   * @param domain The fully qualified domain name (e.g. &#x60;example.com&#x60;). 
   * @param regType The registration type. Common values include &#x60;register&#x60; for new registrations and &#x60;transfer&#x60; for inbound transfers. 
   * @return void
   */
  def getDomainOrderFields(domain: String, regType: String) = {
    val await = Try(Await.result(getDomainOrderFieldsAsync(domain, regType), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Domain Order Fields asynchronously
   * Returns the registration-type-specific form fields required to place a domain order for the given hostname. Use the returned fields to build the POST body for &#x60;POST /domains/order&#x60;.
   *
   * @param domain The fully qualified domain name (e.g. &#x60;example.com&#x60;). 
   * @param regType The registration type. Common values include &#x60;register&#x60; for new registrations and &#x60;transfer&#x60; for inbound transfers. 
   * @return Future(void)
   */
  def getDomainOrderFieldsAsync(domain: String, regType: String) = {
      helper.getDomainOrderFields(domain, regType)
  }

  /**
   * Get Domain Order Search Results
   * Returns order data for the specified domain hostname, including availability and pricing details. Use this to preview order information before placing a registration or transfer.
   *
   * @param domain The fully qualified domain name to look up (e.g. &#x60;example.com&#x60;). 
   * @return void
   */
  def getDomainOrderSearchResults(domain: String) = {
    val await = Try(Await.result(getDomainOrderSearchResultsAsync(domain), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Domain Order Search Results asynchronously
   * Returns order data for the specified domain hostname, including availability and pricing details. Use this to preview order information before placing a registration or transfer.
   *
   * @param domain The fully qualified domain name to look up (e.g. &#x60;example.com&#x60;). 
   * @return Future(void)
   */
  def getDomainOrderSearchResultsAsync(domain: String) = {
      helper.getDomainOrderSearchResults(domain)
  }

  /**
   * Start Domain Renewal Flow
   * Starts the domain renewal workflow for an active domain. This endpoint currently returns a status message indicating that renewal handling has been triggered.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return SuccessTextResponse
   */
  def getDomainRenewal(id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(getDomainRenewalAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Start Domain Renewal Flow asynchronously
   * Starts the domain renewal workflow for an active domain. This endpoint currently returns a status message indicating that renewal handling has been triggered.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return Future(SuccessTextResponse)
   */
  def getDomainRenewalAsync(id: Integer): Future[SuccessTextResponse] = {
      helper.getDomainRenewal(id)
  }

  /**
   * Search Domain Suggestions
   * Queries the registrar for suggested domains and availability data. Use the returned &#x60;lookup&#x60; results to identify availability and then call &#x60;/domains/lookup/{name}&#x60; or &#x60;/domains/order&#x60; to obtain pricing and order fields for the chosen domain.
   *
   * @param name The base domain name to search (for example &#x60;example&#x60; or &#x60;example.com&#x60;). 
   * @return DomainSearchResponse
   */
  def getDomainSearch(name: String): Option[DomainSearchResponse] = {
    val await = Try(Await.result(getDomainSearchAsync(name), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Search Domain Suggestions asynchronously
   * Queries the registrar for suggested domains and availability data. Use the returned &#x60;lookup&#x60; results to identify availability and then call &#x60;/domains/lookup/{name}&#x60; or &#x60;/domains/order&#x60; to obtain pricing and order fields for the chosen domain.
   *
   * @param name The base domain name to search (for example &#x60;example&#x60; or &#x60;example.com&#x60;). 
   * @return Future(DomainSearchResponse)
   */
  def getDomainSearchAsync(name: String): Future[DomainSearchResponse] = {
      helper.getDomainSearch(name)
  }

  /**
   * Start Domain Transfer Flow
   * Initiates the transfer workflow for a domain already in your account. Use this in coordination with &#x60;/domains/{id}/contact&#x60; to ensure registrant details are ready for the transfer request.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return SuccessTextResponse
   */
  def getDomainTransfer(id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(getDomainTransferAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Start Domain Transfer Flow asynchronously
   * Initiates the transfer workflow for a domain already in your account. Use this in coordination with &#x60;/domains/{id}/contact&#x60; to ensure registrant details are ready for the transfer request.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return Future(SuccessTextResponse)
   */
  def getDomainTransferAsync(id: Integer): Future[SuccessTextResponse] = {
      helper.getDomainTransfer(id)
  }

  /**
   * Get Whois Privacy Status
   * Returns status text for the Whois privacy add-on on the domain. Use this to determine whether privacy can be enabled or disabled for the current TLD before submitting updates.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return SuccessTextResponse
   */
  def getDomainWhoisPrivacy(id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(getDomainWhoisPrivacyAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Whois Privacy Status asynchronously
   * Returns status text for the Whois privacy add-on on the domain. Use this to determine whether privacy can be enabled or disabled for the current TLD before submitting updates.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return Future(SuccessTextResponse)
   */
  def getDomainWhoisPrivacyAsync(id: Integer): Future[SuccessTextResponse] = {
      helper.getDomainWhoisPrivacy(id)
  }

  /**
   * List Domain Orders
   * Returns the domain services on your account, including the billing order ID, hostname, expiration date, and status. Use the &#x60;domain_id&#x60; from this response with &#x60;GET /domains/{id}&#x60; to fetch full service details, or with &#x60;/domains/{id}/nameservers&#x60;, &#x60;/domains/{id}/contact&#x60;, and &#x60;/domains/{id}/whois&#x60; to manage registration settings.
   *
   * @return List[DomainRow]
   */
  def getDomainsList(): Option[List[DomainRow]] = {
    val await = Try(Await.result(getDomainsListAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List Domain Orders asynchronously
   * Returns the domain services on your account, including the billing order ID, hostname, expiration date, and status. Use the &#x60;domain_id&#x60; from this response with &#x60;GET /domains/{id}&#x60; to fetch full service details, or with &#x60;/domains/{id}/nameservers&#x60;, &#x60;/domains/{id}/contact&#x60;, and &#x60;/domains/{id}/whois&#x60; to manage registration settings.
   *
   * @return Future(List[DomainRow])
   */
  def getDomainsListAsync(): Future[List[DomainRow]] = {
      helper.getDomainsList()
  }

  /**
   * Resend Domain Welcome Email
   * Resends the welcome email for the domain service. The email contains registration details and management instructions.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return SuccessTextResponse
   */
  def getDomainsWelcomeEmail(id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(getDomainsWelcomeEmailAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Resend Domain Welcome Email asynchronously
   * Resends the welcome email for the domain service. The email contains registration details and management instructions.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return Future(SuccessTextResponse)
   */
  def getDomainsWelcomeEmailAsync(id: Integer): Future[SuccessTextResponse] = {
      helper.getDomainsWelcomeEmail(id)
  }

  /**
   * Get Domain Ordering Information
   * Returns the catalog of available domain services, including TLD service IDs and base Whois privacy pricing. Use the &#x60;tldServices&#x60; map to resolve a TLD into a service ID when you build an order request for &#x60;/domains/order&#x60; (POST) or the domain-field lookup endpoints.
   *
   * @return DomainOrder
   */
  def getNewDomain(): Option[DomainOrder] = {
    val await = Try(Await.result(getNewDomainAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Domain Ordering Information asynchronously
   * Returns the catalog of available domain services, including TLD service IDs and base Whois privacy pricing. Use the &#x60;tldServices&#x60; map to resolve a TLD into a service ID when you build an order request for &#x60;/domains/order&#x60; (POST) or the domain-field lookup endpoints.
   *
   * @return Future(DomainOrder)
   */
  def getNewDomainAsync(): Future[DomainOrder] = {
      helper.getNewDomain()
  }

  /**
   * Validate Domain Order
   * Returns the domain-specific field requirements for the supplied hostname and registration type. Use this to build the form data required for &#x60;/domains/order&#x60; (POST).
   *
   * @return void
   */
  def patchDomains() = {
    val await = Try(Await.result(patchDomainsAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Validate Domain Order asynchronously
   * Returns the domain-specific field requirements for the supplied hostname and registration type. Use this to build the form data required for &#x60;/domains/order&#x60; (POST).
   *
   * @return Future(void)
   */
  def patchDomainsAsync() = {
      helper.patchDomains()
  }

  /**
   * Request Domain Renewal
   * Submits a domain renewal request for the order. Use &#x60;/domains/{id}/invoices&#x60; to identify any invoices created as part of the renewal flow.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return SuccessTextResponse
   */
  def postDomainRenewal(id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(postDomainRenewalAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Request Domain Renewal asynchronously
   * Submits a domain renewal request for the order. Use &#x60;/domains/{id}/invoices&#x60; to identify any invoices created as part of the renewal flow.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return Future(SuccessTextResponse)
   */
  def postDomainRenewalAsync(id: Integer): Future[SuccessTextResponse] = {
      helper.postDomainRenewal(id)
  }

  /**
   * Request Domain Transfer
   * Submits a transfer request for the domain order. Use &#x60;/domains/{id}/invoices&#x60; to identify any invoices generated as part of the transfer process.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return SuccessTextResponse
   */
  def postDomainTransfer(id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(postDomainTransferAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Request Domain Transfer asynchronously
   * Submits a transfer request for the domain order. Use &#x60;/domains/{id}/invoices&#x60; to identify any invoices generated as part of the transfer process.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return Future(SuccessTextResponse)
   */
  def postDomainTransferAsync(id: Integer): Future[SuccessTextResponse] = {
      helper.postDomainTransfer(id)
  }

  /**
   * Domain Order Search
   * Returns validation data for a domain order request (such as availability and pricing information). This is typically used as a pre-flight check before submitting a POST order.
   *
   * @return void
   */
  def putDomains() = {
    val await = Try(Await.result(putDomainsAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Domain Order Search asynchronously
   * Returns validation data for a domain order request (such as availability and pricing information). This is typically used as a pre-flight check before submitting a POST order.
   *
   * @return Future(void)
   */
  def putDomainsAsync() = {
      helper.putDomains()
  }

  /**
   * Update Domain Contact Details
   * Updates the registrant/admin contact details for the domain. Submit the same fields returned by the GET response after making the desired changes. Domain updates require the service to be active and unlocked.
   *
   * @param body  
   * @param status  
   * @param state  
   * @param org_name  
   * @param country  
   * @param postal_code  
   * @param email  
   * @param fax  
   * @param address2  
   * @param address3  
   * @param address1  
   * @param city  
   * @param phone  
   * @param first_name  
   * @param last_name  
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return SuccessTextResponse
   */
  def updateDomainContact(body: DomainContactDetails, status: String, state: String, org_name: String, country: String, postal_code: String, email: String, fax: String, address2: String, address3: String, address1: String, city: String, phone: String, first_name: String, last_name: String, id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(updateDomainContactAsync(body, status, state, org_name, country, postal_code, email, fax, address2, address3, address1, city, phone, first_name, last_name, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update Domain Contact Details asynchronously
   * Updates the registrant/admin contact details for the domain. Submit the same fields returned by the GET response after making the desired changes. Domain updates require the service to be active and unlocked.
   *
   * @param body  
   * @param status  
   * @param state  
   * @param org_name  
   * @param country  
   * @param postal_code  
   * @param email  
   * @param fax  
   * @param address2  
   * @param address3  
   * @param address1  
   * @param city  
   * @param phone  
   * @param first_name  
   * @param last_name  
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return Future(SuccessTextResponse)
   */
  def updateDomainContactAsync(body: DomainContactDetails, status: String, state: String, org_name: String, country: String, postal_code: String, email: String, fax: String, address2: String, address3: String, address1: String, city: String, phone: String, first_name: String, last_name: String, id: Integer): Future[SuccessTextResponse] = {
      helper.updateDomainContact(body, status, state, org_name, country, postal_code, email, fax, address2, address3, address1, city, phone, first_name, last_name, id)
  }

  /**
   * Update Domain Order
   * Updates the domain service record for the order. Use this for account-level changes such as updating stored registration metadata or transfer attributes.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return SuccessTextResponse
   */
  def updateDomainInfo(id: String): Option[SuccessTextResponse] = {
    val await = Try(Await.result(updateDomainInfoAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update Domain Order asynchronously
   * Updates the domain service record for the order. Use this for account-level changes such as updating stored registration metadata or transfer attributes.
   *
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return Future(SuccessTextResponse)
   */
  def updateDomainInfoAsync(id: String): Future[SuccessTextResponse] = {
      helper.updateDomainInfo(id)
  }

  /**
   * Replace Nameserver Set
   * Replaces the full nameserver assignment for the domain with the provided list. This is the primary method for changing which authoritative nameservers the domain delegates to.
   *
   * @param body  
   * @param nameserver  
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return TextResponse
   */
  def updateDomainNameservers(body: DomainNameserverPutRequest, nameserver: List[String], id: Integer): Option[TextResponse] = {
    val await = Try(Await.result(updateDomainNameserversAsync(body, nameserver, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Replace Nameserver Set asynchronously
   * Replaces the full nameserver assignment for the domain with the provided list. This is the primary method for changing which authoritative nameservers the domain delegates to.
   *
   * @param body  
   * @param nameserver  
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return Future(TextResponse)
   */
  def updateDomainNameserversAsync(body: DomainNameserverPutRequest, nameserver: List[String], id: Integer): Future[TextResponse] = {
      helper.updateDomainNameservers(body, nameserver, id)
  }

  /**
   * Update Whois Privacy
   * Enables or disables the Whois privacy add-on for the domain. The API may create invoices when enabling privacy; use &#x60;/domains/{id}/invoices&#x60; to track billing events.
   *
   * @param body  
   * @param func  
   * @param csrf_token  
   * @param domain_firstname  
   * @param domain_lastname  
   * @param domain_email  
   * @param domain_address  
   * @param domain_address2  
   * @param domain_address3  
   * @param domain_city  
   * @param domain_state  
   * @param domain_zip  
   * @param domain_country  
   * @param domain_phone  
   * @param domain_fax  
   * @param domain_company  
   * @param domain_extra  
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return SuccessTextResponse
   */
  def updateDomainWhoisPrivacy(body: DomainWhoisPrivacyRequest, func: String, csrf_token: String, domain_firstname: String, domain_lastname: String, domain_email: String, domain_address: String, domain_address2: String, domain_address3: String, domain_city: String, domain_state: String, domain_zip: String, domain_country: String, domain_phone: String, domain_fax: String, domain_company: String, domain_extra: String, id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(updateDomainWhoisPrivacyAsync(body, func, csrf_token, domain_firstname, domain_lastname, domain_email, domain_address, domain_address2, domain_address3, domain_city, domain_state, domain_zip, domain_country, domain_phone, domain_fax, domain_company, domain_extra, id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update Whois Privacy asynchronously
   * Enables or disables the Whois privacy add-on for the domain. The API may create invoices when enabling privacy; use &#x60;/domains/{id}/invoices&#x60; to track billing events.
   *
   * @param body  
   * @param func  
   * @param csrf_token  
   * @param domain_firstname  
   * @param domain_lastname  
   * @param domain_email  
   * @param domain_address  
   * @param domain_address2  
   * @param domain_address3  
   * @param domain_city  
   * @param domain_state  
   * @param domain_zip  
   * @param domain_country  
   * @param domain_phone  
   * @param domain_fax  
   * @param domain_company  
   * @param domain_extra  
   * @param id The domain service ID. Use &#x60;domain_id&#x60; from &#x60;GET /domains&#x60;. 
   * @return Future(SuccessTextResponse)
   */
  def updateDomainWhoisPrivacyAsync(body: DomainWhoisPrivacyRequest, func: String, csrf_token: String, domain_firstname: String, domain_lastname: String, domain_email: String, domain_address: String, domain_address2: String, domain_address3: String, domain_city: String, domain_state: String, domain_zip: String, domain_country: String, domain_phone: String, domain_fax: String, domain_company: String, domain_extra: String, id: Integer): Future[SuccessTextResponse] = {
      helper.updateDomainWhoisPrivacy(body, func, csrf_token, domain_firstname, domain_lastname, domain_email, domain_address, domain_address2, domain_address3, domain_city, domain_state, domain_zip, domain_country, domain_phone, domain_fax, domain_company, domain_extra, id)
  }

}

class DomainsApiAsyncHelper(client: TransportClient, config: SwaggerConfig) extends ApiClient(client, config) {

  def addDomain()(implicit reader: ClientResponseReader[ServiceOrderPostResponse]): Future[ServiceOrderPostResponse] = {
    // create path and map variables
    val path = (addFmt("/domains/order"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def addDomainDnssec(body: DomainDnssecRequest,
    algorithm: List[Integer],
    digest_type: List[Integer],
    digest: List[String],
    key_tag: List[Integer],
    id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse], writer: RequestWriter[DomainDnssecRequest]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/domains/{id}/dnssec")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling DomainsApi->addDomainDnssec")
    if (algorithm == null) throw new Exception("Missing required parameter 'algorithm' when calling DomainsApi->addDomainDnssec")
    if (digest_type == null) throw new Exception("Missing required parameter 'digest_type' when calling DomainsApi->addDomainDnssec")
    if (digest == null) throw new Exception("Missing required parameter 'digest' when calling DomainsApi->addDomainDnssec")
    if (key_tag == null) throw new Exception("Missing required parameter 'key_tag' when calling DomainsApi->addDomainDnssec")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def addDomainNameserver(body: DomainNameserverPostRequest,
    name: String,
    ipAddress: String,
    id: Integer)(implicit reader: ClientResponseReader[TextResponse], writer: RequestWriter[DomainNameserverPostRequest]): Future[TextResponse] = {
    // create path and map variables
    val path = (addFmt("/domains/{id}/nameservers")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling DomainsApi->addDomainNameserver")
    if (name == null) throw new Exception("Missing required parameter 'name' when calling DomainsApi->addDomainNameserver")

    if (ipAddress == null) throw new Exception("Missing required parameter 'ipAddress' when calling DomainsApi->addDomainNameserver")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def cancelDomain(id: Integer)(implicit reader: ClientResponseReader[inline_response_200_2]): Future[inline_response_200_2] = {
    // create path and map variables
    val path = (addFmt("/domains/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def deleteDomainDnssec(id: Integer,
    action: String)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/domains/{id}/dnssec")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (action == null) throw new Exception("Missing required parameter 'action' when calling DomainsApi->deleteDomainDnssec")

    queryParams += "action" -> action.toString

    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def deleteDomainNameserver(id: Integer,
    index: Integer)(implicit reader: ClientResponseReader[TextResponse]): Future[TextResponse] = {
    // create path and map variables
    val path = (addFmt("/domains/{id}/nameservers")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    queryParams += "index" -> index.toString

    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getDomainContact(id: Integer)(implicit reader: ClientResponseReader[DomainContactDetails]): Future[DomainContactDetails] = {
    // create path and map variables
    val path = (addFmt("/domains/{id}/contact")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getDomainDnssec(id: Integer)(implicit reader: ClientResponseReader[DomainDnssecRecords]): Future[DomainDnssecRecords] = {
    // create path and map variables
    val path = (addFmt("/domains/{id}/dnssec")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getDomainInfo(id: Integer)(implicit reader: ClientResponseReader[Domain]): Future[Domain] = {
    // create path and map variables
    val path = (addFmt("/domains/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getDomainInvoices(id: Integer)(implicit reader: ClientResponseReader[ChargeInvoiceRows]): Future[ChargeInvoiceRows] = {
    // create path and map variables
    val path = (addFmt("/domains/{id}/invoices")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getDomainLookup(name: String)(implicit reader: ClientResponseReader[DomainLookupResponse]): Future[DomainLookupResponse] = {
    // create path and map variables
    val path = (addFmt("/domains/lookup/{name}")
      replaceAll("\\{" + "name" + "\\}", name.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (name == null) throw new Exception("Missing required parameter 'name' when calling DomainsApi->getDomainLookup")


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getDomainNameservers(id: Integer)(implicit reader: ClientResponseReader[DomainNameserverGetResponse]): Future[DomainNameserverGetResponse] = {
    // create path and map variables
    val path = (addFmt("/domains/{id}/nameservers")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getDomainOrderFields(domain: String,
    regType: String)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/domains/order/{domain}/{regType}")
      replaceAll("\\{" + "domain" + "\\}", domain.toString)
      replaceAll("\\{" + "regType" + "\\}", regType.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (domain == null) throw new Exception("Missing required parameter 'domain' when calling DomainsApi->getDomainOrderFields")

    if (regType == null) throw new Exception("Missing required parameter 'regType' when calling DomainsApi->getDomainOrderFields")


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getDomainOrderSearchResults(domain: String)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/domains/order/{domain}")
      replaceAll("\\{" + "domain" + "\\}", domain.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (domain == null) throw new Exception("Missing required parameter 'domain' when calling DomainsApi->getDomainOrderSearchResults")


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getDomainRenewal(id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/domains/{id}/renew")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getDomainSearch(name: String)(implicit reader: ClientResponseReader[DomainSearchResponse]): Future[DomainSearchResponse] = {
    // create path and map variables
    val path = (addFmt("/domains/search/{name}")
      replaceAll("\\{" + "name" + "\\}", name.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (name == null) throw new Exception("Missing required parameter 'name' when calling DomainsApi->getDomainSearch")


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getDomainTransfer(id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/domains/{id}/transfer")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getDomainWhoisPrivacy(id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/domains/{id}/whois")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getDomainsList()(implicit reader: ClientResponseReader[List[DomainRow]]): Future[List[DomainRow]] = {
    // create path and map variables
    val path = (addFmt("/domains"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getDomainsWelcomeEmail(id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/domains/{id}/welcome_email")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getNewDomain()(implicit reader: ClientResponseReader[DomainOrder]): Future[DomainOrder] = {
    // create path and map variables
    val path = (addFmt("/domains/order"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def patchDomains()(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/domains/order"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("PATCH", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postDomainRenewal(id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/domains/{id}/renew")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postDomainTransfer(id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/domains/{id}/transfer")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def putDomains()(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/domains/order"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("PUT", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateDomainContact(body: DomainContactDetails,
    status: String,
    state: String,
    org_name: String,
    country: String,
    postal_code: String,
    email: String,
    fax: String,
    address2: String,
    address3: String,
    address1: String,
    city: String,
    phone: String,
    first_name: String,
    last_name: String,
    id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse], writer: RequestWriter[DomainContactDetails]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/domains/{id}/contact")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling DomainsApi->updateDomainContact")
    if (status == null) throw new Exception("Missing required parameter 'status' when calling DomainsApi->updateDomainContact")

    if (state == null) throw new Exception("Missing required parameter 'state' when calling DomainsApi->updateDomainContact")

    if (org_name == null) throw new Exception("Missing required parameter 'org_name' when calling DomainsApi->updateDomainContact")

    if (country == null) throw new Exception("Missing required parameter 'country' when calling DomainsApi->updateDomainContact")

    if (postal_code == null) throw new Exception("Missing required parameter 'postal_code' when calling DomainsApi->updateDomainContact")

    if (email == null) throw new Exception("Missing required parameter 'email' when calling DomainsApi->updateDomainContact")

    if (fax == null) throw new Exception("Missing required parameter 'fax' when calling DomainsApi->updateDomainContact")

    if (address2 == null) throw new Exception("Missing required parameter 'address2' when calling DomainsApi->updateDomainContact")

    if (address3 == null) throw new Exception("Missing required parameter 'address3' when calling DomainsApi->updateDomainContact")

    if (address1 == null) throw new Exception("Missing required parameter 'address1' when calling DomainsApi->updateDomainContact")

    if (city == null) throw new Exception("Missing required parameter 'city' when calling DomainsApi->updateDomainContact")

    if (phone == null) throw new Exception("Missing required parameter 'phone' when calling DomainsApi->updateDomainContact")

    if (first_name == null) throw new Exception("Missing required parameter 'first_name' when calling DomainsApi->updateDomainContact")

    if (last_name == null) throw new Exception("Missing required parameter 'last_name' when calling DomainsApi->updateDomainContact")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateDomainInfo(id: String)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/domains/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (id == null) throw new Exception("Missing required parameter 'id' when calling DomainsApi->updateDomainInfo")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateDomainNameservers(body: DomainNameserverPutRequest,
    nameserver: List[String],
    id: Integer)(implicit reader: ClientResponseReader[TextResponse], writer: RequestWriter[DomainNameserverPutRequest]): Future[TextResponse] = {
    // create path and map variables
    val path = (addFmt("/domains/{id}/nameservers")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling DomainsApi->updateDomainNameservers")
    if (nameserver == null) throw new Exception("Missing required parameter 'nameserver' when calling DomainsApi->updateDomainNameservers")

    val resFuture = client.submit("PUT", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateDomainWhoisPrivacy(body: DomainWhoisPrivacyRequest,
    func: String,
    csrf_token: String,
    domain_firstname: String,
    domain_lastname: String,
    domain_email: String,
    domain_address: String,
    domain_address2: String,
    domain_address3: String,
    domain_city: String,
    domain_state: String,
    domain_zip: String,
    domain_country: String,
    domain_phone: String,
    domain_fax: String,
    domain_company: String,
    domain_extra: String,
    id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse], writer: RequestWriter[DomainWhoisPrivacyRequest]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/domains/{id}/whois")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling DomainsApi->updateDomainWhoisPrivacy")
    if (func == null) throw new Exception("Missing required parameter 'func' when calling DomainsApi->updateDomainWhoisPrivacy")

    if (csrf_token == null) throw new Exception("Missing required parameter 'csrf_token' when calling DomainsApi->updateDomainWhoisPrivacy")

    if (domain_firstname == null) throw new Exception("Missing required parameter 'domain_firstname' when calling DomainsApi->updateDomainWhoisPrivacy")

    if (domain_lastname == null) throw new Exception("Missing required parameter 'domain_lastname' when calling DomainsApi->updateDomainWhoisPrivacy")

    if (domain_email == null) throw new Exception("Missing required parameter 'domain_email' when calling DomainsApi->updateDomainWhoisPrivacy")

    if (domain_address == null) throw new Exception("Missing required parameter 'domain_address' when calling DomainsApi->updateDomainWhoisPrivacy")

    if (domain_address2 == null) throw new Exception("Missing required parameter 'domain_address2' when calling DomainsApi->updateDomainWhoisPrivacy")

    if (domain_address3 == null) throw new Exception("Missing required parameter 'domain_address3' when calling DomainsApi->updateDomainWhoisPrivacy")

    if (domain_city == null) throw new Exception("Missing required parameter 'domain_city' when calling DomainsApi->updateDomainWhoisPrivacy")

    if (domain_state == null) throw new Exception("Missing required parameter 'domain_state' when calling DomainsApi->updateDomainWhoisPrivacy")

    if (domain_zip == null) throw new Exception("Missing required parameter 'domain_zip' when calling DomainsApi->updateDomainWhoisPrivacy")

    if (domain_country == null) throw new Exception("Missing required parameter 'domain_country' when calling DomainsApi->updateDomainWhoisPrivacy")

    if (domain_phone == null) throw new Exception("Missing required parameter 'domain_phone' when calling DomainsApi->updateDomainWhoisPrivacy")

    if (domain_fax == null) throw new Exception("Missing required parameter 'domain_fax' when calling DomainsApi->updateDomainWhoisPrivacy")

    if (domain_company == null) throw new Exception("Missing required parameter 'domain_company' when calling DomainsApi->updateDomainWhoisPrivacy")

    if (domain_extra == null) throw new Exception("Missing required parameter 'domain_extra' when calling DomainsApi->updateDomainWhoisPrivacy")


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }


}
