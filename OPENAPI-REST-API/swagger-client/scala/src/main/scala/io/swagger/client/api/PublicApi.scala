/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * OpenAPI spec version: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */
package io.swagger.client.api

import java.text.SimpleDateFormat

import io.swagger.client.model.BuyItNowList
import io.swagger.client.model.CaptchaResponse
import io.swagger.client.model.LoginErrorResponse
import io.swagger.client.model.LoginInfo
import io.swagger.client.model.LoginSubmissionExample
import io.swagger.client.model.LoginSubmissionExample_grecaptcharesponse
import io.swagger.client.model.LoginSuccessResponse
import io.swagger.client.model.OauthBody
import io.swagger.client.model.OauthBody2
import io.swagger.client.model.ServicesInfo
import io.swagger.client.model.inline_response_200_5
import io.swagger.client.model.inline_response_200_6
import io.swagger.client.model.inline_response_200_7
import io.swagger.client.model.inline_response_401
import io.swagger.client.{ApiInvoker, ApiException}

import com.sun.jersey.multipart.FormDataMultiPart
import com.sun.jersey.multipart.file.FileDataBodyPart

import javax.ws.rs.core.MediaType

import java.io.File
import java.util.Date
import java.util.TimeZone

import scala.collection.mutable.HashMap

import com.wordnik.swagger.client._
import scala.concurrent.Future
import collection.mutable

import java.net.URI

import com.wordnik.swagger.client.ClientResponseReaders.Json4sFormatsReader._
import com.wordnik.swagger.client.RequestWriters.Json4sFormatsWriter._

import scala.concurrent.ExecutionContext.Implicits.global
import scala.concurrent._
import scala.concurrent.duration._
import scala.util.{Failure, Success, Try}

import org.json4s._

class PublicApi(
  val defBasePath: String = "https://my.interserver.net/apiv2",
  defApiInvoker: ApiInvoker = ApiInvoker
) {
  private lazy val dateTimeFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSSZ")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  private val dateFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  implicit val formats = new org.json4s.DefaultFormats {
    override def dateFormatter = dateTimeFormatter
  }
  implicit val stringReader: ClientResponseReader[String] = ClientResponseReaders.StringReader
  implicit val unitReader: ClientResponseReader[Unit] = ClientResponseReaders.UnitReader
  implicit val jvalueReader: ClientResponseReader[JValue] = ClientResponseReaders.JValueReader
  implicit val jsonReader: ClientResponseReader[Nothing] = JsonFormatsReader
  implicit val stringWriter: RequestWriter[String] = RequestWriters.StringWriter
  implicit val jsonWriter: RequestWriter[Nothing] = JsonFormatsWriter

  var basePath: String = defBasePath
  var apiInvoker: ApiInvoker = defApiInvoker

  def addHeader(key: String, value: String): mutable.HashMap[String, String] = {
    apiInvoker.defaultHeaders += key -> value
  }

  val config: SwaggerConfig = SwaggerConfig.forUrl(new URI(defBasePath))
  val client = new RestClient(config)
  val helper = new PublicApiAsyncHelper(client, config)

  /**
   * Get Captcha Challenge
   * Returns a base64-encoded JPEG captcha image for human verification. Display this image to the user and submit their response along with the registration or order form. No authentication is required for this endpoint.
   *
   * @return CaptchaResponse
   */
  def getCaptcha(): Option[CaptchaResponse] = {
    val await = Try(Await.result(getCaptchaAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Captcha Challenge asynchronously
   * Returns a base64-encoded JPEG captcha image for human verification. Display this image to the user and submit their response along with the registration or order form. No authentication is required for this endpoint.
   *
   * @return Future(CaptchaResponse)
   */
  def getCaptchaAsync(): Future[CaptchaResponse] = {
      helper.getCaptcha()
  }

  /**
   * Get Countries
   * Returns a map of country codes to country names. Use the optional &#x60;fetch_by&#x60; parameter to select the key format: &#x60;iso2&#x60; (default, two-letter codes), &#x60;iso3&#x60; (three-letter codes), or &#x60;numcode&#x60; (numeric codes). Use these values when populating country fields in account registration or domain contact forms.
   *
   * @param fetch_by Get countries by iso2 or iso3 or numcode (optional)
   * @return Any
   */
  def getCountries(fetch_by: Option[String] = None): Option[Any] = {
    val await = Try(Await.result(getCountriesAsync(fetch_by), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Countries asynchronously
   * Returns a map of country codes to country names. Use the optional &#x60;fetch_by&#x60; parameter to select the key format: &#x60;iso2&#x60; (default, two-letter codes), &#x60;iso3&#x60; (three-letter codes), or &#x60;numcode&#x60; (numeric codes). Use these values when populating country fields in account registration or domain contact forms.
   *
   * @param fetch_by Get countries by iso2 or iso3 or numcode (optional)
   * @return Future(Any)
   */
  def getCountriesAsync(fetch_by: Option[String] = None): Future[Any] = {
      helper.getCountries(fetch_by)
  }

  /**
   * Get Server Info
   * Returns metadata about the API server&#x27;s available modules, service packages, categories, and types. This public endpoint does not require authentication and is useful for discovering supported services before placing orders.
   *
   * @return ServicesInfo
   */
  def getInfo(): Option[ServicesInfo] = {
    val await = Try(Await.result(getInfoAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Server Info asynchronously
   * Returns metadata about the API server&#x27;s available modules, service packages, categories, and types. This public endpoint does not require authentication and is useful for discovering supported services before placing orders.
   *
   * @return Future(ServicesInfo)
   */
  def getInfoAsync(): Future[ServicesInfo] = {
      helper.getInfo()
  }

  /**
   * Get Login Info
   * Gets the various pieces of information useful for generating a login page.
   *
   * @return LoginInfo
   */
  def getLoginInfo(): Option[LoginInfo] = {
    val await = Try(Await.result(getLoginInfoAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Login Info asynchronously
   * Gets the various pieces of information useful for generating a login page.
   *
   * @return Future(LoginInfo)
   */
  def getLoginInfoAsync(): Future[LoginInfo] = {
      helper.getLoginInfo()
  }

  /**
   * List Marketplace Servers
   * Returns the list of available Rapid Deploy dedicated servers with current pricing. Each entry includes CPU, memory, disk, bandwidth, IP allocation, and location details. These servers are pre-configured and can be provisioned immediately after purchase.
   *
   * @return BuyItNowList
   */
  def getMPServers(): Option[BuyItNowList] = {
    val await = Try(Await.result(getMPServersAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List Marketplace Servers asynchronously
   * Returns the list of available Rapid Deploy dedicated servers with current pricing. Each entry includes CPU, memory, disk, bandwidth, IP allocation, and location details. These servers are pre-configured and can be provisioned immediately after purchase.
   *
   * @return Future(BuyItNowList)
   */
  def getMPServersAsync(): Future[BuyItNowList] = {
      helper.getMPServers()
  }

  /**
   * Get OAuth Redirect URL
   * Returns the OAuth redirect URL for the specified provider. Redirect the user to this URL to begin the OAuth authentication flow. After the user authorizes, the provider will redirect back to your callback endpoint where you should call &#x60;POST /oauth&#x60;.
   *
   * @param provider The OAuth provider name (e.g. &#x60;Google&#x60;). 
   * @return inline_response_200_5
   */
  def getOauthRedirect(provider: String): Option[inline_response_200_5] = {
    val await = Try(Await.result(getOauthRedirectAsync(provider), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get OAuth Redirect URL asynchronously
   * Returns the OAuth redirect URL for the specified provider. Redirect the user to this URL to begin the OAuth authentication flow. After the user authorizes, the provider will redirect back to your callback endpoint where you should call &#x60;POST /oauth&#x60;.
   *
   * @param provider The OAuth provider name (e.g. &#x60;Google&#x60;). 
   * @return Future(inline_response_200_5)
   */
  def getOauthRedirectAsync(provider: String): Future[inline_response_200_5] = {
      helper.getOauthRedirect(provider)
  }

  /**
   * Get Available Timezones
   * Returns the complete list of available timezone identifiers (e.g. &#x60;America/New_York&#x60;, &#x60;UTC&#x60;). Use these values when updating timezone settings on your account or on VPS and QuickServer services via &#x60;/vps/{id}/change_timezone&#x60; or &#x60;/qs/{id}/change_timezone&#x60;.
   *
   * @return List[String]
   */
  def getTimezones(): Option[List[String]] = {
    val await = Try(Await.result(getTimezonesAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Available Timezones asynchronously
   * Returns the complete list of available timezone identifiers (e.g. &#x60;America/New_York&#x60;, &#x60;UTC&#x60;). Use these values when updating timezone settings on your account or on VPS and QuickServer services via &#x60;/vps/{id}/change_timezone&#x60; or &#x60;/qs/{id}/change_timezone&#x60;.
   *
   * @return Future(List[String])
   */
  def getTimezonesAsync(): Future[List[String]] = {
      helper.getTimezones()
  }

  /**
   * Complete OAuth Two-Factor Verification
   * Completes the OAuth login by verifying a two-factor authentication code. Use the &#x60;account_id&#x60; returned by &#x60;POST /oauth&#x60; when the response contained &#x60;error_code: \&quot;2fa_required\&quot;&#x60;.
   *
   * @param body  
   * @param account_id  
   * @param code  
   * @return inline_response_200_7
   */
  def patchOauthTwoFactor(body: OauthBody2, account_id: Integer, code: String): Option[inline_response_200_7] = {
    val await = Try(Await.result(patchOauthTwoFactorAsync(body, account_id, code), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Complete OAuth Two-Factor Verification asynchronously
   * Completes the OAuth login by verifying a two-factor authentication code. Use the &#x60;account_id&#x60; returned by &#x60;POST /oauth&#x60; when the response contained &#x60;error_code: \&quot;2fa_required\&quot;&#x60;.
   *
   * @param body  
   * @param account_id  
   * @param code  
   * @return Future(inline_response_200_7)
   */
  def patchOauthTwoFactorAsync(body: OauthBody2, account_id: Integer, code: String): Future[inline_response_200_7] = {
      helper.patchOauthTwoFactor(body, account_id, code)
  }

  /**
   * Ping Server
   * Performs a single ping/pong request to let you know if the server is up.
   *
   * @return String
   */
  def pingServer(): Option[String] = {
    val await = Try(Await.result(pingServerAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Ping Server asynchronously
   * Performs a single ping/pong request to let you know if the server is up.
   *
   * @return Future(String)
   */
  def pingServerAsync(): Future[String] = {
      helper.pingServer()
  }

  /**
   * OAuth Callback
   * Handles the OAuth callback after the user authorizes with the provider. Returns the login status. If the account has two-factor authentication enabled, the response will include &#x60;error_code: \&quot;2fa_required\&quot;&#x60; and an &#x60;account_id&#x60;; use &#x60;PATCH /oauth&#x60; with the 2FA code and account ID to complete login.
   *
   * @param provider The OAuth provider name (e.g. &#x60;Google&#x60;). 
   * @param body  (optional)
   * @param provider  (optional)
   * @return inline_response_200_6
   */
  def postOauthCallback(provider: String, body: Option[OauthBody] = None, provider: Option[String] = None): Option[inline_response_200_6] = {
    val await = Try(Await.result(postOauthCallbackAsync(provider, body, provider), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * OAuth Callback asynchronously
   * Handles the OAuth callback after the user authorizes with the provider. Returns the login status. If the account has two-factor authentication enabled, the response will include &#x60;error_code: \&quot;2fa_required\&quot;&#x60; and an &#x60;account_id&#x60;; use &#x60;PATCH /oauth&#x60; with the 2FA code and account ID to complete login.
   *
   * @param provider The OAuth provider name (e.g. &#x60;Google&#x60;). 
   * @param body  (optional)
   * @param provider  (optional)
   * @return Future(inline_response_200_6)
   */
  def postOauthCallbackAsync(provider: String, body: Option[OauthBody] = None, provider: Option[String] = None): Future[inline_response_200_6] = {
      helper.postOauthCallback(provider, body, provider)
  }

  /**
   * Submit Login Information
   * Authenticates using the provided login credentials and returns a session token. Include the returned session ID in the &#x60;sessionid&#x60; header on subsequent requests. If authentication fails, a &#x60;402&#x60; error is returned.
   *
   * @param login  
   * @param passwd  
   * @param remember  
   * @param g_recaptcha_response  
   * @param tfa  
   * @param body  
   * @return LoginSuccessResponse
   */
  def submitLogin(login: String, passwd: String, remember: String, g_recaptcha_response: LoginSubmissionExample_grecaptcharesponse, tfa: String, body: LoginSubmissionExample): Option[LoginSuccessResponse] = {
    val await = Try(Await.result(submitLoginAsync(login, passwd, remember, g_recaptcha_response, tfa, body), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Submit Login Information asynchronously
   * Authenticates using the provided login credentials and returns a session token. Include the returned session ID in the &#x60;sessionid&#x60; header on subsequent requests. If authentication fails, a &#x60;402&#x60; error is returned.
   *
   * @param login  
   * @param passwd  
   * @param remember  
   * @param g_recaptcha_response  
   * @param tfa  
   * @param body  
   * @return Future(LoginSuccessResponse)
   */
  def submitLoginAsync(login: String, passwd: String, remember: String, g_recaptcha_response: LoginSubmissionExample_grecaptcharesponse, tfa: String, body: LoginSubmissionExample): Future[LoginSuccessResponse] = {
      helper.submitLogin(login, passwd, remember, g_recaptcha_response, tfa, body)
  }

  /**
   * Submit Signup Information
   * Creates a new account in our system using the provided information.
   *
   * @param body  (optional)
   * @return void
   */
  def submitSignup(body: Option[LoginSubmissionExample] = None) = {
    val await = Try(Await.result(submitSignupAsync(body), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Submit Signup Information asynchronously
   * Creates a new account in our system using the provided information.
   *
   * @param body  (optional)
   * @return Future(void)
   */
  def submitSignupAsync(body: Option[LoginSubmissionExample] = None) = {
      helper.submitSignup(body)
  }

}

class PublicApiAsyncHelper(client: TransportClient, config: SwaggerConfig) extends ApiClient(client, config) {

  def getCaptcha()(implicit reader: ClientResponseReader[CaptchaResponse]): Future[CaptchaResponse] = {
    // create path and map variables
    val path = (addFmt("/captcha"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getCountries(fetch_by: Option[String] = None
    )(implicit reader: ClientResponseReader[Any]): Future[Any] = {
    // create path and map variables
    val path = (addFmt("/account/countries"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    fetch_by match {
      case Some(param) => queryParams += "fetch_by" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getInfo()(implicit reader: ClientResponseReader[ServicesInfo]): Future[ServicesInfo] = {
    // create path and map variables
    val path = (addFmt("/info"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getLoginInfo()(implicit reader: ClientResponseReader[LoginInfo]): Future[LoginInfo] = {
    // create path and map variables
    val path = (addFmt("/login"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getMPServers()(implicit reader: ClientResponseReader[BuyItNowList]): Future[BuyItNowList] = {
    // create path and map variables
    val path = (addFmt("/buy_now_servers_list"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getOauthRedirect(provider: String)(implicit reader: ClientResponseReader[inline_response_200_5]): Future[inline_response_200_5] = {
    // create path and map variables
    val path = (addFmt("/oauth"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (provider == null) throw new Exception("Missing required parameter 'provider' when calling PublicApi->getOauthRedirect")

    queryParams += "provider" -> provider.toString

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getTimezones()(implicit reader: ClientResponseReader[List[String]]): Future[List[String]] = {
    // create path and map variables
    val path = (addFmt("/account/timezones"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def patchOauthTwoFactor(body: OauthBody2,
    account_id: Integer,
    code: String)(implicit reader: ClientResponseReader[inline_response_200_7], writer: RequestWriter[OauthBody2]): Future[inline_response_200_7] = {
    // create path and map variables
    val path = (addFmt("/oauth"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (body == null) throw new Exception("Missing required parameter 'body' when calling PublicApi->patchOauthTwoFactor")
    if (code == null) throw new Exception("Missing required parameter 'code' when calling PublicApi->patchOauthTwoFactor")


    val resFuture = client.submit("PATCH", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def pingServer()(implicit reader: ClientResponseReader[String]): Future[String] = {
    // create path and map variables
    val path = (addFmt("/ping"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postOauthCallback(provider: String,
    body: Option[OauthBody] = None,
    provider: Option[String] = None
    )(implicit reader: ClientResponseReader[inline_response_200_6], writer: RequestWriter[Option[OauthBody]]): Future[inline_response_200_6] = {
    // create path and map variables
    val path = (addFmt("/oauth"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (provider == null) throw new Exception("Missing required parameter 'provider' when calling PublicApi->postOauthCallback")

    queryParams += "provider" -> provider.toString

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def submitLogin(login: String,
    passwd: String,
    remember: String,
    g_recaptcha_response: LoginSubmissionExample_grecaptcharesponse,
    tfa: String,
    body: LoginSubmissionExample)(implicit reader: ClientResponseReader[LoginSuccessResponse], writer: RequestWriter[LoginSubmissionExample]): Future[LoginSuccessResponse] = {
    // create path and map variables
    val path = (addFmt("/login"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (login == null) throw new Exception("Missing required parameter 'login' when calling PublicApi->submitLogin")

    if (passwd == null) throw new Exception("Missing required parameter 'passwd' when calling PublicApi->submitLogin")

    if (remember == null) throw new Exception("Missing required parameter 'remember' when calling PublicApi->submitLogin")

    if (g_recaptcha_response == null) throw new Exception("Missing required parameter 'g_recaptcha_response' when calling PublicApi->submitLogin")
    if (tfa == null) throw new Exception("Missing required parameter 'tfa' when calling PublicApi->submitLogin")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling PublicApi->submitLogin")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def submitSignup(body: Option[LoginSubmissionExample] = None
    )(implicit reader: ClientResponseReader[Unit], writer: RequestWriter[Option[LoginSubmissionExample]]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/signup"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }


}
