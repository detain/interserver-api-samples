/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * OpenAPI spec version: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */
package io.swagger.client.api

import java.text.SimpleDateFormat

import io.swagger.client.model.Backup
import io.swagger.client.model.BackupLoginResponse
import io.swagger.client.model.BackupOrderPostResponse
import io.swagger.client.model.BackupOrderPutRequest
import io.swagger.client.model.BackupOrderPutResponse
import io.swagger.client.model.BackupRow
import io.swagger.client.model.BackupsOrder
import io.swagger.client.model.ChargeInvoiceRows
import io.swagger.client.model.SuccessTextResponse
import io.swagger.client.model.inline_response_200_1
import io.swagger.client.model.inline_response_401
import io.swagger.client.{ApiInvoker, ApiException}

import com.sun.jersey.multipart.FormDataMultiPart
import com.sun.jersey.multipart.file.FileDataBodyPart

import javax.ws.rs.core.MediaType

import java.io.File
import java.util.Date
import java.util.TimeZone

import scala.collection.mutable.HashMap

import com.wordnik.swagger.client._
import scala.concurrent.Future
import collection.mutable

import java.net.URI

import com.wordnik.swagger.client.ClientResponseReaders.Json4sFormatsReader._
import com.wordnik.swagger.client.RequestWriters.Json4sFormatsWriter._

import scala.concurrent.ExecutionContext.Implicits.global
import scala.concurrent._
import scala.concurrent.duration._
import scala.util.{Failure, Success, Try}

import org.json4s._

class BackupsApi(
  val defBasePath: String = "https://my.interserver.net/apiv2",
  defApiInvoker: ApiInvoker = ApiInvoker
) {
  private lazy val dateTimeFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSSZ")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  private val dateFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  implicit val formats = new org.json4s.DefaultFormats {
    override def dateFormatter = dateTimeFormatter
  }
  implicit val stringReader: ClientResponseReader[String] = ClientResponseReaders.StringReader
  implicit val unitReader: ClientResponseReader[Unit] = ClientResponseReaders.UnitReader
  implicit val jvalueReader: ClientResponseReader[JValue] = ClientResponseReaders.JValueReader
  implicit val jsonReader: ClientResponseReader[Nothing] = JsonFormatsReader
  implicit val stringWriter: RequestWriter[String] = RequestWriters.StringWriter
  implicit val jsonWriter: RequestWriter[Nothing] = JsonFormatsWriter

  var basePath: String = defBasePath
  var apiInvoker: ApiInvoker = defApiInvoker

  def addHeader(key: String, value: String): mutable.HashMap[String, String] = {
    apiInvoker.defaultHeaders += key -> value
  }

  val config: SwaggerConfig = SwaggerConfig.forUrl(new URI(defBasePath))
  val client = new RestClient(config)
  val helper = new BackupsApiAsyncHelper(client, config)

  /**
   * Place Backup Order
   * Places a new backup storage order and generates an invoice. On success, the response includes invoice IDs that can be used with &#x60;/billing/invoices/{id}&#x60; to view the invoice or &#x60;/pay/{method}/{invoices}&#x60; to complete payment. The service is provisioned after payment is confirmed.
   *
   * @param validateOnly  
   * @param serviceType  
   * @param coupon  
   * @param body  
   * @return BackupOrderPostResponse
   */
  def addBackup(validateOnly: Boolean, serviceType: Integer, coupon: String, body: BackupOrderPutRequest): Option[BackupOrderPostResponse] = {
    val await = Try(Await.result(addBackupAsync(validateOnly, serviceType, coupon, body), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Place Backup Order asynchronously
   * Places a new backup storage order and generates an invoice. On success, the response includes invoice IDs that can be used with &#x60;/billing/invoices/{id}&#x60; to view the invoice or &#x60;/pay/{method}/{invoices}&#x60; to complete payment. The service is provisioned after payment is confirmed.
   *
   * @param validateOnly  
   * @param serviceType  
   * @param coupon  
   * @param body  
   * @return Future(BackupOrderPostResponse)
   */
  def addBackupAsync(validateOnly: Boolean, serviceType: Integer, coupon: String, body: BackupOrderPutRequest): Future[BackupOrderPostResponse] = {
      helper.addBackup(validateOnly, serviceType, coupon, body)
  }

  /**
   * Cancel Backup Service
   * Cancels the specified backup storage service. The service remains accessible until the end of the current billing period. This action cannot be undone; a new order must be placed to restore service.
   *
   * @param id The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service. 
   * @return inline_response_200_1
   */
  def cancelBackup(id: Integer): Option[inline_response_200_1] = {
    val await = Try(Await.result(cancelBackupAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Cancel Backup Service asynchronously
   * Cancels the specified backup storage service. The service remains accessible until the end of the current billing period. This action cannot be undone; a new order must be placed to restore service.
   *
   * @param id The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service. 
   * @return Future(inline_response_200_1)
   */
  def cancelBackupAsync(id: Integer): Future[inline_response_200_1] = {
      helper.cancelBackup(id)
  }

  /**
   * Get Backup Service Details
   * Returns detailed service information for the specified backup storage order, including &#x60;backup_username&#x60;, &#x60;backup_ip&#x60;, &#x60;backup_status&#x60;, and &#x60;backup_quota&#x60; in &#x60;serviceInfo&#x60;. Also returns &#x60;client_links&#x60;, &#x60;billingDetails&#x60;, &#x60;extraInfoTables&#x60;, &#x60;package&#x60;, and &#x60;custCurrency&#x60;.
   *
   * @param id The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service. 
   * @return Backup
   */
  def getBackupInfo(id: Integer): Option[Backup] = {
    val await = Try(Await.result(getBackupInfoAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Backup Service Details asynchronously
   * Returns detailed service information for the specified backup storage order, including &#x60;backup_username&#x60;, &#x60;backup_ip&#x60;, &#x60;backup_status&#x60;, and &#x60;backup_quota&#x60; in &#x60;serviceInfo&#x60;. Also returns &#x60;client_links&#x60;, &#x60;billingDetails&#x60;, &#x60;extraInfoTables&#x60;, &#x60;package&#x60;, and &#x60;custCurrency&#x60;.
   *
   * @param id The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service. 
   * @return Future(Backup)
   */
  def getBackupInfoAsync(id: Integer): Future[Backup] = {
      helper.getBackupInfo(id)
  }

  /**
   * Get Backup Order Invoices
   * Retrieves invoices associated with the backup storage order. Use this to confirm billing status or locate invoices for payment.
   *
   * @param id The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service. 
   * @return ChargeInvoiceRows
   */
  def getBackupInvoices(id: Integer): Option[ChargeInvoiceRows] = {
    val await = Try(Await.result(getBackupInvoicesAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Backup Order Invoices asynchronously
   * Retrieves invoices associated with the backup storage order. Use this to confirm billing status or locate invoices for payment.
   *
   * @param id The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service. 
   * @return Future(ChargeInvoiceRows)
   */
  def getBackupInvoicesAsync(id: Integer): Future[ChargeInvoiceRows] = {
      helper.getBackupInvoices(id)
  }

  /**
   * Get Backup Storage Panel Login
   * Creates and returns a login session URL for the backup storage panel. The returned session URL can be used to redirect the user directly into the storage management interface without requiring separate credentials.
   *
   * @param id The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service. 
   * @return BackupLoginResponse
   */
  def getBackupLogin(id: Integer): Option[BackupLoginResponse] = {
    val await = Try(Await.result(getBackupLoginAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Backup Storage Panel Login asynchronously
   * Creates and returns a login session URL for the backup storage panel. The returned session URL can be used to redirect the user directly into the storage management interface without requiring separate credentials.
   *
   * @param id The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service. 
   * @return Future(BackupLoginResponse)
   */
  def getBackupLoginAsync(id: Integer): Future[BackupLoginResponse] = {
      helper.getBackupLogin(id)
  }

  /**
   * List Backup Services
   * Returns all backup storage services on your account. Each entry includes the &#x60;backup_id&#x60;, &#x60;backup_username&#x60;, &#x60;backup_ip&#x60;, &#x60;backup_status&#x60;, and &#x60;backup_quota&#x60;. Use the &#x60;backup_id&#x60; with &#x60;/backups/{id}&#x60; to retrieve full service details or &#x60;/backups/{id}/login&#x60; to obtain a storage panel session.
   *
   * @return List[BackupRow]
   */
  def getBackupsList(): Option[List[BackupRow]] = {
    val await = Try(Await.result(getBackupsListAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List Backup Services asynchronously
   * Returns all backup storage services on your account. Each entry includes the &#x60;backup_id&#x60;, &#x60;backup_username&#x60;, &#x60;backup_ip&#x60;, &#x60;backup_status&#x60;, and &#x60;backup_quota&#x60;. Use the &#x60;backup_id&#x60; with &#x60;/backups/{id}&#x60; to retrieve full service details or &#x60;/backups/{id}/login&#x60; to obtain a storage panel session.
   *
   * @return Future(List[BackupRow])
   */
  def getBackupsListAsync(): Future[List[BackupRow]] = {
      helper.getBackupsList()
  }

  /**
   * Resend Backup Welcome Email
   * Resends the welcome email for the specified backup service. The email contains connection credentials and setup instructions. Use this when the original welcome email was lost or never received.
   *
   * @param id The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service. 
   * @return SuccessTextResponse
   */
  def getBackupsWelcomeEmail(id: Integer): Option[SuccessTextResponse] = {
    val await = Try(Await.result(getBackupsWelcomeEmailAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Resend Backup Welcome Email asynchronously
   * Resends the welcome email for the specified backup service. The email contains connection credentials and setup instructions. Use this when the original welcome email was lost or never received.
   *
   * @param id The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service. 
   * @return Future(SuccessTextResponse)
   */
  def getBackupsWelcomeEmailAsync(id: Integer): Future[SuccessTextResponse] = {
      helper.getBackupsWelcomeEmail(id)
  }

  /**
   * Get Backup Order Form Data
   * Returns available backup storage plans, pricing tiers, and form metadata needed to build an order form. Use the service type IDs from this response when submitting a validation request via &#x60;PUT /backups/order&#x60; or placing an order via &#x60;POST /backups/order&#x60;.
   *
   * @return BackupsOrder
   */
  def getNewBackup(): Option[BackupsOrder] = {
    val await = Try(Await.result(getNewBackupAsync(), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get Backup Order Form Data asynchronously
   * Returns available backup storage plans, pricing tiers, and form metadata needed to build an order form. Use the service type IDs from this response when submitting a validation request via &#x60;PUT /backups/order&#x60; or placing an order via &#x60;POST /backups/order&#x60;.
   *
   * @return Future(BackupsOrder)
   */
  def getNewBackupAsync(): Future[BackupsOrder] = {
      helper.getNewBackup()
  }

  /**
   * Update Backup Information
   * Updates backup storage service metadata, such as stored credentials or settings for the order.
   *
   * @param id The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service. 
   * @return void
   */
  def updateBackupInfo(id: Integer) = {
    val await = Try(Await.result(updateBackupInfoAsync(id), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Update Backup Information asynchronously
   * Updates backup storage service metadata, such as stored credentials or settings for the order.
   *
   * @param id The backup service ID. Use the &#x60;backup_id&#x60; from &#x60;GET /backups&#x60; to identify the service. 
   * @return Future(void)
   */
  def updateBackupInfoAsync(id: Integer) = {
      helper.updateBackupInfo(id)
  }

  /**
   * Validate Backup Order
   * Validates a backup storage order without placing it, returning calculated pricing and any validation errors. Use this to display a confirmation screen with the final price before submitting the order via &#x60;POST /backups/order&#x60;.
   *
   * @param validateOnly  
   * @param serviceType  
   * @param coupon  
   * @param body  
   * @return BackupOrderPutResponse
   */
  def validateBackupOrder(validateOnly: Boolean, serviceType: Integer, coupon: String, body: BackupOrderPutRequest): Option[BackupOrderPutResponse] = {
    val await = Try(Await.result(validateBackupOrderAsync(validateOnly, serviceType, coupon, body), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Validate Backup Order asynchronously
   * Validates a backup storage order without placing it, returning calculated pricing and any validation errors. Use this to display a confirmation screen with the final price before submitting the order via &#x60;POST /backups/order&#x60;.
   *
   * @param validateOnly  
   * @param serviceType  
   * @param coupon  
   * @param body  
   * @return Future(BackupOrderPutResponse)
   */
  def validateBackupOrderAsync(validateOnly: Boolean, serviceType: Integer, coupon: String, body: BackupOrderPutRequest): Future[BackupOrderPutResponse] = {
      helper.validateBackupOrder(validateOnly, serviceType, coupon, body)
  }

}

class BackupsApiAsyncHelper(client: TransportClient, config: SwaggerConfig) extends ApiClient(client, config) {

  def addBackup(validateOnly: Boolean,
    serviceType: Integer,
    coupon: String,
    body: BackupOrderPutRequest)(implicit reader: ClientResponseReader[BackupOrderPostResponse], writer: RequestWriter[BackupOrderPutRequest]): Future[BackupOrderPostResponse] = {
    // create path and map variables
    val path = (addFmt("/backups/order"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (coupon == null) throw new Exception("Missing required parameter 'coupon' when calling BackupsApi->addBackup")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling BackupsApi->addBackup")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def cancelBackup(id: Integer)(implicit reader: ClientResponseReader[inline_response_200_1]): Future[inline_response_200_1] = {
    // create path and map variables
    val path = (addFmt("/backups/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getBackupInfo(id: Integer)(implicit reader: ClientResponseReader[Backup]): Future[Backup] = {
    // create path and map variables
    val path = (addFmt("/backups/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getBackupInvoices(id: Integer)(implicit reader: ClientResponseReader[ChargeInvoiceRows]): Future[ChargeInvoiceRows] = {
    // create path and map variables
    val path = (addFmt("/backups/{id}/invoices")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getBackupLogin(id: Integer)(implicit reader: ClientResponseReader[BackupLoginResponse]): Future[BackupLoginResponse] = {
    // create path and map variables
    val path = (addFmt("/backups/{id}/login")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getBackupsList()(implicit reader: ClientResponseReader[List[BackupRow]]): Future[List[BackupRow]] = {
    // create path and map variables
    val path = (addFmt("/backups"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getBackupsWelcomeEmail(id: Integer)(implicit reader: ClientResponseReader[SuccessTextResponse]): Future[SuccessTextResponse] = {
    // create path and map variables
    val path = (addFmt("/backups/{id}/welcome_email")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getNewBackup()(implicit reader: ClientResponseReader[BackupsOrder]): Future[BackupsOrder] = {
    // create path and map variables
    val path = (addFmt("/backups/order"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def updateBackupInfo(id: Integer)(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/backups/{id}")
      replaceAll("\\{" + "id" + "\\}", id.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]


    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def validateBackupOrder(validateOnly: Boolean,
    serviceType: Integer,
    coupon: String,
    body: BackupOrderPutRequest)(implicit reader: ClientResponseReader[BackupOrderPutResponse], writer: RequestWriter[BackupOrderPutRequest]): Future[BackupOrderPutResponse] = {
    // create path and map variables
    val path = (addFmt("/backups/order"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (coupon == null) throw new Exception("Missing required parameter 'coupon' when calling BackupsApi->validateBackupOrder")

    if (body == null) throw new Exception("Missing required parameter 'body' when calling BackupsApi->validateBackupOrder")

    val resFuture = client.submit("PUT", path, queryParams.toMap, headerParams.toMap, writer.write(body))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }


}
