/*
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * OpenAPI spec version: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 3.0.78
 *
 * Do not edit the class manually.
 *
 */
import ApiClient from '../ApiClient';

/**
 * The ServerSwitchport model module.
 * @module model/ServerSwitchport
 * @version 0.9.0
 */
export default class ServerSwitchport {
  /**
   * Constructs a new <code>ServerSwitchport</code>.
   * @alias module:model/ServerSwitchport
   * @class
   * @param switchport_id {Number} Unique identifier for the switchport.
   * @param switch_id {String} Unique identifier for the switch associated with the switchport.
   * @param _switch {String} Name of the switch associated with the switchport.
   * @param port {String} Port name on the switch.
   * @param blade {String} Blade name associated with the port.
   * @param justport {String} Port identifier.
   * @param graph_id {String} Identifier for the graph associated with the switchport.
   * @param asset_id {Number} Unique identifier of the asset associated with the switchport.
   */
  constructor(switchport_id, switch_id, _switch, port, blade, justport, graph_id, asset_id) {
    this.switchport_id = switchport_id;
    this.switch_id = switch_id;
    this._switch = _switch;
    this.port = port;
    this.blade = blade;
    this.justport = justport;
    this.graph_id = graph_id;
    this.asset_id = asset_id;
  }

  /**
   * Constructs a <code>ServerSwitchport</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/ServerSwitchport} obj Optional instance to populate.
   * @return {module:model/ServerSwitchport} The populated <code>ServerSwitchport</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new ServerSwitchport();
      if (data.hasOwnProperty('switchport_id'))
        obj.switchport_id = ApiClient.convertToType(data['switchport_id'], 'Number');
      if (data.hasOwnProperty('switch_id'))
        obj.switch_id = ApiClient.convertToType(data['switch_id'], 'String');
      if (data.hasOwnProperty('switch'))
        obj._switch = ApiClient.convertToType(data['switch'], 'String');
      if (data.hasOwnProperty('port'))
        obj.port = ApiClient.convertToType(data['port'], 'String');
      if (data.hasOwnProperty('blade'))
        obj.blade = ApiClient.convertToType(data['blade'], 'String');
      if (data.hasOwnProperty('justport'))
        obj.justport = ApiClient.convertToType(data['justport'], 'String');
      if (data.hasOwnProperty('graph_id'))
        obj.graph_id = ApiClient.convertToType(data['graph_id'], 'String');
      if (data.hasOwnProperty('vlans'))
        obj.vlans = ApiClient.convertToType(data['vlans'], ['String']);
      if (data.hasOwnProperty('vlans6'))
        obj.vlans6 = ApiClient.convertToType(data['vlans6'], ['String']);
      if (data.hasOwnProperty('asset_id'))
        obj.asset_id = ApiClient.convertToType(data['asset_id'], 'Number');
    }
    return obj;
  }
}

/**
 * Unique identifier for the switchport.
 * @member {Number} switchport_id
 */
ServerSwitchport.prototype.switchport_id = undefined;

/**
 * Unique identifier for the switch associated with the switchport.
 * @member {String} switch_id
 */
ServerSwitchport.prototype.switch_id = undefined;

/**
 * Name of the switch associated with the switchport.
 * @member {String} _switch
 */
ServerSwitchport.prototype._switch = undefined;

/**
 * Port name on the switch.
 * @member {String} port
 */
ServerSwitchport.prototype.port = undefined;

/**
 * Blade name associated with the port.
 * @member {String} blade
 */
ServerSwitchport.prototype.blade = undefined;

/**
 * Port identifier.
 * @member {String} justport
 */
ServerSwitchport.prototype.justport = undefined;

/**
 * Identifier for the graph associated with the switchport.
 * @member {String} graph_id
 */
ServerSwitchport.prototype.graph_id = undefined;

/**
 * List of VLANs associated with the switchport.
 * @member {Array.<String>} vlans
 */
ServerSwitchport.prototype.vlans = undefined;

/**
 * List of IPv6 VLANs associated with the switchport.
 * @member {Array.<String>} vlans6
 */
ServerSwitchport.prototype.vlans6 = undefined;

/**
 * Unique identifier of the asset associated with the switchport.
 * @member {Number} asset_id
 */
ServerSwitchport.prototype.asset_id = undefined;

