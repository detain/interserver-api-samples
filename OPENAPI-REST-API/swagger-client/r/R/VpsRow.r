# InterServer Management API
#
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
#
# OpenAPI spec version: 0.9.0
# Contact: support@interserver.net
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' VpsRow Class
#'
#' @field vps_id 
#' @field vps_name 
#' @field repeat_invoices_cost 
#' @field vps_hostname 
#' @field vps_ip 
#' @field vps_status 
#' @field services_name 
#' @field vps_comment 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
VpsRow <- R6::R6Class(
  'VpsRow',
  public = list(
    `vps_id` = NULL,
    `vps_name` = NULL,
    `repeat_invoices_cost` = NULL,
    `vps_hostname` = NULL,
    `vps_ip` = NULL,
    `vps_status` = NULL,
    `services_name` = NULL,
    `vps_comment` = NULL,
    initialize = function(`vps_id`, `vps_name`, `repeat_invoices_cost`, `vps_hostname`, `vps_ip`, `vps_status`, `services_name`, `vps_comment`){
      if (!missing(`vps_id`)) {
        stopifnot(is.character(`vps_id`), length(`vps_id`) == 1)
        self$`vps_id` <- `vps_id`
      }
      if (!missing(`vps_name`)) {
        stopifnot(is.character(`vps_name`), length(`vps_name`) == 1)
        self$`vps_name` <- `vps_name`
      }
      if (!missing(`repeat_invoices_cost`)) {
        stopifnot(is.character(`repeat_invoices_cost`), length(`repeat_invoices_cost`) == 1)
        self$`repeat_invoices_cost` <- `repeat_invoices_cost`
      }
      if (!missing(`vps_hostname`)) {
        stopifnot(is.character(`vps_hostname`), length(`vps_hostname`) == 1)
        self$`vps_hostname` <- `vps_hostname`
      }
      if (!missing(`vps_ip`)) {
        stopifnot(is.character(`vps_ip`), length(`vps_ip`) == 1)
        self$`vps_ip` <- `vps_ip`
      }
      if (!missing(`vps_status`)) {
        stopifnot(is.character(`vps_status`), length(`vps_status`) == 1)
        self$`vps_status` <- `vps_status`
      }
      if (!missing(`services_name`)) {
        stopifnot(is.character(`services_name`), length(`services_name`) == 1)
        self$`services_name` <- `services_name`
      }
      if (!missing(`vps_comment`)) {
        stopifnot(is.character(`vps_comment`), length(`vps_comment`) == 1)
        self$`vps_comment` <- `vps_comment`
      }
    },
    toJSON = function() {
      VpsRowObject <- list()
      if (!is.null(self$`vps_id`)) {
        VpsRowObject[['vps_id']] <- self$`vps_id`
      }
      if (!is.null(self$`vps_name`)) {
        VpsRowObject[['vps_name']] <- self$`vps_name`
      }
      if (!is.null(self$`repeat_invoices_cost`)) {
        VpsRowObject[['repeat_invoices_cost']] <- self$`repeat_invoices_cost`
      }
      if (!is.null(self$`vps_hostname`)) {
        VpsRowObject[['vps_hostname']] <- self$`vps_hostname`
      }
      if (!is.null(self$`vps_ip`)) {
        VpsRowObject[['vps_ip']] <- self$`vps_ip`
      }
      if (!is.null(self$`vps_status`)) {
        VpsRowObject[['vps_status']] <- self$`vps_status`
      }
      if (!is.null(self$`services_name`)) {
        VpsRowObject[['services_name']] <- self$`services_name`
      }
      if (!is.null(self$`vps_comment`)) {
        VpsRowObject[['vps_comment']] <- self$`vps_comment`
      }

      VpsRowObject
    },
    fromJSON = function(VpsRowJson) {
      VpsRowObject <- jsonlite::fromJSON(VpsRowJson)
      if (!is.null(VpsRowObject$`vps_id`)) {
        self$`vps_id` <- VpsRowObject$`vps_id`
      }
      if (!is.null(VpsRowObject$`vps_name`)) {
        self$`vps_name` <- VpsRowObject$`vps_name`
      }
      if (!is.null(VpsRowObject$`repeat_invoices_cost`)) {
        self$`repeat_invoices_cost` <- VpsRowObject$`repeat_invoices_cost`
      }
      if (!is.null(VpsRowObject$`vps_hostname`)) {
        self$`vps_hostname` <- VpsRowObject$`vps_hostname`
      }
      if (!is.null(VpsRowObject$`vps_ip`)) {
        self$`vps_ip` <- VpsRowObject$`vps_ip`
      }
      if (!is.null(VpsRowObject$`vps_status`)) {
        self$`vps_status` <- VpsRowObject$`vps_status`
      }
      if (!is.null(VpsRowObject$`services_name`)) {
        self$`services_name` <- VpsRowObject$`services_name`
      }
      if (!is.null(VpsRowObject$`vps_comment`)) {
        self$`vps_comment` <- VpsRowObject$`vps_comment`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "vps_id": %s,
           "vps_name": %s,
           "repeat_invoices_cost": %s,
           "vps_hostname": %s,
           "vps_ip": %s,
           "vps_status": %s,
           "services_name": %s,
           "vps_comment": %s
        }',
        self$`vps_id`,
        self$`vps_name`,
        self$`repeat_invoices_cost`,
        self$`vps_hostname`,
        self$`vps_ip`,
        self$`vps_status`,
        self$`services_name`,
        self$`vps_comment`
      )
    },
    fromJSONString = function(VpsRowJson) {
      VpsRowObject <- jsonlite::fromJSON(VpsRowJson)
      self$`vps_id` <- VpsRowObject$`vps_id`
      self$`vps_name` <- VpsRowObject$`vps_name`
      self$`repeat_invoices_cost` <- VpsRowObject$`repeat_invoices_cost`
      self$`vps_hostname` <- VpsRowObject$`vps_hostname`
      self$`vps_ip` <- VpsRowObject$`vps_ip`
      self$`vps_status` <- VpsRowObject$`vps_status`
      self$`services_name` <- VpsRowObject$`services_name`
      self$`vps_comment` <- VpsRowObject$`vps_comment`
    }
  )
)
