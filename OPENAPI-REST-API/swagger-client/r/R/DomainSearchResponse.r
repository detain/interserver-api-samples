# InterServer Management API
#
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
#
# OpenAPI spec version: 0.9.0
# Contact: support@interserver.net
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' DomainSearchResponse Class
#'
#' @field success 
#' @field response_text 
#' @field response_time 
#' @field lookup 
#' @field suggest 
#' @field tlds 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
DomainSearchResponse <- R6::R6Class(
  'DomainSearchResponse',
  public = list(
    `success` = NULL,
    `response_text` = NULL,
    `response_time` = NULL,
    `lookup` = NULL,
    `suggest` = NULL,
    `tlds` = NULL,
    initialize = function(`success`, `response_text`, `response_time`, `lookup`, `suggest`, `tlds`){
      if (!missing(`success`)) {
        self$`success` <- `success`
      }
      if (!missing(`response_text`)) {
        stopifnot(is.character(`response_text`), length(`response_text`) == 1)
        self$`response_text` <- `response_text`
      }
      if (!missing(`response_time`)) {
        stopifnot(is.character(`response_time`), length(`response_time`) == 1)
        self$`response_time` <- `response_time`
      }
      if (!missing(`lookup`)) {
        stopifnot(is.list(`lookup`), length(`lookup`) != 0)
        lapply(`lookup`, function(x) stopifnot(R6::is.R6(x)))
        self$`lookup` <- `lookup`
      }
      if (!missing(`suggest`)) {
        stopifnot(is.list(`suggest`), length(`suggest`) != 0)
        lapply(`suggest`, function(x) stopifnot(R6::is.R6(x)))
        self$`suggest` <- `suggest`
      }
      if (!missing(`tlds`)) {
        stopifnot(is.list(`tlds`), length(`tlds`) != 0)
        lapply(`tlds`, function(x) stopifnot(is.character(x)))
        self$`tlds` <- `tlds`
      }
    },
    toJSON = function() {
      DomainSearchResponseObject <- list()
      if (!is.null(self$`success`)) {
        DomainSearchResponseObject[['success']] <- self$`success`
      }
      if (!is.null(self$`response_text`)) {
        DomainSearchResponseObject[['response_text']] <- self$`response_text`
      }
      if (!is.null(self$`response_time`)) {
        DomainSearchResponseObject[['response_time']] <- self$`response_time`
      }
      if (!is.null(self$`lookup`)) {
        DomainSearchResponseObject[['lookup']] <- lapply(self$`lookup`, function(x) x$toJSON())
      }
      if (!is.null(self$`suggest`)) {
        DomainSearchResponseObject[['suggest']] <- lapply(self$`suggest`, function(x) x$toJSON())
      }
      if (!is.null(self$`tlds`)) {
        DomainSearchResponseObject[['tlds']] <- self$`tlds`
      }

      DomainSearchResponseObject
    },
    fromJSON = function(DomainSearchResponseJson) {
      DomainSearchResponseObject <- jsonlite::fromJSON(DomainSearchResponseJson)
      if (!is.null(DomainSearchResponseObject$`success`)) {
        self$`success` <- DomainSearchResponseObject$`success`
      }
      if (!is.null(DomainSearchResponseObject$`response_text`)) {
        self$`response_text` <- DomainSearchResponseObject$`response_text`
      }
      if (!is.null(DomainSearchResponseObject$`response_time`)) {
        self$`response_time` <- DomainSearchResponseObject$`response_time`
      }
      if (!is.null(DomainSearchResponseObject$`lookup`)) {
        self$`lookup` <- lapply(DomainSearchResponseObject$`lookup`, function(x) {
          lookupObject <- TODO_OBJECT_MAPPING$new()
          lookupObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          lookupObject
        })
      }
      if (!is.null(DomainSearchResponseObject$`suggest`)) {
        self$`suggest` <- lapply(DomainSearchResponseObject$`suggest`, function(x) {
          suggestObject <- TODO_OBJECT_MAPPING$new()
          suggestObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          suggestObject
        })
      }
      if (!is.null(DomainSearchResponseObject$`tlds`)) {
        self$`tlds` <- DomainSearchResponseObject$`tlds`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "success": %s,
           "response_text": %s,
           "response_time": %s,
           "lookup": [%s],
           "suggest": [%s],
           "tlds": [%s]
        }',
        self$`success`,
        self$`response_text`,
        self$`response_time`,
        lapply(self$`lookup`, function(x) paste(x$toJSON(), sep=",")),
        lapply(self$`suggest`, function(x) paste(x$toJSON(), sep=",")),
        lapply(self$`tlds`, function(x) paste(paste0('"', x, '"'), sep=","))
      )
    },
    fromJSONString = function(DomainSearchResponseJson) {
      DomainSearchResponseObject <- jsonlite::fromJSON(DomainSearchResponseJson)
      self$`success` <- DomainSearchResponseObject$`success`
      self$`response_text` <- DomainSearchResponseObject$`response_text`
      self$`response_time` <- DomainSearchResponseObject$`response_time`
      self$`lookup` <- lapply(DomainSearchResponseObject$`lookup`, function(x) TODO_OBJECT_MAPPING$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
      self$`suggest` <- lapply(DomainSearchResponseObject$`suggest`, function(x) TODO_OBJECT_MAPPING$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
      self$`tlds` <- DomainSearchResponseObject$`tlds`
    }
  )
)
