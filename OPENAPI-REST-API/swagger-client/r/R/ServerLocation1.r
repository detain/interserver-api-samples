# InterServer Management API
#
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
#
# OpenAPI spec version: 0.9.0
# Contact: support@interserver.net
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' ServerLocation1 Class
#'
#' @field location_id 
#' @field location_name 
#' @field location_description 
#' @field location_lat 
#' @field location_long 
#' @field location_ipmi_group 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ServerLocation1 <- R6::R6Class(
  'ServerLocation1',
  public = list(
    `location_id` = NULL,
    `location_name` = NULL,
    `location_description` = NULL,
    `location_lat` = NULL,
    `location_long` = NULL,
    `location_ipmi_group` = NULL,
    initialize = function(`location_id`, `location_name`, `location_description`, `location_lat`, `location_long`, `location_ipmi_group`){
      if (!missing(`location_id`)) {
        stopifnot(is.numeric(`location_id`), length(`location_id`) == 1)
        self$`location_id` <- `location_id`
      }
      if (!missing(`location_name`)) {
        stopifnot(is.character(`location_name`), length(`location_name`) == 1)
        self$`location_name` <- `location_name`
      }
      if (!missing(`location_description`)) {
        stopifnot(is.character(`location_description`), length(`location_description`) == 1)
        self$`location_description` <- `location_description`
      }
      if (!missing(`location_lat`)) {
        stopifnot(is.character(`location_lat`), length(`location_lat`) == 1)
        self$`location_lat` <- `location_lat`
      }
      if (!missing(`location_long`)) {
        stopifnot(is.character(`location_long`), length(`location_long`) == 1)
        self$`location_long` <- `location_long`
      }
      if (!missing(`location_ipmi_group`)) {
        stopifnot(is.numeric(`location_ipmi_group`), length(`location_ipmi_group`) == 1)
        self$`location_ipmi_group` <- `location_ipmi_group`
      }
    },
    toJSON = function() {
      ServerLocation1Object <- list()
      if (!is.null(self$`location_id`)) {
        ServerLocation1Object[['location_id']] <- self$`location_id`
      }
      if (!is.null(self$`location_name`)) {
        ServerLocation1Object[['location_name']] <- self$`location_name`
      }
      if (!is.null(self$`location_description`)) {
        ServerLocation1Object[['location_description']] <- self$`location_description`
      }
      if (!is.null(self$`location_lat`)) {
        ServerLocation1Object[['location_lat']] <- self$`location_lat`
      }
      if (!is.null(self$`location_long`)) {
        ServerLocation1Object[['location_long']] <- self$`location_long`
      }
      if (!is.null(self$`location_ipmi_group`)) {
        ServerLocation1Object[['location_ipmi_group']] <- self$`location_ipmi_group`
      }

      ServerLocation1Object
    },
    fromJSON = function(ServerLocation1Json) {
      ServerLocation1Object <- jsonlite::fromJSON(ServerLocation1Json)
      if (!is.null(ServerLocation1Object$`location_id`)) {
        self$`location_id` <- ServerLocation1Object$`location_id`
      }
      if (!is.null(ServerLocation1Object$`location_name`)) {
        self$`location_name` <- ServerLocation1Object$`location_name`
      }
      if (!is.null(ServerLocation1Object$`location_description`)) {
        self$`location_description` <- ServerLocation1Object$`location_description`
      }
      if (!is.null(ServerLocation1Object$`location_lat`)) {
        self$`location_lat` <- ServerLocation1Object$`location_lat`
      }
      if (!is.null(ServerLocation1Object$`location_long`)) {
        self$`location_long` <- ServerLocation1Object$`location_long`
      }
      if (!is.null(ServerLocation1Object$`location_ipmi_group`)) {
        self$`location_ipmi_group` <- ServerLocation1Object$`location_ipmi_group`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "location_id": %d,
           "location_name": %s,
           "location_description": %s,
           "location_lat": %s,
           "location_long": %s,
           "location_ipmi_group": %d
        }',
        self$`location_id`,
        self$`location_name`,
        self$`location_description`,
        self$`location_lat`,
        self$`location_long`,
        self$`location_ipmi_group`
      )
    },
    fromJSONString = function(ServerLocation1Json) {
      ServerLocation1Object <- jsonlite::fromJSON(ServerLocation1Json)
      self$`location_id` <- ServerLocation1Object$`location_id`
      self$`location_name` <- ServerLocation1Object$`location_name`
      self$`location_description` <- ServerLocation1Object$`location_description`
      self$`location_lat` <- ServerLocation1Object$`location_lat`
      self$`location_long` <- ServerLocation1Object$`location_long`
      self$`location_ipmi_group` <- ServerLocation1Object$`location_ipmi_group`
    }
  )
)
