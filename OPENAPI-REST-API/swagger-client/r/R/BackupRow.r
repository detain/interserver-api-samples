# InterServer Management API
#
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
#
# OpenAPI spec version: 0.9.0
# Contact: support@interserver.net
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' BackupRow Class
#'
#' @field backup_id 
#' @field backup_name 
#' @field backup_cost 
#' @field backup_username 
#' @field backup_status 
#' @field services_name 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BackupRow <- R6::R6Class(
  'BackupRow',
  public = list(
    `backup_id` = NULL,
    `backup_name` = NULL,
    `backup_cost` = NULL,
    `backup_username` = NULL,
    `backup_status` = NULL,
    `services_name` = NULL,
    initialize = function(`backup_id`, `backup_name`, `backup_cost`, `backup_username`, `backup_status`, `services_name`){
      if (!missing(`backup_id`)) {
        stopifnot(is.character(`backup_id`), length(`backup_id`) == 1)
        self$`backup_id` <- `backup_id`
      }
      if (!missing(`backup_name`)) {
        stopifnot(is.character(`backup_name`), length(`backup_name`) == 1)
        self$`backup_name` <- `backup_name`
      }
      if (!missing(`backup_cost`)) {
        stopifnot(is.character(`backup_cost`), length(`backup_cost`) == 1)
        self$`backup_cost` <- `backup_cost`
      }
      if (!missing(`backup_username`)) {
        stopifnot(is.character(`backup_username`), length(`backup_username`) == 1)
        self$`backup_username` <- `backup_username`
      }
      if (!missing(`backup_status`)) {
        stopifnot(is.character(`backup_status`), length(`backup_status`) == 1)
        self$`backup_status` <- `backup_status`
      }
      if (!missing(`services_name`)) {
        stopifnot(is.character(`services_name`), length(`services_name`) == 1)
        self$`services_name` <- `services_name`
      }
    },
    toJSON = function() {
      BackupRowObject <- list()
      if (!is.null(self$`backup_id`)) {
        BackupRowObject[['backup_id']] <- self$`backup_id`
      }
      if (!is.null(self$`backup_name`)) {
        BackupRowObject[['backup_name']] <- self$`backup_name`
      }
      if (!is.null(self$`backup_cost`)) {
        BackupRowObject[['backup_cost']] <- self$`backup_cost`
      }
      if (!is.null(self$`backup_username`)) {
        BackupRowObject[['backup_username']] <- self$`backup_username`
      }
      if (!is.null(self$`backup_status`)) {
        BackupRowObject[['backup_status']] <- self$`backup_status`
      }
      if (!is.null(self$`services_name`)) {
        BackupRowObject[['services_name']] <- self$`services_name`
      }

      BackupRowObject
    },
    fromJSON = function(BackupRowJson) {
      BackupRowObject <- jsonlite::fromJSON(BackupRowJson)
      if (!is.null(BackupRowObject$`backup_id`)) {
        self$`backup_id` <- BackupRowObject$`backup_id`
      }
      if (!is.null(BackupRowObject$`backup_name`)) {
        self$`backup_name` <- BackupRowObject$`backup_name`
      }
      if (!is.null(BackupRowObject$`backup_cost`)) {
        self$`backup_cost` <- BackupRowObject$`backup_cost`
      }
      if (!is.null(BackupRowObject$`backup_username`)) {
        self$`backup_username` <- BackupRowObject$`backup_username`
      }
      if (!is.null(BackupRowObject$`backup_status`)) {
        self$`backup_status` <- BackupRowObject$`backup_status`
      }
      if (!is.null(BackupRowObject$`services_name`)) {
        self$`services_name` <- BackupRowObject$`services_name`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "backup_id": %s,
           "backup_name": %s,
           "backup_cost": %s,
           "backup_username": %s,
           "backup_status": %s,
           "services_name": %s
        }',
        self$`backup_id`,
        self$`backup_name`,
        self$`backup_cost`,
        self$`backup_username`,
        self$`backup_status`,
        self$`services_name`
      )
    },
    fromJSONString = function(BackupRowJson) {
      BackupRowObject <- jsonlite::fromJSON(BackupRowJson)
      self$`backup_id` <- BackupRowObject$`backup_id`
      self$`backup_name` <- BackupRowObject$`backup_name`
      self$`backup_cost` <- BackupRowObject$`backup_cost`
      self$`backup_username` <- BackupRowObject$`backup_username`
      self$`backup_status` <- BackupRowObject$`backup_status`
      self$`services_name` <- BackupRowObject$`services_name`
    }
  )
)
