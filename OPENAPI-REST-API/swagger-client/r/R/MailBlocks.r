# InterServer Management API
#
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
#
# OpenAPI spec version: 0.9.0
# Contact: support@interserver.net
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' MailBlocks Class
#'
#' @field local 
#' @field mbtrap 
#' @field subject 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
MailBlocks <- R6::R6Class(
  'MailBlocks',
  public = list(
    `local` = NULL,
    `mbtrap` = NULL,
    `subject` = NULL,
    initialize = function(`local`, `mbtrap`, `subject`){
      if (!missing(`local`)) {
        stopifnot(is.list(`local`), length(`local`) != 0)
        lapply(`local`, function(x) stopifnot(R6::is.R6(x)))
        self$`local` <- `local`
      }
      if (!missing(`mbtrap`)) {
        stopifnot(is.list(`mbtrap`), length(`mbtrap`) != 0)
        lapply(`mbtrap`, function(x) stopifnot(R6::is.R6(x)))
        self$`mbtrap` <- `mbtrap`
      }
      if (!missing(`subject`)) {
        stopifnot(is.list(`subject`), length(`subject`) != 0)
        lapply(`subject`, function(x) stopifnot(R6::is.R6(x)))
        self$`subject` <- `subject`
      }
    },
    toJSON = function() {
      MailBlocksObject <- list()
      if (!is.null(self$`local`)) {
        MailBlocksObject[['local']] <- lapply(self$`local`, function(x) x$toJSON())
      }
      if (!is.null(self$`mbtrap`)) {
        MailBlocksObject[['mbtrap']] <- lapply(self$`mbtrap`, function(x) x$toJSON())
      }
      if (!is.null(self$`subject`)) {
        MailBlocksObject[['subject']] <- lapply(self$`subject`, function(x) x$toJSON())
      }

      MailBlocksObject
    },
    fromJSON = function(MailBlocksJson) {
      MailBlocksObject <- jsonlite::fromJSON(MailBlocksJson)
      if (!is.null(MailBlocksObject$`local`)) {
        self$`local` <- lapply(MailBlocksObject$`local`, function(x) {
          localObject <- MailBlockClickHouse$new()
          localObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          localObject
        })
      }
      if (!is.null(MailBlocksObject$`mbtrap`)) {
        self$`mbtrap` <- lapply(MailBlocksObject$`mbtrap`, function(x) {
          mbtrapObject <- MailBlockClickHouse$new()
          mbtrapObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          mbtrapObject
        })
      }
      if (!is.null(MailBlocksObject$`subject`)) {
        self$`subject` <- lapply(MailBlocksObject$`subject`, function(x) {
          subjectObject <- MailBlockRspamd$new()
          subjectObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          subjectObject
        })
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "local": [%s],
           "mbtrap": [%s],
           "subject": [%s]
        }',
        lapply(self$`local`, function(x) paste(x$toJSON(), sep=",")),
        lapply(self$`mbtrap`, function(x) paste(x$toJSON(), sep=",")),
        lapply(self$`subject`, function(x) paste(x$toJSON(), sep=","))
      )
    },
    fromJSONString = function(MailBlocksJson) {
      MailBlocksObject <- jsonlite::fromJSON(MailBlocksJson)
      self$`local` <- lapply(MailBlocksObject$`local`, function(x) MailBlockClickHouse$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
      self$`mbtrap` <- lapply(MailBlocksObject$`mbtrap`, function(x) MailBlockClickHouse$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
      self$`subject` <- lapply(MailBlocksObject$`subject`, function(x) MailBlockRspamd$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
    }
  )
)
