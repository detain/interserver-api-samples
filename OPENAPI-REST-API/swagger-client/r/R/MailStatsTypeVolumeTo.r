# InterServer Management API
#
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
#
# OpenAPI spec version: 0.9.0
# Contact: support@interserver.net
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' MailStatsTypeVolumeTo Class
#'
#' @field client@domain.com 
#' @field user@site.net 
#' @field sales@company.com 
#' @field client@anothersite.com 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
MailStatsTypeVolumeTo <- R6::R6Class(
  'MailStatsTypeVolumeTo',
  public = list(
    `client@domain.com` = NULL,
    `user@site.net` = NULL,
    `sales@company.com` = NULL,
    `client@anothersite.com` = NULL,
    initialize = function(`client@domain.com`, `user@site.net`, `sales@company.com`, `client@anothersite.com`){
      if (!missing(`client@domain.com`)) {
        stopifnot(is.numeric(`client@domain.com`), length(`client@domain.com`) == 1)
        self$`client@domain.com` <- `client@domain.com`
      }
      if (!missing(`user@site.net`)) {
        stopifnot(is.numeric(`user@site.net`), length(`user@site.net`) == 1)
        self$`user@site.net` <- `user@site.net`
      }
      if (!missing(`sales@company.com`)) {
        stopifnot(is.numeric(`sales@company.com`), length(`sales@company.com`) == 1)
        self$`sales@company.com` <- `sales@company.com`
      }
      if (!missing(`client@anothersite.com`)) {
        stopifnot(is.numeric(`client@anothersite.com`), length(`client@anothersite.com`) == 1)
        self$`client@anothersite.com` <- `client@anothersite.com`
      }
    },
    toJSON = function() {
      MailStatsTypeVolumeToObject <- list()
      if (!is.null(self$`client@domain.com`)) {
        MailStatsTypeVolumeToObject[['client@domain.com']] <- self$`client@domain.com`
      }
      if (!is.null(self$`user@site.net`)) {
        MailStatsTypeVolumeToObject[['user@site.net']] <- self$`user@site.net`
      }
      if (!is.null(self$`sales@company.com`)) {
        MailStatsTypeVolumeToObject[['sales@company.com']] <- self$`sales@company.com`
      }
      if (!is.null(self$`client@anothersite.com`)) {
        MailStatsTypeVolumeToObject[['client@anothersite.com']] <- self$`client@anothersite.com`
      }

      MailStatsTypeVolumeToObject
    },
    fromJSON = function(MailStatsTypeVolumeToJson) {
      MailStatsTypeVolumeToObject <- jsonlite::fromJSON(MailStatsTypeVolumeToJson)
      if (!is.null(MailStatsTypeVolumeToObject$`client@domain.com`)) {
        self$`client@domain.com` <- MailStatsTypeVolumeToObject$`client@domain.com`
      }
      if (!is.null(MailStatsTypeVolumeToObject$`user@site.net`)) {
        self$`user@site.net` <- MailStatsTypeVolumeToObject$`user@site.net`
      }
      if (!is.null(MailStatsTypeVolumeToObject$`sales@company.com`)) {
        self$`sales@company.com` <- MailStatsTypeVolumeToObject$`sales@company.com`
      }
      if (!is.null(MailStatsTypeVolumeToObject$`client@anothersite.com`)) {
        self$`client@anothersite.com` <- MailStatsTypeVolumeToObject$`client@anothersite.com`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "client@domain.com": %d,
           "user@site.net": %d,
           "sales@company.com": %d,
           "client@anothersite.com": %d
        }',
        self$`client@domain.com`,
        self$`user@site.net`,
        self$`sales@company.com`,
        self$`client@anothersite.com`
      )
    },
    fromJSONString = function(MailStatsTypeVolumeToJson) {
      MailStatsTypeVolumeToObject <- jsonlite::fromJSON(MailStatsTypeVolumeToJson)
      self$`client@domain.com` <- MailStatsTypeVolumeToObject$`client@domain.com`
      self$`user@site.net` <- MailStatsTypeVolumeToObject$`user@site.net`
      self$`sales@company.com` <- MailStatsTypeVolumeToObject$`sales@company.com`
      self$`client@anothersite.com` <- MailStatsTypeVolumeToObject$`client@anothersite.com`
    }
  )
)
