# InterServer Management API
#
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
#
# OpenAPI spec version: 0.9.0
# Contact: support@interserver.net
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' StatusMonthlyBreakdown Class
#'
#' @field default 
#' @field failed 
#' @field rejected 
#' @field pending 
#' @field locked 
#' @field paid 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
StatusMonthlyBreakdown <- R6::R6Class(
  'StatusMonthlyBreakdown',
  public = list(
    `default` = NULL,
    `failed` = NULL,
    `rejected` = NULL,
    `pending` = NULL,
    `locked` = NULL,
    `paid` = NULL,
    initialize = function(`default`, `failed`, `rejected`, `pending`, `locked`, `paid`){
      if (!missing(`default`)) {
        stopifnot(R6::is.R6(`default`))
        self$`default` <- `default`
      }
      if (!missing(`failed`)) {
        stopifnot(R6::is.R6(`failed`))
        self$`failed` <- `failed`
      }
      if (!missing(`rejected`)) {
        stopifnot(R6::is.R6(`rejected`))
        self$`rejected` <- `rejected`
      }
      if (!missing(`pending`)) {
        stopifnot(R6::is.R6(`pending`))
        self$`pending` <- `pending`
      }
      if (!missing(`locked`)) {
        stopifnot(R6::is.R6(`locked`))
        self$`locked` <- `locked`
      }
      if (!missing(`paid`)) {
        stopifnot(R6::is.R6(`paid`))
        self$`paid` <- `paid`
      }
    },
    toJSON = function() {
      StatusMonthlyBreakdownObject <- list()
      if (!is.null(self$`default`)) {
        StatusMonthlyBreakdownObject[['default']] <- self$`default`$toJSON()
      }
      if (!is.null(self$`failed`)) {
        StatusMonthlyBreakdownObject[['failed']] <- self$`failed`$toJSON()
      }
      if (!is.null(self$`rejected`)) {
        StatusMonthlyBreakdownObject[['rejected']] <- self$`rejected`$toJSON()
      }
      if (!is.null(self$`pending`)) {
        StatusMonthlyBreakdownObject[['pending']] <- self$`pending`$toJSON()
      }
      if (!is.null(self$`locked`)) {
        StatusMonthlyBreakdownObject[['locked']] <- self$`locked`$toJSON()
      }
      if (!is.null(self$`paid`)) {
        StatusMonthlyBreakdownObject[['paid']] <- self$`paid`$toJSON()
      }

      StatusMonthlyBreakdownObject
    },
    fromJSON = function(StatusMonthlyBreakdownJson) {
      StatusMonthlyBreakdownObject <- jsonlite::fromJSON(StatusMonthlyBreakdownJson)
      if (!is.null(StatusMonthlyBreakdownObject$`default`)) {
        defaultObject <- MonthlyCounts$new()
        defaultObject$fromJSON(jsonlite::toJSON(StatusMonthlyBreakdownObject$default, auto_unbox = TRUE))
        self$`default` <- defaultObject
      }
      if (!is.null(StatusMonthlyBreakdownObject$`failed`)) {
        failedObject <- MonthlyCounts$new()
        failedObject$fromJSON(jsonlite::toJSON(StatusMonthlyBreakdownObject$failed, auto_unbox = TRUE))
        self$`failed` <- failedObject
      }
      if (!is.null(StatusMonthlyBreakdownObject$`rejected`)) {
        rejectedObject <- MonthlyCounts$new()
        rejectedObject$fromJSON(jsonlite::toJSON(StatusMonthlyBreakdownObject$rejected, auto_unbox = TRUE))
        self$`rejected` <- rejectedObject
      }
      if (!is.null(StatusMonthlyBreakdownObject$`pending`)) {
        pendingObject <- MonthlyCounts$new()
        pendingObject$fromJSON(jsonlite::toJSON(StatusMonthlyBreakdownObject$pending, auto_unbox = TRUE))
        self$`pending` <- pendingObject
      }
      if (!is.null(StatusMonthlyBreakdownObject$`locked`)) {
        lockedObject <- MonthlyCounts$new()
        lockedObject$fromJSON(jsonlite::toJSON(StatusMonthlyBreakdownObject$locked, auto_unbox = TRUE))
        self$`locked` <- lockedObject
      }
      if (!is.null(StatusMonthlyBreakdownObject$`paid`)) {
        paidObject <- MonthlyCounts$new()
        paidObject$fromJSON(jsonlite::toJSON(StatusMonthlyBreakdownObject$paid, auto_unbox = TRUE))
        self$`paid` <- paidObject
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "default": %s,
           "failed": %s,
           "rejected": %s,
           "pending": %s,
           "locked": %s,
           "paid": %s
        }',
        self$`default`$toJSON(),
        self$`failed`$toJSON(),
        self$`rejected`$toJSON(),
        self$`pending`$toJSON(),
        self$`locked`$toJSON(),
        self$`paid`$toJSON()
      )
    },
    fromJSONString = function(StatusMonthlyBreakdownJson) {
      StatusMonthlyBreakdownObject <- jsonlite::fromJSON(StatusMonthlyBreakdownJson)
      MonthlyCountsObject <- MonthlyCounts$new()
      self$`default` <- MonthlyCountsObject$fromJSON(jsonlite::toJSON(StatusMonthlyBreakdownObject$default, auto_unbox = TRUE))
      MonthlyCountsObject <- MonthlyCounts$new()
      self$`failed` <- MonthlyCountsObject$fromJSON(jsonlite::toJSON(StatusMonthlyBreakdownObject$failed, auto_unbox = TRUE))
      MonthlyCountsObject <- MonthlyCounts$new()
      self$`rejected` <- MonthlyCountsObject$fromJSON(jsonlite::toJSON(StatusMonthlyBreakdownObject$rejected, auto_unbox = TRUE))
      MonthlyCountsObject <- MonthlyCounts$new()
      self$`pending` <- MonthlyCountsObject$fromJSON(jsonlite::toJSON(StatusMonthlyBreakdownObject$pending, auto_unbox = TRUE))
      MonthlyCountsObject <- MonthlyCounts$new()
      self$`locked` <- MonthlyCountsObject$fromJSON(jsonlite::toJSON(StatusMonthlyBreakdownObject$locked, auto_unbox = TRUE))
      MonthlyCountsObject <- MonthlyCounts$new()
      self$`paid` <- MonthlyCountsObject$fromJSON(jsonlite::toJSON(StatusMonthlyBreakdownObject$paid, auto_unbox = TRUE))
    }
  )
)
