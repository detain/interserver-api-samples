# InterServer Management API
#
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
#
# OpenAPI spec version: 0.9.0
# Contact: support@interserver.net
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' ScrubIpsRowSchema Class
#'
#' @field scrub_ip_id 
#' @field repeat_invoices_cost 
#' @field scrub_ip_ip 
#' @field scrub_ip_status 
#' @field services_name 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ScrubIpsRowSchema <- R6::R6Class(
  'ScrubIpsRowSchema',
  public = list(
    `scrub_ip_id` = NULL,
    `repeat_invoices_cost` = NULL,
    `scrub_ip_ip` = NULL,
    `scrub_ip_status` = NULL,
    `services_name` = NULL,
    initialize = function(`scrub_ip_id`, `repeat_invoices_cost`, `scrub_ip_ip`, `scrub_ip_status`, `services_name`){
      if (!missing(`scrub_ip_id`)) {
        stopifnot(is.numeric(`scrub_ip_id`), length(`scrub_ip_id`) == 1)
        self$`scrub_ip_id` <- `scrub_ip_id`
      }
      if (!missing(`repeat_invoices_cost`)) {
        stopifnot(is.numeric(`repeat_invoices_cost`), length(`repeat_invoices_cost`) == 1)
        stopifnot(R6::is.R6(`repeat_invoices_cost`))
        self$`repeat_invoices_cost` <- `repeat_invoices_cost`
      }
      if (!missing(`scrub_ip_ip`)) {
        stopifnot(is.character(`scrub_ip_ip`), length(`scrub_ip_ip`) == 1)
        self$`scrub_ip_ip` <- `scrub_ip_ip`
      }
      if (!missing(`scrub_ip_status`)) {
        stopifnot(is.character(`scrub_ip_status`), length(`scrub_ip_status`) == 1)
        self$`scrub_ip_status` <- `scrub_ip_status`
      }
      if (!missing(`services_name`)) {
        stopifnot(is.character(`services_name`), length(`services_name`) == 1)
        self$`services_name` <- `services_name`
      }
    },
    toJSON = function() {
      ScrubIpsRowSchemaObject <- list()
      if (!is.null(self$`scrub_ip_id`)) {
        ScrubIpsRowSchemaObject[['scrub_ip_id']] <- self$`scrub_ip_id`
      }
      if (!is.null(self$`repeat_invoices_cost`)) {
        ScrubIpsRowSchemaObject[['repeat_invoices_cost']] <- self$`repeat_invoices_cost`$toJSON()
      }
      if (!is.null(self$`scrub_ip_ip`)) {
        ScrubIpsRowSchemaObject[['scrub_ip_ip']] <- self$`scrub_ip_ip`
      }
      if (!is.null(self$`scrub_ip_status`)) {
        ScrubIpsRowSchemaObject[['scrub_ip_status']] <- self$`scrub_ip_status`
      }
      if (!is.null(self$`services_name`)) {
        ScrubIpsRowSchemaObject[['services_name']] <- self$`services_name`
      }

      ScrubIpsRowSchemaObject
    },
    fromJSON = function(ScrubIpsRowSchemaJson) {
      ScrubIpsRowSchemaObject <- jsonlite::fromJSON(ScrubIpsRowSchemaJson)
      if (!is.null(ScrubIpsRowSchemaObject$`scrub_ip_id`)) {
        self$`scrub_ip_id` <- ScrubIpsRowSchemaObject$`scrub_ip_id`
      }
      if (!is.null(ScrubIpsRowSchemaObject$`repeat_invoices_cost`)) {
        repeat_invoices_costObject <- BigDecimal$new()
        repeat_invoices_costObject$fromJSON(jsonlite::toJSON(ScrubIpsRowSchemaObject$repeat_invoices_cost, auto_unbox = TRUE))
        self$`repeat_invoices_cost` <- repeat_invoices_costObject
      }
      if (!is.null(ScrubIpsRowSchemaObject$`scrub_ip_ip`)) {
        self$`scrub_ip_ip` <- ScrubIpsRowSchemaObject$`scrub_ip_ip`
      }
      if (!is.null(ScrubIpsRowSchemaObject$`scrub_ip_status`)) {
        self$`scrub_ip_status` <- ScrubIpsRowSchemaObject$`scrub_ip_status`
      }
      if (!is.null(ScrubIpsRowSchemaObject$`services_name`)) {
        self$`services_name` <- ScrubIpsRowSchemaObject$`services_name`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "scrub_ip_id": %d,
           "repeat_invoices_cost": %s,
           "scrub_ip_ip": %s,
           "scrub_ip_status": %s,
           "services_name": %s
        }',
        self$`scrub_ip_id`,
        self$`repeat_invoices_cost`$toJSON(),
        self$`scrub_ip_ip`,
        self$`scrub_ip_status`,
        self$`services_name`
      )
    },
    fromJSONString = function(ScrubIpsRowSchemaJson) {
      ScrubIpsRowSchemaObject <- jsonlite::fromJSON(ScrubIpsRowSchemaJson)
      self$`scrub_ip_id` <- ScrubIpsRowSchemaObject$`scrub_ip_id`
      BigDecimalObject <- BigDecimal$new()
      self$`repeat_invoices_cost` <- BigDecimalObject$fromJSON(jsonlite::toJSON(ScrubIpsRowSchemaObject$repeat_invoices_cost, auto_unbox = TRUE))
      self$`scrub_ip_ip` <- ScrubIpsRowSchemaObject$`scrub_ip_ip`
      self$`scrub_ip_status` <- ScrubIpsRowSchemaObject$`scrub_ip_status`
      self$`services_name` <- ScrubIpsRowSchemaObject$`services_name`
    }
  )
)
