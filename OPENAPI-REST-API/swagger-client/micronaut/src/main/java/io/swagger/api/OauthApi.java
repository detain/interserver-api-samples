/**
 * NOTE: This class is auto generated by the swagger code generator program (3.0.78).
 * https://github.com/swagger-api/swagger-codegen
 * Do not edit the class manually.
 */
package io.swagger.api;

import io.swagger.model.InlineResponse2005;
import io.swagger.model.InlineResponse2006;
import io.swagger.model.InlineResponse2007;
import io.swagger.model.InlineResponse401;
import io.swagger.model.OauthBody;
import io.swagger.model.OauthBody2;
import com.fasterxml.jackson.databind.ObjectMapper;
import io.micronaut.http.*;
import io.micronaut.http.annotation.*;
import io.reactivex.rxjava3.core.Single;
import io.swagger.v3.oas.annotations.*;
import io.swagger.v3.oas.annotations.responses.*;

import javax.annotation.Nullable;
import javax.validation.Valid;
import javax.validation.constraints.*;
import java.io.IOException;
import java.util.List;
import java.util.Map;


public interface OauthApi {


    @Operation(summary = "Get OAuth Redirect URL", operationId = "getOauthRedirect", description = "Returns the OAuth redirect URL for the specified provider. Redirect the user to this URL to begin the OAuth authentication flow. After the user authorizes, the provider will redirect back to your callback endpoint where you should call `POST /oauth`." , tags = {"Public"})
    @ApiResponse(responseCode = "200", description = "OAuth redirect URL for the requested provider.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/oauth", produces = { "application/json" })
    default Single<HttpResponse<InlineResponse2005>> getOauthRedirect(@NotNull @Parameter(description = "The OAuth provider name (e.g. `Google`).") @QueryValue(value = "provider") String provider) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Complete OAuth Two-Factor Verification", operationId = "patchOauthTwoFactor", description = "Completes the OAuth login by verifying a two-factor authentication code. Use the `account_id` returned by `POST /oauth` when the response contained `error_code: \"2fa_required\"`." , tags = {"Public"})
    @ApiResponse(responseCode = "200", description = "OAuth 2FA verification result.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Patch(value = "/oauth", produces = { "application/json" }, consumes = {"application/json", "multipart/form-data"})
    default Single<HttpResponse<InlineResponse2007>> patchOauthTwoFactor(@NotNull @Valid @Parameter(description = "") @Body OauthBody2 body) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Complete OAuth Two-Factor Verification", operationId = "patchOauthTwoFactor", description = "Completes the OAuth login by verifying a two-factor authentication code. Use the `account_id` returned by `POST /oauth` when the response contained `error_code: \"2fa_required\"`." , tags = {"Public"})
    @ApiResponse(responseCode = "200", description = "OAuth 2FA verification result.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Patch(value = "/oauth", produces = { "application/json" }, consumes = {"application/json", "multipart/form-data"})
    default Single<HttpResponse<InlineResponse2007>> patchOauthTwoFactor(@NotNull @Parameter(description = "") @Body(value = "account_id")  Integer accountId,@NotNull @Parameter(description = "") @Body(value = "code")  String code) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "OAuth Callback", operationId = "postOauthCallback", description = "Handles the OAuth callback after the user authorizes with the provider. Returns the login status. If the account has two-factor authentication enabled, the response will include `error_code: \"2fa_required\"` and an `account_id`; use `PATCH /oauth` with the 2FA code and account ID to complete login." , tags = {"Public"})
    @ApiResponse(responseCode = "200", description = "OAuth callback result.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/oauth", produces = { "application/json" }, consumes = {"application/json", "multipart/form-data"})
    default Single<HttpResponse<InlineResponse2006>> postOauthCallback(@NotNull @Parameter(description = "The OAuth provider name (e.g. `Google`).") @QueryValue(value = "provider") String provider,@Nullable @Valid @Parameter(description = "") @Body OauthBody body) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "OAuth Callback", operationId = "postOauthCallback", description = "Handles the OAuth callback after the user authorizes with the provider. Returns the login status. If the account has two-factor authentication enabled, the response will include `error_code: \"2fa_required\"` and an `account_id`; use `PATCH /oauth` with the 2FA code and account ID to complete login." , tags = {"Public"})
    @ApiResponse(responseCode = "200", description = "OAuth callback result.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/oauth", produces = { "application/json" }, consumes = {"application/json", "multipart/form-data"})
    default Single<HttpResponse<InlineResponse2006>> postOauthCallback(@NotNull @Parameter(description = "The OAuth provider name (e.g. `Google`).") @QueryValue(value = "provider") String provider,@Nullable @Parameter(description = "") @Body(value = "provider")  String provider2) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }

}
