/**
 * NOTE: This class is auto generated by the swagger code generator program (3.0.78).
 * https://github.com/swagger-api/swagger-codegen
 * Do not edit the class manually.
 */
package io.swagger.api;

import io.swagger.model.Backup;
import io.swagger.model.BackupLoginResponse;
import io.swagger.model.BackupOrderPostResponse;
import io.swagger.model.BackupOrderPutRequest;
import io.swagger.model.BackupOrderPutResponse;
import io.swagger.model.BackupRow;
import io.swagger.model.BackupsOrder;
import io.swagger.model.ChargeInvoiceRows;
import io.swagger.model.InlineResponse2001;
import io.swagger.model.InlineResponse401;
import io.swagger.model.SuccessTextResponse;
import com.fasterxml.jackson.databind.ObjectMapper;
import io.micronaut.http.*;
import io.micronaut.http.annotation.*;
import io.reactivex.rxjava3.core.Single;
import io.swagger.v3.oas.annotations.*;
import io.swagger.v3.oas.annotations.responses.*;

import javax.annotation.Nullable;
import javax.validation.Valid;
import javax.validation.constraints.*;
import java.io.IOException;
import java.util.List;
import java.util.Map;


public interface BackupsApi {


    @Operation(summary = "Place Backup Order", operationId = "addBackup", description = "Places a new backup storage order and generates an invoice. On success, the response includes invoice IDs that can be used with `/billing/invoices/{id}` to view the invoice or `/pay/{method}/{invoices}` to complete payment. The service is provisioned after payment is confirmed." , tags = {"Backups"})
    @ApiResponse(responseCode = "200", description = "Response from the backup order call including invoice IDs for payment.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @ApiResponse(responseCode = "200", description = "Default response")
    @Post(value = "/backups/order", produces = { "application/json" }, consumes = {"multipart/form-data", "application/json"})
    default Single<HttpResponse<BackupOrderPostResponse>> addBackup(@NotNull @Parameter(description = "") @Body(value = "validateOnly")  Boolean validateOnly,@NotNull @Parameter(description = "") @Body(value = "serviceType")  Integer serviceType,@NotNull @Parameter(description = "") @Body(value = "coupon")  String coupon) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Place Backup Order", operationId = "addBackup", description = "Places a new backup storage order and generates an invoice. On success, the response includes invoice IDs that can be used with `/billing/invoices/{id}` to view the invoice or `/pay/{method}/{invoices}` to complete payment. The service is provisioned after payment is confirmed." , tags = {"Backups"})
    @ApiResponse(responseCode = "200", description = "Response from the backup order call including invoice IDs for payment.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @ApiResponse(responseCode = "200", description = "Default response")
    @Post(value = "/backups/order", produces = { "application/json" }, consumes = {"multipart/form-data", "application/json"})
    default Single<HttpResponse<BackupOrderPostResponse>> addBackup(@NotNull @Valid @Parameter(description = "") @Body BackupOrderPutRequest body) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Cancel Backup Service", operationId = "cancelBackup", description = "Cancels the specified backup storage service. The service remains accessible until the end of the current billing period. This action cannot be undone; a new order must be placed to restore service." , tags = {"Backups"})
    @ApiResponse(responseCode = "200", description = "Backups Cancel")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Delete(value = "/backups/{id}", produces = { "application/json" })
    default Single<HttpResponse<InlineResponse2001>> cancelBackup(@Parameter(description = "The backup service ID. Use the `backup_id` from `GET /backups` to identify the service.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Get Backup Service Details", operationId = "getBackupInfo", description = "Returns detailed service information for the specified backup storage order, including `backup_username`, `backup_ip`, `backup_status`, and `backup_quota` in `serviceInfo`. Also returns `client_links`, `billingDetails`, `extraInfoTables`, `package`, and `custCurrency`." , tags = {"Backups"})
    @ApiResponse(responseCode = "200", description = "Full backup service details including serviceInfo, billingDetails, and client_links.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/backups/{id}", produces = { "application/json" })
    default Single<HttpResponse<Backup>> getBackupInfo(@Parameter(description = "The backup service ID. Use the `backup_id` from `GET /backups` to identify the service.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Get Backup Order Invoices", operationId = "getBackupInvoices", description = "Retrieves invoices associated with the backup storage order. Use this to confirm billing status or locate invoices for payment." , tags = {"Backups"})
    @ApiResponse(responseCode = "200", description = "Get Invoices response")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/backups/{id}/invoices", produces = { "application/json" })
    default Single<HttpResponse<ChargeInvoiceRows>> getBackupInvoices(@Parameter(description = "The backup service ID. Use the `backup_id` from `GET /backups` to identify the service.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Get Backup Storage Panel Login", operationId = "getBackupLogin", description = "Creates and returns a login session URL for the backup storage panel. The returned session URL can be used to redirect the user directly into the storage management interface without requiring separate credentials." , tags = {"Backups"})
    @ApiResponse(responseCode = "200", description = "Login session details for the backup storage service.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/backups/{id}/login", produces = { "application/json" })
    default Single<HttpResponse<BackupLoginResponse>> getBackupLogin(@Parameter(description = "The backup service ID. Use the `backup_id` from `GET /backups` to identify the service.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "List Backup Services", operationId = "getBackupsList", description = "Returns all backup storage services on your account. Each entry includes the `backup_id`, `backup_username`, `backup_ip`, `backup_status`, and `backup_quota`. Use the `backup_id` with `/backups/{id}` to retrieve full service details or `/backups/{id}/login` to obtain a storage panel session." , tags = {"Backups"})
    @ApiResponse(responseCode = "200", description = "The listing of backup storage services on your account.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/backups", produces = { "application/json" })
    default Single<HttpResponse<List<BackupRow>>> getBackupsList() {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Resend Backup Welcome Email", operationId = "getBackupsWelcomeEmail", description = "Resends the welcome email for the specified backup service. The email contains connection credentials and setup instructions. Use this when the original welcome email was lost or never received." , tags = {"Backups"})
    @ApiResponse(responseCode = "200", description = "A response indicating the operation completed successfully with a text message.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/backups/{id}/welcome_email", produces = { "application/json" })
    default Single<HttpResponse<SuccessTextResponse>> getBackupsWelcomeEmail(@Parameter(description = "The backup service ID. Use the `backup_id` from `GET /backups` to identify the service.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Get Backup Order Form Data", operationId = "getNewBackup", description = "Returns available backup storage plans, pricing tiers, and form metadata needed to build an order form. Use the service type IDs from this response when submitting a validation request via `PUT /backups/order` or placing an order via `POST /backups/order`." , tags = {"Backups"})
    @ApiResponse(responseCode = "200", description = "Information needed to generate an order form.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/backups/order", produces = { "application/json" })
    default Single<HttpResponse<BackupsOrder>> getNewBackup() {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Update Backup Information", operationId = "updateBackupInfo", description = "Updates backup storage service metadata, such as stored credentials or settings for the order." , tags = {"Backups"})
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @ApiResponse(responseCode = "200", description = "Default response")
    @Post(value = "/backups/{id}", produces = { "application/json" })
    default Single<HttpResponse<Void>> updateBackupInfo(@Parameter(description = "The backup service ID. Use the `backup_id` from `GET /backups` to identify the service.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Validate Backup Order", operationId = "validateBackupOrder", description = "Validates a backup storage order without placing it, returning calculated pricing and any validation errors. Use this to display a confirmation screen with the final price before submitting the order via `POST /backups/order`." , tags = {"Backups"})
    @ApiResponse(responseCode = "200", description = "Validate Backup Order Response")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Put(value = "/backups/order", produces = { "application/json" }, consumes = {"multipart/form-data", "application/json"})
    default Single<HttpResponse<BackupOrderPutResponse>> validateBackupOrder(@NotNull @Parameter(description = "") @Body(value = "validateOnly")  Boolean validateOnly,@NotNull @Parameter(description = "") @Body(value = "serviceType")  Integer serviceType,@NotNull @Parameter(description = "") @Body(value = "coupon")  String coupon) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Validate Backup Order", operationId = "validateBackupOrder", description = "Validates a backup storage order without placing it, returning calculated pricing and any validation errors. Use this to display a confirmation screen with the final price before submitting the order via `POST /backups/order`." , tags = {"Backups"})
    @ApiResponse(responseCode = "200", description = "Validate Backup Order Response")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Put(value = "/backups/order", produces = { "application/json" }, consumes = {"multipart/form-data", "application/json"})
    default Single<HttpResponse<BackupOrderPutResponse>> validateBackupOrder(@NotNull @Valid @Parameter(description = "") @Body BackupOrderPutRequest body) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }

}
